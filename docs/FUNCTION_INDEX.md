# 𝐊𝐎𝐍𝐆 𝐏𝐚𝐧𝐞𝐥 Function Index

This Markdown index lists the KP procedure library by workflow, so GitHub text search can find commands even when the interactive HTML manual is not open.

For the color-coded manual with search, cross-links, and collapsible control callbacks, open `docs/FUNCTION_BOOK.html` or the GitHub Pages documentation site.

Total entries: 3208

## Featured Utilities

These entries highlight reusable shortcuts, physical constants, and Hamiltonian-construction helpers that are especially useful from the Igor command line or from panel-driven workflows.

### Daily wave/window shortcuts: functions

- `tpw()`: **Topmost Image Wave.** Returns the wave name from the topmost active image or subwindow, so KP commands can operate on the current graph without retyping wave names.
- `di()`: **Smart Image Display.** Displays a wave only when it is not already shown alone in a graph; otherwise it brings the existing graph forward. Use `di2()` for forced redisplay.
- `ed()`: **Smart Table Display.** Table counterpart of `di()`: opens a wave table intelligently and avoids duplicate table windows.
- `grabwin()`: **Find Window by Wave.** Returns the graph/window name containing a given wave, opening the display when needed.
- `grabwinnonew()`: **Find Window Without Opening.** Returns the containing window name without creating a new display, useful for non-invasive checks inside interactive workflows.
- `grabtable()`: **Find Table by Wave.** Finds the table containing a given wave, searching beyond the first graph element where `grabwin()` is not enough.
- `ckfig()`: **Prevent Duplicate Figures.** Call immediately after creating a complex graph to remove or reuse duplicate figure windows; `cklayout()` and `cktable()` provide the same pattern for layouts and tables.
- `wavelistsub()`: **Subwindow Wave List.** Returns wave names from subwindows while ignoring waves in the main window, useful for multi-panel graph management.

### Smart display and 3D workflows: functions

- `d()`: **Compact 2D Displayer.** A one-command image displayer for 2D matrices with KP color controls and formatting hooks.
- `d3d()`: **Smarter 3D Matrix Plotter.** Interactive 3D grid viewer with constant-Z image, integrated spectrum, FFT image, linecut extraction, Z normalization, and FFT-engineering integration.
- `getall3dwave()`: **3D Wave Picker Helper.** Builds the 3D-wave list used by the `d3d()` prompt and related panel buttons.
- `sumoned()`: **Average Z Spectrum.** Sums or averages a 3D grid along one dimension to obtain a representative 1D spectrum, skipping NaN curves in the updated workflow.
- `Normalinecut()`: **Normalize Linecut.** Normalizes a 2D linecut along the x direction for cleaner waterfall and MDC/EDC-style display.
- `Gridtolinecut()`: **Grid to Linecut.** Extracts all spectra from a 3D grid along a selected energy dimension and arranges them as a linecut-style dataset.

### Fourier, filtering, and phase tools: functions

- `f()`: **Smart FFT.** Runs FFT on the first wave in the topmost graph, with handling for arbitrary point counts and NaN-containing data.
- `Const2dfilter()`: **Interactive 2D FFT Filter.** Opens the interactive FFT-filter controller for continuous tuning of the filter window and selected q vector.
- `extendffterealimage()`: **Expand Real-Image FFT.** Expands the half reciprocal-space FFT of a real image into an XY-symmetric image for PR-QPI and related visualization.
- `cyclecolorwave()`: **Cyclic Color Table.** Creates cyclic color waves with a selectable number of periods for phase/cycle visualizations.

### Linecut, spectra, and matrix extraction: functions

- `sEDC()`: **Slice EDC Curves.** Slices a 2D matrix into EDC-style 1D waves for waterfall or spectral display.
- `sMDC()`: **Slice MDC Curves.** Slices a 2D matrix into MDC-style 1D waves for momentum/position-resolved inspection.
- `sum2dlinecut()`: **Integrate Linecut Dimension.** Integrates one dimension of a 2D linecut to produce a representative spectrum or profile.
- `diffeall()`: **Differentiate I/V Batch.** Calculates dI/dV curves from a batch of I/V curves and keeps the result organized for downstream display.
- `padm()`: **Pad 2D Matrix.** Pads a 2D matrix with zeros around the edge and enlarges the matrix dimensions for FFT/filter workflows.

### Drift, strain, and correction workflows: functions

- `ConstLF()`: **Interactive LF Drift Correction.** Opens the Lawler-Fujita drift-correction controller after the required q-vector setup has been prepared.
- `t2dlockin()`: **2D Lock-In Processing.** Runs the two-dimensional lock-in workflow sharing q-vector selection with FFT Engineering.
- `shearall()`: **3D Shear Correction.** Applies shear correction to a 3D matrix using a corrected 2D reference wave, typically the matching topography.
- `cal_strainbyshear()`: **Shear to Strain Estimate.** Calculates lattice mismatch/strain from a shear correction with an arbitrary angle between the lattice and shear axis.
- `correct2Dmapc()`: **Gate-Leak Map Correction.** Shifts gate-dependent dI/dV curves individually and recombines them into a corrected 2D gate map.

### Graph annotation and color helpers: functions

- `color3s_for3dm()`: **Subwindow Color Range.** Calculates a robust symmetric color range for a 3D-viewer subwindow image.
- `Drawarrow()`: **Lattice Direction Arrows.** Draws x/y and a/b lattice-direction arrows on the active graph from an origin, angle, and length.

### Physical Constants: Elementary charge `q0`

\(q_0 = 1.602176634\times10^{-19}\,\mathrm{C}\). KP stores this as `q0` in `root:`.

Use `q0` directly when converting between electron-volts and joules, or when writing charge-based model expressions.

### Physical Constants: Planck constant `h`

\(h = 6.62607015\times10^{-34}\,\mathrm{J\,s}\). KP stores this as `h` in `root:`.

Use `h` together with `q0` or frequency/energy scales in tunneling and model calculations.

### Physical Constants: Conductance quantum `G0`

\(G_0=e^2/h=3.87404586493\times10^{-5}\,\mathrm{S}\). KP stores this as `G0`.

Example: `2*G0` evaluates \(2G_0\) in siemens for quick conductance-scale estimates.

### Physical Constants: Bohr magneton `muB`

\(\mu_B = 9.2740100783\times10^{-24}\,\mathrm{J/T}\). KP stores this as `muB`.

Example: `muB*B/meV` converts the Zeeman energy \(\mu_B B\) from joules to meV.

### Physical Constants: Boltzmann constant `kB`

\(k_B = 1.380649\times10^{-23}\,\mathrm{J/K}\). KP stores this as `kB`.

Example: `5*meV/kB` converts \(5\,\mathrm{meV}\) into a temperature scale in kelvin.

### Physical Constants: Electron-volt `eV`

\(1\,\mathrm{eV}=1.602176634\times10^{-19}\,\mathrm{J}\). KP stores this conversion factor as `eV`.

Use `energy/eV` to convert a joule-valued expression into electron-volts.

### Physical Constants: Millielectron-volt `meV`

\(1\,\mathrm{meV}=1.602176634\times10^{-22}\,\mathrm{J}\). KP stores this conversion factor as `meV`.

Use `energy/meV` for STM/STS gap, Zeeman, and thermal-energy estimates in meV.

### Physical Constants: Electron mass `m0`

\(m_0 = 9.1093837015\times10^{-31}\,\mathrm{kg}\). KP stores this as `m0`.

Use `m0` in effective-mass, dispersion, and model-Hamiltonian calculations.

### Physical Constants: Vacuum permittivity `epslon0`

\(\epsilon_0 = 8.8541878128\times10^{-12}\,\mathrm{F/m}\). KP stores this as `epslon0`.

The historical spelling `epslon0` is preserved so older KP procedures and notebooks keep working.

### Hamiltonian Tools: Pauli-sequence Hamiltonian recipe

Represent a Hamiltonian as \[\begin{aligned} H &= \sum_j c_j\,\bigotimes_{\ell=1}^{N}\sigma_{a_{\ell j}},\\ a_{\ell j} &\in \{0,x,y,z\}. \end{aligned}\] In KP, create one 1x1 parameter wave for each coefficient \(c_j\) with `wT()` or `wC()`, then create a real wave `xyzseq` whose rows label Pauli slots \(\ell\) and whose columns label Hamiltonian terms \(j\).

Use the Pauli code convention \[\begin{aligned}0&\rightarrow\sigma_0,&1&\rightarrow\sigma_x,\\2&\rightarrow\sigma_y,&3&\rightarrow\sigma_z.\end{aligned}\] Call `automatrixT("p1;p2;...", xyzseq)` for the symbolic text Hamiltonian, or `automatrixC("p1;p2;...", xyzseq)` for the complex numerical matrix. For example, a column `{3,1,2}` represents \(\sigma_z\otimes\sigma_x\otimes\sigma_y\), multiplied by the matching coefficient wave.

### Hamiltonian Tools: functions

- `automatrixT()`: **Text Hamiltonian Derivation.** Builds a symbolic/text Hamiltonian matrix from parameter waves and a Pauli-sequence wave. It is useful for deriving and checking Pauli-matrix equations before numerical calculation.
- `automatrixC()`: **Numerical Hamiltonian Matrix.** Builds the complex numerical Hamiltonian matrix from the same parameter-list plus Pauli-sequence representation, returning the final complex matrix for eigenvalue or spectral-function calculations.
- `mpc()`: **Complex Tensor Product.** Computes the matrix tensor product for complex square matrices. Use with `s0()`, `sx()`, `sy()`, and `sz()` when assembling numerical Pauli Hamiltonians.
- `mpt()`: **Text Tensor Product.** Computes the tensor product for text matrices, simplifying zero and one factors so symbolic Hamiltonian expressions stay readable.
- `plust()`: **Text Matrix Sum.** Adds two text matrices element by element and removes explicit zero terms, used by `automatrixT()` to combine Hamiltonian terms.
- `s0()`: **Complex Pauli Identity.** Returns the numerical \(2\times2\) identity matrix, \(\sigma_0\).
- `sx()`: **Complex Pauli X.** Returns the numerical \(2\times2\) Pauli matrix \(\sigma_x\).
- `sy()`: **Complex Pauli Y.** Returns the numerical \(2\times2\) Pauli matrix \(\sigma_y\).
- `sz()`: **Complex Pauli Z.** Returns the numerical \(2\times2\) Pauli matrix \(\sigma_z\).
- `st0()`: **Text Pauli Identity.** Returns the symbolic text identity matrix, \(\sigma_0\).
- `stx()`: **Text Pauli X.** Returns the symbolic text Pauli matrix \(\sigma_x\).
- `sty()`: **Text Pauli Y.** Returns the symbolic text Pauli matrix \(\sigma_y\).
- `stz()`: **Text Pauli Z.** Returns the symbolic text Pauli matrix \(\sigma_z\).

## Start Here: Main Panel, Menus, and Window Entries

### Panel callbacks and control handlers

#### `ButtonProc_mainpanelinitalize`

```igor
Function ButtonProc_mainpanelinitalize(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mainpanelinitalize()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Initialize`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Initialize`, `Initialize`
- Source: `Pierre's Template.ipf:689`


### Interactive procedures and macros

#### `mainpanelinitalize`

```igor
Proc mainpanelinitalize()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Code behavior: reads or updates panel controls. Main internal calls: `Table_Notthesepoints()`, `FFTlist()`, `Mkbad_slices()`, `tabproc_main()`, `Tab4vs6()`.
- Source: `Pierre's Template.ipf:9318`


### Windows, panels, and menus

#### `Kong Panel`

```igor
Menu "Kong Panel"
```
- Usage: defines Igor menu entries; users normally access it from the Igor menu bar.
- Notes: Igor menu definition in `KongPanel.ipf`. Code behavior: loads data from files or paths. Main internal calls: `Kong_Igor_panel()`.
- Source: `KongPanel.ipf:10`

#### `Kong Panel`

```igor
Menu "Kong Panel"
```
- Usage: defines Igor menu entries; users normally access it from the Igor menu bar.
- Notes: Igor menu definition in `Load_KongPanel.ipf`. Code behavior: loads data from files or paths. Main internal calls: `Kong_Igor_panel()`.
- Source: `Load_KongPanel.ipf:10`

#### `Kong_Igor_panel`

```igor
Window Kong_Igor_panel() : Panel
```
- Usage: call `Kong_Igor_panel()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; differentiates wave data. Main internal calls: `KP_EnsureNewColorTables()`, `KP_EnsureStartupGlobals()`, `txt()`, `dat()`, `asc()`, `tabLabel()`.
- Source: `Kong_Igor_panel.ipf:32`



## Nanonis, STM, and STS Loading

### Panel callbacks and control handlers

#### `ButtonProc_2DSTS`

```igor
Function ButtonProc_2DSTS(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Logic of the mat3d wave is follow the order that [0: x] [1: dI/dV] [2: y] IMPORTANT To guarantee the procedure works well, you need Rescale the x and y of slices as (0,a) before do dI/dV(x,y,E) Panel control(s): `2D STS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D STS`
- Source: `Miscellaneous_Codes.ipf:7343`

#### `ButtonProc_autogatemap`

```igor
Function ButtonProc_autogatemap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gatemapauto()`.
- Notes: Auto Gate map Panel control(s): `Auto / GateMp`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto / GateMp`
- Source: `UNISOKU3dscutextract.ipf:1102`

#### `ButtonProc_autoloadgrid`

```igor
Function ButtonProc_autoloadgrid(ctrlName) : ButtonControl
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `autoloadgrid()`.
- Notes: This procedure do autoloading and plot of the GridMap data from nanonis. It can automatically load data and read the dim value of the grid data. The Nanonis loading is handled by KP_LoadNanonisData, so this no longer requires the KM package. Panel control(s): `Auto / GridMp`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto / GridMp`
- Source: `UNISOKU3dscutextract.ipf:826`

#### `ButtonProc_AutoNanislinecut`

```igor
Function ButtonProc_AutoNanislinecut(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `AutoNanislinecut()`.
- Notes: There are several parameters we do not put on the head of procedure, that if you want to change, you need open the code to modify the Kernel. For example, the smooth time for reference normalization; the font size for figure plot, the gap fiting range, the lat Panel control(s): `Auto / Linecut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto / Linecut`
- Source: `UNISOKU3dscutextract.ipf:79`

#### `ButtonProc_autotopo`

```igor
Function ButtonProc_autotopo(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ExtacttopoafterKM()`.
- Notes: Extract Topo after KP loads.sxm/.nsp. The data even number are bwd, and odd number are fwd Panel control(s): `Topo`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Topo`
- Source: `UNISOKU3dscutextract.ipf:1266`

#### `ButtonProc_d3dsimu`

```igor
Function ButtonProc_d3dsimu(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `d3dsimu()`.
- Notes: Special version of smart 3D plot for Simulated 3D matrix Panel control(s): `Plot_{A(k,ω)}`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot_{A(k,ω)}`
- Source: `SmartDisplay.ipf:5124`

#### `ButtonProc_dataplotersts`

```igor
Function ButtonProc_dataplotersts(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `dataplotersts()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `P`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `P`
- Source: `Miscellaneous_Codes.ipf:379`

#### `ButtonProc_displaytopog`

```igor
Function ButtonProc_displaytopog(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `displaytopog()`.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:9279`

#### `ButtonProc_extract3dslinecut`

```igor
Function ButtonProc_extract3dslinecut(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `extract3dslinecut()`.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Reorder 2D / (Grid Linecut)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Reorder 2D / (Grid Linecut)`
- Source: `UNISOKU3dscutextract.ipf:4`

#### `ButtonProc_ExtractSTS`

```igor
Function ButtonProc_ExtractSTS(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `ExtractSTS()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `ExtractSTS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ExtractSTS`
- Source: `Miscellaneous_Codes.ipf:7302`

#### `ButtonProc_ExtractSTSall`

```igor
Function ButtonProc_ExtractSTSall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ExtractallSTS()`.
- Notes: After do dI/dV(E,x,y) and plot 2D STS, you can run Extract sts all to get all the stss from the grid, titled by gridsts# Purpose: extracts values, metadata, cursor information, or derived waves. Panel control(s): `all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `all`
- Source: `Miscellaneous_Codes.ipf:12728`

#### `ButtonProc_getslicerData`

```igor
Function ButtonProc_getslicerData(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Initialize_Global_Variables()`.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `3D to 2Ds / (Grid Map)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3D to 2Ds / (Grid Map)`
- Source: `Miscellaneous_Codes.ipf:9699`

#### `ButtonProc_gridextractpeak`

```igor
Function ButtonProc_gridextractpeak(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gridextractpeak()`.
- Notes: •display peakleft vs onedtopo •print StatsCorrelation(peakright, onedtopo) •print StatsCorrelation(peakleft, onedtopo ) That calculate the correlation coefficient of the two peaks with topography Purpose: extracts values, metadata, cursor information, or derived waves. Panel control(s): `PeakvsGrid`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `PeakvsGrid`
- Source: `Miscellaneous_Codes.ipf:12531`

#### `ButtonProc_Gridtolinecutc`

```igor
Function ButtonProc_Gridtolinecutc(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Usage: triggered as a wrapper that opens/runs `Gridtolinecutc()`.
- Notes: Extract every dI/dV curve from a 3D wave and make a linecut from it. Panel control(s): `It3 to linecut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `It3 to linecut`
- Source: `SmartDisplay.ipf:5689`

#### `ButtonProc_Linenorm`

```igor
Function ButtonProc_Linenorm(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `normstsline()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `LnNorm FitAll`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LnNorm FitAll`
- Source: `Miscellaneous_Codes.ipf:5512`

#### `ButtonProc_map`

```igor
Function ButtonProc_map(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linkstsmap_P()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Linecut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut`
- Source: `Miscellaneous_Codes.ipf:5097`

#### `ButtonProc_mapaa`

```igor
Function ButtonProc_mapaa(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linkstsmap_Paa()`.
- Notes: you want to link them to a linecut Condition: you need to make sure all the slice share same dimdelta Condition: current procedure only support two sets of energy range that the second one is shorter than the first one, this is usually happen when measure tip Panel control(s): `Lc1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Lc1`
- Source: `Miscellaneous_Codes.ipf:10312`

#### `ButtonProc_mapaa2`

```igor
Function ButtonProc_mapaa2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linkstsmap_Paa2()`.
- Notes: you want to link them to a linecut Condition: you need to make sure all the slice share same dimdelta Condition: current procedure only support two sets of energy range that the second one is shorter than the first one, this is usually happen when measure tip Panel control(s): `2`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2`
- Source: `Miscellaneous_Codes.ipf:10404`

#### `ButtonProc_openmat3dslicer`

```igor
Function ButtonProc_openmat3dslicer(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ini_mat3dslicer()`.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Slicer`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Slicer`
- Source: `Pierre's Template.ipf:26186`

#### `ButtonProc_SIMUtopodefectc`

```igor
Function ButtonProc_SIMUtopodefectc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `SIMUtopodefectc()`.
- Notes: Simulate Topological defect Panel control(s): `Topo. Defect`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Topo. Defect`
- Source: `Models.ipf:10414`

#### `ButtonProc_sizemapauto`

```igor
Function ButtonProc_sizemapauto(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `Topo`. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `margin()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Topo`
- Source: `Miscellaneous_Codes.ipf:5318`

#### `ButtonProc_slice1ddpro`

```igor
Function ButtonProc_slice1ddpro(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `slice1ddpro()`.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `2D to 1Ds / (Grid Linecut)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D to 1Ds / (Grid Linecut)`
- Source: `UNISOKU3dscutextract.ipf:23`

#### `ButtonProc_Z_R_Rhomapc`

```igor
Function ButtonProc_Z_R_Rhomapc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Z_R_Rhomapc()`.
- Notes: If the matrix is symmetric in energy, at loading automatically calculate the Z map R map and ρ map, L map Panel control(s): `ZR / ho`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ZR / ho`
- Source: `UNISOKU3dscutextract.ipf:903`

#### `PopMenuProc_stsnormfft`

```igor
Function PopMenuProc_stsnormfft(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `stsnormfft`. Main internal calls: `getsinglestsf()`, `pcsr()`, `qcsr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `stsnormfft`
- Source: `Smart_3D_Viewer_New.ipf:6045`

#### `setvarplotsts`

```igor
Function setvarplotsts(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `plotrawsts()`.
- Notes: Utility function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Data`
- Source: `Miscellaneous_Codes.ipf:419`

#### `SetVarProc_Conshistsigma`

```igor
Function SetVarProc_Conshistsigma(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Sigma mode`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Sigma mode`
- Source: `SmartDisplay.ipf:2290`


### Interactive procedures and macros

#### `autoloadgrid`

```igor
Proc autoloadgrid()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; creates, switches, or removes Igor data folders. Main internal calls: `KP_LoadNanonisData()`, `KP_GetLastLoadedNanonisFolder()`, `Z_R_Rhomap()`, `d3d()`, `KP_NanonisCleanup()`.
- Source: `UNISOKU3dscutextract.ipf:831`

#### `automultiSV`

```igor
proc automultiSV(grid,Kc,Db,deltaGs,deltaGt,tem,xrange,Erange,num,mat)
```
- Usage: run from Igor with parameters `grid`, `Kc`, `Db`, `deltaGs`, `deltaGt`, `tem`, `xrange`, `Erange`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: Grid = Number of data point; Kc = Coherent length (nm); db = Dyne broadenig (meV); deltaGs = Gap Amplitude of sample (meV); deltaGt = Gap Amplitude of tip (meV); tem = Temperature (K). Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `testint()`.
- Parameters:
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `Kc`: Coherent length (nm)
  - `Db`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `deltaGs`: Gap Amplitude of sample (meV)
  - `deltaGt`: Gap Amplitude of tip (meV)
  - `tem`: Temperature (K)
  - `xrange`: Distance from Core center (nm)
  - `Erange`: Energy Range (meV)
  - `num`: Number of cut
  - `mat`: cut name
- Source: `Models.ipf:312`

#### `AutoNanislinecut`

```igor
Proc AutoNanislinecut(FFTp,partial1,partial2,AorB,flag,flag2,flag3)
```
- Usage: run from Igor with parameters `FFTp`, `partial1`, `partial2`, `AorB`, `flag`, `flag2`, `flag3`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Dialog prompts: FFTp = the P number of the FFT peak, where to pick the phase difference; partial1 = the Q Start of interested region of the linecut; partial2 = the Q End of interested region of the linecut; AorB = A or B; flag = Do you want advanced gap analysis (gap extraction & gapsize/Z-height correlation analysis)? Yes[1]/No[0]; / If yes, the fitting parameters need to be adjusted inside the codes; flag2 = Do you want normalized correlation ? Yes[1]/No[0]; / If yes, the smooth number for [Normalization reference curve] need to be adjusted inside the codes. Code behavior: performs Fourier-transform or inverse-transform operations; crea
- Parameters:
  - `FFTp`: the P number of the FFT peak, where to pick the phase difference
  - `partial1`: the Q Start of interested region of the linecut
  - `partial2`: the Q End of interested region of the linecut
  - `AorB`: A or B
  - `flag`: Do you want advanced gap analysis (gap extraction & gapsize/Z-height correlation analysis)? Yes[1]/No[0]; / If yes, the fitting parameters need to be adjusted inside the codes
  - `flag2`: Do you want normalized correlation ? Yes[1]/No[0]; / If yes, the smooth number for [Normalization reference curve] need to be adjusted inside the codes
  - `flag3`: Do you want FFT phase analysis? Yes[1]/No[0]; / If yes, the P value of FFT peak needs to be adjusted inside the codes
- Source: `UNISOKU3dscutextract.ipf:93`

#### `Batchscaling`

```igor
proc Batchscaling(xmin,xmax,tem,po_s,po_delta,num,grid)
```
- Usage: run from Igor with parameters `xmin`, `xmax`, `tem`, `po_s`, `po_delta`, `num`, `grid`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `calGSpo()`.
- Parameters:
  - `xmin`: gamma
  - `xmax`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `tem`: Temperature parameter, usually in kelvin when used in spectral modeling.
  - `po_s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `po_delta`: Step size or energy/voltage increment used by the calculation.
  - `num`: how many posion data curves
  - `grid`: the point number for integration
- Source: `Models.ipf:482`

#### `Batchscalingoure`

```igor
proc Batchscalingoure(xmin,xmax,ymin,ymax,po,grid)
```
- Usage: run from Igor with parameters `xmin`, `xmax`, `ymin`, `ymax`, `po`, `grid`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `calGS()`.
- Parameters:
  - `xmin`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `xmax`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `ymin`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `ymax`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `po`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Models.ipf:399`

#### `d3dsimu`

```igor
Proc d3dsimu(mat3dn,zn)
```
- Usage: run from Igor with parameters `mat3dn`, `zn`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: mat3dn = Name of the 3D simulation matrix; zn = Index of the Energy. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `getall3dwave()`, `sumoned()`, `di2lf()`, `func_zeroNaN()`, `color3s_for3d()`, `grabwin()`.
- Parameters:
  - `mat3dn`: Name of the 3D simulation matrix
  - `zn`: Index of the Energy
- Source: `SmartDisplay.ipf:5128`

#### `displaytopog`

```igor
proc displaytopog(name,size)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `size`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `size`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:9283`

#### `ExtacttopoafterKM`

```igor
Proc ExtacttopoafterKM()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `DataFolderList()`, `KP_LoadNanonisData()`.
- Source: `UNISOKU3dscutextract.ipf:1271`

#### `extract3dslinecut`

```igor
Proc extract3dslinecut(name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: name = Data name from nanonis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Data name from nanonis
- Source: `UNISOKU3dscutextract.ipf:9`

#### `extractallsts`

```igor
proc extractallsts(ptotal, qtotal, name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `ptotal`, `qtotal`, `name`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `ExtractSTS2()`.
- Parameters:
  - `ptotal`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `qtotal`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:12733`

#### `ExtractSTS`

```igor
Proc ExtractSTS(xpoint,ypoint,name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `xpoint`, `ypoint`, `name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: xpoint = P; ypoint = Q; name = Name of Map. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `pcsr()`, `qcsr()`.
- Parameters:
  - `xpoint`: P
  - `ypoint`: Q
  - `name`: Name of Map
- Source: `Miscellaneous_Codes.ipf:7306`

#### `extrastsfrommap3d`

```igor
proc extrastsfrommap3d(sizex,sizey,from,to)
```
- Usage: run from Igor with parameters `sizex`, `sizey`, `from`, `to`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: sizex = How many points Nx; sizey = How many points Ny; from = Start Energy value for search; to = End Energy value for search. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `extractgapbymax()`.
- Parameters:
  - `sizex`: How many points Nx
  - `sizey`: How many points Ny
  - `from`: Start Energy value for search
  - `to`: End Energy value for search
- Source: `Miscellaneous_Codes.ipf:8278`

#### `extrastsfrommap3dGuassian`

```igor
proc extrastsfrommap3dGuassian(sizex,sizey,from,to)
```
- Usage: run from Igor with parameters `sizex`, `sizey`, `from`, `to`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: sizex = How many points Nx; sizey = How many points Ny; from = Start Energy value for search; to = End Energy value for search. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `extractgapbymaxGuassian()`.
- Parameters:
  - `sizex`: How many points Nx
  - `sizey`: How many points Ny
  - `from`: Start Energy value for search
  - `to`: End Energy value for search
- Source: `Miscellaneous_Codes.ipf:8388`

#### `extrastsfrommap3dGuassian2`

```igor
proc extrastsfrommap3dGuassian2(sizex,sizey,froml,tol,fromr,tor)
```
- Usage: run from Igor with parameters `sizex`, `sizey`, `froml`, `tol`, `fromr`, `tor`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Dialog prompts: sizex = How many points Nx; sizey = How many points Ny; froml = Start -Energy value for search; tol = End -Energy value for search; fromr = Start +Energy value for search; tor = End +Energy value for search. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `extractgapbymaxGuassian2()`.
- Parameters:
  - `sizex`: How many points Nx
  - `sizey`: How many points Ny
  - `froml`: Start -Energy value for search
  - `tol`: End -Energy value for search
  - `fromr`: Start +Energy value for search
  - `tor`: End +Energy value for search
- Source: `Miscellaneous_Codes.ipf:8526`

#### `extrastsfrommap3dGuassian3`

```igor
proc extrastsfrommap3dGuassian3(indicate,froml,tol,fromr,tor,flag)
```
- Usage: run from Igor with parameters `indicate`, `froml`, `tol`, `fromr`, `tor`, `flag`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: indicate = Notice!!! Before Launch; froml = Start -Energy value for search; tol = End -Energy value for search; fromr = Start +Energy value for search; tor = End +Energy value for search; flag = If you want individually display gapfit, input '1'. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`, `extractgapbymaxGuassian3()`.
- Parameters:
  - `indicate`: Notice!!! Before Launch
  - `froml`: Start -Energy value for search
  - `tol`: End -Energy value for search
  - `fromr`: Start +Energy value for search
  - `tor`: End +Energy value for search
  - `flag`: If you want individually display gapfit, input '1'
- Source: `Miscellaneous_Codes.ipf:8784`

#### `ffc3dpls`

```igor
Proc ffc3dpls(topo)
```
- Usage: run from Igor with parameters `topo`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Dialog prompts: topo = Name of Topograph (The wave from which lattice location can be extracted). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall2dwave()`, `ff3dpls()`, `grabwinnonew()`.
- Parameters:
  - `topo`: Name of Topograph (The wave from which lattice location can be extracted)
- Source: `Smart_3D_Viewer_New.ipf:9187`

#### `gatemapauto`

```igor
Proc gatemapauto(x,y,gate, evenornot,Interpnum)
```
- Usage: run from Igor with parameters `x`, `y`, `gate`, `evenornot`, `Interpnum`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; symmetry or reflection processing; linecut, slice, or region extraction. Dialog prompts: x = Column Number of x wave; y = Column Number of y wave; gate = Column number for gate; evenornot = Gate even spacing?; Interpnum = If gate is not even spacing, how many points you want to interpolate. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `Makegraphtable()`, `LoadDataFromNewmkgate()`, `Initialize_Global_Variables()`, `linkstsmapgate()`, `rescalexmultiwhat()`, `normrange()`.
- Parameters:
  - `x`: Column Number of x wave
  - `y`: Column Number of y wave
  - `gate`: Column number for gate
  - `evenornot`: Gate even spacing?
  - `Interpnum`: If gate is not even spacing, how many points you want to interpolate
- Source: `UNISOKU3dscutextract.ipf:1106`

#### `gatemapextractpeak`

```igor
Proc gatemapextractpeak(number, right1,right2,left1,left2,vl,vh)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `number`, `right1`, `right2`, `left1`, `left2`, `vl`, `vh`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: number = How many gate slices; right1 = right peak_small point; right2 = right peak_large point; left1 = left peak_small point; left2 = left peak_large point; vl = small gate voltage. Code behavior: assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `secondDall2()`, `autoclbzerogateuse()`, `renameall()`, `linkstsmap_P()`.
- Parameters:
  - `number`: How many gate slices
  - `right1`: right peak_small point
  - `right2`: right peak_large point
  - `left1`: left peak_small point
  - `left2`: left peak_large point
  - `vl`: small gate voltage
  - `vh`: large gate voltage
- Source: `Miscellaneous_Codes.ipf:11966`

#### `gridextractpeak`

```igor
Proc gridextractpeak(start,delta,right1,right2,left1,left2)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `start`, `delta`, `right1`, `right2`, `left1`, `left2`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: start = (meV) The energy of first slice; delta = (meV) energy step of slice set; right1 = right peak_small point; right2 = right peak_large point; left1 = left peak_small point; left2 = left peak_large point. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `mapforSTM()`, `makeextra()`, `extractallsts()`, `secondDall2()`, `autoclbzerogateuse()`, `renameall()`.
- Parameters:
  - `start`: (meV) The energy of first slice
  - `delta`: (meV) energy step of slice set
  - `right1`: right peak_small point
  - `right2`: right peak_large point
  - `left1`: left peak_small point
  - `left2`: left peak_large point
- Source: `Miscellaneous_Codes.ipf:12537`

#### `Gridtolinecutc`

```igor
Proc Gridtolinecutc(grid,indexdim)
```
- Usage: run from Igor with parameters `grid`, `indexdim`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: grid = Name of the 3Dwave; indexdim = Energy dimension index. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `Gridtolinecut()`.
- Parameters:
  - `grid`: Name of the 3Dwave
  - `indexdim`: Energy dimension index
- Source: `SmartDisplay.ipf:5694`

#### `indivlinecut`

```igor
proc indivlinecut(grid,Kc,Db,deltaGs,deltaGt,tem,xrange,Erange,rv)//
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `grid`, `Kc`, `Db`, `deltaGs`, `deltaGt`, `tem`, `xrange`, `Erange`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: Grid = Number of data point; Kc = Coherent length (nm); db = Dyne broadenig (meV); deltaGs = Gap Amplitude of sample (meV); deltaGt = Gap Amplitude of tip (meV); tem = Temperature (K). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; differentiates wave data; asks the user for parameters through Igor prompts. Main internal calls: `integrate1d()`.
- Parameters:
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `Kc`: Coherent length (nm)
  - `Db`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `deltaGs`: Gap Amplitude of sample (meV)
  - `deltaGt`: Gap Amplitude of tip (meV)
  - `tem`: Temperature (K)
  - `xrange`: Distance from Core center (nm)
  - `Erange`: Energy Range (meV)
  - `rv`: Position from vortex (nm)
- Source: `Models.ipf:353`

#### `ini_mat3dslicer`

```igor
Proc ini_mat3dslicer()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:31293`

#### `initialg`

```igor
Proc initialg()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Source: `UNISOKU3dscutextract.ipf:84`

#### `initialgl`

```igor
Proc initialgl()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Source: `UNISOKU3dscutextract.ipf:88`

#### `interpsts`

```igor
proc interpsts(name,num,factor)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `name`, `num`, `factor`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: num = Total number of STSs; factor = Interpolate how many times?. Code behavior: duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `displaymulti_factor()`, `linkstsmap_factor()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Total number of STSs
  - `factor`: Interpolate how many times?
- Source: `Miscellaneous_Codes.ipf:5664`

#### `interpsts00`

```igor
proc interpsts00(name,num,factor,mapname)
```
- Usage: run from Igor with parameters `name`, `num`, `factor`, `mapname`.
- Notes: interpsts00("dest",number,3,mapname) Purpose: resamples, rescales, pads, or changes wave dimensions. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `linkstsmap_factor00()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
  - `mapname`: Igor wave or matrix input consumed by the calculation.
- Source: `Miscellaneous_Codes.ipf:9124`

#### `interpsts2`

```igor
proc interpsts2(name,num,factor,haha,start)
```
- Usage: run from Igor with parameters `name`, `num`, `factor`, `haha`, `start`.
- Notes: interpolate start selectable map Purpose: resamples, rescales, pads, or changes wave dimensions. Dialog prompts: num = Total number of STSs; factor = Interpolate how many times?; haha = rename to what. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `linkstsmap_factor2()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Total number of STSs
  - `factor`: Interpolate how many times?
  - `haha`: rename to what
  - `start`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:7081`

#### `linkstsmap_P`

```igor
proc linkstsmap_P(name,startnum,num)
```
- Usage: run from Igor with parameters `name`, `startnum`, `num`.
- Notes: i+=1 while(i<num) setscale/P x, dimoffset(sts1,0),dimdelta(sts1,0),""mapsts end Purpose: combines many waves/slices into a map, linecut, or averaged output. Dialog prompts: name = Batch name; num = Total number of STSs; startnum = Make map start from which STS. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `linkstsmap()`.
- Parameters:
  - `name`: Batch name
  - `startnum`: Make map start from which STS
  - `num`: Total number of STSs
- Source: `Miscellaneous_Codes.ipf:5572`

#### `linkstsmap_P2`

```igor
proc linkstsmap_P2(name,num,haha,start)
```
- Usage: run from Igor with parameters `name`, `num`, `haha`, `start`.
- Notes: Single start selectable map Purpose: combines many waves/slices into a map, linecut, or averaged output. Dialog prompts: name = Batch name; num = Total number of STSs. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `linkstsmap2()`.
- Parameters:
  - `name`: Batch name
  - `num`: Total number of STSs
  - `haha`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `start`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:7037`

#### `linkstsmap_Paa`

```igor
proc linkstsmap_Paa(name,startnum,num,numc)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `startnum`, `num`, `numc`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: name = Batch name; num = Total number of STSs; startnum = Make map start from which STS; numc = STS number for shrink range happens. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `linkstsmapaa()`.
- Parameters:
  - `name`: Batch name
  - `startnum`: Make map start from which STS
  - `num`: Total number of STSs
  - `numc`: STS number for shrink range happens
- Source: `Miscellaneous_Codes.ipf:10317`

#### `linkstsmap_Paa2`

```igor
proc linkstsmap_Paa2(name,startnum,num,numc,numc2)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `startnum`, `num`, `numc`, `numc2`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: name = Batch name; num = Total number of STSs; startnum = Make map start from which STS; numc = STS number for shrink range happens (1st); numc2 = STS number for shrink range happens (2nd). Code behavior: creates output waves; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `linkstsmapaa2()`.
- Parameters:
  - `name`: Batch name
  - `startnum`: Make map start from which STS
  - `num`: Total number of STSs
  - `numc`: STS number for shrink range happens (1st)
  - `numc2`: STS number for shrink range happens (2nd)
- Source: `Miscellaneous_Codes.ipf:10409`

#### `linkstsmap_Pev`

```igor
proc linkstsmap_Pev(name,startnum,num)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `startnum`, `num`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Dialog prompts: name = Batch name; num = Total number of STSs; startnum = Make map start from which STS. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `linkstsmapev()`.
- Parameters:
  - `name`: Batch name
  - `startnum`: Make map start from which STS
  - `num`: Total number of STSs
- Source: `Miscellaneous_Codes.ipf:10591`

#### `linkstsmap_PqpiH`

```igor
proc linkstsmap_PqpiH(name,num)
```
- Usage: run from Igor with parameters `name`, `num`.
- Notes: Ii2+=1 while(Ii<dimsize(Hlinecut,1)) displaymulti("stsexH",1,dimsize(Hlinecut,1)) end Purpose: combines many waves/slices into a map, linecut, or averaged output. Main internal calls: `linkstsmapH()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:7853`

#### `linkstsmap_PqpiV`

```igor
Proc linkstsmap_PqpiV(name,num)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `num`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis. Main internal calls: `linkstsmapV()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:7884`

#### `LoadDataFromNewmkgate`

```igor
Proc LoadDataFromNewmkgate(x,y,gate)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `x`, `y`, `gate`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; loads data from files or paths; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`.
- Parameters:
  - `x`: Used in: LoadWave/G/D/A=load/K=1/L={0,0,0,x,1}/o/P=$PATHName Filename
  - `y`: y=1: current// y=8: dI/dV
  - `gate`: Used in: LoadWave/G/D/A=load/K=1/L={0,0,0,gate,1}/o/P=$PATHName Filename
- Source: `UNISOKU3dscutextract.ipf:1155`

#### `normstsline`

```igor
proc normstsline(mat)/////delete a line fitted all range
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: Mat = Name of the Batch. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:5517`

#### `openmat3dslicer`

```igor
Proc openmat3dslicer()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`, `Mat3d_slicer()`, `Set_Vline_slice_control()`.
- Source: `Pierre's Template.ipf:28383`

#### `plotrawsts`

```igor
Proc plotrawsts()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Miscellaneous_Codes.ipf:431`

#### `slice1ddpro`

```igor
Proc slice1ddpro(matt)
```
- Usage: run from Igor with parameters `matt`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `slice1dd()`.
- Parameters:
  - `matt`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `UNISOKU3dscutextract.ipf:28`

#### `STSev`

```igor
proc STSev(name,num)
```
- Notes: Input the name of a sts batch, convert the x scale from wavelength (nm) to Energy (eV), This procedure works on raw sts Usage: run from Igor with parameters `name`, `num`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations. Main internal calls: `displaymulti()`.
- Parameters:
  - `name`: sts name
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:10639`

#### `stsnorm`

```igor
Proc stsnorm(name)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:5106`

#### `testint`

```igor
proc testint(grid,Kc,Db,deltaGs,deltaGt,tem,xrange,Erange,rv)//
```
- Usage: run from Igor with parameters `grid`, `Kc`, `Db`, `deltaGs`, `deltaGt`, `tem`, `xrange`, `Erange`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; differentiates wave data. Main internal calls: `integrate1d()`.
- Parameters:
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `Kc`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Db`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `deltaGs`: Step size or energy/voltage increment used by the calculation.
  - `deltaGt`: Step size or energy/voltage increment used by the calculation.
  - `tem`: Temperature parameter, usually in kelvin when used in spectral modeling.
  - `xrange`: nm
  - `Erange`: meV
  - `rv`: nm spectrum
- Source: `Models.ipf:285`

#### `white2transparant`

```igor
Proc white2transparant(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Pierre's Template.ipf:22382`

#### `Z_R_Rhomapc`

```igor
Proc Z_R_Rhomapc(wg,wi,zn_consl)
```
- Usage: run from Igor with parameters `wg`, `wi`, `zn_consl`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: wg = Name of G matrix (3D); wi = Name of I matrix (3D); zn_consl = Index of Energy. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `Z_R_Rhomap()`.
- Parameters:
  - `wg`: Name of G matrix (3D)
  - `wi`: Name of I matrix (3D)
  - `zn_consl`: Index of Energy
- Source: `UNISOKU3dscutextract.ipf:907`


### Computational and data helpers

#### `calGS`

```igor
Function calGS(xmin,xmax,ymin,ymax,po,grid)
```
- Usage: call as `calGS(xmin, xmax, ymin, ymax, po, grid)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Integrate1D()`.
- Parameters:
  - `xmin`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `xmax`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `ymin`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `ymax`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `po`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Models.ipf:415`

#### `calGSpo`

```igor
Function calGSpo(xmin,xmax,tem,po,grid)
```
- Usage: call as `calGSpo(xmin, xmax, tem, po, grid)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `Integrate1D()`.
- Parameters:
  - `xmin`: gamma
  - `xmax`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `tem`: Temperature parameter, usually in kelvin when used in spectral modeling.
  - `po`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Models.ipf:506`

#### `color3s_followtopo`

```igor
Function color3s_followtopo(name,tt,topo)
```
- Notes: V.Auxiliary Functions Usage: call as `color3s_followtopo(name, tt, topo)`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `topo`: Topography wave or topography-derived reference data.
- Source: `Lattice Segregation.ipf:1509`

#### `color3s_followtopo2`

```igor
Function color3s_followtopo2(name,tt,topo)
```
- Usage: call as `color3s_followtopo2(name, tt, topo)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild2()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `topo`: Topography wave or topography-derived reference data.
- Source: `Lattice Segregation.ipf:1533`

#### `color3s_followtopof`

```igor
Function color3s_followtopof(name,tt,topo)
```
- Usage: call as `color3s_followtopof(name, tt, topo)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `topo`: Topography wave or topography-derived reference data.
- Source: `Lattice Segregation.ipf:1557`

#### `ExtractSTS2`

```igor
Function ExtractSTS2(xpoint,ypoint,name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `ExtractSTS2(xpoint, ypoint, name)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `xpoint`: =pcsr(A)
  - `ypoint`: =qcsr(A)
  - `name`: ="mat3d"
- Source: `Miscellaneous_Codes.ipf:12768`

#### `func_give_area1`

```igor
Function func_give_area1(namex,namey,surfx_x,surfx_y,griddelta,lattice)//extrapolates the curve on the x-grid and y-grid
```
- Usage: call as `func_give_area1(namex, namey, surfx_x, surfx_y, griddelta, lattice)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `namex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `namey`: String name used to locate an Igor wave, graph, folder, or output object.
  - `surfx_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfx_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:7159`

#### `func_give_area2`

```igor
Function func_give_area2(surfx_x,surfx_y,inside_out,sizegrid,griddelta,lattice)//calculates the area
```
- Usage: call as `func_give_area2(surfx_x, surfx_y, inside_out, sizegrid, griddelta, lattice)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `surfx_x`: Used in: Make/O/N=(3*dimsize(surfx_x,0)) tempx
  - `surfx_y`: Used in: Make/O/N=(3*dimsize(surfx_y,0)) tempy
  - `inside_out`: Step size or energy/voltage increment used by the calculation.
  - `sizegrid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:7215`

#### `func_give_area2_old`

```igor
Function func_give_area2_old(surfx_x,surfx_y,sizegrid,griddelta,lattice)
```
- Usage: call as `func_give_area2_old(surfx_x, surfx_y, sizegrid, griddelta, lattice)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `surfx_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfx_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizegrid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:7446`

#### `func_homework5a`

```igor
Function func_homework5a(namex,namey,surfx_x,surfx_y,surfy_x,surfy_y,xnoise,ynoise,sizegrid,thenumber,griddelta,lattice)
```
- Usage: call as `func_homework5a(namex, namey, surfx_x, surfx_y, surfy_x, surfy_y, xnoise, ynoise)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `namex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `namey`: String name used to locate an Igor wave, graph, folder, or output object.
  - `surfx_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfx_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfy_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfy_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xnoise`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ynoise`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizegrid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `thenumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - ... 2 additional parameters
- Source: `Pierre's Template.ipf:21241`

#### `func_vol_lac0`

```igor
Function func_vol_lac0(namex,namey)//extrapolates the curve on the x-grid and y-grid
```
- Usage: call as `func_vol_lac0(namex, namey)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `namex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `namey`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Pierre's Template.ipf:21413`

#### `func_vol_lac1`

```igor
Function func_vol_lac1(namex,namey,surfx_x,surfx_y,griddelta,lattice)//extrapolates the curve on the x-grid and y-grid
```
- Usage: call as `func_vol_lac1(namex, namey, surfx_x, surfx_y, griddelta, lattice)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `namex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `namey`: String name used to locate an Igor wave, graph, folder, or output object.
  - `surfx_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfx_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21427`

#### `func_vol_lac2`

```igor
Function func_vol_lac2(surfx_x,surfx_y,inside_out,sizegrid,griddelta,lattice)//calculates the area
```
- Usage: call as `func_vol_lac2(surfx_x, surfx_y, inside_out, sizegrid, griddelta, lattice)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `surfx_x`: Used in: Make/O/N=(3*dimsize(surfx_x,0)) tempx
  - `surfx_y`: Used in: Make/O/N=(3*dimsize(surfx_y,0)) tempy
  - `inside_out`: Step size or energy/voltage increment used by the calculation.
  - `sizegrid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21485`

#### `func_vol_lac3`

```igor
Function func_vol_lac3(tsurfx_x,tsurfx_y,sizegrid,griddelta,lattice,px,py,pdeep,inside_out)
```
- Usage: call as `func_vol_lac3(tsurfx_x, tsurfx_y, sizegrid, griddelta, lattice, px, py, pdeep)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `tsurfx_x`: Used in: Duplicate/O tsurfx_x sq_distance2
  - `tsurfx_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizegrid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `px`: Used in: Duplicate/O px mxtemp
  - `py`: Used in: Duplicate/O py mytemp
  - `pdeep`: Step size or energy/voltage increment used by the calculation.
  - `inside_out`: Step size or energy/voltage increment used by the calculation.
- Source: `Pierre's Template.ipf:21580`

#### `getsinglests`

```igor
Function getsinglests(name,pp,qq,zn)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getsinglests(name, pp, qq, zn)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `mean()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `pp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qq`: Momentum or Q-vector coordinate/index.
  - `zn`: Layer/index value selecting a slice of a 3D wave or energy stack. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:299`

#### `getsinglestsf`

```igor
Function getsinglestsf(name,pp,qq,zn)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getsinglestsf(name, pp, qq, zn)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `autoremovejump1D()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `pp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qq`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `zn`: Layer/index value selecting a slice of a 3D wave or energy stack. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:6096`

#### `Gridtolinecut`

```igor
Function Gridtolinecut(grid,indexdim)
```
- Usage: call as `Gridtolinecut(grid, indexdim)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`. Update log: 2025-05-19: Extracts all dI/dV spectra from a 3D grid wave and organizes them as a linecut for downstream plotting or analysis.
- Parameters:
  - `grid`: 3Dwave
  - `indexdim`: the dimensional index of energy
- Source: `SmartDisplay.ipf:5704`

#### `KP_GetLastLoadedNanonisFolder`

```igor
Function/S KP_GetLastLoadedNanonisFolder()
```
- Purpose: returns the most recently created KP Nanonis data folder.
- Usage: call `KP_GetLastLoadedNanonisFolder()` from Igor procedure code or the command line.
- Notes: String-returning helper for Nanonis/STM data loading or map conversion. Main internal calls: `SVAR_Exists()`, `DataFolderList()`.
- Source: `KP_NanonisLoaders.ipf:118`

#### `KP_LoadNanonis3ds`

```igor
Function/WAVE KP_LoadNanonis3ds(pathStr)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3ds(pathStr)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; Igor wave/matrix/cube data operation. Code behavior: creates, switches, or removes Igor data folders. Main internal calls: `KP_LoadNanonis3dsGetHeader()`, `KP_LoadNanonis3dsGetData()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
- Source: `KP_NanonisLoaders.ipf:144`

#### `KP_LoadNanonis3dsCopyMLSBias`

```igor
Function KP_LoadNanonis3dsCopyMLSBias(srcw, destw)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsCopyMLSBias(srcw, destw)`.
- Notes: Function for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `GetDimLabel()`.
- Parameters:
  - `srcw`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `destw`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `KP_NanonisLoaders.ipf:362`

#### `KP_LoadNanonis3dsGetMLSBias`

```igor
Function/WAVE KP_LoadNanonis3dsGetMLSBias(srcw, dim)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsGetMLSBias(srcw, dim)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `str2num()`, `GetDimLabel()`.
- Parameters:
  - `srcw`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `dim`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:341`

#### `KP_LoadNanonis3dsIsMLSBias`

```igor
Function KP_LoadNanonis3dsIsMLSBias(w)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsIsMLSBias(w)`.
- Notes: Function for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `GetDimLabel()`.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `KP_NanonisLoaders.ipf:357`

#### `KP_LoadNanonisData`

```igor
Function/WAVE KP_LoadNanonisData(pathStr)
```
- Usage: call as `KP_LoadNanonisData(pathStr)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction. Code behavior: creates, switches, or removes Igor data folders. Main internal calls: `KP_LoadNanonisFolder()`, `KP_UniqueRootDataFolderName()`, `KP_RecordLoadedFile()`, `KP_LoadNanonis3ds()`, `KP_LoadNanonisSxmNsp()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
- Source: `KP_NanonisLoaders.ipf:12`

#### `KP_LoadNanonisFolder`

```igor
Function/WAVE KP_LoadNanonisFolder(pathStr)
```
- Purpose: walks a folder and loads supported Nanonis files through the KP-local loader.
- Usage: call as `KP_LoadNanonisFolder(pathStr)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion. Code behavior: loads data from files or paths. Main internal calls: `IndexedDir()`, `KP_LoadNanonisData()`, `IndexedFile()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
- Source: `KP_NanonisLoaders.ipf:80`

#### `KP_LoadNanonisSxmNsp`

```igor
Function/WAVE KP_LoadNanonisSxmNsp(pathStr)
```
- Purpose: parses Nanonis sxm/nsp topography-style files and returns loaded waves.
- Usage: call as `KP_LoadNanonisSxmNsp(pathStr)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; Igor wave/matrix/cube data operation. Code behavior: creates, switches, or removes Igor data folders. Main internal calls: `KP_LoadNanonisSxmNspGetHeader()`, `KP_LoadNanonisSXMGetData()`, `elseif()`, `KP_LoadNanonisNSPGetData()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
- Source: `KP_NanonisLoaders.ipf:371`

#### `KP_NanonisCleanup`

```igor
Function KP_NanonisCleanup()
```
- Purpose: cleanup hook replacing the old KM exit call; KP's local loader has no external package to unload.
- Usage: call `KP_NanonisCleanup()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion.
- Source: `KP_NanonisLoaders.ipf:139`

#### `KP_NanonisCommonConversion`

```igor
Function KP_NanonisCommonConversion(w, [driveamp, modulated])
```
- Usage: call as `KP_NanonisCommonConversion(w, [driveamp, modulated])`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output. Main internal calls: `GrepString()`, `numtype()`, `elseif()`.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `[driveamp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `modulated]`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:696`

#### `KP_NanonisCommonDataAvg`

```igor
Function/WAVE KP_NanonisCommonDataAvg(bwdStr)
```
- Usage: call as `KP_NanonisCommonDataAvg(bwdStr)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `GrepString()`, `numpnts()`.
- Parameters:
  - `bwdStr`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `KP_NanonisLoaders.ipf:726`

#### `KP_NanonisCommonGetHeader`

```igor
Function KP_NanonisCommonGetHeader(pathStr)
```
- Usage: call as `KP_NanonisCommonGetHeader(pathStr)`.
- Notes: Function for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; loads data from files or paths; writes or exports data; creates, switches, or removes Igor data folders. Main internal calls: `str2num()`, `KP_NanonisCommonVariableString()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
- Source: `KP_NanonisLoaders.ipf:645`

#### `KP_NanonisCommonVariableString`

```igor
Function KP_NanonisCommonVariableString(name, value)
```
- Usage: call as `KP_NanonisCommonVariableString(name, value)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction. Main internal calls: `char2num()`, `RemoveEnding()`, `GrepString()`, `str2num()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `value`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:750`

#### `KP_RecordLoadedFile`

```igor
Function KP_RecordLoadedFile(fileName)
```
- Purpose: records loaded file names in `root:File_Name` for downstream KP workflows.
- Usage: call as `KP_RecordLoadedFile(fileName)`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; reshapes existing waves.
- Parameters:
  - `fileName`: File name or file-derived label used by the loader/export routine.
- Source: `KP_NanonisLoaders.ipf:126`

#### `KP_UniqueRootDataFolderName`

```igor
Function/S KP_UniqueRootDataFolderName(baseName)
```
- Usage: call as `KP_UniqueRootDataFolderName(baseName)`.
- Notes: String-returning helper for Nanonis/STM data loading or map conversion.
- Parameters:
  - `baseName`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `KP_NanonisLoaders.ipf:104`

#### `linkstsmap`

```igor
Function linkstsmap(name,num,startnum)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmap(name, num, startnum)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:5585`

#### `linkstsmap2`

```igor
function linkstsmap2(name,num,haha,start)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmap2(name, num, haha, start)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `haha`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `start`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:7047`

#### `linkstsmap_factor`

```igor
Function linkstsmap_factor(name,num,factor)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmap_factor(name, num, factor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:5619`

#### `linkstsmap_factor00`

```igor
function linkstsmap_factor00(name,num,factor,mapname)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmap_factor00(name, num, factor, mapname)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
  - `mapname`: Igor wave or matrix input consumed by the calculation.
- Source: `Miscellaneous_Codes.ipf:9097`

#### `linkstsmap_factor2`

```igor
function linkstsmap_factor2(name,num,factor,start)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmap_factor2(name, num, factor, start)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
  - `start`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:7122`

#### `linkstsmapaa`

```igor
Function linkstsmapaa(name,num,startnum,numc)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapaa(name, num, startnum, numc)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
  - `numc`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:10333`

#### `linkstsmapaa2`

```igor
Function linkstsmapaa2(name,num,startnum,numc,numc2)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapaa2(name, num, startnum, numc, numc2)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
  - `numc`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `numc2`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:10427`

#### `linkstsmapev`

```igor
Function linkstsmapev(name,num,startnum)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapev(name, num, startnum)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:10600`

#### `linkstsmapgate`

```igor
Function linkstsmapgate(name,num,startnum)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapgate(name, num, startnum)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
- Source: `UNISOKU3dscutextract.ipf:1210`

#### `linkstsmapgl`

```igor
Function linkstsmapgl(name,num,startnum)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapgl(name, num, startnum)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
- Source: `UNISOKU3dscutextract.ipf:1235`

#### `linkstsmapH`

```igor
Function linkstsmapH(name,num)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapH(name, num)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:7859`

#### `linkstsmapno`

```igor
Function linkstsmapno(name,num,startnum)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapno(name, num, startnum)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range.
- Source: `Models.ipf:4485`

#### `linkstsmapV`

```igor
Function linkstsmapV(name,num)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkstsmapV(name, num)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:7890`

#### `Matrixtranspose_off`

```igor
Function Matrixtranspose_off(name)
```
- Usage: call as `Matrixtranspose_off(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Symmetrization.ipf:428`

#### `Selnumavests_n`

```igor
Function Selnumavests_n(name,prefwave,thresh,stsname)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `Selnumavests_n(name, prefwave, thresh, stsname)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: 3D wave the STS come from
  - `prefwave`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `thresh`: threshold for defining a lattice position (max=1)
  - `stsname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Smart_3D_Viewer_New.ipf:10220`

#### `Selnumavests_p`

```igor
Function Selnumavests_p(name,prefwave,thresh,stsname)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `Selnumavests_p(name, prefwave, thresh, stsname)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: 3D wave the STS come from
  - `prefwave`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `thresh`: threshold for defining a lattice position (max=1)
  - `stsname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Smart_3D_Viewer_New.ipf:10170`

#### `slice1dd`

```igor
Function slice1dd(matt)
```
- Usage: call as `slice1dd(matt)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `matt`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `UNISOKU3dscutextract.ipf:33`

#### `stsnormlize`

```igor
Function stsnormlize(namehaha,start)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `stsnormlize(namehaha, start)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation.
- Parameters:
  - `namehaha`: String name used to locate an Igor wave, graph, folder, or output object.
  - `start`: Start point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:5124`

#### `stsnormlizesingle`

```igor
Function stsnormlizesingle(namehaha,normhaha)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `stsnormlizesingle(namehaha, normhaha)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `namehaha`: String name used to locate an Igor wave, graph, folder, or output object.
  - `normhaha`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5146`

#### `wavelistsub`

```igor
Function/S wavelistsub(name)
```
- Usage: call as `wavelistsub(name)`.
- Notes: #special checkFig in childen window, [only in child window] Purpose: combines many waves/slices into a map, linecut, or averaged output. Main internal calls: `childWindowList()`. Update log: 08/25/2023: Added new function wavelistsub(name) to get wave list from all the subwindow, only in subwindow, the wave in the main window will not be accounted.
- Parameters:
  - `name`: graph name
- Source: `SmartDisplay.ipf:39`

#### `Z_R_Rhomap`

```igor
Function Z_R_Rhomap(wg,wi,zn_consl)
```
- Usage: call as `Z_R_Rhomap(wg, wi, zn_consl)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; linecut, slice, or region extraction. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `gammaNoise()`.
- Parameters:
  - `wg`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `wi`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `zn_consl`: Layer/index value selecting a slice of a 3D wave or energy stack. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `UNISOKU3dscutextract.ipf:916`


### Internal/static helpers

#### `KP_LoadNanonis3dsGetData`

```igor
Static Function/WAVE KP_LoadNanonis3dsGetData(pathStr, s)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsGetData(pathStr, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; reshapes existing waves; loads data from files or paths; cleans up waves/windows or brings an existing window forward. Main internal calls: `KP_LoadNanonis3dsGetDataParam()`, `KP_LoadNanonis3dsGetDataWaveNames()`, `KP_LoadNanonis3dsGetDataSpec()`, `GetKeyState()`, `numpnts()`, `KP_NanonisCommonDataAvg()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
  - `s`: Used in: GBLoadWave/O/Q/N=KP_tmp3ds/T={2,4}/S=(s.headerSize)/W=1 pathStr
- Source: `KP_NanonisLoaders.ipf:200`

#### `KP_LoadNanonis3dsGetDataParam`

```igor
Static Function/WAVE KP_LoadNanonis3dsGetDataParam(w, fileName, s)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsGetDataParam(w, fileName, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; creates, switches, or removes Igor data folders. Main internal calls: `WhichListItem()`, `x()`, `y()`.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `fileName`: File name or file-derived label used by the loader/export routine.
  - `s`: Used in: Make/N=5 scan_x = ((xw * cos(s.angle / 180 * pi) + yw * sin(s.angle / 180 * pi)) * s.xscale / 2 + s.xcenter) * 1e10 / Make/N=5 scan_y = ((-xw * sin(s.angle / 180 * pi) + yw * cos(s.angle / 180 * pi)) * s.yscale / 2 + s.ycenter) * 1e10
- Source: `KP_NanonisLoaders.ipf:229`

#### `KP_LoadNanonis3dsGetDataSpec`

```igor
Static Function/WAVE KP_LoadNanonis3dsGetDataSpec(w, namew, s)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsGetDataSpec(w, namew, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `WhichListItem()`, `x()`, `y()`, `KP_LoadNanonis3dsSetMLSBias()`, `KP_NanonisCommonConversion()`.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `namew`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `s`: Used in: Make/N=(s.xpnts, s.ypnts, s.zpnts) $namew[i]/WAVE=specw / SetScale/I x, 0, s.xscale * 1e10, "A", specw
- Source: `KP_NanonisLoaders.ipf:283`

#### `KP_LoadNanonis3dsGetDataWaveNames`

```igor
Static Function/WAVE KP_LoadNanonis3dsGetDataWaveNames(fileName, chanList)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsGetDataWaveNames(fileName, chanList)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `fileName`: File name or file-derived label used by the loader/export routine.
  - `chanList`: Used in: Make/N=(ItemsInList(chanList))/T/FREE namew
- Source: `KP_NanonisLoaders.ipf:265`

#### `KP_LoadNanonis3dsGetHeader`

```igor
Static Function KP_LoadNanonis3dsGetHeader(pathStr, s)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsGetHeader(pathStr, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `KP_NanonisCommonGetHeader()`, `NumVarOrDefault()`, `StrVarOrDefault()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:159`

#### `KP_LoadNanonis3dsSetMLSBias`

```igor
Static Function KP_LoadNanonis3dsSetMLSBias(specw, mlsw)
```
- Purpose: parses a Nanonis 3ds spectroscopy/grid file and returns loaded waves.
- Usage: call as `KP_LoadNanonis3dsSetMLSBias(specw, mlsw)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Parameters:
  - `specw`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `mlsw`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `KP_NanonisLoaders.ipf:324`

#### `KP_LoadNanonisNSPGetData`

```igor
Static Function/WAVE KP_LoadNanonisNSPGetData(pathStr, s)
```
- Usage: call as `KP_LoadNanonisNSPGetData(pathStr, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: reshapes existing waves; assigns Igor axis scaling/units to the output; loads data from files or paths.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
  - `s`: Used in: GBLoadWave/O/Q/N=KP_tmpnsp/T={2,4}/S=(s.headerSize)/W=1 pathStr / SetScale/I x, s.starttime, s.endtime, "dat", tw
- Source: `KP_NanonisLoaders.ipf:631`

#### `KP_LoadNanonisNSPGetHeaderCvt`

```igor
Static Function KP_LoadNanonisNSPGetHeaderCvt(s)
```
- Usage: call as `KP_LoadNanonisNSPGetHeaderCvt(s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `date2secs()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:542`

#### `KP_LoadNanonisSXMGetData`

```igor
Static Function/WAVE KP_LoadNanonisSXMGetData(pathStr, s)
```
- Usage: call as `KP_LoadNanonisSXMGetData(pathStr, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; reshapes existing waves; assigns Igor axis scaling/units to the output; uses Igor matrix operations for vectorized calculation; loads data from files or paths. Main internal calls: `WaveUnits()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
  - `s`: Used in: GBLoadWave/O/Q/N=KP_tmpsxm/T={2,4}/S=(s.headerSize)/W=1 pathStr / SetScale/I x, s.xcenter - s.xscale / 2, s.xcenter + s.xscale / 2, "A", lw
- Source: `KP_NanonisLoaders.ipf:570`

#### `KP_LoadNanonisSXMGetHeaderCvt`

```igor
Static Function KP_LoadNanonisSXMGetHeaderCvt(s)
```
- Usage: call as `KP_LoadNanonisSXMGetHeaderCvt(s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping. Main internal calls: `time()`, `width()`, `height()`, `x()`, `y()`, `angle()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:504`

#### `KP_LoadNanonisSXMGetHeaderDI`

```igor
Static Function/WAVE KP_LoadNanonisSXMGetHeaderDI(pathStr, refNum)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `KP_LoadNanonisSXMGetHeaderDI(pathStr, refNum)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; reshapes existing waves.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
  - `refNum`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `KP_NanonisLoaders.ipf:463`

#### `KP_LoadNanonisSXMGetHeaderZC`

```igor
Static Function KP_LoadNanonisSXMGetHeaderZC(refNum)
```
- Usage: call as `KP_LoadNanonisSXMGetHeaderZC(refNum)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion. Main internal calls: `KP_NanonisCommonVariableString()`.
- Parameters:
  - `refNum`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `KP_NanonisLoaders.ipf:449`

#### `KP_LoadNanonisSxmNspGetHeader`

```igor
Static Function KP_LoadNanonisSxmNspGetHeader(pathStr, s)
```
- Purpose: parses Nanonis sxm/nsp topography-style files and returns loaded waves.
- Usage: call as `KP_LoadNanonisSxmNspGetHeader(pathStr, s)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion; Igor wave/matrix/cube data operation. Code behavior: loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `KP_LoadNanonisSXMGetHeaderZC()`, `KP_LoadNanonisSXMGetHeaderDI()`, `KP_NanonisCommonVariableString()`, `NumVarOrDefault()`, `elseif()`, `KP_LoadNanonisSxmNspGetHeaderEnd()`.
- Parameters:
  - `pathStr`: File-system path or data-folder string used for loading or saving data.
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `KP_NanonisLoaders.ipf:390`

#### `KP_LoadNanonisSxmNspGetHeaderEnd`

```igor
Static Function KP_LoadNanonisSxmNspGetHeaderEnd(refNum)
```
- Purpose: parses Nanonis sxm/nsp topography-style files and returns loaded waves.
- Usage: call as `KP_LoadNanonisSxmNspGetHeaderEnd(refNum)`.
- Notes: Internal helper function for Nanonis/STM data loading or map conversion. Code behavior: creates output waves; writes or exports data. Main internal calls: `elseif()`.
- Parameters:
  - `refNum`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `KP_NanonisLoaders.ipf:486`



## Smart Display, Graph Formatting, and Figure Tools

### Panel callbacks and control handlers

#### `Button_image_auto_bothwf1`

```igor
Function Button_image_auto_bothwf1(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_auto_bothwf1(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `auto`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `auto`
- Source: `SmartDisplay.ipf:1822`

#### `Button_image_auto_bothwf2`

```igor
Function Button_image_auto_bothwf2(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_auto_bothwf2(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `auto`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `auto`
- Source: `SmartDisplay.ipf:2088`

#### `Button_set_auto_axis`

```igor
Function Button_set_auto_axis(ctrlName) : ButtonControl
```
- Usage: call as `Button_set_auto_axis(ctrlName)`.
- Notes: Panel callback/helper for graph display, formatting, or window management. Panel control(s): `A axises`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A axises`, `A axises`
- Source: `Pierre's Template.ipf:9413`

#### `ButtonProc_appendboxonly`

```igor
Function ButtonProc_appendboxonly(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Panel control(s): `Box`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Box`
- Source: `Smart_3D_Viewer_New.ipf:4669`

#### `ButtonProc_autodisplay`

```igor
Function ButtonProc_autodisplay(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `autodisplay()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `A`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A`
- Source: `Miscellaneous_Codes.ipf:9025`

#### `ButtonProc_ave`

```igor
Function ButtonProc_ave(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Calculate average`. Main internal calls: `ave_inmarque()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Calculate average`
- Source: `Smart_3D_Viewer_New.ipf:9889`

#### `ButtonProc_Capturename`

```igor
Function ButtonProc_Capturename(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `Capturename()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Capture`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Capture`, `Capture`
- Source: `Pierre's Template.ipf:729`

#### `ButtonProc_Capturename_child`

```igor
Function ButtonProc_Capturename_child(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `Capturename_child()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Cap#G*`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cap#G*`
- Source: `SmartDisplay.ipf:5411`

#### `ButtonProc_closethiswindow`

```igor
Function ButtonProc_closethiswindow(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `Close`. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Close`, `Close`, `Close`, `Close`, `Close`, `Close`, `Close`, `Close`
- Source: `Pierre's Template.ipf:546`

#### `ButtonProc_color_edc`

```igor
Function ButtonProc_color_edc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `color_edc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `Color`, `Color now`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Color`, `Color now`
- Source: `Pierre's Template.ipf:699`

#### `ButtonProc_color_edc_more2`

```igor
Function ButtonProc_color_edc_more2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `color_edc_more2()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `Color`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Color`
- Source: `SmartDisplay.ipf:937`

#### `ButtonProc_colorauto`

```igor
Function ButtonProc_colorauto(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Panel control(s): `Auto`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto`
- Source: `Models.ipf:12331`

#### `ButtonProc_colorbwr`

```igor
Function ButtonProc_colorbwr(ctrlName) : ButtonControl
```
- Notes: $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ Usage: triggered as a wrapper that opens/runs `colorbwr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:11932`

#### `ButtonProc_colorcycling`

```igor
Function ButtonProc_colorcycling(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Color Figure Panel control(s): `CycleC`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CycleC`
- Source: `SmartDisplay.ipf:2671`

#### `ButtonProc_colorcycling_ind`

```igor
Function ButtonProc_colorcycling_ind(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `CycleC`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CycleC`
- Source: `SmartDisplay.ipf:4233`

#### `ButtonProc_colordiv_ind`

```igor
Function ButtonProc_colordiv_ind(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `DivgC`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `DivgC`
- Source: `SmartDisplay.ipf:4257`

#### `ButtonProc_colorFFT`

```igor
Function ButtonProc_colorFFT(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `colorFFT()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `FFT`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FFT`
- Source: `SmartDisplay.ipf:691`

#### `ButtonProc_colormatauto1`

```igor
Function ButtonProc_colormatauto1(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `A`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A`
- Source: `SmartDisplay.ipf:4176`

#### `ButtonProc_colormatauto2`

```igor
Function ButtonProc_colormatauto2(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `∓1`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `∓1`
- Source: `SmartDisplay.ipf:4189`

#### `ButtonProc_colormatauto3`

```igor
Function ButtonProc_colormatauto3(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `∓π`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `∓π`
- Source: `SmartDisplay.ipf:4200`

#### `ButtonProc_colormatauto4`

```igor
Function ButtonProc_colormatauto4(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `∓V`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `imageinfo()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `∓V`
- Source: `SmartDisplay.ipf:4211`

#### `ButtonProc_colorplain`

```igor
Function ButtonProc_colorplain(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Panel control(s): `PlainC`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `PlainC`
- Source: `SmartDisplay.ipf:2679`

#### `ButtonProc_colorplain_ind`

```igor
Function ButtonProc_colorplain_ind(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `PlainC`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `PlainC`
- Source: `SmartDisplay.ipf:4244`

#### `ButtonProc_Cons3dplot`

```igor
Function ButtonProc_Cons3dplot(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `d3d()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `3D Multifunc. Displayer`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3D Multifunc. Displayer`
- Source: `Smart_3D_Viewer_New.ipf:4`

#### `ButtonProc_Cons3dplotlc`

```igor
Function ButtonProc_Cons3dplotlc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Launch arbitary Linecut extraction }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Launch Linecut`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartpp()`, `findendpp()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqq()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Launch Linecut`
- Source: `Smart_3D_Viewer_New.ipf:791`

#### `ButtonProc_Cons3dplotlcf`

```igor
Function ButtonProc_Cons3dplotlcf(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Launch arbitary Linecut extraction }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Linecut FFT`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartppf()`, `findendppf()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqqf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut FFT`
- Source: `Smart_3D_Viewer_New.ipf:6358`

#### `ButtonProc_Cons3dplotmulti`

```igor
Function ButtonProc_Cons3dplotmulti(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Multimap Function }}}}}}}}}} #1 Control Function Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `MultiMap`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `f_for3dmulti()`, `levelimage2()`, `color3s_for3dm()`, `ckfig_child()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MultiMap`
- Source: `Smart_3D_Viewer_New.ipf:3460`

#### `ButtonProc_Consactiveall`

```igor
Function ButtonProc_Consactiveall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Consoffsetx()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `active all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `active all`
- Source: `SmartDisplay.ipf:1254`

#### `ButtonProc_Conshist`

```igor
Function ButtonProc_Conshist(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Conshist()`.
- Notes: continuely tuning Histogram parameters Panel control(s): `Auto Histogram`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto Histogram`
- Source: `SmartDisplay.ipf:2238`

#### `ButtonProc_Conslevel`

```igor
Function ButtonProc_Conslevel(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Conslevel()`.
- Notes: continuely tuning levelingimage order to an image Panel control(s): `Rmv. Fig Trend`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rmv. Fig Trend`
- Source: `SmartDisplay.ipf:2424`

#### `ButtonProc_Conslinethick`

```igor
Function ButtonProc_Conslinethick(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Conslinethick()`.
- Notes: continuely tuning line thickness on the graph Panel control(s): `LThick`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LThick`
- Source: `SmartDisplay.ipf:1417`

#### `ButtonProc_Consmoo`

```igor
Function ButtonProc_Consmoo(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Consmoo()`.
- Notes: continuely tuning smooth to any graph Panel control(s): `Smooth`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Smooth`
- Source: `SmartDisplay.ipf:1268`

#### `ButtonProc_Csrtracor`

```igor
Function ButtonProc_Csrtracor(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Csrtracor()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Cursor Tracor`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cursor Tracor`
- Source: `SmartDisplay.ipf:5646`

#### `ButtonProc_Csrtracor1`

```igor
Function ButtonProc_Csrtracor1(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Contour tracor by cursor Panel control(s): `Csr Tracor`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Csr Tracor`
- Source: `SmartDisplay.ipf:5640`

#### `ButtonProc_cyclecolorwavec`

```igor
Function ButtonProc_cyclecolorwavec(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `cyclecolorwavec()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `Cycle Color`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cycle Color`
- Source: `SmartDisplay.ipf:5052`

#### `ButtonProc_DisplayMat`

```igor
Function ButtonProc_DisplayMat(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `DisplayMat()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Make Image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make Image`, `Make Image`
- Source: `Pierre's Template.ipf:1095`

#### `ButtonProc_displaymulti`

```igor
Function ButtonProc_displaymulti(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `displaymulti()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Display Waterfall`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Display Waterfall`
- Source: `Miscellaneous_Codes.ipf:5058`

#### `ButtonProc_Drawarrowy`

```igor
Function ButtonProc_Drawarrowy(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Drawarrowc()`.
- Notes: Draw axis Panel control(s): `axis`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `axis`
- Source: `SmartDisplay.ipf:5502`

#### `ButtonProc_killags`

```igor
Function ButtonProc_killags(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `killags()`.
- Notes: End of Smart edit table for single wave table kill all graph Purpose: removes waves/windows/data points or cleans intermediate state. Panel control(s): `KILL / Graphs`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `KILL / Graphs`
- Source: `SmartDisplay.ipf:603`

#### `ButtonProc_killlayoutforhis`

```igor
Function ButtonProc_killlayoutforhis(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Kill Layout`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Kill Layout`
- Source: `MultipeakforLinecut.ipf:1732`

#### `ButtonProc_L2dplotdo`

```igor
Function ButtonProc_L2dplotdo(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #2 Update freehand modes Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Go`. Main internal calls: `makefreehandwave_2dplot()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go`
- Source: `SmartDisplay.ipf:2879`

#### `ButtonProc_L3dplotdo`

```igor
Function ButtonProc_L3dplotdo(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #2 Update freehand modes Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Go`. Main internal calls: `makefreehandwave_3dplot()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go`
- Source: `Smart_3D_Viewer_New.ipf:2580`

#### `ButtonProc_L3dplotdof`

```igor
Function ButtonProc_L3dplotdof(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #2 Update freehand modes Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Go`. Main internal calls: `makefreehandwave_3dplotf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go`
- Source: `Smart_3D_Viewer_New.ipf:7296`

#### `ButtonProc_Layoutmap`

```igor
Function ButtonProc_Layoutmap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `layoutmap()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `VMap`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `VMap`
- Source: `Miscellaneous_Codes.ipf:6723`

#### `ButtonProc_linenorm2pongraphc`

```igor
Function ButtonProc_linenorm2pongraphc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linenorm2pongraphc()`.
- Notes: remove linear background from a waterfall Purpose: smooths, normalizes, or removes background/trend components. Panel control(s): `LnBkg (frm graph)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LnBkg (frm graph)`
- Source: `Miscellaneous_Codes.ipf:10977`

#### `ButtonProc_Loaddata`

```igor
Function ButtonProc_Loaddata(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Load`. Code behavior: creates, switches, or removes Igor data folders.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Load`
- Source: `Pierre's Template.ipf:17060`

#### `ButtonProc_Loaddata2`

```igor
Function ButtonProc_Loaddata2(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `LD 2D`. Code behavior: creates, switches, or removes Igor data folders.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LD 2D`
- Source: `Miscellaneous_Codes.ipf:12070`

#### `ButtonProc_Loadnewmk`

```igor
Function ButtonProc_Loadnewmk(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: End of the GUI demo procedure Purpose: loads data or launches an automatic loading workflow. Panel control(s): `Ld_Nnis`. Code behavior: creates, switches, or removes Igor data folders.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ld_Nnis`
- Source: `Miscellaneous_Codes.ipf:300`

#### `ButtonProc_Loadnewmk_ave`

```igor
Function ButtonProc_Loadnewmk_ave(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: Load two columes and then average those column for Y-wave This is an old version, I replace it by a new one. Code behavior: creates, switches, or removes Igor data folders.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:463`

#### `ButtonProc_Loadnewmkfab`

```igor
Function ButtonProc_Loadnewmkfab(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: LOAD DATA and average the forward and backward data Also make the conducatance value correct, follow the setpoint, Assuming the First data point of the forward scan is the real setpoint So that the averaged data is scaled following this point. Panel control(s): `Ave`. Code behavior: creates, switches, or removes Igor data folders.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ave`
- Source: `Miscellaneous_Codes.ipf:548`

#### `ButtonProc_ls3dcons`

```igor
Function ButtonProc_ls3dcons(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ffc3dpls()`.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Lattice segregation }}}}}}}}}} Panel control(s): `Latt.Serg`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Latt.Serg`
- Source: `Smart_3D_Viewer_New.ipf:9183`

#### `ButtonProc_lsturnoff3d`

```igor
Function ButtonProc_lsturnoff3d(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `BACK`, `X`. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BACK`, `X`, `X`, `BACK`, `BACK`, `BACK`, `BACK`, `BACK`
- Source: `Smart_3D_Viewer_New.ipf:9943`

#### `ButtonProc_makeadd_data_table`

```igor
Function ButtonProc_makeadd_data_table(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `makeadd_data_table()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Table`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Table`
- Source: `Pierre's Template.ipf:950`

#### `ButtonProc_MakeCutTable`

```igor
Function ButtonProc_MakeCutTable(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `MakeCutTable()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Cut Table`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cut Table`
- Source: `Pierre's Template.ipf:26935`

#### `ButtonProc_MakeImagetable`

```igor
Function ButtonProc_MakeImagetable(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `MakeImageTable()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Imagetable`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Imagetable`
- Source: `Pierre's Template.ipf:26985`

#### `ButtonProc_makelayoutforhis`

```igor
Function ButtonProc_makelayoutforhis(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Part 3 Auto Layout Purpose: creates new waves, maps, figures, or simulation data. Panel control(s): `Make Layout`. Main internal calls: `makelayoutforhis()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make Layout`
- Source: `MultipeakforLinecut.ipf:1549`

#### `ButtonProc_makevectorfield`

```igor
Function ButtonProc_makevectorfield(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `makevectorfieldc()`.
- Notes: Procedure for vector field plot You need to input the phase map and amplitude map Purpose: creates new waves, maps, figures, or simulation data. Panel control(s): `Vector Field Plot`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Vector Field Plot`
- Source: `SmartDisplay.ipf:4440`

#### `ButtonProc_Mk_nk_table_pi`

```igor
Function ButtonProc_Mk_nk_table_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Mk_nk_table_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1020`

#### `ButtonProc_Mk_vb_table_pi`

```igor
Function ButtonProc_Mk_vb_table_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Mk_vb_table_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1010`

#### `ButtonProc_Normalinecut`

```igor
Function ButtonProc_Normalinecut(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Normalinecutc2()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `Norm Matrix`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Norm Matrix`
- Source: `Smart_3D_Viewer_New.ipf:1582`

#### `ButtonProc_savegraph`

```igor
Function ButtonProc_savegraph(ctrlName) : ButtonControl//"Special"
```
- Purpose: captures a graph recreation command so the graph can be rebuilt later.
- Usage: triggered as a wrapper that opens/runs `savegraph()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1120`

#### `ButtonProc_selecshowsp`

```igor
Function ButtonProc_selecshowsp(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `P`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `P`, `P`
- Source: `Smart_3D_Viewer_New.ipf:9919`

#### `ButtonProc_Showfigind`

```igor
Function ButtonProc_Showfigind(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ## New and Smarter version of Auto display figure## Panel control(s): `d(*)`. Main internal calls: `d()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `d(*)`
- Source: `SmartDisplay.ipf:4087`

#### `ButtonProc_showpntreader`

```igor
Function ButtonProc_showpntreader(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Point_reader()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Bands table`, `Point reader`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Bands table`, `Point reader`, `Bands table`, `Point reader`
- Source: `Pierre's Template.ipf:613`

#### `ButtonProc_showtable`

```igor
Function ButtonProc_showtable(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Show Parameters`. Main internal calls: `grabtable()`, `cktable()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show Parameters`
- Source: `MultipeakforLinecut.ipf:1308`

#### `ButtonProc_sizecurve`

```igor
Function ButtonProc_sizecurve(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Format`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Format`
- Source: `Miscellaneous_Codes.ipf:5394`

#### `ButtonProc_sizecurvelableon`

```igor
Function ButtonProc_sizecurvelableon(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `Label on`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Label on`
- Source: `Miscellaneous_Codes.ipf:5422`

#### `ButtonProc_sizewaterfall`

```igor
Function ButtonProc_sizewaterfall(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:5269`

#### `ButtonProc_Table_Notthesepoints`

```igor
Function ButtonProc_Table_Notthesepoints(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Table_Notthesepoints()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Table`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Table`
- Source: `Pierre's Template.ipf:704`

#### `ButtonProc_tableafs`

```igor
Function ButtonProc_tableafs(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `Show table`, `Table`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show table`, `Table`, `Table`, `Show table`, `Show table`, `Table`
- Source: `Pierre's Template.ipf:671`

#### `ButtonProc_tableallbands`

```igor
Function ButtonProc_tableallbands(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `Show Table`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show Table`
- Source: `Pierre's Template.ipf:662`

#### `ButtonProc_VF`

```igor
Function ButtonProc_VF(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `SmartDisplay.ipf:4626`

#### `ButtonProc_Zcoloro`

```igor
Function ButtonProc_Zcoloro(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Part I: from color table Panel control(s): `Z_color`. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `margin()`, `CTabList()`. Update log: 2023-08-11: Opens the smart Z_color waterfall display from the Adjust-on-Graph section.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Z_color`
- Source: `SmartDisplay.ipf:1559`

#### `ButtonProc_Zcoloroexist`

```igor
Function ButtonProc_Zcoloroexist(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `Exist`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Exist`
- Source: `SmartDisplay.ipf:1648`

#### `ButtonWindow_movie_maker`

```igor
Function ButtonWindow_movie_maker(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Movie_Maker()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Jon's Movie Maker`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Jon's Movie Maker`
- Source: `Pierre's Template.ipf:26264`

#### `make_movie_ButtonProc`

```igor
Function make_movie_ButtonProc(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `make_movie_ButtonProc(ctrlName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Make Movie`. Main internal calls: `make_movie()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make Movie`
- Source: `Pierre's Template.ipf:36691`

#### `movie_maker_axis_SetVarProc_1`

```igor
Function movie_maker_axis_SetVarProc_1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: call as `movie_maker_axis_SetVarProc_1(ctrlName, varNum, varStr, varName)`.
- Notes: Panel callback/helper for graph display, formatting, or window management. Panel control(s): `Axis Height`. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Axis Height`
- Source: `Pierre's Template.ipf:36668`

#### `movie_maker_close_ButtonProc`

```igor
Function movie_maker_close_ButtonProc(ctrlName) : ButtonControl
```
- Usage: call as `movie_maker_close_ButtonProc(ctrlName)`.
- Notes: Panel callback/helper for graph display, formatting, or window management. Panel control(s): `CLOSE`. Code behavior: creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CLOSE`
- Source: `Pierre's Template.ipf:36696`

#### `movie_Maker_dir_PopMenuProc`

```igor
Function movie_Maker_dir_PopMenuProc(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: call as `movie_Maker_dir_PopMenuProc(ctrlName, popNum, popStr)`.
- Notes: WINDOW CONTROLS Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Cut_Direction`. Main internal calls: `doInsetYesNo()`, `elseIf()`, `do_movie_maker_image()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Cut_Direction`
- Source: `Pierre's Template.ipf:36596`

#### `movie_maker_index_SetVarProc`

```igor
Function movie_maker_index_SetVarProc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: call as `movie_maker_index_SetVarProc(ctrlName, varNum, varStr, varName)`.
- Notes: Panel callback/helper in `Pierre's Template.ipf`. Panel control(s): `Comb`, `change_index`. Main internal calls: `do_movie_maker_image()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Comb`, `change_index`
- Source: `Pierre's Template.ipf:36619`

#### `movie_maker_mat_popUp`

```igor
Function movie_maker_mat_popUp(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: call as `movie_maker_mat_popUp(ctrlName, popNum, popStr)`.
- Notes: Panel callback/helper for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `chose_matrix`. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `elseIf()`, `do_movie_maker_image()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `chose_matrix`
- Source: `Pierre's Template.ipf:36627`

#### `PopMenuProc_appendimage`

```igor
Function PopMenuProc_appendimage(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Overlay T(r) and Δ(r) }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `stringmatch()`, `elseif()`, `grabwinnonew()`, `gethistgram_npcolor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `Smart_3D_Viewer_New.ipf:4379`

#### `PopMenuProc_appendimagef`

```igor
Function PopMenuProc_appendimagef(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Append`. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `grabwinnonew()`, `gethistgram_npcolor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Append`
- Source: `Smart_3D_Viewer_New.ipf:6261`

#### `PopMenuProc_appendimagevf`

```igor
Function PopMenuProc_appendimagevf(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `appendtordvf`. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `margin()`, `arrowcolortype_vf()`, `gethistgram_npcolor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `appendtordvf`, `appendtordvf`
- Source: `SmartDisplay.ipf:4797`

#### `PopMenuProc_bkgornot`

```igor
Function PopMenuProc_bkgornot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Gaus_bkg?`. Main internal calls: `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Gaus_bkg?`
- Source: `Smart_3D_Viewer_New.ipf:1052`

#### `PopMenuProc_bkgrmv2D_3dplot`

```igor
Function PopMenuProc_bkgrmv2D_3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Part of symmetrized QPI Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Bkg.Rmv?`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `f_for3d()`, `D4_sym_3dplot()`, `Mdiag_sym_3dplot()`, `Mx_sym_3dplot()`, `C4_sym_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Bkg.Rmv?`
- Source: `Smart_3D_Viewer_New.ipf:10360`

#### `PopMenuProc_colormat`

```igor
Function PopMenuProc_colormat(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `popupmatcolor`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupmatcolor`, `popupmatcolor`
- Source: `Pierre's Template.ipf:12203`

#### `PopMenuProc_colormat_ind`

```igor
Function PopMenuProc_colormat_ind(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `popup0`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup0`, `popup0`
- Source: `SmartDisplay.ipf:4349`

#### `PopMenuProc_colormat_ind2`

```igor
Function PopMenuProc_colormat_ind2(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `popup1`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup1`, `popup1`
- Source: `SmartDisplay.ipf:4373`

#### `PopMenuProc_colormatinv`

```igor
Function PopMenuProc_colormatinv(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `popupinvmatcolor`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupinvmatcolor`, `popupinvmatcolor`
- Source: `Pierre's Template.ipf:12226`

#### `PopMenuProc_colormatinv1`

```igor
Function PopMenuProc_colormatinv1(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `popup1`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `maxwaterfall()`, `minwaterfall()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup1`
- Source: `SmartDisplay.ipf:1695`

#### `PopMenuProc_colormatinv2`

```igor
Function PopMenuProc_colormatinv2(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `popup3`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `maxwaterfall()`, `minwaterfall()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup3`
- Source: `SmartDisplay.ipf:1957`

#### `PopMenuProc_colormatinvmore2`

```igor
Function PopMenuProc_colormatinvmore2(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:1167`

#### `PopMenuProc_colormatmore2`

```igor
Function PopMenuProc_colormatmore2(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `popuptest1`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popuptest1`
- Source: `SmartDisplay.ipf:1100`

#### `PopMenuProc_curvcolor`

```igor
Function PopMenuProc_curvcolor(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `Pierre's Template.ipf:13836`

#### `PopMenuProc_ftyornot`

```igor
Function PopMenuProc_ftyornot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `FT_y?`. Main internal calls: `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `FT_y?`
- Source: `Smart_3D_Viewer_New.ipf:1119`

#### `PopMenuProc_jump2dp`

```igor
Function PopMenuProc_jump2dp(ctrlName,popNum,popStr) : PopupMenuControl
```
- Notes: #1 Control of On/OFF of the remover Usage: called automatically by Igor when the linked panel control changes. Main internal calls: `getMDC_2dplot()`, `getEDC_2dplot()`, `twopgetline()`, `makefreehandwave_2dplot()`, `Lineprofilefromcircle()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:3861`

#### `PopMenuProc_jump2dpmode`

```igor
Function PopMenuProc_jump2dpmode(ctrlName,popNum,popStr) : PopupMenuControl
```
- Notes: #2 Control of Remover Mode <auto or Givem> Usage: called automatically by Igor when the linked panel control changes.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:3956`

#### `PopMenuProc_jumpremover3d`

```igor
Function PopMenuProc_jumpremover3d(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Panel control(s): `jumpremoveon3d`. Main internal calls: `getsinglestsf()`, `pcsr()`, `qcsr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `jumpremoveon3d`
- Source: `Smart_3D_Viewer_New.ipf:6081`

#### `PopMenuProc_L2dpolar`

```igor
Function PopMenuProc_L2dpolar(ctrlName,popNum,popStr) : PopupMenuControl
```
- Notes: #3_08 Polar plot Usage: called automatically by Igor when the linked panel control changes. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `Dopolarfig()`, `grabwinnonew()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:3561`

#### `PopMenuProc_more2`

```igor
Function PopMenuProc_more2(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: end check if a wave on the graph Panel control(s): `popuptest`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popuptest`
- Source: `SmartDisplay.ipf:930`

#### `PopMenuProc_normornot`

```igor
Function PopMenuProc_normornot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##04 Control Function:Change Normalization or not, Purpose: smooths, normalizes, or removes background/trend components. Panel control(s): `Norm?`. Main internal calls: `Normalinecut()`, `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Norm?`
- Source: `Smart_3D_Viewer_New.ipf:1387`

#### `PopMenuProc_selmode2dplot`

```igor
Function PopMenuProc_selmode2dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #1 control of Advanced mode (select modes) Purpose: displays waves, images, contours, or graph overlays. Main internal calls: `tpw()`, `hook()`, `twopgetline()`, `makefreehandwave_2dplot()`, `Lineprofilefromcircle()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:2839`

#### `PopMenuProc_selmode3dplot`

```igor
Function PopMenuProc_selmode3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #1 control of Advanced mode (select modes) Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `grabwin()`, `hook()`, `pcsr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `Smart_3D_Viewer_New.ipf:2484`

#### `PopMenuProc_selmode3dplotf`

```igor
Function PopMenuProc_selmode3dplotf(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #1 control of Advanced mode (select modes) Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `hook()`, `pcsr()`, `di()`, `grabwinnonew()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Source: `Smart_3D_Viewer_New.ipf:7200`

#### `PopMenuProc_vf`

```igor
Function PopMenuProc_vf(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Main internal calls: `arrowcolortype_vf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:4688`

#### `PopMenuProc_vflenmode`

```igor
Function PopMenuProc_vflenmode(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves; extracts image/wave statistics.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Source: `SmartDisplay.ipf:4643`

#### `PopMenuProc_Zcoloronthegraph`

```igor
Function PopMenuProc_Zcoloronthegraph(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `popup0`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `maxwaterfall()`, `minwaterfall()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup0`
- Source: `SmartDisplay.ipf:1653`

#### `PopMenuProc_Zcoloronthegraph2`

```igor
Function PopMenuProc_Zcoloronthegraph2(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Part II: from User color table wave Panel control(s): `popup2`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `maxwaterfall()`, `minwaterfall()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup2`
- Source: `SmartDisplay.ipf:1917`

#### `PopMenuProc_Znormornot`

```igor
Function PopMenuProc_Znormornot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; normalize 3D by sts }}}}}}}}}} Purpose: smooths, normalizes, or removes background/trend components. Panel control(s): `popupnormornot`. Main internal calls: `Norma3dmatrix()`, `Cons3dplotc()`, `f_for3d()`, `color3s_for3d()`, `grabwinnonew()`, `UpdateControls_3dp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupnormornot`, `popupnormornot`
- Source: `Smart_3D_Viewer_New.ipf:348`

#### `SetVarProc_addX3dplot`

```igor
Function SetVarProc_addX3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##02 Control Function:Change addX, Call to change LinecutH and change the indicative line Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `LV`. Main internal calls: `anglelinecutV()`, `angleline_3dp()`, `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `LV`
- Source: `Smart_3D_Viewer_New.ipf:1289`

#### `SetVarProc_addX3dplotf`

```igor
Function SetVarProc_addX3dplotf(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##02 Control Function:Change addX, Call to change LinecutH and change the indicative line Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `LV`. Main internal calls: `anglelinecutVf()`, `angleline_3dpf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `LV`
- Source: `Smart_3D_Viewer_New.ipf:6533`

#### `SetVarProc_addY3dplot`

```igor
Function SetVarProc_addY3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##01 Control Function:Change addY, Call to change LinecutH and change the indicative line Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `LH`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `anglelinecutH()`, `angleline_3dp()`, `FFTL2_3dplot()`, `color3s_subfor3dFFT()`, `grabwinchild()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `LH`
- Source: `Smart_3D_Viewer_New.ipf:1242`

#### `SetVarProc_addY3dplotf`

```igor
Function SetVarProc_addY3dplotf(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##01 Control Function:Change addY, Call to change LinecutH and change the indicative line Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `LH`. Main internal calls: `anglelinecutHf()`, `angleline_3dpf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `LH`
- Source: `Smart_3D_Viewer_New.ipf:6508`

#### `SetVarProc_avedia_3dplot`

```igor
Function SetVarProc_avedia_3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `FWHM(r)`, `Global Q?`. Main internal calls: `lockinp3dp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM(r)`, `Global Q?`
- Source: `Smart_3D_Viewer_New.ipf:8947`

#### `SetVarProc_bkgornotsigma`

```igor
Function SetVarProc_bkgornotsigma(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `σ_Gaus`. Main internal calls: `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ_Gaus`
- Source: `Smart_3D_Viewer_New.ipf:1086`

#### `SetVarProc_bkgrmv2D_3dplot`

```igor
Function SetVarProc_bkgrmv2D_3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `σ_bkg`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `f_for3d()`, `D4_sym_3dplot()`, `Mdiag_sym_3dplot()`, `Mx_sym_3dplot()`, `C4_sym_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ_bkg`
- Source: `Smart_3D_Viewer_New.ipf:10406`

#### `SetVarProc_changeds`

```igor
Function SetVarProc_changeds(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `coloarrangec()`.
- Notes: modifygraph display range follow times sigma modifygraph display range follow times sigma Panel control(s): `σ`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ`
- Source: `SmartDisplay.ipf:661`

#### `SetVarProc_coloarrange_ind`

```igor
Function SetVarProc_coloarrange_ind(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `σ`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_np()`, `tpw()`, `imageinfo()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ`
- Source: `SmartDisplay.ipf:4295`

#### `SetVarProc_colorinver_ind`

```igor
Function SetVarProc_colorinver_ind(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `IV`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `IV`
- Source: `SmartDisplay.ipf:4329`

#### `SetVarProc_colormatmorevv`

```igor
Function SetVarProc_colormatmorevv(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for graph display, formatting, or window management. Panel control(s): `dvg_bwr_20_95_c54`. Main internal calls: `colormatmorevv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `dvg_bwr_20_95_c54`
- Source: `SmartDisplay.ipf:1126`

#### `SetVarProc_colormatmorevv_ind`

```igor
Function SetVarProc_colormatmorevv_ind(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for graph display, formatting, or window management. Panel control(s): `setvarsetciuind`. Main internal calls: `colormatmorevv_ind()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvarsetciuind`
- Source: `SmartDisplay.ipf:4397`

#### `SetVarProc_colormatmorevvline`

```igor
Function SetVarProc_colormatmorevvline(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `colormatmorevvline()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `SpectrumBlack`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `SpectrumBlack`
- Source: `SmartDisplay.ipf:989`

#### `SetVarProc_colornumind`

```igor
Function SetVarProc_colornumind(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `C_Min`, `C_Max`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `imageinfo()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `C_Min`, `C_Max`
- Source: `SmartDisplay.ipf:4276`

#### `SetVarProc_colorratio_consFFT`

```igor
Function SetVarProc_colorratio_consFFT(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `σ`. Main internal calls: `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ`, `σ`
- Source: `Smart_3D_Viewer_New.ipf:10051`

#### `SetVarProc_colorratio_consFFTdemo`

```igor
Function SetVarProc_colorratio_consFFTdemo(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `Color Range σ`. Main internal calls: `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Color Range σ`
- Source: `Symmetrization.ipf:676`

#### `SetVarProc_Cons3dplot`

```igor
Function SetVarProc_Cons3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Dsiplay 3D matrix by selected layer:control function }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Z`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `removeappendimage()`, `tpw()`, `Cons3dplotc()`, `grabwinnonew()`, `Cons3dplotcf()`, `grabwinchild()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`, `Z`
- Source: `Smart_3D_Viewer_New.ipf:396`

#### `SetVarProc_Cons3dplotdivc`

```igor
Function SetVarProc_Cons3dplotdivc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `DivC`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `max()`, `grabwinchild()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `DivC`, `DivC`
- Source: `Smart_3D_Viewer_New.ipf:10030`

#### `SetVarProc_Cons3dplotf`

```igor
Function SetVarProc_Cons3dplotf(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Dsiplay 3D matrix by selected layer:control function }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Z`. Main internal calls: `Cons3dplotc()`, `Cons3dplotcf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `Smart_3D_Viewer_New.ipf:6155`

#### `SetVarProc_Cons3dplotmli`

```igor
Function SetVarProc_Cons3dplotmli(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Igor wave/matrix/cube data operation. Panel control(s): `Z`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `lockinp3dp()`, `levelimage2()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `Smart_3D_Viewer_New.ipf:8779`

#### `SetVarProc_Conshist`

```igor
Function SetVarProc_Conshist(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Bin Num`, `Ratio`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Bin Num`, `Ratio`
- Source: `SmartDisplay.ipf:2256`

#### `SetVarProc_Conslevelp`

```igor
Function SetVarProc_Conslevelp(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Order`, `1D or 2D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Order`, `1D or 2D`
- Source: `SmartDisplay.ipf:2440`

#### `SetVarProc_Conslinethick`

```igor
Function SetVarProc_Conslinethick(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Thick`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Thick`
- Source: `SmartDisplay.ipf:1435`

#### `SetVarProc_Consmoo`

```igor
Function SetVarProc_Consmoo(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Times`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Times`
- Source: `SmartDisplay.ipf:1280`

#### `SetVarProc_ConsZcuiz`

```igor
Function SetVarProc_ConsZcuiz(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for graph display, formatting, or window management. Panel control(s): `dvg_bwr_20_95_c54`. Main internal calls: `ConsZcuiz()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `dvg_bwr_20_95_c54`
- Source: `SmartDisplay.ipf:1589`

#### `SetVarProc_ftyornotsigma`

```igor
Function SetVarProc_ftyornotsigma(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `σ_H`. Main internal calls: `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ_H`
- Source: `Smart_3D_Viewer_New.ipf:985`

#### `SetVarProc_ftyornotsigma2`

```igor
Function SetVarProc_ftyornotsigma2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `σ_V`. Main internal calls: `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ_V`
- Source: `Smart_3D_Viewer_New.ipf:1018`

#### `SetVarProc_jumpCriteria_2dplot`

```igor
Function SetVarProc_jumpCriteria_2dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #0_2 Jump Remove: Given Mode_control jump criteria Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Criteria`. Main internal calls: `getMDC_2dplot()`, `getEDC_2dplot()`, `twopgetline()`, `makefreehandwave_2dplot()`, `Lineprofilefromcircle()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Criteria`
- Source: `SmartDisplay.ipf:3823`

#### `SetVarProc_jumpmount_2dplot`

```igor
Function SetVarProc_jumpmount_2dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #0_1 Jump Remove: Given Mode_control jump delta Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Given δ`. Main internal calls: `getMDC_2dplot()`, `getEDC_2dplot()`, `twopgetline()`, `makefreehandwave_2dplot()`, `Lineprofilefromcircle()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Given δ`
- Source: `SmartDisplay.ipf:3787`

#### `SetVarProc_lsFFTwindow`

```igor
Function SetVarProc_lsFFTwindow(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Panel control(s): `FFTWindow`. Main internal calls: `MDoLSegrereapplyfftmode()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FFTWindow`
- Source: `Lattice Segregation.ipf:1035`

#### `SetVarProc_pinorm_2dplot`

```igor
Function SetVarProc_pinorm_2dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Normalize Line cut by wave/=pi, wave-=wave[0] Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `π norm`. Main internal calls: `getMDC_2dplot()`, `getEDC_2dplot()`, `twopgetline()`, `makefreehandwave_2dplot()`, `Lineprofilefromcircle()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `π norm`
- Source: `SmartDisplay.ipf:2693`

#### `SetVarProc_pinorm_3dplot`

```igor
Function SetVarProc_pinorm_3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `π norm`. Main internal calls: `getsinglestsf()`, `pcsr()`, `qcsr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `π norm`
- Source: `Smart_3D_Viewer_New.ipf:6063`

#### `SetVarProc_rotate3dplot`

```igor
Function SetVarProc_rotate3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##03 Control Function:Change Rotation, Call to change LinecutH, LinecutV and change the indicative line, and Setvariable range setaddX/setaddY Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Rotate`. Code behavior: duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `anglelinecutH()`, `anglelinecutV()`, `angleline_3dp()`, `findrangeforangle_LH()`, `findrangeforangle_LV()`, `FFTL2_3dplot()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotate`
- Source: `Smart_3D_Viewer_New.ipf:1329`

#### `SetVarProc_rotate3dplotf`

```igor
Function SetVarProc_rotate3dplotf(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##03 Control Function:Change Rotation, Call to change LinecutH, LinecutV and change the indicative line, and Setvariable range setaddX/setaddY Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Rotate`. Code behavior: duplicates or stages waves for downstream processing; reads or updates panel controls. Main internal calls: `anglelinecutHf()`, `anglelinecutVf()`, `angleline_3dpf()`, `findrangeforangle_LHf()`, `grabwin()`, `findrangeforangle_LVf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotate`
- Source: `Smart_3D_Viewer_New.ipf:6558`

#### `SetVarProc_smornot_3dplot`

```igor
Function SetVarProc_smornot_3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##05 Control Function:Change Smooth Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Smooth?`. Main internal calls: `anglelinecutH()`, `anglelinecutV()`, `FFTL2_3dplot()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Smooth?`
- Source: `Smart_3D_Viewer_New.ipf:1449`

#### `SetVarProc_thresh_cmp`

```igor
Function SetVarProc_thresh_cmp(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Igor wave/matrix/cube data operation. Panel control(s): `Thres`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `Selnumavests_p()`, `Selnumavests_n()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Thres`
- Source: `Smart_3D_Viewer_New.ipf:10141`

#### `SetVarProc_vf1`

```igor
Function SetVarProc_vf1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `↑ length`, `↑l_bkg`. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves; extracts image/wave statistics.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `↑ length`, `↑ length`, `↑l_bkg`
- Source: `SmartDisplay.ipf:4729`

#### `SetVarProc_vf2`

```igor
Function SetVarProc_vf2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `Thick`, `headLen`, `headFat`, `posMode`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `arrowMarker()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Thick`, `headLen`, `headFat`, `posMode`, `Thick`, `headLen`, `headFat`, `posMode`
- Source: `SmartDisplay.ipf:4776`

#### `SliderProc_topmaxwf1`

```igor
Function SliderProc_topmaxwf1(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_topmaxwf1(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `slider0`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slider0`
- Source: `SmartDisplay.ipf:1736`

#### `SliderProc_topmaxwf2`

```igor
Function SliderProc_topmaxwf2(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_topmaxwf2(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `slider2`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slider2`
- Source: `SmartDisplay.ipf:1998`

#### `SliderProc_topminwf1`

```igor
Function SliderProc_topminwf1(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_topminwf1(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `slider1`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slider1`
- Source: `SmartDisplay.ipf:1780`

#### `SliderProc_topminwf2`

```igor
Function SliderProc_topminwf2(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_topminwf2(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `slider3`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slider3`
- Source: `SmartDisplay.ipf:2044`


### Interactive procedures and macros

#### `autodisplay`

```igor
proc autodisplay(matname,datanumber,per,number,down,up,shift,size,multix)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `matname`, `datanumber`, `per`, `number`, `down`, `up`, `shift`, `size`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: datanumber = Number of DATA lincut; per = Average how many sts; matname = name of the data; number = How many stss?; down = Lowest energy for norm (meV); up = Highest energy for norm (meV). Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `MakeEdc_pi()`, `addper()`, `displaymulti()`, `rescalexmultiwhat()`, `normrange()`, `Constantoffset_n()`.
- Parameters:
  - `matname`: name of the data
  - `datanumber`: Number of DATA lincut
  - `per`: Average how many sts
  - `number`: How many stss?
  - `down`: Lowest energy for norm (meV)
  - `up`: Highest energy for norm (meV)
  - `shift`: the meV value added to calibrate Ef
  - `size`: length (nm)
  - `multix`: rescale x axis, to multiply
- Source: `Miscellaneous_Codes.ipf:9031`

#### `AVG_showtable`

```igor
Proc AVG_showtable()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:12963`

#### `Capturename`

```igor
Proc Capturename()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ Image color scale Purpose: copies, duplicates, or renames Igor waves/traces. Code behavior: extracts image/wave statistics; reads or updates panel controls. Main internal calls: `ImageNameList()`, `ImageInfo()`, `str2num()`, `gethistgram_np()`.
- Source: `Pierre's Template.ipf:11950`

#### `Capturename_child`

```igor
Proc Capturename_child()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: extracts image/wave statistics; reads or updates panel controls. Main internal calls: `tpw()`, `grabwinchild()`, `ImageInfo()`, `str2num()`, `gethistgram_np()`.
- Source: `SmartDisplay.ipf:5415`

#### `Capturenamedd`

```igor
Proc Capturenamedd()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: test2(20,29.283,0.41,-3.68,15) Purpose: copies, duplicates, or renames Igor waves/traces. Code behavior: extracts image/wave statistics. Main internal calls: `ImageNameList()`.
- Source: `Miscellaneous_Codes.ipf:9301`

#### `coloarrangec`

```igor
Proc coloarrangec()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics.
- Source: `SmartDisplay.ipf:668`

#### `color_digimage`

```igor
Proc color_digimage(matname,disp_or_not)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `matname`, `disp_or_not`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: matname = Enter the name of the image; disp_or_not = Do you want to display. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Enter the name of the image
  - `disp_or_not`: Do you want to display
- Source: `Pierre's Template.ipf:6504`

#### `color_edc_more2`

```igor
Proc color_edc_more2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `WaveName()`, `interp()`, `rgb()`.
- Source: `SmartDisplay.ipf:941`

#### `colorbwr`

```igor
Proc colorbwr()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:11936`

#### `colorFFT`

```igor
Proc colorFFT()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Main internal calls: `color3sfft()`, `tpw()`.
- Source: `SmartDisplay.ipf:696`

#### `comparehotandcoldc`

```igor
Proc comparehotandcoldc(name,prefwave,thresh)
```
- Usage: run from Igor with parameters `name`, `prefwave`, `thresh`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: name = 3D wave G(r,E) the STS come from; Prefwave = Gap map; thresh = Threshold for defining a sublattice. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `levelimage2()`, `comparehotandcold()`.
- Parameters:
  - `name`: 3D wave G(r,E) the STS come from
  - `prefwave`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `thresh`: Threshold for defining a sublattice
- Source: `Smart_3D_Viewer_New.ipf:10079`

#### `Conshist`

```igor
Proc Conshist()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: reads or updates panel controls. Main internal calls: `gethistgram()`, `getratiohis()`. Update log: 08/11-03/2023: Added Auto Histogram Conshist(), the single function is gethistgram().
- Source: `SmartDisplay.ipf:2242`

#### `Conshistc`

```igor
Proc Conshistc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Main internal calls: `gethistgramcons()`.
- Source: `SmartDisplay.ipf:2263`

#### `Conshistcsigma`

```igor
Proc Conshistcsigma()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Main internal calls: `gethistgramhowmanysigma()`.
- Source: `SmartDisplay.ipf:2297`

#### `Conslevel`

```igor
Proc Conslevel()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`. Update log: 08/11/2023: Added Remove trend for image, this is a interactive procedure, the knob control the power of Poly2D fitting for this trend removing, Conslevel() Added several color tables from gwyddion.
- Source: `SmartDisplay.ipf:2428`

#### `Conslevelp`

```igor
Proc Conslevelp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `tpw()`, `levelimage2()`, `levelimage2_2D()`.
- Source: `SmartDisplay.ipf:2447`

#### `Conslinethick`

```igor
Proc Conslinethick()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls; cleans up waves/windows or brings an existing window forward.
- Source: `SmartDisplay.ipf:1421`

#### `Conslinethickc`

```igor
Proc Conslinethickc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations.
- Source: `SmartDisplay.ipf:1442`

#### `Consmoo`

```igor
Proc Consmoo()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `duplicateongraph()`. Update log: 07/31-02/2023: Added continuously tuning smooth Consmoo()and trace offset Consoffset().
- Source: `SmartDisplay.ipf:1272`

#### `Consmooc`

```igor
Proc Consmooc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Main internal calls: `revoverongraph()`, `smoothallthegraph()`.
- Source: `SmartDisplay.ipf:1287`

#### `Csrtracor`

```igor
Proc Csrtracor()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Main internal calls: `savexypairbycursor()`.
- Source: `SmartDisplay.ipf:5651`

#### `cyclecolorwavec`

```igor
Proc cyclecolorwavec(c,index)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `c`, `index`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: c = Cycles (>= 2); index = Color index, 14, 15, 16, 38, 39, 40, 84, 87. Code behavior: formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `cyclecolorwave()`, `CtabList()`, `tpw()`.
- Parameters:
  - `c`: Cycles (>= 2)
  - `index`: Color index, 14, 15, 16, 38, 39, 40, 84, 87
- Source: `SmartDisplay.ipf:5057`

#### `d3d`

```igor
Proc d3d(mat3dn,zn)
```
- Usage: run from Igor with parameters `mat3dn`, `zn`. Update-log guidance - 2023-08-17: Added the smarter 3D matrix plotter.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: mat3dn = Name of the; zn = Index of the Energy. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `getall3dwave()`, `sumoned()`, `di2lf()`, `func_zeroNaN()`, `color3s_for3d()`, `stringmatch()`.
- Parameters:
  - `mat3dn`: Name of the
  - `zn`: Index of the Energy
- Source: `Smart_3D_Viewer_New.ipf:8`

#### `DisplayMat`

```igor
Proc DisplayMat()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `Show2DImage()`.
- Source: `Pierre's Template.ipf:1660`

#### `displaymulti`

```igor
Proc displaymulti(dataname,from,to)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `dataname`, `from`, `to`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays. Main internal calls: `displaymultiF()`.
- Parameters:
  - `dataname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: = dimsize(File_name,0)-1
- Source: `Miscellaneous_Codes.ipf:5063`

#### `displaymulti2`

```igor
proc displaymulti2(dataname,from,batchnum,num)
```
- Usage: run from Igor with parameters `dataname`, `from`, `batchnum`, `num`.
- Notes: display equal space (num) for batchnum lines Purpose: displays waves, images, contours, or graph overlays. Code behavior: opens or updates graph/image displays.
- Parameters:
  - `dataname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `batchnum`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:7147`

#### `displaymulti_factor`

```igor
Proc displaymulti_factor(dataname,from,to,factor)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `dataname`, `from`, `to`, `factor`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays.
- Parameters:
  - `dataname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:5642`

#### `Drawarrowc`

```igor
Proc Drawarrowc(zerox,zeroy,angle,length)
```
- Usage: run from Igor with parameters `zerox`, `zeroy`, `angle`, `length`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: zerox = Plot relative Ox; zeroy = Plot relative Oy; angle = axis angle; length = axis length. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Drawarrow()`.
- Parameters:
  - `zerox`: Plot relative Ox
  - `zeroy`: Plot relative Oy
  - `angle`: axis angle
  - `length`: axis length
- Source: `SmartDisplay.ipf:5507`

#### `ffc3dp`

```igor
Proc ffc3dp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Main internal calls: `ff3dp()`, `grabwinnonew()`.
- Source: `Smart_3D_Viewer_New.ipf:8258`

#### `getpeakfromwaterfallc`

```igor
Proc getpeakfromwaterfallc(l1,l2,ss)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `l1`, `l2`, `ss`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Dialog prompts: l1 = V(left); l2 = V(right); ss = name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `xcsr()`, `getpeakfromwaterfall2()`.
- Parameters:
  - `l1`: V(left)
  - `l2`: V(right)
  - `ss`: name
- Source: `Miscellaneous_Codes.ipf:10904`

#### `hshift`

```igor
proc hshift(mat, number,start,am,bm,colortype,Zfrom,Zto)
```
- Usage: run from Igor with parameters `mat`, `number`, `start`, `am`, `bm`, `colortype`, `Zfrom`, `Zto`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: mat = batch name; number = How many?; start = Start number; am = x offset; bm = y offset; colortype = Color Type. Code behavior: formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `offset()`, `zColor()`.
- Parameters:
  - `mat`: batch name
  - `number`: How many?
  - `start`: Start number
  - `am`: x offset
  - `bm`: y offset
  - `colortype`: Color Type
  - `Zfrom`: Zmin for color
  - `Zto`: Zmax for color
- Source: `Miscellaneous_Codes.ipf:8234`

#### `ini_Graph_imageforslicer`

```igor
Proc ini_Graph_imageforslicer(ctrlName) : ButtonControl
```
- Usage: run from Igor with parameters `ctrlName`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:26200`

#### `Intensity_digimage`

```igor
Proc Intensity_digimage(matname,whichcolor,invornot,colorpixels,method,disp_or_not)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `matname`, `whichcolor`, `invornot`, `colorpixels`, `method`, `disp_or_not`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: matname = Enter the name of the image; whichcolor = Which color:; colorpixels = Number of color pixels; method = Enter the minimization degree; disp_or_not = Do you want to display. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `CTablist()`, `color_digimage()`, `CTabList()`, `Interp()`, `Imatrix_digimage()`.
- Parameters:
  - `matname`: Enter the name of the image
  - `whichcolor`: Which color:
  - `invornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `colorpixels`: Number of color pixels
  - `method`: Enter the minimization degree
  - `disp_or_not`: Do you want to display
- Source: `Pierre's Template.ipf:6624`

#### `Intensity_digimage2`

```igor
Proc Intensity_digimage2(matname,colorwavename,v1,v2,invornot,colorpixels,method,disp_or_not)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `matname`, `colorwavename`, `v1`, `v2`, `invornot`, `colorpixels`, `method`, `disp_or_not`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: matname = Enter the name of the image; colorwavename = Color Wave Name; v1 = lowest value of color bar; v2 = highest value of color bar; colorpixels = Number of color pixels; method = Enter the minimization degree. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`, `color_digimage()`, `Interp()`, `Imatrix_digimag Usage: Update-log guidance - 08/15/2023: Improve the Intensity_digimage2(), now users can choose the value range of the color bar.
- Parameters:
  - `matname`: Enter the name of the image
  - `colorwavename`: Color Wave Name
  - `v1`: lowest value of color bar
  - `v2`: highest value of color bar
  - `invornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `colorpixels`: Number of color pixels
  - `method`: Enter the minimization degree
  - `disp_or_not`: Do you want to display
- Source: `Pierre's Template.ipf:6551`

#### `killags`

```igor
Proc killags()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Main internal calls: `KillAllGraphs()`.
- Source: `SmartDisplay.ipf:608`

#### `killalldisplay_pf`

```igor
proc killalldisplay_pf()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward.
- Source: `MultipeakforLinecut.ipf:1736`

#### `labelT`

```igor
Proc labelT()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:15693`

#### `layoutc4`

```igor
proc layoutc4(number,dis)
```
- Usage: run from Igor with parameters `number`, `dis`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `dis`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4575`

#### `layoutc4r`

```igor
proc layoutc4r(number,dis)
```
- Usage: run from Igor with parameters `number`, `dis`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `dis`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:3856`

#### `layoutdata`

```igor
proc layoutdata(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:4535`

#### `layoutdatar`

```igor
proc layoutdatar(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:3819`

#### `layoutmap`

```igor
proc layoutmap(select,matinput,times,box,size)
```
- Usage: run from Igor with parameters `select`, `matinput`, `times`, `box`, `size`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Dialog prompts: select = Mode; matinput = Name; times = Smooth times; Box = Smooth Box; Size = Map Size. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data. Main internal calls: `rot2d_pi2()`, `smoothmat_K2()`.
- Parameters:
  - `select`: Mode
  - `matinput`: Name
  - `times`: Smooth times
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `size`: Used in: SetScale/I x 0,size,"", $mat3 / SetScale/I y 0,size,"", $mat3
- Source: `Miscellaneous_Codes.ipf:6728`

#### `layoutqpi`

```igor
proc layoutqpi(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:4492`

#### `layoutqpir`

```igor
proc layoutqpir(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:3776`

#### `linenorm2pongraphc`

```igor
Proc linenorm2pongraphc(p1,p2)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `p1`, `p2`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: p1 = P_start; p2 = P_end. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `pcsr()`, `linenorm2pongraph()`.
- Parameters:
  - `p1`: P_start
  - `p2`: P_end
- Source: `Miscellaneous_Codes.ipf:10982`

#### `Load_Wisconsin_Table_Onegroup`

```igor
Macro Load_Wisconsin_Table_Onegroup(PATHName, FileName, waveBase, startNum, groupNum)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `PATHName`, `FileName`, `waveBase`, `startNum`, `groupNum`.
- Notes: Igor macro entry point for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output; loads data from files or paths. Main internal calls: `num2istr()`, `Matrix_To_Wave()`, `AppendOneRecord_2002()`.
- Parameters:
  - `PATHName`: File-system path or data-folder string used for loading or saving data.
  - `FileName`: File name or file-derived label used by the loader/export routine.
  - `waveBase`: Igor wave reference used as input or output.
  - `startNum`: Start point, index, or coordinate for the selected range.
  - `groupNum`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:17107`

#### `Load_Wisconsin_TableData`

```igor
Macro Load_Wisconsin_TableData(Wavebase, DataSetStartNum)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `Wavebase`, `DataSetStartNum`.
- Notes: Igor macro entry point for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: DataSetStartNum = From Which Number and Above, / Default is 1. Code behavior: loads data from files or paths; asks the user for parameters through Igor prompts. Main internal calls: `IndexedFile()`, `Load_Wisconsin_Table_Onegroup()`, `num2istr()`.
- Parameters:
  - `Wavebase`: Igor wave reference used as input or output.
  - `DataSetStartNum`: From Which Number and Above, / Default is 1
- Source: `Pierre's Template.ipf:17084`

#### `loadDA30_kly`

```igor
proc loadDA30_kly(thiswave, data,mode)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: thiswave = enter the name of 3DWave; data = enter the dataname; mode = Which kinds of 3D data?. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output; writes or exports data; creates, switches, or removes Igor data folders.
- Parameters:
  - `thiswave`: enter the name of 3DWave
  - `data`: enter the dataname
  - `mode`: Which kinds of 3D data?
- Source: `Miscellaneous_Codes.ipf:1195`

#### `maf`

```igor
proc maf()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data. Main internal calls: `pcsr()`, `qcsr()`, `normwave()`.
- Source: `Smart_3D_Viewer_New.ipf:9923`

#### `makeadd_data_table`

```igor
Proc makeadd_data_table()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:8947`

#### `MakeCutTable`

```igor
Proc MakeCutTable()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `winType()`, `wintype()`.
- Source: `Pierre's Template.ipf:27135`

#### `makegraphtable`

```igor
Proc makegraphtable()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `winType()`, `wintype()`.
- Source: `Pierre's Template.ipf:2125`

#### `MakeImageTable`

```igor
Proc MakeImageTable()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `winType()`, `wintype()`.
- Source: `Pierre's Template.ipf:27095`

#### `makevectorfieldc`

```igor
proc makevectorfieldc(name,amp)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `name`, `amp`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Dialog prompts: name = Phase map; amp = amplitude map. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `makevectorfield()`.
- Parameters:
  - `name`: Phase map
  - `amp`: amplitude map
- Source: `SmartDisplay.ipf:4444`

#### `Mk_nk_table_pi`

```igor
Proc Mk_nk_table_pi(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: number = Enter the number of the table. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `winType()`, `wintype()`.
- Parameters:
  - `number`: Enter the number of the table
- Source: `Pierre's Template.ipf:6198`

#### `Mk_vb_table_pi`

```igor
Proc Mk_vb_table_pi(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: number = Enter the number of the table. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `winType()`, `wintype()`.
- Parameters:
  - `number`: Enter the number of the table
- Source: `Pierre's Template.ipf:5694`

#### `Normalinecutc`

```igor
Proc Normalinecutc(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: INTERACTING FUNCTIONAL: Normalization or not Purpose: smooths, normalizes, or removes background/trend components. Main internal calls: `tpw()`, `Normalinecut()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:1557`

#### `Normalinecutc2`

```igor
Proc Normalinecutc2(name,sel)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`, `sel`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: name = The Name of Linecut; sel = Mode. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `NormalinecutX()`, `NormalinecutY()`.
- Parameters:
  - `name`: The Name of Linecut
  - `sel`: Mode
- Source: `Smart_3D_Viewer_New.ipf:1586`

#### `savegraph`

```igor
Proc savegraph(name,number)
```
- Purpose: captures a graph recreation command so the graph can be rebuilt later.
- Usage: run from Igor with parameters `name`, `number`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: name = Enter the name of the graph; number = Enter the resolution (1,2,4,5,8). Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the graph
  - `number`: Enter the resolution (1,2,4,5,8)
- Source: `Pierre's Template.ipf:7740`

#### `ShowGraph`

```igor
Proc ShowGraph(WaveName)
```
- Usage: run from Igor with parameters `WaveName`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `WaveName`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:1716`

#### `Table_Notthesepoints`

```igor
Proc Table_Notthesepoints()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `winType()`, `wintype()`.
- Source: `Pierre's Template.ipf:3633`


### Windows, panels, and menus

#### `AVG_graph`

```igor
Window AVG_graph() : Graph
```
- Usage: call `AVG_graph()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; reads or updates panel controls.
- Source: `Pierre's Template.ipf:12748`

#### `background_remover`

```igor
Window background_remover() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `background_remover()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:9221`

#### `background_remover_1`

```igor
Window background_remover_1() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `background_remover_1()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10658`

#### `Curv_Panel`

```igor
Window Curv_Panel() : Graph
```
- Usage: call `Curv_Panel()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:13597`

#### `Data_Analysis_BNL`

```igor
Window Data_Analysis_BNL() : Panel
```
- Usage: call `Data_Analysis_BNL()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; smooths wave data; loads data from files or paths; writes or exports data. Main internal calls: `txt()`, `dat()`, `asc()`, `tabLabel()`.
- Source: `Pierre's Template.ipf:155`

#### `dataploter`

```igor
Window dataploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `dataploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; smoothing, normalization, or background removal. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths.
- Source: `Pierre's Template.ipf:36`

#### `dataploter2`

```igor
Window dataploter2() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `dataploter2()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; smoothing, normalization, or background removal. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths.
- Source: `Miscellaneous_Codes.ipf:9644`

#### `dataplotersts`

```igor
Window dataplotersts() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `dataplotersts()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; smoothing, normalization, or background removal. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Miscellaneous_Codes.ipf:389`

#### `EMDC_multiDisplay`

```igor
Window EMDC_multiDisplay() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `EMDC_multiDisplay()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:11922`

#### `Graph3`

```igor
Window Graph3() : Graph
```
- Usage: call `Graph3()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays.
- Source: `Procedure.ipf:281`

#### `Graph_imageforslicer`

```igor
Window Graph_imageforslicer() : Graph
```
- Usage: call `Graph_imageforslicer()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24312`

#### `Graph_zoomforCsrSlicer`

```igor
Window Graph_zoomforCsrSlicer() : Graph
```
- Usage: call `Graph_zoomforCsrSlicer()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `mode()`, `marker()`, `lSize()`, `lStyle()`, `rgb()`, `msize()`.
- Source: `Pierre's Template.ipf:25707`

#### `Help_0_to_NaN`

```igor
Window Help_0_to_NaN() : Graph
```
- Usage: call `Help_0_to_NaN()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10832`

#### `Help_2nd_derivative`

```igor
Window Help_2nd_derivative() : Graph
```
- Usage: call `Help_2nd_derivative()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10842`

#### `Help_Append_pnts`

```igor
Window Help_Append_pnts() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Append_pnts()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11117`

#### `HELP_background_remover`

```igor
Window HELP_background_remover() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `HELP_background_remover()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:9248`

#### `help_bad_slices`

```igor
Window help_bad_slices() : Graph
```
- Usage: call `help_bad_slices()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; integrates wave data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10100`

#### `Help_Bkgndremover_Add_pnt`

```igor
Window Help_Bkgndremover_Add_pnt() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Bkgndremover_Add_pnt()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11330`

#### `Help_Bkgndremover_Bkgnd_in`

```igor
Window Help_Bkgndremover_Bkgnd_in() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Bkgndremover_Bkgnd_in()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11344`

#### `Help_bkgndremover_Delete_pnt`

```igor
Window Help_bkgndremover_Delete_pnt() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_bkgndremover_Delete_pnt()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11361`

#### `Help_bkgndremover_Save_backgnd`

```igor
Window Help_bkgndremover_Save_backgnd() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_bkgndremover_Save_backgnd()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11374`

#### `Help_bkgndremover_Save_result`

```igor
Window Help_bkgndremover_Save_result() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_bkgndremover_Save_result()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11388`

#### `Help_Bkgndremover_Wave_in`

```igor
Window Help_Bkgndremover_Wave_in() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Bkgndremover_Wave_in()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11315`

#### `Help_Change_stepsize`

```igor
Window Help_Change_stepsize() : Graph
```
- Usage: call `Help_Change_stepsize()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10861`

#### `Help_channel_spacing`

```igor
Window Help_channel_spacing() : Graph
```
- Usage: call `Help_channel_spacing()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10115`

#### `Help_Cmbdata`

```igor
Window Help_Cmbdata() : Graph
```
- Usage: call `Help_Cmbdata()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10126`

#### `Help_Combine_all`

```igor
Window Help_Combine_all() : Graph
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call `Help_Combine_all()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10883`

#### `Help_Combine_krange`

```igor
Window Help_Combine_krange() : Graph
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call `Help_Combine_krange()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10900`

#### `Help_Combine_range`

```igor
Window Help_Combine_range() : Graph
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call `Help_Combine_range()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10922`

#### `Help_Combine_this_data`

```igor
Window Help_Combine_this_data() : Graph
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call `Help_Combine_this_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10313`

#### `help_convert_all_data`

```igor
Window help_convert_all_data() : Graph
```
- Usage: call `help_convert_all_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10147`

#### `Help_Cvt_data`

```igor
Window Help_Cvt_data() : Graph
```
- Usage: call `Help_Cvt_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10326`

#### `Help_data`

```igor
Window Help_data() : Graph
```
- Usage: call `Help_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10337`

#### `Help_Data_ploter`

```igor
Window Help_Data_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Data_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10157`

#### `Help_Data_type`

```igor
Window Help_Data_type() : Graph
```
- Usage: call `Help_Data_type()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10196`

#### `Help_Delete_all`

```igor
Window Help_Delete_all() : Graph
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call `Help_Delete_all()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11132`

#### `Help_Delete_last`

```igor
Window Help_Delete_last() : Graph
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call `Help_Delete_last()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11148`

#### `Help_Divide_by_FD`

```igor
Window Help_Divide_by_FD() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Divide_by_FD()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10942`

#### `Help_Equiv_mat`

```igor
Window Help_Equiv_mat() : Graph
```
- Usage: call `Help_Equiv_mat()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10954`

#### `Help_FFT_list`

```igor
Window Help_FFT_list() : Graph
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call `Help_FFT_list()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10206`

#### `Help_Find_kF`

```igor
Window Help_Find_kF() : Graph
```
- Usage: call `Help_Find_kF()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:10552`

#### `Help_Initialize`

```igor
Window Help_Initialize() : Graph
```
- Usage: call `Help_Initialize()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10226`

#### `Help_Keep_minus`

```igor
Window Help_Keep_minus() : Graph
```
- Usage: call `Help_Keep_minus()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10978`

#### `Help_Keep_plus`

```igor
Window Help_Keep_plus() : Graph
```
- Usage: call `Help_Keep_plus()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10991`

#### `Help_Ln_mat`

```igor
Window Help_Ln_mat() : Graph
```
- Usage: call `Help_Ln_mat()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11004`

#### `Help_Load`

```igor
Window Help_Load() : Graph
```
- Usage: call `Help_Load()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10239`

#### `Help_Make_image_data_ploter`

```igor
Window Help_Make_image_data_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Make_image_data_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10390`

#### `Help_mat_exp_n`

```igor
Window Help_mat_exp_n() : Graph
```
- Usage: call `Help_mat_exp_n()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11014`

#### `Help_NaN0mat3d`

```igor
Window Help_NaN0mat3d() : Graph
```
- Usage: call `Help_NaN0mat3d()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11024`

#### `Help_NaN_to_0`

```igor
Window Help_NaN_to_0() : Graph
```
- Usage: call `Help_NaN_to_0()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11035`

#### `Help_New_band`

```igor
Window Help_New_band() : Graph
```
- Usage: call `Help_New_band()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11164`

#### `Help_NK`

```igor
Window Help_NK() : Graph
```
- Usage: call `Help_NK()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10424`

#### `Help_Norm`

```igor
Window Help_Norm() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Norm()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10447`

#### `Help_Norm_matrix`

```igor
Window Help_Norm_matrix() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Norm_matrix()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11045`

#### `Help_Not_these_points`

```igor
Window Help_Not_these_points() : Graph
```
- Usage: call `Help_Not_these_points()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11055`

#### `Help_num_of_band`

```igor
Window Help_num_of_band() : Graph
```
- Usage: call `Help_num_of_band()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11180`

#### `Help_Open_data`

```igor
Window Help_Open_data() : Graph
```
- Usage: call `Help_Open_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10254`

#### `Help_Open_select`

```igor
Window Help_Open_select() : Graph
```
- Usage: call `Help_Open_select()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10284`

#### `Help_Open_this_data`

```igor
Window Help_Open_this_data() : Graph
```
- Usage: call `Help_Open_this_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10459`

#### `Help_Plot_Point_reader`

```igor
Window Help_Plot_Point_reader() : Graph
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `Help_Plot_Point_reader()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11198`

#### `Help_Point_reader`

```igor
Window Help_Point_reader() : Graph
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `Help_Point_reader()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11088`

#### `Help_Point_reader_Show_table`

```igor
Window Help_Point_reader_Show_table() : Graph
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `Help_Point_reader_Show_table()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11241`

#### `Help_Point_reader_Sort`

```igor
Window Help_Point_reader_Sort() : Graph
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `Help_Point_reader_Sort()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11257`

#### `Help_raw_data`

```igor
Window Help_raw_data() : Graph
```
- Notes: ????????????????????????????????????????????????????????????????????????????????????????????????????? ????????????????????????????????????????????????????????????????????????????????????????????????????? ???????????????????????????????????????????????????????? Usage: call `Help_raw_data()` to recreate or bring up the Igor window/panel. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:9378`

#### `Help_Read_point`

```igor
Window Help_Read_point() : Graph
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `Help_Read_point()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11216`

#### `Help_Remove_background_Dploter`

```igor
Window Help_Remove_background_Dploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Remove_background_Dploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; loads data from files or paths; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10472`

#### `Help_Rotate_image`

```igor
Window Help_Rotate_image() : Graph
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call `Help_Rotate_image()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for Nanonis/STM data loading or map conversion; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11272`

#### `Help_Smooth_factor`

```igor
Window Help_Smooth_factor() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Smooth_factor()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10297`

#### `Help_Smooth_mat`

```igor
Window Help_Smooth_mat() : Graph
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `Help_Smooth_mat()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11287`

#### `Help_tab_matrices`

```igor
Window Help_tab_matrices() : Graph
```
- Usage: call `Help_tab_matrices()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10707`

#### `Help_tab_Misc`

```igor
Window Help_tab_Misc() : Graph
```
- Usage: call `Help_tab_Misc()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10811`

#### `Help_tab_Presentation`

```igor
Window Help_tab_Presentation() : Graph
```
- Usage: call `Help_tab_Presentation()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10785`

#### `help_tab_traces`

```igor
Window help_tab_traces() : Graph
```
- Usage: call `help_tab_traces()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10759`

#### `Help_Tools`

```igor
Window Help_Tools() : Graph
```
- Usage: call `Help_Tools()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10685`

#### `Mat3d_slicer`

```igor
Window Mat3d_slicer() : Graph
```
- Usage: call `Mat3d_slicer()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; linecut, slice, or region extraction; smoothing, normalization, or background removal. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `margin()`.
- Source: `Pierre's Template.ipf:24595`

#### `movie_maker`

```igor
Window movie_maker(): Graph
```
- Notes: NOTE: Global variables are stored in a new data folder called "movie_maker" The data folder that is created can cause problems when loading data because of the way Pierre's macro loads data. The data folder MUST be killed before any more data is loaded. If you Usage: call `movie_maker()` to recreate or bring up the Igor window/panel. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; creates, switches, or removes Igor data folders.
- Source: `Pierre's Template.ipf:36350`

#### `movie_maker_startup_warning`

```igor
Window movie_maker_startup_warning() : Graph
```
- Usage: call `movie_maker_startup_warning()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:36702`

#### `Point_reader`

```igor
Window Point_reader() : Panel
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `Point_reader()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; reads or updates panel controls.
- Source: `Pierre's Template.ipf:64`

#### `predict_cuts`

```igor
Window predict_cuts() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `predict_cuts()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `lSize()`, `lStyle()`, `rgb()`, `lblMargin()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:24218`

#### `TransferGraph`

```igor
Menu "TransferGraph"
```
- Usage: defines Igor menu entries; users normally access it from the Igor menu bar.
- Notes: Igor menu definition for graph display, formatting, or window management.
- Source: `transfergraph.ipf:110`


### Computational and data helpers

#### `arrowcolortype_vf`

```igor
Function arrowcolortype_vf(sel)
```
- Usage: call as `arrowcolortype_vf(sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `zColor()`.
- Parameters:
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:4695`

#### `autoallfigure`

```igor
Function autoallfigure()
```
- Usage: call `autoallfigure()` from Igor procedure code or the command line.
- Notes: Function for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Update log: 08/11-03/2023: Added autoallfigure()which can perform some command going through all the graphs. Modified 2DSTS, apply tile.
- Source: `SmartDisplay.ipf:1198`

#### `autoremovejump1D`

```igor
Function autoremovejump1D(name,mode,valueif2,givencriteria)
```
- Usage: call as `autoremovejump1D(name, mode, valueif2, givencriteria)`.
- Notes: #3 Excellent 1D jump remover Purpose: removes waves/windows/data points or cleans intermediate state. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `median()`, `sign()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `mode`: 1 for auto, 2 for specified value
  - `valueif2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `givencriteria`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:3968`

#### `ave_inmarque`

```igor
Function ave_inmarque()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call `ave_inmarque()` from Igor procedure code or the command line.
- Notes: Function for Igor wave/matrix/cube data operation. Main internal calls: `tpw()`. Update log: 05/10/2024: Added ave_inmarque() to the popup window of Frommarqueegetsubmatrixs(name) which calculates the average value on the partial image (only count the non-NaN value).
- Source: `Smart_3D_Viewer_New.ipf:9893`

#### `checkmultiopen`

```igor
Function checkmultiopen(num)
```
- Notes: #5 check if there is a window with 12 subwindow Usage: call as `checkmultiopen(num)`. Main internal calls: `childWindowList()`. Update log: 08/23/2023: (2) checkmultiopen() to check if there are a graph with specific number of subwindow exist.
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Smart_3D_Viewer_New.ipf:3695`

#### `ckfig`

```igor
Function ckfig(name)
```
- Notes: How to use this function: in a Proc or Function, put it direction below the sentence of made a new figure and call "ckfig(winname(0,1))" Note: only complex figures which contain multiples waves need to use these functions, for simple figure which only have one Usage: call as `ckfig(name)`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Killsinglewindow()`. Update log: 07/09-03/2023: Add function ckfig() and cklayout(), which can kill redundant figure and layout, this should be insert in the function or procedure where new figure or layout displayed, in the form of ckfig(winname(0,1)) and cklayout(winname(0,4)), this can automatically display without repeat.
- Parameters:
  - `name`: name of the figure
- Source: `SmartDisplay.ipf:13`

#### `ckfig_child`

```igor
Function ckfig_child(name)
```
- Usage: call as `ckfig_child(name)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `wavelistsub()`, `childWindowList()`, `Killsinglewindow()`. Update log: 2023-08-25: Checks whether a multi-subwindow graph already exists, mirroring the `ckfig` duplicate-window pattern for child-window displays.
- Parameters:
  - `name`: name of the figure
- Source: `SmartDisplay.ipf:54`

#### `cklayout`

```igor
Function cklayout(name)
```
- Notes: Eliminate duplicating display Layout How to use this function: in a Proc or Function, put it direction below the sentence of made a new figure and call "ck(winname(0,4))" Usage: call as `cklayout(name)`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `graphlistinlayout()`, `Killsinglewindow()`. Update log: 07/09-03/2023: Add function ckfig() and cklayout(), which can kill redundant figure and layout, this should be insert in the function or procedure where new figure or layout displayed, in the form of ckfig(winname(0,1)) and cklayout(winname(0,4)), this can automatically display without repeat.
- Parameters:
  - `name`: name of the figure
- Source: `SmartDisplay.ipf:134`

#### `cktable`

```igor
Function cktable(name)
```
- Notes: How to use this function: in a Proc or Function, put it direction below the sentence of made a new figure and call "ckfig(winname(0,2))" Note: only complex figures which contain multiples waves need to use these functions, for simple figure which only have one Usage: call as `cktable(name)`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Killsinglewindow()`. Update log: 07/13-02/2023: Added cktable() which can kill redundant table, this should be insert in the function or procedure where new table displayed, in the form of cktable(winname(0,2)), this can automatically display without repeat.
- Parameters:
  - `name`: name of the figure
- Source: `SmartDisplay.ipf:95`

#### `color3s`

```igor
Function color3s(name,tt)
```
- Notes: Function can be called in a procedure (3*sigma) Usage: call as `color3s(name, tt)`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:730`

#### `color3s_for3d`

```igor
Function color3s_for3d(name,tt)
```
- Notes: ## 02 Special Color Range Usage: call as `color3s_for3d(name, tt)`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinnonew()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2219`

#### `color3s_for3dFFT`

```igor
Function color3s_for3dFFT(name,tt)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `color3s_for3dFFT(name, tt)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinnonew()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2246`

#### `color3s_for3dinv`

```igor
Function color3s_for3dinv(name,tt)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `color3s_for3dinv(name, tt)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinnonew()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2277`

#### `color3s_for3dinvqpirange`

```igor
Function color3s_for3dinvqpirange(name,tt)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `color3s_for3dinvqpirange(name, tt)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:12304`

#### `color3s_for3dm`

```igor
Function color3s_for3dm(name,tt)
```
- Notes: #4 Special Color Range for childwindows Usage: call as `color3s_for3dm(name, tt)`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild()`. Update log: 08/23/2023: (4) New image color scale function color3s_for3dm(name,tt), for the wave at a subwindow.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:3665`

#### `color3s_for3dm2`

```igor
Function color3s_for3dm2(name,tt)
```
- Usage: call as `color3s_for3dm2(name, tt)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild2()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:9555`

#### `color3s_for3dmf`

```igor
Function color3s_for3dmf(name,tt)
```
- Usage: call as `color3s_for3dmf(name, tt)`.
- Notes: Function for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lattice Segregation.ipf:1582`

#### `color3s_for3dmLS`

```igor
Function color3s_for3dmLS(name,tt)
```
- Usage: call as `color3s_for3dmLS(name, tt)`.
- Notes: Function for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `grabwinchild()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lattice Segregation.ipf:1609`

#### `color3s_subfor3dFFT`

```igor
Function color3s_subfor3dFFT(name,tt)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `color3s_subfor3dFFT(name, tt)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `mean()`, `gethistgram_npcolor()`, `grabwinchild()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:1150`

#### `color3sfft`

```igor
Function color3sfft(name,tt)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `color3sfft(name, tt)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:705`

#### `colormatmorevv`

```igor
Function colormatmorevv()
```
- Usage: call `colormatmorevv()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `CtabList()`.
- Source: `SmartDisplay.ipf:1133`

#### `colormatmorevv_ind`

```igor
Function colormatmorevv_ind()
```
- Usage: call `colormatmorevv_ind()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `imageinfo()`, `tpw()`, `CtabList()`.
- Source: `SmartDisplay.ipf:4404`

#### `colormatmorevvline`

```igor
Function colormatmorevvline()
```
- Usage: call `colormatmorevvline()` from Igor procedure code or the command line.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `WaveName()`, `interp()`, `rgb()`, `CTabList()`, `CtabList()`.
- Source: `SmartDisplay.ipf:996`

#### `comparehotandcold`

```igor
Function comparehotandcold(name,prefwave,thresh)
```
- Usage: call as `comparehotandcold(name, prefwave, thresh)`.
- Notes: Function for smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `Selnumavests_p()`, `Selnumavests_n()`, `rgb()`, `axRGB()`, `tlblRGB()`, `alblRGB()`.
- Parameters:
  - `name`: 3D wave the STS come from
  - `prefwave`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `thresh`: threshold for defining a lattice position (max=1)
- Source: `Smart_3D_Viewer_New.ipf:10094`

#### `Complextoimag`

```igor
Function Complextoimag(name1w)
```
- Usage: call as `Complextoimag(name1w)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
- Source: `Smart_3D_Viewer_New.ipf:5910`

#### `Complextorealf13dp`

```igor
Function Complextorealf13dp(name1w,select)
```
- Usage: call as `Complextorealf13dp(name1w, select)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `Smart_3D_Viewer_New.ipf:8302`

#### `Complextorealf13dpls`

```igor
Function Complextorealf13dpls(name1w,select)
```
- Usage: call as `Complextorealf13dpls(name1w, select)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `Smart_3D_Viewer_New.ipf:9248`

#### `Complextorealf_3dplot`

```igor
Function Complextorealf_3dplot(name1w)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Complextorealf_3dplot(name1w)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
- Source: `Smart_3D_Viewer_New.ipf:1207`

#### `Complextorealf_for3d`

```igor
Function Complextorealf_for3d(name1w,select)
```
- Usage: call as `Complextorealf_for3d(name1w, select)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `Smart_3D_Viewer_New.ipf:2200`

#### `Cons3dplotc`

```igor
Function Cons3dplotc()
```
- Usage: call `Cons3dplotc()` from Igor procedure code or the command line.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Update layer }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_zeroNaN()`, `max()`, `grabwinchild()`, `color3s_for3d()`, `checkmultiopen()`, `color3s_for3dm()`.
- Source: `Smart_3D_Viewer_New.ipf:545`

#### `Cons3dplotcf`

```igor
Function Cons3dplotcf()
```
- Usage: call `Cons3dplotcf()` from Igor procedure code or the command line.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Update layer }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `color3s_for3dinv()`.
- Source: `Smart_3D_Viewer_New.ipf:6177`

#### `ConsZcuiz`

```igor
Function ConsZcuiz()
```
- Usage: call `ConsZcuiz()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `maxwaterfall()`, `minwaterfall()`, `CtabList()`, `CTabList()`.
- Source: `SmartDisplay.ipf:1598`

#### `cyclecolorwave`

```igor
Function cyclecolorwave(c,index)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `cyclecolorwave(c, index)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `CtabList()`. Update log: 2024-01-29: Creates cyclic color waves with an arbitrary number of periods; `c` is the number of cycles and `index` selects the base color-table entry.
- Parameters:
  - `c`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:5080`

#### `d`

```igor
Function d(name)
```
- Usage: call as `d(name)`. Update-log guidance - 11/15/2023: Added New image Displayer, now users can call it by d().
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`, `margin()`, `CtabList()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `SmartDisplay.ipf:4092`

#### `di`

```igor
Function di(destw)
```
- Usage: call as `di(destw)`.
- Notes: ##Auto display figure## Display mode 1: When there already a graph with the same single wave Stop display and bring that window to front Purpose: displays waves, images, contours, or graph overlays. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `listMatch()`. Update log: 07/13-02/2023: Added smart single wave table display ed() and ed2(), same as di() and di2(), mode 1 is check if there already displayed if so stop display, and mode 2 is kill the displayed window and display new.
- Parameters:
  - `destw`: Igor wave reference used as input or output.
- Source: `SmartDisplay.ipf:410`

#### `di2`

```igor
Function di2(name)
```
- Usage: call as `di2(name)`.
- Notes: Display mode 2: kill the graph with same single wave and redisplay Purpose: displays waves, images, contours, or graph overlays. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `killgraphonlyhavethewave()`. Update log: 07/13-02/2023: Added smart single wave table display ed() and ed2(), same as di() and di2(), mode 1 is check if there already displayed if so stop display, and mode 2 is kill the displayed window and display new.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `SmartDisplay.ipf:447`

#### `di2lf`

```igor
Function di2lf(name)
```
- Usage: call as `di2lf(name)`.
- Notes: End display Purpose: displays waves, images, contours, or graph overlays. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `killgraphonlyhavethewave()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `SmartDisplay.ipf:494`

#### `dic`

```igor
Function dic(destw)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `dic(destw)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `listMatch()`. Update log: 08/11-03/2023: Added dic() to smart display a single 1D wave.
- Parameters:
  - `destw`: Igor wave reference used as input or output.
- Source: `SmartDisplay.ipf:1216`

#### `dichis`

```igor
Function dichis(destw)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `dichis(destw)`.
- Notes: Function for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `listMatch()`.
- Parameters:
  - `destw`: Igor wave reference used as input or output.
- Source: `SmartDisplay.ipf:2197`

#### `dilf`

```igor
Function dilf(destw)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `dilf(destw)`.
- Notes: Function for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `listMatch()`.
- Parameters:
  - `destw`: Igor wave reference used as input or output.
- Source: `SmartDisplay.ipf:507`

#### `displaymultiF`

```igor
Function  displaymultiF(dataname,from,to)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `displaymultiF(dataname, from, to)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `Constantoffset_nF()`.
- Parameters:
  - `dataname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5071`

#### `displaymultiFedc`

```igor
Function  displaymultiFedc(dataname,from,to)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `displaymultiFedc(dataname, from, to)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Parameters:
  - `dataname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:3337`

#### `do_movie_maker_image`

```igor
function do_movie_maker_image()
```
- Usage: call `do_movie_maker_image()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: reshapes existing waves; assigns Igor axis scaling/units to the output. Main internal calls: `elseif()`.
- Source: `Pierre's Template.ipf:36434`

#### `Dopolarfig`

```igor
Function Dopolarfig(datay,datax,sel)
```
- Usage: call as `Dopolarfig(datay, datax, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `lsize()`, `rgb()`, `mode()`, `lstyle()`, `mrkThick()`, `useMrkStrokeRGB()`.
- Parameters:
  - `datay`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `datax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:3594`

#### `Dopolarfig_nondis`

```igor
Function Dopolarfig_nondis(datay,datax,sel)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Dopolarfig_nondis(datay, datax, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `datay`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `datax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:3681`

#### `Drawarrow`

```igor
Function Drawarrow(zerox,zeroy,angle,length)
```
- Usage: call as `Drawarrow(zerox, zeroy, angle, length)`.
- Notes: Function for graph display, formatting, or window management. Main internal calls: `Drawarrowx()`, `Drawarrowy()`, `Drawarrowa()`, `Drawarrowb()`. Update log: 2024-05-03: Draws x/y and a/b lattice-direction arrows on the active graph from an origin, angle, and length.
- Parameters:
  - `zerox`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `zeroy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `length`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:5520`

#### `Drawarrowa`

```igor
Function Drawarrowa(zerox,zeroy,angle,length)
```
- Usage: call as `Drawarrowa(zerox, zeroy, angle, length)`.
- Notes: Function for graph display, formatting, or window management.
- Parameters:
  - `zerox`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `zeroy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `length`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:5583`

#### `Drawarrowb`

```igor
Function Drawarrowb(zerox,zeroy,angle,length)
```
- Usage: call as `Drawarrowb(zerox, zeroy, angle, length)`.
- Notes: Function for graph display, formatting, or window management.
- Parameters:
  - `zerox`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `zeroy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `length`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:5607`

#### `Drawarrowx`

```igor
Function Drawarrowx(zerox,zeroy,angle,length)
```
- Usage: call as `Drawarrowx(zerox, zeroy, angle, length)`.
- Notes: Function for graph display, formatting, or window management.
- Parameters:
  - `zerox`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `zeroy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `length`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:5533`

#### `Drawarrowy`

```igor
Function Drawarrowy(zerox,zeroy,angle,length)
```
- Usage: call as `Drawarrowy(zerox, zeroy, angle, length)`.
- Notes: Function for graph display, formatting, or window management.
- Parameters:
  - `zerox`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `zeroy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `length`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:5559`

#### `duplicateongraph`

```igor
Function duplicateongraph()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: call `duplicateongraph()` from Igor procedure code or the command line.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Source: `SmartDisplay.ipf:1314`

#### `ed`

```igor
Function ed(destw)
```
- Usage: call as `ed(destw)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `listMatch()`. Update log: 07/13-02/2023: Added smart single wave table display ed() and ed2(), same as di() and di2(), mode 1 is check if there already displayed if so stop display, and mode 2 is kill the displayed window and display new.
- Parameters:
  - `destw`: Igor wave reference used as input or output.
- Source: `SmartDisplay.ipf:573`

#### `ed2`

```igor
Function ed2(name)
```
- Notes: Smart edit table for single wave table Usage: call as `ed2(name)`. Main internal calls: `killgraphonlyhavethewavetable()`. Update log: 07/13-02/2023: Added smart single wave table display ed() and ed2(), same as di() and di2(), mode 1 is check if there already displayed if so stop display, and mode 2 is kill the displayed window and display new.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `SmartDisplay.ipf:547`

#### `f_for3d`

```igor
Function f_for3d()
```
- Notes: ## 01 Special FFT Usage: call `f_for3d()` from Igor procedure code or the command line. Code behavior: performs Fourier-transform or inverse-transform operations. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf_for3d()`, `FTguassianremover()`.
- Source: `Smart_3D_Viewer_New.ipf:2181`

#### `f_for3dmulti`

```igor
Function f_for3dmulti(name)
```
- Notes: #2 FFT by name Usage: call as `f_for3dmulti(name)`. Code behavior: performs Fourier-transform or inverse-transform operations. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf_for3d()`. Update log: 08/23/2023: (5) New FFT function, that do magnitude FFT by a give name f_for3dmulti(name).
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:3625`

#### `ff3dp`

```igor
Function ff3dp(name)
```
- Usage: call as `ff3dp(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf13dp()`, `twoDinterpolatexyFFT3dp()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:8278`

#### `ff3dpls`

```igor
Function ff3dpls(name,sel)
```
- Usage: call as `ff3dpls(name, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf13dpls()`, `twoDinterpolatexyFFT3dpls()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:9213`

#### `ff_3dplot`

```igor
Function ff_3dplot(name)
```
- Usage: call as `ff_3dplot(name)`.
- Notes: #0_2 PreFFT: Main Purpose: displays waves, images, contours, or graph overlays. Code behavior: performs Fourier-transform or inverse-transform operations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf1()`, `twoDinterpolatexyFFT_3dplot()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:4516`

#### `ff_3dplotn`

```igor
Function ff_3dplotn(name)
```
- Usage: call as `ff_3dplotn(name)`.
- Notes: #2_04_01 Extract Shearing: Main called Interoplate FFT without display Purpose: displays waves, images, contours, or graph overlays. Code behavior: performs Fourier-transform or inverse-transform operations; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf1()`, `twoDinterpolatexyFFT_3dplotn()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:5436`

#### `findendpp`

```igor
Function findendpp(mat,angle,Zn,addY)
```
- Notes: ## 03 Determine the Last P for assignment loop Usage: call as `findendpp(mat, angle, Zn, addY)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:1827`

#### `findendppf`

```igor
Function findendppf(mat,angle,Zn,addY)
```
- Notes: ## 03 Determine the Last P for assignment loop Usage: call as `findendppf(mat, angle, Zn, addY)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:6822`

#### `findendqq`

```igor
Function findendqq(mat,angle,Zn,addX)
```
- Notes: ## 03 Determine the Last Q for assignment loop Usage: call as `findendqq(mat, angle, Zn, addX)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2079`

#### `findendqqf`

```igor
Function findendqqf(mat,angle,Zn,addX)
```
- Notes: ## 03 Determine the Last Q for assignment loop Usage: call as `findendqqf(mat, angle, Zn, addX)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:7090`

#### `findstartpp`

```igor
Function findstartpp(mat,angle,Zn,addY)
```
- Notes: ## 02 Determine the First P for assignment loop Usage: call as `findstartpp(mat, angle, Zn, addY)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:1782`

#### `findstartppf`

```igor
Function findstartppf(mat,angle,Zn,addY)
```
- Notes: ## 02 Determine the First P for assignment loop Usage: call as `findstartppf(mat, angle, Zn, addY)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:6777`

#### `findstartqq`

```igor
Function findstartqq(mat,angle,Zn,addX)
```
- Notes: ## 02 Determine the First Q for assignment loop Usage: call as `findstartqq(mat, angle, Zn, addX)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2035`

#### `findstartqqf`

```igor
Function findstartqqf(mat,angle,Zn,addX)
```
- Notes: ## 02 Determine the First Q for assignment loop Usage: call as `findstartqqf(mat, angle, Zn, addX)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:7046`

#### `FTgremover_3dploty`

```igor
Function FTgremover_3dploty(name,sigma)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `FTgremover_3dploty(name, sigma)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `Gauss1D()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `sigma`: Width or sigma parameter for smoothing, fitting, or Fourier filtering.
- Source: `Smart_3D_Viewer_New.ipf:1219`

#### `getall2dwaveongraph`

```igor
Function/S getall2dwaveongraph()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `getall2dwaveongraph()` from Igor procedure code or the command line.
- Notes: String-returning helper for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `waveDims()`. Update log: 01/22/2024: Added getall2dwaveongraph(), which can return the 2D wave name on the top graph, use this function correct the get A and get B of FFT engineer. Added LF to move Q modes.
- Source: `FFT.ipf:2418`

#### `gethistgram`

```igor
Function gethistgram()
```
- Usage: call `gethistgram()` from Igor procedure code or the command line.
- Notes: Procedure for Auto Histogram Purpose: extracts values, metadata, cursor information, or derived waves. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `median()`, `dichis()`. Update log: 08/11-03/2023: Added Auto Histogram Conshist(), the single function is gethistgram().
- Source: `SmartDisplay.ipf:2156`

#### `gethistgram_np`

```igor
Function gethistgram_np()
```
- Usage: call `gethistgram_np()` from Igor procedure code or the command line.
- Notes: End continuely histogram parameters Purpose: extracts values, metadata, cursor information, or derived waves. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `median()`.
- Source: `SmartDisplay.ipf:2362`

#### `gethistgram_npcolor`

```igor
Function gethistgram_npcolor(name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `gethistgram_npcolor(name)`.
- Notes: Utility function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `median()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `SmartDisplay.ipf:756`

#### `gethistgramcons`

```igor
Function gethistgramcons(binnum,coverratio)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `gethistgramcons(binnum, coverratio)`.
- Notes: Utility function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; extracts image/wave statistics. Main internal calls: `tpw()`, `median()`.
- Parameters:
  - `binnum`: > 100 to 1000
  - `coverratio`: (0,1]
- Source: `SmartDisplay.ipf:2266`

#### `gethistgramhowmanysigma`

```igor
Function gethistgramhowmanysigma(aa)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `gethistgramhowmanysigma(aa)`.
- Notes: Utility function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `median()`.
- Parameters:
  - `aa`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:2302`

#### `getpeakfromwaterfall`

```igor
Function getpeakfromwaterfall(left,right,ss)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getpeakfromwaterfall(left, right, ss)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `matusew()`.
- Parameters:
  - `left`: Used in: CurveFit/Q/N=2/W=2 gauss, matusew(left,right)/D=$ddd
  - `right`: Used in: CurveFit/Q/N=2/W=2 gauss, matusew(left,right)/D=$ddd
  - `ss`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10865`

#### `getpeakfromwaterfall2`

```igor
Function getpeakfromwaterfall2(left,right,ss)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getpeakfromwaterfall2(left, right, ss)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `matusew()`, `ckfig()`.
- Parameters:
  - `left`: Used in: CurveFit/Q/N=2/W=2 gauss, matusew(left,right)/D=$ddd
  - `right`: Used in: CurveFit/Q/N=2/W=2 gauss, matusew(left,right)/D=$ddd
  - `ss`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10914`

#### `getratiohis`

```igor
Function getratiohis()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `getratiohis()` from Igor procedure code or the command line.
- Notes: Utility function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `median()`.
- Source: `SmartDisplay.ipf:2328`

#### `getslicerDataf`

```igor
Function getslicerDataf(name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Makegraphtable()`.
- Notes: Utility function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; reshapes existing waves; assigns Igor axis scaling/units to the output; creates, switches, or removes Igor data folders.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:9781`

#### `grabtable`

```igor
Function/s grabtable(name)
```
- Usage: call as `grabtable(name)`.
- Notes: return the table name of the selected data if the data with the name exist, and did not on a display, the data will be displayed and return the name of the newest window. Main internal calls: `ed()`. Update log: 07/20/2023: Added grabtable() which can get the table name of a specific wave, different from grabwin() which can only count the first element at the fig, the grabtable() search among all the element at a fig.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `SmartDisplay.ipf:867`

#### `grabtablenonew`

```igor
Function/s grabtablenonew(name)
```
- Usage: call as `grabtablenonew(name)`.
- Notes: String-returning helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `MultipeakforLinecut.ipf:1757`

#### `grabwin`

```igor
Function/s grabwin(name)
```
- Usage: call as `grabwin(name)`.
- Notes: return the win name of the selected data if the data with the name exist, and did not on a display, the data will be displayed and return the name of the newest window. Main internal calls: `di()`. Update log: 08/23/2023: (3) New Grabwin function grabwinchild(nameofwave(name)) for subwindow, this will only check the wave appear at a subwindow, if the wave also displayed in a main graph, this function will ignore that, for that case please use grabwin(nameofwave(name)) and related variants. 07/20/2023: Added grabtable() which can get the table name of a specific wave, different from grabwin() which can only count the first element at the fig, the grabtable() search among all the element at a fig.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `SmartDisplay.ipf:806`

#### `grabwin2`

```igor
Function/s grabwin2(name)
```
- Usage: call as `grabwin2(name)`.
- Notes: String-returning helper for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `di2()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `SmartDisplay.ipf:832`

#### `grabwinchild`

```igor
Function/s grabwinchild(name)
```
- Notes: #3 Grab chidwindowname of a 2D wave Usage: call as `grabwinchild(name)`. Main internal calls: `childWindowList()`. Update log: 08/23/2023: (3) New Grabwin function grabwinchild(nameofwave(name)) for subwindow, this will only check the wave appear at a subwindow, if the wave also displayed in a main graph, this function will ignore that, for that case please use grabwin(nameofwave(name)) and related variants.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:3635`

#### `grabwinchild2`

```igor
Function/s grabwinchild2(name)
```
- Usage: call as `grabwinchild2(name)`.
- Notes: String-returning helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Main internal calls: `childWindowList()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:9581`

#### `grabwinnonew`

```igor
Function/s grabwinnonew(name)
```
- Notes: ## 03 Special Grabwindown with open new Usage: call as `grabwinnonew(name)`. Update log: 08/17/2023: New functions mounted, Normalinecut(name) to normalize 2D linecut along x direction. sumoned(dest,orin,zn) to calculate averaged Z curve from a 3D matrix. grabwinnonew(name) can grab the winname of a wave without display a new window.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:2307`

#### `graphlistinlayout`

```igor
Function/T graphlistinlayout(name)
```
- Usage: call as `graphlistinlayout(name)`.
- Notes: Function for graph display, formatting, or window management. Main internal calls: `layoutinfo()`, `str2num()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `SmartDisplay.ipf:156`

#### `KillAllGraphs`

```igor
Function KillAllGraphs()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call `KillAllGraphs()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward.
- Source: `SmartDisplay.ipf:613`

#### `killgraphonlyhavethewave`

```igor
Function killgraphonlyhavethewave(dest)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `killgraphonlyhavethewave(dest)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `listMatch()`, `Killsinglewindow()`.
- Parameters:
  - `dest`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:464`

#### `killgraphonlyhavethewavetable`

```igor
Function killgraphonlyhavethewavetable(dest)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `killgraphonlyhavethewavetable(dest)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `listMatch()`, `Killsinglewindow()`.
- Parameters:
  - `dest`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:553`

#### `Killsinglewindow`

```igor
Function Killsinglewindow(name) //kill window of a specific name
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `Killsinglewindow(name)`.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: window name
- Source: `SmartDisplay.ipf:484`

#### `KP_EnsureNewColorTables`

```igor
Function KP_EnsureNewColorTables()
```
- Usage: call `KP_EnsureNewColorTables()` from Igor procedure code or the command line.
- Notes: Public entry point used by Kong_Igor_panel(). It restores the color waves that were embedded in the old template experiment. Code behavior: creates, switches, or removes Igor data folders. Main internal calls: `KP_HaveNewColorTables()`, `SpecialDirPath()`, `KP_LoadNewColorTablesFrom()`.
- Source: `KP_ColorTables.ipf:10`

#### `levelimage2`

```igor
Function levelimage2(name,sel)
```
- Usage: call as `levelimage2(name, sel)`.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `sel`: Used in: CurveFit/Q/W=0 Poly sel, figpara1D /D = figpara1Dfit
- Source: `SmartDisplay.ipf:2487`

#### `levelimage2_2D`

```igor
Function levelimage2_2D(name,sel)
```
- Usage: call as `levelimage2_2D(name, sel)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `sel`: Used in: CurveFit/Q/W=0 poly2D sel, name/D = destfitw
- Source: `SmartDisplay.ipf:2473`

#### `linenorm2pongraph`

```igor
Function linenorm2pongraph(p1,p2)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `linenorm2pongraph(p1, p2)`.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `linenorm2pf()`.
- Parameters:
  - `p1`: Start point, index, or coordinate for the selected range.
  - `p2`: End point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:10991`

#### `Lineprofilefromcircle`

```igor
Function Lineprofilefromcircle()
```
- Notes: #3_07 Extract line profile by Circular Mode {Note: this function will be called every time cursor moves} Usage: call `Lineprofilefromcircle()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `qcsr()`, `tpw()`, `autoremovejump1D()`, `lsize()`, `rgb()`.
- Source: `SmartDisplay.ipf:3410`

#### `Lineprofilefromcircle3d`

```igor
Function Lineprofilefromcircle3d()
```
- Notes: #3_07 Extract line profile by Circular Mode {Note: this function will be called every time cursor moves} Usage: call `Lineprofilefromcircle3d()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `qcsr()`, `grabwin()`, `lsize()`, `rgb()`, `mode()`.
- Source: `Smart_3D_Viewer_New.ipf:3273`

#### `Lineprofilefromcircle3df`

```igor
Function Lineprofilefromcircle3df()
```
- Notes: #3_07 Extract line profile by Circular Mode {Note: this function will be called every time cursor moves} Usage: call `Lineprofilefromcircle3df()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `qcsr()`, `lsize()`, `rgb()`, `mode()`, `lstyle()`.
- Source: `Smart_3D_Viewer_New.ipf:8088`

#### `LoadGraph`

```igor
Function LoadGraph()
```
- Purpose: rebuilds a saved graph from stored recreation/procedure text.
- Usage: call `LoadGraph()` from Igor procedure code or the command line.
- Notes: Utility function for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; writes or exports data; creates, switches, or removes Igor data folders. Main internal calls: `GetScrapText()`, `RemoveSingleQuote()`, `RebuildAppendCmd()`.
- Source: `transfergraph.ipf:44`

#### `make_movie`

```igor
Function make_movie()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call `make_movie()` from Igor procedure code or the command line.
- Notes: Utility function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: frameRate = Enter the desired frame rate (1-60):. Code behavior: cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `elseif()`, `do_movie_maker_image()`.
- Source: `Pierre's Template.ipf:36563`

#### `makechunkdatatamap`

```igor
Function makechunkdatatamap(name2,size1,threshold)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makechunkdatatamap(name2, size1, threshold)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations. Main internal calls: `FFTranglemap2()`, `SWFFTgettwistangle()`, `di()`, `margin()`.
- Parameters:
  - `name2`: String name used to locate an Igor wave, graph, folder, or output object.
  - `size1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `threshold`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Triangle Lattice_Graphene_like.ipf:271`

#### `makelayoutforhis`

```igor
Function makelayoutforhis()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call `makelayoutforhis()` from Igor procedure code or the command line.
- Notes: Utility function for linecut, slice, or region extraction; graph display, formatting, or window management. Main internal calls: `dpfig_pf()`.
- Source: `MultipeakforLinecut.ipf:1554`

#### `makevectorfield`

```igor
Function makevectorfield(name,amp)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makevectorfield(name, amp)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `mode()`, `arrowMarker()`, `margin()`.
- Parameters:
  - `name`: Name of the phase map
  - `amp`: Name of the amplitude map
- Source: `SmartDisplay.ipf:4451`

#### `makevectorfield_shrink`

```igor
Function makevectorfield_shrink(name,amp)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makevectorfield_shrink(name, amp)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; extracts image/wave statistics.
- Parameters:
  - `name`: Name of the phase map
  - `amp`: Name of the amplitude map
- Source: `SmartDisplay.ipf:4930`

#### `maxwaterfall`

```igor
Function maxwaterfall()
```
- Usage: call `maxwaterfall()` from Igor procedure code or the command line.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Update log: 08/11-02/2023: Added maxwaterfall() and minwaterfall() which can get the max and min value from a figure of waterfall traces.
- Source: `SmartDisplay.ipf:1868`

#### `minwaterfall`

```igor
Function minwaterfall()
```
- Usage: call `minwaterfall()` from Igor procedure code or the command line.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Update log: 08/11-02/2023: Added maxwaterfall() and minwaterfall() which can get the max and min value from a figure of waterfall traces.
- Source: `SmartDisplay.ipf:1888`

#### `myCursorMovedHook`

```igor
Function myCursorMovedHook(s)
```
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Interactive Cursor, to get 1D sts }}}}}}}}}} Usage: call as `myCursorMovedHook(s)`. Main internal calls: `UpdateControls_3dp()`, `grabwinnonew()`, `UpdateControls_3dpf()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:243`

#### `myCursorMovedHook2`

```igor
Function myCursorMovedHook2(s)
```
- Usage: call as `myCursorMovedHook2(s)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `UpdateControls_3dp()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:335`

#### `myCursorMovedHook2c`

```igor
Function myCursorMovedHook2c(s)
```
- Usage: call as `myCursorMovedHook2c(s)`.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `InsertPoints()`, `hcsr()`, `vcsr()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:5663`

#### `myCursorMovedHook2Dcc`

```igor
Function myCursorMovedHook2Dcc(s)
```
- Notes: #3_06 Cursor Hook Usage: call as `myCursorMovedHook2Dcc(s)`. Main internal calls: `Lineprofilefromcircle()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:3397`

#### `myCursorMovedHook2Dfdn`

```igor
Function myCursorMovedHook2Dfdn(s)
```
- Notes: #3_01 Cursor Hook Usage: call as `myCursorMovedHook2Dfdn(s)`. Main internal calls: `twopgetline()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:2899`

#### `myCursorMovedHook2Dfreehand`

```igor
Function myCursorMovedHook2Dfreehand(s)
```
- Notes: #3_03 Cursor Hook Usage: call as `myCursorMovedHook2Dfreehand(s)`. Main internal calls: `UpdateControls_2df()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:3202`

#### `myCursorMovedHook3Dcc`

```igor
Function myCursorMovedHook3Dcc(s)
```
- Notes: #3_06 Cursor Hook Usage: call as `myCursorMovedHook3Dcc(s)`. Main internal calls: `Lineprofilefromcircle3d()`, `UpdateControls_3dp()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:3259`

#### `myCursorMovedHook3Dccf`

```igor
Function myCursorMovedHook3Dccf(s)
```
- Notes: #3_06 Cursor Hook Usage: call as `myCursorMovedHook3Dccf(s)`. Main internal calls: `Lineprofilefromcircle3df()`, `UpdateControls_3dpf1()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:8074`

#### `myCursorMovedHook3Dfdn`

```igor
Function myCursorMovedHook3Dfdn(s)
```
- Notes: #3_01 Cursor Hook Usage: call as `myCursorMovedHook3Dfdn(s)`. Main internal calls: `twopgetline3d()`, `UpdateControls_3dp()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2600`

#### `myCursorMovedHook3Dfdnf`

```igor
Function myCursorMovedHook3Dfdnf(s)
```
- Notes: #3_01 Cursor Hook Usage: call as `myCursorMovedHook3Dfdnf(s)`. Main internal calls: `twopgetline3df()`, `UpdateControls_3dpf1()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:7316`

#### `myCursorMovedHook3Dfreehand`

```igor
Function myCursorMovedHook3Dfreehand(s)
```
- Notes: #3_03 Cursor Hook Usage: call as `myCursorMovedHook3Dfreehand(s)`. Main internal calls: `UpdateControls_3df()`, `UpdateControls_3dp()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:2967`

#### `myCursorMovedHook3Dfreehandf`

```igor
Function myCursorMovedHook3Dfreehandf(s)
```
- Notes: #3_03 Cursor Hook Usage: call as `myCursorMovedHook3Dfreehandf(s)`. Main internal calls: `UpdateControls_3dff()`, `UpdateControls_3dpf1()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:7755`

#### `myCursorMovedHookf`

```igor
Function myCursorMovedHookf(s)
```
- Usage: call as `myCursorMovedHookf(s)`.
- Notes: Function for Igor wave/matrix/cube data operation. Main internal calls: `UpdateControls_3dpf()`, `UpdateControls_3dp()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:6219`

#### `Normalinecut`

```igor
Function Normalinecut(name)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `Normalinecut(name)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Update log: 08/17/2023: New functions mounted, Normalinecut(name) to normalize 2D linecut along x direction. sumoned(dest,orin,zn) to calculate averaged Z curve from a 3D matrix.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:1561`

#### `NormalinecutX`

```igor
Function NormalinecutX(name)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `NormalinecutX(name)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:1598`

#### `NormalinecutY`

```igor
Function NormalinecutY(name)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `NormalinecutY(name)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:1619`

#### `RebuildAppendCmd`

```igor
Function/S RebuildAppendCmd(cmd)
```
- Purpose: rebuilds Igor append commands so saved graph traces point to the current waves.
- Usage: call as `RebuildAppendCmd(cmd)`.
- Notes: String-returning helper for graph display, formatting, or window management.
- Parameters:
  - `cmd`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `transfergraph.ipf:94`

#### `recalindicativeline`

```igor
Function recalindicativeline()
```
- Usage: call `recalindicativeline()` from Igor procedure code or the command line.
- Notes: #5 update indicative lines Purpose: displays waves, images, contours, or graph overlays. Main internal calls: `tpw()`.
- Source: `SmartDisplay.ipf:2819`

#### `removeappendimage`

```igor
Function removeappendimage()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `removeappendimage()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls. Main internal calls: `stringmatch()`, `elseif()`, `grabwinnonew()`.
- Source: `Smart_3D_Viewer_New.ipf:9967`

#### `RemoveSingleQuote`

```igor
Function/S RemoveSingleQuote(s0)
```
- Purpose: removes quote characters from a stored graph command string before execution.
- Usage: call as `RemoveSingleQuote(s0)`.
- Notes: String-returning helper for graph display, formatting, or window management.
- Parameters:
  - `s0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `transfergraph.ipf:85`

#### `revoverongraph`

```igor
Function revoverongraph()
```
- Usage: call `revoverongraph()` from Igor procedure code or the command line.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `SmartDisplay.ipf:1328`

#### `SaveGraph2`

```igor
Function SaveGraph2()
```
- Purpose: captures a graph recreation command so the graph can be rebuilt later.
- Usage: call `SaveGraph2()` from Igor procedure code or the command line.
- Notes: Utility function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `WinRecreation()`, `SetWaveNameinProcText()`.
- Source: `transfergraph.ipf:4`

#### `savexypairbycursor`

```igor
Function savexypairbycursor()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: call `savexypairbycursor()` from Igor procedure code or the command line.
- Notes: Utility function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `hook()`.
- Source: `SmartDisplay.ipf:5655`

#### `shrinkvf`

```igor
Function shrinkvf(steps)
```
- Notes: Make vector field plot with shrinked dimsize, steps is a int Usage: call as `shrinkvf(steps)`. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `shrinkmatrixbysteps()`, `makevectorfield_shrink()`.
- Parameters:
  - `steps`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:4898`

#### `smoothallthegraph`

```igor
Function smoothallthegraph(ss)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `smoothallthegraph(ss)`.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: smooths wave data.
- Parameters:
  - `ss`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:1300`

#### `sumoned`

```igor
Function sumoned(dest,orin,zn)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `sumoned(dest, orin, zn)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `mean()`. Update log: 10/03/2023: (7) Upgrade the sumoned() function for sum over the Grid to get average 1D wave.
- Parameters:
  - `dest`: Output name
  - `orin`: Input 3D matrix
  - `zn`: Index of your interesting dimension that keep in the end.
- Source: `Smart_3D_Viewer_New.ipf:2342`

#### `tpw`

```igor
Function/S tpw()
```
- Notes: name = stringfromlist(0,WaveList("*", ";","WIN:"+winname(0,1))) Return name end New Version with getwindow function Usage: call `tpw()` from Igor procedure code or the command line. Update log: 08/23/2023: (1) updated tpw() in order to read out active subwindow. 08/12/2023: Introduce tpw() to Peng's Curvature. Added Error saving syntax to 2D_GCB.
- Source: `SmartDisplay.ipf:636`

#### `twopgetline`

```igor
Function twopgetline()
```
- Notes: #3_02 Hook Called: Extract Line profile from two points {Note: this function will be called every time cursor moves} Usage: call `twopgetline()` from Igor procedure code or the command line. Main internal calls: `pcsr()`, `qcsr()`, `twopgetlinex()`, `twopgetliney()`.
- Source: `SmartDisplay.ipf:2912`

#### `twopgetline3d`

```igor
FUnction twopgetline3d()
```
- Notes: #3_02 Hook Called: Extract Line profile from two points {Note: this function will be called every time cursor moves} Usage: call `twopgetline3d()` from Igor procedure code or the command line. Main internal calls: `pcsr()`, `qcsr()`, `twopgetline3dx()`, `twopgetline3dy()`, `Normalinecut()`, `FFTL2_3dplot()`.
- Source: `Smart_3D_Viewer_New.ipf:2614`

#### `twopgetline3df`

```igor
FUnction twopgetline3df()
```
- Notes: #3_02 Hook Called: Extract Line profile from two points {Note: this function will be called every time cursor moves} Usage: call `twopgetline3df()` from Igor procedure code or the command line. Main internal calls: `pcsr()`, `qcsr()`, `twopgetline3dxf()`, `twopgetline3dyf()`.
- Source: `Smart_3D_Viewer_New.ipf:7346`

#### `twopgetline3dx`

```igor
Function twopgetline3dx()
```
- Usage: call `twopgetline3dx()` from Igor procedure code or the command line.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `qcsr()`, `pcsr()`, `grabwinnonew()`, `lsize()`, `rgb()`, `mode()`.
- Source: `Smart_3D_Viewer_New.ipf:2649`

#### `twopgetline3dxf`

```igor
Function twopgetline3dxf()
```
- Usage: call `twopgetline3dxf()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `qcsr()`, `pcsr()`, `lsize()`, `rgb()`, `mode()`, `lstyle()`.
- Source: `Smart_3D_Viewer_New.ipf:7356`

#### `twopgetline3dy`

```igor
Function twopgetline3dy()
```
- Usage: call `twopgetline3dy()` from Igor procedure code or the command line.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `qcsr()`, `grabwinnonew()`, `lsize()`, `rgb()`, `mode()`.
- Source: `Smart_3D_Viewer_New.ipf:2807`

#### `twopgetline3dyf`

```igor
Function twopgetline3dyf()
```
- Usage: call `twopgetline3dyf()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `qcsr()`, `lsize()`, `rgb()`, `mode()`, `lstyle()`.
- Source: `Smart_3D_Viewer_New.ipf:7549`

#### `twopgetlinex`

```igor
Function twopgetlinex()
```
- Usage: call `twopgetlinex()` from Igor procedure code or the command line.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `qcsr()`, `pcsr()`, `tpw()`, `autoremovejump1D()`, `lsize()`, `rgb()`.
- Source: `SmartDisplay.ipf:2922`

#### `twopgetliney`

```igor
Function twopgetliney()
```
- Usage: call `twopgetliney()` from Igor procedure code or the command line.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `qcsr()`, `tpw()`, `autoremovejump1D()`, `lsize()`, `rgb()`.
- Source: `SmartDisplay.ipf:3062`

#### `UpdateControls_2df`

```igor
Function UpdateControls_2df(traceName, cursorName, pointNumber, yPointNumber)
```
- Usage: call as `UpdateControls_2df(traceName, cursorName, pointNumber, yPointNumber)`.
- Notes: #3_04 Extract line profile by free hand draw {Note: this function will be called every time cursor moves} Purpose: updates parameters, controls, scales, or display state. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`, `lsize()`, `rgb()`, `mode()`, `lstyle()`, `mrkThick()`.
- Parameters:
  - `traceName`: Igor wave or matrix input consumed by the calculation.
  - `cursorName`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `yPointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `SmartDisplay.ipf:3218`

#### `UpdateControls_3df`

```igor
Function UpdateControls_3df(traceName, cursorName, pointNumber, yPointNumber)
```
- Usage: call as `UpdateControls_3df(traceName, cursorName, pointNumber, yPointNumber)`.
- Notes: #3_04 Extract line profile by free hand draw {Note: this function will be called every time cursor moves} Purpose: updates parameters, controls, scales, or display state. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `grabwinnonew()`, `lsize()`, `rgb()`, `mode()`, `lstyle()`, `mrkThick()`.
- Parameters:
  - `traceName`: Igor wave or matrix input consumed by the calculation.
  - `cursorName`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `yPointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:2984`

#### `UpdateControls_3dff`

```igor
Function UpdateControls_3dff(traceName, cursorName, pointNumber, yPointNumber)
```
- Usage: call as `UpdateControls_3dff(traceName, cursorName, pointNumber, yPointNumber)`.
- Notes: #3_04 Extract line profile by free hand draw {Note: this function will be called every time cursor moves} Purpose: updates parameters, controls, scales, or display state. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `lsize()`, `rgb()`, `mode()`, `lstyle()`, `mrkThick()`, `useMrkStrokeRGB()`.
- Parameters:
  - `traceName`: Igor wave or matrix input consumed by the calculation.
  - `cursorName`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `yPointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:7772`

#### `UpdateControls_3dp`

```igor
Function UpdateControls_3dp(traceName, cursorName, pointNumber, yPointNumber)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `UpdateControls_3dp(traceName, cursorName, pointNumber, yPointNumber)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Main internal calls: `getsinglests()`, `grabwinnonew()`, `grabwin_sfft()`.
- Parameters:
  - `traceName`: Igor wave or matrix input consumed by the calculation.
  - `cursorName`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `yPointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:268`

#### `UpdateControls_3dpf`

```igor
Function UpdateControls_3dpf(traceName, cursorName, pointNumber, yPointNumber)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `UpdateControls_3dpf(traceName, cursorName, pointNumber, yPointNumber)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Main internal calls: `getsinglestsf()`, `grabwinnonew()`, `grabwin()`.
- Parameters:
  - `traceName`: Igor wave or matrix input consumed by the calculation.
  - `cursorName`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `yPointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:6232`

#### `UpdateControls_3dpf1`

```igor
Function UpdateControls_3dpf1(traceName, cursorName, pointNumber, yPointNumber)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `UpdateControls_3dpf1(traceName, cursorName, pointNumber, yPointNumber)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `getsinglests()`.
- Parameters:
  - `traceName`: Igor wave or matrix input consumed by the calculation.
  - `cursorName`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `yPointNumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:7328`

#### `wind1uniformity`

```igor
Function wind1uniformity(w,x) : FitFunc
```
- Notes: #3_09 Fit Function for perfect Winding number 1 Polar plot Usage: call as `wind1uniformity(w, x)`.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:3756`


### Internal/static helpers

#### `KP_HaveNewColorTables`

```igor
Static Function KP_HaveNewColorTables()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call `KP_HaveNewColorTables()` from Igor procedure code or the command line.
- Notes: Internal helper function for graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `KP_ColorTables.ipf:38`

#### `KP_LoadNewColorTablesFrom`

```igor
Static Function KP_LoadNewColorTablesFrom(folderPath)
```
- Usage: call as `KP_LoadNewColorTablesFrom(folderPath)`.
- Notes: Internal helper function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `KP_HaveNewColorTables()`.
- Parameters:
  - `folderPath`: File-system path or data-folder string used for loading or saving data.
- Source: `KP_ColorTables.ipf:50`



## FFT, QPI, Fourier Filters, Phase, and Lock-In

### Panel callbacks and control handlers

#### `Button_Help_FFT_filter`

```igor
Function Button_Help_FFT_filter(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `Help_FFT_filter()`.
- Notes: Panel callback/helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9867`

#### `Button_Help_FFT_list`

```igor
Function Button_Help_FFT_list(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `Help_FFT_list()`.
- Notes: Panel callback/helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9716`

#### `ButtonFFTfilterdata`

```igor
Function ButtonFFTfilterdata(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `FFTfilterdata()`.
- Notes: Panel callback/helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `FFT filter`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FFT filter`, `FFT filter`
- Source: `Pierre's Template.ipf:622`

#### `ButtonProc_2dlockinmulti`

```igor
Function ButtonProc_2dlockinmulti(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `2D lock-in`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D lock-in`
- Source: `Smart_3D_Viewer_New.ipf:8492`

#### `ButtonProc_2dlockinmultifft`

```igor
Function ButtonProc_2dlockinmultifft(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ffc3dp()`.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Multiple 2D-lock-in }}}}}}}}}} Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `M-lockin`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `M-lockin`
- Source: `Smart_3D_Viewer_New.ipf:8254`

#### `ButtonProc_allqpi`

```igor
Function ButtonProc_allqpi(ctrlName) : ButtonControl
```
- Notes: 2. The next we do Interp Filter on Modula of FFT. 3. We do C4 symmetrization on Dxy-ed data.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:4386`

#### `ButtonProc_allqpir`

```igor
Function ButtonProc_allqpir(ctrlName) : ButtonControl
```
- Notes: 2. The next we do Interp Filter on Modula of FFT. 3. We do C4 symmetrization on Dxy-ed data.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:3615`

#### `ButtonProc_appendQPIESimu_getGMcut`

```igor
Function ButtonProc_appendQPIESimu_getGMcut(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Append QPI`. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Append QPI`
- Source: `Models.ipf:13461`

#### `ButtonProc_Complextoreal`

```igor
Function ButtonProc_Complextoreal(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Complextoreal()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Panel control(s): `Cmplx2Rl`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cmplx2Rl`
- Source: `FFT.ipf:571`

#### `ButtonProc_Const2dfilter`

```igor
Function ButtonProc_Const2dfilter(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Const2dfilter()`.
- Notes: Interactive procedure for FFT filter Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:1761`

#### `ButtonProc_Const2dlockin`

```igor
Function ButtonProc_Const2dlockin(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Const2dlockin()`.
- Notes: continuely tuning 2D-lock-in avedia Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Lawler-Fujita Drift Correction.ipf:1852`

#### `ButtonProc_Constofftheset`

```igor
Function ButtonProc_Constofftheset(ctrlName) : ButtonControl//"Special"
```
- Usage: triggered as a wrapper that opens/runs `offtheset()`.
- Notes: End of Analyse two compositions of a topograph Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `Offset The`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Offset The`
- Source: `Miscellaneous_Codes.ipf:6000`

#### `ButtonProc_Correct2Dlockinampcoutc`

```igor
Function ButtonProc_Correct2Dlockinampcoutc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Correct2Dlockinampcout()`.
- Notes: Moving window reference Convert 2D lock-in amp to be absolute Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `Correct Amp`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Correct Amp`
- Source: `Lawler-Fujita Drift Correction.ipf:2914`

#### `ButtonProc_Correct2DlockinampcoutInd`

```igor
Function ButtonProc_Correct2DlockinampcoutInd(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `Correct2Dlockinampc()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Cr`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cr`
- Source: `Lawler-Fujita Drift Correction.ipf:2951`

#### `ButtonProc_DBD_PRQPI`

```igor
Function ButtonProc_DBD_PRQPI(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: $ Defect bound state Phase referenced QPI to extract $ superconducting pair phase $ Ref. Phys. Rev. B 99,014507 (2019) & references therein ############################################################## Panel control(s): `Do it`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Do it`
- Source: `PR_QPI.ipf:9`

#### `ButtonProc_determineqbyphase`

```igor
Function ButtonProc_determineqbyphase(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `determineqbyphasec()`.
- Notes: Refine Q vector Ref: https://www.nature.com/articles/s41467-024-48047-0 Minimize the slope of phase map. Panel control(s): `Refine Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Refine Q`, `Refine Q`
- Source: `FFT.ipf:2442`

#### `ButtonProc_determineqbyphase2`

```igor
Function ButtonProc_determineqbyphase2(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `determineqbyphasec()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Refine Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Refine Q`
- Source: `FFT.ipf:2446`

#### `ButtonProc_doampfftforphase`

```igor
Function ButtonProc_doampfftforphase(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `doampfftforphase()`.
- Notes: Pre procedure, to make amplitude FFT Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `PreFFT`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `PreFFT`
- Source: `Map_the_phase_difference_between_two_image.ipf:63`

#### `ButtonProc_ffc`

```igor
Function ButtonProc_ffc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ffc()`.
- Notes: (3) Do real inverse FFT IFFT/DEST=$nameffti namefftw; This will give a filtered Matrix which is dimsize($nameffti,0) = 3*dimsize(name,0), dimsize($nameffti,1) = 3*dimsize(name,1) (4) Select the [dimsize(name,0),dimsize(name,1)] data points to be the final outp Panel control(s): `Launch`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Launch`
- Source: `FFT.ipf:1192`

#### `ButtonProc_FFTapply_3dplot`

```igor
Function ButtonProc_FFTapply_3dplot(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Apply`. Main internal calls: `DoFFT_3dplot()`, `DoFFT_3dplot_marquee()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Apply`
- Source: `Smart_3D_Viewer_New.ipf:5597`

#### `ButtonProc_FFTlist`

```igor
Function ButtonProc_FFTlist(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `FFTlist()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Panel control(s): `FFT list`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FFT list`, `FFT list`, `FFT list`
- Source: `Pierre's Template.ipf:1253`

#### `ButtonProc_FFTpro`

```igor
Function ButtonProc_FFTpro(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `FFTpro()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:3`

#### `ButtonProc_FFTr`

```igor
Function ButtonProc_FFTr(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FFTrc()`.
- Notes: Normal FFT for arbitary data points and all value is positive 1. Convert the wave to be complex wave "cvtcmplx(name)" 2. Do complex FFT to this converted wave, it will get a full sized FFT image 3. use Complextorealf() to convert the complex wave back magnitud Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FFT`
- Source: `FFT.ipf:939`

#### `ButtonProc_FFTstripesimulationc`

```igor
Function ButtonProc_FFTstripesimulationc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FFTstripesimulationc()`.
- Notes: Simulation of stripes features on FFT image Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `Stripe@FFT`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Stripe@FFT`
- Source: `Models.ipf:13491`

#### `ButtonProc_findpolarizationc`

```igor
Function ButtonProc_findpolarizationc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `findpolarizationc()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Panel control(s): `Calculate Pplarization`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Calculate Pplarization`
- Source: `Map_the_phase_difference_between_two_image.ipf:4`

#### `ButtonProc_Fmqfft`

```igor
Function ButtonProc_Fmqfft(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `Frommarqueegetsubmatrixsc2()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Marqueed Area`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Marqueed Area`
- Source: `FFT.ipf:967`

#### `ButtonProc_Fmqfftnormal`

```igor
Function ButtonProc_Fmqfftnormal(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Panel control(s): `Normal`. Main internal calls: `FFTrls2()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Normal`
- Source: `FFT.ipf:958`

#### `ButtonProc_FTc`

```igor
Function ButtonProc_FTc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FTc()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Panel control(s): `QC4`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `QC4`
- Source: `FFT.ipf:1521`

#### `ButtonProc_FTclp`

```igor
Function ButtonProc_FTclp(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FTclp()`.
- Notes: 2D low pass filter locked (0,0) Panel control(s): `LP`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LP`
- Source: `FFT.ipf:2219`

#### `ButtonProc_FTlinecutc`

```igor
Function ButtonProc_FTlinecutc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FTlinecutc()`.
- Notes: 1D FFT filter for linecut, This is useful for Y linecut Update 09/16/23: added 1D wave filter, the procedure can recongnize it automatically but you need to input the avedia by your self. Panel control(s): `Y`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Y`
- Source: `FFT.ipf:1925`

#### `ButtonProc_GpAarea`

```igor
Function ButtonProc_GpAarea(ctrlName) : ButtonControl
```
- Notes: #01_01 Grab Marquee [Q1] Usage: called automatically by Igor when the linked panel control changes. Code behavior: creates output waves. Main internal calls: `grabwinnonew()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Smart_3D_Viewer_New.ipf:4604`

#### `ButtonProc_GPAc`

```igor
Function ButtonProc_GPAc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpac()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Panel control(s): `Get A`, `getA`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get A`, `Get A`, `getA`
- Source: `FFT.ipf:1397`

#### `ButtonProc_GPAc3dp`

```igor
Function ButtonProc_GPAc3dp(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpac3dp()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Get A`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get A`
- Source: `Smart_3D_Viewer_New.ipf:8379`

#### `ButtonProc_GPAc3dpls`

```igor
Function ButtonProc_GPAc3dpls(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpac3dpls()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Get A`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get A`
- Source: `Smart_3D_Viewer_New.ipf:9325`

#### `ButtonProc_GpBarea`

```igor
Function ButtonProc_GpBarea(ctrlName) : ButtonControl
```
- Notes: #01_02 Grab Marquee [Q2] Usage: called automatically by Igor when the linked panel control changes. Code behavior: creates output waves. Main internal calls: `grabwinnonew()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Smart_3D_Viewer_New.ipf:4638`

#### `ButtonProc_GpBc`

```igor
Function ButtonProc_GpBc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpbc()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Panel control(s): `getB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `getB`
- Source: `FFT.ipf:1463`

#### `ButtonProc_GpBc3dp`

```igor
Function ButtonProc_GpBc3dp(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpbc3dp()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Smart_3D_Viewer_New.ipf:8435`

#### `ButtonProc_GpBc3dpls`

```igor
Function ButtonProc_GpBc3dpls(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpbc3dpls()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Smart_3D_Viewer_New.ipf:9390`

#### `ButtonProc_GPc`

```igor
Function ButtonProc_GPc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpc()`.
- Notes: End FFT Filter Panel control(s): `Get Peak / 2D Gaussian`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get Peak / 2D Gaussian`
- Source: `FFT.ipf:1704`

#### `ButtonProc_linecutFFT`

```igor
Function ButtonProc_linecutFFT(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FFTL2c()`.
- Notes: DO 1D FFT (x) to a linecut Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `Line@2D(2)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Line@2D(2)`
- Source: `FFT.ipf:785`

#### `ButtonProc_linecutFFT2`

```igor
Function ButtonProc_linecutFFT2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linecutFFT()`.
- Notes: Following is a home made old version This do slice by slice, faster but only have half Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `(1)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `(1)`
- Source: `FFT.ipf:830`

#### `ButtonProc_mulQPI`

```igor
Function ButtonProc_mulQPI(ctrlName) : ButtonControl
```
- Notes: Label left "ky (Å)" Label bottom "kx (Å)" ModifyImage $final ctab= {*,*,ColdWarm,0} end Usage: triggered as a wrapper that opens/runs `haha()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:4333`

#### `ButtonProc_mulQPIr`

```igor
Function ButtonProc_mulQPIr(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `hahar()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:3567`

#### `ButtonProc_multi4fold`

```igor
Function ButtonProc_multi4fold(ctrlName) : ButtonControl
```
- Notes: to suppress vertical brightness: fft4foldsym*=1-abs((x+y)^2/4-(x-y)^2/4) e.g. FFTpro("data",1,80,15,4.2,29.283) C4symFFT("datafftsym15",15,29.283) Usage: triggered as a wrapper that opens/runs `multi4fold()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:217`

#### `ButtonProc_PhaseMap`

```igor
Function ButtonProc_PhaseMap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `PhaseMap()`.
- Notes: Main Procedure Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `Phase DIF of 2`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Phase DIF of 2`
- Source: `Map_the_phase_difference_between_two_image.ipf:243`

#### `ButtonProc_PhaseMapcorrectbd`

```igor
Function ButtonProc_PhaseMapcorrectbd(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `correctboundPhMapc()`.
- Notes: This procedure used to fix the stripe artifact introduced by the movewindow Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `FixBnd`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FixBnd`
- Source: `Map_the_phase_difference_between_two_image.ipf:507`

#### `ButtonProc_pickupPA`

```igor
Function ButtonProc_pickupPA(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `pickupPA()`.
- Notes: Pre procedure, to pick up the coordinate of FFT peak A Panel control(s): `getA`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `getA`
- Source: `Map_the_phase_difference_between_two_image.ipf:127`

#### `ButtonProc_pickupPB`

```igor
Function ButtonProc_pickupPB(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `pickupPB()`.
- Notes: Pre procedure, to pick up the coordinate of FFT peak B Panel control(s): `getB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `getB`
- Source: `Map_the_phase_difference_between_two_image.ipf:181`

#### `ButtonProc_QPIESimu_getGMcut`

```igor
Function ButtonProc_QPIESimu_getGMcut(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `QPI(G-X)`. Main internal calls: `QPIESimu_getGMcut()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `QPI(G-X)`
- Source: `Models.ipf:13298`

#### `ButtonProc_saveA_refineQ`

```igor
Function ButtonProc_saveA_refineQ(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis. Panel control(s): `Save A`. Main internal calls: `saveA_refineQ()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Save A`
- Source: `FFT.ipf:2464`

#### `ButtonProc_saveB_refineQ`

```igor
Function ButtonProc_saveB_refineQ(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis. Panel control(s): `Save B`. Main internal calls: `saveB_refineQ()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Save B`
- Source: `FFT.ipf:2491`

#### `ButtonProc_SumlayerFFT3Dc`

```igor
Function ButtonProc_SumlayerFFT3Dc(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Sum(E)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sum(E)`
- Source: `Smart_3D_Viewer_New.ipf:10291`

#### `ButtonProc_SumlayerFFT3Dc2`

```igor
Function ButtonProc_SumlayerFFT3Dc2(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `It3/2`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `It3/2`
- Source: `Smart_3D_Viewer_New.ipf:10270`

#### `ButtonProc_t2dlockin`

```igor
Function ButtonProc_t2dlockin(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `t2dlockin()`.
- Notes: The procedure for 2D lock-in technique Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `Old`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Old`
- Source: `Lawler-Fujita Drift Correction.ipf:1428`

#### `ButtonProc_t2dlockinandFilter`

```igor
Function ButtonProc_t2dlockinandFilter(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `t2dlockinandFilter()`.
- Notes: The procedure for New 2D lock-in and Filter together Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `2D lock-in`, `Lckin/Ftd`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D lock-in`, `2D lock-in`, `Lckin/Ftd`
- Source: `Lawler-Fujita Drift Correction.ipf:2229`

#### `PopMenuProc_FFTmarquearea_3dplot`

```igor
Function PopMenuProc_FFTmarquearea_3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; MultiFunctional FFT with in a Marquee }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `FFTmarque`. Main internal calls: `DoFFT_3dplot()`, `DoFFT_3dplot_marquee()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `FFTmarque`
- Source: `Smart_3D_Viewer_New.ipf:9615`

#### `PopMenuProc_FFTmode_3dplot`

```igor
Function PopMenuProc_FFTmode_3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Here is the start of 3D FFT player }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `FFTMode`. Main internal calls: `DoFFT_3dplot()`, `DoFFT_3dplot_marquee()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `FFTMode`
- Source: `Smart_3D_Viewer_New.ipf:5535`

#### `PopMenuProc_FFTwin_3dplot`

```igor
Function PopMenuProc_FFTwin_3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `DoFFT_3dplot()`, `DoFFT_3dplot_marquee()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Source: `Smart_3D_Viewer_New.ipf:5575`

#### `SetVarProc_Cons3dplotPRQPI`

```igor
Function SetVarProc_Cons3dplotPRQPI(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ## Control Z slice Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Z`. Main internal calls: `colorPRQPI()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `PR_QPI.ipf:165`

#### `SetVarProc_Const2dfilter`

```igor
Function SetVarProc_Const2dfilter(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `FWHM(Filter_r)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM(Filter_r)`
- Source: `FFT.ipf:1777`

#### `SetVarProc_Const2dfilterlp`

```igor
Function SetVarProc_Const2dfilterlp(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `N(x)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `N(x)`
- Source: `FFT.ipf:2335`

#### `SetVarProc_FTlinecutcons`

```igor
Function SetVarProc_FTlinecutcons(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `FWHM`, `q0`. Main internal calls: `FTlinecutcons()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM`, `q0`
- Source: `FFT.ipf:2024`

#### `SetVarProc_FTlinecutcons1D`

```igor
Function SetVarProc_FTlinecutcons1D(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `FTlinecutcons1d()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `FFT.ipf:2112`

#### `SetVarProc_lsnormfftq`

```igor
Function SetVarProc_lsnormfftq(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal. Panel control(s): `NormFFT`. Code behavior: creates output waves.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `NormFFT`
- Source: `Lattice Segregation.ipf:926`

#### `SetVarProc_NbSe2simu`

```igor
Function SetVarProc_NbSe2simu(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `theta`, `a_{0} (Å)`, `C1 phase shift of Q_{1} (2π/3)`, `C2 phase shift of Q_{2} (2π/3)`, `C3 phase shift of Q_{3} (2π/3)`. Main internal calls: `simuCDW()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `theta`, `a_{0} (Å)`, `C1 phase shift of Q_{1} (2π/3)`, `C2 phase shift of Q_{2} (2π/3)`, `C3 phase shift of Q_{3} (2π/3)`
- Source: `Models.ipf:13697`

#### `SetVarProc_PRQPI_setcolorratio`

```igor
Function SetVarProc_PRQPI_setcolorratio(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ## Control Color range Panel control(s): `σ`. Main internal calls: `colorPRQPI()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ`
- Source: `PR_QPI.ipf:152`

#### `SetVarProc_qpirangeFFTcolor`

```igor
Function SetVarProc_qpirangeFFTcolor(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `σ`. Main internal calls: `color3s_for3dinvqpirange()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ`
- Source: `Models.ipf:12294`


### Interactive procedures and macros

#### `allqpi2`

```igor
proc allqpi2(name,number,lattice,angle,point,dis,energy)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`, `dis`, `energy`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter; dis = size of display +- A-1. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `FFTpro2()`, `C4symFFT2()`, `D2symetric2()`, `twoDinterpolate2()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
  - `dis`: size of display +- A-1
  - `energy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4410`

#### `allqpi2r`

```igor
proc allqpi2r(name,number,lattice,angle,point,dis)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`, `dis`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter; dis = size of display +- A-1. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `FFTpro2r()`, `C4symFFT2r()`, `rot2d_pim()`, `D2symetric2r()`, `Nan0()`, `twoDinterpolate2r()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
  - `dis`: size of display +- A-1
- Source: `Miscellaneous_Codes.ipf:3638`

#### `allqpihaha`

```igor
proc allqpihaha(name,number,lattice,angle,point,dis,energy)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`, `dis`, `energy`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter; dis = size of display +- A-1. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `allqpipp()`, `allqpi2()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
  - `dis`: size of display +- A-1
  - `energy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4391`

#### `allqpihahar`

```igor
proc allqpihahar(name,number,lattice,angle,point,dis)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`, `dis`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter; dis = size of display +- A-1. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `allqpippr()`, `allqpi2r()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
  - `dis`: size of display +- A-1
- Source: `Miscellaneous_Codes.ipf:3620`

#### `allqpipp`

```igor
proc allqpipp(name,number,lattice,angle,point)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `FFTpro2()`, `C4symFFT2()`, `D2symetric2()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
- Source: `Miscellaneous_Codes.ipf:4616`

#### `allqpippr`

```igor
proc allqpippr(name,number,lattice,angle,point)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `FFTpro2r()`, `C4symFFT2r()`, `rot2d_pim()`, `D2symetric2r()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
- Source: `Miscellaneous_Codes.ipf:3717`

#### `Complextoreal`

```igor
proc Complextoreal(name1,select)
```
- Usage: run from Igor with parameters `name1`, `select`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name1 = Name of Complex Matrix; select = Which Mode. Code behavior: formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Complextorealf()`.
- Parameters:
  - `name1`: Name of Complex Matrix
  - `select`: Which Mode
- Source: `FFT.ipf:575`

#### `Const2dfilter`

```igor
Proc Const2dfilter()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls. Update log: 07/24/2023: Added interactive procedures Const2dfilter() for continuously tuning the window size of FFT filter. Bug fixed.
- Source: `FFT.ipf:1766`

#### `Const2dfilterc`

```igor
Proc Const2dfilterc()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Main internal calls: `FTcons()`.
- Source: `FFT.ipf:1785`

#### `Const2dfilterclp`

```igor
Proc Const2dfilterclp()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Main internal calls: `FTconslp()`.
- Source: `FFT.ipf:2343`

#### `Const2dfilterlp`

```igor
Proc Const2dfilterlp()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `grabwin()`.
- Source: `FFT.ipf:2324`

#### `correctboundPhMapc`

```igor
Proc correctboundPhMapc(nn,ref,block)
```
- Usage: run from Igor with parameters `nn`, `ref`, `block`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Dialog prompts: nn = Source wave; ref = Jump ref wave; block = width for correction. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `correctboundPhMap()`.
- Parameters:
  - `nn`: Source wave
  - `ref`: Jump ref wave
  - `block`: width for correction
- Source: `Map_the_phase_difference_between_two_image.ipf:511`

#### `cutElarge2`

```igor
proc cutElarge2(name,index,point)
```
- Usage: run from Igor with parameters `name`, `index`, `point`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `DeletePdata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `index`: Step size or energy/voltage increment used by the calculation.
  - `point`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:768`

#### `cutklarge2`

```igor
proc cutklarge2(name,index,point)
```
- Usage: run from Igor with parameters `name`, `index`, `point`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Main internal calls: `DeletePdata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `index`: Step size or energy/voltage increment used by the calculation.
  - `point`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:760`

#### `cvtcmplx2`

```igor
Proc cvtcmplx2(data)
```
- Usage: run from Igor with parameters `data`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `data`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:615`

#### `d3dfft`

```igor
Proc d3dfft()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `sumoned()`, `dilf()`, `color3s_for3dinv()`, `grabwin()`, `margin()`, `childWindowList()`.
- Source: `Smart_3D_Viewer_New.ipf:5925`

#### `DBD_PRQPI`

```igor
Proc DBD_PRQPI(name,sel)
```
- Usage: run from Igor with parameters `name`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: name = 3D conductance g matrix, require Zn = 2; sel = Select Mode. Code behavior: performs Fourier-transform or inverse-transform operations; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `DBD_PRQPI_FP_w()`, `DBD_PRQPI_FP_wo()`, `DBD_PRQPI_cP_w()`, `DBD_PRQPI_cP_wo()`, `grabwin()`.
- Parameters:
  - `name`: 3D conductance g matrix, require Zn = 2
  - `sel`: Select Mode
- Source: `PR_QPI.ipf:13`

#### `determineqbyphasec`

```igor
Proc determineqbyphasec()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback. Update-log guidance - 2024-05-09: Refines Q by calculating the median phase-gradient map while changing the Q position.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: writes or exports data; reads or updates panel controls. Main internal calls: `determineqbyphase()`. After clicking "Refine Q", users can inspect the gradient map, refine manually, or fit the center by 2D Gaussian and save the result to Qa/Qb.
- Source: `FFT.ipf:2454`

#### `doampfftforphase`

```igor
Proc doampfftforphase(gapname,topname)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `gapname`, `topname`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `twoDinterpolatexy()`.
- Parameters:
  - `gapname`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `topname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Map_the_phase_difference_between_two_image.ipf:68`

#### `ffc`

```igor
Proc ffc(name,sel)
```
- Usage: run from Igor with parameters `name`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: sel = Do you want to interpolate?. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `ffnointer()`, `ff()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: Do you want to interpolate?
- Source: `FFT.ipf:1196`

#### `FFTfilterdata`

```igor
Proc FFTfilterdata()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `funcFFTfilterdata()`.
- Source: `Pierre's Template.ipf:3550`

#### `FFTfiltermat`

```igor
Proc FFTfiltermat(thismat)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `thismat`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Dialog prompts: thismat = Enter the name of the matrix. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `funcFFTfilterdata()`.
- Parameters:
  - `thismat`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:3519`

#### `FFTL2c`

```igor
Proc FFTL2c(name,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis. Dialog prompts: name = Data Name; sel = Modes. Code behavior: performs Fourier-transform or inverse-transform operations; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `FFTL2()`.
- Parameters:
  - `name`: Data Name
  - `sel`: Modes
- Source: `FFT.ipf:789`

#### `FFTlist`

```igor
Proc FFTlist()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Source: `Pierre's Template.ipf:3532`

#### `FFTpro`

```igor
Proc FFTpro(mat,number,start,a,theta,unit)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `mat`, `number`, `start`, `a`, `theta`, `unit`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: mat = Name of Map Batch; number = Number of the Batch; start = Start number of Batch; a = lattice constance (A); theta = angle between lattice and x+; unit = k-space unit. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `FFTsymind()`, `di()`.
- Parameters:
  - `mat`: Name of Map Batch
  - `number`: Number of the Batch
  - `start`: Start number of Batch
  - `a`: lattice constance (A)
  - `theta`: angle between lattice and x+
  - `unit`: k-space unit
- Source: `FFT.ipf:7`

#### `FFTpro2`

```igor
Proc FFTpro2(mat,number,start,a,theta,unit)
```
- Usage: run from Igor with parameters `mat`, `number`, `start`, `a`, `theta`, `unit`.
- Notes: ModifyGraph width={Plan,1,bottom,left} Label left "ky (Å)" Label bottom "kx (Å)" ModifyImage $final ctab= {*,*,ColdWarm,0} Purpose: performs Fourier, filtering, phase, or lock-in processing. Dialog prompts: mat = Name of Map Batch; number = Number of the Batch; start = Start number of Batch; a = lattice constance (A); theta = angle between lattice and x+; unit = k-space unit. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `FFTsymind2()`.
- Parameters:
  - `mat`: Name of Map Batch
  - `number`: Number of the Batch
  - `start`: Start number of Batch
  - `a`: lattice constance (A)
  - `theta`: angle between lattice and x+
  - `unit`: k-space unit
- Source: `Miscellaneous_Codes.ipf:4673`

#### `FFTpro2r`

```igor
Proc FFTpro2r(mat,number,start,a,theta,unit)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `mat`, `number`, `start`, `a`, `theta`, `unit`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: mat = Name of Map Batch; number = Number of the Batch; start = Start number of Batch; a = lattice constance (A); theta = angle between lattice and x+; unit = k-space unit. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `FFTsymind2r()`.
- Parameters:
  - `mat`: Name of Map Batch
  - `number`: Number of the Batch
  - `start`: Start number of Batch
  - `a`: lattice constance (A)
  - `theta`: angle between lattice and x+
  - `unit`: k-space unit
- Source: `Miscellaneous_Codes.ipf:3894`

#### `FFTrc`

```igor
Proc FFTrc(name,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: name = Data Name; sel = Apply Window Function?. Code behavior: reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `FFTr()`.
- Parameters:
  - `name`: Data Name
  - `sel`: Apply Window Function?
- Source: `FFT.ipf:943`

#### `findpolarizationc`

```igor
Proc findpolarizationc(DA,DB,TA,TB)
```
- Usage: run from Igor with parameters `DA`, `DB`, `TA`, `TB`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: DA = Name of phase Δ(QA); DB = Name of phase Δ(QB); TA = Name of phase T(QA); TB = Name of phase T(QB). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `findpolarization()`, `ckfig_child()`.
- Parameters:
  - `DA`: Name of phase Δ(QA)
  - `DB`: Name of phase Δ(QB)
  - `TA`: Name of phase T(QA)
  - `TB`: Name of phase T(QB)
- Source: `Map_the_phase_difference_between_two_image.ipf:8`

#### `FitFDwithEffT`

```igor
Proc FitFDwithEffT(wname,thistemperature)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `wname`, `thistemperature`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: wname = Name of the wave:; thistemperature = Temperature (Kelvin). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `xcsr()`, `x2pnt()`, `funcFitFDwithEffT()`, `wname()`.
- Parameters:
  - `wname`: Name of the wave:
  - `thistemperature`: Temperature (Kelvin)
- Source: `Pierre's Template.ipf:22466`

#### `FTc`

```igor
Proc FTc(avedia)
```
- Usage: run from Igor with parameters `avedia`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: avedia = The FWHM in real space (guassian window). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `FT()`, `Const2dfilter()`.
- Parameters:
  - `avedia`: The FWHM in real space (guassian window)
- Source: `FFT.ipf:1525`

#### `FTclp`

```igor
Proc FTclp(avedia,ind1,ind2)
```
- Usage: run from Igor with parameters `avedia`, `ind1`, `ind2`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: avedia = The FWHM in real space (guassian window). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `FTlp()`, `Const2dfilterlp()`.
- Parameters:
  - `avedia`: The FWHM in real space (guassian window)
  - `ind1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ind2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:2223`

#### `FTlinecutc`

```igor
Proc FTlinecutc(name,avedia,q0,ind1,ind2)
```
- Usage: run from Igor with parameters `name`, `avedia`, `q0`, `ind1`, `ind2`. Update-log guidance - 2023-09-13: FFT filter for linecuts along the Y direction.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: name = The Linecut to be filtered; avedia = Real space Gaussian window; q0 = Q0 locked (1/a); ind1 = Tip 1; ind2 = Tip 2. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `waveDims()`, `onedpad2d()`, `FTlinecut1d()`, `mode()`, `marker()`.
- Parameters:
  - `name`: The Linecut to be filtered
  - `avedia`: Real space Gaussian window
  - `q0`: Q0 locked (1/a)
  - `ind1`: Tip 1
  - `ind2`: Tip 2
- Source: `FFT.ipf:1929`

#### `gpac`

```igor
Proc gpac()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `getall2dwaveongraph()`, `Gpa()`.
- Source: `FFT.ipf:1401`

#### `gpac3dp`

```igor
Proc gpac3dp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `Gpa3dp()`.
- Source: `Smart_3D_Viewer_New.ipf:8383`

#### `gpac3dpls`

```igor
Proc gpac3dpls()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Main internal calls: `Gpa3dpls()`.
- Source: `Smart_3D_Viewer_New.ipf:9329`

#### `gpbc`

```igor
Proc gpbc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `getall2dwaveongraph()`, `GpB()`.
- Source: `FFT.ipf:1468`

#### `gpbc3dp`

```igor
Proc gpbc3dp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `GpB3dp()`.
- Source: `Smart_3D_Viewer_New.ipf:8440`

#### `gpbc3dpls`

```igor
Proc gpbc3dpls()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Main internal calls: `GpB3dpls()`.
- Source: `Smart_3D_Viewer_New.ipf:9395`

#### `gpc`

```igor
Proc gpc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis. Main internal calls: `Gp()`, `tpw()`.
- Source: `FFT.ipf:1708`

#### `linecutFFT`

```igor
Proc linecutFFT(namedata) // input the name of the linecut
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `namedata`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `linkstsmapfftlinecut()`.
- Parameters:
  - `namedata`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `FFT.ipf:834`

#### `multi4fold`

```igor
proc multi4fold(num,name,lattice,theta)
```
- Usage: run from Igor with parameters `num`, `name`, `lattice`, `theta`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; lattice, moire, or twist-angle simulation/analysis. Main internal calls: `FFTpro()`, `C4symFFT()`.
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `FFT.ipf:222`

#### `offtheset`

```igor
proc offtheset(mat,delta,num)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `mat`, `delta`, `num`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `offset()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `delta`: Step size or energy/voltage increment used by the calculation.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:6004`

#### `PhaseMap`

```igor
Proc PhaseMap(name1,name2,size1,x1,y1,x2,y2)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name1`, `name2`, `size1`, `x1`, `y1`, `x2`, `y2`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: name1 = name of the gap map; name2 = name of the topography; size1 = dimsize of the subwindow (must be even number, but I write a correction code inside, you do not need to care about the even odd here); x1 = x of FFT Point 1; y1 = y of FFT Point 1; x2 = x of FFT Point 2. Code behavior: performs Fourier-transform or inverse-transform operations; asks the user for parameters through Igor prompts. Main internal calls: `makechunkdataFFT()`.
- Parameters:
  - `name1`: name of the gap map
  - `name2`: name of the topography
  - `size1`: dimsize of the subwindow (must be even number, but I write a correction code inside, you do not need to care about the even odd here)
  - `x1`: x of FFT Point 1
  - `y1`: y of FFT Point 1
  - `x2`: x of FFT Point 2
  - `y2`: y of FFT Point 2
- Source: `Map_the_phase_difference_between_two_image.ipf:247`

#### `pickupPA`

```igor
Proc pickupPA()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing. Main internal calls: `pcsr()`, `qcsr()`.
- Source: `Map_the_phase_difference_between_two_image.ipf:131`

#### `pickupPB`

```igor
Proc pickupPB()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing. Main internal calls: `pcsr()`, `qcsr()`.
- Source: `Map_the_phase_difference_between_two_image.ipf:185`

#### `SumlayerFFT3D2`

```igor
Proc SumlayerFFT3D2(name,startE,endE,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `startE`, `endE`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Dialog prompts: name = 3D wave; sel = All range?. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `SumlayerFFT3D()`.
- Parameters:
  - `name`: 3D wave
  - `startE`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `endE`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: All range?
- Source: `Smart_3D_Viewer_New.ipf:10274`

#### `SumlayerFFT3Dc`

```igor
Proc SumlayerFFT3Dc(name,startE,endE)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `startE`, `endE`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Main internal calls: `SumlayerFFT3D()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `startE`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `endE`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:10296`

#### `t2dlockinandFilter`

```igor
Proc t2dlockinandFilter(name,avedia,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `avedia`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Dialog prompts: name = Matrix input; default is the matrix grabbed by; avedia = The FWHM in real space (guassian window); sel = Which Q?. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `lockinpnew()`, `FTnew()`, `Dowindow2dlockinandfilter()`, `Const2dlockinftd()`.
- Parameters:
  - `name`: Matrix input; default is the matrix grabbed by
  - `avedia`: The FWHM in real space (guassian window)
  - `sel`: Which Q?
- Source: `Lawler-Fujita Drift Correction.ipf:2233`

#### `t2dlockinmultic`

```igor
proc t2dlockinmultic(sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: sel = Which Q used?. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `t2dlockinmulti()`, `ckfig_child()`, `grabwinchild()`, `grabwinnonew()`.
- Parameters:
  - `sel`: Which Q used?
- Source: `Smart_3D_Viewer_New.ipf:8496`


### Windows, panels, and menus

#### `Help_FFT_filter`

```igor
Window Help_FFT_filter() : Graph
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call `Help_FFT_filter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10376`


### Computational and data helpers

#### `color3s_for3dmprqpi`

```igor
Function color3s_for3dmprqpi(name,tt)
```
- Notes: ## Return the sigma range value Usage: call as `color3s_for3dmprqpi(name, tt)`. Code behavior: extracts image/wave statistics. Main internal calls: `gethistgram_npcolor()`, `max()`. Update log: 2023-12-03: Returns a symmetric sigma range for PR-QPI diverging color scales; it is intended for use in `ModifyImage ...
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `PR_QPI.ipf:265`

#### `colorPRQPI`

```igor
Function colorPRQPI()
```
- Notes: ## Set color Usage: call `colorPRQPI()` from Igor procedure code or the command line. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_zeroNaN()`, `max()`, `grabwinchild()`, `color3s_for3dmprqpi()`, `color3s_for3d()`.
- Source: `PR_QPI.ipf:224`

#### `Complextorealf`

```igor
Function Complextorealf(name1w,select)
```
- Usage: call as `Complextorealf(name1w, select)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `di()`, `color3s_for3dFFT()`.
- Parameters:
  - `name1w`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `FFT.ipf:584`

#### `Complextorealf1`

```igor
Function Complextorealf1(name1w,select)
```
- Usage: call as `Complextorealf1(name1w, select)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `FFT.ipf:1306`

#### `correctboundPhMap`

```igor
Function correctboundPhMap(nn,ref,block)
```
- Usage: call as `correctboundPhMap(nn, ref, block)`. Update-log guidance - 2023-11-09: Corrects boundary artifacts in moving-window phase-difference maps.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `findjumpphMap()`, `di()`. It removes artificial phase differences that appear when the moving window crosses a lattice phase jump, and is connected to the "FixBnd" button.
- Parameters:
  - `nn`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ref`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `block`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Map_the_phase_difference_between_two_image.ipf:521`

#### `DBD_PRQPI_cP_w`

```igor
Function DBD_PRQPI_cP_w(wg)
```
- Notes: ############################################################## #02 Old version No Phase FFT w/ cvtcmplx of source data ############################################################## Usage: call as `DBD_PRQPI_cP_w(wg)`. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `gammaNoise()`, `cvtcmplx()`.
- Parameters:
  - `wg`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:517`

#### `DBD_PRQPI_cP_wo`

```igor
Function DBD_PRQPI_cP_wo(wg)
```
- Notes: ############################################################## #01 Old version No Phase FFT w/o cvtcmplx of source data ############################################################## Usage: call as `DBD_PRQPI_cP_wo(wg)`. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `extendffterealimage_c()`, `gammaNoise()`, `C4sym_PRQPI()`.
- Parameters:
  - `wg`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:396`

#### `DBD_PRQPI_FP_w`

```igor
Function DBD_PRQPI_FP_w(wg)
```
- Notes: ## Main Functional Body Usage: call as `DBD_PRQPI_FP_w(wg)`. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `gammaNoise()`, `cvtcmplx()`.
- Parameters:
  - `wg`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:63`

#### `DBD_PRQPI_FP_wo`

```igor
Function DBD_PRQPI_FP_wo(wg)
```
- Notes: ############################################################## #03 Old version Phase FFT w/o cvtcmplx of source data ############################################################## Usage: call as `DBD_PRQPI_FP_wo(wg)`. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `extendffterealimage_c()`, `gammaNoise()`, `extendffterealimage()`.
- Parameters:
  - `wg`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:629`

#### `determineqbyphase`

```igor
Function determineqbyphase(grid)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `determineqbyphase(grid)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `phaseextractionforq()`, `getslopmap()`, `di()`. Update log: 2024-05-09: Backend calculation for Refine Q.
- Parameters:
  - `grid`: dimsize of x direction
- Source: `FFT.ipf:2520`

#### `di3`

```igor
Function di3(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `di3(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `killgraphonlyhavethewave()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:1684`

#### `DoFFT_3dplot`

```igor
Function DoFFT_3dplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `DoFFT_3dplot()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `elseif()`, `func_naN0()`, `FTguassianremover()`, `D4_sym_3dplot()`, `Mdiag_sym_3dplot()`, `Mx_sym_3dplot()`.
- Source: `Smart_3D_Viewer_New.ipf:5616`

#### `DoFFT_3dplot_marquee`

```igor
Function DoFFT_3dplot_marquee()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `DoFFT_3dplot_marquee()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `elseif()`, `frommarqueegetsubmatrix()`, `func_naN0()`, `Complextoimag()`.
- Source: `Smart_3D_Viewer_New.ipf:9675`

#### `Dowindow2dlockinandfilter`

```igor
Function Dowindow2dlockinandfilter(name,avedia,qax,qay,qbx,qby)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `Dowindow2dlockinandfilter(name, avedia, qax, qay, qbx, qby)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `modifyphasetoponew()`, `modifyphasefrnew()`, `modifyphasenew()`, `color3s_for3dmf()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qax`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `qay`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `qbx`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `qby`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Lawler-Fujita Drift Correction.ipf:2460`

#### `extendffterealimage`

```igor
Function extendffterealimage(name) // For real wave
```
- Usage: call as `extendffterealimage(name)`.
- Notes: ## Half-FFT to full FFT image, do not rely on cvtcmplx() Purpose: performs Fourier, filtering, phase, or lock-in processing. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Update log: 2023-12-03: Expands the half reciprocal-space FFT of a real image into an XY-symmetric full image for PR-QPI and related visualization.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:293`

#### `extendffterealimage_c`

```igor
Function extendffterealimage_c(name) //For complex wave
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `extendffterealimage_c(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Update log: 2023-12-03: Complex-wave variant of `extendffterealimage`, used when the FFT data are kept in complex form.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:324`

#### `f`

```igor
Function f()
```
- Usage: call `f()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; formats graph axes, labels, colors, or annotations. Main internal calls: `tpw()`, `func_NaN0()`, `cvtcmplx()`, `Complextorealf()`. Update log: 2023-07-10: Performs a smart FFT on the first wave in the topmost graph; the workflow was later updated to handle arbitrary point counts and NaN-containing data.
- Source: `FFT.ipf:1087`

#### `ff`

```igor
Function ff(name)
```
- Usage: call as `ff(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf1()`, `twoDinterpolatexyFFT()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:1245`

#### `ff2`

```igor
Function ff2(name)
```
- Usage: call as `ff2(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf1()`, `twoDinterpolatexyFFT()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:1286`

#### `ffnointer`

```igor
Function ffnointer(name)
```
- Usage: call as `ffnointer(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `func_NaN0()`, `cvtcmplx()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:1221`

#### `FFTL2`

```igor
Function FFTL2(name,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTL2(name, sel)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Code behavior: performs Fourier-transform or inverse-transform operations. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf()`, `tpw()`, `mean()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `sel`: 1 for x, 2 for y
- Source: `FFT.ipf:796`

#### `FFTL2_3dplot`

```igor
Function FFTL2_3dplot(name)
```
- Usage: call as `FFTL2_3dplot(name)`.
- Notes: ##000 1D FT along y for the linecuts Purpose: displays waves, images, contours, or graph overlays. Code behavior: performs Fourier-transform or inverse-transform operations; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf_3dplot()`, `mean()`, `FTgremover_3dploty()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:1181`

#### `FFTr`

```igor
Function FFTr(name,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTr(name, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `di()`, `color3sfft()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:1062`

#### `FFTrls`

```igor
Function FFTrls(name,sel)
```
- Usage: call as `FFTrls(name, sel)`.
- Notes: #IV.3_1 Special FFT Function Purpose: performs Fourier, filtering, phase, or lock-in processing. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealfls()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lattice Segregation.ipf:1251`

#### `FFTrls2`

```igor
Function FFTrls2(name,sel,inter)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTrls2(name, sel, inter)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `twoDinterpFFTover()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `inter`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:982`

#### `FFTrQPIsimu`

```igor
Function FFTrQPIsimu(name)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTrQPIsimu(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Models.ipf:12274`

#### `FFTsymind`

```igor
Function FFTsymind(mat)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTsymind(mat)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:91`

#### `FFTsymind2`

```igor
Function FFTsymind2(mat)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTsymind2(mat)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:4763`

#### `FFTsymind2r`

```igor
Function FFTsymind2r(mat)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTsymind2r(mat)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Miscellaneous_Codes.ipf:3984`

#### `findjumpphMap`

```igor
Function findjumpphMap(nn)
```
- Usage: call as `findjumpphMap(nn)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `nn`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Map_the_phase_difference_between_two_image.ipf:564`

#### `findpolarization`

```igor
Function findpolarization(DA,DB,TA,TB)
```
- Usage: call as `findpolarization(DA, DB, TA, TB)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `shift2pi()`, `modifyphasenew()`, `modifyphasetoponew()`.
- Parameters:
  - `DA`: Used in: Display/HOST=#/W=(0,0.05,0.33,0.33);appendimage DA;ModifyGraph width={Plan,1,bottom,left},noLabel=2,axThick=0;TextBox/C/N=text0/F=0/X=0.00/Y
  - `DB`: Used in: setActiveSubwindow ##;Display/HOST=#/W=(0,0.3,0.33,0.58);appendimage DB;ModifyGraph width={Plan,1,bottom,left},noLabel=2,axThick=0;TextBox/C
  - `TA`: Used in: setActiveSubwindow ##;Display/HOST=#/W=(0.31,0.05,0.64,0.33);appendimage TA;ModifyGraph width={Plan,1,bottom,left},noLabel=2,axThick=0;TextB
  - `TB`: Used in: setActiveSubwindow ##;Display/HOST=#/W=(0.31,0.3,0.64,0.58);appendimage TB;ModifyGraph width={Plan,1,bottom,left},noLabel=2,axThick=0;TextBo
- Source: `Map_the_phase_difference_between_two_image.ipf:20`

#### `FT`

```igor
Function FT(name,avedia)
```
- Usage: call as `FT(name, avedia)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `grabwin()`, `margin()`, `tpw()`, `color3s()`, `grabwinnonew()`, `modifyphasetopo()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:1533`

#### `FTcons`

```igor
Function FTcons(name,avedia)
```
- Usage: call as `FTcons(name, avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `unpadding()`, `grabwin()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:1790`

#### `FTconslp`

```igor
Function FTconslp(avedia)
```
- Usage: call as `FTconslp(avedia)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `unpadding()`, `tpw()`.
- Parameters:
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:2348`

#### `FTlinecut`

```igor
Function FTlinecut(name,avedia,q0)
```
- Usage: call as `FTlinecut(name, avedia, q0)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `q0`: = 0.28483
- Source: `FFT.ipf:1994`

#### `FTlinecut1D`

```igor
Function FTlinecut1D(name,avedia,q0)
```
- Usage: call as `FTlinecut1D(name, avedia, q0)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `ysame()`, `lsize()`, `rgb()`, `ckfig()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `q0`: = 0.28483
- Source: `FFT.ipf:2070`

#### `FTlinecutcons`

```igor
Function FTlinecutcons(avedia,q0)
```
- Usage: call as `FTlinecutcons(avedia, q0)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `avedia`: = avedia_2dlinecut
  - `q0`: =q0_2dlinecut
- Source: `FFT.ipf:2033`

#### `FTlinecutcons1d`

```igor
Function FTlinecutcons1d(avedia,q0)
```
- Usage: call as `FTlinecutcons1d(avedia, q0)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `ysame()`.
- Parameters:
  - `avedia`: = avedia_2dlinecut
  - `q0`: =q0_2dlinecut
- Source: `FFT.ipf:2121`

#### `FTlp`

```igor
Function FTlp(avedia)
```
- Usage: call as `FTlp(avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `twoDinterpolatexyf2()`, `twoDinterpyf()`, `unpadding()`, `di()`.
- Parameters:
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:2232`

#### `funcFFTfilterdata`

```igor
Function funcFFTfilterdata(matname,badlist)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `funcFFTfilterdata(matname, badlist)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data. Main internal calls: `Interp()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `badlist`: Used in: Duplicate/O badlist sortedbad
- Source: `Pierre's Template.ipf:3564`

#### `funcFitFDwithEffT`

```igor
Function funcFitFDwithEffT(parawave,x) : fitfunc
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `funcFitFDwithEffT(parawave, x)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `parawave`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:22458`

#### `getslopmap`

```igor
Function getslopmap(name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getslopmap(name)`.
- Notes: Utility function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; differentiates wave data. Main internal calls: `median()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `FFT.ipf:2789`

#### `GoldFittingp`

```igor
function GoldFittingp(dos,Tem) //the same with fitgold(dos,Tem), only different is user need to guess all the initial values of the six parameters.
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `GoldFittingp(dos, Tem)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: Wfit0 = fermi level; Wfit1 = fitting temperature; Wfit2 = below Ef line's Y value when x=0; Wfit3 = the slope of the below Ef line; Wfit4 = above Ef line's Y value when x=0; Wfit5 = the slope of the above Ef line. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; differentiates wave data. Main internal calls: `csrinfo()`, `hcsr()`, `vcsr()`, `dos()`.
- Parameters:
  - `dos`: Used in: FuncFit/Q line_fermi Wfit dos(startx, endx) /D / FuncFit/Q line_fermi Wfit dos(startx, endx) /D
  - `Tem`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `Pierre's Template.ipf:36780`

#### `Gp`

```igor
Function/Wave Gp(name111)
```
- Usage: call as `Gp(name111)`.
- Notes: Wave-returning helper for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: performs Fourier-transform or inverse-transform operations; runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; writes or exports data. Main internal calls: `pcsr()`, `qcsr()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `FFT.ipf:1711`

#### `GpA`

```igor
Function/Wave GpA(name111,nameraw)
```
- Usage: call as `GpA(name111, nameraw)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `atan2()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
  - `nameraw`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:1406`

#### `GpA3dp`

```igor
Function/Wave GpA3dp(name111)
```
- Usage: call as `GpA3dp(name111)`.
- Notes: Wave-returning helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Smart_3D_Viewer_New.ipf:8387`

#### `GpA3dpls`

```igor
Function/Wave GpA3dpls(name111)
```
- Usage: call as `GpA3dpls(name111)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Smart_3D_Viewer_New.ipf:9337`

#### `GpA_twistgofit`

```igor
Function GpA_twistgofit(nameFFT,marqueewave)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `GpA_twistgofit(nameFFT, marqueewave)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `nameFFTw()`, `nametempw()`.
- Parameters:
  - `nameFFT`: Igor wave or matrix input consumed by the calculation.
  - `marqueewave`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Triangle Lattice_Graphene_like.ipf:175`

#### `GpAarea`

```igor
Function/Wave GpAarea(nameraw,rangA)
```
- Notes: #2_03_01 Extract Shearing: Main called get Q1 Usage: call as `GpAarea(nameraw, rangA)`. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`, `grabwinnonew()`.
- Parameters:
  - `nameraw`: Name of the layer image
  - `rangA`: Used in: CurveFit/Q gauss2D name111w(rangA[0],rangA[1])(rangA[2],rangA[3]) /D=name1111w(rangA[0],rangA[1])(rangA[2],rangA[3]);
- Source: `Smart_3D_Viewer_New.ipf:5335`

#### `GpB`

```igor
Function/Wave GpB(name111,nameraw)
```
- Usage: call as `GpB(name111, nameraw)`.
- Notes: Wave-returning helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
  - `nameraw`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:1473`

#### `GpB3dp`

```igor
Function/Wave GpB3dp(name111)
```
- Usage: call as `GpB3dp(name111)`.
- Notes: Wave-returning helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Smart_3D_Viewer_New.ipf:8444`

#### `GpB3dpls`

```igor
Function/Wave GpB3dpls(name111)
```
- Usage: call as `GpB3dpls(name111)`.
- Notes: Wave-returning helper for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Smart_3D_Viewer_New.ipf:9403`

#### `GpB_twistgofit`

```igor
Function GpB_twistgofit(nameFFT,marqueewave)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `GpB_twistgofit(nameFFT, marqueewave)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `nameFFTw()`, `nametempw()`.
- Parameters:
  - `nameFFT`: Igor wave or matrix input consumed by the calculation.
  - `marqueewave`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Triangle Lattice_Graphene_like.ipf:198`

#### `GpBarea`

```igor
Function/Wave GpBarea(nameraw,rangB)
```
- Notes: #2_03_02 Extract Shearing: Main called get Q2 Usage: call as `GpBarea(nameraw, rangB)`. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`, `grabwinnonew()`.
- Parameters:
  - `nameraw`: Name of the layer image
  - `rangB`: Used in: CurveFit/Q gauss2D name111w(rangB[0],rangB[1])(rangB[2],rangB[3]) /D=name1111w(rangB[0],rangB[1])(rangB[2],rangB[3]);
- Source: `Smart_3D_Viewer_New.ipf:5384`

#### `GpC_twistgofit`

```igor
Function GpC_twistgofit(nameFFT,marqueewave)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `GpC_twistgofit(nameFFT, marqueewave)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `nameFFTw()`, `nametempw()`.
- Parameters:
  - `nameFFT`: Igor wave or matrix input consumed by the calculation.
  - `marqueewave`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Triangle Lattice_Graphene_like.ipf:221`

#### `grabwin_sfft`

```igor
Function/s grabwin_sfft(name)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `grabwin_sfft(name)`.
- Notes: String-returning helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:6322`

#### `linkstsmapfftlinecut`

```igor
Function linkstsmapfftlinecut(name,num,startnum)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `linkstsmapfftlinecut(name, num, startnum)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `startnum`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:891`

#### `lockinp3dp`

```igor
Function lockinp3dp(name,qx1,qy1,qx2,qy2,avedia)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `lockinp3dp(name, qx1, qy1, qx2, qy2, avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing. Main internal calls: `unpadding()`, `atan2()`.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qx2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
- Source: `Smart_3D_Viewer_New.ipf:9005`

#### `makechunkdataFFT`

```igor
Function makechunkdataFFT(name1,name2,size1,x1,y1,x2,y2)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makechunkdataFFT(name1, name2, size1, x1, y1, x2, y2)`.
- Notes: Utility function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `twoDinterpolatexyfd()`, `margin()`.
- Parameters:
  - `name1`: String name used to locate an Igor wave, graph, folder, or output object.
  - `name2`: String name used to locate an Igor wave, graph, folder, or output object.
  - `size1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `y1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `x2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `y2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Map_the_phase_difference_between_two_image.ipf:276`

#### `MDoLSegrereapplyfftmode`

```igor
Function MDoLSegrereapplyfftmode()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call `MDoLSegrereapplyfftmode()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves. Main internal calls: `FFTrls()`, `color3s_for3dm2()`, `color3s_for3dmf()`, `color3s_followtopo()`, `color3s_followtopof()`, `color3s_followtopo2()`.
- Source: `Lattice Segregation.ipf:1042`

#### `MinEnvelopeFilter`

```igor
Function MinEnvelopeFilter(wave1, outputWave, window1)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `MinEnvelopeFilter(wave1, outputWave, window1)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `numpnts()`, `max()`, `min()`.
- Parameters:
  - `wave1`: Igor wave reference used as input or output.
  - `outputWave`: Igor wave reference used as input or output.
  - `window1`: Igor wave reference used as input or output.
- Source: `ModelingTunneling.ipf:926`

#### `phaseextractionforq`

```igor
Function phaseextractionforq(name,qx1,qy1,avedia)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `phaseextractionforq(name, qx1, qy1, avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `atan2()`.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
- Source: `FFT.ipf:2607`

#### `phaseextractionforq_pad`

```igor
Function phaseextractionforq_pad(name,qx1,qy1,avedia)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `phaseextractionforq_pad(name, qx1, qy1, avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `unpadding()`, `atan2()`.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
- Source: `FFT.ipf:2701`

#### `saveA_refineQ`

```igor
Function saveA_refineQ()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: call `saveA_refineQ()` from Igor procedure code or the command line.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations.
- Source: `FFT.ipf:2468`

#### `saveB_refineQ`

```igor
Function saveB_refineQ()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: call `saveB_refineQ()` from Igor procedure code or the command line.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations.
- Source: `FFT.ipf:2495`

#### `smoothphasejump`

```igor
Function smoothphasejump(xx,tt)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `smoothphasejump(xx, tt)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Main internal calls: `tanh()`.
- Parameters:
  - `xx`: the angle input
  - `tt`: control the jump smoothness, 0 is very sharp
- Source: `Models.ipf:11036`

#### `SumlayerFFT3D`

```igor
Function SumlayerFFT3D(name,startE,endE)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `SumlayerFFT3D(name, startE, endE)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; integrates wave data. Main internal calls: `d()`, `tpw()`, `imageinfo()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `startE`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `endE`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:10303`

#### `SWFFTgettwistangle`

```igor
Function SWFFTgettwistangle(nameFFT)
```
- Usage: call as `SWFFTgettwistangle(nameFFT)`.
- Notes: Read out the twist angle Purpose: performs Fourier, filtering, phase, or lock-in processing. Code behavior: creates output waves. Main internal calls: `GpA_twistgofit()`, `GpB_twistgofit()`, `GpC_twistgofit()`, `atan2()`.
- Parameters:
  - `nameFFT`: "name of FFT of window image"
- Source: `Triangle Lattice_Graphene_like.ipf:151`

#### `t2dlockinmulti`

```igor
Function t2dlockinmulti(sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `t2dlockinmulti(sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `lockinp3dp()`, `levelimage2()`.
- Parameters:
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:8628`

#### `twoDinterpFFTover`

```igor
Function twoDinterpFFTover(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpFFTover(name, xpoint, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:1098`

#### `twoDinterpolatexyFFT`

```igor
Function twoDinterpolatexyFFT(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexyFFT(name, xpoint, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:1333`

#### `twoDinterpolatexyFFT3dp`

```igor
Function twoDinterpolatexyFFT3dp(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexyFFT3dp(name, xpoint, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `makematrix2()`, `makematrix3()`, `grabwin2()`, `dilf()`, `color3s()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:8317`

#### `twoDinterpolatexyFFT3dpls`

```igor
Function twoDinterpolatexyFFT3dpls(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexyFFT3dpls(name, xpoint, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `makematrix2()`, `makematrix3()`, `grabwin2()`, `dilf()`, `color3s()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:9263`

#### `twoDinterpolatexyFFT_3dplot`

```igor
Function twoDinterpolatexyFFT_3dplot(name,xpoint,ypoint)
```
- Usage: call as `twoDinterpolatexyFFT_3dplot(name, xpoint, ypoint)`.
- Notes: #0_2_01 PreFFT: Main_called Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `makematrix2()`, `makematrix3()`, `grabwin2()`, `dilf()`, `color3s()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:4539`

#### `twoDinterpolatexyFFT_3dplotn`

```igor
Function twoDinterpolatexyFFT_3dplotn(name,xpoint,ypoint)
```
- Usage: call as `twoDinterpolatexyFFT_3dplotn(name, xpoint, ypoint)`.
- Notes: #2_04_02 Extract Shearing: Main called Interoplate FFT without display Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`, `color3s()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:5452`

#### `ysame`

```igor
Function ysame(name,ref)
```
- Usage: call as `ysame(name, ref)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `ref`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:2173`



## Matrix, Cube, Wave, and Batch Operations

### Panel callbacks and control handlers

#### `B_rescaleforspec_orientations`

```igor
Function B_rescaleforspec_orientations(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `rescaleforspec_orientations()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Rescale for special orientations`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rescale for special orientations`
- Source: `Pierre's Template.ipf:25236`

#### `Button_Help_BkgndremoverWavein`

```igor
Function Button_Help_BkgndremoverWavein(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_Bkgndremover_Wave_in()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9992`

#### `Button_Help_NaN0mat3d`

```igor
Function Button_Help_NaN0mat3d(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_NaN0mat3d()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9581`

#### `Button_Help_Norm_matrix`

```igor
Function Button_Help_Norm_matrix(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_Norm_matrix()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10055`

#### `Button_Help_Smooth_factor`

```igor
Function Button_Help_Smooth_factor(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_Smooth_factor()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9545`

#### `Button_Help_Smooth_mat`

```igor
Function Button_Help_Smooth_mat(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_Smooth_mat()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9437`

#### `Button_wave4bkgndremoval`

```igor
Function Button_wave4bkgndremoval(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `wave4bkgndremoval()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `kgnd in`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `kgnd in`, `kgnd in`
- Source: `Pierre's Template.ipf:531`

#### `ButtonDuplicate_hvmat3d`

```igor
Function ButtonDuplicate_hvmat3d(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: call as `ButtonDuplicate_hvmat3d(ctrlName)`.
- Notes: Panel callback/helper for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `-->mat3d`. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `-->mat3d`, `-->mat3d`
- Source: `Pierre's Template.ipf:35660`

#### `ButtonDuplicate_kzmat3d`

```igor
Function ButtonDuplicate_kzmat3d(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: call as `ButtonDuplicate_kzmat3d(ctrlName)`.
- Notes: Panel callback/helper for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `-->mat3d`. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `-->mat3d`, `-->mat3d`
- Source: `Pierre's Template.ipf:35665`

#### `ButtonProc_automatrixTC`

```igor
Function ButtonProc_automatrixTC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `automatrixTC()`.
- Notes: # 01. Proc Head Panel control(s): `Cal. Matrix H(k)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cal. Matrix H(k)`
- Source: `MatrixCalculation.ipf:79`

#### `ButtonProc_deletepoint`

```igor
Function ButtonProc_deletepoint(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `DeletePdata()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `DeletePoints`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `DeletePoints`
- Source: `Miscellaneous_Codes.ipf:1845`

#### `ButtonProc_duplicateall`

```igor
Function ButtonProc_duplicateall(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `duplicateall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Dup All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Dup All`
- Source: `Miscellaneous_Codes.ipf:6047`

#### `ButtonProc_duplicatekly`

```igor
Function ButtonProc_duplicatekly(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `duplicatekly()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:1307`

#### `ButtonProc_duplicatepart`

```igor
Function ButtonProc_duplicatepart(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `duplicatpart()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `x1~x2`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `x1~x2`
- Source: `Miscellaneous_Codes.ipf:6018`

#### `ButtonProc_file2wave`

```igor
Function ButtonProc_file2wave(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: triggered as a wrapper that opens/runs `file2wave()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `File 2 wave`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `File 2 wave`, `File 2 wave`
- Source: `Pierre's Template.ipf:27045`

#### `ButtonProc_Fmarqueegetsub`

```igor
Function ButtonProc_Fmarqueegetsub(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Frommarqueegetsubmatrixsc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Partial by Marquee`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Partial by Marquee`
- Source: `Smart_3D_Viewer_New.ipf:9829`

#### `ButtonProc_interpavedata`

```igor
Function ButtonProc_interpavedata(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `interpavedata()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Intra`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Intra`
- Source: `Miscellaneous_Codes.ipf:7177`

#### `ButtonProc_Interpoint`

```igor
Function ButtonProc_Interpoint(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `intercurve()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Interpolate All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Interpolate All`
- Source: `Miscellaneous_Codes.ipf:5783`

#### `ButtonProc_InterpSTS`

```igor
Function ButtonProc_InterpSTS(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `interpsts()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `InterpL`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `InterpL`
- Source: `Miscellaneous_Codes.ipf:5659`

#### `ButtonProc_Killwave`

```igor
Function ButtonProc_Killwave(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `KillWave()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1100`

#### `ButtonProc_Lsmatrix3d`

```igor
Function ButtonProc_Lsmatrix3d(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Lsmatrix3d()`.
- Notes: V.Lattice segregation on the whole 3D matrix Panel control(s): `Make3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make3D`
- Source: `Lattice Segregation.ipf:1644`

#### `ButtonProc_madewavebytemplate`

```igor
Function ButtonProc_madewavebytemplate(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `madewavebytemplatec()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Format wave A as B`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Format wave A as B`
- Source: `Miscellaneous_Codes.ipf:12935`

#### `ButtonProc_make_mat3dk`

```igor
Function ButtonProc_make_mat3dk(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `make_mat3dk()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3dk`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3dk`, `Mat3dk`
- Source: `Pierre's Template.ipf:26940`

#### `ButtonProc_make_mat3dkHD`

```igor
Function ButtonProc_make_mat3dkHD(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `make_mat3dkHD()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3dkHD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3dkHD`, `Mat3dkHD`
- Source: `Pierre's Template.ipf:26950`

#### `ButtonProc_make_mat3dkVD`

```igor
Function ButtonProc_make_mat3dkVD(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `make_mat3dkVD()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3dkVD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3dkVD`, `Mat3dkVD`
- Source: `Pierre's Template.ipf:26945`

#### `ButtonProc_mat3d_makeimage`

```igor
Function ButtonProc_mat3d_makeimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3d_makeimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make image`
- Source: `Pierre's Template.ipf:26955`

#### `ButtonProc_mat3d_pi`

```igor
Function ButtonProc_mat3d_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3d_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3D`, `Mat3D`
- Source: `Pierre's Template.ipf:26995`

#### `ButtonProc_mat3dHC_pi`

```igor
Function ButtonProc_mat3dHC_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dHC_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `C`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `C`, `C`, `C`, `C`
- Source: `Pierre's Template.ipf:27015`

#### `ButtonProc_mat3dHD_makeimage`

```igor
Function ButtonProc_mat3dHD_makeimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dHD_makeimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make image`
- Source: `Pierre's Template.ipf:26965`

#### `ButtonProc_mat3dHD_pi`

```igor
Function ButtonProc_mat3dHD_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dHD_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `3dHD`, `HD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3dHD`, `HD`, `3dHD`, `HD`
- Source: `Pierre's Template.ipf:27010`

#### `ButtonProc_mat3dk_makeimage`

```igor
Function ButtonProc_mat3dk_makeimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dk_makeimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make image`
- Source: `Pierre's Template.ipf:26970`

#### `ButtonProc_mat3dk_pi`

```igor
Function ButtonProc_mat3dk_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dk_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3dk`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3dk`, `Mat3dk`
- Source: `Pierre's Template.ipf:27020`

#### `ButtonProc_mat3dkHD_makeimage`

```igor
Function ButtonProc_mat3dkHD_makeimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dkHD_makeimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make image`
- Source: `Pierre's Template.ipf:26980`

#### `ButtonProc_mat3dkHD_pi`

```igor
Function ButtonProc_mat3dkHD_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dkHD_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3dkHD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3dkHD`, `Mat3dkHD`
- Source: `Pierre's Template.ipf:27030`

#### `ButtonProc_mat3dkHDrot`

```igor
Function ButtonProc_mat3dkHDrot(ctrlName) : ButtonControl//"fit MDC"
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `mat3dkHDrot()`.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Panel control(s): `Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Q`, `Q`
- Source: `Pierre's Template.ipf:31669`

#### `ButtonProc_mat3dkrot`

```igor
Function ButtonProc_mat3dkrot(ctrlName) : ButtonControl//"fit MDC"
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `mat3dkrot()`.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Panel control(s): `Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Q`, `Q`
- Source: `Pierre's Template.ipf:31641`

#### `ButtonProc_mat3dkVD_makeimage`

```igor
Function ButtonProc_mat3dkVD_makeimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dkVD_makeimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make image`
- Source: `Pierre's Template.ipf:26975`

#### `ButtonProc_mat3dkVD_pi`

```igor
Function ButtonProc_mat3dkVD_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dkVD_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Mat3dkVD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mat3dkVD`, `Mat3dkVD`
- Source: `Pierre's Template.ipf:27025`

#### `ButtonProc_mat3dkVDrot`

```igor
Function ButtonProc_mat3dkVDrot(ctrlName) : ButtonControl//"fit MDC"
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `mat3dkVDrot()`.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Panel control(s): `Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Q`, `Q`
- Source: `Pierre's Template.ipf:31655`

#### `ButtonProc_mat3dVC_pi`

```igor
Function ButtonProc_mat3dVC_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dVC_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `C`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `C`, `C`, `C`, `C`
- Source: `Pierre's Template.ipf:27005`

#### `ButtonProc_mat3dVD_makeimage`

```igor
Function ButtonProc_mat3dVD_makeimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dVD_makeimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make image`
- Source: `Pierre's Template.ipf:26960`

#### `ButtonProc_mat3dVD_pi`

```igor
Function ButtonProc_mat3dVD_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat3dVD_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `3dVD`, `VD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3dVD`, `VD`, `3dVD`, `VD`
- Source: `Pierre's Template.ipf:27000`

#### `ButtonProc_NaN0mat3d`

```igor
Function ButtonProc_NaN0mat3d(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `NaN0mat3d()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `NaN0mat3d`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `NaN0mat3d`, `NaN0mat3d`
- Source: `Pierre's Template.ipf:734`

#### `ButtonProc_normcut`

```igor
Function ButtonProc_normcut(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `normcut()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Norm matrix`, `Norm cut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Norm matrix`, `Norm cut`, `Norm matrix`
- Source: `Pierre's Template.ipf:995`

#### `ButtonProc_normwave`

```igor
Function ButtonProc_normwave(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `normwave()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `Normwave`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Normwave`, `Normwave`
- Source: `Pierre's Template.ipf:1000`

#### `ButtonProc_normwavemulti`

```igor
Function ButtonProc_normwavemulti(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `normrange()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `NumNorm_SR`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `NumNorm_SR`
- Source: `Miscellaneous_Codes.ipf:5725`

#### `ButtonProc_normwavemulti2`

```igor
Function ButtonProc_normwavemulti2(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `normrange2()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `NormNum_TR`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `NormNum_TR`
- Source: `Miscellaneous_Codes.ipf:6426`

#### `ButtonProc_padmc`

```igor
Function ButtonProc_padmc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `padmc()`.
- Notes: Padding two dimensional matrix Purpose: resamples, rescales, pads, or changes wave dimensions. Panel control(s): `Pad 2D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Pad 2D`
- Source: `Miscellaneous_Codes.ipf:3218`

#### `ButtonProc_renameall`

```igor
Function ButtonProc_renameall(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `renameall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Rename`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rename`
- Source: `Miscellaneous_Codes.ipf:5752`

#### `ButtonProc_renamep`

```igor
Function ButtonProc_renamep(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `renamep()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Partial`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Partial`
- Source: `Miscellaneous_Codes.ipf:9197`

#### `ButtonProc_renormcuts_k`

```igor
Function ButtonProc_renormcuts_k(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `renormcuts_k()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Rescale k`, `Rscl.*/`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rescale k`, `Rscl.*/`
- Source: `Miscellaneous_Codes.ipf:655`

#### `ButtonProc_rescale`

```igor
Function ButtonProc_rescale(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `rescale()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Resl.`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Resl.`
- Source: `Miscellaneous_Codes.ipf:10022`

#### `ButtonProc_rescale_pi`

```igor
Function ButtonProc_rescale_pi(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `rescale_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `Rscl.+-`, `Rescale`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rscl.+-`, `Rescale`
- Source: `Pierre's Template.ipf:944`

#### `ButtonProc_rescaleallpr`

```igor
Function ButtonProc_rescaleallpr(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `rescaleallpr()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Rsclall`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rsclall`
- Source: `Miscellaneous_Codes.ipf:9985`

#### `ButtonProc_rescalegroupc`

```igor
Function ButtonProc_rescalegroupc(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `rescalegroupc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Linecut with divider`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut with divider`
- Source: `Miscellaneous_Codes.ipf:12894`

#### `ButtonProc_rescalemapasa1dcurve`

```igor
Function ButtonProc_rescalemapasa1dcurve(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `rescalemapasa1dcurve()`.
- Notes: uneven y-axis value of the linecut, make sure the correct one-to-one correspondance, namecurve[p] --> namemap[q] The prepare the scaler, you should make sure dimsize($namecurve,0)= dimsize($namemap,1) you may encounter a situation that the y-axis is like "0.01 Purpose: resamples, rescales, pads, or changes wave dimensions. Panel control(s): `UnevenY`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `UnevenY`
- Source: `Miscellaneous_Codes.ipf:10059`

#### `ButtonProc_Rflwaves`

```igor
Function ButtonProc_Rflwaves(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `Rflwaves()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Reflection`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Reflection`
- Source: `Miscellaneous_Codes.ipf:3099`

#### `ButtonProc_Rm_simplebgnd_mat`

```igor
Function ButtonProc_Rm_simplebgnd_mat(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Remove_simplebgnd_mat()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Matrix`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Matrix`, `Matrix`
- Source: `Pierre's Template.ipf:829`

#### `ButtonProc_scale_e_pi`

```igor
Function ButtonProc_scale_e_pi(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `scale_e_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1080`

#### `ButtonProc_scalewave`

```igor
Function ButtonProc_scalewave(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `scalewaves()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Panel control(s): `Apply divider (single)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Apply divider (single)`
- Source: `Miscellaneous_Codes.ipf:12824`

#### `ButtonProc_setmatrixindex`

```igor
Function ButtonProc_setmatrixindex(ctrlName) : ButtonControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Initialize_Global_Variables()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:9821`

#### `ButtonProc_shrinkmatrixbysteps`

```igor
Function ButtonProc_shrinkmatrixbysteps(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `shrinkmatrixbystepsc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `CoarseM`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CoarseM`
- Source: `SmartDisplay.ipf:5011`

#### `ButtonProc_shrinktocube`

```igor
Function ButtonProc_shrinktocube(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `shrinktocube()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:742`

#### `ButtonProc_smoothall`

```igor
Function ButtonProc_smoothall(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `smoothall()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Smt All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Smt All`
- Source: `Miscellaneous_Codes.ipf:6138`

#### `ButtonProc_smoothallmatrix`

```igor
Function ButtonProc_smoothallmatrix(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `smoothallmatrix()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `(2D)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `(2D)`
- Source: `Miscellaneous_Codes.ipf:6115`

#### `ButtonProc_smoothmat_k`

```igor
Function ButtonProc_smoothmat_k(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `smoothmat_k()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Smooth`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Smooth`, `Smooth`
- Source: `Pierre's Template.ipf:774`

#### `ButtonProc_SmoothMat_k23`

```igor
Function ButtonProc_SmoothMat_k23(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `SmoothMat_k23()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Sm_BS`, `Smooth`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sm_BS`, `Smooth`
- Source: `Pierre's Template.ipf:7618`

#### `ButtonProc_trans2equiv_waves`

```igor
Function ButtonProc_trans2equiv_waves(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `transform2equiv_waves()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Equivalent waves`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Equivalent waves`, `Equivalent waves`
- Source: `Pierre's Template.ipf:794`

#### `ButtonProc_twoDinterpolatel`

```igor
Function ButtonProc_twoDinterpolatel(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `twoDinterpolatexy()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Interp NxN`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Interp NxN`
- Source: `FFT.ipf:337`

#### `ButtonProc_twoDinterpy`

```igor
Function ButtonProc_twoDinterpy(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `twoDinterpy()`.
- Notes: This procedure will only interpolate y axis Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Y`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Y`
- Source: `FFT.ipf:500`

#### `ButtonProc_twoDlinterp2Dall`

```igor
Function ButtonProc_twoDlinterp2Dall(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `interp2Dall()`.
- Notes: This procesure is designed for Interpolate a 2D matrix to arbiratary points. Panel control(s): `all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `all`
- Source: `FFT.ipf:315`

#### `ButtonProc_wave2bkgndremoved`

```igor
Function ButtonProc_wave2bkgndremoved(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `wave2bkgndremoved()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `Wave in`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Wave in`, `Wave in`
- Source: `Pierre's Template.ipf:563`

#### `ButtonProc_wave2file`

```igor
Function ButtonProc_wave2file(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: triggered as a wrapper that opens/runs `wave2file()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Wave 2 file`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Wave 2 file`, `Wave 2 file`
- Source: `Pierre's Template.ipf:27035`

#### `ButtonProc_wave2filenokill`

```igor
Function ButtonProc_wave2filenokill(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: triggered as a wrapper that opens/runs `wave2filenokill()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Wave2file (no kill)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Wave2file (no kill)`, `Wave2file (no kill)`
- Source: `Pierre's Template.ipf:27040`

#### `ButtonProc_xmultisall`

```igor
Function ButtonProc_xmultisall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `rescalexmultiwhat()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `V*`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `V*`
- Source: `Miscellaneous_Codes.ipf:5226`

#### `ButtonProc_xplusall`

```igor
Function ButtonProc_xplusall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `rescalex_plus()`.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `V+`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `V+`
- Source: `Miscellaneous_Codes.ipf:5221`

#### `ButtonProcInterp3D`

```igor
Function ButtonProcInterp3D(ctrlName) : ButtonControl
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: triggered as a wrapper that opens/runs `Interpmat3D()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Panel control(s): `Interp`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Interp`, `Interp`
- Source: `Pierre's Template.ipf:35793`

#### `CheckProcMat3d`

```igor
Function CheckProcMat3d(ctrlName,checked) : CheckBoxControl
```
- Usage: call as `CheckProcMat3d(ctrlName, checked)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `Mat3d`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `checked`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Panel controls: `Mat3d`
- Source: `Pierre's Template.ipf:36278`

#### `CheckProcMat3dHD`

```igor
Function CheckProcMat3dHD(ctrlName,checked) : CheckBoxControl
```
- Usage: call as `CheckProcMat3dHD(ctrlName, checked)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `Mat3dHD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `checked`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Panel controls: `Mat3dHD`
- Source: `Pierre's Template.ipf:36292`

#### `CheckProcMat3dk`

```igor
Function CheckProcMat3dk(ctrlName,checked) : CheckBoxControl
```
- Usage: call as `CheckProcMat3dk(ctrlName, checked)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `Mat3dk`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `checked`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Panel controls: `Mat3dk`
- Source: `Pierre's Template.ipf:36299`

#### `CheckProcMat3dkHD`

```igor
Function CheckProcMat3dkHD(ctrlName,checked) : CheckBoxControl
```
- Usage: call as `CheckProcMat3dkHD(ctrlName, checked)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `Mat3dkHD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `checked`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Panel controls: `Mat3dkHD`
- Source: `Pierre's Template.ipf:36313`

#### `CheckProcMat3dkVD`

```igor
Function CheckProcMat3dkVD(ctrlName,checked) : CheckBoxControl
```
- Usage: call as `CheckProcMat3dkVD(ctrlName, checked)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `Mat3dkVD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `checked`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Panel controls: `Mat3dkVD`
- Source: `Pierre's Template.ipf:36306`

#### `CheckProcMat3dVD`

```igor
Function CheckProcMat3dVD(ctrlName,checked) : CheckBoxControl
```
- Usage: call as `CheckProcMat3dVD(ctrlName, checked)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `Mat3dVD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `checked`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Panel controls: `Mat3dVD`
- Source: `Pierre's Template.ipf:36285`

#### `PopMenuProc_5`

```igor
Function PopMenuProc_5(ctrlName,popNum,popStr) : PopupMenuControl  //which matrix 3d for slicer
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Whichmat3d`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Whichmat3d`
- Source: `Pierre's Template.ipf:27430`

#### `PopMenuProc_Lsmatrix3d`

```igor
Function PopMenuProc_Lsmatrix3d(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `Lattice Segregation.ipf:1772`

#### `PopMenuProc_MatrixName`

```igor
Function PopMenuProc_MatrixName(ctrlName,popNum,popStr) //: PopopMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Source: `Pierre's Template.ipf:28505`

#### `setvarcenter_emat3d`

```igor
Function setvarcenter_emat3d(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3d_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Central energy`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Central energy`
- Source: `Pierre's Template.ipf:26839`

#### `setvarcenter_emat3dHD`

```igor
Function setvarcenter_emat3dHD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dHD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Central energy`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Central energy`
- Source: `Pierre's Template.ipf:26855`

#### `setvarcenter_emat3dk`

```igor
Function setvarcenter_emat3dk(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dk_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Central energy`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Central energy`
- Source: `Pierre's Template.ipf:26863`

#### `setvarcenter_emat3dkHD`

```igor
Function setvarcenter_emat3dkHD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dkHD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Central energy`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Central energy`
- Source: `Pierre's Template.ipf:26879`

#### `setvarcenter_emat3dkVD`

```igor
Function setvarcenter_emat3dkVD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dkVD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Central energy`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Central energy`
- Source: `Pierre's Template.ipf:26871`

#### `setvarcenter_emat3dVD`

```igor
Function setvarcenter_emat3dVD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dVD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Central energy`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Central energy`
- Source: `Pierre's Template.ipf:26847`

#### `setvarincrement_emat3d`

```igor
Function setvarincrement_emat3d(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3d_eplot_setincrement()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26791`

#### `setvarincrement_emat3dHD`

```igor
Function setvarincrement_emat3dHD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dHD_eplot_setincrement()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26807`

#### `setvarincrement_emat3dk`

```igor
Function setvarincrement_emat3dk(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dk_eplot_setincrement()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26815`

#### `setvarincrement_emat3dkHD`

```igor
Function setvarincrement_emat3dkHD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dkHD_eplot_setincrement()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26831`

#### `setvarincrement_emat3dkVD`

```igor
Function setvarincrement_emat3dkVD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dkVD_eplot_setincrement()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26823`

#### `setvarincrement_emat3dVD`

```igor
Function setvarincrement_emat3dVD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dVD_eplot_setincrement()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26799`

#### `SetVarProc_appendmatrix`

```igor
Function SetVarProc_appendmatrix(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Ctr`. Code behavior: opens or updates graph/image displays.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Ctr`
- Source: `Smart_3D_Viewer_New.ipf:9520`

#### `SetVarProc_smt`

```igor
Function SetVarProc_smt(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Smooth`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Smooth`
- Source: `Miscellaneous_Codes.ipf:7548`

#### `setvarwide_emat3d`

```igor
Function setvarwide_emat3d(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3d_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Integral range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral range`
- Source: `Pierre's Template.ipf:26887`

#### `setvarwide_emat3dHD`

```igor
Function setvarwide_emat3dHD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dHD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Integral range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral range`
- Source: `Pierre's Template.ipf:26903`

#### `setvarwide_emat3dk`

```igor
Function setvarwide_emat3dk(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dk_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Integral range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral range`
- Source: `Pierre's Template.ipf:26911`

#### `setvarwide_emat3dkHD`

```igor
Function setvarwide_emat3dkHD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dkHD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Integral range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral range`
- Source: `Pierre's Template.ipf:26927`

#### `setvarwide_emat3dkVD`

```igor
Function setvarwide_emat3dkVD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dkVD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Integral range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral range`
- Source: `Pierre's Template.ipf:26919`

#### `setvarwide_emat3dVD`

```igor
Function setvarwide_emat3dVD(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `mat3dVD_eplot()`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Integral range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral range`
- Source: `Pierre's Template.ipf:26895`


### Interactive procedures and macros

#### `automatrixTC`

```igor
Proc automatrixTC(paralist,xyzseq,sel)
```
- Usage: run from Igor with parameters `paralist`, `xyzseq`, `sel`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: paralist = List (by;) of parameter waves (1x1 waves); xyzseq = Wave of Pauli sequence, e.g. a polynomial with 6 terms, each term has 3 paulis, the wave should be like &#123;&#123;0,1,2},{2,3,1},{0,2,2},{1,1,1},{2,2,2},{1,0,0}}; sel = Select Modes. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `paralist`: List (by;) of parameter waves (1x1 waves)
  - `xyzseq`: Wave of Pauli sequence, e.g. a polynomial with 6 terms, each term has 3 paulis, the wave should be like &#123;&#123;0,1,2},{2,3,1},{0,2,2},{1,1,1},{2,2,2},{1,0,0}}
  - `sel`: Select Modes
- Source: `MatrixCalculation.ipf:83`

#### `AVG_wavein`

```igor
Proc AVG_wavein(namein)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `namein`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: namein = Name of the image. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `AVG_UpdateCirc()`, `AVG_Calc_avgcirc()`.
- Parameters:
  - `namein`: Name of the image
- Source: `Pierre's Template.ipf:12941`

#### `BEatheight_mat3d`

```igor
Proc BEatheight_mat3d(mat3dname,outputname,typeofmatrix,height,smoothfactor)
```
- Usage: run from Igor with parameters `mat3dname`, `outputname`, `typeofmatrix`, `height`, `smoothfactor`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: mat3dname = Enter the name of the matrix; outputname = Enter the outputname; typeofmatrix = Type of matrix; height = Height for the leading edge; smoothfactor = Enter the smooth factor. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `func_BEatheight_mat3d()`.
- Parameters:
  - `mat3dname`: Enter the name of the matrix
  - `outputname`: Enter the outputname
  - `typeofmatrix`: Type of matrix
  - `height`: Height for the leading edge
  - `smoothfactor`: Enter the smooth factor
- Source: `Pierre's Template.ipf:35840`

#### `bkremoverp`

```igor
proc bkremoverp (thisdata,usewave)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `thisdata`, `usewave`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thisdata = which one to handle; usewave = name of norm wave. Every EDC of Data will be substracte by Normwave.. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `thisdata`: which one to handle
  - `usewave`: name of norm wave. Every EDC of Data will be substracte by Normwave.
- Source: `Miscellaneous_Codes.ipf:1336`

#### `ChangeOneWaveLook`

```igor
Proc ChangeOneWaveLook(Waveoffset, R,G,B, size)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `Waveoffset`, `R`, `G`, `B`, `size`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `num2istr()`, `lsize()`, `rgb()`.
- Parameters:
  - `Waveoffset`: Igor wave reference used as input or output.
  - `R`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `G`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `size`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1646`

#### `correct2Dmapc`

```igor
proc correct2Dmapc(indicate,zbwavename)
```
- Usage: run from Igor with parameters `indicate`, `zbwavename`. Update-log guidance - 2023-08-09: Added the gate-map leakage corrector behind the "Fix gate leak" button.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: Indicate = Notice!!! Before Lauch; zbwavename = please enter the name of a 1D wave containing the real zero bias voltage of different gate voltages, / if this wave can be extracted from the same watefall, please enter /. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getZB()`, `str2num()`, `correct2Dmap()`.
- Parameters:
  - `indicate`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `zbwavename`: please enter the name of a 1D wave containing the real zero bias voltage of different gate voltages, / if this wave can be extracted from the same watefall, please enter /
- Source: `Miscellaneous_Codes.ipf:10699`

#### `DerivMatrix`

```igor
Macro DerivMatrix(OldM,num,box,choice)
```
- Usage: run from Igor with parameters `OldM`, `num`, `box`, `choice`.
- Notes: Igor macro entry point for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: OldM = Input the matrix Name; num = how many times to be smoothed; box = Input the box width; choice = choose direciton. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `DM()`.
- Parameters:
  - `OldM`: Input the matrix Name
  - `num`: how many times to be smoothed
  - `box`: Input the box width
  - `choice`: choose direciton
- Source: `Pierre's Template.ipf:2345`

#### `duplicateall`

```igor
Proc duplicateall(mat,mat2,suf,totalnum)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `mat`, `mat2`, `suf`, `totalnum`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; mat2 = duplicate to What?; totalnum = numbers of the Batch; suf = If you have a suffix,Please input. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `mat2`: duplicate to What?
  - `suf`: If you have a suffix,Please input
  - `totalnum`: numbers of the Batch
- Source: `Miscellaneous_Codes.ipf:6052`

#### `duplicatekly`

```igor
proc duplicatekly (mat,matd)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `mat`, `matd`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: mat = which one; matd = save as. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: which one
  - `matd`: save as
- Source: `Miscellaneous_Codes.ipf:1312`

#### `duplicatemat`

```igor
Proc duplicatemat(oldname,newname)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `oldname`, `newname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: oldname = Which matrix would you like to duplicate?; newname = Enter the new name. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `oldname`: Which matrix would you like to duplicate?
  - `newname`: Enter the new name
- Source: `Pierre's Template.ipf:6322`

#### `file2wave`

```igor
Proc file2wave(name)
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the wave or matrix. Code behavior: loads data from files or paths; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the wave or matrix
- Source: `Pierre's Template.ipf:5218`

#### `Frommarqueegetsubmatrixsc`

```igor
Proc Frommarqueegetsubmatrixsc(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: name = Wave name to put marquee. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Frommarqueegetsubmatrixs()`.
- Parameters:
  - `name`: Wave name to put marquee
- Source: `Smart_3D_Viewer_New.ipf:9833`

#### `Frommarqueegetsubmatrixsc2`

```igor
Proc Frommarqueegetsubmatrixsc2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `Frommarqueegetsubmatrixs2()`, `FFTrls2()`.
- Source: `FFT.ipf:971`

#### `HWL_subbgnd`

```igor
Proc HWL_subbgnd(matname,matbgnd,matflat,wavebelow,waveabove)
```
- Usage: run from Igor with parameters `matname`, `matbgnd`, `matflat`, `wavebelow`, `waveabove`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Dialog prompts: matname = Input name; matbgnd = Background name; matflat = Output name; wavebelow = Background curve bottom; waveabove = Background curve top. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Input name
  - `matbgnd`: Background name
  - `matflat`: Output name
  - `wavebelow`: Background curve bottom
  - `waveabove`: Background curve top
- Source: `Pierre's Template.ipf:23963`

#### `initmat3d`

```igor
Proc initmat3d()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:30896`

#### `initmat3dHD`

```igor
Proc initmat3dHD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:30928`

#### `initmat3dk`

```igor
Proc initmat3dk()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:30944`

#### `initmat3dkHD`

```igor
Proc initmat3dkHD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:30976`

#### `initmat3dkVD`

```igor
Proc initmat3dkVD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:30960`

#### `initmat3dVD`

```igor
Proc initmat3dVD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:30912`

#### `interp2Dall`

```igor
Proc interp2Dall(name,number,xpoint,ypoint)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `name`, `number`, `xpoint`, `ypoint`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `twoDinterpolatexyf()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:319`

#### `interpavedata`

```igor
proc interpavedata(from, to)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `from`, `to`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Dialog prompts: from = Number begin; to = Number end. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `from`: Number begin
  - `to`: Number end
- Source: `Miscellaneous_Codes.ipf:7181`

#### `interpcurve`

```igor
Proc interpcurve(wave4x,wave4y,matname)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `wave4x`, `wave4y`, `matname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: wave4x = Enter the name of the xwave; wave4y = Enter the name of the ywave; matname = Enter the name of the background natrix. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `func_give_area1()`.
- Parameters:
  - `wave4x`: Enter the name of the xwave
  - `wave4y`: Enter the name of the ywave
  - `matname`: Enter the name of the background natrix
- Source: `Pierre's Template.ipf:17042`

#### `Interpmat3D`

```igor
Proc Interpmat3D(matchoice,matdirection,qfactor)
```
- Usage: run from Igor with parameters `matchoice`, `matdirection`, `qfactor`.
- Notes: hv mapping project end Purpose: resamples, rescales, pads, or changes wave dimensions. Dialog prompts: matchoice = Matrix; matdirection = Direction; qfactor = Enter the quality factor. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `func_Interpmat3D()`.
- Parameters:
  - `matchoice`: Matrix
  - `matdirection`: Direction
  - `qfactor`: Enter the quality factor
- Source: `Pierre's Template.ipf:35675`

#### `limitmat3d`

```igor
Proc limitmat3d(theexponent)
```
- Usage: run from Igor with parameters `theexponent`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: theexponent = Enter the exponent for the highest value. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `theexponent`: Enter the exponent for the highest value
- Source: `Pierre's Template.ipf:23718`

#### `Lsmatrix3d`

```igor
Proc Lsmatrix3d(indicate,mat3dn,refwave)
```
- Usage: run from Igor with parameters `indicate`, `mat3dn`, `refwave`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: indicate = Indication; mat3dn = The 3D matrix to Segregated; refwave = The 2D shape reference matrix [Topography]. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Lsmatrix3dall()`.
- Parameters:
  - `indicate`: Indication
  - `mat3dn`: The 3D matrix to Segregated
  - `refwave`: The 2D shape reference matrix [Topography]
- Source: `Lattice Segregation.ipf:1648`

#### `madewavebytemplatec`

```igor
proc madewavebytemplatec(datawave,templatewave,notes)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `datawave`, `templatewave`, `notes`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: datawave = A wave; templatewave = B wave. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `madewavebytemplate2()`.
- Parameters:
  - `datawave`: A wave
  - `templatewave`: B wave
  - `notes`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:12940`

#### `makedata`

```igor
proc makedata(thiswave)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `thiswave`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves; writes or exports data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `thiswave`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:1101`

#### `mat3d_eplot`

```igor
Proc mat3d_eplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:30603`

#### `mat3d_eplot_setincrement`

```igor
Proc mat3d_eplot_setincrement()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:30465`

#### `mat3d_makeimage`

```igor
Proc mat3d_makeimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:30501`

#### `mat3d_pi`

```igor
Proc mat3d_pi()//scaling)
```
- Notes: Begining of Pierre's macros Usage: run interactively from Igor or from a panel callback. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:28569`

#### `mat3d_pir`

```igor
Proc mat3d_pir()//scaling)
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Source: `Miscellaneous_Codes.ipf:724`

#### `mat3dHC_pi`

```igor
Proc mat3dHC_pi(num,box,factor)//Use mat3d
```
- Usage: run from Igor with parameters `num`, `box`, `factor`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: num = How many times to be smoothed; box = Input the box width; factor = Curvature factor. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `func_mat3dHC_pi()`.
- Parameters:
  - `num`: How many times to be smoothed
  - `box`: Input the box width
  - `factor`: Curvature factor
- Source: `Pierre's Template.ipf:28776`

#### `mat3dHD_eplot`

```igor
Proc mat3dHD_eplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:30666`

#### `mat3dHD_eplot_setincrement`

```igor
Proc mat3dHD_eplot_setincrement()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:30477`

#### `mat3dHD_makeimage`

```igor
Proc mat3dHD_makeimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:30535`

#### `mat3dHD_pi`

```igor
Proc mat3dHD_pi(num,box)//Use mat3d
```
- Usage: run from Igor with parameters `num`, `box`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: num = How many times to be smoothed; box = Input the box width. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `func_mat3dHD_pi()`, `func_mat3dHD_als_pi()`.
- Parameters:
  - `num`: How many times to be smoothed
  - `box`: Input the box width
- Source: `Pierre's Template.ipf:33783`

#### `mat3dk_eplot`

```igor
Proc mat3dk_eplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:30698`

#### `mat3dk_eplot_setincrement`

```igor
Proc mat3dk_eplot_setincrement()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:30483`

#### `mat3dk_makeimage`

```igor
Proc mat3dk_makeimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:30552`

#### `mat3dk_slice`

```igor
Proc mat3dk_slice(e_low,e_high)
```
- Usage: run from Igor with parameters `e_low`, `e_high`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: e_low = integrate from:; e_high = to:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; integrates wave data.
- Parameters:
  - `e_low`: integrate from:
  - `e_high`: to:
- Source: `Pierre's Template.ipf:30794`

#### `mat3dkHD_eplot`

```igor
Proc mat3dkHD_eplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:30762`

#### `mat3dkHD_eplot_setincrement`

```igor
Proc mat3dkHD_eplot_setincrement()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:30495`

#### `mat3dkHD_makeimage`

```igor
Proc mat3dkHD_makeimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:30586`

#### `mat3dkHd_slice`

```igor
Proc mat3dkHd_slice(e_low,e_high)
```
- Usage: run from Igor with parameters `e_low`, `e_high`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: e_low = integrate from:; e_high = to:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; integrates wave data.
- Parameters:
  - `e_low`: integrate from:
  - `e_high`: to:
- Source: `Pierre's Template.ipf:30850`

#### `mat3dkVD_eplot`

```igor
Proc mat3dkVD_eplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:30730`

#### `mat3dkVD_eplot_setincrement`

```igor
Proc mat3dkVD_eplot_setincrement()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:30489`

#### `mat3dkVD_makeimage`

```igor
Proc mat3dkVD_makeimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:30569`

#### `mat3dkvd_slice`

```igor
Proc mat3dkvd_slice(e_low,e_high)
```
- Usage: run from Igor with parameters `e_low`, `e_high`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: e_low = integrate from:; e_high = to:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; integrates wave data.
- Parameters:
  - `e_low`: integrate from:
  - `e_high`: to:
- Source: `Pierre's Template.ipf:30822`

#### `mat3dVC_pi`

```igor
Proc mat3dVC_pi(num,box,factor)//Use mat3d
```
- Usage: run from Igor with parameters `num`, `box`, `factor`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: num = How many times to be smoothed; box = Input the box width; factor = Curvature factor. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `func_mat3dVC_pi()`.
- Parameters:
  - `num`: How many times to be smoothed
  - `box`: Input the box width
  - `factor`: Curvature factor
- Source: `Pierre's Template.ipf:28740`

#### `mat3dVD_eplot`

```igor
Proc mat3dVD_eplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:30634`

#### `mat3dVD_eplot_setincrement`

```igor
Proc mat3dVD_eplot_setincrement()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:30471`

#### `mat3dVD_makeimage`

```igor
Proc mat3dVD_makeimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:30518`

#### `mat3dVD_pi`

```igor
Proc mat3dVD_pi(num,box)//Use mat3d
```
- Usage: run from Igor with parameters `num`, `box`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: num = How many times to be smoothed; box = Input the box width. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `func_mat3dVD_pi()`.
- Parameters:
  - `num`: How many times to be smoothed
  - `box`: Input the box width
- Source: `Pierre's Template.ipf:28705`

#### `Matrix_To_Wave`

```igor
Macro Matrix_To_Wave(Matname, WaveBase, StartNum, toFolder)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `Matname`, `WaveBase`, `StartNum`, `toFolder`.
- Notes: Igor macro entry point for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: MatName = Input the matrix name. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `num2istr()`.
- Parameters:
  - `Matname`: Name or reference of the source wave/matrix being processed.
  - `WaveBase`: Igor wave reference used as input or output.
  - `StartNum`: Start point, index, or coordinate for the selected range.
  - `toFolder`: File-system path or data-folder string used for loading or saving data.
- Source: `Pierre's Template.ipf:2060`

#### `NaN0mat3d`

```igor
Proc NaN0mat3d()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:7673`

#### `normlizeformap`

```igor
proc normlizeformap(normwave)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `normwave`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: normwave = Name of NormCurve.Data will be divided by this curve.. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `normwave`: Name of NormCurve.Data will be divided by this curve.
- Source: `Miscellaneous_Codes.ipf:2010`

#### `normmat3d`

```igor
Proc normmat3d(name)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix. Code behavior: creates output waves; extracts image/wave statistics; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:16945`

#### `normwave`

```igor
Proc normwave(name,x_from,x_to)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`, `x_from`, `x_to`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix; x_from = Normalize from:; x_to = to:. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `mean()`.
- Parameters:
  - `name`: Enter the name of the matrix
  - `x_from`: Normalize from:
  - `x_to`: to:
- Source: `Pierre's Template.ipf:6932`

#### `normwave_2`

```igor
Proc normwave_2(name,x_from,x_to,x_from2,x_to2)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`, `x_from`, `x_to`, `x_from2`, `x_to2`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix; x_from = Normalize from:; x_to = to:. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `mean()`.
- Parameters:
  - `name`: Enter the name of the matrix
  - `x_from`: Normalize from:
  - `x_to`: to:
  - `x_from2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x_to2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:6453`

#### `padmc`

```igor
Proc padmc(namew,factor)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `namew`, `factor`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: namew = wave to pad; factor = How many time to enlarge dimsize. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `padm()`.
- Parameters:
  - `namew`: wave to pad
  - `factor`: How many time to enlarge dimsize
- Source: `Miscellaneous_Codes.ipf:3222`

#### `putslicermat3d`

```igor
Proc putslicermat3d()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `Set_Hline_slice_control()`, `Set_Vline_slice_control()`, `wintype()`, `Graph_imageforslicer()`, `putVline()`.
- Source: `Pierre's Template.ipf:27176`

#### `putslicermat3dHD`

```igor
Proc putslicermat3dHD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `Set_Hline_slice_control()`, `Set_Vline_slice_control()`, `wintype()`, `Graph_imageforslicer()`, `putVline()`.
- Source: `Pierre's Template.ipf:27346`

#### `putslicermat3dk`

```igor
Proc putslicermat3dk()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `Set_Hline_slice_control()`, `Set_Vline_slice_control()`, `wintype()`, `Graph_imageforslicer()`, `putVline()`.
- Source: `Pierre's Template.ipf:27219`

#### `putslicermat3dkHD`

```igor
Proc putslicermat3dkHD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `Set_Hline_slice_control()`, `Set_Vline_slice_control()`, `wintype()`, `Graph_imageforslicer()`, `putVline()`.
- Source: `Pierre's Template.ipf:27387`

#### `putslicermat3dkVD`

```igor
Proc putslicermat3dkVD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `Set_Hline_slice_control()`, `Set_Vline_slice_control()`, `wintype()`, `Graph_imageforslicer()`, `putVline()`.
- Source: `Pierre's Template.ipf:27303`

#### `putslicermat3dVD`

```igor
Proc putslicermat3dVD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `Set_Hline_slice_control()`, `Set_Vline_slice_control()`, `wintype()`, `Graph_imageforslicer()`, `putVline()`.
- Source: `Pierre's Template.ipf:27262`

#### `real_im_waves`

```igor
Proc real_im_waves(thiswave)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `thiswave`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: thiswave = Enter the name of the wave. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `thiswave`: Enter the name of the wave
- Source: `Pierre's Template.ipf:3502`

#### `reflectionwave`

```igor
proc reflectionwave(mat,xvalue)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `mat`, `xvalue`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: mat = which matrix to reflect; xvalue = x value of reference point. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `zeronan()`.
- Parameters:
  - `mat`: which matrix to reflect
  - `xvalue`: x value of reference point
- Source: `Miscellaneous_Codes.ipf:3054`

#### `Reflectmatrix`

```igor
Proc Reflectmatrix(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3164`

#### `Reflectmatrix_X`

```igor
Proc Reflectmatrix_X(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3147`

#### `Reflectmatrix_Y`

```igor
Proc Reflectmatrix_Y(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3129`

#### `renameall`

```igor
proc renameall(mat,mat2,suf,totalnum)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `mat`, `mat2`, `suf`, `totalnum`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; mat2 = Change to What?; totalnum = numbers of the Batch; suf = If you have a suffix,Please input. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `mat2`: Change to What?
  - `suf`: If you have a suffix,Please input
  - `totalnum`: numbers of the Batch
- Source: `Miscellaneous_Codes.ipf:5756`

#### `renamep`

```igor
proc renamep(mat,mat2,suf,start,endi)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `mat`, `mat2`, `suf`, `start`, `endi`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; mat2 = Change to What?; start = start of the Batch; endi = end of the Batch; suf = If you have a suffix,Please input. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `mat2`: Change to What?
  - `suf`: If you have a suffix,Please input
  - `start`: start of the Batch
  - `endi`: end of the Batch
- Source: `Miscellaneous_Codes.ipf:9202`

#### `rescale`

```igor
Proc rescale(sizex,sizey)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `sizex`, `sizey`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations. Main internal calls: `Capturenamedd()`.
- Parameters:
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10027`

#### `rescale_pi`

```igor
Proc rescale_pi(mat,orientation,amount)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `mat`, `orientation`, `amount`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix you want the rescale; orientation = You want to rescale the angle or the energy?; amount = How much do you want to add?. Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `x()`, `y()`, `z()`.
- Parameters:
  - `mat`: Enter the name of the matrix you want the rescale
  - `orientation`: You want to rescale the angle or the energy?
  - `amount`: How much do you want to add?
- Source: `Pierre's Template.ipf:3392`

#### `rescaleallpr`

```igor
Proc rescaleallpr(name,num,lenx,leny)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `name`, `num`, `lenx`, `leny`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch name; num = how many data; lenx = x size; leny = y size. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `rescaleall()`.
- Parameters:
  - `name`: Batch name
  - `num`: how many data
  - `lenx`: x size
  - `leny`: y size
- Source: `Miscellaneous_Codes.ipf:9990`

#### `rescaleforspec_orientations`

```igor
Proc rescaleforspec_orientations()
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:25255`

#### `rescalegroupc`

```igor
Proc rescalegroupc(ratiowave,batchname,num,notes)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `ratiowave`, `batchname`, `num`, `notes`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: ratiowave = Divider ratio wave; batchname = Batch name; num = Total spectra in Batch. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `rescalegroup()`.
- Parameters:
  - `ratiowave`: Divider ratio wave
  - `batchname`: Batch name
  - `num`: Total spectra in Batch
  - `notes`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:12899`

#### `rescaleload`

```igor
proc rescaleload(wavenames)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `wavenames`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `wavenames`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:1180`

#### `rescalemapasa1dcurve`

```igor
Proc rescalemapasa1dcurve(namemap,namecurve,Howmany)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `namemap`, `namecurve`, `Howmany`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: namemap = The source map; namecurve = The uneven y-axis wave; Howmany = How many dimsize(y) you want. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `NaN0()`, `rescalemapasa1dcurveF()`, `linkEDCs()`, `killEDCs()`, `zeroNaN()`.
- Parameters:
  - `namemap`: The source map
  - `namecurve`: The uneven y-axis wave
  - `Howmany`: How many dimsize(y) you want
- Source: `Miscellaneous_Codes.ipf:10064`

#### `rescalex_plus`

```igor
Proc rescalex_plus(name,num,amount)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `name`, `num`, `amount`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Main internal calls: `rescale_pi()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `amount`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5231`

#### `rescalexmultiwhat`

```igor
proc rescalexmultiwhat(name,num,amount)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `name`, `num`, `amount`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Main internal calls: `renormcuts_k()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `amount`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5246`

#### `resize2`

```igor
Proc resize2()
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:17843`

#### `Rflwaves`

```igor
Proc Rflwaves(name,a)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `a`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = The wave name you want to do reflection; a = Please Select Mode. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Reflecttrace()`, `reflectmatrix_X()`, `reflectmatrix_Y()`, `reflectmatrix()`.
- Parameters:
  - `name`: The wave name you want to do reflection
  - `a`: Please Select Mode
- Source: `Miscellaneous_Codes.ipf:3104`

#### `scale_e_pi`

```igor
Proc scale_e_pi(matrix,energy_shift)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `matrix`, `energy_shift`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: matrix = Enter the name of the matrix; energy_shift = Enter the energy you want to add. Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `matrix`: Enter the name of the matrix
  - `energy_shift`: Enter the energy you want to add
- Source: `Pierre's Template.ipf:5678`

#### `scalewaves`

```igor
proc scalewaves(mode,name,s,Pvalue,xshouldbe)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: run from Igor with parameters `mode`, `name`, `s`, `Pvalue`, `xshouldbe`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: mode = Mode; name = Wave to apply divider; s = divider ratio; Pvalue = [Mode 2 only] Reference P point; xshouldbe = [Mode 2 only] Correct x value of the reference P point. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `scalewave()`, `calculates()`.
- Parameters:
  - `mode`: Mode
  - `name`: Wave to apply divider
  - `s`: divider ratio
  - `Pvalue`: [Mode 2 only] Reference P point
  - `xshouldbe`: [Mode 2 only] Correct x value of the reference P point
- Source: `Miscellaneous_Codes.ipf:12829`

#### `secondDall`

```igor
proc secondDall(mat,suf,totalnum,smootht)
```
- Usage: run from Igor with parameters `mat`, `suf`, `totalnum`, `smootht`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: mat = name of the Batch waves; totalnum = numbers of the Batch; suf = If you have a suffix,Please input; smootht = How many times smooth. Code behavior: duplicates or stages waves for downstream processing; smooths wave data; differentiates wave data; asks the user for parameters through Igor prompts. Main internal calls: `displaymulti()`.
- Parameters:
  - `mat`: name of the Batch waves
  - `suf`: If you have a suffix,Please input
  - `totalnum`: numbers of the Batch
  - `smootht`: How many times smooth
- Source: `Miscellaneous_Codes.ipf:6082`

#### `secondDall2`

```igor
proc secondDall2(mat,suf,totalnum,smootht)
```
- Usage: run from Igor with parameters `mat`, `suf`, `totalnum`, `smootht`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: mat = name of the Batch waves; totalnum = numbers of the Batch; suf = If you have a suffix,Please input; smootht = How many times smooth. Code behavior: duplicates or stages waves for downstream processing; smooths wave data; differentiates wave data; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `suf`: If you have a suffix,Please input
  - `totalnum`: numbers of the Batch
  - `smootht`: How many times smooth
- Source: `Miscellaneous_Codes.ipf:12004`

#### `setmatrixindex`

```igor
Proc setmatrixindex(a,b,c)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run from Igor with parameters `a`, `b`, `c`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: a = X is which index in Matrix? Row (0); Coloum (1); Layer (2); b = Y is which index in Matrix? Row (0); Coloum (1); Layer (2); c = E is which index in Matrix? Row (0); Coloum (1); Layer (2). Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `a`: X is which index in Matrix? Row (0); Coloum (1); Layer (2)
  - `b`: Y is which index in Matrix? Row (0); Coloum (1); Layer (2)
  - `c`: E is which index in Matrix? Row (0); Coloum (1); Layer (2)
- Source: `Miscellaneous_Codes.ipf:9827`

#### `Shearmat3d`

```igor
Proc Shearmat3d(indicate,mat3dn,refwave,factor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `indicate`, `mat3dn`, `refwave`, `factor`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: indicate = Indication; mat3dn = The 3D matrix to be sheared; refwave = The 2D reference matrix; factor = Scatter Interp factor (must be the same as refwave shear). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `shearall()`.
- Parameters:
  - `indicate`: Indication
  - `mat3dn`: The 3D matrix to be sheared
  - `refwave`: The 2D reference matrix
  - `factor`: Scatter Interp factor (must be the same as refwave shear)
- Source: `Shear Correction for C4.ipf:973`

#### `shrinkmatrixbystepsc`

```igor
proc shrinkmatrixbystepsc(name,steps)
```
- Usage: run from Igor with parameters `name`, `steps`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name; steps = Times to shrink, increase the dimdelta step times. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `shrinkmatrixbysteps()`.
- Parameters:
  - `name`: Matrix name
  - `steps`: Times to shrink, increase the dimdelta step times
- Source: `SmartDisplay.ipf:5015`

#### `shrinktocube`

```igor
proc shrinktocube(name,num,point)
```
- Usage: run from Igor with parameters `name`, `num`, `point`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `cutklarge2()`, `cutElarge2()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `point`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:746`

#### `smoothall`

```igor
proc smoothall(mat,totalnum,t)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `mat`, `totalnum`, `t`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; totalnum = numbers of the Batch; t = smooth times. Code behavior: smooths wave data; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `totalnum`: numbers of the Batch
  - `t`: smooth times
- Source: `Miscellaneous_Codes.ipf:6142`

#### `smoothallmatrix`

```igor
proc smoothallmatrix(total,OldM,num,box)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `total`, `OldM`, `num`, `box`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `SmoothMat_k23()`.
- Parameters:
  - `total`: =dimsize(File_name,0)-1
  - `OldM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:6119`

#### `SmoothMat_k`

```igor
Proc SmoothMat_k(OldM,num,box,choice)//only for Vertical smooth at the moment
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `OldM`, `num`, `box`, `choice`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: OldM = Input the matrix Name; num = how many times to be smoothed; box = Input the box width; choice = choose direciton. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `func_SmoothMat_k()`.
- Parameters:
  - `OldM`: Input the matrix Name
  - `num`: how many times to be smoothed
  - `box`: Input the box width
  - `choice`: choose direciton
- Source: `Pierre's Template.ipf:7526`

#### `SmoothMat_k2`

```igor
Proc SmoothMat_k2(OldM,num,box,choice)//only for Vertical smooth at the moment
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `OldM`, `num`, `box`, `choice`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: OldM = Input the matrix Name; num = how many times to be smoothed; box = Input the box width; choice = choose direciton. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `func_SmoothMat_k()`.
- Parameters:
  - `OldM`: Input the matrix Name
  - `num`: how many times to be smoothed
  - `box`: Input the box width
  - `choice`: choose direciton
- Source: `Miscellaneous_Codes.ipf:6881`

#### `SmoothMat_k23`

```igor
Proc SmoothMat_k23(OldM,num,box)//only for Vertical smooth at the moment
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `OldM`, `num`, `box`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: OldM = Input the matrix Name; num = how many times to be smoothed; box = Input the box width. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `func_SmoothMat_k()`.
- Parameters:
  - `OldM`: Input the matrix Name
  - `num`: how many times to be smoothed
  - `box`: Input the box width
- Source: `Pierre's Template.ipf:7622`

#### `sub_bline_pi_matrix`

```igor
Proc sub_bline_pi_matrix(matname,numbpnts)
```
- Usage: run from Igor with parameters `matname`, `numbpnts`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: matname = Name of the matrix; numbpnts = Number of points. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `sub_bline_pi()`.
- Parameters:
  - `matname`: Name of the matrix
  - `numbpnts`: Number of points
- Source: `Pierre's Template.ipf:9815`

#### `transform2equiv_waves`

```igor
Proc transform2equiv_waves(nameref,name2)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `nameref`, `name2`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: nameref = Enter the name of the model wave; name2 = Enter the name of the wave to modify. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Func_transform2equiv_waves()`.
- Parameters:
  - `nameref`: Enter the name of the model wave
  - `name2`: Enter the name of the wave to modify
- Source: `Pierre's Template.ipf:9274`

#### `tta`

```igor
proc tta(name,setpointv,currentwave)
```
- Usage: run from Igor with parameters `name`, `setpointv`, `currentwave`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = Name of the data set; setpointv = Voltage setpoint (mV); currentwave = The name of the wave of current (nA). Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `linkstsmap_P()`, `duplicateall()`.
- Parameters:
  - `name`: Name of the data set
  - `setpointv`: Voltage setpoint (mV)
  - `currentwave`: The name of the wave of current (nA)
- Source: `Miscellaneous_Codes.ipf:11035`

#### `twoDinterpolate2`

```igor
proc twoDinterpolate2(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `xpoint`, `ypoint`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = Name of Raw Data; xpoint = Point number of x you wanted; ypoint = Point number of y you wanted. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `aamakematrix2()`, `aamakematrix3()`.
- Parameters:
  - `name`: Name of Raw Data
  - `xpoint`: Point number of x you wanted
  - `ypoint`: Point number of y you wanted
- Source: `Miscellaneous_Codes.ipf:4973`

#### `twoDinterpolate2r`

```igor
proc twoDinterpolate2r(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `xpoint`, `ypoint`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = Name of Raw Data; xpoint = Point number of x you wanted; ypoint = Point number of y you wanted. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `aamakematrix2r()`, `aamakematrix3r()`.
- Parameters:
  - `name`: Name of Raw Data
  - `xpoint`: Point number of x you wanted
  - `ypoint`: Point number of y you wanted
- Source: `Miscellaneous_Codes.ipf:4194`

#### `twoDinterpolatexy`

```igor
Proc twoDinterpolatexy(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `xpoint`, `ypoint`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Dialog prompts: name = Name of Raw Data; xpoint = Point number of x you wanted; ypoint = Point number of y you wanted. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `twoDinterpolatexyf()`.
- Parameters:
  - `name`: Name of Raw Data
  - `xpoint`: Point number of x you wanted
  - `ypoint`: Point number of y you wanted
- Source: `FFT.ipf:342`

#### `twoDinterpy`

```igor
proc twoDinterpy(name,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `ypoint`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: name = Name of Raw Data; ypoint = Point number of y points you wanted. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `makematrix2()`, `di()`.
- Parameters:
  - `name`: Name of Raw Data
  - `ypoint`: Point number of y points you wanted
- Source: `FFT.ipf:504`

#### `unevenlinepc`

```igor
Proc unevenlinepc(batchname,templatewave,num)
```
- Usage: run from Igor with parameters `batchname`, `templatewave`, `num`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: batchname = Name of the batch; templatewave = Template wave; num = Total spectra in Batch. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `unevenlinep()`.
- Parameters:
  - `batchname`: Name of the batch
  - `templatewave`: Template wave
  - `num`: Total spectra in Batch
- Source: `Miscellaneous_Codes.ipf:13013`

#### `wave2bkgndremoved`

```igor
Proc wave2bkgndremoved(newname)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `newname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `subbkgndremover()`, `mode()`, `marker()`, `lstyle()`, `rgb()`, `lsize()`.
- Parameters:
  - `newname`: Used in: Duplicate/O $newname bkgndremoved
- Source: `Pierre's Template.ipf:8992`

#### `wave2file`

```igor
Proc wave2file(name)
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the wave or matrix. Code behavior: writes or exports data; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the wave or matrix
- Source: `Pierre's Template.ipf:5201`

#### `wave2filenokill`

```igor
Proc wave2filenokill(name)
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the wave or matrix. Code behavior: writes or exports data; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the wave or matrix
- Source: `Pierre's Template.ipf:5210`

#### `wave4bkgndremoval`

```igor
Proc wave4bkgndremoval(newname)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `newname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: newname = Enter the name of the wave. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; asks the user for parameters through Igor prompts. Main internal calls: `subbkgndremover()`.
- Parameters:
  - `newname`: Enter the name of the wave
- Source: `Pierre's Template.ipf:9032`


### Computational and data helpers

#### `aamakematrix2`

```igor
Function aamakematrix2(namee,sizex,sizey)
```
- Usage: call as `aamakematrix2(namee, sizex, sizey)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5015`

#### `aamakematrix2r`

```igor
Function aamakematrix2r(namee,sizex,sizey)
```
- Usage: call as `aamakematrix2r(namee, sizex, sizey)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4236`

#### `aamakematrix3`

```igor
Function aamakematrix3(namee,sizex,sizey)
```
- Usage: call as `aamakematrix3(namee, sizex, sizey)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5034`

#### `aamakematrix3r`

```igor
Function aamakematrix3r(namee,sizex,sizey)
```
- Usage: call as `aamakematrix3r(namee, sizex, sizey)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4255`

#### `appropriate_smoothing`

```igor
Function appropriate_smoothing(nonsmoothed,smoothed,smoothlimit) //not completed
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `appropriate_smoothing(nonsmoothed, smoothed, smoothlimit)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `nonsmoothed`: Used in: Duplicate/O nonsmoothed tempsmoothed / Duplicate/O nonsmoothed errorwave
  - `smoothed`: Used in: Duplicate/O tempsmoothed smoothed
  - `smoothlimit`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:4032`

#### `automatrixC`

```igor
Function/Wave automatrixC(paralist,xyzseq)
```
- Notes: # 02. Main Function for complex numerical wave [This useful for model calculation in function] Usage: call as `automatrixC(paralist, xyzseq)`. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `mpc()`, `s0()`, `sx()`, `sy()`, `sz()`.
- Parameters:
  - `paralist`: String name used to locate an Igor wave, graph, folder, or output object.
  - `xyzseq`: Momentum or Q-vector coordinate/index.
- Source: `MatrixCalculation.ipf:214`

#### `automatrixT`

```igor
Function automatrixT(paralist,xyzseq)
```
- Notes: # 02. Main Function for Text wave [This useful for derivation equation] Usage: call as `automatrixT(paralist, xyzseq)`. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `mpt()`, `st0()`, `stx()`, `sty()`, `stz()`, `plust()`.
- Parameters:
  - `paralist`: String name used to locate an Igor wave, graph, folder, or output object.
  - `xyzseq`: Momentum or Q-vector coordinate/index.
- Source: `MatrixCalculation.ipf:103`

#### `bkremover`

```igor
Function bkremover(thisdata, usewave)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `bkremover(thisdata, usewave)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: thisdata = which one; usewave = norm wave. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `thisdata`: which one
  - `usewave`: norm wave
- Source: `Miscellaneous_Codes.ipf:1345`

#### `calrandom_matrix`

```igor
Function calrandom_matrix(matrix_random,doping,infected)
```
- Usage: call as `calrandom_matrix(matrix_random, doping, infected)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `matrix_random`: Matrix/2D wave input or matrix output name.
  - `doping`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `infected`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:22535`

#### `ckwaveonfig`

```igor
Function ckwaveonfig(name,waven)
```
- Usage: call as `ckwaveonfig(name, waven)`.
- Notes: check if a wave on the graph name = winame(0,1) waven is the name of the wave want to append If the wave on the graph, it will return =!0, if it is not on the graph, return 0 Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Parameters:
  - `name`: name of the graph
  - `waven`: name of the wave
- Source: `SmartDisplay.ipf:902`

#### `correct2Dmap`

```igor
Function correct2Dmap(zerobiaswave)
```
- Usage: call as `correct2Dmap(zerobiaswave)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `di()`, `displaymultiF()`, `ckfig()`, `Constantoffset_nF()`.
- Parameters:
  - `zerobiaswave`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:10717`

#### `CutWave_EcenteredRange`

```igor
Function CutWave_EcenteredRange(sourceWave)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `CutWave_EcenteredRange(sourceWave)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Main internal calls: `ceil()`.
- Parameters:
  - `sourceWave`: Igor wave reference used as input or output.
- Source: `ModelingTunneling.ipf:949`

#### `cvtcmplx`

```igor
Function/Wave cvtcmplx(data)
```
- Usage: call as `cvtcmplx(data)`.
- Notes: Wave-returning helper for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output; uses Igor matrix operations for vectorized calculation.
- Parameters:
  - `data`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:626`

#### `DeNaN`

```igor
Function DeNaN(OriginalWave)       //Evaluate all NaN to 0
```
- Usage: call as `DeNaN(OriginalWave)`.
- Notes: #pragma rtGlobals=1 // Use modern global access method.
- Parameters:
  - `OriginalWave`: Igor wave reference used as input or output.
- Source: `Procedure.ipf:17`

#### `diagM`

```igor
Function/Wave diagM(num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `diagM(num)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/29/2023: Added diagM(num) SuperdiagM(num) SubdiagM(num) (V2: added cursor hooks to also the bulk bands) (V3 is a debugged version).
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `MatrixCalculation.ipf:865`

#### `diagM_cmplx`

```igor
Function/Wave diagM_cmplx(num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `diagM_cmplx(num)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `MatrixCalculation.ipf:822`

#### `DoLSegreXY`

```igor
Function DoLSegreXY(name,prefwave,prefwave2,thresh,mode)
```
- Notes: #IV.4 Main Functional Codes (special case for FeXY) Usage: call as `DoLSegreXY(name, prefwave, prefwave2, thresh, mode)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mean()`.
- Parameters:
  - `name`: 2D wave to be segregated
  - `prefwave`: Igor wave reference used as input or output.
  - `prefwave2`: Igor wave reference used as input or output.
  - `thresh`: threshold for defining a lattice position (max=1)
  - `mode`: (= 1 is Scatter imageinterpolate mode, more modes to be added)
- Source: `Lattice Segregation.ipf:1404`

#### `Frommarqueegetsubmatrix`

```igor
Function Frommarqueegetsubmatrix(name)
```
- Usage: call as `Frommarqueegetsubmatrix(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `mean()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:9632`

#### `Frommarqueegetsubmatrixs`

```igor
Function Frommarqueegetsubmatrixs(name)
```
- Usage: call as `Frommarqueegetsubmatrixs(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`, `mean()`. Update log: 05/10/2024: Added ave_inmarque() to the popup window of Frommarqueegetsubmatrixs(name) which calculates the average value on the partial image (only count the non-NaN value).
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:9838`

#### `Frommarqueegetsubmatrixs2`

```igor
Function Frommarqueegetsubmatrixs2(name)
```
- Usage: call as `Frommarqueegetsubmatrixs2(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`, `func_NaN0()`, `mean()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `FFT.ipf:1013`

#### `func_addpntbkgndremover`

```igor
Function func_addpntbkgndremover(wavex,wavey)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `func_addpntbkgndremover(wavex, wavey)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `xcsr()`, `vcsr()`.
- Parameters:
  - `wavex`: Igor wave reference used as input or output.
  - `wavey`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:9077`

#### `func_BEatheight_mat3d`

```igor
Function func_BEatheight_mat3d(mat3dname,outputname,typeofmatrix,height,smoothfactor)
```
- Usage: call as `func_BEatheight_mat3d(mat3dname, outputname, typeofmatrix, height, smoothfactor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_BEatheight_EDC()`.
- Parameters:
  - `mat3dname`: Matrix/2D wave input or matrix output name.
  - `outputname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `typeofmatrix`: Matrix/2D wave input or matrix output name.
  - `height`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smoothfactor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:35866`

#### `func_CVT2EK_pi`

```igor
Function func_CVT2EK_pi(newmat,oldmat,newwavex,newwavez,k0,dk,i)
```
- Usage: call as `func_CVT2EK_pi(newmat, oldmat, newwavex, newwavez, k0, dk, i)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `interp()`.
- Parameters:
  - `newmat`: Matrix/2D wave input or matrix output name.
  - `oldmat`: Matrix/2D wave input or matrix output name.
  - `newwavex`: Igor wave reference used as input or output.
  - `newwavez`: Igor wave reference used as input or output.
  - `k0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `dk`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `i`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:30229`

#### `func_delpntbkgndremover`

```igor
Function func_delpntbkgndremover(wavex,wavey)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `func_delpntbkgndremover(wavex, wavey)`.
- Notes: Function for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `xcsr()`.
- Parameters:
  - `wavex`: Igor wave reference used as input or output.
  - `wavey`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:9087`

#### `func_Interpmat3D`

```igor
Function func_Interpmat3D(matname,indexofcol,qfactor)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `func_Interpmat3D(matname, indexofcol, qfactor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `indexofcol`: Step size or energy/voltage increment used by the calculation.
  - `qfactor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:35719`

#### `func_mat3dHC_pi`

```igor
Function func_mat3dHC_pi(mat3d,mat3dHD,num,box,factor,slitorientation)
```
- Usage: call as `func_mat3dHC_pi(mat3d, mat3dHD, num, box, factor, slitorientation)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `curvature4mat3d()`.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dHD`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
  - `slitorientation`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:28793`

#### `func_mat3dHD_als_pi`

```igor
Function func_mat3dHD_als_pi(mat3d,mat3dHD,num,box)
```
- Usage: call as `func_mat3dHD_als_pi(mat3d, mat3dHD, num, box)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dHD`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:33799`

#### `func_mat3dHD_pi`

```igor
Function func_mat3dHD_pi(mat3d,mat3dHD,num,box)
```
- Usage: call as `func_mat3dHD_pi(mat3d, mat3dHD, num, box)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dHD`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:33822`

#### `func_mat3dVC_pi`

```igor
Function func_mat3dVC_pi(mat3d,mat3dVD,num,box,factor)
```
- Usage: call as `func_mat3dVC_pi(mat3d, mat3dVD, num, box, factor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `curvature4mat3d()`.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dVD`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:28755`

#### `func_mat3dVD_pi`

```igor
Function func_mat3dVD_pi(mat3d,mat3dVD,num,box)
```
- Usage: call as `func_mat3dVD_pi(mat3d, mat3dVD, num, box)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dVD`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:28717`

#### `func_smoothmat_k`

```igor
function func_smoothmat_k(OldM,NewM,num,box,choice)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `func_smoothmat_k(OldM, NewM, num, box, choice)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data.
- Parameters:
  - `OldM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `NewM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:7569`

#### `Func_transform2equiv_waves`

```igor
Function Func_transform2equiv_waves(nameref,name2)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `Func_transform2equiv_waves(nameref, name2)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp()`.
- Parameters:
  - `nameref`: Used in: Duplicate/O nameref newwave
  - `name2`: Used in: Duplicate/O name2 name2x / Duplicate/O newwave name2
- Source: `Pierre's Template.ipf:9284`

#### `getall1or2dwave`

```igor
Function/S getall1or2dwave()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `getall1or2dwave()` from Igor procedure code or the command line.
- Notes: Function to get a stringlist containing the name of all the 1 & 2 dimensional data. Main internal calls: `waveDims()`.
- Source: `MultipeakforLinecut.ipf:1516`

#### `getall2dwave`

```igor
Function/S getall2dwave()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `getall2dwave()` from Igor procedure code or the command line.
- Notes: ## 06 Function to get a stringlist containing the name of all the 2 dimensional data. Main internal calls: `waveDims()`.
- Source: `Smart_3D_Viewer_New.ipf:2463`

#### `getall3dwave`

```igor
Function/S getall3dwave()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call `getall3dwave()` from Igor procedure code or the command line. Update-log guidance - 2023-08-17: Provides the 3D-wave list used by the smarter 3D matrix plotter prompt, so users can select a root-level 3D wave before launching `d3d`.
- Notes: ## 05 Function to get a stringlist containing the name of all the three dimensional data. Main internal calls: `waveDims()`.
- Source: `Smart_3D_Viewer_New.ipf:2445`

#### `getzb`

```igor
Function/wave getzb(l1,l2,r1,r2)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getzb(l1, l2, r1, r2)`.
- Notes: Wave-returning helper for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `getpeakfromwaterfall()`, `ed()`.
- Parameters:
  - `l1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `l2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `r1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `r2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10839`

#### `Imatrix_digimage`

```igor
Function Imatrix_digimage(newmat,matred,matgreen,matblue,redwave,greenwave,bluewave,colorpixels,method)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Imatrix_digimage(newmat, matred, matgreen, matblue, redwave, greenwave, bluewave, colorpixels)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `Return_oneI_digimage()`.
- Parameters:
  - `newmat`: Matrix/2D wave input or matrix output name.
  - `matred`: Matrix/2D wave input or matrix output name.
  - `matgreen`: Matrix/2D wave input or matrix output name.
  - `matblue`: Matrix/2D wave input or matrix output name.
  - `redwave`: Igor wave reference used as input or output.
  - `greenwave`: Igor wave reference used as input or output.
  - `bluewave`: Igor wave reference used as input or output.
  - `colorpixels`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `method`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:6706`

#### `Indexofmatrix`

```igor
Function/wave Indexofmatrix(mat,show)
```
- Usage: call as `Indexofmatrix(mat, show)`.
- Notes: Wave-returning helper for spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations. Main internal calls: `sortColumnindex()`, `di()`. Update log: 10/31/2023: (3) Indexofmatrix(mat,show): Calculate the LDOS distribution of each band, indexed by the eigenvalue index.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `show`: Igor wave reference used as input or output.
- Source: `Models.ipf:10019`

#### `Indexofmatrix_cmplx`

```igor
Function/wave Indexofmatrix_cmplx(mat,show)
```
- Notes: Momentum resolved LDOS(index) Usage: call as `Indexofmatrix_cmplx(mat, show)`. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations. Main internal calls: `sortColumnindex()`, `di()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `show`: Igor wave reference used as input or output.
- Source: `Models.ipf:9189`

#### `Interpolate_x`

```igor
Function Interpolate_x(OriginalWave, Factor, IntMethod)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `Interpolate_x(OriginalWave, Factor, IntMethod)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Parameters:
  - `OriginalWave`: Igor wave reference used as input or output.
  - `Factor`: Scaling, normalization, interpolation, or correction factor.
  - `IntMethod`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Procedure.ipf:63`

#### `Interpolate_xy`

```igor
Function Interpolate_xy(OriginalWave, Factor, IntMethod)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `Interpolate_xy(OriginalWave, Factor, IntMethod)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `Interpolate_x()`, `Interpolate_y()`.
- Parameters:
  - `OriginalWave`: Igor wave reference used as input or output.
  - `Factor`: Scaling, normalization, interpolation, or correction factor.
  - `IntMethod`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Procedure.ipf:227`

#### `Interpolate_y`

```igor
Function Interpolate_y(OriginalWave, Factor, IntMethod)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `Interpolate_y(OriginalWave, Factor, IntMethod)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Parameters:
  - `OriginalWave`: Igor wave reference used as input or output.
  - `Factor`: Scaling, normalization, interpolation, or correction factor.
  - `IntMethod`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Procedure.ipf:145`

#### `intersectionforCsrslider`

```igor
Function/WAVE intersectionforCsrslider()
```
- Usage: call `intersectionforCsrslider()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:25366`

#### `LDOSofmatrix`

```igor
Function/wave LDOSofmatrix(mat,Vmin,Vmax,show)
```
- Usage: call as `LDOSofmatrix(mat, Vmin, Vmax, show)`.
- Notes: Wave-returning helper for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `sortColumnindex()`, `di()`. Update log: 10/31/2023: (5) LDOSofmatrix(mat,Vmin,Vmax,show): Calculate the overall LDOS as a function of energy.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `Vmin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Vmax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `show`: Igor wave reference used as input or output.
- Source: `Models.ipf:10177`

#### `LDOSofmatrix_cmplx`

```igor
Function/wave LDOSofmatrix_cmplx(mat,Vmin,Vmax,show)
```
- Usage: call as `LDOSofmatrix_cmplx(mat, Vmin, Vmax, show)`.
- Notes: Wave-returning helper for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `sortColumnindex()`, `di()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `Vmin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Vmax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `show`: Igor wave reference used as input or output.
- Source: `Models.ipf:9334`

#### `Lsmatrix3dall`

```igor
Function Lsmatrix3dall(mat3dn,refwave,factor)
```
- Usage: call as `Lsmatrix3dall(mat3dn, refwave, factor)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `DoLSegre()`, `DoLSegreXY()`.
- Parameters:
  - `mat3dn`: name of the mat3d to be sheared = "g2_001_G"
  - `refwave`: name of the shear reference wave = "g2_001_T" string barename = replaceString("_G",mat3dn,"_T")
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Lattice Segregation.ipf:1661`

#### `madewavebytemplate`

```igor
Function madewavebytemplate(datawave,templatewave)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `madewavebytemplate(datawave, templatewave)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `datawave`: Igor wave reference used as input or output.
  - `templatewave`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:12978`

#### `madewavebytemplate2`

```igor
Function madewavebytemplate2(datawave,templatewave)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `madewavebytemplate2(datawave, templatewave)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Update log: 2025-05-19: Reformats a wave so that its dimensions and scaling follow a reference/template wave.
- Parameters:
  - `datawave`: Igor wave reference used as input or output.
  - `templatewave`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:12949`

#### `makefreehandwave_2dplot`

```igor
Function makefreehandwave_2dplot()
```
- Usage: call `makefreehandwave_2dplot()` from Igor procedure code or the command line.
- Notes: #3_05 GO Function {Note: this function Define the end of the free hand draw and set the next cursor hook call belong to new lineprofile} Purpose: creates new waves, maps, figures, or simulation data. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `autoremovejump1D()`, `lsize()`, `rgb()`, `mode()`, `lstyle()`.
- Source: `SmartDisplay.ipf:3282`

#### `makefreehandwave_3dplot`

```igor
Function makefreehandwave_3dplot()
```
- Usage: call `makefreehandwave_3dplot()` from Igor procedure code or the command line.
- Notes: #3_05 GO Function {Note: this function Define the end of the free hand draw and set the next cursor hook call belong to new lineprofile} Purpose: creates new waves, maps, figures, or simulation data. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_NaN0()`, `rescalemapasa1dcurveF()`, `linkEDCs_nodis()`, `killEDCs()`, `func_zeroNaN()`, `grabwinnonew()`.
- Source: `Smart_3D_Viewer_New.ipf:3097`

#### `makefreehandwave_3dplotf`

```igor
Function makefreehandwave_3dplotf()
```
- Usage: call `makefreehandwave_3dplotf()` from Igor procedure code or the command line.
- Notes: #3_05 GO Function {Note: this function Define the end of the free hand draw and set the next cursor hook call belong to new lineprofile} Purpose: creates new waves, maps, figures, or simulation data. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_NaN0()`, `rescalemapasa1dcurveFf()`, `linkEDCs_nodisf()`, `killEDCsf()`, `func_zeroNaN()`, `lsize()`.
- Source: `Smart_3D_Viewer_New.ipf:7883`

#### `makematrix`

```igor
function makematrix(namee)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makematrix(namee)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `FFT.ipf:290`

#### `makematrix2`

```igor
Function makematrix2(namee,sizex,sizey)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makematrix2(namee, sizex, sizey)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:464`

#### `makematrix3`

```igor
Function makematrix3(namee,sizex,sizey)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makematrix3(namee, sizex, sizey)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namee`: String name used to locate an Igor wave, graph, folder, or output object.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:480`

#### `Medianfinder`

```igor
Function/D Medianfinder(w) // Returns median value of wave w
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Medianfinder(w)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `tempMedianWave()`, `numpnts()`.
- Parameters:
  - `w`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:5897`

#### `mpc`

```igor
Function/wave mpc(A,B)
```
- Notes: #1. Complex Usage: call as `mpc(A, B)`. Code behavior: creates output waves. Update log: 10/12/2023: (5) mpc(A,B) mpn(A,B) mpt(A,B): matrix tensor product for complex, real, text square matrix, A and B matrix should be under same dimension.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `MatrixCalculation.ipf:331`

#### `mpn`

```igor
Function/wave mpn(A,B)
```
- Notes: #2. Numerical (real) Usage: call as `mpn(A, B)`. Code behavior: creates output waves. Update log: 10/12/2023: (5) mpc(A,B) mpn(A,B) mpt(A,B): matrix tensor product for complex, real, text square matrix, A and B matrix should be under same dimension.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `MatrixCalculation.ipf:343`

#### `mpt`

```igor
Function/Wave mpt(A,B)
```
- Notes: #3. Text Usage: call as `mpt(A, B)`. Code behavior: creates output waves. Main internal calls: `str2num()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `MatrixCalculation.ipf:355`

#### `NewDerivPRB98_214503_eq2_N`

```igor
Function NewDerivPRB98_214503_eq2_N()
```
- Notes: # 02. New version numerical; equation (2) of PRB 98, 214503 Usage: call `NewDerivPRB98_214503_eq2_N()` from Igor procedure code or the command line. Main internal calls: `wC()`, `Wn()`, `automatrixC()`, `cktable()`. Update log: 10/12/2023: (4) NewDerivPRB98_214503_eq2_N(): Demo to show how to do complex numerical wave Hamiltonian derivation.
- Source: `MatrixCalculation.ipf:42`

#### `NewDerivPRB98_214503_eq2_T`

```igor
Function NewDerivPRB98_214503_eq2_T()
```
- Notes: # 01. New version Text; equation (2) of PRB 98, 214503 Usage: call `NewDerivPRB98_214503_eq2_T()` from Igor procedure code or the command line. Main internal calls: `wT()`, `Wn()`, `automatrixT()`. Update log: 10/12/2023: (3) NewDerivPRB98_214503_eq2_T(): Demo to show how to do Text wave Hamiltonian derivation.
- Source: `MatrixCalculation.ipf:15`

#### `Norma3dmatrix`

```igor
Function Norma3dmatrix(name,Zn)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `Norma3dmatrix(name, Zn)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:706`

#### `numslicehnam`

```igor
Function/Wave numslicehnam()
```
- Usage: call `numslicehnam()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: smooths wave data. Main internal calls: `mean()`.
- Source: `Miscellaneous_Codes.ipf:7719`

#### `numslicevnam`

```igor
Function/Wave numslicevnam()
```
- Usage: call `numslicevnam()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: smooths wave data. Main internal calls: `mean()`.
- Source: `Miscellaneous_Codes.ipf:7760`

#### `OldderivPRB98_214503_eq1_T`

```igor
Function OldderivPRB98_214503_eq1_T()
```
- Notes: # 01. Old version Text; equation (1) of PRB 98, 214503 Usage: call `OldderivPRB98_214503_eq1_T()` from Igor procedure code or the command line. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpt()`, `st0()`, `stz()`, `stx()`, `sty()`, `plust()`.
- Source: `MatrixCalculation.ipf:578`

#### `OldDerivPRB98_214503_eq2_N`

```igor
Function OldDerivPRB98_214503_eq2_N()
```
- Notes: # 03. Old version Numerical; equation (2) of PRB 98, 214503 Usage: call `OldDerivPRB98_214503_eq2_N()` from Igor procedure code or the command line. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpc()`, `sz()`, `s0()`, `sx()`, `sy()`, `cktable()`.
- Source: `MatrixCalculation.ipf:733`

#### `OldderivPRB98_214503_eq2_T`

```igor
Function OldderivPRB98_214503_eq2_T()
```
- Notes: # 02. Old version Text; equation (2) of PRB 98, 214503 Usage: call `OldderivPRB98_214503_eq2_T()` from Igor procedure code or the command line. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpt()`, `stz()`, `st0()`, `stx()`, `sty()`, `plust()`.
- Source: `MatrixCalculation.ipf:634`

#### `onedpad2d`

```igor
Function onedpad2d(name)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `onedpad2d(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:2162`

#### `padm`

```igor
Function padm(namew,factor)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `padm(namew, factor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Main internal calls: `di()`. Update log: 07/09-02/2023: Add function padm() to pad the 2D matrix, add some 0 points in the outer of the data and enlarge the dimension size.
- Parameters:
  - `namew`: Igor wave reference used as input or output.
  - `factor`: how many time to enlarge the matrix
- Source: `Miscellaneous_Codes.ipf:3230`

#### `peakget`

```igor
Function/Wave peakget(name111,nameraw)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `peakget(name111, nameraw)`.
- Notes: Wave-returning helper for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `name111w()`, `name1111w()`, `ckwaveonfig()`.
- Parameters:
  - `name111`: String name used to locate an Igor wave, graph, folder, or output object.
  - `nameraw`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `FFT.ipf:2848`

#### `plust`

```igor
Function/Wave plust(A,B)
```
- Notes: Text matrix Tensor Add Usage: call as `plust(A, B)`. Code behavior: creates output waves. Main internal calls: `str2num()`. Update log: 10/12/2023: (6) plust(A,B): Text matrix wave summation.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `MatrixCalculation.ipf:399`

#### `PrintMarqueeCoords`

```igor
Function PrintMarqueeCoords()
```
- Usage: call `PrintMarqueeCoords()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management.
- Source: `SmartDisplay.ipf:1242`

#### `Putinsameoctant`

```igor
Function/WAVE Putinsameoctant(waveforx,wavefory)//moves all the points of the 1st zone of the same X-Centered Fermi surface in the same octant
```
- Usage: call as `Putinsameoctant(waveforx, wavefory)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `waveforx`: Igor wave reference used as input or output.
  - `wavefory`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:29062`

#### `Putkfin1zone`

```igor
Function/WAVE Putkfin1zone(waveforx,wavefory)//moves all the points of the same X-Centered Fermi surface inside the first zone
```
- Usage: call as `Putkfin1zone(waveforx, wavefory)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `waveforx`: Igor wave reference used as input or output.
  - `wavefory`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:29084`

#### `PutonsameXcorner`

```igor
Function/WAVE PutonsameXcorner(waveforx,wavefory)//translates all the points to the same X-Centered Fermi surface
```
- Usage: call as `PutonsameXcorner(waveforx, wavefory)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `waveforx`: Igor wave reference used as input or output.
  - `wavefory`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:29115`

#### `rescaleall`

```igor
Function rescaleall(name,num,lenx,leny)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `rescaleall(name, num, lenx, leny)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `lenx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `leny`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10002`

#### `rescalegroup`

```igor
Function rescalegroup(ratiowave,batchname,num)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `rescalegroup(ratiowave, batchname, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `scalewave()`, `madewavebytemplate()`, `linkstsmap()`. Update log: 2025-05-19: Builds a corrected linecut by applying individual voltage-divider correction ratios to a batch of dI/dV curves.
- Parameters:
  - `ratiowave`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `batchname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:12910`

#### `rescalemapasa1dcurveF`

```igor
Function rescalemapasa1dcurveF(namemap,namecurve,factor)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `rescalemapasa1dcurveF(namemap, namecurve, factor)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
  - `namecurve`: String name used to locate an Igor wave, graph, folder, or output object.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:10088`

#### `rescalemapasa1dcurveFf`

```igor
Function rescalemapasa1dcurveFf(namemap,namecurve,factor)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `rescalemapasa1dcurveFf(namemap, namecurve, factor)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
  - `namecurve`: String name used to locate an Igor wave, graph, folder, or output object.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Smart_3D_Viewer_New.ipf:8006`

#### `Return_oneI_digimage`

```igor
Function Return_oneI_digimage(redwave,greenwave,bluewave,thisred,thisgreen,thisblue,method)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Return_oneI_digimage(redwave, greenwave, bluewave, thisred, thisgreen, thisblue, method)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics.
- Parameters:
  - `redwave`: Igor wave reference used as input or output.
  - `greenwave`: Igor wave reference used as input or output.
  - `bluewave`: Igor wave reference used as input or output.
  - `thisred`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `thisgreen`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `thisblue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `method`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:6688`

#### `Rotxy`

```igor
Function/Wave Rotxy(degree,x0,y0,Dis)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `Rotxy(degree, x0, y0, Dis)`.
- Notes: Wave-returning helper for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Update log: 01/22/2024: Added Rotxy(degree,x0,y0,Dis),which calculates the rotated (x,y) coordinate by an arbitrary rotation.
- Parameters:
  - `degree`: Step size or energy/voltage increment used by the calculation.
  - `x0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Dis`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4`

#### `s0`

```igor
Function/wave s0()
```
- Notes: Complex Pauli matrix Usage: call `s0()` from Igor procedure code or the command line. Code behavior: creates output waves. Update log: 10/12/2023: (7) s0() sx() sy() sz(): Complex numerical Pauli matrix.
- Source: `MatrixCalculation.ipf:474`

#### `scalewave`

```igor
Function scalewave(name,s)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `scalewave(name, s)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output. Update log: 2025-05-19: Rescales a dI/dV curve using a voltage-divider correction factor.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `s`: 你可以自行更改
- Source: `Miscellaneous_Codes.ipf:12850`

#### `setscaleall`

```igor
Function setscaleall(len)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setscaleall(len)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `len`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:7628`

#### `SetWaveNameinProcText`

```igor
Function/S SetWaveNameinProcText(wList,procText)
```
- Purpose: rewrites wave names inside stored graph recreation/procedure text.
- Usage: call as `SetWaveNameinProcText(wList, procText)`.
- Notes: String-returning helper for graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Parameters:
  - `wList`: String name used to locate an Igor wave, graph, folder, or output object.
  - `procText`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `transfergraph.ipf:32`

#### `shrinkmatrixbysteps`

```igor
Function shrinkmatrixbysteps(name,steps)
```
- Usage: call as `shrinkmatrixbysteps(name, steps)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `steps`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:5023`

#### `smoothfast`

```igor
Function smoothfast()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `smoothfast()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: smooths wave data.
- Source: `Miscellaneous_Codes.ipf:7666`

#### `smoothjumphalfvortex_V`

```igor
Function smoothjumphalfvortex_V(name,judge,smoothnp)
```
- Usage: call as `smoothjumphalfvortex_V(name, judge, smoothnp)`.
- Notes: Failed to organize: smooth the phase jump Not implement in the current version Purpose: smooths, normalizes, or removes background/trend components. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `judge`: (=2.9)
  - `smoothnp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:10958`

#### `smoothtimes`

```igor
Function smoothtimes(name)
```
- Usage: call as `smoothtimes(name)`.
- Notes: INTERACTING FUNCTIONAL: Smooth times update Purpose: smooths, normalizes, or removes background/trend components. Code behavior: creates output waves; duplicates or stages waves for downstream processing; smooths wave data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:1645`

#### `st0`

```igor
Function/wave st0()
```
- Notes: Text Pauli matrix Usage: call `st0()` from Igor procedure code or the command line. Code behavior: creates output waves. Update log: 10/12/2023: (8) st0() stx() sty() stz(): Text Pauli matrix.
- Source: `MatrixCalculation.ipf:525`

#### `stx`

```igor
Function/wave stx()
```
- Usage: call `stx()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/12/2023: (8) st0() stx() sty() stz(): Text Pauli matrix.
- Source: `MatrixCalculation.ipf:547`

#### `sty`

```igor
Function/wave sty()
```
- Usage: call `sty()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/12/2023: (8) st0() stx() sty() stz(): Text Pauli matrix.
- Source: `MatrixCalculation.ipf:558`

#### `stz`

```igor
Function/wave stz()
```
- Usage: call `stz()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/12/2023: (8) st0() stx() sty() stz(): Text Pauli matrix.
- Source: `MatrixCalculation.ipf:536`

#### `subbkgndremover`

```igor
Function subbkgndremover(Wavex,wavey,waveoriginal,Waveresult)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `subbkgndremover(Wavex, wavey, waveoriginal, Waveresult)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `Interp()`.
- Parameters:
  - `Wavex`: Igor wave reference used as input or output.
  - `wavey`: Igor wave reference used as input or output.
  - `waveoriginal`: Igor wave reference used as input or output.
  - `Waveresult`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:9105`

#### `SubdiagM`

```igor
Function/Wave SubdiagM(num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `SubdiagM(num)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/29/2023: Added diagM(num) SuperdiagM(num) SubdiagM(num) (V2: added cursor hooks to also the bulk bands) (V3 is a debugged version).
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `MatrixCalculation.ipf:879`

#### `SubdiagM_cmplx`

```igor
Function/Wave SubdiagM_cmplx(num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `SubdiagM_cmplx(num)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `MatrixCalculation.ipf:836`

#### `SuperdiagM`

```igor
Function/Wave SuperdiagM(num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `SuperdiagM(num)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/29/2023: Added diagM(num) SuperdiagM(num) SubdiagM(num) (V2: added cursor hooks to also the bulk bands) (V3 is a debugged version).
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `MatrixCalculation.ipf:893`

#### `SuperdiagM_cmplx`

```igor
Function/Wave SuperdiagM_cmplx(num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `SuperdiagM_cmplx(num)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `MatrixCalculation.ipf:850`

#### `sx`

```igor
Function/wave sx()
```
- Usage: call `sx()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/12/2023: (7) s0() sx() sy() sz(): Complex numerical Pauli matrix.
- Source: `MatrixCalculation.ipf:496`

#### `sy`

```igor
Function/wave sy()
```
- Usage: call `sy()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/12/2023: (7) s0() sx() sy() sz(): Complex numerical Pauli matrix.
- Source: `MatrixCalculation.ipf:507`

#### `sz`

```igor
Function/wave sz()
```
- Usage: call `sz()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Update log: 10/12/2023: (7) s0() sx() sy() sz(): Complex numerical Pauli matrix.
- Source: `MatrixCalculation.ipf:485`

#### `tdtod`

```igor
Function tdtod(waven)
```
- Usage: call as `tdtod(waven)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves.
- Parameters:
  - `waven`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:1835`

#### `testkrnkproduct`

```igor
Function testkrnkproduct(wave1,wave2)
```
- Usage: call as `testkrnkproduct(wave1, wave2)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `mpt()`, `cktable()`.
- Parameters:
  - `wave1`: Igor wave reference used as input or output.
  - `wave2`: Igor wave reference used as input or output.
- Source: `MatrixCalculation.ipf:424`

#### `Transformpredicted`

```igor
Function Transformpredicted(parawave,perpwave,kthetawave,kphiwave)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Transformpredicted(parawave, perpwave, kthetawave, kphiwave)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `parawave`: Igor wave reference used as input or output.
  - `perpwave`: Igor wave reference used as input or output.
  - `kthetawave`: Igor wave reference used as input or output.
  - `kphiwave`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:26083`

#### `twoDinterpolatexycr`

```igor
Function twoDinterpolatexycr(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexycr(name, xpoint, ypoint)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:244`

#### `twoDinterpolatexyf`

```igor
Function twoDinterpolatexyf(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexyf(name, xpoint, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`, `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:353`

#### `twoDinterpolatexyf2`

```igor
Function twoDinterpolatexyf2(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexyf2(name, xpoint, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:411`

#### `twoDinterpolatexyfd`

```igor
Function twoDinterpolatexyfd(name,xpoint,ypoint)
```
- Usage: call as `twoDinterpolatexyfd(name, xpoint, ypoint)`.
- Notes: the unplot interpolation function called in the main procedure Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Map_the_phase_difference_between_two_image.ipf:454`

#### `twoDinterpyf`

```igor
Function twoDinterpyf(name,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpyf(name, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:535`

#### `unevenlinep`

```igor
Function unevenlinep(batchname,templatewave,num)
```
- Usage: call as `unevenlinep(batchname, templatewave, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `madewavebytemplate()`, `linkstsmap()`. Update log: 2025-05-19: Builds a linecut from a batch of delta waves with uneven dimensions by using a template wave as the reference geometry.
- Parameters:
  - `batchname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `templatewave`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:13025`

#### `wC`

```igor
Function/Wave wC(name,ww)
```
- Notes: #3. Complex wave (real) try wT("name",{cmplx(,)}), {cmplx(1,1)} for 1D, &#123;&#123;cmplx(1,1)}} for 2D, &#123;&#123;{cmplx(1,1)}}} for 3D Usage: call as `wC(name, ww)`. Code behavior: duplicates or stages waves for downstream processing. Update log: 10/12/2023: (9) wN(name,ww) wC(name,ww) wT(name,ww): Make wave ww under "name" for real wave, complex wave, text wave.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `ww`: Igor wave reference used as input or output.
- Source: `MatrixCalculation.ipf:459`

#### `WFofmatrix`

```igor
Function/wave WFofmatrix(mat,show)
```
- Notes: Momentum resolved Wavefunction(index) Usage: call as `WFofmatrix(mat, show)`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `sortColumnindex()`, `di()`. Update log: 10/31/2023: (4) WFofmatrix(mat,show): Calculate the wavefunction distribution of each band, indexed by the eigenvalue index.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `show`: Igor wave reference used as input or output.
- Source: `Models.ipf:9215`

#### `wN`

```igor
Function/Wave wN(name,ww)
```
- Notes: #1. Numerical wave (real) try wN("name",{}), {1} for 1D, &#123;&#123;1}} for 2D, &#123;&#123;{1}}} for 3D Usage: call as `wN(name, ww)`. Code behavior: duplicates or stages waves for downstream processing. Update log: 10/12/2023: (9) wN(name,ww) wC(name,ww) wT(name,ww): Make wave ww under "name" for real wave, complex wave, text wave.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `ww`: Igor wave reference used as input or output.
- Source: `MatrixCalculation.ipf:441`

#### `wT`

```igor
Function/Wave wT(name,ww)
```
- Notes: #2. Text wave (real) try wT("name",{""}), {"t"} for 1D, &#123;&#123;"t"}} for 2D, &#123;&#123;{"t"}}} for 3D Usage: call as `wT(name, ww)`. Code behavior: duplicates or stages waves for downstream processing. Update log: 10/12/2023: (9) wN(name,ww) wC(name,ww) wT(name,ww): Make wave ww under "name" for real wave, complex wave, text wave.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `ww`: Igor wave reference used as input or output.
- Source: `MatrixCalculation.ipf:450`

#### `xreflectmatrix`

```igor
Function xreflectmatrix(name,xpoint)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `xreflectmatrix(name, xpoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:681`

#### `xreflectmatrix2`

```igor
Function xreflectmatrix2(name,xpoint)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `xreflectmatrix2(name, xpoint)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4838`

#### `xreflectmatrix2r`

```igor
Function xreflectmatrix2r(name,xpoint)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `xreflectmatrix2r(name, xpoint)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4059`

#### `yreflectmatrix`

```igor
Function yreflectmatrix(name,ypoint)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `yreflectmatrix(name, ypoint)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:711`

#### `yreflectmatrix2`

```igor
Function yreflectmatrix2(name,ypoint)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `yreflectmatrix2(name, ypoint)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4867`

#### `yreflectmatrix2r`

```igor
Function yreflectmatrix2r(name,ypoint)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `yreflectmatrix2r(name, ypoint)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4088`



## Spectroscopy, Gap, Peak, and Fitting Workflows

### Panel callbacks and control handlers

#### `ButtonProc_choiceparameterc`

```igor
Function ButtonProc_choiceparameterc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Simulation of 2D gap modulation with 2 gaps Panel control(s): `2D PDM with 2 Gap`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D PDM with 2 Gap`
- Source: `General_Simu.ipf:17`

#### `ButtonProc_Cons3dplotgapmap`

```igor
Function ButtonProc_Cons3dplotgapmap(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: will only show up button when the 3D matrix put into 3D smart displayer is the g(r,V) the indentification is the name with the ending character "_G", which was defined in the loading procedure. #1 Gap Map Fitting, Control Button Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `GapMap`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `GapMap`
- Source: `Smart_3D_Viewer_New.ipf:3724`

#### `ButtonProc_DOS_kf`

```igor
Function ButtonProc_DOS_kf(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `DOS_kf()`.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `DOS at KF vs θ`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `DOS at KF vs θ`
- Source: `Pierre's Template.ipf:1238`

#### `ButtonProc_dscgap`

```igor
Function ButtonProc_dscgap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `plotdgap()`.
- Notes: under this procedure the energy under unit "meV" Ref: 10.1038/ncomms11139 Revisiting the vortex-core tunnelling spectroscopy in YBa2Cu3O7-d Panel control(s): `dSC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `dSC`
- Source: `Models.ipf:635`

#### `ButtonProc_DyneFit`

```igor
Function ButtonProc_DyneFit(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `STSFiTDyne()`.
- Notes: Normal tip with a linear density of states: a*(-x)+b SC sample with Dyne function density of states Head and fitting command part of this procedure Purpose: fits or extracts spectral/peak parameters. Panel control(s): `BCS_Dyne Fit`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BCS_Dyne Fit`
- Source: `Miscellaneous_Codes.ipf:12158`

#### `ButtonProc_DyneFitS`

```igor
Function ButtonProc_DyneFitS(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `STSFiTDyneS()`.
- Notes: This is a function for SC-SC tunneling simulatioon SC tip with Dyne density of states SC sample with Dyne function density of states Purpose: fits or extracts spectral/peak parameters. Panel control(s): `SC-SC Fit`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `SC-SC Fit`
- Source: `Miscellaneous_Codes.ipf:12344`

#### `ButtonProc_extracnumfromstr`

```igor
Function ButtonProc_extracnumfromstr(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `extracnumfromstr()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Extract_dI/dV`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Extract_dI/dV`
- Source: `Miscellaneous_Codes.ipf:8061`

#### `ButtonProc_extractfromdos`

```igor
Function ButtonProc_extractfromdos(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `extractdatafromdos()`.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `ExSTS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ExSTS`
- Source: `Miscellaneous_Codes.ipf:5101`

#### `ButtonProc_fixgauss_Fatcsr`

```igor
Function ButtonProc_fixgauss_Fatcsr(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `fixgauss_Fatcsr()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:24087`

#### `ButtonProc_gapdiis`

```igor
Function ButtonProc_gapdiis(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `extrastsfrommap3d()`.
- Notes: The method is 1. Linear substraction around a peak, [From, to] 2. Wavestatus to find the x_position of Maxmimun Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `2D_LS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D_LS`
- Source: `Miscellaneous_Codes.ipf:8274`

#### `ButtonProc_gapdiisGuassian`

```igor
Function ButtonProc_gapdiisGuassian(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `extrastsfrommap3dGuassian()`.
- Notes: The method is 1. Guassian Fit around a single peak [From, to] Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `2D_G`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D_G`
- Source: `Miscellaneous_Codes.ipf:8384`

#### `ButtonProc_gapdiisGuassian2`

```igor
Function ButtonProc_gapdiisGuassian2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `extrastsfrommap3dGuassian2()`.
- Notes: valid result, we then take the value form linear background substration method, and choose the gapsize as the average of the left and right.............................................................................................................. In the fu Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `2D_GCB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D_GCB`
- Source: `Miscellaneous_Codes.ipf:8522`

#### `ButtonProc_gapdiisGuassian3`

```igor
Function ButtonProc_gapdiisGuassian3(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `extrastsfrommap3dGuassian3()`.
- Notes: Get Gap distribution from linecut, mapsts window should be in active This is the 1D version of GCB [Guassian-combined] method !!!!! Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `1D_GCB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `1D_GCB`
- Source: `Miscellaneous_Codes.ipf:8780`

#### `ButtonProc_gapdisGua1_3dplot`

```igor
Function ButtonProc_gapdisGua1_3dplot(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gapdiisGua1_3dplot()`.
- Notes: #3_03_01 Guassian mode <Main Function> Purpose: displays waves, images, contours, or graph overlays.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Smart_3D_Viewer_New.ipf:4237`

#### `ButtonProc_gapdisGua2_3dplot`

```igor
Function ButtonProc_gapdisGua2_3dplot(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gapdiisGua2_3dplot()`.
- Notes: #3_01_01 GCB mode <spare Button control> Purpose: displays waves, images, contours, or graph overlays.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Smart_3D_Viewer_New.ipf:3872`

#### `ButtonProc_gatemapextractpeak`

```igor
Function ButtonProc_gatemapextractpeak(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gatemapextractpeak()`.
- Notes: Two peaks can be choose We use 4th derivatives to enhance the signal To begin with this procedure, please load the gate map data and normalized it properly, and then make one sts 4th derivatived, to find right1,right2,left1,left2 Purpose: extracts values, metadata, cursor information, or derived waves. Panel control(s): `PeakvsGate`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `PeakvsGate`
- Source: `Miscellaneous_Codes.ipf:11960`

#### `ButtonProc_gauss_Fat_matin`

```igor
Function ButtonProc_gauss_Fat_matin(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `gauss_Fat_matin()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:24082`

#### `ButtonProc_getpeakfromwfc`

```igor
Function ButtonProc_getpeakfromwfc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `getpeakfromwaterfallc()`.
- Notes: get peak Purpose: extracts values, metadata, cursor information, or derived waves. Panel control(s): `Get Peak / 1D multi`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get Peak / 1D multi`
- Source: `Miscellaneous_Codes.ipf:10900`

#### `ButtonProc_IExyf`

```igor
Function ButtonProc_IExyf(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mapforSTMf()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `dI/dV(E,x,y)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `dI/dV(E,x,y)`
- Source: `Miscellaneous_Codes.ipf:9839`

#### `ButtonProc_legend_dIdV_vs_V`

```igor
Function ButtonProc_legend_dIdV_vs_V(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `dI/dV`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `dI/dV`
- Source: `Miscellaneous_Codes.ipf:5261`

#### `ButtonProc_linecutgapdistrpp`

```igor
Function ButtonProc_linecutgapdistrpp(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linecutgapdistr()`.
- Notes: Get Gap distribution from linecut, mapsts window should be in active This is the linear background substraction method!!!!! Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `1D_LS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `1D_LS`
- Source: `Miscellaneous_Codes.ipf:8696`

#### `ButtonProc_LLDOS`

```igor
Function ButtonProc_LLDOS(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `makeLLspectra()`.
- Notes: The procedure bellow is to calculate LL DOS within different Temperature and Field Simple model regard LL peak as a Lorentzian, and add them all Panel control(s): `LL DOS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LL DOS`
- Source: `Models.ipf:4413`

#### `ButtonProc_manualvalue_1Dextracp`

```igor
Function ButtonProc_manualvalue_1Dextracp(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Adds-ons Manual change extraction value Panel control(s): `Manual. Extraction`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Manual. Extraction`
- Source: `MultipeakforLinecut.ipf:1796`

#### `ButtonProc_p2dtopeak_proc`

```igor
Function ButtonProc_p2dtopeak_proc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `p2dtopeak_proc()`.
- Notes: Convert 2D data to 1D data, the order is, first fix p, run q; then p+1, run q Purpose: fits or extracts spectral/peak parameters.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:1788`

#### `ButtonProc_PE_CalculateDCBc`

```igor
Function ButtonProc_PE_CalculateDCBc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Batch DCB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Batch DCB`
- Source: `ModelingTunneling.ipf:816`

#### `ButtonProc_peakIndi`

```igor
Function ButtonProc_peakIndi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `peakIndi()`.
- Notes: while (i<dimsize(name,0)) variable ave = suma/count return ave end Purpose: fits or extracts spectral/peak parameters. Panel control(s): `Get Q`, `Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get Q`, `Q`
- Source: `FFT.ipf:2839`

#### `ButtonProc_peakp2d_proc`

```igor
Function ButtonProc_peakp2d_proc(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `peakp2d_proc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:12584`

#### `ButtonProc_renameEDCMDC`

```igor
Function ButtonProc_renameEDCMDC(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `renameEDCMDC()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:3545`

#### `ButtonProc_sEDC`

```igor
Function ButtonProc_sEDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `sEDC()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `EDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `EDC`
- Source: `Miscellaneous_Codes.ipf:3305`

#### `ButtonProc_sizedos`

```igor
Function ButtonProc_sizedos(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Panel control(s): `2e2/h`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2e2/h`
- Source: `Miscellaneous_Codes.ipf:5295`

#### `ButtonProc_Smart2DEMDC`

```igor
Function ButtonProc_Smart2DEMDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Smart2DEMDC()`.
- Notes: $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ Panel control(s): `2D Multifunc. Displayer`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D Multifunc. Displayer`
- Source: `SmartDisplay.ipf:2556`

#### `ButtonProc_sMDC`

```igor
Function ButtonProc_sMDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `sMDC()`.
- Notes: SLicing EDC and MDC Panel control(s): `MDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MDC`
- Source: `Miscellaneous_Codes.ipf:3272`

#### `ButtonProc_subavedidv`

```igor
Function ButtonProc_subavedidv(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `comparehotandcoldc()`.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Select area average dI/dV curve }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Grp.dI/dV`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Grp.dI/dV`, `Grp.dI/dV`
- Source: `Smart_3D_Viewer_New.ipf:10074`

#### `ButtonProc_sumonedc`

```igor
Function ButtonProc_sumonedc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `sumonedc()`.
- Notes: ## 04 Average out two dimensions for a 3D Matrix Purpose: combines many waves/slices into a map, linecut, or averaged output. Panel control(s): `It3/1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `It3/1`
- Source: `Smart_3D_Viewer_New.ipf:2328`

#### `ButtonProc_t2nddpeakc`

```igor
Function ButtonProc_t2nddpeakc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `t2nddpeakc()`.
- Notes: Part 1 Multipeak extraction for linecut Purpose: fits or extracts spectral/peak parameters. Panel control(s): `MultiPeak 1D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MultiPeak 1D`
- Source: `MultipeakforLinecut.ipf:15`

#### `ButtonProc_t2nddpeakcd`

```igor
Function ButtonProc_t2nddpeakcd(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `t2nddpeakcd()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Show / dI/dV / History`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show / dI/dV / History`
- Source: `MultipeakforLinecut.ipf:1028`

#### `ButtonProc_t2nddpeakturnoff`

```igor
Function ButtonProc_t2nddpeakturnoff(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Part 2 Extraction History Displayer Purpose: fits or extracts spectral/peak parameters. Panel control(s): `X`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `grabwinnonew()`, `tpw()`, `grabtablenonew()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `X`
- Source: `MultipeakforLinecut.ipf:1008`

#### `PopMenuProc_QHZgapsym`

```igor
Function PopMenuProc_QHZgapsym(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `Gap. Sym.`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Gap. Sym.`
- Source: `Models.ipf:5701`

#### `SetVarProc_changegap`

```igor
Function SetVarProc_changegap(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `D`
- Source: `Miscellaneous_Codes.ipf:6393`

#### `SetVarProc_Cons3dplotprbcSC`

```igor
Function SetVarProc_Cons3dplotprbcSC(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Control the simulation parameters }}}}}}}}}} Purpose: fits or extracts spectral/peak parameters. Panel control(s): `Z`. Main internal calls: `updatePRB_98_214503_SCFS()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `Models.ipf:1912`

#### `SetVarProc_const2nddpeakind`

```igor
Function SetVarProc_const2nddpeakind(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `MDC index`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `MDC index`
- Source: `MultipeakforLinecut.ipf:507`

#### `SetVarProc_const2nddpeakindd`

```igor
Function SetVarProc_const2nddpeakindd(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Show Extraction History (MDC index)`. Main internal calls: `const2nddpeakcontrold()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Show Extraction History (MDC index)`
- Source: `MultipeakforLinecut.ipf:1318`

#### `SetVarProc_const2nddpeakl1`

```igor
Function SetVarProc_const2nddpeakl1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `L1`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `L1`
- Source: `MultipeakforLinecut.ipf:476`

#### `SetVarProc_const2nddpeakl2`

```igor
Function SetVarProc_const2nddpeakl2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `L2`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `L2`
- Source: `MultipeakforLinecut.ipf:468`

#### `SetVarProc_const2nddpeakmode`

```igor
Function SetVarProc_const2nddpeakmode(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: const2nddpeakcontrol() end Purpose: fits or extracts spectral/peak parameters. Panel control(s): `2dDBgRemove?`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `2dDBgRemove?`
- Source: `MultipeakforLinecut.ipf:435`

#### `SetVarProc_const2nddpeakmp`

```igor
Function SetVarProc_const2nddpeakmp(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `minPeak%`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `minPeak%`
- Source: `MultipeakforLinecut.ipf:444`

#### `SetVarProc_const2nddpeakpm`

```igor
Function SetVarProc_const2nddpeakpm(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `MultipeakforLinecut.ipf:484`

#### `SetVarProc_const2nddpeakr1`

```igor
Function SetVarProc_const2nddpeakr1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `R1`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `R1`
- Source: `MultipeakforLinecut.ipf:460`

#### `SetVarProc_const2nddpeakr2`

```igor
Function SetVarProc_const2nddpeakr2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `R2`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `R2`
- Source: `MultipeakforLinecut.ipf:452`

#### `SetVarProc_const2nddpeaksmt2ndd`

```igor
Function SetVarProc_const2nddpeaksmt2ndd(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Smt.(both)`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Smt.(both)`
- Source: `MultipeakforLinecut.ipf:500`

#### `SetVarProc_const2nddpeaksmt2nddadd`

```igor
Function SetVarProc_const2nddpeaksmt2nddadd(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Smt.(addR)`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Smt.(addR)`
- Source: `MultipeakforLinecut.ipf:492`

#### `SetVarProc_const2nddpext`

```igor
Function SetVarProc_const2nddpext(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Corct.extrct.`. Main internal calls: `const2nddpeakcontrol()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Corct.extrct.`
- Source: `MultipeakforLinecut.ipf:406`

#### `SetVarProc_const2nddpstyle`

```igor
Function SetVarProc_const2nddpstyle(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `Style`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `mode()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Style`
- Source: `MultipeakforLinecut.ipf:388`

#### `SetVarProc_SmartEDC_2dplot`

```igor
Function SetVarProc_SmartEDC_2dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #2 control EDC Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `EDC`. Main internal calls: `getEDC_2dplot()`, `recalindicativeline()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `EDC`
- Source: `SmartDisplay.ipf:2741`

#### `SetVarProc_SmartMDC_2dplot`

```igor
Function SetVarProc_SmartMDC_2dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #1 control MDC Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `MDC`. Main internal calls: `getMDC_2dplot()`, `recalindicativeline()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `MDC`
- Source: `SmartDisplay.ipf:2728`


### Interactive procedures and macros

#### `Au_DOS`

```igor
Proc Au_DOS(datanumber,smoothf)
```
- Usage: run from Igor with parameters `datanumber`, `smoothf`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: datanumber = Data number; smoothf = Smooth factor. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`, `setvarplot()`, `rgb()`.
- Parameters:
  - `datanumber`: Data number
  - `smoothf`: Smooth factor
- Source: `Pierre's Template.ipf:8406`

#### `AutomaticallyFindPeaksc`

```igor
Proc AutomaticallyFindPeaksc(wname,maxPeaks,minPeakPercent,show)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `wname`, `maxPeaks`, `minPeakPercent`, `show`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: wname = Peak Wave; maxPeaks = Maximum Peaks; minPeakPercent = Minimum Peak Amplitude (% max). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `AutomaticallyFindPeaks2()`.
- Parameters:
  - `wname`: Peak Wave
  - `maxPeaks`: Maximum Peaks
  - `minPeakPercent`: Minimum Peak Amplitude (% max)
  - `show`: Igor wave reference used as input or output.
- Source: `MultipeakforLinecut.ipf:715`

#### `BKFAgapatM`

```igor
Proc BKFAgapatM(indexwave,gapwave,nametogive)
```
- Usage: run from Igor with parameters `indexwave`, `gapwave`, `nametogive`.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: indexwave = Wave of indexes:; gapwave = Wave for gap:; nametogive = Base name for results:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `wintype()`.
- Parameters:
  - `indexwave`: Wave of indexes:
  - `gapwave`: Wave for gap:
  - `nametogive`: Base name for results:
- Source: `Pierre's Template.ipf:14654`

#### `CalculateDCBc`

```igor
Proc CalculateDCBc(startT,deltaT,EndT)
```
- Usage: run from Igor with parameters `startT`, `deltaT`, `EndT`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: startT = Start T (K); deltaT = delta T (K); EndT = End T (K). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `CalculateDCB()`.
- Parameters:
  - `startT`: Start T (K)
  - `deltaT`: delta T (K)
  - `EndT`: End T (K)
- Source: `ModelingTunneling.ipf:821`

#### `extractdatafromdos`

```igor
Proc extractdatafromdos()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `Show_dos()`, `make_dos()`.
- Source: `Miscellaneous_Codes.ipf:5206`

#### `FindEdcPeakDispersion`

```igor
proc FindEdcPeakDispersion()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics.
- Source: `Pierre's Template.ipf:1830`

#### `fit_peaks`

```igor
Proc fit_peaks(name_wave,xstart,xend)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name_wave`, `xstart`, `xend`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: name_wave = Enter the name of the wave; xstart = Fit to:. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `name_wave()`, `lorgauss()`.
- Parameters:
  - `name_wave`: Enter the name of the wave
  - `xstart`: Fit to:
  - `xend`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:18080`

#### `fixgauss_Fatcsr`

```igor
Proc fixgauss_Fatcsr()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `xcsr()`, `vcsr()`.
- Source: `Pierre's Template.ipf:24037`

#### `gapdiis`

```igor
proc gapdiis(mat,num)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `mat`, `num`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: creates output waves; extracts image/wave statistics.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:8756`

#### `gapdisGua1_3dplot`

```igor
proc gapdisGua1_3dplot(name,froml,tol)
```
- Usage: run from Igor with parameters `name`, `froml`, `tol`.
- Notes: #3_03_02 Guassian mode <Control layout> Purpose: displays waves, images, contours, or graph overlays. Dialog prompts: froml = Start -Energy value for search; tol = End -Energy value for search. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `gapdisGua1_3dplotf()`, `dilf()`, `modifyphasetopo()`, `color3s_for3d()`, `grabwinnonew()`.
- Parameters:
  - `name`: Input the name of 3d matrix g(r,V)
  - `froml`: Start -Energy value for search
  - `tol`: End -Energy value for search
- Source: `Smart_3D_Viewer_New.ipf:4243`

#### `gapdisGua2_3dplot`

```igor
proc gapdisGua2_3dplot(name,froml,tol,fromr,tor)
```
- Usage: run from Igor with parameters `name`, `froml`, `tol`, `fromr`, `tor`.
- Notes: #3_01_02 GCB mode <Procedure control layout> Purpose: displays waves, images, contours, or graph overlays. Dialog prompts: froml = Start -Energy value for search; tol = End -Energy value for search; fromr = Start +Energy value for search; tor = End +Energy value for search. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `gapdisGua2_3dplotf()`, `dilf()`, `modifyphasetopo()`, `color3s_for3d()`, `grabwinnonew()`.
- Parameters:
  - `name`: Input the name of 3d matrix g(r,V)
  - `froml`: Start -Energy value for search
  - `tol`: End -Energy value for search
  - `fromr`: Start +Energy value for search
  - `tor`: End +Energy value for search
- Source: `Smart_3D_Viewer_New.ipf:3878`

#### `gapdisGua2_3dplotsel`

```igor
Proc gapdisGua2_3dplotsel(sel,name,froml,tol,fromr,tor)
```
- Usage: run from Igor with parameters `sel`, `name`, `froml`, `tol`, `fromr`, `tor`.
- Notes: #2 Gap Map Fitting, select mode and set the indicative lines Purpose: displays waves, images, contours, or graph overlays. Dialog prompts: sel = Mode; Name = name of the 3D matrix g(r,V); froml = Start -Energy value for search; tol = End -Energy value for search; fromr = Start +Energy value for search; tor = End +Energy value for search. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `grabwinchild()`, `lstyle()`, `rgb()`, `gapdisGua2_3dplot()`, `gapdisGua1_3dplot()`, `gapdisLinear_3dplot()`.
- Parameters:
  - `sel`: Mode
  - `name`: Input the name of 3d matrix g(r,V)
  - `froml`: Start -Energy value for search
  - `tol`: End -Energy value for search
  - `fromr`: Start +Energy value for search
  - `tor`: End +Energy value for search
- Source: `Smart_3D_Viewer_New.ipf:3749`

#### `gapdisLinear_3dplot`

```igor
proc gapdisLinear_3dplot(name,from,to)
```
- Usage: run from Igor with parameters `name`, `from`, `to`.
- Notes: #3_02_01 Linear substraction mode <control layout> Purpose: displays waves, images, contours, or graph overlays. Dialog prompts: from = Start -Energy value for search; to = End -Energy value for search. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `gapdisLinear_3dplotf()`, `dilf()`, `modifyphasetopo()`, `color3s_for3d()`, `grabwinnonew()`.
- Parameters:
  - `name`: Input the name of 3d matrix g(r,V)
  - `from`: Start -Energy value for search
  - `to`: End -Energy value for search
- Source: `Smart_3D_Viewer_New.ipf:4098`

#### `gauss_Fat_matin`

```igor
Proc gauss_Fat_matin(matname)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:24045`

#### `gauss_Fdivide`

```igor
Proc gauss_Fdivide()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:24068`

#### `gauss_Fupdate`

```igor
Proc gauss_Fupdate()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:24054`

#### `linecutgapdistr`

```igor
proc linecutgapdistr(name, from,to,suffix)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `from`, `to`, `suffix`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `suffix`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Miscellaneous_Codes.ipf:8700`

#### `make_dos`

```igor
Proc make_dos()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Miscellaneous_Codes.ipf:5173`

#### `makeEDC_KLY`

```igor
proc makeEDC_KLY()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Source: `Miscellaneous_Codes.ipf:1532`

#### `manualvalue_1Dextracp`

```igor
Proc manualvalue_1Dextracp(Note1, Note2, leftvalue, rightvalue)
```
- Usage: run from Igor with parameters `Note1`, `Note2`, `leftvalue`, `rightvalue`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: leftvalue = Left peaks; rightvalue = Right peaks. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `manualvalue_1Dextrac()`.
- Parameters:
  - `Note1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Note2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `leftvalue`: Left peaks
  - `rightvalue`: Right peaks
- Source: `MultipeakforLinecut.ipf:1800`

#### `p2dtopeak_proc`

```igor
Proc p2dtopeak_proc(name)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Name of the 2D wave, Now convert it to 1D. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `p2dtopeak()`.
- Parameters:
  - `name`: Name of the 2D wave, Now convert it to 1D
- Source: `Miscellaneous_Codes.ipf:1793`

#### `peakIndi`

```igor
Proc peakIndi()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `getall2dwaveongraph()`, `peakget()`.
- Source: `FFT.ipf:2843`

#### `peakp2d_proc`

```igor
proc peakp2d_proc(name)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Name of the 1D wave, Now convert it to 2D map. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `peakp2d()`.
- Parameters:
  - `name`: Name of the 1D wave, Now convert it to 2D map
- Source: `Miscellaneous_Codes.ipf:12589`

#### `plotdgap`

```igor
proc plotdgap(grid, gam, delta, range)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `grid`, `gam`, `delta`, `range`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: grid = Number of points; range = Energy range of spectra (meV); gam = Dyne width (meV); delta = SC gap (meV). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `integrate1D()`.
- Parameters:
  - `grid`: Number of points
  - `gam`: Dyne width (meV)
  - `delta`: SC gap (meV)
  - `range`: Energy range of spectra (meV)
- Source: `Models.ipf:639`

#### `pseudogap_profile`

```igor
Proc pseudogap_profile(gap,temperature)
```
- Usage: run from Igor with parameters `gap`, `temperature`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: gap = Enter the impurity gap; temperature = Enter the temperature of the profile. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `fermigap()`.
- Parameters:
  - `gap`: Enter the impurity gap
  - `temperature`: Enter the temperature of the profile
- Source: `Pierre's Template.ipf:20814`

#### `remove_peak_2order`

```igor
Proc remove_peak_2order()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `pnt2x()`.
- Source: `Pierre's Template.ipf:16786`

#### `renameEDCMDC`

```igor
proc renameEDCMDC(num,name)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `num`, `name`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `displaymulti()`.
- Parameters:
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3549`

#### `sEDC`

```igor
Proc sEDC(mat)
```
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Main internal calls: `tpw()`, `slicesEDC()`, `color_edc()`. Update log: 07/30/2023: Added sEDC()and sMDC() which can slice a 2D matrix to be 1D waves.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3310`

#### `set_gauss_F`

```igor
Proc set_gauss_F()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Source: `Pierre's Template.ipf:24019`

#### `Show_dos`

```igor
Proc Show_dos()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:121`

#### `simul1`

```igor
Proc simul1(gap,igap,dosgap)
```
- Usage: run from Igor with parameters `gap`, `igap`, `dosgap`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Dialog prompts: gap = Enter the hybridization gap; igap = Enter the impurity gap (gap method); dosgap = Enter the impurity gap (DOS method). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `pnt2x()`, `fermigap()`.
- Parameters:
  - `gap`: Enter the hybridization gap
  - `igap`: Enter the impurity gap (gap method)
  - `dosgap`: Enter the impurity gap (DOS method)
- Source: `Pierre's Template.ipf:19976`

#### `simul1WPI`

```igor
Proc simul1WPI(gap,igap,dosgap)
```
- Usage: run from Igor with parameters `gap`, `igap`, `dosgap`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Dialog prompts: gap = Enter the hybridization gap; igap = Enter the impurity gap (gap method); dosgap = Enter the impurity gap (DOS method). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `pnt2x()`, `fermigap()`.
- Parameters:
  - `gap`: Enter the hybridization gap
  - `igap`: Enter the impurity gap (gap method)
  - `dosgap`: Enter the impurity gap (DOS method)
- Source: `Pierre's Template.ipf:20037`

#### `simul2`

```igor
Proc simul2(gap,igap,dosgap)
```
- Usage: run from Igor with parameters `gap`, `igap`, `dosgap`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: gap = Enter the hybridization gap; igap = Enter the impurity gap (gap method); dosgap = Enter the impurity gap (DOS method). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `tbfunc()`, `pnt2x()`, `fermigap()`.
- Parameters:
  - `gap`: Enter the hybridization gap
  - `igap`: Enter the impurity gap (gap method)
  - `dosgap`: Enter the impurity gap (DOS method)
- Source: `Pierre's Template.ipf:20283`

#### `simul3`

```igor
Proc simul3(gap,igap,dosgap)
```
- Usage: run from Igor with parameters `gap`, `igap`, `dosgap`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: gap = Enter the hybridization gap; igap = Enter the impurity gap (gap method); dosgap = Enter the impurity gap (DOS method). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `tbfunc()`, `pnt2x()`, `rot2d_pi()`.
- Parameters:
  - `gap`: Enter the hybridization gap
  - `igap`: Enter the impurity gap (gap method)
  - `dosgap`: Enter the impurity gap (DOS method)
- Source: `Pierre's Template.ipf:20099`

#### `simul4`

```igor
Proc simul4(gap,igap,dosgap)
```
- Usage: run from Igor with parameters `gap`, `igap`, `dosgap`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: gap = Enter the hybridization gap; igap = Enter the impurity gap (gap method); dosgap = Enter the impurity gap (DOS method). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `tbfunc()`, `pnt2x()`, `rot2d_pi()`.
- Parameters:
  - `gap`: Enter the hybridization gap
  - `igap`: Enter the impurity gap (gap method)
  - `dosgap`: Enter the impurity gap (DOS method)
- Source: `Pierre's Template.ipf:20188`

#### `Smart2DEMDC`

```igor
Proc Smart2DEMDC(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: name = 2D matrix name. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `tpw()`, `getall2dwave()`, `margin()`, `getMDC_2dplot()`, `getEDC_2dplot()`, `childWindowList()`.
- Parameters:
  - `name`: 2D matrix name
- Source: `SmartDisplay.ipf:2560`

#### `sMDC`

```igor
Proc sMDC(mat)
```
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Main internal calls: `tpw()`, `slicesMDC()`, `color_edc()`. Update log: 07/30/2023: Added sEDC()and sMDC() which can slice a 2D matrix to be 1D waves.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3277`

#### `STSFiTDyne`

```igor
proc STSFiTDyne(mat,Db,gap,tem,gradient_tip,offset_tip)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `mat`, `Db`, `gap`, `tem`, `gradient_tip`, `offset_tip`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: mat = Name of Fit data; Db = Dyne Broadening (meV); gap = SC gap (meV); tem = Temperature (K) Fix; gradient_tip = a_tip(slope); offset_tip = b_tip(offset). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `scsc()`, `lsize()`, `rgb()`.
- Parameters:
  - `mat`: Name of Fit data
  - `Db`: Dyne Broadening (meV)
  - `gap`: SC gap (meV)
  - `tem`: Temperature (K) Fix
  - `gradient_tip`: a_tip(slope)
  - `offset_tip`: b_tip(offset)
- Source: `Miscellaneous_Codes.ipf:12162`

#### `STSFiTDyneS`

```igor
proc STSFiTDyneS(mat,Db,gap,gapt,tem,gradient_tip,offset_tip)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `mat`, `Db`, `gap`, `gapt`, `tem`, `gradient_tip`, `offset_tip`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: mat = Name of Fit data; Db = Dyne Broadening (meV); gap = SC gap (meV)_sample; gapt = SC gap (meV)_tip; tem = Temperature (K) Fix; gradient_tip = a_tip(slope). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `scscS()`, `lsize()`, `rgb()`.
- Parameters:
  - `mat`: Name of Fit data
  - `Db`: Dyne Broadening (meV)
  - `gap`: SC gap (meV)_sample
  - `gapt`: SC gap (meV)_tip
  - `tem`: Temperature (K) Fix
  - `gradient_tip`: a_tip(slope)
  - `offset_tip`: b_tip(offset)
- Source: `Miscellaneous_Codes.ipf:12348`

#### `sumonedc`

```igor
Proc sumonedc(orin,dest,zn)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `orin`, `dest`, `zn`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: orin = 3D wave; dest = Suffix of averaged 1D wave; zn = Dimension Index to retain. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `sumoned()`.
- Parameters:
  - `orin`: 3D wave
  - `dest`: Suffix of averaged 1D wave
  - `zn`: Dimension Index to retain
- Source: `Smart_3D_Viewer_New.ipf:2332`

#### `t2nddpeakc`

```igor
Proc t2nddpeakc(inputname,peaknum,smootht,smoothtadd,l1,l2,r1,r2,minPeakPercent,bgr)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `inputname`, `peaknum`, `smootht`, `smoothtadd`, `l1`, `l2`, `r1`, `r2`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: inputname = The wave name for mp extract (1D or 2D); peaknum = How much peaks in expectation; smootht = Times of smooth (both); smoothtadd = Times of smooth (Add to right); l1 = L1 (meV); l2 = L2 (meV). Code behavior: smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `t2nddpeak()`.
- Parameters:
  - `inputname`: The wave name for mp extract (1D or 2D)
  - `peaknum`: How much peaks in expectation
  - `smootht`: Times of smooth (both)
  - `smoothtadd`: Times of smooth (Add to right)
  - `l1`: L1 (meV)
  - `l2`: L2 (meV)
  - `r1`: R1 (meV)
  - `r2`: R2 (meV)
  - `minPeakPercent`: Minimum Peak Amplitude (% max)
  - `bgr`: Background remove (1) or not (0)
- Source: `MultipeakforLinecut.ipf:19`

#### `t2nddpeakcd`

```igor
Proc t2nddpeakcd()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `t2nddpeakd()`.
- Source: `MultipeakforLinecut.ipf:1032`


### Computational and data helpers

#### `appendattractedmin_max`

```igor
Function appendattractedmin_max(MDC,namew) //plot the extracted positions.
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `appendattractedmin_max(MDC, namew)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `mode()`, `marker()`, `msize()`, `rgb()`, `ckfig()`.
- Parameters:
  - `MDC`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `namew`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:11257`

#### `AutoFindPeaksWorker2`

```igor
Function AutoFindPeaksWorker2(w, wx, pBegin, pEnd, maxPeaks, minPeakPercent, noiseLevel, smoothingFactor,show)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `AutoFindPeaksWorker2(w, wx, pBegin, pEnd, maxPeaks, minPeakPercent, noiseLevel, smoothingFactor)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `AutoFindPeaks()`, `TrimAmpAutoPeakInfo()`, `AdjustAutoPeakInfoForX()`, `rgb()`, `mode()`, `marker()`.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `wx`: Used in: Display/N=ShowPeaks w vs wx
  - `pBegin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `pEnd`: End point, index, or coordinate for the selected range.
  - `maxPeaks`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `minPeakPercent`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `noiseLevel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smoothingFactor`: Scaling, normalization, interpolation, or correction factor.
  - `show`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `MultipeakforLinecut.ipf:790`

#### `AutomaticallyFindPeaks1`

```igor
Function AutomaticallyFindPeaks1(wname,maxPeaks,minPeakPercent,show)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `AutomaticallyFindPeaks1(wname, maxPeaks, minPeakPercent, show)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `numpnts()`, `EstPeakNoiseAndSmfact2()`, `AutoFindPeaksWorker2()`.
- Parameters:
  - `wname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `maxPeaks`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `minPeakPercent`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `show`: Igor wave reference used as input or output.
- Source: `MultipeakforLinecut.ipf:726`

#### `AutomaticallyFindPeaks2`

```igor
Function AutomaticallyFindPeaks2(wname,maxPeaks,minPeakPercent,show)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `AutomaticallyFindPeaks2(wname, maxPeaks, minPeakPercent, show)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `numpnts()`, `EstPeakNoiseAndSmfact2()`, `AutoFindPeaksWorker2()`.
- Parameters:
  - `wname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `maxPeaks`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `minPeakPercent`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `show`: Igor wave reference used as input or output.
- Source: `MultipeakforLinecut.ipf:758`

#### `BCS_delta_T`

```igor
Function BCS_delta_T(w,x) : FitFunc
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `BCS_delta_T(w, x)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Main internal calls: `tanh()`.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Procedure.ipf:243`

#### `CalculateDCB`

```igor
Function CalculateDCB(startT,deltaT,EndT)
```
- Usage: call as `CalculateDCB(startT, deltaT, EndT)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `CalculateP0E_auto()`, `SimulateDCB_IV()`.
- Parameters:
  - `startT`: Start point, index, or coordinate for the selected range.
  - `deltaT`: Step size or energy/voltage increment used by the calculation.
  - `EndT`: End point, index, or coordinate for the selected range.
- Source: `ModelingTunneling.ipf:831`

#### `const2nddpeak`

```igor
Function const2nddpeak()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call `const2nddpeak()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: reads or updates panel controls; cleans up waves/windows or brings an existing window forward.
- Source: `MultipeakforLinecut.ipf:345`

#### `const2nddpeakcontrol`

```igor
Function/Wave const2nddpeakcontrol()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `const2nddpeakcontrol()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; smooths wave data. Main internal calls: `smooth()`, `AutomaticallyFindPeaks1()`, `AutomaticallyFindPeaks2()`, `grabwinnonew()`.
- Source: `MultipeakforLinecut.ipf:515`

#### `const2nddpeakcontrold`

```igor
Function/Wave const2nddpeakcontrold()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `const2nddpeakcontrold()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; smooths wave data. Main internal calls: `smooth()`, `AutomaticallyFindPeaks1()`, `AutomaticallyFindPeaks2()`, `grabwinnonew()`.
- Source: `MultipeakforLinecut.ipf:1326`

#### `const2nddpeakd`

```igor
Function const2nddpeakd()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call `const2nddpeakd()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `grabwinnonew()`.
- Source: `MultipeakforLinecut.ipf:1288`

#### `DOSvor`

```igor
Function DOSvor(grid,K,b1,delta,xrange,Erange)
```
- Usage: call as `DOSvor(grid, K, b1, delta, xrange, Erange)`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `tanh()`.
- Parameters:
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `K`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `b1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `delta`: Step size or energy/voltage increment used by the calculation.
  - `xrange`: nm
  - `Erange`: meV
- Source: `Models.ipf:702`

#### `dpfig_pf`

```igor
Function dpfig_pf(i)
```
- Usage: call as `dpfig_pf(i)`.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `const2nddpeakcontrold()`, `mode()`, `marker()`, `rgb()`, `lsize()`, `standoff()`.
- Parameters:
  - `i`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `MultipeakforLinecut.ipf:1601`

#### `drawdyne`

```igor
Function drawdyne(width,delta,width2,delta2)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `drawdyne(width, delta, width2, delta2)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `width`: Igor wave reference used as input or output.
  - `delta`: Step size or energy/voltage increment used by the calculation.
  - `width2`: Igor wave reference used as input or output.
  - `delta2`: Step size or energy/voltage increment used by the calculation.
- Source: `General_Simu.ipf:1629`

#### `DyneS`

```igor
Function DyneS(a,delta,width)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `DyneS(a, delta, width)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis. Update log: 2024-05-12: Added the Dynes superconducting density-of-states function. Example: `make/n=100/o test; setscale/I x,-5,5,"",test; test=DyneS(x,1,0.1)`.
- Parameters:
  - `a`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `delta`: Step size or energy/voltage increment used by the calculation.
  - `width`: Igor wave reference used as input or output.
- Source: `General_Simu.ipf:1642`

#### `EdcPeakFit`

```igor
Function EdcPeakFit(w,x) : FitFunc
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `EdcPeakFit(w, x)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1750`

#### `EstPeakNoiseAndSmfact2`

```igor
Function/C EstPeakNoiseAndSmfact2(w,pBegin,pEnd[,userWidth])
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `EstPeakNoiseAndSmfact2(w, pBegin, pEnd[, userWidth])`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data; differentiates wave data. Main internal calls: `max()`, `min()`, `numpnts()`, `ceil()`, `deltax()`, `pnt2x()`.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `pBegin`: Used in: Duplicate/O/R=[pBegin,pEnd] w, w_temp
  - `pEnd[`: End point, index, or coordinate for the selected range.
  - `userWidth]`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `MultipeakforLinecut.ipf:841`

#### `extractgapbymax`

```igor
Function extractgapbymax(from,to,sizex,sizey)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `extractgapbymax(from, to, sizex, sizey)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:8316`

#### `extractgapbymaxGuassian`

```igor
Function extractgapbymaxGuassian(from,to,sizex,sizey)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `extractgapbymaxGuassian(from, to, sizex, sizey)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:8424`

#### `extractgapbymaxGuassian2`

```igor
Function extractgapbymaxGuassian2(froml,tol,fromr,tor,sizex,sizey)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `extractgapbymaxGuassian2(froml, tol, fromr, tor, sizex, sizey)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `froml`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tol`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fromr`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tor`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sizey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:8557`

#### `extractgapbymaxGuassian3`

```igor
Function extractgapbymaxGuassian3(froml,tol,fromr,tor,p1,p2,q1,q2,namedd)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `extractgapbymaxGuassian3(froml, tol, fromr, tor, p1, p2, q1, q2)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `froml`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tol`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fromr`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tor`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `p1`: Start point, index, or coordinate for the selected range.
  - `p2`: End point, index, or coordinate for the selected range.
  - `q1`: Start point, index, or coordinate for the selected range.
  - `q2`: End point, index, or coordinate for the selected range.
  - `namedd`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Miscellaneous_Codes.ipf:8836`

#### `findjumpslice`

```igor
Function findjumpslice(MDCww,jumpheight) //Locate the position of jump, use MDC to extract.
```
- Usage: call as `findjumpslice(MDCww, jumpheight)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; reshapes existing waves.
- Parameters:
  - `MDCww`: MDC slice name
  - `jumpheight`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:11216`

#### `gapdisGua1_3dplotf`

```igor
Function gapdisGua1_3dplotf(namemat3d,from,to)
```
- Usage: call as `gapdisGua1_3dplotf(namemat3d, from, to)`.
- Notes: #3_03_03 Guassian mode <Main Function> Purpose: displays waves, images, contours, or graph overlays. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namemat3d`: the 3D matrix g(r,v)
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:4293`

#### `gapdisGua2_3dplotf`

```igor
Function gapdisGua2_3dplotf(namemat3d,froml,tol,fromr,tor)
```
- Usage: call as `gapdisGua2_3dplotf(namemat3d, froml, tol, fromr, tor)`.
- Notes: #3_01_03 GCB mode <Main Function> Purpose: displays waves, images, contours, or graph overlays. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `grabwin()`.
- Parameters:
  - `namemat3d`: the 3D matrix g(r,v)
  - `froml`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tol`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fromr`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tor`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:3933`

#### `gapdisLinear_3dplotf`

```igor
Function gapdisLinear_3dplotf(namemat3d,from,to)
```
- Usage: call as `gapdisLinear_3dplotf(namemat3d, from, to)`.
- Notes: #3_02_02 Linear substraction mode <Main Function> Purpose: displays waves, images, contours, or graph overlays. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `namemat3d`: the 3D matrix g(r,v)
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:4151`

#### `getEDC_2dplot`

```igor
Function getEDC_2dplot(index)
```
- Usage: call as `getEDC_2dplot(index)`.
- Notes: #4 get EDC Purpose: extracts values, metadata, cursor information, or derived waves. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `tpw()`, `autoremovejump1D()`.
- Parameters:
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:2786`

#### `getMDC_2dplot`

```igor
Function getMDC_2dplot(index)
```
- Usage: call as `getMDC_2dplot(index)`.
- Notes: #3 get MDC Purpose: extracts values, metadata, cursor information, or derived waves. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `tpw()`, `autoremovejump1D()`.
- Parameters:
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `SmartDisplay.ipf:2754`

#### `GoldFitting`

```igor
function GoldFitting(dos,Tem)//dos is the wave to fit, Tem is the experimental Temperature.
```
- Usage: call as `GoldFitting(dos, Tem)`.
- Notes: End of Movie maker, by Jonathan Henry Bowen Purpose: fits or extracts spectral/peak parameters. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; formats graph axes, labels, colors, or annotations. Main internal calls: `csrinfo()`, `hcsr()`, `vcsr()`, `dos()`.
- Parameters:
  - `dos`: Used in: CurveFit/Q line, dos(2,startx)/D// the result return in a wave by default w_coef / CurveFit/Q line, dos(endx,18)/D
  - `Tem`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `Pierre's Template.ipf:36718`

#### `killEDCs`

```igor
Function killEDCs(namemap)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `killEDCs(namemap)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
- Source: `Miscellaneous_Codes.ipf:10153`

#### `killEDCsf`

```igor
Function killEDCsf(namemap)
```
- Usage: call as `killEDCsf(namemap)`.
- Notes: #3_05_02 GO Function Called Interpolation fucntions Purpose: removes waves/windows/data points or cleans intermediate state. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
- Source: `Smart_3D_Viewer_New.ipf:7987`

#### `LHGap`

```igor
Function LHGap(hv)
```
- Usage: call as `LHGap(hv)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `hv`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:1262`

#### `linkEDCs`

```igor
Function linkEDCs(namemap,namecurve,factor)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `linkEDCs(namemap, namecurve, factor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
  - `namecurve`: String name used to locate an Igor wave, graph, folder, or output object.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:10118`

#### `linkEDCs_nodis`

```igor
Function linkEDCs_nodis(namemap,namecurve,factor)
```
- Usage: call as `linkEDCs_nodis(namemap, namecurve, factor)`.
- Notes: #3_05_02 GO Function Called Interpolation fucntion Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
  - `namecurve`: String name used to locate an Igor wave, graph, folder, or output object.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Smart_3D_Viewer_New.ipf:3220`

#### `linkEDCs_nodisf`

```igor
Function linkEDCs_nodisf(namemap,namecurve,factor)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `linkEDCs_nodisf(namemap, namecurve, factor)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `namemap`: Igor wave or matrix input consumed by the calculation.
  - `namecurve`: String name used to locate an Igor wave, graph, folder, or output object.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Smart_3D_Viewer_New.ipf:8035`

#### `lorgauss`

```igor
Function lorgauss(m,x)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `lorgauss(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:18397`

#### `lorgauss_bl_fixed`

```igor
Function lorgauss_bl_fixed(m,x)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `lorgauss_bl_fixed(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:18454`

#### `lorgauss_strait`

```igor
Function lorgauss_strait(m,x)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `lorgauss_strait(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:18426`

#### `makegap1UCFeSe`

```igor
Function makegap1UCFeSe()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call `makegap1UCFeSe()` from Igor procedure code or the command line.
- Notes: Utility function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Models.ipf:13794`

#### `makegap1UCFeSebabs`

```igor
Function makegap1UCFeSebabs()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call `makegap1UCFeSebabs()` from Igor procedure code or the command line.
- Notes: Utility function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Models.ipf:14135`

#### `makegap1UCFeSed`

```igor
Function makegap1UCFeSed()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call `makegap1UCFeSed()` from Igor procedure code or the command line.
- Notes: Utility function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Models.ipf:13965`

#### `manualvalue_1Dextrac`

```igor
Function manualvalue_1Dextrac(leftvalue, rightvalue)
```
- Usage: call as `manualvalue_1Dextrac(leftvalue, rightvalue)`. Update-log guidance - 2024-06-27: Added manual correction for MultiPeak 1D extraction.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `str2num()`. Use `manualvalue_1Dextrac(leftvalue, rightvalue)` from the command line or panel button; each argument is a semicolon-separated list. A blank value writes NaN for a missing peak, `0` leaves the existing peak unchanged, and the entries are ordered by increasing absolute peak energy.
- Parameters:
  - `leftvalue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `rightvalue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `MultipeakforLinecut.ipf:1811`

#### `p2dtopeak`

```igor
Function p2dtopeak(name)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `p2dtopeak(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:1799`

#### `p2dtopeak_rot`

```igor
Function p2dtopeak_rot(name)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `p2dtopeak_rot(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:92`

#### `peakp2d`

```igor
Function peakp2d(name)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `peakp2d(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:12595`

#### `reorganizedata`

```igor
Function reorganizedata(MDC,namew,initialnum,sel,killim)
```
- Notes: From multiple horizental waves to construct the slanted wave Usage: call as `reorganizedata(MDC, namew, initialnum, sel, killim)`. Code behavior: creates output waves; reshapes existing waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `checkfirstNonnan()`, `rgb()`, `ckfig()`.
- Parameters:
  - `MDC`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `namew`: Igor wave reference used as input or output.
  - `initialnum`: how many lines you want to initialy create
  - `sel`: select mode, 0 is 2/4 quadrant, 1 is first 1/3 quadrant.
  - `killim`: kill (1) or not kill (0) intermedia waves, that is the horizental waves from fit
- Source: `Miscellaneous_Codes.ipf:11325`

#### `sftvfollowjump`

```igor
Function sftvfollowjump(MDC,namew,sel,increase) //Jumpping value remover
```
- Usage: call as `sftvfollowjump(MDC, namew, sel, increase)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `di()`.
- Parameters:
  - `MDC`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `sel`: select mode, 0 is 2/4 quadrant, 1 is first 1/3 quadrant.
  - `increase`: the value to add sequencely to each domain
- Source: `Miscellaneous_Codes.ipf:11690`

#### `slicemdc`

```igor
Function slicemdc(name)
```
- Usage: call as `slicemdc(name)`.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:10538`

#### `slicesEDC`

```igor
Function slicesEDC(mat)
```
- Usage: call as `slicesEDC(mat)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `displaymultiFedc()`, `ckfig()`, `Constantoffset_nF()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3315`

#### `slicesMDC`

```igor
Function slicesMDC(mat)
```
- Usage: call as `slicesMDC(mat)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `displaymultiF()`, `ckfig()`, `Constantoffset_nF()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3282`

#### `t2nddpeak`

```igor
Function/Wave t2nddpeak(inputname,smootht,smoothtadd,peaknum,l1,l2,r1,r2,minPeakPercent,show,bgr)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `t2nddpeak(inputname, smootht, smoothtadd, peaknum, l1, l2, r1, r2)`.
- Notes: Wave-returning helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `smooth()`, `AutomaticallyFindPeaks1()`, `AutomaticallyFindPeaks2()`, `mode()`, `marker()`, `rgb()`. Update log: 2023-09-21: MultiPeak extraction for 1D or 2D waves; the upgraded layout can show an arbitrary number of extracted peak figures and includes cleanup controls.
- Parameters:
  - `inputname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `smootht`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smoothtadd`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `peaknum`: Number of spectra, waves, points, bins, or iterations to process.
  - `l1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `l2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `r1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `r2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `minPeakPercent`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `show`: Igor wave reference used as input or output.
  - ... 1 additional parameters
- Source: `MultipeakforLinecut.ipf:45`

#### `t2nddpeakd`

```igor
Function/Wave t2nddpeakd()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call `t2nddpeakd()` from Igor procedure code or the command line.
- Notes: Wave-returning helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `smooth()`, `AutomaticallyFindPeaks1()`, `AutomaticallyFindPeaks2()`, `mode()`, `marker()`, `rgb()`.
- Source: `MultipeakforLinecut.ipf:1036`

#### `ZeroTonum`

```igor
function ZeroTonum()//all data, search which points equal to 0 ,and then let them equal to the average value of corresponding MDC
```
- Usage: call `ZeroTonum()` from Igor procedure code or the command line.
- Notes: to solve zero data points problem of DA30 high angle data Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Miscellaneous_Codes.ipf:1272`



## ARPES Template and Momentum Conversion

### Panel callbacks and control handlers

#### `Button_adjust_all_to_fp`

```igor
Function Button_adjust_all_to_fp(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `adjust_all_to_fp()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `All`, `All`
- Source: `Pierre's Template.ipf:583`

#### `Button_adjust_to_fp`

```igor
Function Button_adjust_to_fp(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `adjust_to_fp()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `Fix FP 1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Fix FP 1`, `Fix FP 1`
- Source: `Pierre's Template.ipf:578`

#### `Button_createbkgndremovedbkgnd`

```igor
Function Button_createbkgndremovedbkgnd(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `createbkgndremovedbkgnd()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `ave background`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ave background`, `ave background`
- Source: `Pierre's Template.ipf:541`

#### `Button_createresultremovedbkgnd`

```igor
Function Button_createresultremovedbkgnd(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `createresultremovedbkgnd()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `ave result`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ave result`
- Source: `Pierre's Template.ipf:536`

#### `Button_EMDC_Showmulti`

```igor
Function Button_EMDC_Showmulti(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_Showmulti()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Display multi`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Display multi`
- Source: `Pierre's Template.ipf:11849`

#### `Button_Help_0_to_NaN`

```igor
Function Button_Help_0_to_NaN(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_0_to_NaN()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9473`

#### `Button_Help_2nd_derivative`

```igor
Function Button_Help_2nd_derivative(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_2nd_derivative()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9662`

#### `Button_Help_Append_pnts`

```igor
Function Button_Help_Append_pnts(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Append_pnts()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9590`

#### `Button_Help_bad_slices`

```igor
Function Button_Help_bad_slices(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `help_bad_slices()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9788`

#### `Button_Help_bkgndremoverAddpnt`

```igor
Function Button_Help_bkgndremoverAddpnt(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_bkgndremover_Add_pnt()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10028`

#### `Button_Help_bkgndremoverDelpnt`

```igor
Function Button_Help_bkgndremoverDelpnt(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_bkgndremover_Delete_pnt()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9572`

#### `Button_Help_bkgndremSavebackgnd`

```igor
Function Button_Help_bkgndremSavebackgnd(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_bkgndremover_Save_backgnd()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10037`

#### `Button_Help_bkgndremSaveresult`

```igor
Function Button_Help_bkgndremSaveresult(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_bkgndremover_Save_result()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9849`

#### `Button_Help_ch_spacing`

```igor
Function Button_Help_ch_spacing(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_channel_spacing()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9779`

#### `Button_Help_Change_stepsize`

```igor
Function Button_Help_Change_stepsize(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Change_stepsize()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10001`

#### `Button_Help_Cmbdata`

```igor
Function Button_Help_Cmbdata(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Cmbdata()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9858`

#### `Button_Help_Combine_all`

```igor
Function Button_Help_Combine_all(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `Help_Combine_all()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9464`

#### `Button_Help_Combine_krange`

```igor
Function Button_Help_Combine_krange(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `Help_Combine_krange()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9752`

#### `Button_Help_Combine_range`

```igor
Function Button_Help_Combine_range(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `Help_Combine_range()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9903`

#### `Button_Help_Combine_this_data`

```igor
Function Button_Help_Combine_this_data(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `Help_Combine_this_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9671`

#### `Button_help_convert_all_data`

```igor
Function Button_help_convert_all_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `help_convert_all_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9761`

#### `Button_Help_Cvt_data`

```igor
Function Button_Help_Cvt_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Cvt_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9689`

#### `Button_Help_Data`

```igor
Function Button_Help_Data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9608`

#### `Button_Help_Data_ploter`

```igor
Function Button_Help_Data_ploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Data_ploter()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9894`

#### `Button_Help_Data_type`

```igor
Function Button_Help_Data_type(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Data_type()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9446`

#### `Button_Help_Delete_all`

```igor
Function Button_Help_Delete_all(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `Help_Delete_all()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9500`

#### `Button_Help_Delete_last`

```igor
Function Button_Help_Delete_last(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `Help_Delete_last()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9509`

#### `Button_Help_EDC`

```igor
Function Button_Help_EDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_EDC()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9983`

#### `Button_Help_EDC_data`

```igor
Function Button_Help_EDC_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_EDC_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9734`

#### `Button_Help_Equiv_mat`

```igor
Function Button_Help_Equiv_mat(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Equiv_mat()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9491`

#### `Button_Help_Fermi_profile`

```igor
Function Button_Help_Fermi_profile(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Fermi_profile()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9876`

#### `Button_Help_Find_kF`

```igor
Function Button_Help_Find_kF(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Find_kF()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9885`

#### `Button_Help_Initialize`

```igor
Function Button_Help_Initialize(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Initialize()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9707`

#### `Button_Help_Keep_minus`

```igor
Function Button_Help_Keep_minus(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Keep_minus()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9626`

#### `Button_Help_Keep_plus`

```igor
Function Button_Help_Keep_plus(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Keep_plus()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9617`

#### `Button_Help_Ln_mat`

```igor
Function Button_Help_Ln_mat(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Ln_mat()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9527`

#### `Button_Help_Load`

```igor
Function Button_Help_Load(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Load()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9698`

#### `Button_Help_mat_exp_n`

```igor
Function Button_Help_mat_exp_n(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_mat_exp_n()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9536`

#### `Button_Help_MDC`

```igor
Function Button_Help_MDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_MDC()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9680`

#### `Button_Help_MDC_Data_ploter`

```igor
Function Button_Help_MDC_Data_ploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_MDC_Data_ploter()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9956`

#### `Button_Help_MDC_EDC`

```igor
Function Button_Help_MDC_EDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_MDC_EDC()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9797`

#### `Button_Help_MDCfit`

```igor
Function Button_Help_MDCfit(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `Help_MDCfit()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9938`

#### `Button_Help_NaN_to_0`

```igor
Function Button_Help_NaN_to_0(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_NaN_to_0()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9563`

#### `Button_Help_New_band`

```igor
Function Button_Help_New_band(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_New_band()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9644`

#### `Button_Help_NK`

```igor
Function Button_Help_NK(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_NK()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9947`

#### `Button_Help_Norm`

```igor
Function Button_Help_Norm(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `Help_Norm()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9831`

#### `Button_Help_Not_these_points`

```igor
Function Button_Help_Not_these_points(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Not_these_points()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10073`

#### `Button_Help_num_of_band`

```igor
Function Button_Help_num_of_band(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_num_of_band()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9929`

#### `Button_Help_Open_data`

```igor
Function Button_Help_Open_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Open_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9635`

#### `Button_Help_Open_select`

```igor
Function Button_Help_Open_select(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Open_select()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9554`

#### `Button_Help_Open_this_data`

```igor
Function Button_Help_Open_this_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Open_this_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9921`

#### `Button_Help_Plot_EDC`

```igor
Function Button_Help_Plot_EDC(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Plot_EDC()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9965`

#### `Button_Help_Plot_MDC`

```igor
Function Button_Help_Plot_MDC(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Plot_MDC()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9725`

#### `Button_Help_Plot_Point_reader`

```igor
Function Button_Help_Plot_Point_reader(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Help_Plot_Point_reader()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9599`

#### `Button_Help_Point_reader`

```igor
Function Button_Help_Point_reader(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Help_Point_reader()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`, `?`, `?`, `?`, `?`, `?`
- Source: `Pierre's Template.ipf:10010`

#### `Button_Help_Point_Reader_Sort`

```igor
Function Button_Help_Point_Reader_Sort(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Help_Point_Reader_Sort()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9974`

#### `Button_Help_PointreaderShowtbl`

```igor
Function Button_Help_PointreaderShowtbl(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Help_Point_reader_Show_table()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9455`

#### `Button_Help_raw_data`

```igor
Function Button_Help_raw_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_raw_data()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9419`

#### `Button_Help_Read_point`

```igor
Function Button_Help_Read_point(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Help_Read_point()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10064`

#### `Button_Help_RemovebkgndDploter`

```igor
Function Button_Help_RemovebkgndDploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Remove_background_Dploter()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10091`

#### `Button_Help_Rotate_image`

```igor
Function Button_Help_Rotate_image(ctrlName) : ButtonControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `Help_Rotate_image()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9428`

#### `Button_Help_SubtractEDCDploter`

```igor
Function Button_Help_SubtractEDCDploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Subtract_EDC_Dploter()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9653`

#### `Button_Help_Symetrize_EDC_mat`

```igor
Function Button_Help_Symetrize_EDC_mat(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `Help_Symetrize_EDC_mat()`.
- Notes: Panel callback/helper for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9743`

#### `Button_Help_tab_matrices`

```igor
Function Button_Help_tab_matrices(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_tab_matrices()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:10046`

#### `Button_Help_tab_Misc`

```igor
Function Button_Help_tab_Misc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_tab_Misc()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9482`

#### `Button_Help_tab_presentation`

```igor
Function Button_Help_tab_presentation(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_tab_presentation()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`
- Source: `Pierre's Template.ipf:9840`

#### `Button_help_tab_traces`

```igor
Function Button_help_tab_traces(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `help_tab_traces()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9770`

#### `Button_Help_Tools`

```igor
Function Button_Help_Tools(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Help_Tools()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10019`

#### `Button_HELPbackgroundremover`

```igor
Function Button_HELPbackgroundremover(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `HELP_background_remover()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`, `?`, `?`, `?`, `?`, `?`, `?`, `?`
- Source: `Pierre's Template.ipf:9806`

#### `Button_HelpBkgndremoverBkgndin`

```igor
Function Button_HelpBkgndremoverBkgndin(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Bkgndremover_Bkgnd_in()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:9912`

#### `Button_HelpMakeimageDataploter`

```igor
Function Button_HelpMakeimageDataploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Make_image_Data_ploter()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `?`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `?`
- Source: `Pierre's Template.ipf:10082`

#### `Button_image_0_down`

```igor
Function Button_image_0_down(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_0_down(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `0`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `0`, `0`
- Source: `Pierre's Template.ipf:12179`

#### `Button_image_0_up`

```igor
Function Button_image_0_up(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_0_up(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `0`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `0`, `0`
- Source: `Pierre's Template.ipf:12155`

#### `Button_image_auto_both`

```igor
Function Button_image_auto_both(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_auto_both(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Auto`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto`, `Auto`
- Source: `Pierre's Template.ipf:12106`

#### `Button_image_auto_down`

```igor
Function Button_image_auto_down(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_auto_down(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `A`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A`, `A`
- Source: `Pierre's Template.ipf:12131`

#### `Button_image_auto_up`

```igor
Function Button_image_auto_up(ctrlName) : ButtonControl
```
- Usage: call as `Button_image_auto_up(ctrlName)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `A`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A`, `A`
- Source: `Pierre's Template.ipf:12082`

#### `Button_list4bkgndremoveral`

```igor
Function Button_list4bkgndremoveral(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `list4bkgndremoveral1()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `elect from list`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `elect from list`, `elect from list`
- Source: `Pierre's Template.ipf:526`

#### `Button_makeEMDCtable`

```igor
Function Button_makeEMDCtable(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `makeEMDCtable()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `Table`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Table`
- Source: `Pierre's Template.ipf:11844`

#### `Button_selectlistbkgndremover`

```igor
Function Button_selectlistbkgndremover(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `selectlistbkgndremover1()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `elect from list`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `elect from list`, `elect from list`
- Source: `Pierre's Template.ipf:521`

#### `Button_zoom_forCsrslicer`

```igor
Function Button_zoom_forCsrslicer(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `zoom_forCsrslicer()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `Zoom`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Zoom`, `Zoom`, `Zoom`
- Source: `Pierre's Template.ipf:25249`

#### `ButtonCurv_mat_in`

```igor
Function ButtonCurv_mat_in(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Curv_mat_in()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `at in`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `at in`
- Source: `Pierre's Template.ipf:12346`

#### `ButtonCurv_mat_out`

```igor
Function ButtonCurv_mat_out(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Curv_mat_out()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `ave`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ave`
- Source: `Pierre's Template.ipf:12411`

#### `ButtonCurvature_deriv`

```igor
Function ButtonCurvature_deriv(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Curvature_deriv()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `Derivatives`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Derivatives`
- Source: `Pierre's Template.ipf:12432`

#### `ButtonProc`

```igor
Function ButtonProc(ctrlName) : ButtonControl
```
- Usage: call as `ButtonProc(ctrlName)`.
- Notes: Panel callback/helper for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `Table`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Table`
- Source: `Pierre's Template.ipf:36320`

#### `ButtonProc_2dCurv`

```igor
Function ButtonProc_2dCurv(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for Igor wave/matrix/cube data operation. Main internal calls: `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13855`

#### `ButtonProc_4`

```igor
Function ButtonProc_4(ctrlName) : ButtonControl //"Fix"
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1155`

#### `ButtonProc_add_data`

```igor
Function ButtonProc_add_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `add_data()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Cmb. data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cmb. data`
- Source: `Pierre's Template.ipf:1185`

#### `ButtonProc_add_NTP`

```igor
Function ButtonProc_add_NTP(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `increase_NTP_num_max()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Add_NTP_wave`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Add_NTP_wave`
- Source: `Pierre's Template.ipf:709`

#### `ButtonProc_add_this_data`

```igor
Function ButtonProc_add_this_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `add_this_data()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Combine this data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Combine this data`, `Combine this data`
- Source: `Pierre's Template.ipf:1190`

#### `ButtonProc_addpntbkgndremover`

```igor
Function ButtonProc_addpntbkgndremover(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `addpntbkgndremover()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Add pnt`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Add pnt`, `Add pnt`
- Source: `Pierre's Template.ipf:568`

#### `ButtonProc_allcuts`

```igor
Function ButtonProc_allcuts(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `allcuts()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `All cuts`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `All cuts`, `All cuts`
- Source: `Pierre's Template.ipf:804`

#### `ButtonProc_angmodeconverterfs`

```igor
Function ButtonProc_angmodeconverterfs(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `anglemode_converter_fs()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Angle Cvter FS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Angle Cvter FS`, `Angle Cvter FS`
- Source: `Pierre's Template.ipf:27090`

#### `ButtonProc_append_circles`

```igor
Function ButtonProc_append_circles(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `append_circles()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Append TB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Append TB`
- Source: `Pierre's Template.ipf:25785`

#### `ButtonProc_appendpnts`

```igor
Function ButtonProc_appendpnts(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Appendpnts()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Append pnts`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Append pnts`
- Source: `Pierre's Template.ipf:5487`

#### `ButtonProc_AVG_CirccenterCsr`

```igor
Function ButtonProc_AVG_CirccenterCsr(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `AVG_CirccenterCsr()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Center at cursor`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Center at cursor`
- Source: `Pierre's Template.ipf:12918`

#### `ButtonProc_AVG_namein`

```igor
Function ButtonProc_AVG_namein(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `AVG_wavein()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Image in`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Image in`
- Source: `Pierre's Template.ipf:12935`

#### `ButtonProc_backtomain`

```igor
Function ButtonProc_backtomain(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Data_Analysis_BNL()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:680`

#### `ButtonProc_bluetored`

```igor
Function ButtonProc_bluetored(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `bluetored()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:719`

#### `ButtonProc_calc_tbarea`

```igor
Function ButtonProc_calc_tbarea(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `calc_tbarea()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Calc. doping`, `Doping`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Calc. doping`, `Doping`, `Doping`, `Calc. doping`
- Source: `Pierre's Template.ipf:26229`

#### `ButtonProc_CEM2DToKZ`

```igor
Function ButtonProc_CEM2DToKZ(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Kz_predict2()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `CEM kz`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CEM kz`
- Source: `Miscellaneous_Codes.ipf:1557`

#### `ButtonProc_comb_krange`

```igor
Function ButtonProc_comb_krange(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `comb_krange()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Cb krange`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cb krange`, `Cb krange`
- Source: `Pierre's Template.ipf:934`

#### `ButtonProc_combine_range`

```igor
Function ButtonProc_combine_range(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `combine_range()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Cb range`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cb range`, `Cb range`
- Source: `Pierre's Template.ipf:924`

#### `ButtonProc_combineall`

```igor
Function ButtonProc_combineall(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `combineall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Cb all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cb all`, `Cb all`
- Source: `Pierre's Template.ipf:939`

#### `ButtonProc_Consoffset`

```igor
Function ButtonProc_Consoffset(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Consoffset()`.
- Notes: continuely tuning Y offset to any graph Panel control(s): `OffsetXY`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `OffsetXY`
- Source: `SmartDisplay.ipf:1361`

#### `ButtonProc_Consoffsetx`

```igor
Function ButtonProc_Consoffsetx(ctrlName) : ButtonControl
```
- Notes: continuely tuning X offset to any graph Usage: triggered as a wrapper that opens/runs `Consoffsetx()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `SmartDisplay.ipf:1465`

#### `ButtonProc_Constant`

```igor
Function ButtonProc_Constant(ctrlName) : ButtonControl  // "Const"
```
- Usage: triggered as a wrapper that opens/runs `ConstantOffset_n()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1160`

#### `ButtonProc_ConstOffset`

```igor
Function ButtonProc_ConstOffset(ctrlName) : ButtonControl//"Special"
```
- Usage: triggered as a wrapper that opens/runs `constantoffset_n()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `ConstOff`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ConstOff`
- Source: `Pierre's Template.ipf:1125`

#### `ButtonProc_ConvertAllData`

```igor
Function ButtonProc_ConvertAllData(ctrlName) : ButtonControl//"Convert All Data"
```
- Usage: triggered as a wrapper that opens/runs `ConvtAll()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Convert All Data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Convert All Data`
- Source: `Pierre's Template.ipf:17068`

#### `ButtonProc_ConvertSingleData`

```igor
Function ButtonProc_ConvertSingleData(ctrlName) : ButtonControl//"Convert Single Data"
```
- Usage: triggered as a wrapper that opens/runs `ConvtOne()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Cvt data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cvt data`, `Cvt data`
- Source: `Pierre's Template.ipf:17073`

#### `ButtonProc_copy_circles`

```igor
Function ButtonProc_copy_circles(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `copy_circles()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Copy TB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Copy TB`
- Source: `Pierre's Template.ipf:25774`

#### `ButtonProc_copy_circles2f`

```igor
Function ButtonProc_copy_circles2f(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `copy_circles_2f()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Copy TB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Copy TB`, `Copy TB`
- Source: `Pierre's Template.ipf:25779`

#### `ButtonProc_copymapactive`

```igor
Function ButtonProc_copymapactive(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `copymap()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `ave`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ave`
- Source: `Pierre's Template.ipf:36273`

#### `ButtonProc_Copytraces`

```igor
Function ButtonProc_Copytraces(ctrlName) : ButtonControl
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: triggered as a wrapper that opens/runs `Copytraces()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Copy all traces`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Copy all traces`, `Copy all traces`
- Source: `Pierre's Template.ipf:694`

#### `ButtonProc_createband`

```igor
Function ButtonProc_createband(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `showpointreader()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `New band`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `New band`
- Source: `Pierre's Template.ipf:5497`

#### `ButtonProc_curv_data`

```igor
Function ButtonProc_curv_data(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `Pierre's Template.ipf`. Main internal calls: `curv_data()`, `refresh_disp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13875`

#### `ButtonProc_curv_Zhang`

```igor
Function ButtonProc_curv_Zhang(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `Peng's Curvature`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`, `curv_data()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Peng's Curvature`, `Peng's Curvature`
- Source: `Pierre's Template.ipf:13881`

#### `ButtonProc_CVT2EK`

```igor
Function ButtonProc_CVT2EK(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CVT2EK_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `θ to k`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `θ to k`, `θ to k`
- Source: `Pierre's Template.ipf:1025`

#### `ButtonProc_CVT2EK_exact1cut`

```igor
Function ButtonProc_CVT2EK_exact1cut(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CVT2EK_exact1cut()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `θ2k`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `θ2k`, `θ2k`
- Source: `Pierre's Template.ipf:724`

#### `ButtonProc_CVT2K_vs_hv`

```igor
Function ButtonProc_CVT2K_vs_hv(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CVT2K_vs_hv()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Map hv`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Map hv`, `Map hv`
- Source: `Pierre's Template.ipf:35650`

#### `ButtonProc_CVT2K_vs_kz`

```igor
Function ButtonProc_CVT2K_vs_kz(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CVT2K_vs_kz()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Map kz`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Map kz`, `Map kz`
- Source: `Pierre's Template.ipf:35655`

#### `ButtonProc_dataploter`

```igor
Function ButtonProc_dataploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `dataploter()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Ploter`, `Data ploter`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ploter`, `Data ploter`
- Source: `Pierre's Template.ipf:834`

#### `ButtonProc_deleteallpnts`

```igor
Function ButtonProc_deleteallpnts(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `Deleteallpnts()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Delete all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Delete all`
- Source: `Pierre's Template.ipf:5508`

#### `ButtonProc_deletelastpnt`

```igor
Function ButtonProc_deletelastpnt(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `Deletelastpnt()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Delete last`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Delete last`
- Source: `Pierre's Template.ipf:5503`

#### `ButtonProc_delpntbkgndremover`

```igor
Function ButtonProc_delpntbkgndremover(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `delpntbkgndremover()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Delete pnt`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Delete pnt`, `Delete pnt`
- Source: `Pierre's Template.ipf:573`

#### `ButtonProc_deriv_1chunkcut`

```igor
Function ButtonProc_deriv_1chunkcut(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `deriv_1chunkcut()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Deriv1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Deriv1`, `Deriv1`
- Source: `Pierre's Template.ipf:27080`

#### `ButtonProc_deriv_allchunkcut`

```igor
Function ButtonProc_deriv_allchunkcut(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `deriv_allchunkcut()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Deriv all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Deriv all`, `Deriv all`
- Source: `Pierre's Template.ipf:27085`

#### `ButtonProc_derivmat`

```igor
Function ButtonProc_derivmat(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `derivmat()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `2nd derivative`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2nd derivative`, `2nd derivative`
- Source: `Pierre's Template.ipf:955`

#### `ButtonProc_disp2Dcurv`

```igor
Function ButtonProc_disp2Dcurv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for Igor wave/matrix/cube data operation. Main internal calls: `idisp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13804`

#### `ButtonProc_disp2DDeriv`

```igor
Function ButtonProc_disp2DDeriv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for Igor wave/matrix/cube data operation. Main internal calls: `idisp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13826`

#### `ButtonProc_dispData`

```igor
Function ButtonProc_dispData(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `Pierre's Template.ipf`. Main internal calls: `disp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13783`

#### `ButtonProc_dispEDCcurv`

```igor
Function ButtonProc_dispEDCcurv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `idisp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13790`

#### `ButtonProc_dispEDCDeriv`

```igor
Function ButtonProc_dispEDCDeriv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `idisp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13812`

#### `ButtonProc_dispMDCcurv`

```igor
Function ButtonProc_dispMDCcurv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `idisp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13797`

#### `ButtonProc_dispMDCDeriv`

```igor
Function ButtonProc_dispMDCDeriv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `idisp()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13819`

#### `ButtonProc_dividematrixbyfermi`

```igor
Function ButtonProc_dividematrixbyfermi(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `dividematrixbyfermi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `Divide by FD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Divide by FD`, `Divide by FD`
- Source: `Pierre's Template.ipf:1195`

#### `ButtonProc_dividewavebyfermi`

```igor
Function ButtonProc_dividewavebyfermi(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `dividewavebyfermi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `Divide by FD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Divide by FD`, `Divide by FD`
- Source: `Pierre's Template.ipf:1200`

#### `ButtonProc_doping_hexagon`

```igor
Function ButtonProc_doping_hexagon(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Calc. doping`, `Doping`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Calc. doping`, `Doping`
- Source: `Pierre's Template.ipf:33298`

#### `ButtonProc_dpemdc`

```igor
Function ButtonProc_dpemdc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `duplicateemdcc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Dupli`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Dupli`
- Source: `Pierre's Template.ipf:11799`

#### `ButtonProc_edc_pi`

```igor
Function ButtonProc_edc_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `edc_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `E`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `E`
- Source: `Pierre's Template.ipf:1055`

#### `ButtonProc_edc_to_zero_pi`

```igor
Function ButtonProc_edc_to_zero_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `edc_to_zero_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1075`

#### `ButtonProc_EMDC`

```igor
Function ButtonProc_EMDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_ini()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `EMDC`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `EMDC`, `EMDC`
- Source: `Pierre's Template.ipf:11423`

#### `ButtonProc_EMDC_insertmat`

```igor
Function ButtonProc_EMDC_insertmat(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_insertmat()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Insert Matrix`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Insert Matrix`
- Source: `Pierre's Template.ipf:11433`

#### `ButtonProc_EMDC_Showwave`

```igor
Function ButtonProc_EMDC_Showwave(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `EMDC_Showwave()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `how wave`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `how wave`
- Source: `Pierre's Template.ipf:11438`

#### `ButtonProc_FFTTwistanglemap`

```igor
Function ButtonProc_FFTTwistanglemap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `FFTTwistanglemap()`.
- Notes: Triangle lattice map the twist angle Moving window technique Purpose: performs Fourier, filtering, phase, or lock-in processing. Panel control(s): `TBG Twist angle Map`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `TBG Twist angle Map`
- Source: `Triangle Lattice_Graphene_like.ipf:19`

#### `ButtonProc_find_kf_pi`

```igor
Function ButtonProc_find_kf_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `find_kf_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `find KF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `find KF`
- Source: `Pierre's Template.ipf:1050`

#### `ButtonProc_finddispersion_min`

```igor
Function ButtonProc_finddispersion_min(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `finddispersion_min()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `V-`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `V-`, `V-`
- Source: `Pierre's Template.ipf:754`

#### `ButtonProc_finddispersion_plus`

```igor
Function ButtonProc_finddispersion_plus(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `finddispersion_plus()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `V+`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `V+`, `V+`
- Source: `Pierre's Template.ipf:764`

#### `ButtonProc_finddispersionHD_min`

```igor
Function ButtonProc_finddispersionHD_min(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `finddispersionHD_min()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `H-`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `H-`, `H-`
- Source: `Pierre's Template.ipf:759`

#### `ButtonProc_finddispersionHDplus`

```igor
Function ButtonProc_finddispersionHDplus(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `finddispersionHD_plus()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `H+`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `H+`, `H+`
- Source: `Pierre's Template.ipf:769`

#### `ButtonProc_findEres`

```igor
Function ButtonProc_findEres(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `findEres()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `E-Resolution`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `E-Resolution`, `E-Resolution`
- Source: `Pierre's Template.ipf:8366`

#### `ButtonProc_FitAu`

```igor
Function ButtonProc_FitAu(ctrlName) : ButtonControl//" Fit Au Edge"
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `Fit_Au()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:2083`

#### `ButtonProc_FitEDC`

```igor
Function ButtonProc_FitEDC(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `FitEDC()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1090`

#### `ButtonProc_FlipRGBimage`

```igor
Function ButtonProc_FlipRGBimage(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Flip_digimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Flip`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Flip`, `Flip`
- Source: `Pierre's Template.ipf:647`

#### `ButtonProc_Generate_chunklist`

```igor
Function ButtonProc_Generate_chunklist(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `Generate_chunklist()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Mk List`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mk List`, `Mk List`
- Source: `Pierre's Template.ipf:27075`

#### `ButtonProc_get_fermi_profile`

```igor
Function ButtonProc_get_fermi_profile(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `get_fermi_profile()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Fermi P`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Fermi P`, `Fermi P`
- Source: `Pierre's Template.ipf:814`

#### `ButtonProc_Get_ledge_kf`

```igor
Function ButtonProc_Get_ledge_kf(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Get_ledge_kf()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Leading Edge KF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Leading Edge KF`
- Source: `Pierre's Template.ipf:27050`

#### `ButtonProc_give_area`

```igor
Function ButtonProc_give_area(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `give_area()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Give area`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Give area`, `Give area`
- Source: `Pierre's Template.ipf:789`

#### `ButtonProc_image_fs`

```igor
Function ButtonProc_image_fs(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ini_image_fs()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Image FS`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Image FS`
- Source: `Pierre's Template.ipf:26147`

#### `ButtonProc_image_fs2f`

```igor
Function ButtonProc_image_fs2f(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ini_image_fs2f()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Image FS`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Image FS`, `Image FS`
- Source: `Pierre's Template.ipf:26159`

#### `ButtonProc_image_fsHEX`

```igor
Function ButtonProc_image_fsHEX(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ini_image_fsHex()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Panel control(s): `Image FS`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Image FS`, `Image FS`
- Source: `Pierre's Template.ipf:33608`

#### `ButtonProc_keep_negative`

```igor
Function ButtonProc_keep_negative(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `keep_negative()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Keep -`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Keep -`, `Keep -`
- Source: `Pierre's Template.ipf:1170`

#### `ButtonProc_keep_positive`

```igor
Function ButtonProc_keep_positive(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `keep_positive()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Keep +`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Keep +`, `Keep +`
- Source: `Pierre's Template.ipf:1175`

#### `ButtonProc_kly_Kzmap`

```igor
Function ButtonProc_kly_Kzmap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `kly_Kzmap()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `map kz`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `map kz`
- Source: `Miscellaneous_Codes.ipf:680`

#### `ButtonProc_kly_mapping`

```igor
Function ButtonProc_kly_mapping(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `kly_mapping()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `map kxky`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `map kxky`
- Source: `Miscellaneous_Codes.ipf:660`

#### `ButtonProc_Kz_predict`

```igor
Function ButtonProc_Kz_predict(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Kz_predict()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `kz/V before`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `kz/V before`
- Source: `Miscellaneous_Codes.ipf:670`

#### `ButtonProc_Kz_predict2`

```igor
Function ButtonProc_Kz_predict2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Kz_predict2()`.
- Notes: Please use this procedure after finished "map hv""-->mat3d" Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `kz/V after`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `kz/V after`
- Source: `Miscellaneous_Codes.ipf:1552`

#### `ButtonProc_Kzprojection`

```igor
Function ButtonProc_Kzprojection(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `Project kz`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mpc()`, `s0()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Project kz`
- Source: `Models.ipf:2964`

#### `ButtonProc_legend_BE_vs_angle`

```igor
Function ButtonProc_legend_BE_vs_angle(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `BE vs θ`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BE vs θ`, `BE vs θ`
- Source: `Pierre's Template.ipf:13934`

#### `ButtonProc_legend_int_vs_BE`

```igor
Function ButtonProc_legend_int_vs_BE(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Peng Zhang curvature end Panel control(s): `Int. vs BE`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Int. vs BE`, `Int. vs BE`
- Source: `Pierre's Template.ipf:13918`

#### `ButtonProc_legend_int_vs_KE`

```igor
Function ButtonProc_legend_int_vs_KE(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `Int. vs KE`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Int. vs KE`, `Int. vs KE`
- Source: `Pierre's Template.ipf:13926`

#### `ButtonProc_legend_KE_vs_angle`

```igor
Function ButtonProc_legend_KE_vs_angle(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `KE vs θ`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `KE vs θ`, `KE vs θ`
- Source: `Pierre's Template.ipf:13942`

#### `ButtonProc_legendBEvsmomang`

```igor
Function ButtonProc_legendBEvsmomang(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `BE vs Momentum (√Ö)`, `BE vs Momentum (Å)`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BE vs Momentum (√Ö)`, `BE vs Momentum (Å)`
- Source: `Pierre's Template.ipf:13958`

#### `ButtonProc_legendBEvsmompia`

```igor
Function ButtonProc_legendBEvsmompia(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `BE vs Momentum (ώ/a)`, `BE vs Momentum (π/a)`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BE vs Momentum (ώ/a)`, `BE vs Momentum (π/a)`
- Source: `Pierre's Template.ipf:13950`

#### `ButtonProc_LiftMdc`

```igor
Function ButtonProc_LiftMdc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `LiftMdcAtEf()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1115`

#### `ButtonProc_ln_mat_pi`

```igor
Function ButtonProc_ln_mat_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ln_mat_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Ln(mat)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ln(mat)`, `Ln(mat)`
- Source: `Pierre's Template.ipf:990`

#### `ButtonProc_Load3D`

```igor
Function ButtonProc_Load3D(ctrlName) : ButtonControl
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `Load3D1()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Load3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Load3D`
- Source: `Pierre's Template.ipf:36178`

#### `ButtonProc_Make_allchunkcuts`

```igor
Function ButtonProc_Make_allchunkcuts(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `Make_allchunkcuts()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `All Ckts`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `All Ckts`, `All Ckts`
- Source: `Pierre's Template.ipf:27070`

#### `ButtonProc_Make_onechunkcut`

```igor
Function ButtonProc_Make_onechunkcut(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `Make_onechunkcut()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `1chunkcut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `1chunkcut`, `1chunkcut`
- Source: `Pierre's Template.ipf:27065`

#### `ButtonProc_make_table_hv_kz`

```igor
Function ButtonProc_make_table_hv_kz(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `make_table_hv_kz()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `Table`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Table`, `Table`
- Source: `Pierre's Template.ipf:35645`

#### `ButtonProc_Makecutimage`

```igor
Function ButtonProc_Makecutimage(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `Makecutimage_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Cut Image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cut Image`, `Cut Image`
- Source: `Pierre's Template.ipf:26990`

#### `ButtonProc_MakeEDC`

```igor
Function ButtonProc_MakeEDC(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `MakeEDC_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `EDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `EDC`, `EDC`
- Source: `Pierre's Template.ipf:1085`

#### `ButtonProc_makeFeSC_normalFSc`

```igor
Function ButtonProc_makeFeSC_normalFSc(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Make FS`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make FS`
- Source: `Models.ipf:11444`

#### `ButtonProc_Makenewmap`

```igor
Function ButtonProc_Makenewmap(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `Makenewmap()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `As`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `As`
- Source: `Pierre's Template.ipf:36263`

#### `ButtonProc_making_fs`

```igor
Function ButtonProc_making_fs(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Table_Notthesepoints()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Making FS`. Code behavior: reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Making FS`
- Source: `Pierre's Template.ipf:25802`

#### `ButtonProc_making_fs2f`

```igor
Function ButtonProc_making_fs2f(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Table_Notthesepoints()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Making FS`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Making FS`, `Making FS`
- Source: `Pierre's Template.ipf:25817`

#### `ButtonProc_making_fsH`

```igor
Function ButtonProc_making_fsH(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Table_Notthesepoints()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Panel control(s): `Making FS`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Making FS`, `Making FS`
- Source: `Pierre's Template.ipf:25790`

#### `ButtonProc_mat3dfsploter`

```igor
Function ButtonProc_mat3dfsploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `initmat3d()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `-->Plot`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `-->Plot`, `-->Plot`, `-->Plot`, `-->Plot`
- Source: `Pierre's Template.ipf:843`

#### `ButtonProc_mat3dHDfsploter`

```igor
Function ButtonProc_mat3dHDfsploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `initmat3dHD()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `-->Plot`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `-->Plot`, `-->Plot`, `-->Plot`, `-->Plot`
- Source: `Pierre's Template.ipf:865`

#### `ButtonProc_mat3dkfsploter`

```igor
Function ButtonProc_mat3dkfsploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `initmat3dk()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `->Plot`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `->Plot`, `->Plot`
- Source: `Pierre's Template.ipf:876`

#### `ButtonProc_mat3dkHDfsploter`

```igor
Function ButtonProc_mat3dkHDfsploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `initmat3dkHD()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `->Plot`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `->Plot`, `->Plot`
- Source: `Pierre's Template.ipf:898`

#### `ButtonProc_mat3dkVDfsploter`

```igor
Function ButtonProc_mat3dkVDfsploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `initmat3dkVD()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `->Plot`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `->Plot`, `->Plot`
- Source: `Pierre's Template.ipf:887`

#### `ButtonProc_mat3dVDfsploter`

```igor
Function ButtonProc_mat3dVDfsploter(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `initmat3dVD()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `-->Plot`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `-->Plot`, `-->Plot`, `-->Plot`, `-->Plot`
- Source: `Pierre's Template.ipf:854`

#### `ButtonProc_mat_n_pi`

```igor
Function ButtonProc_mat_n_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mat_n_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `(mat)^n`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `(mat)^n`, `(mat)^n`
- Source: `Pierre's Template.ipf:975`

#### `ButtonProc_MatNkPlot`

```igor
Function ButtonProc_MatNkPlot(ctrlName) : ButtonControl//"Plot N(k)"
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `MatNKPlot()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `NK`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `NK`, `NK`
- Source: `Pierre's Template.ipf:1150`

#### `ButtonProc_MatNorm`

```igor
Function ButtonProc_MatNorm(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `MatNorm()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Norm`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Norm`, `Norm`
- Source: `Pierre's Template.ipf:1105`

#### `ButtonProc_mdc_pi`

```igor
Function ButtonProc_mdc_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `mdc_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `M`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `M`
- Source: `Pierre's Template.ipf:1035`

#### `ButtonProc_MDCfit`

```igor
Function ButtonProc_MDCfit(ctrlName) : ButtonControl//"fit MDC"
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `MDCfit()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1130`

#### `ButtonProc_mdcfit_pi`

```igor
Function ButtonProc_mdcfit_pi(ctrlName) : ButtonControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: triggered as a wrapper that opens/runs `mdcfit_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `MDCfit`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MDCfit`
- Source: `Pierre's Template.ipf:1045`

#### `ButtonProc_Mkbad_slices`

```igor
Function ButtonProc_Mkbad_slices(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Mkbad_slices()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Bad slices`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Bad slices`
- Source: `Pierre's Template.ipf:1030`

#### `ButtonProc_MkMDC`

```igor
Function ButtonProc_MkMDC(ctrlName) : ButtonControl//"Full MDC"
```
- Usage: triggered as a wrapper that opens/runs `MakeMDC_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `MDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MDC`, `MDC`
- Source: `Pierre's Template.ipf:1135`

#### `ButtonProc_Multiply`

```igor
Function ButtonProc_Multiply(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Multiply()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1110`

#### `ButtonProc_nan0`

```igor
Function ButtonProc_nan0(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Nan0()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `NaN to 0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `NaN to 0`, `NaN to 0`
- Source: `Pierre's Template.ipf:980`

#### `ButtonProc_new_eres_pi`

```igor
Function ButtonProc_new_eres_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `new_eres_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `E-comb`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `E-comb`, `E-comb`
- Source: `Pierre's Template.ipf:960`

#### `ButtonProc_new_kres_pi`

```igor
Function ButtonProc_new_kres_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `new_kres_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `M-comb`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `M-comb`, `M-comb`
- Source: `Pierre's Template.ipf:965`

#### `ButtonProc_new_step`

```igor
Function ButtonProc_new_step(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `new_stepsize()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `New-stepsize`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `New-stepsize`, `New-stepsize`
- Source: `Pierre's Template.ipf:1180`

#### `ButtonProc_norm_high_e_pi`

```igor
Function ButtonProc_norm_high_e_pi(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `norm_high_e_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1015`

#### `ButtonProc_norm_range`

```igor
Function ButtonProc_norm_range(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `norm_range()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:914`

#### `ButtonProc_not_in_fct`

```igor
Function ButtonProc_not_in_fct(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `not_in_fct()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:714`

#### `ButtonProc_open_data_pi`

```igor
Function ButtonProc_open_data_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `open_data_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Open data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Open data`
- Source: `Pierre's Template.ipf:1065`

#### `ButtonProc_open_onedata_pi`

```igor
Function ButtonProc_open_onedata_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `open_onedata_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Open this data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Open this data`, `Open this data`
- Source: `Pierre's Template.ipf:1070`

#### `ButtonProc_open_select`

```igor
Function ButtonProc_open_select(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `open_select()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Open select`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Open select`
- Source: `Pierre's Template.ipf:1005`

#### `ButtonProc_openbkgndremover`

```igor
Function ButtonProc_openbkgndremover(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `background_remover()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Panel control(s): `ackground remover`. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ackground remover`, `ackground remover`
- Source: `Pierre's Template.ipf:552`

#### `ButtonProc_phi_cst_pi`

```igor
Function ButtonProc_phi_cst_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `phi_cst_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:27060`

#### `ButtonProc_plot_edc_pi`

```igor
Function ButtonProc_plot_edc_pi(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_edc_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Plot`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot`
- Source: `Pierre's Template.ipf:1060`

#### `ButtonProc_plot_kf_edcs`

```igor
Function ButtonProc_plot_kf_edcs(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_kf_edcs()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Plot kf EDCs`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot kf EDCs`
- Source: `Pierre's Template.ipf:784`

#### `ButtonProc_plot_mdc_pi`

```igor
Function ButtonProc_plot_mdc_pi(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_mdc_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Plot`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot`
- Source: `Pierre's Template.ipf:1040`

#### `ButtonProc_plot_predicttbGXMG`

```igor
Function ButtonProc_plot_predicttbGXMG(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_predicttbGXMG()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Plot GXMG`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot GXMG`
- Source: `Pierre's Template.ipf:26224`

#### `ButtonProc_plot_tbGXMG`

```igor
Function ButtonProc_plot_tbGXMG(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_tbGXMG()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Plot GXMG`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot GXMG`, `Plot GXMG`
- Source: `Pierre's Template.ipf:26214`

#### `ButtonProc_plot_tbGXMGMprimeX2f`

```igor
Function ButtonProc_plot_tbGXMGMprimeX2f(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_tbGXMGMprimeX_2f()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Plot GXMGM'X`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot GXMGM'X`, `Plot GXMGM'X`
- Source: `Pierre's Template.ipf:26219`

#### `ButtonProc_plot_vb_pi`

```igor
Function ButtonProc_plot_vb_pi(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plot_vb_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:909`

#### `ButtonProc_plotpnts`

```igor
Function ButtonProc_plotpnts(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Plotpnts()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Plot`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plot`
- Source: `Pierre's Template.ipf:5482`

#### `ButtonProc_PReader_draw_live`

```igor
Function ButtonProc_PReader_draw_live(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `PReader_draw_live()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Draw continous`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Draw continous`
- Source: `Pierre's Template.ipf:516`

#### `ButtonProc_PReader_edit_mode`

```igor
Function ButtonProc_PReader_edit_mode(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `PReader_switch_edit_mode()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Edit`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Edit`
- Source: `Pierre's Template.ipf:506`

#### `ButtonProc_PReader_normal_mode`

```igor
Function ButtonProc_PReader_normal_mode(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `PReader_normal_mode()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Normal`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Normal`
- Source: `Pierre's Template.ipf:511`

#### `ButtonProc_predict_cuts`

```igor
Function ButtonProc_predict_cuts(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `ini_predict_cuts()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Predict cuts`. Code behavior: reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Predict cuts`
- Source: `Pierre's Template.ipf:25934`

#### `ButtonProc_predictcalc_tbarea`

```igor
Function ButtonProc_predictcalc_tbarea(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `predictcalc_tbarea()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Calc. doping`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Calc. doping`
- Source: `Pierre's Template.ipf:26234`

#### `ButtonProc_put_fermi_edc`

```igor
Function ButtonProc_put_fermi_edc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `put_fermi_edc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `EF line (EDC)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `EF line (EDC)`, `EF line (EDC)`
- Source: `Pierre's Template.ipf:744`

#### `ButtonProc_put_fermi_image`

```igor
Function ButtonProc_put_fermi_image(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `put_fermi_image()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `EF line (image)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `EF line (image)`, `EF line (image)`
- Source: `Pierre's Template.ipf:749`

#### `ButtonProc_readcsr`

```igor
Function ButtonProc_readcsr(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: triggered as a wrapper that opens/runs `Readcsr()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Read point`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Read point`
- Source: `Pierre's Template.ipf:5477`

#### `ButtonProc_Remove_data_EDC`

```igor
Function ButtonProc_Remove_data_EDC(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `Remove_data_EDC()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Subtract EDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Subtract EDC`, `Subtract EDC`
- Source: `Pierre's Template.ipf:739`

#### `ButtonProc_renormcut_k`

```igor
Function ButtonProc_renormcut_k(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `renormcut_k()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Renormcut_k`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Renormcut_k`
- Source: `Pierre's Template.ipf:970`

#### `ButtonProc_return_phi_theta`

```igor
Function ButtonProc_return_phi_theta(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `return_phi_theta()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Pt at new hv`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Pt at new hv`, `Pt at new hv`, `Pt at new hv`
- Source: `Pierre's Template.ipf:1243`

#### `ButtonProc_return_phi_theta1`

```igor
Function ButtonProc_return_phi_theta1(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `return_phi_theta1()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Pt at new hv`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Pt at new hv`, `Pt at new hv`, `Pt at new hv`
- Source: `Pierre's Template.ipf:1248`

#### `ButtonProc_RGBimagetoIntensity`

```igor
Function ButtonProc_RGBimagetoIntensity(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Intensity_digimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `RGB2Int`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `RGB2Int`, `RGB2Int`
- Source: `Pierre's Template.ipf:657`

#### `ButtonProc_RGBimagetoIntensity2`

```igor
Function ButtonProc_RGBimagetoIntensity2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Intensity_digimage2()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Extract Image Data From Paper`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Extract Image Data From Paper`
- Source: `Pierre's Template.ipf:6546`

#### `ButtonProc_RGBimagetoRGB`

```igor
Function ButtonProc_RGBimagetoRGB(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `color_digimage()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Image2RGB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Image2RGB`, `Image2RGB`
- Source: `Pierre's Template.ipf:652`

#### `ButtonProc_Rm_simplebgnd_data`

```igor
Function ButtonProc_Rm_simplebgnd_data(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Remove_simplebgnd_data()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Remove backgrnd`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Remove backgrnd`, `Remove backgrnd`
- Source: `Pierre's Template.ipf:824`

#### `ButtonProc_Rm_simplebgnd_edc`

```igor
Function ButtonProc_Rm_simplebgnd_edc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Remove_simplebgnd_edc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `EDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `EDC`, `EDC`
- Source: `Pierre's Template.ipf:819`

#### `ButtonProc_rot2d_pi`

```igor
Function ButtonProc_rot2d_pi(ctrlName) : ButtonControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `rot2d_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Panel control(s): `Rotate image`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rotate image`, `Rotate image`
- Source: `Pierre's Template.ipf:919`

#### `ButtonProc_rot_vector`

```igor
Function ButtonProc_rot_vector(ctrlName) : ButtonControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `rot_vector()`.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotate`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rotate`, `Rotate`
- Source: `Pierre's Template.ipf:1165`

#### `ButtonProc_Save3D`

```igor
Function ButtonProc_Save3D(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: triggered as a wrapper that opens/runs `Save3D()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `ave3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ave3D`
- Source: `Pierre's Template.ipf:36173`

#### `ButtonProc_selectcut_pi`

```igor
Function ButtonProc_selectcut_pi(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `selectcut_pi()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:27055`

#### `ButtonProc_selectlistEMDC`

```igor
Function ButtonProc_selectlistEMDC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `selectlistEMDC()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `From list`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `From list`
- Source: `Pierre's Template.ipf:11443`

#### `ButtonProc_selectlistmap`

```igor
Function ButtonProc_selectlistmap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `selectlistmap1()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Choose`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Choose`
- Source: `Pierre's Template.ipf:36268`

#### `ButtonProc_shift`

```igor
Function ButtonProc_shift(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `hshift()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Offset`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Offset`
- Source: `Miscellaneous_Codes.ipf:8230`

#### `ButtonProc_shiftedc`

```igor
Function ButtonProc_shiftedc(ctrlName) : ButtonControl//"Combine"
```
- Usage: triggered as a wrapper that opens/runs `shiftedc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1145`

#### `ButtonProc_showmain`

```igor
Function ButtonProc_showmain(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Data_Analysis_BNL()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Main Panel`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Main Panel`, `Main Panel`, `Main Panel`, `Main Panel`, `Main Panel`, `Main Panel`, `Main Panel`, `Main Panel`
- Source: `Pierre's Template.ipf:638`

#### `ButtonProc_showpresentation`

```igor
Function ButtonProc_showpresentation(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Presentation_Panel()`.
- Notes: Panel button callback for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:629`

#### `ButtonProc_showUpdate`

```igor
Function ButtonProc_showUpdate(ctrlName) : ButtonControl//Update
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `showupdate()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:1140`

#### `ButtonProc_sortpnts`

```igor
Function ButtonProc_sortpnts(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Sortpnts()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Sort`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sort`
- Source: `Pierre's Template.ipf:5492`

#### `ButtonProc_sym_edc_matrix`

```igor
Function ButtonProc_sym_edc_matrix(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `sym_edc_matrix_far()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Symetrize EDC mat`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Symetrize EDC mat`, `Symetrize EDC mat`
- Source: `Pierre's Template.ipf:1205`

#### `ButtonProc_sym_kf_edcs`

```igor
Function ButtonProc_sym_kf_edcs(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `sym_kf_edcs()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Sym kf EDCs`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sym kf EDCs`
- Source: `Pierre's Template.ipf:779`

#### `ButtonProc_sym_one_edc`

```igor
Function ButtonProc_sym_one_edc(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `sym_one_edc()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Sym EDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sym EDC`, `Sym EDC`
- Source: `Pierre's Template.ipf:809`

#### `ButtonProc_symmat3dhv`

```igor
Function ButtonProc_symmat3dhv(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `symmat3dhv()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Sym hv`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sym hv`, `Sym hv`
- Source: `Pierre's Template.ipf:33882`

#### `ButtonProc_trans2equiv_mat`

```igor
Function ButtonProc_trans2equiv_mat(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `transform2equiv_mat()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Equiv. mat`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Equiv. mat`, `Equiv. mat`
- Source: `Pierre's Template.ipf:799`

#### `ButtonProc_twistanglemap`

```igor
Function ButtonProc_twistanglemap(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `twistanglemap_3qave()`.
- Notes: Main Function Panel control(s): `Go twist angle map`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go twist angle map`
- Source: `Triangle Lattice_Graphene_like.ipf:248`

#### `ButtonProc_update_image_fs`

```igor
Function ButtonProc_update_image_fs(ctrlName) : ButtonControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `update_image_fs()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Update`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Update`, `Update`, `Update`
- Source: `Pierre's Template.ipf:26209`

#### `ButtonProc_update_making_fs`

```igor
Function ButtonProc_update_making_fs(ctrlName) : ButtonControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `update_making_fs()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Update points`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Update points`
- Source: `Pierre's Template.ipf:26171`

#### `ButtonProc_update_making_fs2f`

```igor
Function ButtonProc_update_making_fs2f(ctrlName) : ButtonControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `update_making_fs2f()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Update points`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Update points`
- Source: `Pierre's Template.ipf:26176`

#### `ButtonProc_update_making_fsH`

```igor
Function ButtonProc_update_making_fsH(ctrlName) : ButtonControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `update_making_fsH()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Update points`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Update points`
- Source: `Pierre's Template.ipf:26181`

#### `ButtonProc_zeronan`

```igor
Function ButtonProc_zeronan(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ZeroNaN()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `0 to NaN`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `0 to NaN`, `0 to NaN`
- Source: `Pierre's Template.ipf:985`

#### `ButtonProcAVG_Showres`

```igor
Function ButtonProcAVG_Showres(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `AVG_showtable()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Show results`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show results`
- Source: `Pierre's Template.ipf:12973`

#### `ButtonProcAVG_tool`

```igor
Function ButtonProcAVG_tool(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `AVG_ini()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `AVG_tool`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WinType()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `AVG_tool`, `AVG_tool`
- Source: `Pierre's Template.ipf:12776`

#### `ButtonProcCutNaNedges`

```igor
Function ButtonProcCutNaNedges(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CutNaNedges()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `CutNaNedges`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CutNaNedges`, `CutNaNedges`
- Source: `Pierre's Template.ipf:2275`

#### `Buttonshowimageforslicer`

```igor
Function Buttonshowimageforslicer(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Graph_imageforslicer()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Show image`, `Show Image`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show image`, `Show Image`, `Show Image`, `Show Image`
- Source: `Pierre's Template.ipf:27167`

#### `EF_SetVarProc`

```igor
Function EF_SetVarProc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: call as `EF_SetVarProc(ctrlName, varNum, varStr, varName)`.
- Notes: Panel callback/helper for Igor wave/matrix/cube data operation. Panel control(s): `EF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `EF`
- Source: `Pierre's Template.ipf:36681`

#### `PopMenuProc`

```igor
Function PopMenuProc(ctrlName,popNum,popStr) : PopupMenuControl   //type of data menu
```
- Usage: call as `PopMenuProc(ctrlName, popNum, popStr)`.
- Notes: Panel callback/helper in `Pierre's Template.ipf`. Panel control(s): `Data_type`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Data_type`, `Data_type`
- Source: `Pierre's Template.ipf:588`

#### `PopMenuProc_1`

```igor
Function PopMenuProc_1(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `edccolorset`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `edccolorset`, `edccolorset`
- Source: `Pierre's Template.ipf:605`

#### `PopMenuProc_2`

```igor
Function PopMenuProc_2(ctrlName,popNum,popStr) : PopupMenuControl  //inverse the color
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Panel control(s): `inversecoloredc`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `inversecoloredc`, `inversecoloredc`
- Source: `Pierre's Template.ipf:597`

#### `PopMenuProc_3`

```igor
Function PopMenuProc_3(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback in `Pierre's Template.ipf`. Panel control(s): `popup0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup0`
- Source: `Pierre's Template.ipf:25766`

#### `PopMenuProc_4`

```igor
Function PopMenuProc_4(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: triggered as a wrapper that opens/runs `Set_Hline_slice_control()`.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Panel control(s): `buttonVvsH`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `WIntype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `buttonVvsH`
- Source: `Pierre's Template.ipf:27469`

#### `PopMenuProc_6`

```igor
Function PopMenuProc_6(ctrlName,popNum,popStr) : PopupMenuControl  //for ticks_in or out
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `popupthicks_inout`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupthicks_inout`, `popupthicks_inout`
- Source: `Pierre's Template.ipf:1216`

#### `PopMenuProc_auto_ifs4`

```igor
Function PopMenuProc_auto_ifs4(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback in `Pierre's Template.ipf`. Panel control(s): `popup0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup0`
- Source: `Pierre's Template.ipf:35002`

#### `PopMenuProc_curvchoice`

```igor
Function PopMenuProc_curvchoice(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: triggered as a wrapper that opens/runs `curv_update()`.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction. Panel control(s): `popup_choice`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup_choice`
- Source: `Pierre's Template.ipf:12666`

#### `PopMenuProc_EMDC_choice`

```igor
Function PopMenuProc_EMDC_choice(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `popupEDCMDC`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupEDCMDC`
- Source: `Pierre's Template.ipf:11809`

#### `PopMenuProc_EMDCdiv`

```igor
Function PopMenuProc_EMDCdiv(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for ARPES-style loading, plotting, or momentum conversion. Panel control(s): `/ by last`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `/ by last`
- Source: `Pierre's Template.ipf:11914`

#### `PopMenuProc_EMDConoff`

```igor
Function PopMenuProc_EMDConoff(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Multi-matrices`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Multi-matrices`
- Source: `Pierre's Template.ipf:11854`

#### `PopMenuProc_FS_Htype`

```igor
Function PopMenuProc_FS_Htype(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `FS type:`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `FS type:`
- Source: `Pierre's Template.ipf:33218`

#### `PopMenuProc_FS_Htype1`

```igor
Function PopMenuProc_FS_Htype1(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `FS type:`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `FS type:`
- Source: `Pierre's Template.ipf:33237`

#### `PopMenuProc_mode_ifs4`

```igor
Function PopMenuProc_mode_ifs4(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ############################################################################################ ############################################################################################ ########################################################### Panel control(s): `Mode`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Mode`
- Source: `Pierre's Template.ipf:34170`

#### `PopMenuProc_mode_mfs4`

```igor
Function PopMenuProc_mode_mfs4(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: CHUNK STORY Panel control(s): `Mode`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Mode`
- Source: `Pierre's Template.ipf:34190`

#### `PopMenuProc_predictHV`

```igor
Function PopMenuProc_predictHV(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Update_predict_cuts()`.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `popup0`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popup0`
- Source: `Pierre's Template.ipf:26130`

#### `PopMenuProc_slicermode`

```igor
Function PopMenuProc_slicermode(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `Mode:`. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`, `margin()`, `PopMenuProcorientationforslicer()`, `mode()`, `marker()`, `lsize()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Mode:`
- Source: `Pierre's Template.ipf:25519`

#### `PopMenuProc_viewimage_fs`

```igor
Function PopMenuProc_viewimage_fs(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `View`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `View`
- Source: `Pierre's Template.ipf:24968`

#### `PopMenuProc_viewimage_fs2f`

```igor
Function PopMenuProc_viewimage_fs2f(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `View`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `View`
- Source: `Pierre's Template.ipf:25133`

#### `PopMenuProc_viewimage_fsH`

```igor
Function PopMenuProc_viewimage_fsH(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `View`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `View`
- Source: `Pierre's Template.ipf:25029`

#### `PopMenuProc_viewmaking_fs`

```igor
Function PopMenuProc_viewmaking_fs(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `View`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `View`
- Source: `Pierre's Template.ipf:24914`

#### `PopMenuProc_viewmaking_fs2f`

```igor
Function PopMenuProc_viewmaking_fs2f(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `View`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `View`
- Source: `Pierre's Template.ipf:25079`

#### `PopMenuProc_viewmaking_fsH`

```igor
Function PopMenuProc_viewmaking_fsH(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for graph display, formatting, or window management. Panel control(s): `View`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `View`
- Source: `Pierre's Template.ipf:25187`

#### `PopMenuProcAVG_inout`

```igor
Function PopMenuProcAVG_inout(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: triggered as a wrapper that opens/runs `AVG_Calc_avgcirc()`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `popupinout`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupinout`
- Source: `Pierre's Template.ipf:12798`

#### `PopMenuProcorientationforslicer`

```igor
Function PopMenuProcorientationforslicer(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: call as `PopMenuProcorientationforslicer(ctrlName, popNum, popStr)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `Special orientation:`. Code behavior: reads or updates panel controls. Main internal calls: `PopMenuProcwhichpnt()`, `SliderProcCsrslicerx()`, `SliderProcCsrslicery()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Special orientation:`, `Special orientation:`, `Special orientation:`
- Source: `Pierre's Template.ipf:25621`

#### `PopMenuProcpredictorientation`

```igor
Function PopMenuProcpredictorientation(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `PopMenuProcpredictorientation(ctrlName, popNum, popStr)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `popuppredict_orientation`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popuppredict_orientation`
- Source: `Pierre's Template.ipf:25899`

#### `PopMenuProcwhichpnt`

```igor
Function PopMenuProcwhichpnt(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: call as `PopMenuProcwhichpnt(ctrlName, popNum, popStr)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `popupwhichpnt`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `popupwhichpnt`, `popupwhichpnt`, `popupwhichpnt`
- Source: `Pierre's Template.ipf:25602`

#### `setKEatEF`

```igor
Function setKEatEF(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `KEatEF_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Kenetic E`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Kenetic E`
- Source: `Pierre's Template.ipf:26480`

#### `setKEatEF2f`

```igor
Function setKEatEF2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `KEatEF_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Kenetic E`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Kenetic E`
- Source: `Pierre's Template.ipf:26489`

#### `setKEatEFH`

```igor
Function setKEatEFH(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `KEatEF_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Kenetic E`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Kenetic E`
- Source: `Pierre's Template.ipf:26498`

#### `setpredictKEatEF`

```igor
Function setpredictKEatEF(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `predictKEatEF_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `KE`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `KE`
- Source: `Pierre's Template.ipf:26507`

#### `setquality_factor`

```igor
Function setquality_factor(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `quality_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Quality`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Quality`, `Quality`, `Quality`
- Source: `Pierre's Template.ipf:26274`

#### `setvaraonb2f`

```igor
Function setvaraonb2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `aonb_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `a/b a>b`. Main internal calls: `PopMenuProc_viewmaking_fs2f()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a/b a>b`
- Source: `Pierre's Template.ipf:26418`

#### `setvaraonb2f_1`

```igor
Function setvaraonb2f_1(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `aonb_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `a/b a>b`. Main internal calls: `PopMenuProc_viewimage_fs2f()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a/b a>b`
- Source: `Pierre's Template.ipf:26433`

#### `setvarAVG_circcenterx`

```igor
Function setvarAVG_circcenterx(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarAVG_circcenterx(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Inc:`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Inc:`
- Source: `Pierre's Template.ipf:12999`

#### `setvarAVG_circcentery`

```igor
Function setvarAVG_circcentery(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarAVG_circcentery(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Inc:`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Inc:`
- Source: `Pierre's Template.ipf:13009`

#### `setvarAVG_circsize`

```igor
Function setvarAVG_circsize(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarAVG_circsize(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Inc`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Inc`
- Source: `Pierre's Template.ipf:13019`

#### `SetVarAVG_UpdateCirc`

```igor
Function SetVarAVG_UpdateCirc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `AVG_UpdateCirc()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Center x:`, `Center y:`, `Size`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Center x:`, `Center y:`, `Size`
- Source: `Pierre's Template.ipf:12990`

#### `setvarEDCF`

```igor
Function setvarEDCF(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarEDCF(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion. Panel control(s): `setvar_EDCF`. Code behavior: reads or updates panel controls. Main internal calls: `ceil()`, `log()`, `edccurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_EDCF`
- Source: `Pierre's Template.ipf:13677`

#### `setvarEDCt`

```igor
Function setvarEDCt(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarEDCt(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `setvar_EDCT`. Main internal calls: `edccurv()`, `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_EDCT`
- Source: `Pierre's Template.ipf:13646`

#### `setvarEDCw`

```igor
Function setvarEDCw(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarEDCw(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `setvar_EDCW`. Main internal calls: `edccurv()`, `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_EDCW`
- Source: `Pierre's Template.ipf:13663`

#### `setvarglobal_mu`

```igor
Function setvarglobal_mu(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_mu_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `m`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `m`, `m`
- Source: `Pierre's Template.ipf:26647`

#### `setvarglobal_mu2f`

```igor
Function setvarglobal_mu2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_mu_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `m`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `m`, `m`
- Source: `Pierre's Template.ipf:26657`

#### `setvarglobal_t`

```igor
Function setvarglobal_t(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_t_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `t`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t`, `t`
- Source: `Pierre's Template.ipf:26677`

#### `setvarglobal_t2f`

```igor
Function setvarglobal_t2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_t_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `t`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t`, `t`
- Source: `Pierre's Template.ipf:26687`

#### `setvarglobal_tprime`

```igor
Function setvarglobal_tprime(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_tprime_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `t'`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t'`, `t'`
- Source: `Pierre's Template.ipf:26707`

#### `setvarglobal_tprime2f`

```igor
Function setvarglobal_tprime2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_tprime_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `t'`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t'`, `t'`
- Source: `Pierre's Template.ipf:26717`

#### `setvarincrement_aonb`

```igor
Function setvarincrement_aonb(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `aonb_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26631`

#### `setvarincrement_global_mu`

```igor
Function setvarincrement_global_mu(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_mu_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26737`

#### `setvarincrement_global_t`

```igor
Function setvarincrement_global_t(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_t_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26755`

#### `setvarincrement_global_tprime`

```igor
Function setvarincrement_global_tprime(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `global_tprime_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26773`

#### `setvarincrement_ifact`

```igor
Function setvarincrement_ifact(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `SetVarProc_ifact_inc()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:12720`

#### `setvarincrement_phi1_zero`

```igor
Function setvarincrement_phi1_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi1_zero_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26567`

#### `setvarincrement_phi_zero`

```igor
Function setvarincrement_phi_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi_zero_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26551`

#### `setvarincrement_phi_zeroH`

```igor
Function setvarincrement_phi_zeroH(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi_zero_incsetH()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26559`

#### `setvarincrement_predict_mu`

```igor
Function setvarincrement_predict_mu(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarincrement_predict_mu(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Increment`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26745`

#### `setvarincrement_predict_t`

```igor
Function setvarincrement_predict_t(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarincrement_predict_t(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Increment`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26763`

#### `setvarincrement_predict_tprime`

```igor
Function setvarincrement_predict_tprime(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarincrement_predict_tprime(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `Increment`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26781`

#### `setvarincrement_rot1_zero`

```igor
Function setvarincrement_rot1_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `rot1_zero_incset()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26583`

#### `setvarincrement_rot_zero`

```igor
Function setvarincrement_rot_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `rot_zero_incset()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26575`

#### `setvarincrement_theta_zero`

```igor
Function setvarincrement_theta_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta_zero_incset()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26527`

#### `setvarincrement_theta_zeroH`

```igor
Function setvarincrement_theta_zeroH(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta_zero_incsetH()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26535`

#### `setvarincrementpredictlattice0`

```igor
Function setvarincrementpredictlattice0(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarincrementpredictlattice0(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Increment`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26591`

#### `setvarincrementpredictrotation`

```igor
Function setvarincrementpredictrotation(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarincrementpredictrotation(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Increment`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`
- Source: `Pierre's Template.ipf:26613`

#### `setvarmDCF`

```igor
Function setvarmDCF(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarmDCF(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion. Panel control(s): `setvar_MDCF`. Code behavior: reads or updates panel controls. Main internal calls: `ceil()`, `log()`, `mdccurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_MDCF`
- Source: `Pierre's Template.ipf:13729`

#### `setvarmDCt`

```igor
Function setvarmDCt(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarmDCt(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `setvar_MDCT`. Main internal calls: `mdccurv()`, `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_MDCT`
- Source: `Pierre's Template.ipf:13699`

#### `setvarmDCw`

```igor
Function setvarmDCw(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarmDCw(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `setvar_MDCW`. Main internal calls: `mdccurv()`, `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_MDCW`
- Source: `Pierre's Template.ipf:13714`

#### `setvarNTP_num`

```igor
Function setvarNTP_num(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `NTP_num_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `varNTP_num`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `varNTP_num`
- Source: `Pierre's Template.ipf:2604`

#### `setvarphi1_zero`

```igor
Function setvarphi1_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi1_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `j_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `j_0`, `j_0`, `j_0`
- Source: `Pierre's Template.ipf:26349`

#### `setvarphi_zero`

```igor
Function setvarphi_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `j_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `j_0`
- Source: `Pierre's Template.ipf:26322`

#### `setvarphi_zero2f`

```igor
Function setvarphi_zero2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `j_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `j_0`
- Source: `Pierre's Template.ipf:26331`

#### `setvarphi_zeroH`

```igor
Function setvarphi_zeroH(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `phi_zero_setH()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `j_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `j_0`
- Source: `Pierre's Template.ipf:26340`

#### `setvarplot`

```igor
Function setvarplot(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `plotraw()`.
- Notes: Utility function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Data`
- Source: `Pierre's Template.ipf:92`

#### `setvarpredict_mu`

```igor
Function setvarpredict_mu(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `predict_mu_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `m`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `m`
- Source: `Pierre's Template.ipf:26667`

#### `setvarpredict_t`

```igor
Function setvarpredict_t(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `predict_t_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `t`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t`
- Source: `Pierre's Template.ipf:26697`

#### `setvarpredict_tprime`

```igor
Function setvarpredict_tprime(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `predict_tprime_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `t'`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t'`
- Source: `Pierre's Template.ipf:26727`

#### `setvarpredictacept`

```igor
Function setvarpredictacept(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Update_predict_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Acept.`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Acept.`
- Source: `Pierre's Template.ipf:26106`

#### `SetVarpredictrotation`

```igor
Function SetVarpredictrotation(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Update_predict_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotation`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotation`
- Source: `Pierre's Template.ipf:26118`

#### `SetVarProc_2`

```igor
Function SetVarProc_2(ctrlName,varNum,varStr,varName) : SetVariableControl //base
```
- Usage: triggered as a wrapper that opens/runs `Update()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:1267`

#### `SetVarProc_4`

```igor
Function SetVarProc_4(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: reads or updates panel controls. Main internal calls: `num2istr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:1624`

#### `SetVarProc_7`

```igor
Function SetVarProc_7(ctrlName,varNum,varStr,varName) : SetVariableControl //Delta_offset
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:1258`

#### `SetVarProc_8`

```igor
Function SetVarProc_8(ctrlName,varNum,varStr,varName) : SetVariableControl  //Delta_Base
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:1275`

#### `SetVarProc_central_ifs4`

```igor
Function SetVarProc_central_ifs4(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `Update_image_fs()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:31184`

#### `SetVarProc_Consoffset`

```igor
Function SetVarProc_Consoffset(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Offset(Y)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Offset(Y)`
- Source: `SmartDisplay.ipf:1380`

#### `SetVarProc_Consoffsetx`

```igor
Function SetVarProc_Consoffsetx(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `Offset(x)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Offset(x)`
- Source: `SmartDisplay.ipf:1484`

#### `SetVarProc_EMDC_change_SF`

```igor
Function SetVarProc_EMDC_change_SF(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_changeEDC()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Smooth`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Smooth`
- Source: `Pierre's Template.ipf:11733`

#### `SetVarProc_EMDC_changecmbine`

```igor
Function SetVarProc_EMDC_changecmbine(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_changeEDC()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Combine`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Combine`
- Source: `Pierre's Template.ipf:11718`

#### `SetVarProc_EMDC_changeEDC`

```igor
Function SetVarProc_EMDC_changeEDC(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_changeEDC()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `EDC slice`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `EDC slice`
- Source: `Pierre's Template.ipf:11678`

#### `SetVarProc_EMDC_changeMDC`

```igor
Function SetVarProc_EMDC_changeMDC(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `EMDC_changeMDC()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `MDC slice`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `MDC slice`
- Source: `Pierre's Template.ipf:11688`

#### `SetVarProc_ifact`

```igor
Function SetVarProc_ifact(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `curv_update()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `I_0 factor`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `I_0 factor`
- Source: `Pierre's Template.ipf:12704`

#### `SetVarProc_ifact_inc`

```igor
Proc SetVarProc_ifact_inc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Panel set-variable callback in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:12713`

#### `SetVarProc_irange_ifs4`

```igor
Function SetVarProc_irange_ifs4(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `Update_image_fs()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Integral window`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Integral window`
- Source: `Pierre's Template.ipf:31195`

#### `SetVarProc_KZ_changea`

```igor
Function SetVarProc_KZ_changea(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `adjustkz()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `a (A)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a (A)`
- Source: `Miscellaneous_Codes.ipf:1665`

#### `SetVarProc_KZ_changeC`

```igor
Function SetVarProc_KZ_changeC(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `adjustkz()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `c (A)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `c (A)`
- Source: `Miscellaneous_Codes.ipf:1653`

#### `SetVarProc_KZ_changeendx`

```igor
Function SetVarProc_KZ_changeendx(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `adjustkz()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `kxrange (A)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `kxrange (A)`
- Source: `Miscellaneous_Codes.ipf:1687`

#### `SetVarProc_KZ_changehv`

```igor
Function SetVarProc_KZ_changehv(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `adjustkz()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Ek (eV)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Ek (eV)`
- Source: `Miscellaneous_Codes.ipf:1631`

#### `SetVarProc_KZ_changestartx`

```igor
Function SetVarProc_KZ_changestartx(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `adjustkz()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Miscellaneous_Codes.ipf:1677`

#### `SetVarProc_KZ_changev0`

```igor
Function SetVarProc_KZ_changev0(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `adjustkz()`.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Miscellaneous_Codes.ipf:1641`

#### `SetVarProc_tdefecct`

```igor
Function SetVarProc_tdefecct(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal. Panel control(s): `Latt.Q(°)`, `Latt. a`, `Dislo.(°)`, `Ph. shif`, `Background`, `Amplitude`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Latt.Q(°)`, `Latt. a`, `Dislo.(°)`, `Ph. shif`, `Background`, `Amplitude`, `Dislo.(°)`, `Ph. shif`
- Source: `Models.ipf:10514`

#### `SetVarProc_vfshrink`

```igor
Function SetVarProc_vfshrink(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for graph display, formatting, or window management. Panel control(s): `Coarse`. Main internal calls: `shrinkvf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Coarse`, `Coarse`
- Source: `SmartDisplay.ipf:4618`

#### `SetVarProcanglephidelta`

```igor
Function SetVarProcanglephidelta(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Removepredicted_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Delta`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Delta`
- Source: `Pierre's Template.ipf:25876`

#### `SetVarProcanglephifrom`

```igor
Function SetVarProcanglephifrom(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Removepredicted_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Phi: From`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Phi: From`
- Source: `Pierre's Template.ipf:25854`

#### `SetVarProcanglephito`

```igor
Function SetVarProcanglephito(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Removepredicted_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `To`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `To`
- Source: `Pierre's Template.ipf:25863`

#### `SetVarProcanglethetadelta`

```igor
Function SetVarProcanglethetadelta(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `Removepredicted_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Delta`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Delta`
- Source: `Pierre's Template.ipf:25887`

#### `SetVarProcanglethetafrom`

```igor
Function SetVarProcanglethetafrom(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `update_predict_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `Theta: From`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Theta: From`
- Source: `Pierre's Template.ipf:25844`

#### `SetVarProcanglethetato`

```igor
Function SetVarProcanglethetato(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `update_predict_cuts()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `To`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `To`
- Source: `Pierre's Template.ipf:25830`

#### `SetVarProcCmbslicer`

```igor
Function SetVarProcCmbslicer(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `SetVarProcCmbslicer(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Combine`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Combine`
- Source: `Pierre's Template.ipf:27682`

#### `SetVarProclattice_hexagon`

```igor
Function SetVarProclattice_hexagon(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `updateHexagonFS()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Hexagon size`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Hexagon size`
- Source: `Pierre's Template.ipf:33256`

#### `SetVarProclattice_hexagon1`

```igor
Function SetVarProclattice_hexagon1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `updateHexagonFS1()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Hexagon size`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Hexagon size`
- Source: `Pierre's Template.ipf:33264`

#### `setvarrot1_zero`

```igor
Function setvarrot1_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `rot1_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotation`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotation`, `Rotation`, `Rotation`
- Source: `Pierre's Template.ipf:26388`

#### `setvarrot_zero`

```igor
Function setvarrot_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `rot_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotation`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotation`
- Source: `Pierre's Template.ipf:26361`

#### `setvarrot_zero2f`

```igor
Function setvarrot_zero2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `rot_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotation`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotation`
- Source: `Pierre's Template.ipf:26370`

#### `setvarrot_zeroH`

```igor
Function setvarrot_zeroH(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `rot_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotation`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotation`
- Source: `Pierre's Template.ipf:26379`

#### `setvartheta1_zero`

```igor
Function setvartheta1_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta1_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `q_0`, `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `q_0`, `Increment`, `q_0`, `Increment`, `q_0`, `Increment`
- Source: `Pierre's Template.ipf:26309`

#### `setvartheta_zero`

```igor
Function setvartheta_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `q_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `q_0`
- Source: `Pierre's Template.ipf:26282`

#### `setvartheta_zero2f`

```igor
Function setvartheta_zero2f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta_zero_set()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `q_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `q_0`
- Source: `Pierre's Template.ipf:26291`

#### `setvartheta_zeroH`

```igor
Function setvartheta_zeroH(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta_zero_setH()`.
- Notes: Utility function for linecut, slice, or region extraction. Panel control(s): `q_0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `q_0`
- Source: `Pierre's Template.ipf:26300`

#### `setvartwoDF`

```igor
Function setvartwoDF(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvartwoDF(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Panel control(s): `setvar_TwoDF`. Code behavior: reads or updates panel controls. Main internal calls: `ceil()`, `log()`, `twodcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `setvar_TwoDF`
- Source: `Pierre's Template.ipf:13751`

#### `SliderProc`

```igor
Function SliderProc(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Panel control(s): `slider0`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slider0`
- Source: `Pierre's Template.ipf:27893`

#### `SliderProc_1`

```igor
Function SliderProc_1(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_1(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Panel control(s): `slider1`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slider1`
- Source: `Pierre's Template.ipf:28138`

#### `SliderProc_topmax`

```igor
Function SliderProc_topmax(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_topmax(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `topimagetop`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `topimagetop`, `topimagetop`
- Source: `Pierre's Template.ipf:12032`

#### `SliderProc_topmin`

```igor
Function SliderProc_topmin(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProc_topmin(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; graph display, formatting, or window management. Panel control(s): `topimagebot`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `topimagebot`, `topimagebot`
- Source: `Pierre's Template.ipf:12057`

#### `SliderProcCsrslicerx`

```igor
Function SliderProcCsrslicerx(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProcCsrslicerx(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `sliderx`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `sliderx`, `sliderx`, `sliderx`
- Source: `Pierre's Template.ipf:25476`

#### `SliderProcCsrslicery`

```igor
Function SliderProcCsrslicery(ctrlName,sliderValue,event) : SliderControl
```
- Usage: call as `SliderProcCsrslicery(ctrlName, sliderValue, event)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction. Panel control(s): `slidery`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `sliderValue`: Step size or energy/voltage increment used by the calculation.
  - `event`: bit field: bit 0: value set, 1: mouse down, 2: mouse up, 3: mouse moved
- Panel controls: `slidery`, `slidery`, `slidery`
- Source: `Pierre's Template.ipf:25498`

#### `Tab4vs6`

```igor
Function Tab4vs6(ctrlName,tabNum) : TabControl
```
- Usage: call as `Tab4vs6(ctrlName, tabNum)`.
- Notes: Panel callback/helper for spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `tab4vs6`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `tabNum`: Number of spectra, waves, points, bins, or iterations to process.
- Panel controls: `tab4vs6`
- Source: `Pierre's Template.ipf:32781`

#### `Tab_convert`

```igor
Function Tab_convert(ctrlName,tabNum) : TabControl
```
- Usage: call as `Tab_convert(ctrlName, tabNum)`.
- Notes: Panel callback/helper for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `mat3d_tab`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `tabNum`: Number of spectra, waves, points, bins, or iterations to process.
- Panel controls: `mat3d_tab`
- Source: `Pierre's Template.ipf:33887`

#### `TabProc_main`

```igor
Function TabProc_main(ctrlName,tabNum) : TabControl
```
- Usage: call as `TabProc_main(ctrlName, tabNum)`.
- Notes: Panel callback/helper for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `tab_matrices`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `tabNum`: Number of spectra, waves, points, bins, or iterations to process.
- Panel controls: `tab_matrices`, `tab_matrices`
- Source: `Pierre's Template.ipf:2625`


### Interactive procedures and macros

#### `Add_above`

```igor
Proc Add_above(from,to,exception,number_of_points)
```
- Usage: run from Igor with parameters `from`, `to`, `exception`, `number_of_points`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: from = From; to = To; exception = Name of wave:; number_of_points = Number of points:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `from`: From
  - `to`: To
  - `exception`: Name of wave:
  - `number_of_points`: Number of points:
- Source: `Pierre's Template.ipf:15432`

#### `Add_all`

```igor
Proc Add_all(from,to,exception)
```
- Usage: run from Igor with parameters `from`, `to`, `exception`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: from = From; to = To; exception = Name of wave:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics; asks the user for parameters through Igor prompts.
- Parameters:
  - `from`: From
  - `to`: To
  - `exception`: Name of wave:
- Source: `Pierre's Template.ipf:15485`

#### `Add_bottom`

```igor
Proc Add_bottom(from,to,exception)
```
- Usage: run from Igor with parameters `from`, `to`, `exception`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: from = From; to = To; exception = Name of wave:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics; asks the user for parameters through Igor prompts.
- Parameters:
  - `from`: From
  - `to`: To
  - `exception`: Name of wave:
- Source: `Pierre's Template.ipf:15387`

#### `add_data`

```igor
Proc add_data()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `str2num()`, `ConvtOne()`, `Wintype()`, `setvarplot()`.
- Source: `Pierre's Template.ipf:8907`

#### `add_this_data`

```igor
Proc add_this_data()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `str2num()`, `ConvtOne()`, `setvarplot()`.
- Source: `Pierre's Template.ipf:8864`

#### `addpntbkgndremover`

```igor
Proc addpntbkgndremover()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Main internal calls: `func_addpntbkgndremover()`, `subbkgndremover()`.
- Source: `Pierre's Template.ipf:8985`

#### `adjust_all_to_fp`

```igor
Proc adjust_all_to_fp(indexfrom,indexto,ferminame,Fermilevel)
```
- Usage: run from Igor with parameters `indexfrom`, `indexto`, `ferminame`, `Fermilevel`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: indexfrom = Enter the first index; indexto = Enter the last index; ferminame = Enter the name of the Fermi profile; Fermilevel = Enter the value of the Fermi level. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `adjust_to_fp()`.
- Parameters:
  - `indexfrom`: Enter the first index
  - `indexto`: Enter the last index
  - `ferminame`: Enter the name of the Fermi profile
  - `Fermilevel`: Enter the value of the Fermi level
- Source: `Pierre's Template.ipf:6391`

#### `adjust_col`

```igor
Proc adjust_col()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `rgb()`.
- Source: `Pierre's Template.ipf:15721`

#### `adjust_fp`

```igor
Proc adjust_fp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp()`.
- Source: `Pierre's Template.ipf:19123`

#### `adjust_meV`

```igor
Proc adjust_meV()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: assigns Igor axis scaling/units to the output; smooths wave data.
- Source: `Pierre's Template.ipf:15708`

#### `adjust_to_fp`

```igor
Proc adjust_to_fp(matname,ferminame,Fermilevel)
```
- Usage: run from Igor with parameters `matname`, `ferminame`, `Fermilevel`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; ferminame = Enter the name of the Fermi profile; Fermilevel = Enter the value of the Fermi level. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `Ceil()`, `func_adjust_to_fp()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `ferminame`: Enter the name of the Fermi profile
  - `Fermilevel`: Enter the value of the Fermi level
- Source: `Pierre's Template.ipf:6412`

#### `adjustkz`

```igor
Proc  adjustkz()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion.
- Source: `Miscellaneous_Codes.ipf:1756`

#### `allcuts`

```igor
Proc allcuts(overlap,display_or_not)
```
- Usage: run from Igor with parameters `overlap`, `display_or_not`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: overlap = Enter the number of overlapping slices; display_or_not = Do you want to show the display the cuts?. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `makecutimage_pi()`.
- Parameters:
  - `overlap`: Enter the number of overlapping slices
  - `display_or_not`: Do you want to show the display the cuts?
- Source: `Pierre's Template.ipf:30249`

#### `allHD`

```igor
Proc allHD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `derivmat()`.
- Source: `Pierre's Template.ipf:30265`

#### `angle2kVcut`

```igor
Proc angle2kVcut(matname,thetaangle,phiangle,Fermilevel)
```
- Usage: run from Igor with parameters `matname`, `thetaangle`, `phiangle`, `Fermilevel`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; thetaangle = Enter the theta angle; phiangle = Enter the phiangle; Fermilevel = Enter the Fermi level. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `calcangle2kVcut()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `thetaangle`: Enter the theta angle
  - `phiangle`: Enter the phiangle
  - `Fermilevel`: Enter the Fermi level
- Source: `Pierre's Template.ipf:22220`

#### `anglemode_converter_fs`

```igor
Proc anglemode_converter_fs(sequence)
```
- Usage: run from Igor with parameters `sequence`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Dialog prompts: sequence = Enter the index sequence. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `Str2num()`.
- Parameters:
  - `sequence`: Enter the index sequence
- Source: `Pierre's Template.ipf:35291`

#### `aonb_incset`

```igor
Proc aonb_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31984`

#### `aonb_set`

```igor
Proc aonb_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32003`

#### `append_circles`

```igor
Proc append_circles(thename)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `thename`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: thename = Enter the name for the circles. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `thename`: Enter the name for the circles
- Source: `Pierre's Template.ipf:7926`

#### `appendpnts`

```igor
Proc appendpnts()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:5465`

#### `appendthis`

```igor
Proc appendthis(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: name = Enter the name. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name
- Source: `Pierre's Template.ipf:6845`

#### `AppendtoCut`

```igor
Macro AppendtoCut(Mat)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `Mat`.
- Notes: Igor macro entry point for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: Mat = input mat name. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `Mat`: input mat name
- Source: `Pierre's Template.ipf:1898`

#### `AppendtoCutFromMat`

```igor
proc AppendtoCutFromMat(oldmat,newmat,startp,endp)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `oldmat`, `newmat`, `startp`, `endp`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction.
- Parameters:
  - `oldmat`: Matrix/2D wave input or matrix output name.
  - `newmat`: Matrix/2D wave input or matrix output name.
  - `startp`: Start point, index, or coordinate for the selected range.
  - `endp`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:28492`

#### `Au_100`

```igor
Proc Au_100(datanumber,smoothf,slicecomb, thistemparature)
```
- Usage: run from Igor with parameters `datanumber`, `smoothf`, `slicecomb`, `thistemparature`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: datanumber = Data number; smoothf = Smooth factor; slicecomb = Slices combined; thistemparature = Temperature (K). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Get_Fermi_Profile()`, `rgb()`, `findEres()`.
- Parameters:
  - `datanumber`: Data number
  - `smoothf`: Smooth factor
  - `slicecomb`: Slices combined
  - `thistemparature`: Temperature (K)
- Source: `Pierre's Template.ipf:23726`

#### `au_temp`

```igor
Proc au_temp(datanumber)
```
- Usage: run from Igor with parameters `datanumber`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: datanumber = Enter the number of the data. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `rgb()`.
- Parameters:
  - `datanumber`: Enter the number of the data
- Source: `Pierre's Template.ipf:18758`

#### `auto2d_pi`

```igor
Proc auto2d_pi(energy)
```
- Usage: run from Igor with parameters `energy`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: energy = Enter the energy at which you want to map. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `energy`: Enter the energy at which you want to map
- Source: `Pierre's Template.ipf:29667`

#### `AVG_Appendimage`

```igor
Proc AVG_Appendimage()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Source: `Pierre's Template.ipf:12807`

#### `AVG_CirccenterCsr`

```igor
Proc AVG_CirccenterCsr()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `xcsr()`, `vcsr()`, `AVG_Calc_avgcirc()`.
- Source: `Pierre's Template.ipf:12789`

#### `AVG_Editresults`

```igor
Proc AVG_Editresults()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:12813`

#### `AVG_ini`

```igor
Proc AVG_ini()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:12820`

#### `AVG_MakeCirc`

```igor
Proc AVG_MakeCirc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:12978`

#### `backin360`

```igor
Proc backin360(name1)
```
- Usage: run from Igor with parameters `name1`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: name1 = Enter name with angle. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `name1`: Enter name with angle
- Source: `Pierre's Template.ipf:16573`

#### `BEatheight_EDC`

```igor
Proc BEatheight_EDC(EDCname,height,smoothfactor)
```
- Usage: run from Igor with parameters `EDCname`, `height`, `smoothfactor`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: EDCname = Enter the name of the ECC; height = Height for the leading edge; smoothfactor = Enter the smooth factor. Code behavior: smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `func_BEatheight_EDC()`.
- Parameters:
  - `EDCname`: Enter the name of the ECC
  - `height`: Height for the leading edge
  - `smoothfactor`: Enter the smooth factor
- Source: `Pierre's Template.ipf:35798`

#### `BFA_corrposition`

```igor
Proc BFA_corrposition(fixslice)
```
- Usage: run from Igor with parameters `fixslice`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: fixslice = Enter the final slice position. Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `transform2equiv_mat()`.
- Parameters:
  - `fixslice`: Enter the final slice position
- Source: `Pierre's Template.ipf:16611`

#### `BKFA_adjust_EF_all`

```igor
Proc BKFA_adjust_EF_all()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `BKFA_adjust_EF()`.
- Source: `Pierre's Template.ipf:14750`

#### `BKFA_Fit1FD1L`

```igor
Proc BKFA_Fit1FD1L(slicenum,pntnum)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `slicenum`, `pntnum`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: slicenum = Enter the slice:; pntnum = Enter the point (1 or 2):. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `xCsr()`, `thisname()`, `rgb()`, `marker()`.
- Parameters:
  - `slicenum`: Enter the slice:
  - `pntnum`: Enter the point (1 or 2):
- Source: `Pierre's Template.ipf:14072`

#### `BKFA_Fit1FD2L`

```igor
Proc BKFA_Fit1FD2L(slicenum,pntnum)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `slicenum`, `pntnum`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: slicenum = Enter the slice:; pntnum = Enter the point (1 or 2):. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `xCsr()`, `thisname()`, `rgb()`, `marker()`.
- Parameters:
  - `slicenum`: Enter the slice:
  - `pntnum`: Enter the point (1 or 2):
- Source: `Pierre's Template.ipf:14141`

#### `BKFA_Fit1FD2L_Analysis`

```igor
Proc BKFA_Fit1FD2L_Analysis(slicenum,ncomb,band)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `slicenum`, `ncomb`, `band`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: slicenum = Enter the slice:; ncomb = Number of EDCs to combine:; band = Band (alpha,beta, gamma):. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `mean()`, `pnt2x()`, `sym_one_EDC()`, `thisname()`, `sym1FDplus2asLor()`, `OneasLor()`.
- Parameters:
  - `slicenum`: Enter the slice:
  - `ncomb`: Number of EDCs to combine:
  - `band`: Band (alpha,beta, gamma):
- Source: `Pierre's Template.ipf:14476`

#### `BKFA_Shiftdata`

```igor
Proc BKFA_Shiftdata()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Source: `Pierre's Template.ipf:13967`

#### `BKFA_Symmat`

```igor
Proc BKFA_Symmat()
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `sym_edc_matrix_far()`, `Wintype()`.
- Source: `Pierre's Template.ipf:13995`

#### `BKFA_SymmatEDC`

```igor
Proc BKFA_SymmatEDC(thepnt,num2comb)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `thepnt`, `num2comb`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: thepnt = Enter the point (1 or 2):; num2comb = Number of EDC to combine (odd):. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `Wintype()`, `mean()`, `pnt2x()`.
- Parameters:
  - `thepnt`: Enter the point (1 or 2):
  - `num2comb`: Number of EDC to combine (odd):
- Source: `Pierre's Template.ipf:14019`

#### `bluegreenred`

```igor
Proc bluegreenred()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `WaveName()`, `rgb()`.
- Source: `Pierre's Template.ipf:6758`

#### `bluetored`

```igor
Proc bluetored()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `WaveName()`, `rgb()`.
- Source: `Pierre's Template.ipf:6737`

#### `BMGmacro1`

```igor
Proc BMGmacro1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:23492`

#### `BMGmacro2`

```igor
Proc BMGmacro2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:23526`

#### `calc4circles`

```igor
Proc calc4circles(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing. Dialog prompts: name = Enter the name of the sample. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `func_calc4circles()`.
- Parameters:
  - `name`: Enter the name of the sample
- Source: `Pierre's Template.ipf:32153`

#### `calc4circles2f`

```igor
Proc calc4circles2f(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing. Dialog prompts: name = Enter the name of the sample. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `func_calc4circles()`.
- Parameters:
  - `name`: Enter the name of the sample
- Source: `Pierre's Template.ipf:32168`

#### `calc_tbarea`

```igor
Proc calc_tbarea()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves. Main internal calls: `tbfunc()`.
- Source: `Pierre's Template.ipf:32581`

#### `calc_xpos`

```igor
Proc calc_xpos()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:19498`

#### `calculate_v0`

```igor
Proc calculate_v0(kz15_minus_kzV0,Energy_Reference,Delta_k)
```
- Usage: run from Igor with parameters `kz15_minus_kzV0`, `Energy_Reference`, `Delta_k`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: kz15_minus_kzV0 = Kz(15meV,ER)-Kz(exp,ER); Energy_Reference = Reference Energy to compare Kz; Delta_k = inplane K deviation from G. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `kz15_minus_kzV0`: Kz(15meV,ER)-Kz(exp,ER)
  - `Energy_Reference`: Reference Energy to compare Kz
  - `Delta_k`: inplane K deviation from G
- Source: `Miscellaneous_Codes.ipf:1010`

#### `calculateLLNewc`

```igor
Proc calculateLLNewc(hv,B,ED,num)
```
- Usage: run from Igor with parameters `hv`, `B`, `ED`, `num`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: hv = Fermi velocity (meV*A); B = Magnetic Field (T); ED = Binding Energy of Dirac Point (meV); num = number of levels. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `calculateLLNew()`.
- Parameters:
  - `hv`: Fermi velocity (meV*A)
  - `B`: Magnetic Field (T)
  - `ED`: Binding Energy of Dirac Point (meV)
  - `num`: number of levels
- Source: `Models.ipf:4221`

#### `calculateRCc`

```igor
proc calculateRCc(pp,qq,pc,qc,angle)
```
- Usage: run from Igor with parameters `pp`, `qq`, `pc`, `qc`, `angle`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: pp = x_i; qq = y_i; pc = x_0; qc = y_0; angle = roate angle (˚). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `calculateRC2()`.
- Parameters:
  - `pp`: x_i
  - `qq`: y_i
  - `pc`: x_0
  - `qc`: y_0
  - `angle`: roate angle (˚)
- Source: `Symmetrization.ipf:210`

#### `calibrationFS`

```igor
proc calibrationFS(cutnumber)
```
- Usage: triggered as a wrapper that opens/runs `get_fermi_profile2()`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `y()`, `Wintype()`.
- Parameters:
  - `cutnumber`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:1369`

#### `ChangeHline`

```igor
Proc ChangeHline(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `funcChangeHline()`.
- Parameters:
  - `matname`: Enter the matrix name
- Source: `Pierre's Template.ipf:27512`

#### `ChangeVline`

```igor
Proc ChangeVline(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `funcChangeVline()`.
- Parameters:
  - `matname`: Enter the matrix name
- Source: `Pierre's Template.ipf:27723`

#### `color_edc`

```igor
Proc color_edc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `CTabList()`, `WaveName()`, `interp()`, `rgb()`.
- Source: `Pierre's Template.ipf:6801`

#### `comb_krange`

```igor
Proc comb_krange(thisdata,outputname,x_center,x_wide)
```
- Usage: run from Igor with parameters `thisdata`, `outputname`, `x_center`, `x_wide`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: thisdata = Enter the name of the matrix; outputname = Enter the outputname; x_center = Enter the position; x_wide = Enter the full range to combine. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `Func_comb_krange()`.
- Parameters:
  - `thisdata`: Enter the name of the matrix
  - `outputname`: Enter the outputname
  - `x_center`: Enter the position
  - `x_wide`: Enter the full range to combine
- Source: `Pierre's Template.ipf:6946`

#### `comb_krange1`

```igor
Proc comb_krange1(thisdata,outputname,x_center,x_wide)
```
- Usage: run from Igor with parameters `thisdata`, `outputname`, `x_center`, `x_wide`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: thisdata = Enter the name of the matrix; outputname = Enter the outputname; x_center = Enter the position; x_wide = Enter the full range to combine. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `mean()`.
- Parameters:
  - `thisdata`: Enter the name of the matrix
  - `outputname`: Enter the outputname
  - `x_center`: Enter the position
  - `x_wide`: Enter the full range to combine
- Source: `Pierre's Template.ipf:7027`

#### `comballcuts`

```igor
Proc comballcuts(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `new_kres_pi()`.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:22194`

#### `comballdata`

```igor
Proc comballdata(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `new_kres_pi()`.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:22207`

#### `combEDCsallcuts`

```igor
Proc combEDCsallcuts(thenumber)
```
- Usage: run from Igor with parameters `thenumber`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: thenumber = Number of EDCs to combine. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `new_kres_pi()`.
- Parameters:
  - `thenumber`: Number of EDCs to combine
- Source: `Pierre's Template.ipf:22123`

#### `combine2MDCs`

```igor
Proc combine2MDCs(from,to)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `from`, `to`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: from = From; to = To. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `new_kres_pi()`.
- Parameters:
  - `from`: From
  - `to`: To
- Source: `Pierre's Template.ipf:16871`

#### `combine_hv`

```igor
Proc combine_hv()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `new_eres_pi()`.
- Source: `Pierre's Template.ipf:23675`

#### `combine_hv346RRRL`

```igor
Proc combine_hv346RRRL()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `new_kres_pi()`.
- Source: `Pierre's Template.ipf:23689`

#### `combine_hv346RRRS`

```igor
Proc combine_hv346RRRS()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `new_kres_pi()`.
- Source: `Pierre's Template.ipf:23703`

#### `combine_range`

```igor
Proc combine_range(name,outputname,x_from,x_to)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `outputname`, `x_from`, `x_to`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix; outputname = Enter the name of the resulting wave; x_from = Combine from:; x_to = Combine to:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the matrix
  - `outputname`: Enter the name of the resulting wave
  - `x_from`: Combine from:
  - `x_to`: Combine to:
- Source: `Pierre's Template.ipf:6873`

#### `combineall`

```igor
Proc combineall(name)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:6853`

#### `combineH_all`

```igor
Proc combineH_all(matname)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:3615`

#### `combMDCsallcuts`

```igor
Proc combMDCsallcuts(thenumber)
```
- Usage: run from Igor with parameters `thenumber`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: thenumber = Number of EDCs to combine. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `new_eres_pi()`.
- Parameters:
  - `thenumber`: Number of EDCs to combine
- Source: `Pierre's Template.ipf:22137`

#### `comp100_200`

```igor
Proc comp100_200()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `EDC_pi()`, `mean()`, `rgb()`, `manTick()`, `manMinor()`, `zero()`.
- Source: `Pierre's Template.ipf:22317`

#### `comp_symedc`

```igor
Proc comp_symedc(sample1,cutname1,first_edc1,sample2,cutname2,first_edc2,total_number,showevery)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `sample1`, `cutname1`, `first_edc1`, `sample2`, `cutname2`, `first_edc2`, `total_number`, `showevery`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: sample1 = Enter the name of the first sample; cutname1 = Enter the name of the cut; first_edc1 = Enter the numer of the first edc; sample2 = Enter the name of the second sample; cutname2 = Enter the name of the cut; first_edc2 = Enter the numer of the first edc. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `rgb()`.
- Parameters:
  - `sample1`: Enter the name of the first sample
  - `cutname1`: Enter the name of the cut
  - `first_edc1`: Enter the numer of the first edc
  - `sample2`: Enter the name of the second sample
  - `cutname2`: Enter the name of the cut
  - `first_edc2`: Enter the numer of the first edc
  - `total_number`: Enter the number of EDCs to load.
  - `showevery`: Show one EDC every...:
- Source: `Pierre's Template.ipf:19276`

#### `compare2`

```igor
Proc compare2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `rgb()`.
- Source: `Pierre's Template.ipf:20420`

#### `compare_mdc`

```igor
Proc compare_mdc(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `pcsr()`, `xcsr()`, `rgb()`.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:3300`

#### `Consoffset`

```igor
Proc Consoffset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `str2num()`, `TraceInfo()`. Update log: 07/31-02/2023: Added continuously tuning smooth Consmoo()and trace offset Consoffset().
- Source: `SmartDisplay.ipf:1366`

#### `Consoffsetc`

```igor
Proc Consoffsetc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics. Main internal calls: `Constantoffset_nFx()`.
- Source: `SmartDisplay.ipf:1387`

#### `Consoffsetcx`

```igor
Proc Consoffsetcx()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics. Main internal calls: `Constantoffset_nFx()`.
- Source: `SmartDisplay.ipf:1491`

#### `Consoffsetx`

```igor
Proc Consoffsetx()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics; reads or updates panel controls; cleans up waves/windows or brings an existing window forward.
- Source: `SmartDisplay.ipf:1470`

#### `Constantoffset_n`

```igor
Proc Constantoffset_n(Shift,number_of_same)
```
- Usage: run from Igor with parameters `Shift`, `number_of_same`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: shift = Input the amount of shift for offset; number_of_same = Enter the number of graphs with the same offset. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Constantoffset_nF()`.
- Parameters:
  - `Shift`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `number_of_same`: Enter the number of graphs with the same offset
- Source: `Pierre's Template.ipf:6473`

#### `convertforzrs`

```igor
Proc convertforzrs(sample,gamma,work,first_energy)
```
- Usage: run from Igor with parameters `sample`, `gamma`, `work`, `first_energy`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: sample = Enter the name of the sample; gamma = Enter the position of gamma; work = Enter the work potentiel; first_energy = Energy (below Ef) from which you start normalization. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `normcut()`, `rescale_pi()`, `cvt2ek_pi()`.
- Parameters:
  - `sample`: Enter the name of the sample
  - `gamma`: Enter the position of gamma
  - `work`: Enter the work potentiel
  - `first_energy`: Energy (below Ef) from which you start normalization
- Source: `Pierre's Template.ipf:18875`

#### `ConvtAll`

```igor
Proc ConvtAll()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `ConvtAllpxt()`, `ConvtAlltxt()`, `ConvtAlldat()`, `ConvtAllA1()`.
- Source: `Pierre's Template.ipf:17693`

#### `ConvtAllA1`

```igor
Proc ConvtAllA1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `ConvtOneProcA1()`.
- Source: `Pierre's Template.ipf:17711`

#### `ConvtAlldat`

```igor
Proc ConvtAlldat()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `ConvtOneProcdat()`.
- Source: `Pierre's Template.ipf:17723`

#### `ConvtAllpxt`

```igor
Proc ConvtAllpxt()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `GetIndexedObjName()`, `Center_detector()`.
- Source: `Pierre's Template.ipf:17747`

#### `ConvtAlltxt`

```igor
Proc ConvtAlltxt()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `ConvtOneProctxt()`.
- Source: `Pierre's Template.ipf:17735`

#### `ConvtOne`

```igor
Proc ConvtOne()
```
- Notes: pauseupdate silent 1 ConvtOneProc(gv_groupnum-1) End Usage: run interactively from Igor or from a panel callback. Main internal calls: `ConvtOneProc()`, `ConvtOneProctxt()`, `ConvtOneProcdat()`, `ConvtOneProcA1()`.
- Source: `Pierre's Template.ipf:17472`

#### `ConvtOneProc`

```igor
Proc ConvtOneProc(index)
```
- Usage: run from Igor with parameters `index`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `GetIndexedObjName()`, `Center_detector()`.
- Parameters:
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Pierre's Template.ipf:17491`

#### `ConvtOneProcA1`

```igor
Proc ConvtOneProcA1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `Center_detector()`.
- Source: `Pierre's Template.ipf:17516`

#### `ConvtOneProcdat`

```igor
Proc ConvtOneProcdat()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `str2num()`, `Center_detector()`.
- Source: `Pierre's Template.ipf:17546`

#### `ConvtOneProctxt`

```igor
Proc ConvtOneProctxt()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `str2num()`, `Center_detector()`.
- Source: `Pierre's Template.ipf:17619`

#### `copy_circles`

```igor
Proc copy_circles(thename)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `thename`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: thename = Enter the name for the circles. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `thename`: Enter the name for the circles
- Source: `Pierre's Template.ipf:7855`

#### `copy_circles_2f`

```igor
Proc copy_circles_2f(thename)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `thename`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: thename = Enter the name for the circles. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `thename`: Enter the name for the circles
- Source: `Pierre's Template.ipf:7888`

#### `copycut_hv`

```igor
Proc copycut_hv(hv)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `hv`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: hv = Enter the photon energy. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `hv`: Enter the photon energy
- Source: `Pierre's Template.ipf:18782`

#### `Copymap`

```igor
Proc Copymap(prefix)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `prefix`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: prefix = Prefix. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `prefix`: Prefix
- Source: `Pierre's Template.ipf:36183`

#### `Copynamehv`

```igor
Proc Copynamehv()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `comb_krange()`.
- Source: `Pierre's Template.ipf:16740`

#### `copynameshv`

```igor
Proc copynameshv()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23617`

#### `copynameshv1`

```igor
Proc copynameshv1()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23632`

#### `copynameshv346RRRL`

```igor
Proc copynameshv346RRRL()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23591`

#### `copynameshv346RRRS`

```igor
Proc copynameshv346RRRS()
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23604`

#### `Copytraces`

```igor
Proc Copytraces(prefix)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `prefix`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: prefix = Enter a prefix:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `XWaveRefFromTrace()`.
- Parameters:
  - `prefix`: Enter a prefix:
- Source: `Pierre's Template.ipf:3332`

#### `core_level_pi`

```igor
Proc core_level_pi(matrix,first,last,photon_e,Fermi_energy)
```
- Usage: run from Igor with parameters `matrix`, `first`, `last`, `photon_e`, `Fermi_energy`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matrix = Enter the name of the matrix; first = Enter the first slice; last = Enter the last slice; photon_e = Enter the photon energy (eV); Fermi_energy = Enter the Fermi energy (eV). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `matrix`: Enter the name of the matrix
  - `first`: Enter the first slice
  - `last`: Enter the last slice
  - `photon_e`: Enter the photon energy (eV)
  - `Fermi_energy`: Enter the Fermi energy (eV)
- Source: `Pierre's Template.ipf:5777`

#### `correct_fp`

```igor
Proc correct_fp(fermil)
```
- Usage: run from Igor with parameters `fermil`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: fermil = Enter the Fermi level position. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp()`.
- Parameters:
  - `fermil`: Enter the Fermi level position
- Source: `Pierre's Template.ipf:19176`

#### `correctionnameshv`

```igor
Proc correctionnameshv()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23657`

#### `createband`

```igor
Proc createband()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:5378`

#### `createbkgndremovedbkgnd`

```igor
Proc createbkgndremovedbkgnd()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `interp()`.
- Source: `Pierre's Template.ipf:8965`

#### `createresultremovedbkgnd`

```igor
Proc createresultremovedbkgnd()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `Interp()`.
- Source: `Pierre's Template.ipf:8975`

#### `curv_ini`

```igor
macro curv_ini()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Igor macro entry point for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Main internal calls: `curv_data()`.
- Source: `Pierre's Template.ipf:13513`

#### `Curv_mat_in`

```igor
Proc Curv_mat_in(this_mat)
```
- Usage: run from Igor with parameters `this_mat`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: this_mat = Enter the name of the matrix:. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `this_mat`: Enter the name of the matrix:
- Source: `Pierre's Template.ipf:12337`

#### `Curv_mat_out`

```igor
Proc Curv_mat_out(this_mat,show_or_not)
```
- Usage: run from Igor with parameters `this_mat`, `show_or_not`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: this_mat = Save under:; show_or_not = Do you want to display the info?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data.
- Parameters:
  - `this_mat`: Save under:
  - `show_or_not`: Do you want to display the info?
- Source: `Pierre's Template.ipf:12351`

#### `Curv_panel_ini`

```igor
Proc Curv_panel_ini()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Source: `Pierre's Template.ipf:12308`

#### `curv_update`

```igor
Proc curv_update()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `curv_calc_EDC()`, `curv_calc_MDC()`.
- Source: `Pierre's Template.ipf:12646`

#### `Curvature_deriv`

```igor
Proc Curvature_deriv()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `func_Curvaturex()`, `func_Curvaturey()`, `func_Curvaturexy_yx()`.
- Source: `Pierre's Template.ipf:12437`

#### `cut_edge_all`

```igor
Proc cut_edge_all()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `cut_the_edge_pi()`.
- Source: `Pierre's Template.ipf:6185`

#### `cut_the_edge_pi`

```igor
Proc cut_the_edge_pi(dataname,start_num,end_num,overwrite)
```
- Usage: run from Igor with parameters `dataname`, `start_num`, `end_num`, `overwrite`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: dataname = Enter the name of the matrix; start_num = Enter the first angle; end_num = Enter the last angle; overwrite = Do you want to overwrite the old matrix?. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `dataname`: Enter the name of the matrix
  - `start_num`: Enter the first angle
  - `end_num`: Enter the last angle
  - `overwrite`: Do you want to overwrite the old matrix?
- Source: `Pierre's Template.ipf:6165`

#### `cutdata`

```igor
Proc cutdata()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:18923`

#### `CutNaNedges`

```igor
Proc CutNaNedges(matname)
```
- Notes: Begining of Pierre's macros Usage: run from Igor with parameters `matname`. Dialog prompts: matname = Name of the matrix. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `funcCutNaNedges()`.
- Parameters:
  - `matname`: Name of the matrix
- Source: `Pierre's Template.ipf:2188`

#### `CVT2EK_exact1cut`

```igor
Proc CVT2EK_exact1cut(oldmat,Slit_orientation,fe,theta,phi,V0ornot,V0value,Showornot)
```
- Usage: run from Igor with parameters `oldmat`, `Slit_orientation`, `fe`, `theta`, `phi`, `V0ornot`, `V0value`, `Showornot`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: oldmat = Name of the matrix to transform; Slit_orientation = Slit orientation; fe = Enter the Fermi energy; theta = Enter the real theta (with Gamma at 0); phi = Enter the real phi (with Gamma at 0); V0ornot = Do you want to calculate kz?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `funcCVT2EK_exact1cutV()`, `funcCVT2EK_exact1cutH()`, `func_associate_kz()`.
- Parameters:
  - `oldmat`: Name of the matrix to transform
  - `Slit_orientation`: Slit orientation
  - `fe`: Enter the Fermi energy
  - `theta`: Enter the real theta (with Gamma at 0)
  - `phi`: Enter the real phi (with Gamma at 0)
  - `V0ornot`: Do you want to calculate kz?
  - `V0value`: If you want to calculate kz, enter V0
  - `Showornot`: Do you want to display the result
- Source: `Pierre's Template.ipf:29981`

#### `CVT2EK_exacttab`

```igor
Proc CVT2EK_exacttab(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: name = Enter the name to give to the cut. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `Wintype()`.
- Parameters:
  - `name`: Enter the name to give to the cut
- Source: `Pierre's Template.ipf:29948`

#### `CVT2EK_pi`

```igor
Proc CVT2EK_pi(oldmat,answ,fe,phi)
```
- Usage: run from Igor with parameters `oldmat`, `answ`, `fe`, `phi`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: oldmat = Enter the matrix to transform; answ = Do you want also to convert the energy?; fe = Enter the Fermi energy; phi = Enter the manipulator angle (for V polarization only). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_CVT2EK_pi()`.
- Parameters:
  - `oldmat`: Enter the matrix to transform
  - `answ`: Do you want also to convert the energy?
  - `fe`: Enter the Fermi energy
  - `phi`: Enter the manipulator angle (for V polarization only)
- Source: `Pierre's Template.ipf:30145`

#### `CVT2K_vs_hv`

```igor
Proc CVT2K_vs_hv(slitorientation,CVTornot,Showornot)
```
- Usage: run from Igor with parameters `slitorientation`, `CVTornot`, `Showornot`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: slitorientation = Slit orientation; CVTornot = Do you convert in k space; Showornot = Do you want to display the E vs k cuts. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `CVT2EK_exact1cut()`, `Ceil()`, `interp2d()`.
- Parameters:
  - `slitorientation`: Slit orientation
  - `CVTornot`: Do you convert in k space
  - `Showornot`: Do you want to display the E vs k cuts
- Source: `Pierre's Template.ipf:35546`

#### `CVT2K_vs_kz`

```igor
Proc CVT2K_vs_kz(slitorientation,Vzero,kzgridfactor)
```
- Notes: hv mapping project Usage: run from Igor with parameters `slitorientation`, `Vzero`, `kzgridfactor`. Dialog prompts: slitorientation = Slit orientation; Vzero = Enter V0; kzgridfactor = How many times more points. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_CVT2K_vs_kz_boundaries()`, `func_CVT2K_vs_kz_interp()`.
- Parameters:
  - `slitorientation`: Slit orientation
  - `Vzero`: Enter V0
  - `kzgridfactor`: How many times more points
- Source: `Pierre's Template.ipf:35435`

#### `CVT2K_vs_kz_2D`

```igor
Proc CVT2K_vs_kz_2D(matname,BEmat,Vzero,kzgridfactor,LatticeC)
```
- Usage: run from Igor with parameters `matname`, `BEmat`, `Vzero`, `kzgridfactor`, `LatticeC`.
- Notes: convert a CEM to I(kx,kz). Dialog prompts: matname = name of CEM to convert; BEmat = Binding Energy of CEM; Vzero = Enter V0; kzgridfactor = How many times more points; LatticeC = Enter Lattice Periodity along Kz. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `func_CVT2K_vs_kz_boundaries_2D()`, `func_CVT2K_vs_kz_interp_2D()`, `grid()`.
- Parameters:
  - `matname`: name of CEM to convert
  - `BEmat`: Binding Energy of CEM
  - `Vzero`: Enter V0
  - `kzgridfactor`: How many times more points
  - `LatticeC`: Enter Lattice Periodity along Kz
- Source: `Miscellaneous_Codes.ipf:1412`

#### `CVT2K_vs_kz_2D_2`

```igor
Proc CVT2K_vs_kz_2D_2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_CVT2K_vs_kz_boundaries_2D()`, `func_CVT2K_vs_kz_interp_2D()`.
- Source: `Miscellaneous_Codes.ipf:1698`

#### `deleteallpnts`

```igor
Proc deleteallpnts()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:5428`

#### `deletelastpnt`

```igor
Proc deletelastpnt()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:5403`

#### `delpntbkgndremover`

```igor
Proc delpntbkgndremover()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Main internal calls: `func_delpntbkgndremover()`, `subbkgndremover()`.
- Source: `Pierre's Template.ipf:9070`

#### `deriv_1chunkcut`

```igor
Proc deriv_1chunkcut(number,pass,boxwidth,factor, method)
```
- Usage: run from Igor with parameters `number`, `pass`, `boxwidth`, `factor`, `method`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal. Dialog prompts: number = Enter the number of the chunkcut; pass = Number of smooth pass; boxwidth = Smooth box width; factor = Curvature factor (for curvature only); method = Derivative(curvature) direction. Code behavior: duplicates or stages waves for downstream processing; smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `derivthischunkcut()`.
- Parameters:
  - `number`: Enter the number of the chunkcut
  - `pass`: Number of smooth pass
  - `boxwidth`: Smooth box width
  - `factor`: Curvature factor (for curvature only)
  - `method`: Derivative(curvature) direction
- Source: `Pierre's Template.ipf:34842`

#### `deriv_allchunkcut`

```igor
Proc deriv_allchunkcut(pass,boxwidth,factor,method)
```
- Usage: run from Igor with parameters `pass`, `boxwidth`, `factor`, `method`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal. Dialog prompts: pass = Number of smooth pass; boxwidth = Smooth box width; factor = Curvature factor (curvature only); method = Derivative(curvature) direction. Code behavior: smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `deriv_1chunkcut()`.
- Parameters:
  - `pass`: Number of smooth pass
  - `boxwidth`: Smooth box width
  - `factor`: Curvature factor (curvature only)
  - `method`: Derivative(curvature) direction
- Source: `Pierre's Template.ipf:34886`

#### `derive_all`

```igor
Proc derive_all()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `DerivMatrix()`.
- Source: `Pierre's Template.ipf:18575`

#### `DerivMat`

```igor
Proc DerivMat(OldM,num,box,choice)
```
- Usage: run from Igor with parameters `OldM`, `num`, `box`, `choice`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: OldM = Input the matrix Name; num = how many times to be smoothed; box = Input the box width; choice = choose direciton. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `DM()`.
- Parameters:
  - `OldM`: Input the matrix Name
  - `num`: how many times to be smoothed
  - `box`: Input the box width
  - `choice`: choose direciton
- Source: `Pierre's Template.ipf:2280`

#### `DisplayEfmdc`

```igor
Macro DisplayEfmdc()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Igor macro entry point for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:1992`

#### `dividemat3dkbyfermiconv`

```igor
Proc dividemat3dkbyfermiconv(temperatureK,resolution,offset)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `temperatureK`, `resolution`, `offset`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: temperatureK = Temperature (K):; resolution = Enter the system resolution (meV):; offset = Y offset. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `temperatureK`: Temperature (K):
  - `resolution`: Enter the system resolution (meV):
  - `offset`: Y offset
- Source: `Pierre's Template.ipf:8764`

#### `dividematrixbyfermi`

```igor
Proc dividematrixbyfermi(mode)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `mode`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: mode = Method. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `dividematrixbyfermieffectif()`, `dividematrixbyfermiconv()`.
- Parameters:
  - `mode`: Method
- Source: `Pierre's Template.ipf:8646`

#### `dividematrixbyfermiconv`

```igor
Proc dividematrixbyfermiconv(thisname,temperatureK,resolution,offset)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `thisname`, `temperatureK`, `resolution`, `offset`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thisname = Name of matrix:; temperatureK = Temperature (K):; resolution = Enter the system resolution (meV):; offset = Y offset. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `thisname`: Name of matrix:
  - `temperatureK`: Temperature (K):
  - `resolution`: Enter the system resolution (meV):
  - `offset`: Y offset
- Source: `Pierre's Template.ipf:8699`

#### `dividematrixbyfermieffectif`

```igor
Proc dividematrixbyfermieffectif(thisname,temperature,offset)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `thisname`, `temperature`, `offset`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thisname = Name of matrix:; temperature = Effective temperature (meV):; offset = Y offset. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `thisname`: Name of matrix:
  - `temperature`: Effective temperature (meV):
  - `offset`: Y offset
- Source: `Pierre's Template.ipf:8740`

#### `dividewavebyfermi`

```igor
Proc dividewavebyfermi(mode)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `mode`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: mode = Method. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `dividewavebyfermieffectif()`, `dividewavebyfermiconv()`.
- Parameters:
  - `mode`: Method
- Source: `Pierre's Template.ipf:8634`

#### `dividewavebyfermiconv`

```igor
Proc dividewavebyfermiconv(thisname,temperatureK,resolution,offset)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `thisname`, `temperatureK`, `resolution`, `offset`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thisname = Name of wave:; temperatureK = Temperature (K):; resolution = Enter the system resolution (meV):; offset = Y offset. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `thisname`: Name of wave:
  - `temperatureK`: Temperature (K):
  - `resolution`: Enter the system resolution (meV):
  - `offset`: Y offset
- Source: `Pierre's Template.ipf:8658`

#### `dividewavebyfermieffectif`

```igor
Proc dividewavebyfermieffectif(thisname,temperature,offset)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `thisname`, `temperature`, `offset`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thisname = Name of wave:; temperature = Effective temperature (meV):; offset = Y offset. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `thisname`: Name of wave:
  - `temperature`: Effective temperature (meV):
  - `offset`: Y offset
- Source: `Pierre's Template.ipf:8615`

#### `doallcvt2k`

```igor
Proc doallcvt2k()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Main internal calls: `rescale_pi()`, `cvt2ek_pi()`.
- Source: `Pierre's Template.ipf:30878`

#### `doallkvd`

```igor
Proc doallkvd()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `derivmat()`, `wave2filenokill()`.
- Source: `Pierre's Template.ipf:18836`

#### `DOS_kf`

```igor
Proc DOS_kf(Fermi_level,avg_num,backgnd_type,num_back,normfrom,normto,smoothmax,neworold)
```
- Usage: run from Igor with parameters `Fermi_level`, `avg_num`, `backgnd_type`, `num_back`, `normfrom`, `normto`, `smoothmax`, `neworold`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: Fermi_level = Fermi level:; avg_num = Number of averged EDCs (odd):; backgnd_type = Background type:; num_back = Points for background fitting:; normfrom = Norm from (-1=no norm):; normto = Norm to (index):. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `update_making_fs()`, `PutonsameXcorner()`, `Putkfin1zone()`, `Putinsameoctant()`, `Calcangleinoctant()`, `wi
- Parameters:
  - `Fermi_level`: Fermi level:
  - `avg_num`: Number of averged EDCs (odd):
  - `backgnd_type`: Background type:
  - `num_back`: Points for background fitting:
  - `normfrom`: Norm from (-1=no norm):
  - `normto`: Norm to (index):
  - `smoothmax`: MAX (-1=NO) smoothing IF necessary (danger)
  - `neworold`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:3858`

#### `double_elipse`

```igor
Proc double_elipse(waveangle,wavesize)
```
- Usage: run from Igor with parameters `waveangle`, `wavesize`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: waveangle = Enter the wave with the angles; wavesize = Enter the wave with the sizes. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; asks the user for parameters through Igor prompts.
- Parameters:
  - `waveangle`: Enter the wave with the angles
  - `wavesize`: Enter the wave with the sizes
- Source: `Pierre's Template.ipf:16012`

#### `Drawcone`

```igor
Proc Drawcone()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:15210`

#### `drawkline`

```igor
Proc drawkline(linename,mat)
```
- Usage: run from Igor with parameters `linename`, `mat`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Dialog prompts: linename = Enter the name of the new line; mat = Enter the name of the cut. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `linename`: Enter the name of the new line
  - `mat`: Enter the name of the cut
- Source: `Pierre's Template.ipf:19900`

#### `duplicateemdcc`

```igor
proc duplicateemdcc(name)
```
- Purpose: copies, duplicates, or renames Igor waves/traces.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:11803`

#### `EDC_GvsM`

```igor
Proc EDC_GvsM()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `comb_krange()`, `transform2equiv_waves()`, `rgb()`.
- Source: `Pierre's Template.ipf:16656`

#### `EDC_pi`

```igor
Proc EDC_pi(thismatname,addnum)
```
- Usage: run from Igor with parameters `thismatname`, `addnum`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thismatname = Input the matrix for the EDCs; addnum = Input the number of combined edc curves. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `num2istr()`.
- Parameters:
  - `thismatname`: Input the matrix for the EDCs
  - `addnum`: Input the number of combined edc curves
- Source: `Pierre's Template.ipf:4690`

#### `edc_to_zero_pi`

```igor
Proc edc_to_zero_pi(mat,start,Combined)
```
- Usage: run from Igor with parameters `mat`, `start`, `Combined`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; Start = Enter the number of the starting energy index; Combined = Enter the number of combined EDC. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `num2istr()`.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `start`: Start point, index, or coordinate for the selected range.
  - `Combined`: Enter the number of combined EDC
- Source: `Pierre's Template.ipf:3420`

#### `EfMdcArea`

```igor
Proc EfMdcArea()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `mean()`.
- Source: `Pierre's Template.ipf:2046`

#### `ellipse4silver`

```igor
Proc ellipse4silver()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:15648`

#### `EMDC_changeEDC`

```igor
Proc EMDC_changeEDC()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; smooths wave data. Main internal calls: `EMDC_adjustEDClines()`, `transform2equiv_waves()`.
- Source: `Pierre's Template.ipf:11534`

#### `EMDC_changeMDC`

```igor
Proc EMDC_changeMDC()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data. Main internal calls: `EMDC_adjustMDClines()`.
- Source: `Pierre's Template.ipf:11632`

#### `EMDC_ini`

```igor
Proc EMDC_ini()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion.
- Source: `Pierre's Template.ipf:11483`

#### `EMDC_insertmat`

```igor
Proc EMDC_insertmat(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: matname = Insert the name of the matrix. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `EMDC_changeEDC()`, `EMDC_putVline()`.
- Parameters:
  - `matname`: Insert the name of the matrix
- Source: `Pierre's Template.ipf:11748`

#### `EMDC_putVline`

```igor
Proc EMDC_putVline()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:11780`

#### `EMDC_Showmulti`

```igor
Proc EMDC_Showmulti()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`, `EMDC_multiDisplay()`, `color_edc()`.
- Source: `Pierre's Template.ipf:11862`

#### `EMDC_Showwave`

```igor
Proc EMDC_Showwave()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:11790`

#### `ES_polar_pi`

```igor
Proc ES_polar_pi(energy,energy_add)
```
- Usage: run from Igor with parameters `energy`, `energy_add`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: energy = Enter the energy at which you want to make a 2D surface image; energy_add = Enter the number of curves you want to add below and above this energy. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `energy`: Enter the energy at which you want to make a 2D surface image
  - `energy_add`: Enter the number of curves you want to add below and above this energy
- Source: `Pierre's Template.ipf:29734`

#### `extract_exp_res`

```igor
Proc extract_exp_res(subs,fitpnts)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `subs`, `fitpnts`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: subs = Enter the substitution factor; fitpnts = Enter the number of points to use in the fit. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; smooths wave data.
- Parameters:
  - `subs`: Enter the substitution factor
  - `fitpnts`: Enter the number of points to use in the fit
- Source: `Pierre's Template.ipf:20446`

#### `factor_src_all`

```igor
Proc factor_src_all()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:8297`

#### `factor_src_one`

```igor
Proc factor_src_one(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Dialog prompts: number = Enter the data number. Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `number`: Enter the data number
- Source: `Pierre's Template.ipf:8346`

#### `Fe_resonance`

```igor
Proc Fe_resonance(work_function)
```
- Usage: run from Igor with parameters `work_function`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: work_function = Enter the work function. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `sub_blineedc_pi()`.
- Parameters:
  - `work_function`: Enter the work function
- Source: `Pierre's Template.ipf:15308`

#### `Fe_resonance_long`

```igor
Proc Fe_resonance_long(work_function)
```
- Usage: run from Igor with parameters `work_function`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: work_function = Enter the work function. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `sub_blineedc_pi()`.
- Parameters:
  - `work_function`: Enter the work function
- Source: `Pierre's Template.ipf:15352`

#### `fedorov_fact`

```igor
Proc fedorov_fact()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:6455`

#### `fermi_au`

```igor
Proc fermi_au(thiswave)
```
- Usage: run from Igor with parameters `thiswave`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thiswave = Enter the name of the wave. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data. Main internal calls: `pcsr()`, `xcsr()`, `Vcsr()`, `thiswave()`.
- Parameters:
  - `thiswave`: Enter the name of the wave
- Source: `Pierre's Template.ipf:18798`

#### `FeTeTdep`

```igor
Proc FeTeTdep(T1index,T2index, combined,shownumber, Flevel)
```
- Usage: run from Igor with parameters `T1index`, `T2index`, `combined`, `shownumber`, `Flevel`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: T1index = Index of T1; T2index = Index of T2; combined = number of EDCs to combine:; shownumber = Show every...:; Flevel = Fermi level:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `rgb()`.
- Parameters:
  - `T1index`: Index of T1
  - `T2index`: Index of T2
  - `combined`: number of EDCs to combine:
  - `shownumber`: Show every...:
  - `Flevel`: Fermi level:
- Source: `Pierre's Template.ipf:15532`

#### `FeTeTdepslice`

```igor
Proc FeTeTdepslice(thisslice, combined,Flevel)
```
- Usage: run from Igor with parameters `thisslice`, `combined`, `Flevel`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: thisslice = Slice:; combined = number of EDCs to combine:; Flevel = Flevel:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `transform2equiv_waves()`, `color_edc()`.
- Parameters:
  - `thisslice`: Slice:
  - `combined`: number of EDCs to combine:
  - `Flevel`: Flevel:
- Source: `Pierre's Template.ipf:15590`

#### `FFTTwistanglemap`

```igor
Proc FFTTwistanglemap(name,Notes)
```
- Usage: run from Igor with parameters `name`, `Notes`.
- Notes: Pre-FFT Purpose: performs Fourier, filtering, phase, or lock-in processing. Dialog prompts: name = Name of wave to extract twist angle map; notes = Note. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `FFTranglemap()`.
- Parameters:
  - `name`: Name of wave to extract twist angle map
  - `Notes`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Triangle Lattice_Graphene_like.ipf:28`

#### `find_kf_pi`

```igor
Proc find_kf_pi(MatrixName,Combined)
```
- Usage: run from Igor with parameters `MatrixName`, `Combined`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: MatrixName = Input the name of the matrix; Combined = Input the number of combined edc curves. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `pcsr()`, `vcsr()`, `num2istr()`, `wintype()`, `Interp()`.
- Parameters:
  - `MatrixName`: Input the name of the matrix
  - `Combined`: Input the number of combined edc curves
- Source: `Pierre's Template.ipf:4762`

#### `find_ycomp`

```igor
Proc find_ycomp(name,angle,ef,factor)
```
- Usage: run from Igor with parameters `name`, `angle`, `ef`, `factor`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: name = Enter the name of the vector; angle = Enter the phi angle; ef = Enter the kinetic energy at Ef; factor = Enter the conversion factor. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the vector
  - `angle`: Enter the phi angle
  - `ef`: Enter the kinetic energy at Ef
  - `factor`: Enter the conversion factor
- Source: `Pierre's Template.ipf:18944`

#### `finddetails`

```igor
Proc finddetails()//for YBCO
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `str2num()`.
- Source: `Pierre's Template.ipf:20912`

#### `finddispersion_min`

```igor
Proc finddispersion_min(name,peak_name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `peak_name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: name = Enter the name of the cut; peak_name = Enter the name of the peak. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `pcsr()`, `vcsr()`, `xcsr()`.
- Parameters:
  - `name`: Enter the name of the cut
  - `peak_name`: Enter the name of the peak
- Source: `Pierre's Template.ipf:5245`

#### `finddispersion_plus`

```igor
Proc finddispersion_plus(name,peak_name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `peak_name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: name = Enter the name of the cut; peak_name = Enter the name of the peak. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `pcsr()`, `vcsr()`, `xcsr()`.
- Parameters:
  - `name`: Enter the name of the cut
  - `peak_name`: Enter the name of the peak
- Source: `Pierre's Template.ipf:5274`

#### `finddispersionHD_min`

```igor
Proc finddispersionHD_min(name,peak_name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `peak_name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: name = Enter the name of the cut; peak_name = Enter the name of the peak. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `qcsr()`, `xcsr()`, `vcsr()`.
- Parameters:
  - `name`: Enter the name of the cut
  - `peak_name`: Enter the name of the peak
- Source: `Pierre's Template.ipf:5304`

#### `finddispersionHD_plus`

```igor
Proc finddispersionHD_plus(name,peak_name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `peak_name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: name = Enter the name of the cut; peak_name = Enter the name of the peak. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `qcsr()`, `xcsr()`, `vcsr()`.
- Parameters:
  - `name`: Enter the name of the cut
  - `peak_name`: Enter the name of the peak
- Source: `Pierre's Template.ipf:5335`

#### `findEres`

```igor
Proc findEres(thiswave, Temperature, largest_res, choiceofrange,fitfrom,fitto, displayornot)
```
- Usage: run from Igor with parameters `thiswave`, `Temperature`, `largest_res`, `choiceofrange`, `fitfrom`, `fitto`, `displayornot`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: thiswave = Name of the wave:; Temperature = Temperature (K); largest_res = Largest resolution (meV):; choiceofrange = Do you use cursors?; fitfrom = Fit from (eV):; fitto = Fit to (eV):. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `xcsr()`, `x2pnt()`, `thiswave()`, `func_findEres()`.
- Parameters:
  - `thiswave`: Name of the wave:
  - `Temperature`: Temperature (K)
  - `largest_res`: Largest resolution (meV):
  - `choiceofrange`: Do you use cursors?
  - `fitfrom`: Fit from (eV):
  - `fitto`: Fit to (eV):
  - `displayornot`: Do you want to display?
- Source: `Pierre's Template.ipf:8371`

#### `FindgapangleBKFA`

```igor
Proc FindgapangleBKFA(nameFS)   //not finished
```
- Usage: run from Igor with parameters `nameFS`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: nameFS = Name of FS:. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `nameedc()`.
- Parameters:
  - `nameFS`: Name of FS:
- Source: `Pierre's Template.ipf:14320`

#### `findmaxonedc`

```igor
Proc findmaxonedc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data; differentiates wave data. Main internal calls: `x2pnt()`.
- Source: `Pierre's Template.ipf:20364`

#### `Fit_Au`

```igor
Proc Fit_Au()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; formats graph axes, labels, colors, or annotations. Main internal calls: `vcsr()`, `hcsr()`, `num2istr()`, `waveName()`, `xcsr()`, `GetWaveOffset()`.
- Source: `Pierre's Template.ipf:2088`

#### `fit_vb_bl_fixed`

```igor
Proc fit_vb_bl_fixed(name_wave,xstart,xend)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name_wave`, `xstart`, `xend`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: name_wave = Enter the name of the wave; xstart = Fit to:. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `name_wave()`, `lorgauss_bl_fixed()`.
- Parameters:
  - `name_wave`: Enter the name of the wave
  - `xstart`: Fit to:
  - `xend`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:17979`

#### `fit_vb_gen`

```igor
Proc fit_vb_gen(name_wave,total_peaks,xstart,xend)
```
- Usage: run from Igor with parameters `name_wave`, `total_peaks`, `xstart`, `xend`.
- Notes: unchecked Purpose: fits or extracts spectral/peak parameters. Dialog prompts: name_wave = Enter the name of the wave; total_peaks = Enter the number of peaks; xstart = Fit to:. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `Define_pk()`, `name_wave()`, `lorgauss()`.
- Parameters:
  - `name_wave`: Enter the name of the wave
  - `total_peaks`: Enter the number of peaks
  - `xstart`: Fit to:
  - `xend`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:17925`

#### `fitbilayer`

```igor
Proc fitbilayer(name,num)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`, `num`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the sample; num = Enter the number of the table. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `SNT()`, `fitwave()`, `funclorentz3()`.
- Parameters:
  - `name`: Enter the name of the sample
  - `num`: Enter the number of the table
- Source: `Pierre's Template.ipf:18302`

#### `FitEfMdc`

```igor
Macro FitEfMdc()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Igor macro entry point for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces. Main internal calls: `efmw()`, `xcsr()`.
- Source: `Pierre's Template.ipf:1974`

#### `fittemp`

```igor
Proc fittemp()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `xcsr()`, `pcsr()`, `vcsr()`, `thiswave()`, `lorentzfermi()`.
- Source: `Pierre's Template.ipf:19795`

#### `fitwithbgnd`

```igor
Proc fitwithbgnd(name_wave,xstart,xend,number)//not finished yet
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name_wave`, `xstart`, `xend`, `number`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: name_wave = Enter the name of the wave; xstart = Fit to (1000=automatic):; number = Number of the fitted range. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `no_bgnd()`, `lorgauss()`, `rgb()`, `lsize()`.
- Parameters:
  - `name_wave`: Enter the name of the wave
  - `xstart`: Fit to (1000=automatic):
  - `xend`: End point, index, or coordinate for the selected range.
  - `number`: Number of the fitted range
- Source: `Pierre's Template.ipf:18191`

#### `Flip_digimage`

```igor
Proc Flip_digimage(matname)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: matname = Enter the name of the image. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `func_Flip_digimage()`.
- Parameters:
  - `matname`: Enter the name of the image
- Source: `Pierre's Template.ipf:6528`

#### `fsurf`

```igor
Proc fsurf(matname,result_no)
```
- Usage: run from Igor with parameters `matname`, `result_no`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; result_no = Enter the number of the result. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `xcsr()`, `qcsr()`, `vcsr()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `result_no`: Enter the number of the result
- Source: `Pierre's Template.ipf:7690`

#### `fuck_ostie`

```igor
Proc fuck_ostie(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: number = Enter the energy. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `number`: Enter the energy
- Source: `Pierre's Template.ipf:22507`

#### `funcfindangleBKFA`

```igor
Proc funcfindangleBKFA()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `funcfind1angleBKFA()`.
- Source: `Pierre's Template.ipf:14460`

#### `funimage`

```igor
Proc funimage()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:19096`

#### `funimage1`

```igor
Proc funimage1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:19111`

#### `Ga2O3_1`

```igor
Proc Ga2O3_1(number,maxi)
```
- Usage: run from Igor with parameters `number`, `maxi`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: number = Enter the data index; maxi = Maximum. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `number`: Enter the data index
  - `maxi`: Maximum
- Source: `Pierre's Template.ipf:23157`

#### `Ga2O3_10`

```igor
Proc Ga2O3_10()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:23356`

#### `Ga2O3_11`

```igor
Proc Ga2O3_11()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:23376`

#### `Ga2O3_13`

```igor
Proc Ga2O3_13()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:23390`

#### `Ga2O3_14`

```igor
Proc Ga2O3_14()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `thiswave()`.
- Source: `Pierre's Template.ipf:23415`

#### `Ga2O3_15`

```igor
Proc Ga2O3_15()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:23463`

#### `Ga2O3_2`

```igor
Proc Ga2O3_2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:23177`

#### `Ga2O3_3`

```igor
Proc Ga2O3_3()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23191`

#### `Ga2O3_4`

```igor
Proc Ga2O3_4(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: number = Enter the photon energy. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `interp()`, `wave2filenokill()`.
- Parameters:
  - `number`: Enter the photon energy
- Source: `Pierre's Template.ipf:23205`

#### `Ga2O3_5`

```igor
Proc Ga2O3_5()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays. Main internal calls: `comb_krange()`.
- Source: `Pierre's Template.ipf:23243`

#### `Ga2O3_6`

```igor
Proc Ga2O3_6()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; extracts image/wave statistics.
- Source: `Pierre's Template.ipf:23258`

#### `Ga2O3_7`

```igor
Proc Ga2O3_7()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:23272`

#### `Ga2O3_8`

```igor
Proc Ga2O3_8()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics.
- Source: `Pierre's Template.ipf:23290`

#### `Ga2O3_9`

```igor
Proc Ga2O3_9()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `CVT2EK_exact1cut()`, `wave2filenokill()`, `comb_krange()`.
- Source: `Pierre's Template.ipf:23326`

#### `Ga2O3adjustEF`

```igor
Proc Ga2O3adjustEF()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `transform2equiv_waves()`.
- Source: `Pierre's Template.ipf:23885`

#### `Ga2O3backgrounddata18`

```igor
Proc Ga2O3backgrounddata18()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics.
- Source: `Pierre's Template.ipf:23919`

#### `Ga2O3coorectone`

```igor
Proc Ga2O3coorectone(number,method,points)
```
- Usage: run from Igor with parameters `number`, `method`, `points`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: number = Enter hv; method = Method; points = Number of points. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics; asks the user for parameters through Igor prompts.
- Parameters:
  - `number`: Enter hv
  - `method`: Method
  - `points`: Number of points
- Source: `Pierre's Template.ipf:23844`

#### `Ga2O3Normall`

```igor
Proc Ga2O3Normall()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:23869`

#### `Ga2O3weightabove`

```igor
Proc Ga2O3weightabove()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; extracts image/wave statistics.
- Source: `Pierre's Template.ipf:23829`

#### `Generate_chunklist`

```igor
Proc Generate_chunklist(imtheta,imphi1,imphi2,number)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `imtheta`, `imphi1`, `imphi2`, `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: imtheta = Enter the theta angle from Image_Table; imphi1 = Enter the lowest phi angle from Image_Table; imphi2 = Enter the highest phi angle from Image_Table; number = Number of the chunkcut. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Func_Generate_chunklist()`, `str2num()`.
- Parameters:
  - `imtheta`: Enter the theta angle from Image_Table
  - `imphi1`: Enter the lowest phi angle from Image_Table
  - `imphi2`: Enter the highest phi angle from Image_Table
  - `number`: Number of the chunkcut
- Source: `Pierre's Template.ipf:34250`

#### `get_fermi_profile`

```igor
Proc get_fermi_profile(matname,smooth1,smooth2,method,putongraph)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `matname`, `smooth1`, `smooth2`, `method`, `putongraph`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: matname = Enter the name of the matrix; smooth1 = Enter the number of smooth operations; smooth2 = Enter the smooth box width; method = Which method; putongraph = Do you want to append to the graph?. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `qcsr()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `smooth1`: Enter the number of smooth operations
  - `smooth2`: Enter the smooth box width
  - `method`: Which method
  - `putongraph`: Do you want to append to the graph?
- Source: `Pierre's Template.ipf:7971`

#### `Get_ledge_edc`

```igor
Proc Get_ledge_edc(edcname,fermi_level,search_halfwindow)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `edcname`, `fermi_level`, `search_halfwindow`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: edcname = Name of the wave:; fermi_level = Fermi level:; search_halfwindow = Enter the half window size:. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data; differentiates wave data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `edcname`: Name of the wave:
  - `fermi_level`: Fermi level:
  - `search_halfwindow`: Enter the half window size:
- Source: `Pierre's Template.ipf:3372`

#### `Get_ledge_kf`

```igor
Proc Get_ledge_kf(numedc,fermi_level,search_halfwindow,method,smoothlevel,backgnd_type,num_back,neworold)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `numedc`, `fermi_level`, `search_halfwindow`, `method`, `smoothlevel`, `backgnd_type`, `num_back`, `neworold`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: numedc = Number of EDC to combine (odd):; fermi_level = Fermi level:; search_halfwindow = Enter the half window size:; method = Method; smoothlevel = Smoothlevel (-1=no smoothing):; backgnd_type = Type of background. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `update_making_fs()`, 
- Parameters:
  - `numedc`: Number of EDC to combine (odd):
  - `fermi_level`: Fermi level:
  - `search_halfwindow`: Enter the half window size:
  - `method`: Method
  - `smoothlevel`: Smoothlevel (-1=no smoothing):
  - `backgnd_type`: Type of background
  - `num_back`: Points for background fitting
  - `neworold`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:28855`

#### `GetAverageWidth`

```igor
Macro GetAverageWidth()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Igor macro entry point in `Pierre's Template.ipf`.
- Source: `Pierre's Template.ipf:2021`

#### `GetSigma`

```igor
Macro GetSigma(index)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `index`.
- Notes: Igor macro entry point for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `smoothntimes()`, `pnt2x()`, `mean()`, `rgb()`, `num2istr()`, `Num2istr()`.
- Parameters:
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Pierre's Template.ipf:1542`

#### `getslopes`

```igor
Proc getslopes()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:18686`

#### `GetWidth`

```igor
Macro GetWidth()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Igor macro entry point for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Main internal calls: `mean()`.
- Source: `Pierre's Template.ipf:2007`

#### `give_area`

```igor
Proc give_area(namex,namey,sizegrid)
```
- Usage: run from Igor with parameters `namex`, `namey`, `sizegrid`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; smoothing, normalization, or background removal; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: namex = Enter the name of the wave in x:; namey = Enter the name of the wave in y:; sizegrid = Enter the number of rows and columns for the grid. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; asks the user for parameters through Igor prompts. Main internal calls: `func_give_area1()`, `func_give_area2()`.
- Parameters:
  - `namex`: Enter the name of the wave in x:
  - `namey`: Enter the name of the wave in y:
  - `sizegrid`: Enter the number of rows and columns for the grid
- Source: `Pierre's Template.ipf:7097`

#### `give_area_old`

```igor
Proc give_area_old(namex,namey,sizegrid)
```
- Usage: run from Igor with parameters `namex`, `namey`, `sizegrid`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; smoothing, normalization, or background removal; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: namex = Enter the name of the wave in x:; namey = Enter the name of the wave in y:; sizegrid = Enter the number of rows and columns for the grid. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_give_area1_old()`, `func_give_area2_old()`.
- Parameters:
  - `namex`: Enter the name of the wave in x:
  - `namey`: Enter the name of the wave in y:
  - `sizegrid`: Enter the number of rows and columns for the grid
- Source: `Pierre's Template.ipf:7310`

#### `give_vol_lac`

```igor
Proc give_vol_lac(namex,namey,sizegrid,measure_x,measure_y,measure_deep)
```
- Usage: run from Igor with parameters `namex`, `namey`, `sizegrid`, `measure_x`, `measure_y`, `measure_deep`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Dialog prompts: namex = Enter the name of the wave in x:; namey = Enter the name of the wave in y:; sizegrid = Enter the number of rows and columns for the grid; measure_x = Enter the position x-wave name:; measure_y = Enter the position y-wave name:; measure_deep = Enter the deepness wave name:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; asks the user for parameters through Igor prompts. Main internal calls: `func_vol_lac0()`, `func_vol_lac1()`, `func_vol_lac2()`, `func_vol_lac3()`, `func_vol_
- Parameters:
  - `namex`: Enter the name of the wave in x:
  - `namey`: Enter the name of the wave in y:
  - `sizegrid`: Enter the number of rows and columns for the grid
  - `measure_x`: Enter the position x-wave name:
  - `measure_y`: Enter the position y-wave name:
  - `measure_deep`: Enter the deepness wave name:
- Source: `Pierre's Template.ipf:21320`

#### `global_mu_incset`

```igor
Proc global_mu_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32069`

#### `global_mu_set`

```igor
Proc global_mu_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32075`

#### `global_t_incset`

```igor
Proc global_t_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32087`

#### `global_t_set`

```igor
Proc global_t_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32093`

#### `global_tprime_incset`

```igor
Proc global_tprime_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32105`

#### `global_tprime_set`

```igor
Proc global_tprime_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32111`

#### `goldfermiall`

```igor
Proc goldfermiall()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; extracts image/wave statistics; smooths wave data; differentiates wave data. Main internal calls: `combine_range()`.
- Source: `Pierre's Template.ipf:19951`

#### `GXMG`

```igor
Proc GXMG(name)//Plot the tigh-binding band along Gamma-X-M-Gamma
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: name = Enter the name of the sample. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `tbfunc()`.
- Parameters:
  - `name`: Enter the name of the sample
- Source: `Pierre's Template.ipf:8025`

#### `GXMG_image`

```igor
Proc GXMG_image()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:19316`

#### `haha`

```igor
proc haha(name,number,lattice,angle,point,dis,energystart,energydelta)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`, `dis`, `energystart`, `energydelta`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter; dis = size of display +- A-1. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `allqpihaha()`, `layoutqpi()`, `layoutdata()`, `layoutc4()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
  - `dis`: size of display +- A-1
  - `energystart`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `energydelta`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:4337`

#### `hahar`

```igor
proc hahar(name,number,lattice,angle,point,dis)
```
- Usage: run from Igor with parameters `name`, `number`, `lattice`, `angle`, `point`, `dis`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name = Batch Name; number = Data number; lattice = Lattice constance (A); angle = angle of lattice to x+; point = Points of Interp FIlter; dis = size of display +- A-1. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `allqpihahar()`, `layoutqpir()`, `layoutdatar()`, `layoutc4r()`.
- Parameters:
  - `name`: Batch Name
  - `number`: Data number
  - `lattice`: Lattice constance (A)
  - `angle`: angle of lattice to x+
  - `point`: Points of Interp FIlter
  - `dis`: size of display +- A-1
- Source: `Miscellaneous_Codes.ipf:3571`

#### `Halfslit`

```igor
Proc Halfslit(from,to)
```
- Usage: run from Igor with parameters `from`, `to`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Dialog prompts: from = From; to = To. Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `from`: From
  - `to`: To
- Source: `Pierre's Template.ipf:16853`

#### `highlight1`

```igor
Proc highlight1(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `interp2D()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:16758`

#### `homework2b`

```igor
Proc homework2b(mat)
```
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `Ceil()`.
- Parameters:
  - `mat`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:20961`

#### `homework3a`

```igor
Proc homework3a(boxwidth,boxheight,boxoffset,boxcenter,pourcentage_noise) // must have noise_fct
```
- Usage: run from Igor with parameters `boxwidth`, `boxheight`, `boxoffset`, `boxcenter`, `pourcentage_noise`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: boxwidth = Enter the width of the box; boxheight = Enter the heigth of the box; boxoffset = Enter the vertical offset; boxcenter = Enter the horizontal offset; pourcentage_noise = Enter the % of noise. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics. Main internal calls: `file2wave()`, `Interp()`.
- Parameters:
  - `boxwidth`: Enter the width of the box
  - `boxheight`: Enter the heigth of the box
  - `boxoffset`: Enter the vertical offset
  - `boxcenter`: Enter the horizontal offset
  - `pourcentage_noise`: Enter the % of noise
- Source: `Pierre's Template.ipf:21016`

#### `homework3b`

```igor
Proc homework3b(name,boxwidth,boxheight,boxoffset,boxcenter)
```
- Usage: run from Igor with parameters `name`, `boxwidth`, `boxheight`, `boxoffset`, `boxcenter`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the wave to fit; boxwidth = Enter the width of the box; boxheight = Enter the heigth of the box; boxoffset = Enter the vertical offset; boxcenter = Enter the horizontal offset. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `xcsr()`, `name()`, `boxfunc()`, `rgb()`.
- Parameters:
  - `name`: Enter the name of the wave to fit
  - `boxwidth`: Enter the width of the box
  - `boxheight`: Enter the heigth of the box
  - `boxoffset`: Enter the vertical offset
  - `boxcenter`: Enter the horizontal offset
- Source: `Pierre's Template.ipf:21063`

#### `homework5a`

```igor
Proc homework5a(namex,namey,sizegrid,thenumber)
```
- Usage: run from Igor with parameters `namex`, `namey`, `sizegrid`, `thenumber`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; smoothing, normalization, or background removal; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: namex = Enter the name of the wave in x:; namey = Enter the name of the wave in y:; sizegrid = Enter the number of rows and columns for the grid; thenumber = Enter the number of pnts for calc.. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_homework5a()`, `mode()`, `marker()`, `msize()`, `rgb()`.
- Parameters:
  - `namex`: Enter the name of the wave in x:
  - `namey`: Enter the name of the wave in y:
  - `sizegrid`: Enter the number of rows and columns for the grid
  - `thenumber`: Enter the number of pnts for calc.
- Source: `Pierre's Template.ipf:21115`

#### `Honeycomb`

```igor
Proc Honeycomb()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:32999`

#### `HWL_makemat`

```igor
Proc HWL_makemat(forx,fory,forz,matxyzraw)
```
- Notes: Wenwen related Usage: run from Igor with parameters `forx`, `fory`, `forz`, `matxyzraw`. Dialog prompts: forx = Enter the wave in x; fory = Enter the wave in x; forz = Enter the wave in x. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `forx`: Enter the wave in x
  - `fory`: Enter the wave in x
  - `forz`: Enter the wave in x
  - `matxyzraw`: Matrix/2D wave input or matrix output name.
- Source: `Pierre's Template.ipf:23947`

#### `HWL_xyz`

```igor
Proc HWL_xyz(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: matname = Input name. Code behavior: creates output waves; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Input name
- Source: `Pierre's Template.ipf:23985`

#### `increase_NTP_num_max`

```igor
Proc increase_NTP_num_max()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:2612`

#### `inf0`

```igor
Proc inf0(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:7507`

#### `ini_image_fs`

```igor
Proc ini_image_fs()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:31206`

#### `ini_image_fs2f`

```igor
Proc ini_image_fs2f()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:31250`

#### `ini_image_fsHex`

```igor
Proc ini_image_fsHex()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:33620`

#### `ini_making_fs`

```igor
Proc ini_making_fs()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:30992`

#### `ini_making_fs2f`

```igor
Proc ini_making_fs2f()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:31046`

#### `ini_making_fsH`

```igor
Proc ini_making_fsH()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:31098`

#### `ini_predict_cuts`

```igor
Proc ini_predict_cuts()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:31138`

#### `Initialize_Global_Variables`

```igor
Proc Initialize_Global_Variables()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates, switches, or removes Igor data folders.
- Source: `Pierre's Template.ipf:17423`

#### `integrate_VB_range`

```igor
Proc integrate_VB_range(xlow,xup)
```
- Usage: run from Igor with parameters `xlow`, `xup`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: xlow = Average from:; xup = to:. Code behavior: creates output waves; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `mean()`.
- Parameters:
  - `xlow`: Average from:
  - `xup`: to:
- Source: `Pierre's Template.ipf:5175`

#### `JAPS1`

```igor
Proc JAPS1(matname,Eslice_first,Eslice_last,kleft,kright,combMDC)
```
- Usage: run from Igor with parameters `matname`, `Eslice_first`, `Eslice_last`, `kleft`, `kright`, `combMDC`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matname = Enter the name of the matrix; Eslice_first = First Energy slice:; Eslice_last = Last Energy slice:; kleft = Momentum left:; kright = Momentum right:; combMDC = Number of MDC to combine (odd). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `thiswave()`, `rgb()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `Eslice_first`: First Energy slice:
  - `Eslice_last`: Last Energy slice:
  - `kleft`: Momentum left:
  - `kright`: Momentum right:
  - `combMDC`: Number of MDC to combine (odd)
- Source: `Pierre's Template.ipf:21886`

#### `JAPS2`

```igor
Proc JAPS2(matname,Eslice_first,Eslice_last,kleft,kright,combMDC)
```
- Usage: run from Igor with parameters `matname`, `Eslice_first`, `Eslice_last`, `kleft`, `kright`, `combMDC`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matname = Enter the name of the matrix; Eslice_first = First Energy slice:; Eslice_last = Last Energy slice:; kleft = Momentum left:; kright = Momentum right:; combMDC = Number of MDC to combine (odd). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `thiswave()`, `rgb()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `Eslice_first`: First Energy slice:
  - `Eslice_last`: Last Energy slice:
  - `kleft`: Momentum left:
  - `kright`: Momentum right:
  - `combMDC`: Number of MDC to combine (odd)
- Source: `Pierre's Template.ipf:22000`

#### `KE1atEF_set`

```igor
Proc KE1atEF_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32057`

#### `KEatEF_set`

```igor
Proc KEatEF_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32051`

#### `keep_negative`

```igor
Proc keep_negative(matname,Replaceby)
```
- Usage: run from Igor with parameters `matname`, `Replaceby`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; replaceby = Replace by. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `func_keep_negative()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `Replaceby`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:8803`

#### `keep_positive`

```igor
Proc keep_positive(matname,replaceby)
```
- Usage: run from Igor with parameters `matname`, `replaceby`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; replaceby = Replace by. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `func_keep_positive()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `replaceby`: Replace by
- Source: `Pierre's Template.ipf:8834`

#### `kly_Kzmap`

```igor
Proc kly_Kzmap(initial_energy,E_delta,Efi,Eff,phii,phif,map_numcuts,initial_index)
```
- Usage: run from Igor with parameters `initial_energy`, `E_delta`, `Efi`, `Eff`, `phii`, `phif`, `map_numcuts`, `initial_index`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: initial_energy = Energy of mapping begining; E_delta = Energy delta; initial_index = index of data begining; map_numcuts = number of energy cuts; Eff = Ef of end data; Efi = Ef of initial data. Code behavior: duplicates or stages waves for downstream processing; reshapes existing waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `make_table_hv_kz()`, `CVT2K_vs_hv()`, `Wintype()`.
- Parameters:
  - `initial_energy`: Energy of mapping begining
  - `E_delta`: Energy delta
  - `Efi`: Ef of initial data
  - `Eff`: Ef of end data
  - `phii`: angel deviation of initial data
  - `phif`: angel deviation of end data
  - `map_numcuts`: number of energy cuts
  - `initial_index`: index of data begining
- Source: `Miscellaneous_Codes.ipf:1022`

#### `Kz_predict`

```igor
Proc Kz_predict(C,v0,Delta_k)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `C`, `v0`, `Delta_k`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: Delta_k = In-plane K (1/A); v0 = Inner potential - phi; C = Lattice constance of Z. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `grid()`.
- Parameters:
  - `C`: Lattice constance of Z
  - `v0`: Inner potential - phi
  - `Delta_k`: In-plane K (1/A)
- Source: `Miscellaneous_Codes.ipf:987`

#### `Kz_predict2`

```igor
Proc Kz_predict2()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `makeCEM()`, `CVT2K_vs_kz_2D_2()`, `grid()`, `nticks()`.
- Source: `Miscellaneous_Codes.ipf:1563`

#### `lattice1_0_incset`

```igor
Proc lattice1_0_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31991`

#### `lattice1_0_set`

```igor
Proc lattice1_0_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32015`

#### `lattice_0_incset`

```igor
Proc lattice_0_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31978`

#### `lattice_0_set`

```igor
Proc lattice_0_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31997`

#### `lattice_hexagon_incset`

```igor
Proc lattice_hexagon_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:33281`

#### `lattice_hexagon_set`

```igor
Proc lattice_hexagon_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:33287`

#### `Lift`

```igor
Proc Lift(WaveName)
```
- Usage: run from Igor with parameters `WaveName`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics.
- Parameters:
  - `WaveName`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:1740`

#### `LiftMdcAtEf`

```igor
Proc LiftMdcAtEf()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Main internal calls: `Lift()`.
- Source: `Pierre's Template.ipf:1730`

#### `list4bkgndremoveral1`

```igor
Proc list4bkgndremoveral1()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `list4bkgndremoveral2()`.
- Source: `Pierre's Template.ipf:9141`

#### `list4bkgndremoveral2`

```igor
Proc list4bkgndremoveral2(newname)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `newname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: newname = Background in:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; asks the user for parameters through Igor prompts. Main internal calls: `subbkgndremover()`.
- Parameters:
  - `newname`: Background in:
- Source: `Pierre's Template.ipf:9194`

#### `liste_hazard`

```igor
Proc liste_hazard(nombre_a_prendre,nombre_total,nomliste)
```
- Usage: run from Igor with parameters `nombre_a_prendre`, `nombre_total`, `nomliste`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: nombre_a_prendre = Nombre à prendre:; nombre_total = Nombre total; nomliste = Nom de la liste. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `nombre_a_prendre`: Nombre à prendre:
  - `nombre_total`: Nombre total
  - `nomliste`: Nom de la liste
- Source: `Pierre's Template.ipf:19865`

#### `ln_mat_pi`

```igor
Proc ln_mat_pi(mat)
```
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:5228`

#### `Load3D1`

```igor
Proc Load3D1()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Main internal calls: `Load3D2()`.
- Source: `Pierre's Template.ipf:36053`

#### `Load3D2`

```igor
Proc Load3D2(thischoiceof3d)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `thischoiceof3d`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: thischoiceof3d = Mapping:. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `WhichListItem()`, `file2wave()`, `wintype()`, `initmat3d()`, `mat3d_fs_ploter()`, `initmat3dVD()`.
- Parameters:
  - `thischoiceof3d`: Mapping:
- Source: `Pierre's Template.ipf:36067`

#### `load_conefigs`

```igor
Proc load_conefigs()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Main internal calls: `File2wave()`.
- Source: `Pierre's Template.ipf:15746`

#### `loadallibw`

```igor
Proc loadallibw()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: creates output waves; loads data from files or paths. Main internal calls: `IndexedFile()`, `file2wave()`.
- Source: `Pierre's Template.ipf:20891`

#### `loadBFAcuts`

```igor
Proc loadBFAcuts()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `file2wave()`.
- Source: `Pierre's Template.ipf:16599`

#### `LoadDataalltypes`

```igor
Proc LoadDataalltypes()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `LoadDataFromIgorFiles()`, `LoadDataFromtxt()`, `LoadDataFromdat()`, `LoadDataFromA1()`.
- Source: `Pierre's Template.ipf:17137`

#### `LoadDataFromA1`

```igor
Proc LoadDataFromA1()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`, `Center_detector()`.
- Source: `Pierre's Template.ipf:17380`

#### `LoadDataFromdat`

```igor
Proc LoadDataFromdat()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`, `str2num()`, `Center_detector()`.
- Source: `Pierre's Template.ipf:17191`

#### `LoadDataFromIgorFiles`

```igor
Proc LoadDataFromIgorFiles()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`, `ConvtOneProc()`.
- Source: `Pierre's Template.ipf:17155`

#### `LoadDataFromtxt`

```igor
Proc LoadDataFromtxt()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`, `str2num()`, `Center_detector()`.
- Source: `Pierre's Template.ipf:17286`

#### `loadnplot`

```igor
Proc loadnplot()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `derivmat()`.
- Source: `Pierre's Template.ipf:15965`

#### `LoadSTM_csv`

```igor
Proc LoadSTM_csv()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: STM Purpose: loads data or launches an automatic loading workflow. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; cleans up waves/windows or brings an existing window forward. Main internal calls: `Indexeddir()`, `Indexedfile()`.
- Source: `Pierre's Template.ipf:17778`

#### `Make_allchunkcuts`

```igor
Proc Make_allchunkcuts()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `Make_onechunkcut()`.
- Source: `Pierre's Template.ipf:34240`

#### `make_mat2dk`

```igor
Proc make_mat2dk(addtheta,addphi,Fermilevel,irange)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `addtheta`, `addphi`, `Fermilevel`, `irange`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: addtheta = Quantity to add to the physical theta; addphi = Quantity to add to the physical phi; Fermilevel = Fermi level; irange = Integral window:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `size4cvtchunkcut2d()`, `convert1chunkcut2d()`, `merged1kchunk2d()`, `mergekchunks2d()`.
- Parameters:
  - `addtheta`: Quantity to add to the physical theta
  - `addphi`: Quantity to add to the physical phi
  - `Fermilevel`: Fermi level
  - `irange`: Integral window:
- Source: `Pierre's Template.ipf:35012`

#### `make_mat3dk`

```igor
Proc make_mat3dk(addtheta,addphi,Fermi_level)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `addtheta`, `addphi`, `Fermi_level`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: addtheta = Quantity to add to the physical theta; addphi = Quantity to add to the physical phi; Fermi_level = Fermi level. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `size4cvtchunkcut()`, `convert1chunkcut()`, `merged1kchunk()`, `mergekchunks()`.
- Parameters:
  - `addtheta`: Quantity to add to the physical theta
  - `addphi`: Quantity to add to the physical phi
  - `Fermi_level`: Fermi level
- Source: `Pierre's Template.ipf:34309`

#### `make_mat3dkHD`

```igor
Proc make_mat3dkHD(addtheta,addphi,Fermi_level)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `addtheta`, `addphi`, `Fermi_level`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: addtheta = Quantity to add to the physical theta; addphi = Quantity to add to the physical phi; Fermi_level = Fermi level. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `size4cvtchunkcut()`, `convert1chunkcut()`, `merged1kchunk()`, `mergekchunks()`.
- Parameters:
  - `addtheta`: Quantity to add to the physical theta
  - `addphi`: Quantity to add to the physical phi
  - `Fermi_level`: Fermi level
- Source: `Pierre's Template.ipf:34493`

#### `make_mat3dkVD`

```igor
Proc make_mat3dkVD(addtheta,addphi,Fermi_level)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `addtheta`, `addphi`, `Fermi_level`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: addtheta = Quantity to add to the physical theta; addphi = Quantity to add to the physical phi; Fermi_level = Fermi level. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `size4cvtchunkcut()`, `convert1chunkcut()`, `merged1kchunk()`, `mergekchunks()`.
- Parameters:
  - `addtheta`: Quantity to add to the physical theta
  - `addphi`: Quantity to add to the physical phi
  - `Fermi_level`: Fermi level
- Source: `Pierre's Template.ipf:34401`

#### `Make_onechunkcut`

```igor
Proc Make_onechunkcut(number)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `str2num()`.
- Parameters:
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:34198`

#### `make_table_hv_kz`

```igor
Proc make_table_hv_kz()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:35624`

#### `Makearcshadow`

```igor
Proc Makearcshadow(matname,csize,cwidth,arc1,arc2)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `matname`, `csize`, `cwidth`, `arc1`, `arc2`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; csize = Size of the circle; cwidth = Width; arc1 = From; arc2 = To. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `func_Makearcshadow()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `csize`: Size of the circle
  - `cwidth`: Width
  - `arc1`: From
  - `arc2`: To
- Source: `Pierre's Template.ipf:16170`

#### `makebold`

```igor
Proc makebold(matname,curvenamex,curvenamey,width)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `matname`, `curvenamex`, `curvenamey`, `width`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; curvenamex = Enter the name of the curve in x; curvenamey = Enter the name of the curve in y; width = Enter the Gaussian width. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `Gauss()`, `Func_makebold()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `curvenamex`: Enter the name of the curve in x
  - `curvenamey`: Enter the name of the curve in y
  - `width`: Enter the Gaussian width
- Source: `Pierre's Template.ipf:16970`

#### `makeboundaryforCsrslicer`

```igor
Proc makeboundaryforCsrslicer()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: triggered as a wrapper that opens/runs `update_image_fs()`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `lstyle()`, `rgb()`.
- Source: `Pierre's Template.ipf:25456`

#### `makeCsrslicerimage`

```igor
Proc makeCsrslicerimage()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `intersectionforCsrslider()`, `findinglengthforCsrslicer()`, `fillCsrslicerimage()`.
- Source: `Pierre's Template.ipf:25288`

#### `MakeCutImage`

```igor
Proc MakeCutImage(phai)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `phai`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: phai = input the phai angle. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `appendtocutfrommat()`.
- Parameters:
  - `phai`: input the phai angle
- Source: `Pierre's Template.ipf:28513`

#### `MakeCutImage_pi`

```igor
Proc MakeCutImage_pi(phai,overlap,display_or_not)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `phai`, `overlap`, `display_or_not`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: phai = input the phai angle; overlap = input the wave overlap; display_or_not = Do you want to display the image?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `appendtocutfrommat()`.
- Parameters:
  - `phai`: input the phai angle
  - `overlap`: input the wave overlap
  - `display_or_not`: Do you want to display the image?
- Source: `Pierre's Template.ipf:29841`

#### `MakeEdc`

```igor
Proc MakeEdc(edcstart,edcend,addnum)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `edcstart`, `edcend`, `addnum`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: edcstart = Input the start num; edcend = Input the end num; addnum = Input the Num to be combined. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `edcstart`: Input the start num
  - `edcend`: Input the end num
  - `addnum`: Input the Num to be combined
- Source: `Pierre's Template.ipf:1772`

#### `MakeEdc_pi`

```igor
Proc MakeEdc_pi(edcstart,edcend,addnum,one_each)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `edcstart`, `edcend`, `addnum`, `one_each`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: edcstart = Input the start num; edcend = Input the end num (-1=END); addnum = Input the Num to be combined (odd number); one_each = Show one curve each...?. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `edcstart`: Input the start num
  - `edcend`: Input the end num (-1=END)
  - `addnum`: Input the Num to be combined (odd number)
  - `one_each`: Show one curve each...?
- Source: `Pierre's Template.ipf:3189`

#### `MakeEfMdcTable`

```igor
Macro  MakeEfMdcTable()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Igor macro entry point for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `winType()`, `wintype()`.
- Source: `Pierre's Template.ipf:1937`

#### `makeEMDCtable`

```igor
Proc makeEMDCtable()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:11822`

#### `MakeHexagon`

```igor
Proc MakeHexagon(sizeofhexagon,angle,name)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `sizeofhexagon`, `angle`, `name`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Dialog prompts: sizeofhexagon = Enter the parameter a; angle = Enter the angle (conterclockwise); name = Enter the name of the hexagon. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `funcMakeHexagon()`.
- Parameters:
  - `sizeofhexagon`: Enter the parameter a
  - `angle`: Enter the angle (conterclockwise)
  - `name`: Enter the name of the hexagon
- Source: `Pierre's Template.ipf:32961`

#### `makekfedc`

```igor
Proc makekfedc()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:19217`

#### `makeLLspectra`

```igor
proc makeLLspectra(hv,ED,tem,num,field,ER)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `hv`, `ED`, `tem`, `num`, `field`, `ER`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: hv = Fermi velocity (meV*A); ED = Binding Energy of Dirac Point (meV); tem = Temperature of the system (K); num = number of levels; field = Field Range: (1 to ?)T; ER = Energe range of spectrum (meV). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `makeLLspectraf()`.
- Parameters:
  - `hv`: Fermi velocity (meV*A)
  - `ED`: Binding Energy of Dirac Point (meV)
  - `tem`: Temperature of the system (K)
  - `num`: number of levels
  - `field`: Field Range: (1 to ?)T
  - `ER`: Energe range of spectrum (meV)
- Source: `Models.ipf:4418`

#### `makematedckfBKFA`

```igor
Proc makematedckfBKFA()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:14428`

#### `makematedckfBKFAout`

```igor
Proc makematedckfBKFAout()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:14444`

#### `MakeMDC_pi`

```igor
Proc MakeMDC_pi(mdcstart,mdcend,addnum,one_each)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `mdcstart`, `mdcend`, `addnum`, `one_each`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: mdcstart = Input the start num; mdcend = Input the end num (-1=END); addnum = Input the Num to be combined (odd number); one_each = Show one curve each...?. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `mdcstart`: Input the start num
  - `mdcend`: Input the end num (-1=END)
  - `addnum`: Input the Num to be combined (odd number)
  - `one_each`: Show one curve each...?
- Source: `Pierre's Template.ipf:3234`

#### `Makenewmap`

```igor
Proc Makenewmap(thisnameofmap,thiscomment4map)
```
- Usage: run from Igor with parameters `thisnameofmap`, `thiscomment4map`.
- Notes: Map list Purpose: creates new waves, maps, figures, or simulation data. Dialog prompts: thisnameofmap = Enter a name for your mapping; thiscomment4map = Enter a comment for your mapping (do not use. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `copymap()`.
- Parameters:
  - `thisnameofmap`: Enter a name for your mapping
  - `thiscomment4map`: Enter a comment for your mapping (do not use
- Source: `Pierre's Template.ipf:35917`

#### `MakeNK`

```igor
Proc MakeNK()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:22095`

#### `makepredicted_cuts`

```igor
Proc makepredicted_cuts() : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `rgb()`.
- Source: `Pierre's Template.ipf:25981`

#### `makereversecone`

```igor
Proc makereversecone()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:16150`

#### `makingEDCgamma`

```igor
Proc makingEDCgamma()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:16911`

#### `mat2dk_pi`

```igor
Proc mat2dk_pi(mat2d,offset_theta,offset_phi,Kenergy)
```
- Usage: run from Igor with parameters `mat2d`, `offset_theta`, `offset_phi`, `Kenergy`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: mat2d = Enter the name of the matrix; offset_theta = Enter the theta value to add:; Kenergy = Enter kenetic energy. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_mat2dk_pi()`.
- Parameters:
  - `mat2d`: Enter the name of the matrix
  - `offset_theta`: Enter the theta value to add:
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Kenergy`: Enter kenetic energy
- Source: `Pierre's Template.ipf:31471`

#### `mat3dk_pi`

```igor
Proc mat3dk_pi(offset_theta,offset_phi,fermi_level)//Use mat3d
```
- Usage: run from Igor with parameters `offset_theta`, `offset_phi`, `fermi_level`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: offset_theta = Enter the theta value to add:; fermi_level = Enter the Fermi level:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_mat3dk_pi()`.
- Parameters:
  - `offset_theta`: Enter the theta value to add:
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fermi_level`: Enter the Fermi level:
- Source: `Pierre's Template.ipf:29194`

#### `mat3dkHD_pi`

```igor
Proc mat3dkHD_pi(offset_theta,offset_phi,fermi_level)//Use mat3d
```
- Usage: run from Igor with parameters `offset_theta`, `offset_phi`, `fermi_level`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: offset_theta = Enter the theta value to add:; fermi_level = Enter the Fermi level:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_mat3dk_pi()`.
- Parameters:
  - `offset_theta`: Enter the theta value to add:
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fermi_level`: Enter the Fermi level:
- Source: `Pierre's Template.ipf:29405`

#### `mat3dkHDrot`

```igor
Proc mat3dkHDrot(angle,quality_fact)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `angle`, `quality_fact`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: angle = Enter the rotation angle (degrees, counterclockwise); Quality_fact = Enter the quality factor. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `rotmat3dk_pi()`.
- Parameters:
  - `angle`: Enter the rotation angle (degrees, counterclockwise)
  - `quality_fact`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:31674`

#### `mat3dkrot`

```igor
Proc mat3dkrot(angle,quality_fact)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `angle`, `quality_fact`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: angle = Enter the rotation angle (degrees, counterclockwise); Quality_fact = Enter the quality factor. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `rotmat3dk_pi()`.
- Parameters:
  - `angle`: Enter the rotation angle (degrees, counterclockwise)
  - `quality_fact`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:31646`

#### `mat3dkVD_pi`

```igor
Proc mat3dkVD_pi(offset_theta,offset_phi,fermi_level)//Use mat3d
```
- Usage: run from Igor with parameters `offset_theta`, `offset_phi`, `fermi_level`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: offset_theta = Enter the theta value to add:; fermi_level = Enter the Fermi level:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_mat3dk_pi()`.
- Parameters:
  - `offset_theta`: Enter the theta value to add:
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fermi_level`: Enter the Fermi level:
- Source: `Pierre's Template.ipf:29344`

#### `mat3dkVD_pi1`

```igor
Proc mat3dkVD_pi1(offset_theta,offset_phi,fermi_level)//Use mat3d
```
- Usage: run from Igor with parameters `offset_theta`, `offset_phi`, `fermi_level`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: offset_theta = Enter the theta value to add:; fermi_level = Enter the Fermi level:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `interp()`.
- Parameters:
  - `offset_theta`: Enter the theta value to add:
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fermi_level`: Enter the Fermi level:
- Source: `Pierre's Template.ipf:29466`

#### `mat3dkVDrot`

```igor
Proc mat3dkVDrot(angle,quality_fact)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `angle`, `quality_fact`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: angle = Enter the rotation angle (degrees, counterclockwise); Quality_fact = Enter the quality factor. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `rotmat3dk_pi()`.
- Parameters:
  - `angle`: Enter the rotation angle (degrees, counterclockwise)
  - `quality_fact`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:31660`

#### `mat_n_pi`

```igor
Proc mat_n_pi(mat,n)
```
- Usage: run from Igor with parameters `mat`, `n`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; n = Enter the power by which you want to multiply the data. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `n`: Enter the power by which you want to multiply the data
- Source: `Pierre's Template.ipf:5156`

#### `MatNKPlot`

```igor
Proc MatNKPlot(first,last)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `first`, `last`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: first = Enter the first slice of the range; last = Enter the last slice of the range. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `mean()`, `pnt2x()`, `ShowGraph()`.
- Parameters:
  - `first`: Enter the first slice of the range
  - `last`: Enter the last slice of the range
- Source: `Pierre's Template.ipf:1672`

#### `MatNorm`

```igor
Proc MatNorm()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal.
- Source: `Pierre's Template.ipf:1701`

#### `mayalu`

```igor
Proc mayalu(matname,firstweight0,lastweight0)
```
- Usage: run from Igor with parameters `matname`, `firstweight0`, `lastweight0`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; firstweight0 = Enter the first index for weight=0; lastweight0 = Enter the last index for weight=0. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `poly()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `firstweight0`: Enter the first index for weight=0
  - `lastweight0`: Enter the last index for weight=0
- Source: `Pierre's Template.ipf:19624`

#### `mayalu2`

```igor
Proc mayalu2(matname,fitfrom,fitto)
```
- Usage: run from Igor with parameters `matname`, `fitfrom`, `fitto`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix; fitfrom = Fit from:; fitto = Fit to:. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `fitfrom`: Fit from:
  - `fitto`: Fit to:
- Source: `Pierre's Template.ipf:19723`

#### `MDC_pi`

```igor
Proc MDC_pi(thismatname,addnum)
```
- Usage: run from Igor with parameters `thismatname`, `addnum`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: thismatname = Input the matrix for the MDCs; addnum = Input the number of combined MDC curves. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `num2istr()`.
- Parameters:
  - `thismatname`: Input the matrix for the MDCs
  - `addnum`: Input the number of combined MDC curves
- Source: `Pierre's Template.ipf:4908`

#### `MDC_pi_old`

```igor
Proc MDC_pi_old(MatrixName,Combine)
```
- Usage: run from Igor with parameters `MatrixName`, `Combine`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: MatrixName = Input the matrix for the MDCs; Combine = Input the number of combined mdc curves. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `num2istr()`.
- Parameters:
  - `MatrixName`: Input the matrix for the MDCs
  - `Combine`: Input the number of combined mdc curves
- Source: `Pierre's Template.ipf:4867`

#### `mdc_profile`

```igor
Proc mdc_profile(matname,slice_low,slice_high)
```
- Usage: run from Igor with parameters `matname`, `slice_low`, `slice_high`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matname = Enter the name of the matrix; slice_low = Enter the first slice; slice_high = Enter the last slice. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `slice_low`: Enter the first slice
  - `slice_high`: Enter the last slice
- Source: `Pierre's Template.ipf:18504`

#### `MDCfit`

```igor
Proc MDCfit(EF,number)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `EF`, `number`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: Ef = Fermi Energy:; number = Enter the mdc you want to compare. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `qcsr()`, `vcsr()`, `xcsr()`, `WaveName()`, `num2istr()`.
- Parameters:
  - `EF`: Used in: SetScale/p x, Mstart-Ef,newdy,"",$peakname / SetScale/p x, Mstart-Ef,newdy,"",$widthname
  - `number`: Enter the mdc you want to compare
- Source: `Pierre's Template.ipf:1468`

#### `MDCfit2_pi`

```igor
Proc MDCfit2_pi(Matname,combined,number)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `Matname`, `combined`, `number`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: Matname = Enter the name of the matrix; combined = Enter the number of combined MDCs; number = Enter the mdc you want to compare. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `vcsr()`, `xcsr()`, `qcsr()`, `pcsr()`, `num2istr()`, `WaveName()`.
- Parameters:
  - `Matname`: Enter the name of the matrix
  - `combined`: Enter the number of combined MDCs
  - `number`: Enter the mdc you want to compare
- Source: `Pierre's Template.ipf:5058`

#### `MDCfit_pi`

```igor
Proc MDCfit_pi(Matname,combined)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `Matname`, `combined`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: Matname = Enter the name of the matrix; combined = Enter the number of combined MDCs. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `vcsr()`, `xcsr()`, `qcsr()`, `pcsr()`, `num2istr()`, `WaveName()`.
- Parameters:
  - `Matname`: Enter the name of the matrix
  - `combined`: Enter the number of combined MDCs
- Source: `Pierre's Template.ipf:4984`

#### `minus_background`

```igor
Proc minus_background(temperature)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `temperature`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: temperature = Enter the effective temperature. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; cleans up waves/windows or brings an existing window forward. Main internal calls: `x2pnt()`, `Interp()`.
- Parameters:
  - `temperature`: Enter the effective temperature
- Source: `Pierre's Template.ipf:20773`

#### `mkallbds`

```igor
Proc mkallbds()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:19064`

#### `mkallfp`

```igor
Proc mkallfp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:19079`

#### `Mkbad_slices`

```igor
Proc Mkbad_slices()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Source: `Pierre's Template.ipf:7828`

#### `mkfit_ini_tbl`

```igor
Proc mkfit_ini_tbl()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:18181`

#### `NaN0`

```igor
Proc NaN0(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `func_NaN0()`.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:7649`

#### `new_eres_pi`

```igor
Proc new_eres_pi(dataname,num_to_combine,overwrite)
```
- Usage: run from Igor with parameters `dataname`, `num_to_combine`, `overwrite`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: dataname = Enter the name of the matrix; num_to_combine = How many energy steps do you want to combine?; overwrite = Do you want to overwrite the old matrix?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `dataname`: Enter the name of the matrix
  - `num_to_combine`: How many energy steps do you want to combine?
  - `overwrite`: Do you want to overwrite the old matrix?
- Source: `Pierre's Template.ipf:6070`

#### `new_kres_pi`

```igor
Proc new_kres_pi(dataname,num_to_combine,overwrite)
```
- Usage: run from Igor with parameters `dataname`, `num_to_combine`, `overwrite`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: dataname = Enter the name of the matrix; num_to_combine = How many k steps do you want to combine?; overwrite = Do you want to overwrite the old matrix?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `dataname`: Enter the name of the matrix
  - `num_to_combine`: How many k steps do you want to combine?
  - `overwrite`: Do you want to overwrite the old matrix?
- Source: `Pierre's Template.ipf:6120`

#### `new_stepsize`

```igor
Proc new_stepsize(mat,choice,newdelta,overwrite) //Given as homework2a
```
- Usage: run from Igor with parameters `mat`, `choice`, `newdelta`, `overwrite`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; choice = Choose the direction; newdelta = Enter the new stepsize; overwrite = Do you want to overwrite?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Ceil()`.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `choice`: Choose the direction
  - `newdelta`: Enter the new stepsize
  - `overwrite`: Do you want to overwrite?
- Source: `Pierre's Template.ipf:7754`

#### `Newrotateproc`

```igor
Proc Newrotateproc(name,theta1)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `name`, `theta1`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: name = the name of the matrix to be rotated; theta1 = Angel (couterclockwise is positive), please only use value within -90 to 90. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Newrotate()`.
- Parameters:
  - `name`: the name of the matrix to be rotated
  - `theta1`: Angel (couterclockwise is positive), please only use value within -90 to 90
- Source: `Miscellaneous_Codes.ipf:31`

#### `Nkplot`

```igor
Proc Nkplot()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `num2istr()`, `fAverage()`, `Mean()`.
- Source: `Pierre's Template.ipf:1439`

#### `norm_above_ef_pi`

```igor
Proc norm_above_ef_pi(data_number)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `data_number`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: data_number = Enter the index number of the file. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `DisplayMat()`, `mean()`, `pnt2x()`.
- Parameters:
  - `data_number`: Enter the index number of the file
- Source: `Pierre's Template.ipf:6291`

#### `norm_high_e_pi`

```igor
Proc norm_high_e_pi(number,e_start,e_end)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `number`, `e_start`, `e_end`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: number = Enter the number of the normalization table; e_start = Range of normalization (positive value below the photon energy). From; e_end = to. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `number`: Enter the number of the normalization table
  - `e_start`: Range of normalization (positive value below the photon energy). From
  - `e_end`: to
- Source: `Pierre's Template.ipf:6226`

#### `norm_range`

```igor
Proc norm_range(name,x_from,x_to)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`, `x_from`, `x_to`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix; x_from = Normalize from:; x_to = to:. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `mean()`.
- Parameters:
  - `name`: Enter the name of the matrix
  - `x_from`: Normalize from:
  - `x_to`: to:
- Source: `Pierre's Template.ipf:7947`

#### `norm_transmission_above`

```igor
Proc norm_transmission_above()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics.
- Source: `Pierre's Template.ipf:18709`

#### `normall`

```igor
Proc normall()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal. Main internal calls: `normcut()`.
- Source: `Pierre's Template.ipf:20732`

#### `normcut`

```igor
Proc normcut(name,index_from,index_to)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `name`, `index_from`, `index_to`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the matrix; index_from = Enter the lower index; index_to = Enter the upper index. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `func_normcut()`.
- Parameters:
  - `name`: Enter the name of the matrix
  - `index_from`: Enter the lower index
  - `index_to`: Enter the upper index
- Source: `Pierre's Template.ipf:6898`

#### `normcut2one`

```igor
Proc normcut2one()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: assigns Igor axis scaling/units to the output. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:18557`

#### `Normwithlong`

```igor
Proc Normwithlong()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `ConvtOne()`, `open_onedata_pi()`.
- Source: `Pierre's Template.ipf:16889`

#### `normwithvb`

```igor
Proc normwithvb()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; smooths wave data. Main internal calls: `sub_bline_pi()`.
- Source: `Pierre's Template.ipf:18851`

#### `not_dv`

```igor
Proc not_dv()
```
- Notes: templates Usage: run interactively from Igor or from a panel callback. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:17826`

#### `not_in_fct`

```igor
Proc not_in_fct()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Source: `Pierre's Template.ipf:2592`

#### `NTP_num_set`

```igor
Proc NTP_num_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:2598`

#### `numb1`

```igor
Proc numb1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Main internal calls: `wave2file()`, `wave2filenokill()`.
- Source: `Pierre's Template.ipf:23556`

#### `numbernorm`

```igor
Proc numbernorm(numbpoints)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `numbpoints`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: numbpoints = Enter the number of points. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; asks the user for parameters through Igor prompts. Main internal calls: `sub_bline_pi()`.
- Parameters:
  - `numbpoints`: Enter the number of points
- Source: `Pierre's Template.ipf:16715`

#### `open_data_pi`

```igor
Proc open_data_pi(from,to,normway,e_from,e_to,background,Normcurve,backpnts,fftonoff)
```
- Usage: run from Igor with parameters `from`, `to`, `normway`, `e_from`, `e_to`, `background`, `Normcurve`, `backpnts`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Dialog prompts: from = From which data index?; to = To index:; normway = Normalization way?; e_from = Normalize from (E)(optinal):; e_to = To (optional):; background = Which background?. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data. Main internal calls: `FFTfilterdata()`, `sub_bline_pi()`, `func_rm_one_linear_background()`, `MatNKPlot()`, `MatNorm()`, `Interp()`.
- Parameters:
  - `from`: From which data index?
  - `to`: To index:
  - `normway`: Normalization way?
  - `e_from`: Normalize from (E)(optinal):
  - `e_to`: To (optional):
  - `background`: Which background?
  - `Normcurve`: Name of the norm curve (optional):
  - `backpnts`: Points for background fit?
  - `fftonoff`: FFT filter?
- Source: `Pierre's Template.ipf:4265`

#### `open_onedata_pi`

```igor
Proc open_onedata_pi(normway,e_from,e_to,background,Normcurve,backpnts,fftonoff)
```
- Usage: run from Igor with parameters `normway`, `e_from`, `e_to`, `background`, `Normcurve`, `backpnts`, `fftonoff`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Dialog prompts: normway = Normalization way?; e_from = Normalize from (E)(optinal):; e_to = To (optional):; background = Which background?; Normcurve = Name of the norm curve (optional):; backpnts = Points for background fit?. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data. Main internal calls: `FFTfilterdata()`, `sub_bline_pi()`, `func_rm_one_linear_background()`, `MatNKPlot()`, `MatNorm()`, `Interp()`.
- Parameters:
  - `normway`: Normalization way?
  - `e_from`: Normalize from (E)(optinal):
  - `e_to`: To (optional):
  - `background`: Which background?
  - `Normcurve`: Name of the norm curve (optional):
  - `backpnts`: Points for background fit?
  - `fftonoff`: FFT filter?
- Source: `Pierre's Template.ipf:4416`

#### `open_select`

```igor
Proc open_select(normway,e_from,e_to,background,Normcurve,backpnts,fftonoff)
```
- Usage: run from Igor with parameters `normway`, `e_from`, `e_to`, `background`, `Normcurve`, `backpnts`, `fftonoff`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Dialog prompts: normway = Normalization; e_from = Normalize from (E)(optinal):; e_to = To (optional):; background = Which background?; Normcurve = Name of the norm curve (optional):; backpnts = How many points for the background fit?. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data. Main internal calls: `FFTfilterdata()`, `sub_bline_pi()`, `MatNKPlot()`, `MatNorm()`, `Interp()`, `func_rm_one_linear_background()`.
- Parameters:
  - `normway`: Normalization
  - `e_from`: Normalize from (E)(optinal):
  - `e_to`: To (optional):
  - `background`: Which background?
  - `Normcurve`: Name of the norm curve (optional):
  - `backpnts`: How many points for the background fit?
  - `fftonoff`: FFT filter?
- Source: `Pierre's Template.ipf:4554`

#### `openalldif`

```igor
Proc openalldif(e_from) //normalize above EF
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `e_from`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: e_from = Normalize from (E):. Code behavior: creates output waves; duplicates or stages waves for downstream processing; smooths wave data; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `MatNKPlot()`, `Interp()`, `sub_bline_pi()`, `MatNorm()`.
- Parameters:
  - `e_from`: Normalize from (E):
- Source: `Pierre's Template.ipf:19017`

#### `pastemap`

```igor
Proc pastemap(prefix)
```
- Usage: run from Igor with parameters `prefix`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: prefix = Prefix. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `prefix`: Prefix
- Source: `Pierre's Template.ipf:36206`

#### `phi1_zero_incset`

```igor
Proc phi1_zero_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31954`

#### `phi1_zero_set`

```igor
Proc phi1_zero_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31972`

#### `phi_cst_pi`

```igor
Proc phi_cst_pi(k_phi)
```
- Usage: run from Igor with parameters `k_phi`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: k_phi = Enter k_phi. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `ceil()`.
- Parameters:
  - `k_phi`: Enter k_phi
- Source: `Pierre's Template.ipf:29625`

#### `phi_zero_incset`

```igor
Proc phi_zero_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31942`

#### `phi_zero_incsetH`

```igor
Proc phi_zero_incsetH()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31948`

#### `phi_zero_set`

```igor
Proc phi_zero_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31960`

#### `phi_zero_setH`

```igor
Proc phi_zero_setH()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31966`

#### `pi102_1`

```igor
Proc pi102_1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data; differentiates wave data.
- Source: `Pierre's Template.ipf:20400`

#### `pickupedc`

```igor
Proc pickupedc(sample,hvenergy)//Designed for YBCO
```
- Usage: run from Igor with parameters `sample`, `hvenergy`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: sample = Enter the name of the sample; hvenergy = Enter the photon energy. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `wave2filenokill()`.
- Parameters:
  - `sample`: Enter the name of the sample
  - `hvenergy`: Enter the photon energy
- Source: `Pierre's Template.ipf:20684`

#### `plot2`

```igor
Proc plot2()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays. Main internal calls: `Bluetored()`.
- Source: `Pierre's Template.ipf:20832`

#### `plot_and_adjust2dv`

```igor
Proc plot_and_adjust2dv()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `DerivMatrix()`.
- Source: `Pierre's Template.ipf:18589`

#### `plot_colorangle25KHD`

```igor
Proc plot_colorangle25KHD()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:15885`

#### `plot_cone25K`

```igor
Proc plot_cone25K()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:15762`

#### `plot_cone25KHD`

```igor
Proc plot_cone25KHD()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`, `noLabel()`, `fSize()`.
- Source: `Pierre's Template.ipf:15842`

#### `plot_cone25KVD`

```igor
Proc plot_cone25KVD()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:15802`

#### `plot_edc_pi`

```igor
Proc plot_edc_pi(MatrixName,Combined,one_each)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `MatrixName`, `Combined`, `one_each`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: MatrixName = Input the name of the matrix; Combined = Input the number of combined edc curves; one_each = Show one curve over each.... Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `pcsr()`, `qcsr()`, `num2istr()`.
- Parameters:
  - `MatrixName`: Input the name of the matrix
  - `Combined`: Input the number of combined edc curves
  - `one_each`: Show one curve over each...
- Source: `Pierre's Template.ipf:4728`

#### `Plot_kf_edcs`

```igor
Proc Plot_kf_edcs(Fermi_level,avg_num,backgnd_type,num_back,normfrom,normto)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `Fermi_level`, `avg_num`, `backgnd_type`, `num_back`, `normfrom`, `normto`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: Fermi_level = Fermi level:; avg_num = Number of averged EDCs (odd):; backgnd_type = Background type:; num_back = Points for background fitting:; normfrom = Normalize from (-1=no normalization):; normto = Normalize to (index):. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`, `checkNTP()`, `Remove_simplebgnd_edc
- Parameters:
  - `Fermi_level`: Fermi level:
  - `avg_num`: Number of averged EDCs (odd):
  - `backgnd_type`: Background type:
  - `num_back`: Points for background fitting:
  - `normfrom`: Normalize from (-1=no normalization):
  - `normto`: Normalize to (index):
- Source: `Pierre's Template.ipf:3781`

#### `plot_mdc_pi`

```igor
Proc plot_mdc_pi(MatrixName,Combined,one_each,SmoothFactor)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `MatrixName`, `Combined`, `one_each`, `SmoothFactor`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: MatrixName = Input the name of the matrix; Combined = Input the number of combined mdc curves; one_each = Show one curve over each...; SmoothFactor = Smooth by ? times. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `qcsr()`, `pcsr()`, `num2istr()`.
- Parameters:
  - `MatrixName`: Input the name of the matrix
  - `Combined`: Input the number of combined mdc curves
  - `one_each`: Show one curve over each...
  - `SmoothFactor`: Smooth by ? times
- Source: `Pierre's Template.ipf:4946`

#### `plot_predicttbGXMG`

```igor
Proc plot_predicttbGXMG()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `tbfunc()`, `winType()`.
- Source: `Pierre's Template.ipf:32743`

#### `plot_tbGXMG`

```igor
Proc plot_tbGXMG()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `tbfunc()`, `winType()`.
- Source: `Pierre's Template.ipf:32639`

#### `plot_tbGXMGMprimeX_2f`

```igor
Proc plot_tbGXMGMprimeX_2f()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `tbfunc()`, `winType()`.
- Source: `Pierre's Template.ipf:32677`

#### `plot_the_max`

```igor
Proc plot_the_max()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; extracts image/wave statistics; smooths wave data. Main internal calls: `x2pnt()`.
- Source: `Pierre's Template.ipf:20542`

#### `plot_vb_pi`

```igor
Proc plot_vb_pi(e_fermi,normal_energy1,normal_energy2,number)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `e_fermi`, `normal_energy1`, `normal_energy2`, `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: e_fermi = Enter the fermi energy (positive value below the photon energy); normal_energy1 = Energy range of normalization (positive value below the photon energy). From:; normal_energy2 = to:; number = Enter the number of the table. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `e_fermi`: Enter the fermi energy (positive value below the photon energy)
  - `normal_energy1`: Energy range of normalization (positive value below the photon energy). From:
  - `normal_energy2`: to:
  - `number`: Enter the number of the table
- Source: `Pierre's Template.ipf:5726`

#### `plot_vk`

```igor
Proc plot_vk()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `x2pnt()`.
- Source: `Pierre's Template.ipf:19533`

#### `plot_vk1`

```igor
Proc plot_vk1()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays. Main internal calls: `x2pnt()`.
- Source: `Pierre's Template.ipf:19578`

#### `plotatG`

```igor
Proc plotatG()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `normcut()`, `comb_krange()`, `ln_mat_pi()`, `NaN0()`, `derivmat()`.
- Source: `Pierre's Template.ipf:20745`

#### `plotBZ_pi`

```igor
Proc plotBZ_pi(lattice,orientation)//,second)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `lattice`, `orientation`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Dialog prompts: lattice = Enter the lattice parameter a; orientation = Enter the orientation. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `lattice`: Enter the lattice parameter a
  - `orientation`: Enter the orientation
- Source: `Pierre's Template.ipf:30425`

#### `plotfermi`

```igor
Proc plotfermi(temperature)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `temperature`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: temperature = Temperature:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `temperature`: Temperature:
- Source: `Pierre's Template.ipf:21870`

#### `plotGa2O3hv`

```igor
Proc plotGa2O3hv()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:23816`

#### `plotGMcalc`

```igor
Proc plotGMcalc()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:16629`

#### `plotpnts`

```igor
Proc plotpnts()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:5441`

#### `plotraw`

```igor
Proc plotraw()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `ImageNameList()`.
- Source: `Pierre's Template.ipf:102`

#### `plottin`

```igor
Proc plottin()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:19613`

#### `PlotVBatpoint_vs_hv`

```igor
Proc PlotVBatpoint_vs_hv(prefix,listindex,hv4VB,workwave,position, range)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `prefix`, `listindex`, `hv4VB`, `workwave`, `position`, `range`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: prefix = Prefix:; listindex = Wave of index:; hv4VB = Wave of hv:; workwave = Wave of work functions:; position = Center (angle or k):; range = Range of integration (angle or k):. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `comb_krange()`, `transform2equiv_waves()`.
- Parameters:
  - `prefix`: Prefix:
  - `listindex`: Wave of index:
  - `hv4VB`: Wave of hv:
  - `workwave`: Wave of work functions:
  - `position`: Center (angle or k):
  - `range`: Range of integration (angle or k):
- Source: `Pierre's Template.ipf:16811`

#### `polarerrorBKFA`

```igor
Proc polarerrorBKFA()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `rot_vector()`, `rgb()`.
- Source: `Pierre's Template.ipf:14259`

#### `polarerrorBKFAatM`

```igor
Proc polarerrorBKFAatM()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `rot_vector()`, `rgb()`.
- Source: `Pierre's Template.ipf:14300`

#### `polybackgnd`

```igor
Proc polybackgnd(name,xstart,xend,order,cutstart,cutend,number)
```
- Usage: run from Igor with parameters `name`, `xstart`, `xend`, `order`, `cutstart`, `cutend`, `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the curve; xstart = Fit from:; xend = To:; order = Enter the order of the polynomial; cutstart = Remove weigth from:; cutend = To:. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `rgb()`.
- Parameters:
  - `name`: Enter the name of the curve
  - `xstart`: Fit from:
  - `xend`: To:
  - `order`: Enter the order of the polynomial
  - `cutstart`: Remove weigth from:
  - `cutend`: To:
  - `number`: Number of the region
- Source: `Pierre's Template.ipf:17864`

#### `PReader_draw_live`

```igor
Proc PReader_draw_live()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:8335`

#### `PReader_normal_mode`

```igor
Proc PReader_normal_mode()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; graph display, formatting, or window management.
- Source: `Pierre's Template.ipf:8329`

#### `PReader_switch_edit_mode`

```igor
Proc PReader_switch_edit_mode()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:8318`

#### `predict_anglephi_from_set`

```igor
Proc predict_anglephi_from_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32129`

#### `predict_anglephi_to_set`

```igor
Proc predict_anglephi_to_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32141`

#### `predict_angleprepfrom_set`

```igor
Proc predict_angleprepfrom_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32147`

#### `predict_angletheta_from_set`

```igor
Proc predict_angletheta_from_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32123`

#### `predict_angletheta_to_set`

```igor
Proc predict_angletheta_to_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32135`

#### `predict_mu_set`

```igor
Proc predict_mu_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32081`

#### `predict_t_set`

```igor
Proc predict_t_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32099`

#### `predict_tprime_set`

```igor
Proc predict_tprime_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32117`

#### `predictcalc4circles`

```igor
Proc predictcalc4circles(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing. Dialog prompts: name = Enter the name of the sample. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `func_predictcalc4circles()`.
- Parameters:
  - `name`: Enter the name of the sample
- Source: `Pierre's Template.ipf:32209`

#### `predictcalc_tbarea`

```igor
Proc predictcalc_tbarea()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves. Main internal calls: `tbfunc()`.
- Source: `Pierre's Template.ipf:32610`

#### `predictKEatEF_set`

```igor
Proc predictKEatEF_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32045`

#### `predictlattice_0_set`

```igor
Proc predictlattice_0_set()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32009`

#### `predictprep_bondaries`

```igor
Proc predictprep_bondaries()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:31837`

#### `predictupdate_4circles`

```igor
Proc predictupdate_4circles()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing. Main internal calls: `predictcalc4circles()`.
- Source: `Pierre's Template.ipf:31898`

#### `prep_bondaries`

```igor
Proc prep_bondaries()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:31778`

#### `prep_bondaries2f`

```igor
Proc prep_bondaries2f()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: creates output waves.
- Source: `Pierre's Template.ipf:31807`

#### `put45`

```igor
Proc put45()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:19261`

#### `put_curved_thicksH`

```igor
Proc put_curved_thicksH(name,Large_step,putfrom,putto,arclarge,smallnum,updateput)
```
- Usage: run from Igor with parameters `name`, `Large_step`, `putfrom`, `putto`, `arclarge`, `smallnum`, `updateput`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Dialog prompts: name = Give a name; Large_step = Enter the stepsize; putfrom = Minimum horizontal; putto = Max horizontal; arclarge = Thicks size (degrees); smallnum = Number of small thicks per big ones. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `rgb()`.
- Parameters:
  - `name`: Give a name
  - `Large_step`: Enter the stepsize
  - `putfrom`: Minimum horizontal
  - `putto`: Max horizontal
  - `arclarge`: Thicks size (degrees)
  - `smallnum`: Number of small thicks per big ones
  - `updateput`: Mode
- Source: `Pierre's Template.ipf:16477`

#### `put_error_bars_on_VF`

```igor
Proc put_error_bars_on_VF()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `rot_vector()`.
- Source: `Pierre's Template.ipf:16042`

#### `put_fermi_edc`

```igor
Proc put_fermi_edc(matname,energy,last_intensity)
```
- Usage: run from Igor with parameters `matname`, `energy`, `last_intensity`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the wave; energy = Enter the energy; last_intensity = Enter the lenght of the line. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `rgb()`.
- Parameters:
  - `matname`: Enter the name of the wave
  - `energy`: Enter the energy
  - `last_intensity`: Enter the lenght of the line
- Source: `Pierre's Template.ipf:6347`

#### `put_fermi_image`

```igor
Proc put_fermi_image(matname,energy)
```
- Usage: run from Igor with parameters `matname`, `energy`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: matname = Enter the name of the matrix; energy = Enter the energy. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `energy`: Enter the energy
- Source: `Pierre's Template.ipf:6332`

#### `put_psuperlattice`

```igor
Proc put_psuperlattice()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `lstyle()`.
- Source: `Pierre's Template.ipf:30303`

#### `put_pts_in_theta`

```igor
Proc put_pts_in_theta(kxwave,kywave,hnenergy)
```
- Usage: run from Igor with parameters `kxwave`, `kywave`, `hnenergy`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: kxwave = Enter the name for the kx wave; kywave = Enter the name for the ky wave; hnenergy = Enter the name of the photon energy. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `kxwave`: Enter the name for the kx wave
  - `kywave`: Enter the name for the ky wave
  - `hnenergy`: Enter the name of the photon energy
- Source: `Pierre's Template.ipf:20569`

#### `put_superlattice`

```igor
Proc put_superlattice()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:30281`

#### `put_superlattice1`

```igor
Proc put_superlattice1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:30381`

#### `putHline`

```igor
Proc putHline()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:27607`

#### `putlegend`

```igor
Proc putlegend()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:19602`

#### `putplus`

```igor
Proc putplus()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:19246`

#### `putVline`

```igor
Proc putVline()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:27818`

#### `quality_set`

```igor
Proc quality_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32063`

#### `r1`

```igor
Proc r1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:23576`

#### `random_circles`

```igor
Proc random_circles(doping,infected)
```
- Usage: run from Igor with parameters `doping`, `infected`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: doping = Enter the doping; infected = Enter which number is infected. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `calrandom_matrix()`.
- Parameters:
  - `doping`: Enter the doping
  - `infected`: Enter which number is infected
- Source: `Pierre's Template.ipf:22517`

#### `readcontour`

```igor
Proc readcontour()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `readcsr()`.
- Source: `Pierre's Template.ipf:16159`

#### `readcsr`

```igor
Proc readcsr()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Main internal calls: `vcsr()`, `hcsr()`.
- Source: `Pierre's Template.ipf:5366`

#### `readjusth_IVH`

```igor
Proc readjusth_IVH()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:15914`

#### `remove241`

```igor
Proc remove241(from,to)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `from`, `to`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: from = From; to = To. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `from`: From
  - `to`: To
- Source: `Pierre's Template.ipf:22173`

#### `remove60`

```igor
Proc remove60(sstart,eend)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `sstart`, `eend`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Parameters:
  - `sstart`: Start point, index, or coordinate for the selected range.
  - `eend`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:21761`

#### `Remove_data_EDC`

```igor
Proc Remove_data_EDC()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `xcsr()`, `combine_range()`.
- Source: `Pierre's Template.ipf:4105`

#### `remove_psuperlattice`

```igor
Proc remove_psuperlattice()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management.
- Source: `Pierre's Template.ipf:30359`

#### `Remove_simplebgnd_data`

```igor
Proc Remove_simplebgnd_data(background,backpnts)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `background`, `backpnts`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: background = Type of background:; backpnts = Num of pnts used:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `sub_blineedc_pi()`, `func_rm_one_linear_edcbackgnd()`.
- Parameters:
  - `background`: Type of background:
  - `backpnts`: Num of pnts used:
- Source: `Pierre's Template.ipf:4162`

#### `Remove_simplebgnd_edc`

```igor
Proc Remove_simplebgnd_edc(edcname,background,backpnts)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `edcname`, `background`, `backpnts`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: edcname = Name of EDC:; background = Type of background:; backpnts = Num of pnts used:. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `sub_blineedc_pi()`, `func_rm_one_linear_edcbackgnd()`.
- Parameters:
  - `edcname`: Name of EDC:
  - `background`: Type of background:
  - `backpnts`: Num of pnts used:
- Source: `Pierre's Template.ipf:4119`

#### `Remove_simplebgnd_mat`

```igor
Proc Remove_simplebgnd_mat(matname,background,backpnts)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `matname`, `background`, `backpnts`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matname = Name of matrix:; background = Type of background:; backpnts = Num of pnts used:. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `sub_blineedc_pi()`, `func_rm_one_linear_edcbackgnd()`.
- Parameters:
  - `matname`: Name of matrix:
  - `background`: Type of background:
  - `backpnts`: Num of pnts used:
- Source: `Pierre's Template.ipf:4136`

#### `remove_superlattice`

```igor
Proc remove_superlattice()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:30337`

#### `remove_superlattice1`

```igor
Proc remove_superlattice1()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:30403`

#### `Removepredicted_cuts`

```igor
Proc Removepredicted_cuts() : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management.
- Source: `Pierre's Template.ipf:25961`

#### `renorm_pi`

```igor
Proc renorm_pi(dataname,the_start,the_end)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `dataname`, `the_start`, `the_end`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: dataname = Enter the name of the wave; the_start = Enter the energy range. From:; the_end = To:. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `dataname`: Enter the name of the wave
  - `the_start`: Enter the energy range. From:
  - `the_end`: To:
- Source: `Pierre's Template.ipf:6368`

#### `renormcut_k`

```igor
Proc renormcut_k(matname,factor)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `matname`, `factor`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: matname = Enter the name of the cut; factor = Enter the convertion factor (to divide). Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Enter the name of the cut
  - `factor`: Enter the convertion factor (to divide)
- Source: `Pierre's Template.ipf:6381`

#### `renormcut_ky`

```igor
Proc renormcut_ky(matname,factor)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `matname`, `factor`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: matname = Enter the name of the cut; factor = Enter the convertion factor (to divide). Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `matname`: Enter the name of the cut
  - `factor`: Enter the convertion factor (to divide)
- Source: `Miscellaneous_Codes.ipf:640`

#### `return_phi_theta`

```igor
Proc return_phi_theta(newhv)
```
- Usage: run from Igor with parameters `newhv`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: newhv = Enter hv. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `csrwave()`, `pcsr()`, `vcsr()`.
- Parameters:
  - `newhv`: Enter hv
- Source: `Pierre's Template.ipf:5855`

#### `return_phi_theta1`

```igor
Proc return_phi_theta1(newhv)
```
- Usage: run from Igor with parameters `newhv`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: newhv = Enter hv. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `csrwave()`, `pcsr()`, `xcsr()`, `vcsr()`.
- Parameters:
  - `newhv`: Enter hv
- Source: `Pierre's Template.ipf:5964`

#### `ReturnFermiEandRes`

```igor
Proc ReturnFermiEandRes(FTemp,thiswavename)
```
- Usage: run from Igor with parameters `FTemp`, `thiswavename`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: FTemp1 = Enter the temperature (in Kelvin); thiswavename = Enter the name of the wave. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data. Main internal calls: `xcsr()`, `x2pnt()`, `thiswavename()`.
- Parameters:
  - `FTemp`: Temperature parameter, usually in kelvin when used in spectral modeling.
  - `thiswavename`: Enter the name of the wave
- Source: `Pierre's Template.ipf:22428`

#### `rm_linear_background`

```igor
Proc rm_linear_background(number_of_pnts,method)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `number_of_pnts`, `method`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: number_of_pnts = Enter the number of points; method = Which method?. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `func_rm_one_linear_background()`, `func_rm_one_AVG_linear_backgnd()`.
- Parameters:
  - `number_of_pnts`: Enter the number of points
  - `method`: Which method?
- Source: `Pierre's Template.ipf:4244`

#### `rot1_zero_incset`

```igor
Proc rot1_zero_incset()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32027`

#### `rot1_zero_set`

```igor
Proc rot1_zero_set()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32039`

#### `rot2d_pi`

```igor
Proc rot2d_pi(mat,angle,px,py,qualityfactor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `mat`, `angle`, `px`, `py`, `qualityfactor`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; angle = Enter the rotation angle (degrees, clockwise); px = Enter the x value of the pivot point; py = Enter the y value of the pivot point; qualityfactor = Enter the quality factor. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_rot2d_pi()`.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `angle`: Enter the rotation angle (degrees, clockwise)
  - `px`: Enter the x value of the pivot point
  - `py`: Enter the y value of the pivot point
  - `qualityfactor`: Enter the quality factor
- Source: `Pierre's Template.ipf:5513`

#### `rot2d_pi2`

```igor
Proc rot2d_pi2(mat,angle,px,py,qualityfactor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `mat`, `angle`, `px`, `py`, `qualityfactor`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; angle = Enter the rotation angle (degrees, clockwise); px = Enter the x value of the pivot point; py = Enter the y value of the pivot point; qualityfactor = Enter the quality factor. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_rot2d_pi()`.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `angle`: Enter the rotation angle (degrees, clockwise)
  - `px`: Enter the x value of the pivot point
  - `py`: Enter the y value of the pivot point
  - `qualityfactor`: Enter the quality factor
- Source: `Miscellaneous_Codes.ipf:6796`

#### `rot2d_pim`

```igor
Proc rot2d_pim(mat,angle,px,py,qualityfactor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `mat`, `angle`, `px`, `py`, `qualityfactor`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; angle = Enter the rotation angle (degrees, clockwise); px = Enter the x value of the pivot point; py = Enter the y value of the pivot point; qualityfactor = Enter the quality factor. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_rot2d_pi()`.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `angle`: Enter the rotation angle (degrees, clockwise)
  - `px`: Enter the x value of the pivot point
  - `py`: Enter the y value of the pivot point
  - `qualityfactor`: Enter the quality factor
- Source: `Miscellaneous_Codes.ipf:7977`

#### `rot2dimage_pi`

```igor
Proc rot2dimage_pi(mat,angle,px,py,quality_fact)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `mat`, `angle`, `px`, `py`, `quality_fact`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: mat = Enter the name of the matrix; angle = Enter the rotation angle (degrees, counterclockwise); px = Enter the x value of the pivot point; py = Enter the y value of the pivot point; Quality_fact = Enter the quality factor. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_rot2d_pi()`.
- Parameters:
  - `mat`: Enter the name of the matrix
  - `angle`: Enter the rotation angle (degrees, counterclockwise)
  - `px`: Enter the x value of the pivot point
  - `py`: Enter the y value of the pivot point
  - `quality_fact`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:31557`

#### `rot_vector`

```igor
Proc rot_vector(xname,yname,angle,pivotx,pivoty)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `xname`, `yname`, `angle`, `pivotx`, `pivoty`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: xname = Enter the name of the vector x (optional); yname = Enter the name of the vector y; angle = Enter the angle of rotation (counterclockwise); pivotx = Pivot x=:; pivoty = Pivot y=:. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `xname`: Enter the name of the vector x (optional)
  - `yname`: Enter the name of the vector y
  - `angle`: Enter the angle of rotation (counterclockwise)
  - `pivotx`: Pivot x=:
  - `pivoty`: Pivot y=:
- Source: `Pierre's Template.ipf:8071`

#### `rot_zero_incset`

```igor
Proc rot_zero_incset()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32021`

#### `rot_zero_set`

```igor
Proc rot_zero_set()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:32033`

#### `rotall`

```igor
Proc rotall(name,angle,Accuracy)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `name`, `angle`, `Accuracy`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: name = Name of Batch; angle = clockwise; Accuracy = Times. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `rot2d_pimf()`.
- Parameters:
  - `name`: Name of Batch
  - `angle`: clockwise
  - `Accuracy`: Times
- Source: `Miscellaneous_Codes.ipf:7960`

#### `rotmat3dk_pi`

```igor
Proc rotmat3dk_pi(matof3dform,angle,px,py,quality_fact,overwriteornot)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `matof3dform`, `angle`, `px`, `py`, `quality_fact`, `overwriteornot`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: matof3dform = Enter the name of the matrix; angle = Enter the rotation angle (degrees, counterclockwise); px = Enter the x value of the pivot point; py = Enter the y value of the pivot point; Quality_fact = Enter the quality factor; overwriteornot = Do you want to overwrite?. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor
- Parameters:
  - `matof3dform`: Enter the name of the matrix
  - `angle`: Enter the rotation angle (degrees, counterclockwise)
  - `px`: Enter the x value of the pivot point
  - `py`: Enter the y value of the pivot point
  - `quality_fact`: Momentum or Q-vector coordinate/index.
  - `overwriteornot`: Do you want to overwrite?
- Source: `Pierre's Template.ipf:31683`

#### `Save3D`

```igor
Proc Save3D(suffixe,comment)
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run from Igor with parameters `suffixe`, `comment`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: suffixe = Enter a suffixe; comment = Enter a comment. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `Wave2file()`.
- Parameters:
  - `suffixe`: Enter a suffixe
  - `comment`: Enter a comment
- Source: `Pierre's Template.ipf:35957`

#### `save_cuts`

```igor
Proc save_cuts()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:7725`

#### `save_edckfname`

```igor
Proc save_edckfname(numcbm)
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run from Igor with parameters `numcbm`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: numcbm = Enter the number of combined EDCs. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `wave2filenokill()`.
- Parameters:
  - `numcbm`: Enter the number of combined EDCs
- Source: `Pierre's Template.ipf:18542`

#### `saveBFAcuts`

```igor
Proc saveBFAcuts()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:16587`

#### `savebulkim`

```igor
Proc savebulkim()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `savegraph()`.
- Source: `Pierre's Template.ipf:18483`

#### `savecuts`

```igor
Proc savecuts()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:18617`

#### `savekcuts`

```igor
Proc savekcuts(sample)
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run from Igor with parameters `sample`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: sample = Enter the name of the sample. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `wave2filenokill()`.
- Parameters:
  - `sample`: Enter the name of the sample
- Source: `Pierre's Template.ipf:18907`

#### `selectcut_pi`

```igor
Proc selectcut_pi(ky1,ky2)
```
- Usage: run from Igor with parameters `ky1`, `ky2`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: ky1 = Enter k_phi1; ky2 = Enter k_phi2. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `ky1`: Enter k_phi1
  - `ky2`: Enter k_phi2
- Source: `Pierre's Template.ipf:29559`

#### `selectlistbkgndremover1`

```igor
Proc selectlistbkgndremover1()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `selectlistbkgndremover2()`.
- Source: `Pierre's Template.ipf:9114`

#### `selectlistbkgndremover2`

```igor
Proc selectlistbkgndremover2(newname)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `newname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: newname = Wave in:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `subbkgndremover()`, `mode()`, `marker()`, `lstyle()`, `rgb()`, `lsize()`.
- Parameters:
  - `newname`: Wave in:
- Source: `Pierre's Template.ipf:9151`

#### `selectlistEMDC`

```igor
Proc selectlistEMDC()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `selectlistEMDC2()`.
- Source: `Pierre's Template.ipf:11448`

#### `selectlistEMDC2`

```igor
Proc selectlistEMDC2(newname)
```
- Usage: run from Igor with parameters `newname`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: newname = Matrix in:. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `EMDC_insertmat()`.
- Parameters:
  - `newname`: Matrix in:
- Source: `Pierre's Template.ipf:11475`

#### `selectlistmap1`

```igor
Proc selectlistmap1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Main internal calls: `selectlistmap2()`.
- Source: `Pierre's Template.ipf:36229`

#### `selectlistmap2`

```igor
Proc selectlistmap2(thischoiceofmap)
```
- Usage: run from Igor with parameters `thischoiceofmap`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: thischoiceofmap = Mapping:. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `WhichListItem()`, `pastemap()`.
- Parameters:
  - `thischoiceofmap`: Mapping:
- Source: `Pierre's Template.ipf:36244`

#### `Set_Hline_slice_control`

```igor
Proc Set_Hline_slice_control()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:28457`

#### `Set_numcombslicer`

```igor
Proc Set_numcombslicer()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:28421`

#### `Set_Vline_slice_control`

```igor
Proc Set_Vline_slice_control()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:28474`

#### `Shift_and_sym_edc_matrix_far`

```igor
Proc Shift_and_sym_edc_matrix_far(matname,Fermilevel)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `matname`, `Fermilevel`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matname = Enter the name of the matrix; Fermilevel = Enter the Fermi level. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `Rescale_pi()`, `sym_edc_far()`, `func_sym_edc_matrix_far()`.
- Parameters:
  - `matname`: Enter the name of the matrix
  - `Fermilevel`: Enter the Fermi level
- Source: `Pierre's Template.ipf:8253`

#### `Show2DImage`

```igor
Proc Show2DImage(matName)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `matName`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `matName`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:1284`

#### `showpointreader`

```igor
Proc showpointreader()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: loads data from files or paths; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:5453`

#### `ShowUpdate`

```igor
Proc ShowUpdate()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `ImageNameList()`, `num2istr()`, `offset()`, `MatName()`, `WaveName()`, `standoff()`.
- Source: `Pierre's Template.ipf:1299`

#### `simulBKFA`

```igor
Proc simulBKFA()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `interp()`, `lstyle()`, `lsize()`, `rgb()`.
- Source: `Pierre's Template.ipf:14762`

#### `simulBugolioubov`

```igor
Proc simulBugolioubov(curvex,curvey,Gap,Gamma0,alpha)
```
- Usage: run from Igor with parameters `curvex`, `curvey`, `Gap`, `Gamma0`, `alpha`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Dialog prompts: curvex = Enter the name of the x wave:; curvey = Enter the name of the y wave:; gap = Enter the superconducting gap Delta (meV); Gamma0 = Im sigma 0:; alpha = alpha in: Im sigma 0+alpha|omega|:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `interp()`, `lstyle()`, `lsize()`, `rgb()`.
- Parameters:
  - `curvex`: Enter the name of the x wave:
  - `curvey`: Enter the name of the y wave:
  - `Gap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `Gamma0`: Im sigma 0:
  - `alpha`: alpha in: Im sigma 0+alpha|omega|:
- Source: `Pierre's Template.ipf:14809`

#### `simulBugolioubovplusmode`

```igor
Proc simulBugolioubovplusmode(curvex,curvey,Gap,mode,lambda,gamma0,alpha,Eresolution,kresolution)
```
- Usage: run from Igor with parameters `curvex`, `curvey`, `Gap`, `mode`, `lambda`, `gamma0`, `alpha`, `Eresolution`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: curvex = Enter the name of the x wave:; curvey = Enter the name of the y wave:; gap = Enter the superconducting gap Delta (meV); Mode = Mode energy:; lambda = Coupling constant lambda:; Gamma0 = Im sigma 0:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Calcselfmode()`, `rgb()`, `FillAkwinSCstate3()`, `SimulkEresolution()`, `lstyle()`, `lsize()`.
- Parameters:
  - `curvex`: Enter the name of the x wave:
  - `curvey`: Enter the name of the y wave:
  - `Gap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `mode`: Step size or energy/voltage increment used by the calculation.
  - `lambda`: Coupling constant lambda:
  - `gamma0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `alpha`: alpha in: Im sigma 0+alpha|omega|:
  - `Eresolution`: Momentum resolution:
  - `kresolution`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:14868`

#### `sm_allcuts`

```igor
Proc sm_allcuts()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `smoothmat_k()`.
- Source: `Pierre's Template.ipf:15678`

#### `sm_edc`

```igor
Proc sm_edc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management.
- Source: `Pierre's Template.ipf:15951`

#### `sm_mdc`

```igor
Proc sm_mdc()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: smooths wave data.
- Source: `Pierre's Template.ipf:15733`

#### `Sortpnts`

```igor
Proc Sortpnts()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:5417`

#### `STM_shift`

```igor
Proc STM_shift(matfixed,matmoved,diffname)
```
- Usage: run from Igor with parameters `matfixed`, `matmoved`, `diffname`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: matfixed = Matrix 1:; matmoved = Matrix 2:; diffname = Name for (1)-(2). Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_STM_shift()`, `transform2equiv_mat()`, `CutNaNedges()`.
- Parameters:
  - `matfixed`: Matrix 1:
  - `matmoved`: Matrix 2:
  - `diffname`: Name for (1)-(2)
- Source: `Pierre's Template.ipf:24092`

#### `sub_bline_pi`

```igor
Proc sub_bline_pi(number_of_average)
```
- Usage: run from Igor with parameters `number_of_average`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mean()`, `pnt2x()`.
- Parameters:
  - `number_of_average`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:5814`

#### `sub_blineedc_pi`

```igor
Proc sub_blineedc_pi(edcname,number_of_average)
```
- Usage: run from Igor with parameters `edcname`, `number_of_average`.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `mean()`, `pnt2x()`.
- Parameters:
  - `edcname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `number_of_average`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:5842`

#### `subEF1`

```igor
Proc subEF1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:20855`

#### `sum_first`

```igor
Proc sum_first(which,how_much)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `which`, `how_much`.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Parameters:
  - `which`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `how_much`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:3280`

#### `SumCompOrder`

```igor
Proc SumCompOrder(sel,A1,A2,A3,theta,wLength,A4,inc_L,inc_an)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `sel`, `A1`, `A2`, `A3`, `theta`, `wLength`, `A4`, `inc_L`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Dialog prompts: sel = Lattice; A1 = Amplitude of Q1; A2 = Amplitude of Q2; A3 = Amplitude of Q3; theta = Angel of Q1 (°); wLength = Wave length of the commensurate orders. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `SumCompOrder_square()`, `SumCompOrder_tria()`.
- Parameters:
  - `sel`: Lattice
  - `A1`: Amplitude of Q1
  - `A2`: Amplitude of Q2
  - `A3`: Amplitude of Q3
  - `theta`: Angel of Q1 (°)
  - `wLength`: Wave length of the commensurate orders
  - `A4`: Amplitude of an additional incommensurate vector
  - `inc_L`: wavelength off from the commensurate value
  - `inc_an`: angle off from the direction of commensurate Q1
- Source: `Models.ipf:11489`

#### `supertemp`

```igor
Proc supertemp()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:23541`

#### `sym_edc`

```igor
Proc sym_edc(cutname,fermi_level,numcombine,numshow)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `cutname`, `fermi_level`, `numcombine`, `numshow`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Dialog prompts: cutname = Enter the name of the cut; fermi_level = Enter the Fermi energy; numcombine = Enter the number of EDCs to be combined (odd number).; numshow = Show one EDC every... EDCs. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `cutname`: Enter the name of the cut
  - `fermi_level`: Enter the Fermi energy
  - `numcombine`: Enter the number of EDCs to be combined (odd number).
  - `numshow`: Show one EDC every... EDCs
- Source: `Pierre's Template.ipf:8104`

#### `sym_edc_far`

```igor
Proc sym_edc_far(edcname)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `edcname`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: edcname = Enter the name of the EDC curvet. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `func_sym_edc_far()`, `interp()`.
- Parameters:
  - `edcname`: Enter the name of the EDC curvet
- Source: `Pierre's Template.ipf:8177`

#### `sym_edc_matrix_far`

```igor
Proc sym_edc_matrix_far(matname)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: matname = Enter the name of the matrix. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `sym_edc_far()`, `func_sym_edc_matrix_far()`.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:8233`

#### `sym_kf_edcs`

```igor
Proc sym_kf_edcs(Fermi_level,avg_num,backgnd_type,num_back,normfrom,normto)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `Fermi_level`, `avg_num`, `backgnd_type`, `num_back`, `normfrom`, `normto`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; smoothing, normalization, or background removal. Dialog prompts: Fermi_level = Fermi level:; avg_num = Number of averged EDCs (odd):; backgnd_type = Background type:; num_back = Points for background fitting:; normfrom = Normalize from (-1=no normalization):; normto = Normalize to (index):. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`, `checkNTP()`, `Remove_simplebgnd_edc()`, `mean()`, `pnt2x
- Parameters:
  - `Fermi_level`: Fermi level:
  - `avg_num`: Number of averged EDCs (odd):
  - `backgnd_type`: Background type:
  - `num_back`: Points for background fitting:
  - `normfrom`: Normalize from (-1=no normalization):
  - `normto`: Normalize to (index):
- Source: `Pierre's Template.ipf:3703`

#### `sym_one_edc`

```igor
Proc sym_one_edc(edcname,Fermi_level)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `edcname`, `Fermi_level`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: edcname = Enter the name of the EDC; Fermi_level = Fermi level:. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `interp()`.
- Parameters:
  - `edcname`: Enter the name of the EDC
  - `Fermi_level`: Fermi level:
- Source: `Pierre's Template.ipf:3670`

#### `Symall`

```igor
Proc Symall()
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Main internal calls: `Shift_and_sym_edc_matrix_far()`.
- Source: `Pierre's Template.ipf:16703`

#### `symG_on_M`

```igor
Proc symG_on_M(matname)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: matname = Enter the name of the matrix. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:20481`

#### `symmat3dhv`

```igor
Proc symmat3dhv()  // symmetrize mat_khv with respect to zero. The zero must be set first
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_symmat3dhv()`.
- Source: `Pierre's Template.ipf:33845`

#### `symright`

```igor
Proc symright(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: name = Enter the name of the matrix. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `Ceil()`, `Interp()`.
- Parameters:
  - `name`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:18975`

#### `tbfit`

```igor
Proc tbfit(name)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Dialog prompts: name = Enter the name of the sample. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Enter the name of the sample
- Source: `Pierre's Template.ipf:3463`

#### `Tdep_temp`

```igor
Proc Tdep_temp(num_to_combine)
```
- Usage: run from Igor with parameters `num_to_combine`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `Remove_simplebgnd_edc()`, `sym_one_edc()`.
- Parameters:
  - `num_to_combine`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:18632`

#### `Tdep_temp1`

```igor
Proc Tdep_temp1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:18664`

#### `temp1001`

```igor
Proc temp1001()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Main internal calls: `normcut()`.
- Source: `Pierre's Template.ipf:16929`

#### `Temp200`

```igor
Proc Temp200()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:22151`

#### `temp33`

```igor
Proc temp33()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Code behavior: smooths wave data.
- Source: `Pierre's Template.ipf:16222`

#### `temp34`

```igor
Proc temp34()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `Shift_and_sym_edc_matrix_far()`, `derivmat()`.
- Source: `Pierre's Template.ipf:16234`

#### `temp35`

```igor
Proc temp35()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:16272`

#### `temp36`

```igor
Proc temp36()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:16291`

#### `temp37`

```igor
Proc temp37()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:16310`

#### `temp38`

```igor
Proc temp38()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays. Main internal calls: `comb_krange()`.
- Source: `Pierre's Template.ipf:16329`

#### `temp39`

```igor
Proc temp39()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics.
- Source: `Pierre's Template.ipf:16356`

#### `temp4`

```igor
Proc temp4()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp()`.
- Source: `Pierre's Template.ipf:19931`

#### `temp40`

```igor
Proc temp40()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics.
- Source: `Pierre's Template.ipf:16373`

#### `temp42`

```igor
Proc temp42()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:16416`

#### `temp45`

```igor
Proc temp45()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays. Main internal calls: `file2wave()`.
- Source: `Pierre's Template.ipf:16430`

#### `temp47`

```igor
Proc temp47()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `interp2D()`.
- Source: `Pierre's Template.ipf:16446`

#### `temp6macro`

```igor
Proc temp6macro()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis.
- Source: `Pierre's Template.ipf:21776`

#### `temp7macro`

```igor
Proc temp7macro()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis.
- Source: `Pierre's Template.ipf:21800`

#### `temp8`

```igor
Proc temp8()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Code behavior: duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:8197`

#### `temp8macro`

```igor
Proc temp8macro()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis.
- Source: `Pierre's Template.ipf:21824`

#### `temp9macro`

```igor
Proc temp9macro(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: Matname = Matrix name. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `rescale_pi()`, `sym_edc_matrix_far()`, `EDC_pi()`, `derivmat()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:21848`

#### `temphv`

```igor
Proc temphv()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Pierre's Template.ipf:20635`

#### `temphv1`

```igor
Proc temphv1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:20664`

#### `temphv2`

```igor
Proc temphv2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:20649`

#### `text_to_pxt`

```igor
Proc text_to_pxt(datanumber)
```
- Usage: run from Igor with parameters `datanumber`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: datanumber = Enter the number of the data. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts.
- Parameters:
  - `datanumber`: Enter the number of the data
- Source: `Pierre's Template.ipf:18732`

#### `theta1_zero_incset`

```igor
Proc theta1_zero_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31918`

#### `theta1_zero_set`

```igor
Proc theta1_zero_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31936`

#### `theta_zero_incset`

```igor
Proc theta_zero_incset()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31906`

#### `theta_zero_incsetH`

```igor
Proc theta_zero_incsetH()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31912`

#### `theta_zero_set`

```igor
Proc theta_zero_set()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31924`

#### `theta_zero_setH`

```igor
Proc theta_zero_setH()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Source: `Pierre's Template.ipf:31930`

#### `thisguy`

```igor
Proc thisguy()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:22595`

#### `thisguy1`

```igor
Proc thisguy1()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output. Main internal calls: `CutNaNedges()`, `wave2filenokill()`.
- Source: `Pierre's Template.ipf:22608`

#### `thisguy120`

```igor
Proc thisguy120()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22827`

#### `thisguy120VD`

```igor
Proc thisguy120VD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `CutNaNedges()`, `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22862`

#### `thisguy150`

```igor
Proc thisguy150()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22997`

#### `thisguy150VD`

```igor
Proc thisguy150VD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `CutNaNedges()`, `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:23032`

#### `thisguy2`

```igor
Proc thisguy2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22622`

#### `thisguy25Co`

```igor
Proc thisguy25Co()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:23067`

#### `thisguy25CoVD`

```igor
Proc thisguy25CoVD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:23122`

#### `thisguy3`

```igor
Proc thisguy3()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `CutNaNedges()`, `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22656`

#### `thisguy4`

```igor
Proc thisguy4()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `derivmat()`, `Wave2filenokill()`.
- Source: `Pierre's Template.ipf:22691`

#### `thisguy5`

```igor
Proc thisguy5()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `derivmat()`.
- Source: `Pierre's Template.ipf:22713`

#### `thisguy6`

```igor
Proc thisguy6()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Main internal calls: `ln_mat_pi()`, `NaN0()`.
- Source: `Pierre's Template.ipf:22735`

#### `thisguy80`

```igor
Proc thisguy80()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22757`

#### `thisguy80VD`

```igor
Proc thisguy80VD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `CutNaNedges()`, `wave2filenokill()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:22792`

#### `thisguychange120`

```igor
Proc thisguychange120()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:22897`

#### `thisguychange25`

```igor
Proc thisguychange25()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:22957`

#### `thisguychange25Co`

```igor
Proc thisguychange25Co()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:23102`

#### `thisguychange25VD`

```igor
Proc thisguychange25VD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:22977`

#### `thisguychange80`

```igor
Proc thisguychange80()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:22917`

#### `thisguychange80VD`

```igor
Proc thisguychange80VD()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:22937`

#### `tosave`

```igor
Proc tosave()
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Main internal calls: `wave2filenokill()`.
- Source: `Pierre's Template.ipf:18962`

#### `total_edcarea`

```igor
Proc total_edcarea(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Dialog prompts: matname = Enter the name of the matrix. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `pnt2x()`, `mean()`.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:19462`

#### `transform2equiv_mat`

```igor
Proc transform2equiv_mat(nameref,name2)
```
- Usage: run from Igor with parameters `nameref`, `name2`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: nameref = Enter the name of the model matrix; name2 = Enter the name of the matrix to modify. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Func_transform2equiv_mat()`.
- Parameters:
  - `nameref`: Enter the name of the model matrix
  - `name2`: Enter the name of the matrix to modify
- Source: `Pierre's Template.ipf:9297`

#### `tt`

```igor
proc tt(theta1,name)
```
- Notes: End Initial procedure, now adapted to be function Usage: run from Igor with parameters `theta1`, `name`. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `p2dtopeak()`.
- Parameters:
  - `theta1`: angle to rotate counterclockwise
  - `name`: the data want to be rotated
- Source: `Miscellaneous_Codes.ipf:124`

#### `TTGsimu`

```igor
Proc TTGsimu(xx,a1,a2,a3,theta1,theta2,theta3)
```
- Usage: run from Igor with parameters `xx`, `a1`, `a2`, `a3`, `theta1`, `theta2`, `theta3`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Dialog prompts: xx = Length of the image (Å); a1 = Lattice constant of layer 1 (Å); a2 = Lattice constant of layer 2 (Å); a3 = Lattice constant of layer 3 (Å); theta1 = Twist angle of layer 1 (˚); theta2 = Twist angle of layer 2 (˚). Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `ExampleTriangularLattice()`, `rgb()`.
- Parameters:
  - `xx`: Length of the image (Å)
  - `a1`: Lattice constant of layer 1 (Å)
  - `a2`: Lattice constant of layer 2 (Å)
  - `a3`: Lattice constant of layer 3 (Å)
  - `theta1`: Twist angle of layer 1 (˚)
  - `theta2`: Twist angle of layer 2 (˚)
  - `theta3`: Twist angle of layer 3 (˚)
- Source: `General_Simu.ipf:1935`

#### `twistanglemap_3qave`

```igor
Proc twistanglemap_3qave(name2,size,threshold)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name2`, `size`, `threshold`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Dialog prompts: name2 = name of the topography; size = Move window length (nm); threshold = Threshold for twist angle (˚). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `makechunkdatatamap()`.
- Parameters:
  - `name2`: name of the topography
  - `size`: Move window length (nm)
  - `threshold`: Threshold for twist angle (˚)
- Source: `Triangle Lattice_Graphene_like.ipf:252`

#### `unstore_edc_ef`

```igor
Proc unstore_edc_ef(number)
```
- Usage: run from Igor with parameters `number`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: number = Enter the number of the list. Code behavior: loads data from files or paths; asks the user for parameters through Igor prompts.
- Parameters:
  - `number`: Enter the number of the list
- Source: `Pierre's Template.ipf:18526`

#### `update_4circles`

```igor
Proc update_4circles()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing. Main internal calls: `calc4circles()`.
- Source: `Pierre's Template.ipf:31882`

#### `update_4circles2f`

```igor
Proc update_4circles2f()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing. Main internal calls: `calc4circles2f()`.
- Source: `Pierre's Template.ipf:31890`

#### `update_global_par`

```igor
Proc update_global_par()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Source: `Pierre's Template.ipf:31866`

#### `update_image_fs`

```igor
Proc update_image_fs()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `update_image_fs_approx()`, `update_image_fs_exact()`.
- Source: `Pierre's Template.ipf:31437`

#### `update_image_fs_approx`

```igor
Proc update_image_fs_approx()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `mat2dk_pi()`, `rot2dimage_pi()`.
- Source: `Pierre's Template.ipf:31449`

#### `update_image_fs_exact`

```igor
Proc update_image_fs_exact()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `ImageNameList()`, `make_mat2dk()`, `rot2dimage_pi()`.
- Source: `Pierre's Template.ipf:34975`

#### `update_making_fs`

```igor
Proc update_making_fs()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`. Main internal calls: `update_making_fs_approx()`, `update_making_fs_exact()`.
- Source: `Pierre's Template.ipf:31344`

#### `update_making_fs2f`

```igor
Proc update_making_fs2f()//note: the calculated points are in units of pi/a for both a and b axis. The user can adjust the calculated curves to find a/b
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:33430`

#### `update_making_fs_approx`

```igor
Proc update_making_fs_approx()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:31356`

#### `update_making_fs_exact`

```igor
Proc update_making_fs_exact()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:31393`

#### `update_making_fsH`

```igor
Proc update_making_fsH()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing.
- Source: `Pierre's Template.ipf:33055`

#### `update_predict_cuts`

```igor
Proc update_predict_cuts() : ButtonControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Transformpredicted()`.
- Source: `Pierre's Template.ipf:26052`

#### `update_predict_par`

```igor
Proc update_predict_par()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Pierre's Template.ipf`.
- Source: `Pierre's Template.ipf:31874`

#### `UpdateAppend`

```igor
Macro UpdateAppend(Mat)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run from Igor with parameters `Mat`.
- Notes: Igor macro entry point for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: Mat = input mat name. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `Mat`: input mat name
- Source: `Pierre's Template.ipf:1917`

#### `updateCsrforslicer`

```igor
Proc updateCsrforslicer()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction.
- Source: `Pierre's Template.ipf:25358`

#### `UpdateEdc`

```igor
Proc UpdateEdc()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `num2istr()`, `offset()`.
- Source: `Pierre's Template.ipf:1417`

#### `updateHexagonFS`

```igor
Proc updateHexagonFS()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Makehexagon()`, `rgb()`.
- Source: `Pierre's Template.ipf:33092`

#### `updateHexagonFS1`

```igor
Proc updateHexagonFS1()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Makehexagon()`, `rgb()`.
- Source: `Pierre's Template.ipf:33658`

#### `UpdateMdc`

```igor
Proc UpdateMdc()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `num2istr()`, `offset()`.
- Source: `Pierre's Template.ipf:1393`

#### `vb_vs_hv`

```igor
Proc vb_vs_hv(normvaluelow,normvaluehigh)
```
- Usage: run from Igor with parameters `normvaluelow`, `normvaluehigh`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: normvaluelow = Low E of normalization; normvaluehigh = High E of normalization. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `combine_range()`, `mean()`, `bluetored()`.
- Parameters:
  - `normvaluelow`: Low E of normalization
  - `normvaluehigh`: High E of normalization
- Source: `Pierre's Template.ipf:20595`

#### `XGM_image`

```igor
Proc XGM_image()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:19420`

#### `XGMX_image`

```igor
Proc XGMX_image()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:19368`

#### `zeroNaN`

```igor
Proc zeroNaN(matname)
```
- Usage: run from Igor with parameters `matname`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: matname = Enter the name of the matrix. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `func_zeroNaN()`.
- Parameters:
  - `matname`: Enter the name of the matrix
- Source: `Pierre's Template.ipf:7483`

#### `zoom_forCsrslicer`

```igor
Proc zoom_forCsrslicer()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`, `Graph_zoomforCsrSlicer()`.
- Source: `Pierre's Template.ipf:25748`


### Windows, panels, and menus

#### `Curvature_panel`

```igor
Window Curvature_panel() : Graph
```
- Notes: Curvature panel Usage: call `Curvature_panel()` to recreate or bring up the Igor window/panel. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; writes or exports data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:12264`

#### `EMDC_Visualizer_Control`

```igor
Window EMDC_Visualizer_Control() : Graph
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `EMDC_Visualizer_Control()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; smooths wave data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11506`

#### `Help_EDC`

```igor
Window Help_EDC() : Graph
```
- Usage: call `Help_EDC()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10529`

#### `Help_EDC_data`

```igor
Window Help_EDC_data() : Graph
```
- Usage: call `Help_EDC_data()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10354`

#### `Help_Fermi_Profile`

```igor
Window Help_Fermi_Profile() : Graph
```
- Usage: call `Help_Fermi_Profile()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; smooths wave data; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10964`

#### `Help_MDC`

```igor
Window Help_MDC() : Graph
```
- Usage: call `Help_MDC()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10579`

#### `Help_MDC_Data_ploter`

```igor
Window Help_MDC_Data_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_MDC_Data_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10402`

#### `Help_MDC_EDC`

```igor
Window Help_MDC_EDC() : Graph
```
- Usage: call `Help_MDC_EDC()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10505`

#### `Help_MDCfit`

```igor
Window Help_MDCfit() : Graph
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call `Help_MDCfit()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10632`

#### `Help_Plot_EDC`

```igor
Window Help_Plot_EDC() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Plot_EDC()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10602`

#### `Help_Plot_MDC`

```igor
Window Help_Plot_MDC() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_Plot_MDC()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10617`

#### `Help_subtract_EDC_Dploter`

```igor
Window Help_subtract_EDC_Dploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Help_subtract_EDC_Dploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:10491`

#### `Help_symetrize_EDC_mat`

```igor
Window Help_symetrize_EDC_mat() : Graph
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call `Help_symetrize_EDC_mat()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:11301`

#### `image_fs_ploter`

```igor
Window image_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `image_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `rgb()`, `lstyle()`, `lsize()`, `lSize()`, `lStyle()`, `manTick()`.
- Source: `Pierre's Template.ipf:24625`

#### `image_fs_ploter2f`

```igor
Window image_fs_ploter2f() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `image_fs_ploter2f()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `lSize()`, `lStyle()`, `rgb()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:24772`

#### `image_fs_ploterHex`

```igor
Window image_fs_ploterHex() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `image_fs_ploterHex()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `lStyle()`, `rgb()`, `manTick()`, `manMinor()`.
- Source: `Pierre's Template.ipf:33463`

#### `making_fs`

```igor
Window making_fs() : Graph
```
- Usage: call `making_fs()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `mode()`, `marker()`, `lSize()`, `lStyle()`, `rgb()`, `msize()`.
- Source: `Pierre's Template.ipf:24471`

#### `making_fs2f`

```igor
Window making_fs2f() : Graph
```
- Usage: call `making_fs2f()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `mode()`, `marker()`, `lSize()`, `lStyle()`, `rgb()`, `msize()`.
- Source: `Pierre's Template.ipf:33307`

#### `making_fsHEX`

```igor
Window making_fsHEX() : Graph
```
- Usage: call `making_fsHEX()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `mode()`, `marker()`, `lStyle()`, `rgb()`, `msize()`, `manTick()`.
- Source: `Pierre's Template.ipf:32827`

#### `mat3d_fs_ploter`

```igor
Window mat3d_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `mat3d_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24333`

#### `mat3dHD_fs_ploter`

```igor
Window mat3dHD_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `mat3dHD_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24379`

#### `mat3dk_fs_ploter`

```igor
Window mat3dk_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `mat3dk_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24402`

#### `mat3dkHD_fs_ploter`

```igor
Window mat3dkHD_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `mat3dkHD_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24448`

#### `mat3dkVD_fs_ploter`

```igor
Window mat3dkVD_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `mat3dkVD_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24425`

#### `mat3dVD_fs_ploter`

```igor
Window mat3dVD_fs_ploter() : Graph
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `mat3dVD_fs_ploter()` to recreate or bring up the Igor window/panel.
- Notes: Igor window/panel recreation routine for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Source: `Pierre's Template.ipf:24356`


### Computational and data helpers

#### `angleline_3dp`

```igor
Function angleline_3dp()
```
- Notes: INTERACTING FUNCTIONAL: Update indicative lines Usage: call `angleline_3dp()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartpp()`, `findendpp()`, `findstartqq()`, `findendqq()`.
- Source: `Smart_3D_Viewer_New.ipf:1489`

#### `angleline_3dpf`

```igor
Function angleline_3dpf()
```
- Notes: INTERACTING FUNCTIONAL: Update indicative lines Usage: call `angleline_3dpf()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartppf()`, `findendppf()`, `findstartqqf()`, `findendqqf()`.
- Source: `Smart_3D_Viewer_New.ipf:6600`

#### `angleline_3dpf2`

```igor
Function angleline_3dpf2()
```
- Notes: INTERACTING FUNCTIONAL: Update indicative lines Usage: call `angleline_3dpf2()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartppf()`, `findendppf()`, `findstartqqf()`, `findendqqf()`.
- Source: `Models.ipf:1311`

#### `anglelinecutH`

```igor
Function anglelinecutH(mat,angle,Zn,addY,normornot,smornot)
```
- Notes: ## 01 Main Function Usage: call as `anglelinecutH(mat, angle, Zn, addY, normornot, smornot)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartpp()`, `findendpp()`, `Normalinecut()`, `smoothtimes()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `normornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:1690`

#### `anglelinecutH_absamp`

```igor
Function anglelinecutH_absamp(mat,angle,Zn,addY)
```
- Usage: call as `anglelinecutH_absamp(mat, angle, Zn, addY)`.
- Notes: Function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartpp()`, `findendpp()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:3281`

#### `anglelinecutHf`

```igor
Function anglelinecutHf(mat,angle,Zn,addY,normornot,smornot)
```
- Notes: ## 01 Main Function Usage: call as `anglelinecutHf(mat, angle, Zn, addY, normornot, smornot)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartppf()`, `findendppf()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `normornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:6685`

#### `anglelinecutV`

```igor
Function anglelinecutV(mat,angle,Zn,addX,normornot,smornot)
```
- Notes: ## 01 Main Function Usage: call as `anglelinecutV(mat, angle, Zn, addX, normornot, smornot)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartqq()`, `findendqq()`, `Normalinecut()`, `smoothtimes()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `normornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:1932`

#### `anglelinecutV_absamp`

```igor
Function anglelinecutV_absamp(mat,angle,Zn,addX)
```
- Usage: call as `anglelinecutV_absamp(mat, angle, Zn, addX)`.
- Notes: Function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartqq()`, `findendqq()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:3355`

#### `anglelinecutVf`

```igor
Function anglelinecutVf(mat,angle,Zn,addX,normornot,smornot)
```
- Notes: ## 01 Main Function Usage: call as `anglelinecutVf(mat, angle, Zn, addX, normornot, smornot)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findstartqqf()`, `findendqqf()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
  - `addX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `normornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Smart_3D_Viewer_New.ipf:6927`

#### `AVG_Calc_avgcirc`

```igor
Function AVG_Calc_avgcirc()
```
- Usage: call `AVG_Calc_avgcirc()` from Igor procedure code or the command line.
- Notes: Function for Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:12850`

#### `AVG_UpdateCirc`

```igor
Function AVG_UpdateCirc()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `AVG_UpdateCirc()` from Igor procedure code or the command line.
- Notes: Function for Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:12924`

#### `BKFA_adjust_EF`

```igor
Function BKFA_adjust_EF(matname,correctwave)
```
- Usage: call as `BKFA_adjust_EF(matname, correctwave)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `correctwave`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:14716`

#### `boxfunc`

```igor
Function boxfunc(m,x)
```
- Usage: call as `boxfunc(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21699`

#### `Button_Help_Divide_by_FD`

```igor
Function Button_Help_Divide_by_FD(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `Help_Divide_by_FD()`.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:9518`

#### `ButtonCurv_panel`

```igor
Function ButtonCurv_panel(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Curv_panel_ini()`.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `Wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:12416`

#### `Cal_Posi`

```igor
Function Cal_Posi(x,y,z)
```
- Usage: call as `Cal_Posi(x, y, z)`.
- Notes: Function in `Pierre's Template.ipf`.
- Parameters:
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `z`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1891`

#### `calcangle2kVcut`

```igor
Function calcangle2kVcut(matname,newname,paraVy,W_root,phiangle,thetaangle)
```
- Usage: call as `calcangle2kVcut(matname, newname, paraVy, W_root, phiangle, thetaangle)`.
- Notes: Utility function for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Main internal calls: `Interp2D()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `newname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `paraVy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `W_root`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `phiangle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `thetaangle`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Pierre's Template.ipf:22288`

#### `Calcangleinoctant`

```igor
Function/WAVE Calcangleinoctant(waveforx,wavefory)//Calculate the angle along FS with respect to M-X (antinode line)  Note: in degrees
```
- Usage: call as `Calcangleinoctant(waveforx, wavefory)`.
- Notes: Wave-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `atan2()`.
- Parameters:
  - `waveforx`: Igor wave reference used as input or output.
  - `wavefory`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:29046`

#### `calcrealself_i`

```igor
Function calcrealself_i(realself,energy,cmode,cgap,lambda)
```
- Usage: call as `calcrealself_i(realself, energy, cmode, cgap, lambda)`.
- Notes: Utility function for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `realself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `energy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `cmode`: Step size or energy/voltage increment used by the calculation.
  - `cgap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `lambda`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:15175`

#### `Calcselfmode`

```igor
Function Calcselfmode(realself,imself,longimself,deltaself,offself,mode,cmode,cGap,lambda,Gamma0,alpha)
```
- Usage: call as `Calcselfmode(realself, imself, longimself, deltaself, offself, mode, cmode, cGap)`.
- Notes: Utility function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `realself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `imself`: Used in: Duplicate/O longimself imself
  - `longimself`: Used in: Duplicate/O longimself thiswave / Duplicate/O longimself imself
  - `deltaself`: Step size or energy/voltage increment used by the calculation.
  - `offself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mode`: Step size or energy/voltage increment used by the calculation.
  - `cmode`: Step size or energy/voltage increment used by the calculation.
  - `cGap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `lambda`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Gamma0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - ... 1 additional parameters
- Source: `Pierre's Template.ipf:14937`

#### `calculateLLall`

```igor
Function calculateLLall(hv,B,ED,num)
```
- Usage: call as `calculateLLall(hv, B, ED, num)`.
- Notes: Utility function for topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves.
- Parameters:
  - `hv`: =4.4/0.025//fermi velocity (meV*A)
  - `B`: =4// magnetic field (T)
  - `ED`: =-4.4//Dirac point (meV)
  - `num`: =100
- Source: `Models.ipf:4569`

#### `calculateLLNew`

```igor
Function calculateLLNew(hv,B,ED,num)
```
- Usage: call as `calculateLLNew(hv, B, ED, num)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `zero()`, `margin()`.
- Parameters:
  - `hv`: =4.4/0.025//fermi velocity (meV*A)
  - `B`: =4// magnetic field (T)
  - `ED`: =-4.4//Dirac point (meV)
  - `num`: =40
- Source: `Models.ipf:4234`

#### `calculateLLNewcons`

```igor
Function calculateLLNewcons(hv,B,ED,num)
```
- Usage: call as `calculateLLNewcons(hv, B, ED, num)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `hv`: =4.4/0.025//fermi velocity (meV*A)
  - `B`: =4// magnetic field (T)
  - `ED`: =-4.4//Dirac point (meV)
  - `num`: =40
- Source: `Models.ipf:4337`

#### `calculateRC`

```igor
Function calculateRC(pp,qq,pc,qc,angle)
```
- Usage: call as `calculateRC(pp, qq, pc, qc, angle)`.
- Notes: Utility function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: creates output waves.
- Parameters:
  - `pp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qq`: Momentum or Q-vector coordinate/index.
  - `pc`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qc`: Momentum or Q-vector coordinate/index.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Symmetrization.ipf:229`

#### `calculateRC2`

```igor
Function calculateRC2(pp,qq,pc,qc,angle)
```
- Usage: call as `calculateRC2(pp, qq, pc, qc, angle)`.
- Notes: Utility function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: creates output waves.
- Parameters:
  - `pp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qq`: Momentum or Q-vector coordinate/index.
  - `pc`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qc`: Momentum or Q-vector coordinate/index.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Symmetrization.ipf:263`

#### `Center_detector`

```igor
Function Center_detector(matname)
```
- Usage: call as `Center_detector(matname)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:17078`

#### `checkNTP`

```igor
Function checkNTP(NTPname,thisnumber)
```
- Usage: call as `checkNTP(NTPname, thisnumber)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `NTPname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `thisnumber`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:4085`

#### `CheckProc_2dupdate`

```igor
Function CheckProc_2dupdate(ctrlName) : CheckBoxControl //problem!!
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `CheckProc_2dupdate(ctrlName)`.
- Notes: Function for Igor wave/matrix/cube data operation. Main internal calls: `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Pierre's Template.ipf:13862`

#### `cons_SumCompOrder_tria`

```igor
Function cons_SumCompOrder_tria(A1,A2,A3,theta,A4,inc_L,inc_an,wLength)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `cons_SumCompOrder_tria(A1, A2, A3, theta, A4, inc_L, inc_an, wLength)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations. Main internal calls: `cvtcmplx()`, `max()`.
- Parameters:
  - `A1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `A2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `A3`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `A4`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `inc_L`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `inc_an`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `wLength`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:11713`

#### `Constantoffset_nF`

```igor
Function Constantoffset_nF(Shift,number_of_same)
```
- Usage: call as `Constantoffset_nF(Shift, number_of_same)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `WaveName()`, `offset()`.
- Parameters:
  - `Shift`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `number_of_same`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Pierre's Template.ipf:6480`

#### `Constantoffset_nFx`

```igor
Function Constantoffset_nFx(Shift,number_of_same,xx)
```
- Usage: call as `Constantoffset_nFx(Shift, number_of_same, xx)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `WaveName()`, `offset()`.
- Parameters:
  - `Shift`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `number_of_same`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `xx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `SmartDisplay.ipf:1504`

#### `convert1chunkcut`

```igor
Function convert1chunkcut(achunk,kchunk,angle,addphi,addtheta,slit)   //Findroots method
```
- Usage: call as `convert1chunkcut(achunk, kchunk, angle, addphi, addtheta, slit)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp2d()`, `Interp3d()`.
- Parameters:
  - `achunk`: Used in: Make/O/N=(dimsize(achunk,0),dimsize(achunk,1)) thismat / Make/O/N=(dimsize(achunk,0),dimsize(achunk,1)) thismat
  - `kchunk`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `addphi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `addtheta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `slit`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34630`

#### `convert1chunkcut2d`

```igor
Function convert1chunkcut2d(achunk,kchunk,angle,addphi,addtheta,slit,Fermilevel,irange)   //Findroots method
```
- Usage: call as `convert1chunkcut2d(achunk, kchunk, angle, addphi, addtheta, slit, Fermilevel, irange)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp2d()`.
- Parameters:
  - `achunk`: Used in: Make/O/N=(dimsize(achunk,0),dimsize(achunk,1)) achunk2d
  - `kchunk`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `addphi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `addtheta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `slit`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Fermilevel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `irange`: Axis limit or numeric range boundary used for cropping, fitting, or display.
- Source: `Pierre's Template.ipf:35183`

#### `curv_calc_EDC`

```igor
function curv_calc_EDC(maty,matyy,thematrix,ifact)
```
- Usage: call as `curv_calc_EDC(maty, matyy, thematrix, ifact)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `maty`: Matrix/2D wave input or matrix output name.
  - `matyy`: Matrix/2D wave input or matrix output name.
  - `thematrix`: Matrix/2D wave input or matrix output name.
  - `ifact`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:12676`

#### `curv_calc_MDC`

```igor
function curv_calc_MDC(matx,matxx,thematrix,ifact)
```
- Usage: call as `curv_calc_MDC(matx, matxx, thematrix, ifact)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `matx`: Matrix/2D wave input or matrix output name.
  - `matxx`: Matrix/2D wave input or matrix output name.
  - `thematrix`: Matrix/2D wave input or matrix output name.
  - `ifact`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:12690`

#### `curv_data`

```igor
function curv_data()
```
- Usage: call `curv_data()` from Igor procedure code or the command line.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: temp = Enter the wave. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `EDCCurv()`, `MDCcurv()`, `twoDcurv()`.
- Source: `Pierre's Template.ipf:13534`

#### `curvature`

```igor
function curvature(mat,num,box,choice,factor) //curvature. 1 is EDC, 2 is MDC, 3 is 2D with unit problem, 4 is the method that using the max between EDC and MDC(not efficient).
```
- Usage: call as `curvature(mat, num, box, choice, factor)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `DM1()`, `max()`, `wintype()`. Update log: 2021-09-17: Signal-enhancement routine based on Peng Zhang's curvature-analysis method for highlighting dispersive features.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:13073`

#### `curvature2`

```igor
function curvature2(mat,num,box,num2,box2,choice,factor) //there are two groups of smooth parameters compared to curvature(). choice are 3 and 4.
```
- Usage: call as `curvature2(mat, num, box, num2, box2, choice, factor)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `DM1()`, `max()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num2`: Number of spectra, waves, points, bins, or iterations to process.
  - `box2`: End point, index, or coordinate for the selected range.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:13193`

#### `curvature4mat3d`

```igor
Function curvature4mat3d(mat,matcurv,num,box,choice,factor) //curvature. 1 is EDC, 2 is MDC
```
- Usage: call as `curvature4mat3d(mat, matcurv, num, box, choice, factor)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `DM1()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `matcurv`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:28830`

#### `Deriv`

```igor
function Deriv(OldM,num,box,choice) //command line of 2nd derivative, the same as main panel.
```
- Notes: Peng Zhang curvature Usage: call as `Deriv(OldM, num, box, choice)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `DM()`.
- Parameters:
  - `OldM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:13041`

#### `derivthischunkcut`

```igor
Function derivthischunkcut(oldname,newname,pass,boxwidth,factor, choice)
```
- Usage: call as `derivthischunkcut(oldname, newname, pass, boxwidth, factor, choice)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data. Main internal calls: `curvature4mat3d()`.
- Parameters:
  - `oldname`: Used in: Make/O/N=(dimsize(oldname,2)) thiswave / Make/O/N=(dimsize(oldname,1)) thiswave
  - `newname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `pass`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `boxwidth`: Igor wave reference used as input or output.
  - `factor`: for curvature only
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34904`

#### `disp`

```igor
function disp(mat) //disp a matrix
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `disp(mat)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:13478`

#### `DM`

```igor
function DM(OldM,NewM,num,box,choice)
```
- Usage: call as `DM(OldM, NewM, num, box, choice)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `OldM`: Used in: Duplicate/O OldM intermediatemat / Duplicate/O OldM intermediatemat
  - `NewM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:2409`

#### `DM1`

```igor
function DM1(OldM,NewM,num,box,choice)
```
- Usage: call as `DM1(OldM, NewM, num, box, choice)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `OldM`: Used in: Duplicate/O OldM intermediatemat / Duplicate/O OldM intermediatemat
  - `NewM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `choice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:13298`

#### `doInsetYesNo`

```igor
Function doInsetYesNo(answer)  // This funciton adds or removes the inset in the graph
```
- Notes: FUNCTIONS Usage: call as `doInsetYesNo(answer)`. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `lSize()`, `lStyle()`, `tick()`, `mirror()`, `lblPos()`, `freePos()`.
- Parameters:
  - `answer`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:36407`

#### `DrawconeBFA`

```igor
Function DrawconeBFA()
```
- Usage: call `DrawconeBFA()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward. Main internal calls: `Contourz()`.
- Source: `Pierre's Template.ipf:16072`

#### `EDCcurv`

```igor
function EDCcurv()
```
- Usage: call `EDCcurv()` from Igor procedure code or the command line.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `curvature()`, `Deriv()`.
- Source: `Pierre's Template.ipf:13550`

#### `EdcHWHM`

```igor
Function EdcHWHM(para,x)
```
- Usage: call as `EdcHWHM(para, x)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `para`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1813`

#### `EMDC_adjustEDClines`

```igor
Function EMDC_adjustEDClines(Waveleft,Waveright,startvalue,endvalue)
```
- Usage: call as `EMDC_adjustEDClines(Waveleft, Waveright, startvalue, endvalue)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `Waveleft`: Igor wave reference used as input or output.
  - `Waveright`: Igor wave reference used as input or output.
  - `startvalue`: Start point, index, or coordinate for the selected range.
  - `endvalue`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:11698`

#### `EMDC_adjustMDClines`

```igor
Function EMDC_adjustMDClines(Wavelow,Wavehigh,startvalue,endvalue)
```
- Usage: call as `EMDC_adjustMDClines(Wavelow, Wavehigh, startvalue, endvalue)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `Wavelow`: Igor wave reference used as input or output.
  - `Wavehigh`: Igor wave reference used as input or output.
  - `startvalue`: Start point, index, or coordinate for the selected range.
  - `endvalue`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:11708`

#### `Fermi`

```igor
Function Fermi(w,x)
```
- Usage: call as `Fermi(w, x)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:2120`

#### `Fermi_convolve_at_T_old`

```igor
Function Fermi_convolve_at_T_old(parawave,thiswave,thisenergy) : fitfunc
```
- Usage: call as `Fermi_convolve_at_T_old(parawave, thiswave, thisenergy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp()`.
- Parameters:
  - `parawave`: Igor wave reference used as input or output.
  - `thiswave`: Igor wave reference used as input or output.
  - `thisenergy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:22410`

#### `fermigap`

```igor
Function fermigap(m,x)
```
- Usage: call as `fermigap(m, x)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21740`

#### `FFTranglemap`

```igor
Function FFTranglemap(name)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTranglemap(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `di()`, `color3sfft()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Triangle Lattice_Graphene_like.ipf:37`

#### `FFTranglemap2`

```igor
Function FFTranglemap2(name)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTranglemap2(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `cvtcmplx()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Triangle Lattice_Graphene_like.ipf:59`

#### `FillAkw`

```igor
Function FillAkw(Akw,curvex,curvey,realself,imself,cGap)
```
- Usage: call as `FillAkw(Akw, curvex, curvey, realself, imself, cGap)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Main internal calls: `interp()`.
- Parameters:
  - `Akw`: Igor wave reference used as input or output.
  - `curvex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `curvey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `realself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `imself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `cGap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
- Source: `Pierre's Template.ipf:14986`

#### `FillAkwinSCstate`

```igor
Function FillAkwinSCstate(Akw,curvex,curvey,realself,imself,cGap)
```
- Usage: call as `FillAkwinSCstate(Akw, curvex, curvey, realself, imself, cGap)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `interp()`.
- Parameters:
  - `Akw`: Igor wave reference used as input or output.
  - `curvex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `curvey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `realself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `imself`: Used in: Make/O/C/N=(dimsize(imself,0)) cself
  - `cGap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
- Source: `Pierre's Template.ipf:15020`

#### `FillAkwinSCstate2`

```igor
Function FillAkwinSCstate2(Akw,curvex,curvey,realself,imself,Gap)
```
- Usage: call as `FillAkwinSCstate2(Akw, curvex, curvey, realself, imself, Gap)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `Akw`: Igor wave reference used as input or output.
  - `curvex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `curvey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `realself`: Used in: Duplicate/O realself Ek / Duplicate/O realself uk
  - `imself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Gap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
- Source: `Pierre's Template.ipf:15049`

#### `FillAkwinSCstate3`

```igor
Function FillAkwinSCstate3(Akw,curvex,curvey,realself,imself,Gap)
```
- Usage: call as `FillAkwinSCstate3(Akw, curvex, curvey, realself, imself, Gap)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `Akw`: Igor wave reference used as input or output.
  - `curvex`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `curvey`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `realself`: Used in: Make/O/C/N=(dimsize(realself,0)) Zofw / Make/O/N=(dimsize(realself,0)) omegawave
  - `imself`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Gap`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
- Source: `Pierre's Template.ipf:15080`

#### `fillCsrslicerimage`

```igor
Function fillCsrslicerimage(thisma3d,thisimageforslicer,thisCsrslicerinter,thislattice,thisrot)
```
- Usage: call as `fillCsrslicerimage(thisma3d, thisimageforslicer, thisCsrslicerinter, thislattice, thisrot)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Main internal calls: `Interp3d()`.
- Parameters:
  - `thisma3d`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `thisimageforslicer`: Igor wave or matrix input consumed by the calculation.
  - `thisCsrslicerinter`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `thislattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `thisrot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:25320`

#### `findinglengthforCsrslicer`

```igor
Function findinglengthforCsrslicer(interpntsforCsrslicer)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `findinglengthforCsrslicer(interpntsforCsrslicer)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation.
- Parameters:
  - `interpntsforCsrslicer`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:24907`

#### `findrangeforangle_LH`

```igor
Function/Wave findrangeforangle_LH(mat,angle,Zn)
```
- Notes: ## 04 Find the addY Range for rotated H-Linecut Usage: call as `findrangeforangle_LH(mat, angle, Zn)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findendpp()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
- Source: `Smart_3D_Viewer_New.ipf:1872`

#### `findrangeforangle_LHf`

```igor
Function/Wave findrangeforangle_LHf(mat,angle,Zn)
```
- Notes: ## 04 Find the addY Range for rotated H-Linecut Usage: call as `findrangeforangle_LHf(mat, angle, Zn)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findendppf()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
- Source: `Smart_3D_Viewer_New.ipf:6867`

#### `findrangeforangle_LV`

```igor
Function/Wave findrangeforangle_LV(mat,angle,Zn)
```
- Notes: ## 04 Find the addX Range for rotated V-Linecut Usage: call as `findrangeforangle_LV(mat, angle, Zn)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findendqq()`, `findendpp()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Smart_3D_Viewer_New.ipf:2124`

#### `findrangeforangle_LVf`

```igor
Function/Wave findrangeforangle_LVf(mat,angle,Zn)
```
- Notes: ## 04 Find the addX Range for rotated V-Linecut Usage: call as `findrangeforangle_LVf(mat, angle, Zn)`. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findendqqf()`, `findendppf()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `Zn`: Layer/index value selecting a slice of a 3D wave or energy stack.
- Source: `Smart_3D_Viewer_New.ipf:7135`

#### `findtheintersection`

```igor
Function findtheintersection(xi,yi,many,theta,av,ah,sigmaa)
```
- Usage: call as `findtheintersection(xi, yi, many, theta, av, ah, sigmaa)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `xi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `many`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `av`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ah`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sigmaa`: Width or sigma parameter for smoothing, fitting, or Fourier filtering.
- Source: `Models.ipf:93`

#### `fit_fermi`

```igor
Function fit_fermi(W,x)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `fit_fermi(W, x)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `W`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:8605`

#### `fit_fermi5`

```igor
Function fit_fermi5(W,x)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `fit_fermi5(W, x)`.
- Notes: Utility function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `W`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:8610`

#### `fit_slope`

```igor
Function fit_slope(slope,x)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `fit_slope(slope, x)`.
- Notes: Utility function for Igor wave/matrix/cube data operation.
- Parameters:
  - `slope`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:18831`

#### `fitvk`

```igor
Function fitvk(m,x,y) : fitfunc
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `fitvk(m, x, y)`.
- Notes: Utility function for Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:19860`

#### `func4angle2kVcut`

```igor
Function func4angle2kVcut(paraVy,yy)
```
- Usage: call as `func4angle2kVcut(paraVy, yy)`.
- Notes: Function for symmetry or reflection processing; linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `paraVy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:22281`

#### `Func_adjust_to_fp`

```igor
Function Func_adjust_to_fp(matname,thenewmat,ferminame,Fermilevel)
```
- Usage: call as `Func_adjust_to_fp(matname, thenewmat, ferminame, Fermilevel)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `thenewmat`: Matrix/2D wave input or matrix output name.
  - `ferminame`: String name used to locate an Igor wave, graph, folder, or output object.
  - `Fermilevel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:6435`

#### `func_associate_kz`

```igor
Function func_associate_kz(oldname,newname,fe,V0value)
```
- Usage: call as `func_associate_kz(oldname, newname, fe, V0value)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `oldname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `newname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `fe`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `V0value`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:30085`

#### `func_BEatheight_EDC`

```igor
Function func_BEatheight_EDC(EDCname,height,smoothfactor)
```
- Usage: call as `func_BEatheight_EDC(EDCname, height, smoothfactor)`.
- Notes: Function for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data; cleans up waves/windows or brings an existing window forward. Main internal calls: `x2pnt()`, `sign()`, `pnt2x()`.
- Parameters:
  - `EDCname`: Used in: Duplicate/O EDCname smoothtemp
  - `height`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smoothfactor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Pierre's Template.ipf:35809`

#### `func_calc4circles`

```igor
Function func_calc4circles(parameters,quadrant1_x,quadrant1_y)
```
- Usage: call as `func_calc4circles(parameters, quadrant1_x, quadrant1_y)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `tbfunc()`.
- Parameters:
  - `parameters`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `quadrant1_x`: Momentum or Q-vector coordinate/index.
  - `quadrant1_y`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:32224`

#### `func_comb_krange`

```igor
Function func_comb_krange(thisdata,outputname,x_center,x_wide)
```
- Usage: call as `func_comb_krange(thisdata, outputname, x_center, x_wide)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mean()`.
- Parameters:
  - `thisdata`: Used in: Make/O/N=(dimsize(thisdata,0)) tempwave1
  - `outputname`: Used in: Duplicate/O/R=[tocut,*] outputname bidon / Duplicate/O bidon outputname
  - `x_center`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x_wide`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:6962`

#### `func_Curvaturex`

```igor
function func_Curvaturex(oldmat,matx,matxx,num,box)
```
- Usage: call as `func_Curvaturex(oldmat, matx, matxx, num, box)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `oldmat`: Matrix/2D wave input or matrix output name.
  - `matx`: Matrix/2D wave input or matrix output name.
  - `matxx`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:12572`

#### `func_Curvaturexy_yx`

```igor
function func_Curvaturexy_yx(oldmat,matxy,matyx,numxy,boxxy,numyx,boxyx)
```
- Usage: call as `func_Curvaturexy_yx(oldmat, matxy, matyx, numxy, boxxy, numyx, boxyx)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `oldmat`: Matrix/2D wave input or matrix output name.
  - `matxy`: Matrix/2D wave input or matrix output name.
  - `matyx`: Matrix/2D wave input or matrix output name.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
  - `boxxy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numyx`: Number of spectra, waves, points, bins, or iterations to process.
  - `boxyx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:12462`

#### `func_Curvaturey`

```igor
function func_Curvaturey(oldmat,maty,matyy,num,box)
```
- Usage: call as `func_Curvaturey(oldmat, maty, matyy, num, box)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; differentiates wave data.
- Parameters:
  - `oldmat`: Matrix/2D wave input or matrix output name.
  - `maty`: Matrix/2D wave input or matrix output name.
  - `matyy`: Matrix/2D wave input or matrix output name.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `box`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:12609`

#### `func_CVT2K_vs_kz_boundaries`

```igor
Function func_CVT2K_vs_kz_boundaries(matkhv,matkkz1,EFwave,lowestwave,highestwave,Vzero)
```
- Usage: call as `func_CVT2K_vs_kz_boundaries(matkhv, matkkz1, EFwave, lowestwave, highestwave, Vzero)`.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_mat2d_1orNaN()`.
- Parameters:
  - `matkhv`: Matrix/2D wave input or matrix output name.
  - `matkkz1`: Matrix/2D wave input or matrix output name.
  - `EFwave`: Igor wave reference used as input or output.
  - `lowestwave`: Igor wave reference used as input or output.
  - `highestwave`: Igor wave reference used as input or output.
  - `Vzero`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:35520`

#### `func_CVT2K_vs_kz_boundaries_2D`

```igor
Function func_CVT2K_vs_kz_boundaries_2D(matkhv,matkkz1,EFwave,lowestwave,highestwave,Vzero,BEmat)
```
- Usage: call as `func_CVT2K_vs_kz_boundaries_2D(matkhv, matkkz1, EFwave, lowestwave, highestwave, Vzero, BEmat)`.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_mat2d_1orNaN_2d()`.
- Parameters:
  - `matkhv`: Matrix/2D wave input or matrix output name.
  - `matkkz1`: Matrix/2D wave input or matrix output name.
  - `EFwave`: Igor wave reference used as input or output.
  - `lowestwave`: Igor wave reference used as input or output.
  - `highestwave`: Igor wave reference used as input or output.
  - `Vzero`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `BEmat`: Matrix/2D wave input or matrix output name.
- Source: `Miscellaneous_Codes.ipf:1461`

#### `func_CVT2K_vs_kz_interp`

```igor
Function func_CVT2K_vs_kz_interp(matkhv,matkkz,matkkz1)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `func_CVT2K_vs_kz_interp(matkhv, matkkz, matkkz1)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Ceil()`, `interp()`.
- Parameters:
  - `matkhv`: Matrix/2D wave input or matrix output name.
  - `matkkz`: Matrix/2D wave input or matrix output name.
  - `matkkz1`: Matrix/2D wave input or matrix output name.
- Source: `Pierre's Template.ipf:35470`

#### `func_CVT2K_vs_kz_interp_2D`

```igor
Function func_CVT2K_vs_kz_interp_2D(matkhv,matkkz,matkkz1)
```
- Purpose: resamples, rescales, pads, or changes wave dimensions.
- Usage: call as `func_CVT2K_vs_kz_interp_2D(matkhv, matkkz, matkkz1)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `Ceil()`, `interp()`.
- Parameters:
  - `matkhv`: Matrix/2D wave input or matrix output name.
  - `matkkz`: Matrix/2D wave input or matrix output name.
  - `matkkz1`: Matrix/2D wave input or matrix output name.
- Source: `Miscellaneous_Codes.ipf:1506`

#### `func_double_elipse`

```igor
Function func_double_elipse(para,x) : fitfunc
```
- Usage: call as `func_double_elipse(para, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `para`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:15999`

#### `func_findEres`

```igor
Function func_findEres(thiswave, W, largest_res, fitfrom, fitto,displayornot)
```
- Usage: call as `func_findEres(thiswave, W, largest_res, fitfrom, fitto, displayornot)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `x2pnt()`, `fit_Fermi()`, `rgb()`.
- Parameters:
  - `thiswave`: Igor wave reference used as input or output.
  - `W`: Igor wave reference used as input or output.
  - `largest_res`: Used in: Make/O/N=(floor(largest_res*1000)) reswave / Make/O/N=(floor(largest_res*1000)) minsqwave
  - `fitfrom`: Used in: Duplicate/O/R=[x2pnt(thiswave,fitfrom),x2pnt(thiswave,fitto)] thiswave, thiswaveshort
  - `fitto`: Used in: Duplicate/O/R=[x2pnt(thiswave,fitfrom),x2pnt(thiswave,fitto)] thiswave, thiswaveshort
  - `displayornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:8433`

#### `func_Flip_digimage`

```igor
Function func_Flip_digimage(matname)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `func_Flip_digimage(matname)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:6537`

#### `Func_Generate_chunklist`

```igor
Function/S Func_Generate_chunklist(imageindex,imagetheta,imagephi,imtheta,imphi1,imphi2)
```
- Usage: call as `Func_Generate_chunklist(imageindex, imagetheta, imagephi, imtheta, imphi1, imphi2)`.
- Notes: String-returning helper for Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `imageindex`: Step size or energy/voltage increment used by the calculation.
  - `imagetheta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `imagephi`: Igor wave or matrix input consumed by the calculation.
  - `imtheta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `imphi1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `imphi2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34277`

#### `Func_Get_ledge_edc`

```igor
Function Func_Get_ledge_edc(edcname,fermi_level,search_halfwindow,which_way,smoothlevel)
```
- Usage: call as `Func_Get_ledge_edc(edcname, fermi_level, search_halfwindow, which_way, smoothlevel)`.
- Notes: Function for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; smooths wave data. Main internal calls: `x2pnt()`.
- Parameters:
  - `edcname`: Used in: Duplicate/O edcname tempwave
  - `fermi_level`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `search_halfwindow`: Igor wave reference used as input or output.
  - `which_way`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `smoothlevel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:28999`

#### `func_give_area1_old`

```igor
Function func_give_area1_old(namex,namey,surfx_x,surfx_y,surfy_x,surfy_y,griddelta,lattice,firstx,firsty)
```
- Usage: call as `func_give_area1_old(namex, namey, surfx_x, surfx_y, surfy_x, surfy_y, griddelta, lattice)`.
- Notes: Function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves.
- Parameters:
  - `namex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `namey`: String name used to locate an Igor wave, graph, folder, or output object.
  - `surfx_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfx_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `surfy_x`: Used in: Make/O/N=1 surfy_x
  - `surfy_y`: Used in: Make/O/N=1 surfy_y
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `firstx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `firsty`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:7367`

#### `func_keep_negative`

```igor
Function func_keep_negative(mat,replaceby)
```
- Usage: call as `func_keep_negative(mat, replaceby)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `replaceby`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:8813`

#### `func_keep_positive`

```igor
Function func_keep_positive(mat,replaceby)
```
- Usage: call as `func_keep_positive(mat, replaceby)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `replaceby`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:8844`

#### `func_Makearcshadow`

```igor
Function func_Makearcshadow(matname,csize,cwidth,arc1,arc2)
```
- Usage: call as `func_Makearcshadow(matname, csize, cwidth, arc1, arc2)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `csize`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `cwidth`: Igor wave reference used as input or output.
  - `arc1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `arc2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:16188`

#### `Func_makebold`

```igor
Function Func_makebold(matname,curvenamex,curvenamey,profilecurve,xwaveprofile,distance)
```
- Usage: call as `Func_makebold(matname, curvenamex, curvenamey, profilecurve, xwaveprofile, distance)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics. Main internal calls: `interp()`.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `curvenamex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `curvenamey`: String name used to locate an Igor wave, graph, folder, or output object.
  - `profilecurve`: File name or file-derived label used by the loader/export routine.
  - `xwaveprofile`: File name or file-derived label used by the loader/export routine.
  - `distance`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:17012`

#### `func_mat2d_1orNaN`

```igor
Function func_mat2d_1orNaN(matname)
```
- Usage: call as `func_mat2d_1orNaN(matname)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:35501`

#### `func_mat2dk_pi`

```igor
function func_mat2dk_pi(mat2d,matrix2dk,offset_theta,offset_phi,ktheta_min,delta_ktheta,ktheta_max,number_of_ktheta,kphi_min,kphi_max,delta_kphi,number_of_kphi,theta_min,theta_max,phi_min,phi_max,ktheta,kphi,iktheta,ikphi,i,theta,phi,Kenergy)
```
- Usage: call as `func_mat2dk_pi(mat2d, matrix2dk, offset_theta, offset_phi, ktheta_min, delta_ktheta, ktheta_max, number_of_ktheta)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp2d()`.
- Parameters:
  - `mat2d`: Matrix/2D wave input or matrix output name.
  - `matrix2dk`: Matrix/2D wave input or matrix output name.
  - `offset_theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ktheta_min`: Angle parameter used for ARPES momentum conversion or rotation.
  - `delta_ktheta`: Step size or energy/voltage increment used by the calculation.
  - `ktheta_max`: Angle parameter used for ARPES momentum conversion or rotation.
  - `number_of_ktheta`: Number of spectra, waves, points, bins, or iterations to process.
  - `kphi_min`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kphi_max`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - ... 14 additional parameters
- Source: `Pierre's Template.ipf:31528`

#### `func_mat3dk_pi`

```igor
function func_mat3dk_pi(mat3d,mat3dk,offset_theta,offset_phi,fermi_level,ktheta_min,delta_ktheta,ktheta_max,number_of_ktheta,kphi_min,kphi_max,delta_kphi,number_of_kphi,theta_min,theta_max,phi_min,phi_max,ktheta,kphi,iktheta,ikphi,i,j,theta,phi,KE,sqrtKE)//Use mat3d
```
- Usage: call as `func_mat3dk_pi(mat3d, mat3dk, offset_theta, offset_phi, fermi_level, ktheta_min, delta_ktheta, ktheta_max)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp2d()`.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dk`: Matrix/2D wave input or matrix output name.
  - `offset_theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fermi_level`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ktheta_min`: Angle parameter used for ARPES momentum conversion or rotation.
  - `delta_ktheta`: Step size or energy/voltage increment used by the calculation.
  - `ktheta_max`: Angle parameter used for ARPES momentum conversion or rotation.
  - `number_of_ktheta`: Number of spectra, waves, points, bins, or iterations to process.
  - `kphi_min`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - ... 17 additional parameters
- Source: `Pierre's Template.ipf:29305`

#### `func_mat3dk_pi1`

```igor
function func_mat3dk_pi1(mat3d,mat3dk,offset_theta,offset_phi,fermi_level,ktheta_min,delta_ktheta,ktheta_max,number_of_ktheta,kphi_min,kphi_max,delta_kphi,number_of_kphi,theta_min,theta_max,phi_min,phi_max,ktheta,kphi,iktheta,ikphi,i,j,theta,phi,KE,sqrtKE)//Use mat3d
```
- Usage: call as `func_mat3dk_pi1(mat3d, mat3dk, offset_theta, offset_phi, fermi_level, ktheta_min, delta_ktheta, ktheta_max)`.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `interp()`.
- Parameters:
  - `mat3d`: Matrix/2D wave input or matrix output name.
  - `mat3dk`: Matrix/2D wave input or matrix output name.
  - `offset_theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `offset_phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fermi_level`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ktheta_min`: Angle parameter used for ARPES momentum conversion or rotation.
  - `delta_ktheta`: Step size or energy/voltage increment used by the calculation.
  - `ktheta_max`: Angle parameter used for ARPES momentum conversion or rotation.
  - `number_of_ktheta`: Number of spectra, waves, points, bins, or iterations to process.
  - `kphi_min`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - ... 17 additional parameters
- Source: `Pierre's Template.ipf:29251`

#### `func_NaN0`

```igor
Function func_NaN0(matname)
```
- Usage: call as `func_NaN0(matname)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:7657`

#### `func_normcut`

```igor
Function func_normcut(name,x_from,x_to)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `func_normcut(name, x_from, x_to)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mean()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `x_from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x_to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:6913`

#### `func_predictcalc4circles`

```igor
Function func_predictcalc4circles(parameters,quadrant1_x,quadrant1_y)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `func_predictcalc4circles(parameters, quadrant1_x, quadrant1_y)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `tbfunc()`.
- Parameters:
  - `parameters`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `quadrant1_x`: Momentum or Q-vector coordinate/index.
  - `quadrant1_y`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:32425`

#### `func_rm_one_AVG_linear_backgnd`

```igor
Function func_rm_one_AVG_linear_backgnd(thisdata,number_of_pnts) //remove an averaged linear background
```
- Usage: call as `func_rm_one_AVG_linear_backgnd(thisdata, number_of_pnts)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `thisdata`: Used in: Make/O/N=(dimsize(thisdata,1)) thiswave
  - `number_of_pnts`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:4222`

#### `func_rm_one_linear_background`

```igor
Function func_rm_one_linear_background(thisdata,number_of_pnts) //remove a linear background EDC by EDC
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `func_rm_one_linear_background(thisdata, number_of_pnts)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `thisdata`: Used in: Make/O/N=(dimsize(thisdata,1)) thiswave
  - `number_of_pnts`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:4187`

#### `func_rm_one_linear_edcbackgnd`

```igor
Function func_rm_one_linear_edcbackgnd(thisedc,number_of_pnts) //remove a linear background EDC by EDC
```
- Usage: call as `func_rm_one_linear_edcbackgnd(thisedc, number_of_pnts)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `thisedc`: Used in: CurveFit/NTHR=0/Q/TBOX=0 line kwCWave=line_coef, thisedc[dimsize(thisedc,0)-number_of_pnts,dimsize(thisedc,0)-1] /D / Duplicate/O thisedc bgndedc
  - `number_of_pnts`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:4208`

#### `func_rot2d_pi`

```igor
Function func_rot2d_pi(mat,nnewmat,angle,px,py,new_delta,delta_x,delta_y,newsize_x,newsize_y,new_xoffset,new_yoffset,x_0,x_last,y_0,y_last)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `func_rot2d_pi(mat, nnewmat, angle, px, py, new_delta, delta_x, delta_y)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `nnewmat`: Matrix/2D wave input or matrix output name.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `px`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `py`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `new_delta`: Step size or energy/voltage increment used by the calculation.
  - `delta_x`: Step size or energy/voltage increment used by the calculation.
  - `delta_y`: Step size or energy/voltage increment used by the calculation.
  - `newsize_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `newsize_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - ... 6 additional parameters
- Source: `Pierre's Template.ipf:5597`

#### `func_rot3d_pi`

```igor
Function func_rot3d_pi(mat,nnewmat,angle,px,py,new_delta,delta_x,delta_y,newsize_x,newsize_y,new_xoffset,new_yoffset,x_0,x_last,y_0,y_last)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `func_rot3d_pi(mat, nnewmat, angle, px, py, new_delta, delta_x, delta_y)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `nnewmat`: Matrix/2D wave input or matrix output name.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `px`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `py`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `new_delta`: Step size or energy/voltage increment used by the calculation.
  - `delta_x`: Step size or energy/voltage increment used by the calculation.
  - `delta_y`: Step size or energy/voltage increment used by the calculation.
  - `newsize_x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `newsize_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - ... 6 additional parameters
- Source: `Pierre's Template.ipf:5636`

#### `func_STM_shift`

```igor
Function func_STM_shift(matfixed,matmoved,Corr_STMshift)
```
- Usage: call as `func_STM_shift(matfixed, matmoved, Corr_STMshift)`.
- Notes: Function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: uses Igor matrix operations for vectorized calculation. Main internal calls: `Correlate()`.
- Parameters:
  - `matfixed`: Matrix/2D wave input or matrix output name.
  - `matmoved`: Matrix/2D wave input or matrix output name.
  - `Corr_STMshift`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:24133`

#### `func_sym_edc_far`

```igor
Function func_sym_edc_far(edcname,new1,new2)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `func_sym_edc_far(edcname, new1, new2)`.
- Notes: Function for symmetry or reflection processing; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `edcname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `new1`: Igor wave reference used as input or output.
  - `new2`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:8211`

#### `func_sym_edc_matrix_far`

```igor
Function func_sym_edc_matrix_far(matname,newname)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `func_sym_edc_matrix_far(matname, newname)`.
- Notes: Function for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
  - `newname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Pierre's Template.ipf:8277`

#### `func_symmat3dhv`

```igor
Function func_symmat3dhv(matold,matnew)  // symmetrize mat_khv with respect to zero. The zero must be set first
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `func_symmat3dhv(matold, matnew)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves. Main internal calls: `interp()`.
- Parameters:
  - `matold`: Matrix/2D wave input or matrix output name.
  - `matnew`: Matrix/2D wave input or matrix output name.
- Source: `Pierre's Template.ipf:33858`

#### `Func_transform2equiv_mat`

```igor
Function Func_transform2equiv_mat(nameref,name2)
```
- Usage: call as `Func_transform2equiv_mat(nameref, name2)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `Interp2d()`.
- Parameters:
  - `nameref`: Used in: Duplicate/O nameref newmat
  - `name2`: Used in: Duplicate/O newmat name2
- Source: `Pierre's Template.ipf:9307`

#### `func_zeroNaN`

```igor
Function func_zeroNaN(matname)
```
- Usage: call as `func_zeroNaN(matname)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:7491`

#### `funcChangeHline`

```igor
Function funcChangeHline(matname)
```
- Usage: call as `funcChangeHline(matname)`.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:27520`

#### `funcChangeVline`

```igor
Function funcChangeVline(matname)
```
- Usage: call as `funcChangeVline(matname)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:27731`

#### `funcCutNaNedges`

```igor
Function funcCutNaNedges(matname)
```
- Usage: call as `funcCutNaNedges(matname)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:2195`

#### `funcCVT2EK_exact1cutH`

```igor
Function funcCVT2EK_exact1cutH(oldname,newname,theta,phi)
```
- Usage: call as `funcCVT2EK_exact1cutH(oldname, newname, theta, phi)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics. Main internal calls: `interp()`.
- Parameters:
  - `oldname`: Used in: Make/O/N=(dimsize(oldname,0)) wavek / Make/O/N=(dimsize(oldname,0)) waveI
  - `newname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:30121`

#### `funcCVT2EK_exact1cutV`

```igor
Function funcCVT2EK_exact1cutV(oldname,newname,theta,phi)
```
- Usage: call as `funcCVT2EK_exact1cutV(oldname, newname, theta, phi)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics. Main internal calls: `interp()`.
- Parameters:
  - `oldname`: Used in: Make/O/N=(dimsize(oldname,0)) wavek / Make/O/N=(dimsize(oldname,0)) waveI
  - `newname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:30097`

#### `funcfind1angleBKFA`

```igor
Function funcfind1angleBKFA(xvalue,yvalue)
```
- Usage: call as `funcfind1angleBKFA(xvalue, yvalue)`.
- Notes: Function in `Pierre's Template.ipf`.
- Parameters:
  - `xvalue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yvalue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:14410`

#### `funclorentz3`

```igor
Function funclorentz3(m,x) : fitfunc
```
- Usage: call as `funclorentz3(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:18498`

#### `funcMakeHexagon`

```igor
Function funcMakeHexagon(size_a,angle,namex,namey)
```
- Usage: call as `funcMakeHexagon(size_a, angle, namex, namey)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `size_a`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `namex`: String name used to locate an Igor wave, graph, folder, or output object.
  - `namey`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Pierre's Template.ipf:32977`

#### `GetWaveOffset`

```igor
Function GetWaveOffset(w)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `GetWaveOffset(w)`.
- Notes: Utility function for Igor wave/matrix/cube data operation. Main internal calls: `TraceInfo()`, `str2num()`.
- Parameters:
  - `w`: Igor wave reference used as input or output.
- Source: `Pierre's Template.ipf:1851`

#### `give_b`

```igor
Function give_b(m,x1,y1)
```
- Usage: call as `give_b(m, x1, y1)`.
- Notes: Function in `Pierre's Template.ipf`.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x1`: Start point, index, or coordinate for the selected range.
  - `y1`: Start point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:7850`

#### `give_slope`

```igor
Function give_slope(x1,y1,x2,y2)
```
- Usage: call as `give_slope(x1, y1, x2, y2)`.
- Notes: Function in `Pierre's Template.ipf`.
- Parameters:
  - `x1`: Start point, index, or coordinate for the selected range.
  - `y1`: Start point, index, or coordinate for the selected range.
  - `x2`: End point, index, or coordinate for the selected range.
  - `y2`: End point, index, or coordinate for the selected range.
- Source: `Pierre's Template.ipf:7845`

#### `hexfunction`

```igor
function hexfunction(kx,ky)
```
- Usage: call as `hexfunction(kx, ky)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `elseif()`.
- Parameters:
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
- Source: `Pierre's Template.ipf:2147`

#### `idisp`

```igor
function idisp(mat)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `idisp(mat)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Pierre's Template.ipf:13493`

#### `KP_EnsureTemplateRootGlobals`

```igor
Function KP_EnsureTemplateRootGlobals()
```
- Usage: call `KP_EnsureTemplateRootGlobals()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Main internal calls: `KP_EnsureRootGlobalVariable()`, `KP_EnsureRootGlobalString()`.
- Source: `KP_GlobalState.ipf:40`

#### `kxtransH`

```igor
Function kxtransH(paraHx,xx,yy)
```
- Usage: call as `kxtransH(paraHx, xx, yy)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `paraHx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34734`

#### `kxtransV`

```igor
Function kxtransV(paraVx,xx,yy)
```
- Usage: call as `kxtransV(paraVx, xx, yy)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `paraVx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34718`

#### `kytransH`

```igor
Function kytransH(paraHy,xx,yy)
```
- Usage: call as `kytransH(paraHy, xx, yy)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `paraHy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34742`

#### `kytransV`

```igor
Function kytransV(paraVy,xx,yy)
```
- Usage: call as `kytransV(paraVy, xx, yy)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `paraVy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34726`

#### `line_fermi`

```igor
function line_fermi(Wfit,x)//function used to fit. Both below and above Ef are used lines, not constant.
```
- Usage: call as `line_fermi(Wfit, x)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `Wfit`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:2360`

#### `linear_bkgnd_plus2Lor`

```igor
Function linear_bkgnd_plus2Lor(parwave,x) : fitfunc
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `linear_bkgnd_plus2Lor(parwave, x)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation.
- Parameters:
  - `parwave`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21747`

#### `linear_bkgnd_plus2LorsameW`

```igor
Function linear_bkgnd_plus2LorsameW(parwave,x) : fitfunc
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `linear_bkgnd_plus2LorsameW(parwave, x)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation.
- Parameters:
  - `parwave`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21754`

#### `ListBoxProc`

```igor
Function ListBoxProc(lba) : ListBoxControl
```
- Usage: call as `ListBoxProc(lba)`.
- Notes: Function for graph display, formatting, or window management.
- Parameters:
  - `lba`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:25242`

#### `Lorentz`

```igor
Function Lorentz(m,x)
```
- Usage: call as `Lorentz(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1619`

#### `Lorentz2`

```igor
Function Lorentz2(m,x) : fitfunc
```
- Usage: call as `Lorentz2(m, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1614`

#### `Lorentzfermi`

```igor
Function Lorentzfermi(m,x) : fitfunc
```
- Usage: call as `Lorentzfermi(m, x)`.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:19855`

#### `LorFit`

```igor
Function LorFit(w,x) : FitFunc
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `LorFit(w, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:1872`

#### `makeLLspectraf`

```igor
Function makeLLspectraf(hv,ED,tem,num,field,ER)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makeLLspectraf(hv, ED, tem, num, field, ER)`.
- Notes: Utility function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: hv = Fermi velocity (meV*A); ED = Binding Energy of Dirac Point (meV); tem = Temperature of the system (K); num = number of levels; field = Field Range: (1 to ?)T; ER = Energe range of spectrum (meV). Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `madelldos()`, `linkstsmapno()`, `di()`.
- Parameters:
  - `hv`: Fermi velocity (meV*A)
  - `ED`: Binding Energy of Dirac Point (meV)
  - `tem`: Temperature of the system (K)
  - `num`: number of levels
  - `field`: Field Range: (1 to ?)T
  - `ER`: Energe range of spectrum (meV)
- Source: `Models.ipf:4434`

#### `MDCcurv`

```igor
function MDCcurv()
```
- Usage: call `MDCcurv()` from Igor procedure code or the command line.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `curvature()`, `Deriv()`.
- Source: `Pierre's Template.ipf:13563`

#### `merged1kchunk`

```igor
Function merged1kchunk(kchunk,merged,number)
```
- Usage: call as `merged1kchunk(kchunk, merged, number)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `interp2D()`.
- Parameters:
  - `kchunk`: Used in: Make/O/N=(dimsize(kchunk,0),dimsize(kchunk,1)) thismat
  - `merged`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:34585`

#### `merged1kchunk2d`

```igor
Function merged1kchunk2d(kchunk,merged,number)
```
- Usage: call as `merged1kchunk2d(kchunk, merged, number)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `interp2D()`.
- Parameters:
  - `kchunk`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `merged`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Pierre's Template.ipf:35263`

#### `mergekchunks`

```igor
Function mergekchunks(merged,matout)
```
- Usage: call as `mergekchunks(merged, matout)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `merged`: Used in: Make/O/N=(dimsize(merged,3)) thiswave
  - `matout`: Matrix/2D wave input or matrix output name.
- Source: `Pierre's Template.ipf:34603`

#### `mergekchunks2d`

```igor
Function mergekchunks2d(merged,matout)
```
- Usage: call as `mergekchunks2d(merged, matout)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `merged`: Used in: Make/O/N=(dimsize(merged,2)) thiswave
  - `matout`: Matrix/2D wave input or matrix output name.
- Source: `Pierre's Template.ipf:35271`

#### `Newrotate`

```igor
Function Newrotate(name,theta1)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `Newrotate(name, theta1)`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output. Main internal calls: `func_NaN0()`, `dilf()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `theta1`: counter clock-wise degree
- Source: `Miscellaneous_Codes.ipf:39`

#### `Newrotatecont`

```igor
Function Newrotatecont(name,theta1)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `Newrotatecont(name, theta1)`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output. Main internal calls: `func_NaN0()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `theta1`: counter clock-wise degree
- Source: `Miscellaneous_Codes.ipf:192`

#### `OneasLor`

```igor
Function OneasLor(parwave,x) : fitfunc  //only one lorenztian is asymmetric
```
- Usage: call as `OneasLor(parwave, x)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `parwave`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21732`

#### `refresh_disp`

```igor
Function refresh_disp()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `refresh_disp()` from Igor procedure code or the command line.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `Pierre's Template.ipf:13904`

#### `remove20cone`

```igor
Function remove20cone()
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call `remove20cone()` from Igor procedure code or the command line.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays.
- Source: `Pierre's Template.ipf:16127`

#### `rot2d_pimf`

```igor
Function rot2d_pimf(mats,angle,px,py,qualityfactor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `rot2d_pimf(mats, angle, px, py, qualityfactor)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_rot2d_pi()`.
- Parameters:
  - `mats`: Matrix/2D wave input or matrix output name.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `px`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `py`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qualityfactor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:9900`

#### `set_textimageforslicer`

```igor
Function set_textimageforslicer(textname)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `set_textimageforslicer(textname)`.
- Notes: Utility function for linecut, slice, or region extraction; graph display, formatting, or window management. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `textname`: Used in: ValDisplay ttextimageforslicer format=textname //textforslicer[0]
- Source: `Pierre's Template.ipf:27501`

#### `setKE1atEF`

```igor
Function setKE1atEF(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `KE1atEF_set()`.
- Notes: Utility function for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:26518`

#### `setvarincrement_theta1_zero`

```igor
Function setvarincrement_theta1_zero(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `theta1_zero_incset()`.
- Notes: Utility function for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:26543`

#### `setvarincrementpredictwedge`

```igor
Function setvarincrementpredictwedge(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvarincrementpredictwedge(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Code behavior: reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:26601`

#### `setvartwoDW`

```igor
Function setvartwoDW(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `setvartwoDW(ctrlName, varNum, varStr, varName)`.
- Notes: Utility function in `Pierre's Template.ipf`. Main internal calls: `twoDcurv()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Pierre's Template.ipf:13772`

#### `SimulkEresolution`

```igor
Function SimulkEresolution(mat,Eresolution,kresolution)
```
- Usage: call as `SimulkEresolution(mat, Eresolution, kresolution)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `Eresolution`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kresolution`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:15131`

#### `size4cvtchunkcut`

```igor
Function size4cvtchunkcut(thischunk,angle,addphi,addtheta,slit,kproperties)
```
- Usage: call as `size4cvtchunkcut(thischunk, angle, addphi, addtheta, slit, kproperties)`.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `Ceil()`.
- Parameters:
  - `thischunk`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `addphi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `addtheta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `slit`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kproperties`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:34750`

#### `size4cvtchunkcut2d`

```igor
Function size4cvtchunkcut2d(thischunk,angle,addphi,addtheta,slit,kproperties,Fermilevel)
```
- Usage: call as `size4cvtchunkcut2d(thischunk, angle, addphi, addtheta, slit, kproperties, Fermilevel)`.
- Notes: Function for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `Ceil()`.
- Parameters:
  - `thischunk`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `addphi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `addtheta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `slit`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kproperties`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Fermilevel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:35103`

#### `SumCompOrder_tria`

```igor
Function SumCompOrder_tria(A1,A2,A3,theta,A4,inc_L,inc_an,wLength)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `SumCompOrder_tria(A1, A2, A3, theta, A4, inc_L, inc_an, wLength)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `cvtcmplx()`, `max()`, `ckfig_child()`.
- Parameters:
  - `A1`: Used in: Display/HOST=#/W=(0,0.05,0.4,0.55);appendimage trian1;ModifyGraph mirror=2;ModifyImage trian1 ctab= {-max(A1,A2,A3),max(A1,A2,A3),VioletOran / setActiveSubwindow ##;Display/HOST=#/W=(0.3,0.05,0.7,0.55);appendimage trian2;ModifyGraph mirror=2;ModifyImage trian2 ctab= {-max(A1,A2,A3),
  - `A2`: Used in: Display/HOST=#/W=(0,0.05,0.4,0.55);appendimage trian1;ModifyGraph mirror=2;ModifyImage trian1 ctab= {-max(A1,A2,A3),max(A1,A2,A3),VioletOran / setActiveSubwindow ##;Display/HOST=#/W=(0.3,0.05,0.7,0.55);appendimage trian2;ModifyGraph mirror=2;ModifyImage trian2 ctab= {-max(A1,A2,A3),
  - `A3`: Used in: Display/HOST=#/W=(0,0.05,0.4,0.55);appendimage trian1;ModifyGraph mirror=2;ModifyImage trian1 ctab= {-max(A1,A2,A3),max(A1,A2,A3),VioletOran / setActiveSubwindow ##;Display/HOST=#/W=(0.3,0.05,0.7,0.55);appendimage trian2;ModifyGraph mirror=2;ModifyImage trian2 ctab= {-max(A1,A2,A3),
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
  - `A4`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `inc_L`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `inc_an`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `wLength`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:11608`

#### `sym1FDplus1Lor`

```igor
Function sym1FDplus1Lor(parwave,x) : fitfunc
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `sym1FDplus1Lor(parwave, x)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation.
- Parameters:
  - `parwave`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21706`

#### `sym1FDplus2Lor`

```igor
Function sym1FDplus2Lor(parwave,x) : fitfunc
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `sym1FDplus2Lor(parwave, x)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation.
- Parameters:
  - `parwave`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21713`

#### `tbfunc`

```igor
Function tbfunc(w,x,y) : FitFunc
```
- Usage: call as `tbfunc(w, x, y)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:3450`

#### `temp41`

```igor
Function temp41()
```
- Usage: call `temp41()` from Igor procedure code or the command line.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Source: `Pierre's Template.ipf:16389`

#### `temp43`

```igor
Function temp43(mat3dkfirstVD)
```
- Usage: call as `temp43(mat3dkfirstVD)`.
- Notes: Function for smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; smooths wave data; differentiates wave data.
- Parameters:
  - `mat3dkfirstVD`: Matrix/2D wave input or matrix output name.
- Source: `Pierre's Template.ipf:16458`

#### `testnewangle`

```igor
Function testnewangle(tt)
```
- Notes: Failed to organize: smooth the phase jump Not implement in the current version Usage: call as `testnewangle(tt)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `atan2()`, `smoothphasejump()`, `di()`.
- Parameters:
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:11014`

#### `twoDcurv`

```igor
function twoDcurv()
```
- Usage: call `twoDcurv()` from Igor procedure code or the command line.
- Notes: Function for ARPES-style loading, plotting, or momentum conversion. Main internal calls: `curvature2()`.
- Source: `Pierre's Template.ipf:13576`

#### `updatePRB_98_214503_normalFS`

```igor
Function updatePRB_98_214503_normalFS()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `updatePRB_98_214503_normalFS()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation.
- Source: `Models.ipf:1550`

#### `updatePRB_98_214503_SCFS`

```igor
Function updatePRB_98_214503_SCFS()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `updatePRB_98_214503_SCFS()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation.
- Source: `Models.ipf:2113`



## Lattice, Moire, Symmetry, Drift, and Strain

### Panel callbacks and control handlers

#### `ButtonProc__SQConst2dlockinftd`

```igor
Function ButtonProc__SQConst2dlockinftd(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Panel control(s): `Save Q`. Code behavior: creates output waves.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Save Q`
- Source: `Lawler-Fujita Drift Correction.ipf:2320`

#### `ButtonProc_C4_symc`

```igor
Function ButtonProc_C4_symc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ####. C4 symmetrization of a matrix Purpose: applies symmetry, mirror, or reflection operations. Panel control(s): `C4`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `C4`
- Source: `Symmetrization.ipf:128`

#### `ButtonProc_C4shearcorrectc`

```igor
Function ButtonProc_C4shearcorrectc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `C4shearcorrectc()`.
- Notes: Correct the shear image Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `Go!`, `Go`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go!`, `Go!`, `Go`
- Source: `Shear Correction for C4.ipf:255`

#### `ButtonProc_C4sheargetparac`

```igor
Function ButtonProc_C4sheargetparac(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `C4sheargetparac()`.
- Notes: (1) Click "Fit Shear" to call C4sheargetpara(), this will fit the FFT Q1, Q2 to get the shear parameter. Maybe need multiple tries to fit good (2) Click "Go" to call C4shearcorrect(), calculate the correction matrix and get the corrected X and Y wave, do scatt Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `C4 shearing`, `Coef`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `C4 shearing`, `C4 shearing`, `Coef`
- Source: `Shear Correction for C4.ipf:28`

#### `ButtonProc_c4special`

```igor
Function ButtonProc_c4special(ctrlName) : ButtonControl
```
- Notes: This procedure is used to make non Nxm (m <N) matrix to become (N x N-1) matrix, after this you can make C4 symmetric operation Usage: triggered as a wrapper that opens/runs `c4special()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:243`

#### `ButtonProc_C4symFFT`

```igor
Function ButtonProc_C4symFFT(ctrlName) : ButtonControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: triggered as a wrapper that opens/runs `C4symFFT()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:134`

#### `ButtonProc_cal_strainbyshearc`

```igor
Function ButtonProc_cal_strainbyshearc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `cal_strainbyshearc()`.
- Notes: Calculate lattice strain (a-b)/a after shear The lattice can be at arbitary angle compare to the shear axis Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `ε`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ε`
- Source: `Shear Correction for C4.ipf:1119`

#### `ButtonProc_calculateMoireLc`

```igor
Function ButtonProc_calculateMoireLc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `calculateMoireLc()`.
- Notes: Calculate Moire wavelength Panel control(s): `Moiré λ`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Moiré λ`
- Source: `Triangle Lattice_Graphene_like.ipf:345`

#### `ButtonProc_calculateRCc`

```igor
Function ButtonProc_calculateRCc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `Rnu_{ / m i}= nu_{ / m j}`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rnu_{ / m i}= nu_{ / m j}`
- Source: `Symmetrization.ipf:206`

#### `ButtonProc_cnfyjc`

```igor
Function ButtonProc_cnfyjc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `cnfyjc()`.
- Notes: Main Function Panel control(s): `Jump 2D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Jump 2D`
- Source: `Lawler-Fujita Drift Correction.ipf:438`

#### `ButtonProc_Cons3dplotgetshear`

```igor
Function ButtonProc_Cons3dplotgetshear(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Extract Energy dependent Shearing parameters }}}}}}}}}} #0_1 PreFFT:control Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Shear(V)`. Main internal calls: `ff_3dplot()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Shear(V)`
- Source: `Smart_3D_Viewer_New.ipf:4508`

#### `ButtonProc_ConstLF`

```igor
Function ButtonProc_ConstLF(ctrlName) : ButtonControl
```
- Notes: An interactive GUI to tune the Avedia for LF correction you need to run LF normally before use this part Usage: triggered as a wrapper that opens/runs `ConstLF()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Lawler-Fujita Drift Correction.ipf:1179`

#### `ButtonProc_ContinitialShearFit`

```igor
Function ButtonProc_ContinitialShearFit(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `ContinitialShearFit()`.
- Notes: Then click the "c" button will lead to a interactive button on the FFT image Please check the value change in Table while tuning the value of the button After get the local minimum wanted, just click "GO". This is helpful for change the fitting local minimum Purpose: fits or extracts spectral/peak parameters.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Shear Correction for C4.ipf:709`

#### `ButtonProc_D2symetric`

```igor
Function ButtonProc_D2symetric(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `D2symetric()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `FFT.ipf:649`

#### `ButtonProc_D4_symc`

```igor
Function ButtonProc_D4_symc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: 1: Mx Mdiag; Mx Moffdiag; My Mdiag; My Moffdiag; 2: C4 Mx; C4 My; 3: C4 Moffdiag; C4 Mdiag We use method 1 Purpose: applies symmetry, mirror, or reflection operations. Panel control(s): `D4`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `D4`
- Source: `Symmetrization.ipf:516`

#### `ButtonProc_Fitimagehoneycombc`

```igor
Function ButtonProc_Fitimagehoneycombc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Fitimagehoneycombc()`.
- Notes: Append Honeycomb lattice to a Topography Purpose: fits or extracts spectral/peak parameters. Panel control(s): `Gr-lat`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Gr-lat`
- Source: `General_Simu.ipf:2272`

#### `ButtonProc_Fitimagesquarec`

```igor
Function ButtonProc_Fitimagesquarec(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Fitimagesquarec()`.
- Notes: Append Square lattice to a Topography Purpose: fits or extracts spectral/peak parameters. Panel control(s): `Sq`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sq`
- Source: `General_Simu.ipf:2538`

#### `ButtonProc_Fitimagetriangularc`

```igor
Function ButtonProc_Fitimagetriangularc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Fitimagetriangularc()`.
- Notes: Append Triangular lattice to a Topography Purpose: fits or extracts spectral/peak parameters. Panel control(s): `Tri`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Tri`
- Source: `General_Simu.ipf:2416`

#### `ButtonProc_GPAtwist`

```igor
Function ButtonProc_GPAtwist(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpac_twist()`.
- Notes: Get the area for FFT peak fit Panel control(s): `Get A`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get A`
- Source: `Triangle Lattice_Graphene_like.ipf:77`

#### `ButtonProc_GPBtwist`

```igor
Function ButtonProc_GPBtwist(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpbc_twist()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Panel control(s): `Get B`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get B`
- Source: `Triangle Lattice_Graphene_like.ipf:99`

#### `ButtonProc_GPCtwist`

```igor
Function ButtonProc_GPCtwist(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `gpCc_twist()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Triangle Lattice_Graphene_like.ipf:122`

#### `ButtonProc_Gshear`

```igor
Function ButtonProc_Gshear(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #2_01 Extract Shearing:control Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `Get Shear`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `elseif()`, `stringmatch()`, `lstyle()`, `rgb()`, `lsize()`, `mode()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Get Shear`
- Source: `Smart_3D_Viewer_New.ipf:4695`

#### `ButtonProc_Honeycomb`

```igor
Function ButtonProc_Honeycomb(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Ahoneycombc()`.
- Notes: Simu Honeycomb lattice Panel control(s): `A(honeycomb)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A(honeycomb)`
- Source: `General_Simu.ipf:2207`

#### `ButtonProc_latticesergeon3d`

```igor
Function ButtonProc_latticesergeon3d(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Lattice Segregation`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `LS22()`, `grabwinnonew()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Lattice Segregation`
- Source: `Smart_3D_Viewer_New.ipf:9457`

#### `ButtonProc_LF`

```igor
Function ButtonProc_LF(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `LawlerFujita()`.
- Notes: Put cursor A & B, at left-bottom and right-top corner around the FFT peak in the 1st quandrant, click GetA to call "gpac()", the procedure will save the cooridnate of Q_A by 2D Gaussian Fit. Repeat this operation around Q_B, click GetB. (4) Click LF button, to Panel control(s): `Lawler-Fujita`, `2D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Lawler-Fujita`, `Lawler-Fujita`, `2D`
- Source: `Lawler-Fujita Drift Correction.ipf:29`

#### `ButtonProc_LFtc`

```igor
Function ButtonProc_LFtc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `LFtc()`.
- Notes: LF correction for 3D wave Panel control(s): `3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3D`
- Source: `Lawler-Fujita Drift Correction.ipf:2145`

#### `ButtonProc_LS`

```igor
Function ButtonProc_LS(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #I.2 Single 2D wave segregation [Do Segregation to itself] Panel control(s): `Self`. Main internal calls: `LS()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Self`
- Source: `Lattice Segregation.ipf:35`

#### `ButtonProc_LS2`

```igor
Function ButtonProc_LS2(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #I.2 Single 2D wave segregation [Do Segregation to other wave] Panel control(s): `Other`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Other`
- Source: `Lattice Segregation.ipf:57`

#### `ButtonProc_lsturnoff`

```igor
Function ButtonProc_lsturnoff(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `X`. Code behavior: cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `X`
- Source: `Lattice Segregation.ipf:733`

#### `ButtonProc_makelatticec`

```igor
Function ButtonProc_makelatticec(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `makelatticec()`.
- Notes: Estimate QPI Grid and Range Purpose: creates new waves, maps, figures, or simulation data. Panel control(s): `Go`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go`
- Source: `Models.ipf:12134`

#### `ButtonProc_Makelatticedata`

```igor
Function ButtonProc_Makelatticedata(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Makelatticedata()`.
- Notes: the kernal part is to determine the positions of the atomics 1. find a initial point (xi,yi) of the left-top atom, 2. determine the ''many'' to fully cover the region of interest xn,yn 3, use the same value of theta, av, ah, in the last procedure Purpose: creates new waves, maps, figures, or simulation data. Panel control(s): `Old`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Old`
- Source: `Models.ipf:56`

#### `ButtonProc_makeshearXimagec`

```igor
Function ButtonProc_makeshearXimagec(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `makeshearXimagec()`.
- Notes: Make a only X-Sheared image Purpose: creates new waves, maps, figures, or simulation data.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Shear Correction for C4.ipf:609`

#### `ButtonProc_Mdiag_symc`

```igor
Function ButtonProc_Mdiag_symc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ####. Mirror symmetrization (Mirror plane: diagnoal) Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `M_dia`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `M_dia`
- Source: `Symmetrization.ipf:303`

#### `ButtonProc_Moffdiag_symc`

```igor
Function ButtonProc_Moffdiag_symc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ####. Mirror symmetrization (Mirror plane: off diagnoal) For FFT data, owing to the C2 symmetry of the FFT data, the off-diagonal Mirror symmetrization is same as the diagonal symmetrization So, this procedure does not provide new information Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `M_offdia`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `M_offdia`
- Source: `Symmetrization.ipf:383`

#### `ButtonProc_Mx_symc`

```igor
Function ButtonProc_Mx_symc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ####. Mirror symmetrization of a matrix along x Purpose: applies symmetry, mirror, or reflection operations. Panel control(s): `Mx`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Mx`
- Source: `Symmetrization.ipf:332`

#### `ButtonProc_My_symc`

```igor
Function ButtonProc_My_symc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ####. Mirror symmetrization of a matrix along y For FFT data, owing to the C2 symmetry of the FFT data, the y-Mirror symmetrization is same as the x-mirror symmetrization So, this procedure does not provide new information Purpose: applies symmetry, mirror, or reflection operations. Panel control(s): `My`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `My`
- Source: `Symmetrization.ipf:464`

#### `ButtonProc_plotlattice`

```igor
Function ButtonProc_plotlattice(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `plotlattice()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Models.ipf:4`

#### `ButtonProc_put_psuperlattice`

```igor
Function ButtonProc_put_psuperlattice(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `put_psuperlattice()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Superlattices ON`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Superlattices ON`
- Source: `Pierre's Template.ipf:26244`

#### `ButtonProc_put_superlattice`

```igor
Function ButtonProc_put_superlattice(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `put_superlattice()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Superlattices ON`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Superlattices ON`
- Source: `Pierre's Template.ipf:26239`

#### `ButtonProc_put_superlattice1`

```igor
Function ButtonProc_put_superlattice1(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `put_superlattice1()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Superlattices ON`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Superlattices ON`
- Source: `Pierre's Template.ipf:26259`

#### `ButtonProc_RDF`

```igor
Function ButtonProc_RDF(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `RDF()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `RDF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `RDF`
- Source: `Miscellaneous_Codes.ipf:3356`

#### `ButtonProc_reflecttrace`

```igor
Function ButtonProc_reflecttrace(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `reflectionwave()`.
- Notes: This is a stupid procedure I replace it by a smarter one Purpose: applies symmetry, mirror, or reflection operations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:3049`

#### `ButtonProc_remove_psuperlattice`

```igor
Function ButtonProc_remove_psuperlattice(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `remove_psuperlattice()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `OFF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `OFF`
- Source: `Pierre's Template.ipf:26254`

#### `ButtonProc_remove_superlattice`

```igor
Function ButtonProc_remove_superlattice(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `remove_superlattice()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `OFF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `OFF`
- Source: `Pierre's Template.ipf:26249`

#### `ButtonProc_remove_superlattice1`

```igor
Function ButtonProc_remove_superlattice1(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `remove_superlattice1()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `OFF`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `OFF`
- Source: `Pierre's Template.ipf:26269`

#### `ButtonProc_rotatedshearc`

```igor
Function ButtonProc_rotatedshearc(ctrlName) : ButtonControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `rotatedshearc()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Shear`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Shear`
- Source: `Shear Correction for C4.ipf:486`

#### `ButtonProc_rotatedsheard`

```igor
Function ButtonProc_rotatedsheard(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Demo_rotatedshear()`.
- Notes: Make a shear rotated image This procedure is used to make sheared image with the shear axis rotated +θ from the +x axis. The formula is (x_sheared,y_sheared)^T = R(-θ)^-1 x S x R(-θ) x (x',y')^T Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `Demo`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Demo`
- Source: `Shear Correction for C4.ipf:469`

#### `ButtonProc_Shearmat3d`

```igor
Function ButtonProc_Shearmat3d(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Shearmat3d()`.
- Notes: Shear Correction on the whole 3D matrix Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `Go3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Go3D`
- Source: `Shear Correction for C4.ipf:969`

#### `ButtonProc_shiftNsym`

```igor
Function ButtonProc_shiftNsym(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `Shift_and_sym_edc_matrix_far()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `ShiftNsym`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `ShiftNsym`, `ShiftNsym`
- Source: `Pierre's Template.ipf:929`

#### `ButtonProc_Strain_anglec`

```igor
Function ButtonProc_Strain_anglec(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ## ANGULAR DEPENDENT FUNCTION Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `Caculate ε(θ)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `Strain_angle()`, `max()`, `min()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Caculate ε(θ)`
- Source: `Lawler-Fujita Drift Correction.ipf:4893`

#### `ButtonProc_Strainanalysisc`

```igor
Function ButtonProc_Strainanalysisc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Strainanalysisc()`.
- Notes: ## STRAIN ANALYSIS Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `Cal. Tensor`, `Strain Analysis`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Cal. Tensor`, `Strain Analysis`
- Source: `Lawler-Fujita Drift Correction.ipf:3496`

#### `ButtonProc_StrainFTtheta`

```igor
Function ButtonProc_StrainFTtheta(ctrlName) : ButtonControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `θ = θ(Se-Se)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `Drawarrow()`, `Strainanalysis()`, `mdg_strain2()`, `max()`, `min()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `θ = θ(Se-Se)`
- Source: `Lawler-Fujita Drift Correction.ipf:3933`

#### `ButtonProc_symbands`

```igor
Function ButtonProc_symbands(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `symbandsall()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `Symx`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Symx`
- Source: `Miscellaneous_Codes.ipf:2872`

#### `ButtonProc_symbands_cmplx`

```igor
Function ButtonProc_symbands_cmplx(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `symbands_cmplx()`.
- Notes: Panel button callback for symmetry or reflection processing; linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:2967`

#### `ButtonProc_symmetrizeQPIdallc`

```igor
Function ButtonProc_symmetrizeQPIdallc(ctrlName) : ButtonControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: triggered as a wrapper that opens/runs `symmetrizeQPIdallc()`.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `Sym. All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sym. All`
- Source: `Symmetrization.ipf:578`

#### `PopMenuProc_bkgrmv2Ddemo_3dplot`

```igor
Function PopMenuProc_bkgrmv2Ddemo_3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; smoothing, normalization, or background removal. Panel control(s): `Bkg.Rmv?`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `D4_sym_3dplot()`, `Mdiag_sym_3dplot()`, `Mx_sym_3dplot()`, `C4_sym_3dplot()`, `FTguassianremover()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Bkg.Rmv?`
- Source: `Symmetrization.ipf:698`

#### `PopMenuProc_Constsrain`

```igor
Function PopMenuProc_Constsrain(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `Drawarrow()`, `Strainanalysis()`, `mdg_strain2()`, `max()`, `min()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Source: `Lawler-Fujita Drift Correction.ipf:4036`

#### `PopMenuProc_mirroronoff`

```igor
Function PopMenuProc_mirroronoff(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `mirror_onoff`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `mirror_onoff`, `mirror_onoff`
- Source: `Pierre's Template.ipf:1227`

#### `PopMenuProc_symmode_3dplot`

```igor
Function PopMenuProc_symmode_3dplot(ctrlName,popNum,popStr) : PopupMenuControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; linecut, slice, or region extraction. Panel control(s): `Sym. Mode`. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index. In this context it is used for Fourier/QPI or phase-map processing.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Sym. Mode`
- Source: `Smart_3D_Viewer_New.ipf:10455`

#### `setvarincrement_lattice1_0`

```igor
Function setvarincrement_lattice1_0(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice1_0_incset()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26639`

#### `setvarincrement_lattice_0`

```igor
Function setvarincrement_lattice_0(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice_0_incset()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`, `Increment`
- Source: `Pierre's Template.ipf:26623`

#### `setvarincrement_lattice_hexagon`

```igor
Function setvarincrement_lattice_hexagon(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice_hexagon_incset()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Increment`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Increment`, `Increment`
- Source: `Pierre's Template.ipf:33272`

#### `setvarlattice1_0`

```igor
Function setvarlattice1_0(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice1_0_set()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `p/a`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `p/a`, `p/a`, `p/a`
- Source: `Pierre's Template.ipf:26468`

#### `setvarlattice_0`

```igor
Function setvarlattice_0(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice_0_set()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `p/a`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `p/a`
- Source: `Pierre's Template.ipf:26400`

#### `setvarlattice_02f`

```igor
Function setvarlattice_02f(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice_0_set()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `p/a`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `p/a`
- Source: `Pierre's Template.ipf:26409`

#### `setvarlattice_0H`

```igor
Function setvarlattice_0H(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `lattice_0_set()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `p/a`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `p/a`
- Source: `Pierre's Template.ipf:26448`

#### `setvarpredictlattice_0`

```igor
Function setvarpredictlattice_0(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `predictlattice_0_set()`.
- Notes: Utility function for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `p/a`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `p/a`
- Source: `Pierre's Template.ipf:26457`

#### `SetVarProc_apsquare`

```igor
Function SetVarProc_apsquare(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis. Panel control(s): `a(Å)`, `θ(˚)`, `dx(Å)`, `dy(Å)`. Main internal calls: `Fitimagetriangular()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a(Å)`, `θ(˚)`, `dx(Å)`, `dy(Å)`
- Source: `General_Simu.ipf:2442`

#### `SetVarProc_bkgrmv2Ddemo_3dplot`

```igor
Function SetVarProc_bkgrmv2Ddemo_3dplot(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; smoothing, normalization, or background removal. Panel control(s): `σ_bkg`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `D4_sym_3dplot()`, `Mdiag_sym_3dplot()`, `Mx_sym_3dplot()`, `C4_sym_3dplot()`, `FTguassianremover()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `σ_bkg`
- Source: `Symmetrization.ipf:745`

#### `SetVarProc_caclcshearx`

```igor
Function SetVarProc_caclcshearx(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for symmetry or reflection processing; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `theta`, `alpha`, `pp1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `theta`, `alpha`, `pp1`
- Source: `Shear Correction for C4.ipf:942`

#### `SetVarProc_ciShearFit`

```igor
Function SetVarProc_ciShearFit(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for symmetry or reflection processing; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `θ / (trial) / [degree]`, `α / (trial) / [degree]`, `P / (trial)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `θ / (trial) / [degree]`, `α / (trial) / [degree]`, `P / (trial)`
- Source: `Shear Correction for C4.ipf:732`

#### `SetVarProc_Const2dlockin`

```igor
Function SetVarProc_Const2dlockin(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `FWHM(l)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM(l)`
- Source: `Lawler-Fujita Drift Correction.ipf:1876`

#### `SetVarProc_Const2dlockinftd`

```igor
Function SetVarProc_Const2dlockinftd(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `FWHM(L)`, `QA(x)`, `QA(y)`, `QB(x)`, `QB(y)`. Main internal calls: `LF()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM(L)`, `QA(x)`, `QA(y)`, `QB(x)`, `QB(y)`
- Source: `Lawler-Fujita Drift Correction.ipf:2334`

#### `SetVarProc_Const2dlockinftd2`

```igor
Function SetVarProc_Const2dlockinftd2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Panel control(s): `Do / LF?`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `LF()`, `di()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Do / LF?`
- Source: `Lawler-Fujita Drift Correction.ipf:3438`

#### `SetVarProc_Const2dlockininampabs`

```igor
Function SetVarProc_Const2dlockininampabs(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Panel control(s): `C_Min`, `C_Max`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `C_Min`, `C_Max`
- Source: `Lawler-Fujita Drift Correction.ipf:3059`

#### `SetVarProc_ConstLF`

```igor
Function SetVarProc_ConstLF(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `FWHM(r)`, `Cond`, `factor`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM(r)`, `Cond`, `factor`
- Source: `Lawler-Fujita Drift Correction.ipf:1213`

#### `SetVarProc_ConstLFstrain`

```igor
Function SetVarProc_ConstLFstrain(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `FWHM(r)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `Drawarrow()`, `Strainanalysis()`, `mdg_strain2()`, `max()`, `min()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `FWHM(r)`
- Source: `Lawler-Fujita Drift Correction.ipf:3828`

#### `SetVarProc_Constsrain`

```igor
Function SetVarProc_Constsrain(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Order of Polynomial`, `Fe-Fe Frame?`, `θ(˚)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `Drawarrow()`, `Strainanalysis()`, `mdg_strain2()`, `max()`, `min()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Order of Polynomial`, `Fe-Fe Frame?`, `θ(˚)`
- Source: `Lawler-Fujita Drift Correction.ipf:4141`

#### `SetVarProc_lsend`

```igor
Function SetVarProc_lsend(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #III.3 Interactively tuning the contourplot [Contour level end] Panel control(s): `End`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `End`, `End`, `End`
- Source: `Lattice Segregation.ipf:345`

#### `SetVarProc_lsends`

```igor
Function SetVarProc_lsends(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `End`. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `End`, `End`, `End`, `End`
- Source: `Lattice Segregation.ipf:1219`

#### `SetVarProc_lsn`

```igor
Function SetVarProc_lsn(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Notes: #III.4 Interactively tuning the contourplot [Contour level Number] Usage: called automatically by Igor when the linked panel control changes. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Lattice Segregation.ipf:388`

#### `SetVarProc_lsns`

```igor
Function SetVarProc_lsns(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `Lattice Segregation.ipf:1165`

#### `SetVarProc_lsstart`

```igor
Function SetVarProc_lsstart(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #III.2 Interactively tuning the contourplot [Contour level start] Panel control(s): `Start`. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Start`
- Source: `Lattice Segregation.ipf:302`

#### `SetVarProc_lsstarts`

```igor
Function SetVarProc_lsstarts(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Panel control(s): `Start`. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Start`
- Source: `Lattice Segregation.ipf:1192`

#### `SetVarProc_lsthresh`

```igor
Function SetVarProc_lsthresh(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #IV.2 Control threshold Panel control(s): `Threshold`. Main internal calls: `MDoLSegrereapply()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Threshold`
- Source: `Lattice Segregation.ipf:757`

#### `SetVarProc_SumCompOrder_square`

```igor
Function SetVarProc_SumCompOrder_square(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis. Panel control(s): `|A1|`, `|A2|`, `θ`. Main internal calls: `cons_SumCompOrder_square()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `|A1|`, `|A2|`, `θ`
- Source: `Models.ipf:11569`


### Interactive procedures and macros

#### `Ahoneycombc`

```igor
Proc Ahoneycombc(xx, a, theta, widthr)
```
- Usage: run from Igor with parameters `xx`, `a`, `theta`, `widthr`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: xx = L, Length of image (Å); a = a, Lattice constant (Å); theta = Lattice orientation (˚); widthr = r, Width of Gaussian = a/r. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Ahoneycomb()`.
- Parameters:
  - `xx`: L, Length of image (Å)
  - `a`: a, Lattice constant (Å)
  - `theta`: Lattice orientation (˚)
  - `widthr`: r, Width of Gaussian = a/r
- Source: `General_Simu.ipf:2212`

#### `C4_symc`

```igor
proc C4_symc(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `C4_sym()`.
- Parameters:
  - `name`: Matrix name
- Source: `Symmetrization.ipf:132`

#### `C4shearcorrectc`

```igor
Proc C4shearcorrectc(name,factor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `name`, `factor`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis. Main internal calls: `C4shearcorrect()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Shear Correction for C4.ipf:259`

#### `C4sheargetparac`

```igor
Proc C4sheargetparac(alphaini,ppini,thetaini,qq,hold)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `alphaini`, `ppini`, `thetaini`, `qq`, `hold`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: alphaini = α (Ideal vector angle from +x in degree); ppini = P (X shear parameter); thetaini = θ (Shear axis angle from +x in degree); qq = |q| (vector norm of the ideal lattice. ##Note## this should be real value, the length directly read from FFT should be multipied by 2pi. #This value affects only choose Hold |q| and put a non-zero value, other wise, the |q| will be automatically given by q1 and q2#); hold = Hold ideal |q| ?.
- Parameters:
  - `alphaini`: α (Ideal vector angle from +x in degree)
  - `ppini`: P (X shear parameter)
  - `thetaini`: θ (Shear axis angle from +x in degree)
  - `qq`: |q| (vector norm of the ideal lattice. ##Note## this should be real value, the length directly read from FFT should be multipied by 2pi. #This value affects only choose Hold |q| and put a non-zero value, other wise, the |q| will be automatically given by q1 and q2#)
  - `hold`: Hold ideal |q| ?
- Source: `Shear Correction for C4.ipf:32`

#### `c4special`

```igor
proc c4special(name1,point)
```
- Usage: run from Igor with parameters `name1`, `point`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name1 = Name of the data to interpolate; point = point number of the short dimension. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `makematrix()`, `di()`.
- Parameters:
  - `name1`: Name of the data to interpolate
  - `point`: point number of the short dimension
- Source: `FFT.ipf:247`

#### `C4symFFT`

```igor
proc C4symFFT(name,num,theta)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `num`, `theta`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: theta = angle between lattice and x+. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `theta`: angle between lattice and x+
- Source: `FFT.ipf:140`

#### `C4symFFT2`

```igor
proc C4symFFT2(name,num,theta)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `num`, `theta`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: theta = angle between lattice and x+. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `theta`: angle between lattice and x+
- Source: `Miscellaneous_Codes.ipf:4897`

#### `C4symFFT2r`

```igor
proc C4symFFT2r(name,num,theta)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `name`, `num`, `theta`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: theta = angle between lattice and x+. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `theta`: angle between lattice and x+
- Source: `Miscellaneous_Codes.ipf:4118`

#### `caclcshearxc`

```igor
Proc caclcshearxc()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis. Main internal calls: `caclcshearx()`.
- Source: `Shear Correction for C4.ipf:949`

#### `cal_strainbyshearc`

```igor
Proc cal_strainbyshearc(angleoff,sh)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `angleoff`, `sh`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: angleoff = The angle from shear axis-X to one of the lattice axis (˚)###### it always equal to α-θ [lattice Q angle - shear axis angle]; sh = Shear strength. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `cal_strainbyshear()`.
- Parameters:
  - `angleoff`: The angle from shear axis-X to one of the lattice axis (˚)###### it always equal to α-θ [lattice Q angle - shear axis angle]
  - `sh`: Shear strength
- Source: `Shear Correction for C4.ipf:1124`

#### `calccshearparameter`

```igor
Proc calccshearparameter()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: A quick Template procedure to make an interactive GUI Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: opens or updates graph/image displays; reads or updates panel controls.
- Source: `Shear Correction for C4.ipf:930`

#### `calculateMoireLc`

```igor
Proc calculateMoireLc(asample,asub,angle)
```
- Usage: run from Igor with parameters `asample`, `asub`, `angle`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Dialog prompts: asample = lattice constant of top layer; asub = lattice constant of bottom layer; angle = twist angle ˚. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `calculateMoireL()`.
- Parameters:
  - `asample`: lattice constant of top layer
  - `asub`: lattice constant of bottom layer
  - `angle`: twist angle ˚
- Source: `Triangle Lattice_Graphene_like.ipf:349`

#### `CisFitc`

```igor
Proc CisFitc()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis. Main internal calls: `C4sheargetpara2()`.
- Source: `Shear Correction for C4.ipf:739`

#### `cnfyjc`

```igor
Proc cnfyjc(namew,add,cond)
```
- Usage: run from Igor with parameters `namew`, `add`, `cond`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Dialog prompts: namew = Name of the wave to be manipulated; cond = Threshold for dinstinguish a jump; add = the theoretical phase difference at the jump. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `cnfyj()`.
- Parameters:
  - `namew`: Name of the wave to be manipulated
  - `add`: the theoretical phase difference at the jump
  - `cond`: Threshold for dinstinguish a jump
- Source: `Lawler-Fujita Drift Correction.ipf:443`

#### `Const2dlockin`

```igor
Proc Const2dlockin()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: reads or updates panel controls. Main internal calls: `grabwin()`.
- Source: `Lawler-Fujita Drift Correction.ipf:1856`

#### `Const2dlockinc`

```igor
Proc Const2dlockinc()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Main internal calls: `lockinp2()`, `grabwin()`.
- Source: `Lawler-Fujita Drift Correction.ipf:1883`

#### `Const2dlockincftd`

```igor
Proc Const2dlockincftd()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Main internal calls: `lockinpnew()`, `FTnew()`.
- Source: `Lawler-Fujita Drift Correction.ipf:2371`

#### `ConstLF`

```igor
Proc ConstLF()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: reads or updates panel controls. Update log: 2023-07-20: Interactive Lawler-Fujita tuning panel. Run the single-value LF workflow first, then use the "adj" control to continuously tune the LF correction parameters.
- Source: `Lawler-Fujita Drift Correction.ipf:1183`

#### `ContinitialShearFit`

```igor
Proc ContinitialShearFit()
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `grabtable()`, `grabwin()`, `margin()`. Update log: 2023-07-20: Interactive shear-correction fitting panel.
- Source: `Shear Correction for C4.ipf:713`

#### `Correct2Dlockinampc`

```igor
Proc Correct2Dlockinampc(ind,ind2,scr,angle,windowsize)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `ind`, `ind2`, `scr`, `angle`, `windowsize`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: ind = Indication; ind2 = Indication; scr = Name of gap map; angle = angle of QA (°); windowsize = window size (Usually it equal to lamda). Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `Correct2Dlockinamp()`, `modifyphasetoponew()`, `modifyphasenew()`, `ckfig_child()`.
- Parameters:
  - `ind`: Indication
  - `ind2`: Indication
  - `scr`: Name of gap map
  - `angle`: angle of QA (°)
  - `windowsize`: window size (Usually it equal to lamda)
- Source: `Lawler-Fujita Drift Correction.ipf:2955`

#### `Correct2Dlockinampcout`

```igor
Proc Correct2Dlockinampcout()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Main internal calls: `atan2()`, `Correct2Dlockinampc()`.
- Source: `Lawler-Fujita Drift Correction.ipf:2919`

#### `D2symetric`

```igor
proc D2symetric(name2,centralx,centraly)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name2`, `centralx`, `centraly`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name2 = Data name. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `xreflectmatrix()`, `yreflectmatrix()`, `di()`.
- Parameters:
  - `name2`: Data name
  - `centralx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `centraly`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:653`

#### `D2symetric2`

```igor
proc D2symetric2(name2,centralx,centraly)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name2`, `centralx`, `centraly`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name2 = Data name. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `xreflectmatrix2()`, `yreflectmatrix2()`.
- Parameters:
  - `name2`: Data name
  - `centralx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `centraly`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4807`

#### `D2symetric2r`

```igor
proc D2symetric2r(name2,centralx,centraly)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name2`, `centralx`, `centraly`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: name2 = Data name. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `xreflectmatrix2r()`, `yreflectmatrix2r()`.
- Parameters:
  - `name2`: Data name
  - `centralx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `centraly`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:4028`

#### `D4_symc`

```igor
proc D4_symc(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `D4_sym()`.
- Parameters:
  - `name`: Matrix name
- Source: `Symmetrization.ipf:520`

#### `Demo_rotatedshear`

```igor
Proc Demo_rotatedshear(pp,theta)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `pp`, `theta`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Dialog prompts: pp = P (X shear coef); theta = θ (Shear axis angle from +x in degree). Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `dilf()`, `rotatedshear()`.
- Parameters:
  - `pp`: P (X shear coef)
  - `theta`: θ (Shear axis angle from +x in degree)
- Source: `Shear Correction for C4.ipf:473`

#### `Fitimagehoneycombc`

```igor
Proc Fitimagehoneycombc(name, a, theta, dx, dy)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`, `a`, `theta`, `dx`, `dy`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `tpw()`, `Fitimagehoneycomb()`, `grabwin()`. Update log: 2024-11-25: Appends or fits a honeycomb lattice overlay on an image using lattice constant, rotation angle, and xy offset parameters.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2276`

#### `Fitimagesquarec`

```igor
Proc Fitimagesquarec(name, a, theta, dx, dy)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`, `a`, `theta`, `dx`, `dy`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `tpw()`, `Fitimagesquare()`, `grabwin()`. Update log: 2024-11-25: Appends or fits a square lattice overlay on an image using lattice constant, rotation angle, and xy offset parameters.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2542`

#### `Fitimagetriangularc`

```igor
Proc Fitimagetriangularc(name, a, theta, dx, dy)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `name`, `a`, `theta`, `dx`, `dy`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `tpw()`, `Fitimagetriangular()`, `grabwin()`. Update log: 2024-11-25: Appends or fits a triangular lattice overlay on an image using lattice constant, rotation angle, and xy offset parameters.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2420`

#### `gpac_twist`

```igor
Proc gpac_twist()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Main internal calls: `Gpa_twist()`.
- Source: `Triangle Lattice_Graphene_like.ipf:81`

#### `gpbc_twist`

```igor
Proc gpbc_twist()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Main internal calls: `GpB_twist()`.
- Source: `Triangle Lattice_Graphene_like.ipf:104`

#### `gpCc_twist`

```igor
Proc gpCc_twist()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Main internal calls: `GpC_twist()`.
- Source: `Triangle Lattice_Graphene_like.ipf:127`

#### `LawlerFujita`

```igor
Proc LawlerFujita(avedia,condA,condB,factor,sel)
```
- Usage: run from Igor with parameters `avedia`, `condA`, `condB`, `factor`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Dialog prompts: avedia = The FWHM in real space (guassian window); factor = factor of the output, how many times the dimsize of original matrix; condA = the threshold for determine a jump for vector A, in portion of pi; condB = the threshold for determine a jump for vector B, in portion of pi; sel = Which Q?. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `grabwinnonew()`, `LF()`, `Dosubwindisplay()`, `ConstLF()`.
- Parameters:
  - `avedia`: The FWHM in real space (guassian window)
  - `condA`: the threshold for determine a jump for vector A, in portion of pi
  - `condB`: the threshold for determine a jump for vector B, in portion of pi
  - `factor`: factor of the output, how many times the dimsize of original matrix
  - `sel`: Which Q?
- Source: `Lawler-Fujita Drift Correction.ipf:34`

#### `LawlerFujita2`

```igor
Proc LawlerFujita2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Main internal calls: `LF()`, `grabwinchild()`.
- Source: `Lawler-Fujita Drift Correction.ipf:1220`

#### `LFtc`

```igor
Proc LFtc(ind1,ind2,ind3,scrwave,XX,YY)
```
- Usage: run from Igor with parameters `ind1`, `ind2`, `ind3`, `scrwave`, `XX`, `YY`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: ind1 = Tip #1; ind2 = Tip #2; ind3 = Tip #3; scrwave = The 3D wave; XX = LF YY wave. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getall3dwave()`, `LFt()`.
- Parameters:
  - `ind1`: Tip #1
  - `ind2`: Tip #2
  - `ind3`: Tip #3
  - `scrwave`: The 3D wave
  - `XX`: LF YY wave
  - `YY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:2149`

#### `LSc2`

```igor
Proc LSc2(src,topo)
```
- Usage: run from Igor with parameters `src`, `topo`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: src = Source Wave [Data]; topo = Location wave [Topograph]. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `LS22()`.
- Parameters:
  - `src`: Source Wave [Data]
  - `topo`: Location wave [Topograph]
- Source: `Lattice Segregation.ipf:61`

#### `makelatticec`

```igor
Proc makelatticec(a,angle,num,areaL,q_por)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `a`, `angle`, `num`, `areaL`, `q_por`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing. Dialog prompts: a = Lattice constant (Å); angle = degree of the lattice axis; num = Grid Points; areaL = Map area (Å x Å); q_por = Interpocket |q| vector, portion of G. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `makelattice()`.
- Parameters:
  - `a`: Lattice constant (Å)
  - `angle`: degree of the lattice axis
  - `num`: Grid Points
  - `areaL`: Map area (Å x Å)
  - `q_por`: Interpocket |q| vector, portion of G
- Source: `Models.ipf:12138`

#### `Makelatticedata`

```igor
Proc Makelatticedata(xi,yi,many,theta,av,ah,sigmaa,xn,yn)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `xi`, `yi`, `many`, `theta`, `av`, `ah`, `sigmaa`, `xn`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Dialog prompts: xi = xi(initial x for calculation); yi = yi(initial y for calculation); many = The size of lattice will be calculated is many x many; theta = angle of lattice to x+ (degree); ah = ah(lattice constance h); av = av(lattice constance v). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `findtheintersection()`.
- Parameters:
  - `xi`: xi(initial x for calculation)
  - `yi`: yi(initial y for calculation)
  - `many`: The size of lattice will be calculated is many x many
  - `theta`: angle of lattice to x+ (degree)
  - `av`: av(lattice constance v)
  - `ah`: ah(lattice constance h)
  - `sigmaa`: sigma of the Guassian
  - `xn`: x scale of lattice
  - `yn`: y scale of lattice
- Source: `Models.ipf:61`

#### `makeshearXimagec`

```igor
Proc makeshearXimagec(pp,name,factor)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `pp`, `name`, `factor`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: pp = X shear parameter; name = name of the wave to be sheared; factor = interpolate factor. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `makeshearXimage()`.
- Parameters:
  - `pp`: X shear parameter
  - `name`: name of the wave to be sheared
  - `factor`: interpolate factor
- Source: `Shear Correction for C4.ipf:613`

#### `Mdiag_symc`

```igor
proc Mdiag_symc(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Mdiag_sym()`.
- Parameters:
  - `name`: Matrix name
- Source: `Symmetrization.ipf:307`

#### `Moffdiag_symc`

```igor
proc Moffdiag_symc(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Moffdiag_sym()`.
- Parameters:
  - `name`: Matrix name
- Source: `Symmetrization.ipf:387`

#### `Mx_symc`

```igor
proc Mx_symc(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `Mx_sym()`.
- Parameters:
  - `name`: Matrix name
- Source: `Symmetrization.ipf:336`

#### `My_symc`

```igor
proc My_symc(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; Igor wave/matrix/cube data operation. Dialog prompts: name = Matrix name. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `My_sym()`.
- Parameters:
  - `name`: Matrix name
- Source: `Symmetrization.ipf:468`

#### `plotlattice`

```igor
proc plotlattice(num,theta,ah,av,b,c)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `num`, `theta`, `ah`, `av`, `b`, `c`.
- Notes: this procedure can draw lattice in 2D. it can also be used as a fitting to an experimental data, by manually adjust these parameters. Dialog prompts: num = number of line; theta = angle of lattice to x+; ah = lattice constance h; av = lattice constance v; b = offset#1; c = offset#2. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `num`: number of line
  - `theta`: angle of lattice to x+
  - `ah`: lattice constance h
  - `av`: lattice constance v
  - `b`: offset#1
  - `c`: offset#2
- Source: `Models.ipf:12`

#### `RDF`

```igor
Proc RDF(name,OP,OQ)
```
- Usage: run from Igor with parameters `name`, `OP`, `OQ`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Main internal calls: `tpw()`, `pcsr()`, `qcsr()`, `getpolaraveRcurve()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `OP`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `OQ`: Momentum or Q-vector coordinate/index.
- Source: `Miscellaneous_Codes.ipf:3360`

#### `Reflecttrace`

```igor
Proc Reflecttrace(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:3182`

#### `rotatedshearc`

```igor
Proc rotatedshearc(name,pp,theta)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `name`, `pp`, `theta`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Dialog prompts: name = Name of the wave to be manipulated; pp = P (X shear coef); theta = θ (Shear axis angle from +x in degree). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `rotatedshear()`.
- Parameters:
  - `name`: Name of the wave to be manipulated
  - `pp`: P (X shear coef)
  - `theta`: θ (Shear axis angle from +x in degree)
- Source: `Shear Correction for C4.ipf:490`

#### `Strainanalysisc`

```igor
Proc Strainanalysisc(uxt,uyt,order,mothername,selqmode)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run from Igor with parameters `uxt`, `uyt`, `order`, `mothername`, `selqmode`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Dialog prompts: uxt = name of the displacement matrix ux; uyt = name of the displacement matrix uy; order = Polynimal order for BKremove; mothername = name of the topography; selqmode = Which Q?. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `atan2()`, `Strainanalysis()`, `mdg_strain1()`, `manTick()`, `manMinor()`, `grid()`.
- Parameters:
  - `uxt`: name of the displacement matrix ux
  - `uyt`: name of the displacement matrix uy
  - `order`: Polynimal order for BKremove
  - `mothername`: name of the topography
  - `selqmode`: Which Q?
- Source: `Lawler-Fujita Drift Correction.ipf:3500`

#### `symbands`

```igor
proc symbands(matname,point,flag)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `matname`, `point`, `flag`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: matname = which matrix to symmetry; point = The point as reference; flag = Please Select Mode. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `DeletePdata()`, `symkrtr()`, `symkrtl()`.
- Parameters:
  - `matname`: which matrix to symmetry
  - `point`: The point as reference
  - `flag`: Please Select Mode
- Source: `Miscellaneous_Codes.ipf:2893`

#### `symbands_cmplx`

```igor
proc symbands_cmplx(matname,point,flag)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `matname`, `point`, `flag`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; graph display, formatting, or window management. Dialog prompts: matname = which matrix to symmetry; point = The point as reference; flag = Please Select Mode. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `DeletePdata()`, `symkrtr_cmplx()`, `symkrtl_cmplx()`.
- Parameters:
  - `matname`: which matrix to symmetry
  - `point`: The point as reference
  - `flag`: Please Select Mode
- Source: `Miscellaneous_Codes.ipf:2973`

#### `symbandsall`

```igor
proc symbandsall(s,matname,point,flag)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `s`, `matname`, `point`, `flag`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Dialog prompts: s = Data Type; matname = which matrix to symmetry; point = The point as reference; flag = Please Select Mode. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `symbands()`, `symbands_cmplx()`.
- Parameters:
  - `s`: Data Type
  - `matname`: which matrix to symmetry
  - `point`: The point as reference
  - `flag`: Please Select Mode
- Source: `Miscellaneous_Codes.ipf:2877`

#### `symmetrizeQPIdallc`

```igor
Proc symmetrizeQPIdallc(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing. Main internal calls: `tpw()`, `symmetrizeQPIdall()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Symmetrization.ipf:582`

#### `t2dlockin`

```igor
Proc t2dlockin(avedia)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `avedia`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: avedia = The FWHM in real space (guassian window). Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `lockinp()`, `grabwin2()`, `modifyphasetopo()`, `grabwin()`, `tpw()`, `Const2dlockin()`.
- Parameters:
  - `avedia`: The FWHM in real space (guassian window)
- Source: `Lawler-Fujita Drift Correction.ipf:1433`

#### `testlattice`

```igor
proc testlattice(a,b,c,theta)
```
- Usage: run from Igor with parameters `a`, `b`, `c`, `theta`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Parameters:
  - `a`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `b`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `c`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Miscellaneous_Codes.ipf:9363`


### Computational and data helpers

#### `Ahoneycomb`

```igor
Function Ahoneycomb(xx, a, theta, widthr)
```
- Usage: call as `Ahoneycomb(xx, a, theta, widthr)`.
- Notes: Function for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `CreateTriangularLatticeCoordinates()`, `gauss2D()`, `di()`.
- Parameters:
  - `xx`: = 100 // x-scale limit
  - `a`: = 10 // Lattice constant
  - `theta`: =0
  - `widthr`: = 5.5
- Source: `General_Simu.ipf:2224`

#### `appendhoneycomb00`

```igor
Function appendhoneycomb00(xx, yy, a, theta, dx, dy)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `appendhoneycomb00(xx, yy, a, theta, dx, dy)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `ceil()`.
- Parameters:
  - `xx`: = 100 // x-scale limit
  - `yy`: = 100 // y-scale limit
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2340`

#### `appendsquare00`

```igor
Function appendsquare00(xx, yy, a, theta, dx, dy)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `appendsquare00(xx, yy, a, theta, dx, dy)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `ceil()`.
- Parameters:
  - `xx`: = 100 // x-scale limit
  - `yy`: = 100 // y-scale limit
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1 // Phase shift in y-direction
- Source: `General_Simu.ipf:2606`

#### `appendtriangular00`

```igor
Function appendtriangular00(xx, yy, a, theta, dx, dy)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `appendtriangular00(xx, yy, a, theta, dx, dy)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `ceil()`.
- Parameters:
  - `xx`: = 100 // x-scale limit
  - `yy`: = 100 // y-scale limit
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2484`

#### `C4_sym`

```igor
Function C4_sym(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `C4_sym(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `calculateRC()`, `di()`, `color3s_for3dFFT()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Symmetrization.ipf:137`

#### `C4_sym_3dplot`

```igor
Function C4_sym_3dplot(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `C4_sym_3dplot(name)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `calculateRC()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:10493`

#### `C4shearcorrect`

```igor
Function C4shearcorrect(name,factor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `C4shearcorrect(name, factor)`.
- Notes: Function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; formats graph axes, labels, colors, or annotations; uses Igor matrix operations for vectorized calculation. Main internal calls: `func_NaN0()`, `di2lf()`, `grabwin()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Shear Correction for C4.ipf:264`

#### `C4sheargetpara`

```igor
Function C4sheargetpara(name,alphaini,ppini,thetaini,qq,hold)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `C4sheargetpara(name, alphaini, ppini, thetaini, qq, hold)`.
- Notes: Function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `DoNewGlobalFit()`, `cktable()`, `ckwaveonfig()`, `mode()`, `marker()`, `rgb()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `alphaini`: c1: alpha [The vector q1 angle] [0,pi/2]
  - `ppini`: c2: pp [The X shear parameter]
  - `thetaini`: c3: theta [the angle of X shear axis] [0,pi/2]
  - `qq`: c0: the vector norm of ideal q
  - `hold`: select the mode for Hold |q| (1), not Hold (2)
- Source: `Shear Correction for C4.ipf:62`

#### `C4sheargetpara2`

```igor
Function C4sheargetpara2(name,alphaini,ppini,thetaini,qq,hold)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `C4sheargetpara2(name, alphaini, ppini, thetaini, qq, hold)`.
- Notes: Function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `DoNewGlobalFit()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `alphaini`: c1: alpha [The vector q1 angle] [0,pi/2]
  - `ppini`: c2: pp [The X shear parameter]
  - `thetaini`: c3: theta [the angle of X shear axis] [0,pi/2]
  - `qq`: c0: the vector norm of ideal q
  - `hold`: select the mode for Hold |q| (1), not Hold (2)
- Source: `Shear Correction for C4.ipf:744`

#### `C4sheargetpara_3dplot`

```igor
Function C4sheargetpara_3dplot(name)
```
- Usage: call as `C4sheargetpara_3dplot(name)`.
- Notes: #2_02 Extract Shearing:Main Purpose: displays waves, images, contours, or graph overlays. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `DoNewGlobalFit()`, `ckwaveonfig()`, `mode()`, `marker()`, `rgb()`, `mrkThick()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:5140`

#### `C4sym_PRQPI`

```igor
Function C4sym_PRQPI(name)
```
- Usage: call as `C4sym_PRQPI(name)`.
- Notes: ## C4 symmetrilization of a wave Purpose: applies symmetry, mirror, or reflection operations. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Update log: 2023-12-03: Applies fourfold symmetrization to a PR-QPI wave before display or further analysis.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `PR_QPI.ipf:358`

#### `caclcshearx`

```igor
Function caclcshearx(theta1,alpha1,pp1)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `caclcshearx(theta1, alpha1, pp1)`.
- Notes: Function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `theta1`: Angle parameter used for ARPES momentum conversion or rotation.
  - `alpha1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `pp1`: Start point, index, or coordinate for the selected range.
- Source: `Shear Correction for C4.ipf:952`

#### `cal_strainbyshear`

```igor
Function cal_strainbyshear(angleoff,sh)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `cal_strainbyshear(angleoff, sh)`. Update-log guidance - 2024-01-24: Calculates lattice mismatch after shear correction for a lattice at an arbitrary angle relative to the shear axis; connected to the epsilon button in the strain workflow.
- Notes: Function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Main internal calls: `shearxy()`, `max()`.
- Parameters:
  - `angleoff`: degree
  - `sh`: shear strength
- Source: `Shear Correction for C4.ipf:1133`

#### `calculateMoireL`

```igor
Function calculateMoireL(asample,asub,angle)
```
- Usage: call as `calculateMoireL(asample, asub, angle)`.
- Notes: Utility function for lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Parameters:
  - `asample`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `asub`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Triangle Lattice_Graphene_like.ipf:358`

#### `cnfyj`

```igor
Function cnfyj(namew,cond,add)
```
- Usage: call as `cnfyj(namew, cond, add)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `findysjump()`, `linkstsmapjr()`, `findxsjump()`, `minorbugremove()`. Update log: 2023-07-16: Improved the Lawler-Fujita phase-jump remover for images with multiple phase jumps.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `cond`: pi, the jump height threshold to define a jump, usually it can be set as add/2
  - `add`: 2pi, the theoretical phase difference at the jump
- Source: `Lawler-Fujita Drift Correction.ipf:453`

#### `color3slf`

```igor
Function color3slf(name)
```
- Usage: call as `color3slf(name)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; formats graph axes, labels, colors, or annotations; extracts image/wave statistics.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:1138`

#### `Complextorealfls`

```igor
Function Complextorealfls(name1w,select)
```
- Notes: #IV.3_2 Special FFT Function (called) Usage: call as `Complextorealfls(name1w, select)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `Lattice Segregation.ipf:1276`

#### `cons_SumCompOrder_square`

```igor
Function cons_SumCompOrder_square(A1,A2,theta)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `cons_SumCompOrder_square(A1, A2, theta)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations. Main internal calls: `cvtcmplx()`, `max()`.
- Parameters:
  - `A1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `A2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Models.ipf:11582`

#### `Const2dlockinftd`

```igor
Function Const2dlockinftd()
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call `Const2dlockinftd()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: writes or exports data; reads or updates panel controls.
- Source: `Lawler-Fujita Drift Correction.ipf:2286`

#### `Correct2Dlockinamp`

```igor
Function Correct2Dlockinamp(scr,angle,windowsize)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `Correct2Dlockinamp(scr, angle, windowsize)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `getabsamp()`, `mean()`, `gethistgram_absamp()`. Update log: 05/01/2024: Changed the method for converting the relative amplitude from 2D lock-in to the real value from individual factor mode to averaged factor modes.
- Parameters:
  - `scr`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `windowsize`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Lawler-Fujita Drift Correction.ipf:3075`

#### `countaverage`

```igor
Function countaverage(mat)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `countaverage(mat)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:5194`

#### `countaveragetop10`

```igor
function countaveragetop10(mat)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `countaveragetop10(mat)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: extracts image/wave statistics.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:5217`

#### `CreateTriangularLatticeCoordinates`

```igor
Function CreateTriangularLatticeCoordinates(xx, yy, a, theta)
```
- Usage: call as `CreateTriangularLatticeCoordinates(xx, yy, a, theta)`.
- Notes: #1 Core Function to output atomic positions of Honeycomb lattice Purpose: creates new waves, maps, figures, or simulation data. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `ceil()`. Update log: 2024-11-23: Calculates honeycomb/triangular lattice coordinates from the origin, lattice constant, and rotation angle; used by the TBG/TTG and lattice-simulation tools.
- Parameters:
  - `xx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `a`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `General_Simu.ipf:2134`

#### `D4_sym`

```igor
Function D4_sym(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `D4_sym(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`, `color3s_for3dFFT()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Symmetrization.ipf:525`

#### `D4_sym_3dplot`

```igor
Function D4_sym_3dplot(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `D4_sym_3dplot(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:10578`

#### `differentiatealonga`

```igor
Function differentiatealonga(name)
```
- Usage: call as `differentiatealonga(name)`.
- Notes: Kernel#07 ** Function for ua derivative Purpose: displays waves, images, contours, or graph overlays. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:4712`

#### `differentiatealongb`

```igor
Function differentiatealongb(name)
```
- Usage: call as `differentiatealongb(name)`.
- Notes: Kernel#08 ** Function for ub derivative Purpose: displays waves, images, contours, or graph overlays. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:4742`

#### `differentiatealongx`

```igor
Function differentiatealongx(name)
```
- Usage: call as `differentiatealongx(name)`.
- Notes: Kernel#05 ** Function for ux derivative Purpose: displays waves, images, contours, or graph overlays. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:4653`

#### `differentiatealongy`

```igor
Function differentiatealongy(name)
```
- Usage: call as `differentiatealongy(name)`.
- Notes: Kernel#06 ** Function for uy derivative Purpose: displays waves, images, contours, or graph overlays. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:4683`

#### `DoLSegre`

```igor
Function DoLSegre(name,prefwave,thresh,mode)
```
- Notes: #IV.4 Main Functional Codes Usage: call as `DoLSegre(name, prefwave, thresh, mode)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mean()`.
- Parameters:
  - `name`: 2D wave to be segregated
  - `prefwave`: Igor wave reference used as input or output.
  - `thresh`: threshold for defining a lattice position (max=1)
  - `mode`: (= 1 is Scatter imageinterpolate mode, more modes to be added)
- Source: `Lattice Segregation.ipf:1306`

#### `Dosubwindisplay`

```igor
Function Dosubwindisplay(name,avedia)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Dosubwindisplay(name, avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `color3s_for3dmf()`, `grabwinchild()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:1292`

#### `DPdata2`

```igor
Function DPdata2(name,flag,stp,edp,numbercut)//testing finished
```
- Usage: call as `DPdata2(name, flag, stp, edp, numbercut)`.
- Notes: Function for symmetry or reflection processing; linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `flag`: delete k demision(raw)=0 delete E demision(colum) =1
  - `stp`: Start point, index, or coordinate for the selected range.
  - `edp`: End point, index, or coordinate for the selected range.
  - `numbercut`: number of cuts
- Source: `Shear Correction for C4.ipf:1048`

#### `ExampleTriangularLattice`

```igor
Function ExampleTriangularLattice(xx, yy, a, theta)
```
- Notes: #0 Main Function Usage: call as `ExampleTriangularLattice(xx, yy, a, theta)`. Main internal calls: `CreateTriangularLatticeCoordinates()`.
- Parameters:
  - `xx`: = 100 // x-scale limit
  - `yy`: = 100 // y-scale limit
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
- Source: `General_Simu.ipf:2119`

#### `fflf`

```igor
Function fflf(name)
```
- Notes: ############################################################################# End Jump Remover Version 1 ############################################################################# Usage: call as `fflf(name)`. Code behavior: performs Fourier-transform or inverse-transform operations. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealf1()`, `twoDinterpolatexyflf()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Lawler-Fujita Drift Correction.ipf:1067`

#### `findxsjump`

```igor
Function findxsjump (namew,cond,add)
```
- Notes: First EDC check the phase of the first coloum, they are the start phase of each row Usage: call as `findxsjump(namew, cond, add)`. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `checkfirstNonnan()`, `sign()`.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `cond`: pi, the jump height threshold to define a jump
  - `add`: 2pi, the theoretical phase difference at the jump
- Source: `Lawler-Fujita Drift Correction.ipf:647`

#### `findysjump`

```igor
Function findysjump(namew,cond,add,i)
```
- Usage: call as `findysjump(namew, cond, add, i)`.
- Notes: MDCs Get phases of the Row [i], the intra-MDC phase relationship are correct, but inter-MDC phase is not correct, it will be corrected by EDC procedure later. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `checkfirstNonnan()`, `sign()`.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `cond`: pi, the jump height threshold to define a jump, usually it can be set as add/2
  - `add`: 2pi, the theoretical phase difference at the jump
  - `i`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:512`

#### `Fitimagehoneycomb`

```igor
Function Fitimagehoneycomb(namew, a, theta, dx, dy)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `Fitimagehoneycomb(namew, a, theta, dx, dy)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Main internal calls: `Appendhoneycomb00()`.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2313`

#### `Fitimagesquare`

```igor
Function Fitimagesquare(namew, a, theta, dx, dy)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `Fitimagesquare(namew, a, theta, dx, dy)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Main internal calls: `Appendsquare00()`.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2579`

#### `Fitimagetriangular`

```igor
Function Fitimagetriangular(namew, a, theta, dx, dy)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: call as `Fitimagetriangular(namew, a, theta, dx, dy)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; lattice, moire, or twist-angle simulation/analysis. Main internal calls: `Appendtriangular00()`.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `a`: = 10 // Lattice constant
  - `theta`: = 0.1 // Rotation angle in radians
  - `dx`: = 2 // Phase shift in x-direction
  - `dy`: = 1
- Source: `General_Simu.ipf:2457`

#### `FTconsls`

```igor
Function FTconsls(name,avedia)
```
- Notes: #II.1 Special FT Filter for LS package Usage: call as `FTconsls(name, avedia)`. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `unpadding()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lattice Segregation.ipf:94`

#### `FTguassianremover`

```igor
Function FTguassianremover(name,sigma)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `FTguassianremover(name, sigma)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `Gauss2D()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `sigma`: Width or sigma parameter for smoothing, fitting, or Fourier filtering.
- Source: `Symmetrization.ipf:4`

#### `FTnew`

```igor
Function FTnew(name,avedia,qax,qay,qbx,qby)
```
- Usage: call as `FTnew(name, avedia, qax, qay, qbx, qby)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `unpadding()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `qax`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `qay`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `qbx`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
  - `qby`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Lawler-Fujita Drift Correction.ipf:2581`

#### `func_nantonoise`

```igor
Function func_nantonoise(matname)
```
- Usage: call as `func_nantonoise(matname)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:2196`

#### `func_vol_lac4`

```igor
Function func_vol_lac4(surfx_x,surfx_y,sizegrid,griddelta,lattice,deepness_matrix,position_x,position_y,position_deepness,inside_out)//calculates the volume
```
- Usage: call as `func_vol_lac4(surfx_x, surfx_y, sizegrid, griddelta, lattice, deepness_matrix, position_x, position_y)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `surfx_x`: Used in: Duplicate/O surfx_x xsurfx_x
  - `surfx_y`: Used in: Duplicate/O surfx_y xsurfx_y
  - `sizegrid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `griddelta`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `lattice`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `deepness_matrix`: Matrix/2D wave input or matrix output name. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `position_x`: Used in: Duplicate/O position_x sq_distance1
  - `position_y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `position_deepness`: Step size or energy/voltage increment used by the calculation.
  - `inside_out`: Step size or energy/voltage increment used by the calculation.
- Source: `Pierre's Template.ipf:21629`

#### `getabsamp`

```igor
Function getabsamp(scr,angle,windowsize)
```
- Usage: call as `getabsamp(scr, angle, windowsize)`.
- Notes: Moving window Procedure to extract correct amplitude Purpose: extracts values, metadata, cursor information, or derived waves. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `findrangeforangle_LH()`, `anglelinecutH_absamp()`, `removezerop()`.
- Parameters:
  - `scr`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `windowsize`: Igor wave reference used as input or output.
- Source: `Lawler-Fujita Drift Correction.ipf:3168`

#### `gethistgram_absamp`

```igor
Function gethistgram_absamp(name)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `gethistgram_absamp(name)`.
- Notes: Utility function for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `median()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:3135`

#### `GpA_twist`

```igor
Function GpA_twist()
```
- Usage: call `GpA_twist()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Source: `Triangle Lattice_Graphene_like.ipf:84`

#### `GpB_twist`

```igor
Function GpB_twist()
```
- Usage: call `GpB_twist()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Source: `Triangle Lattice_Graphene_like.ipf:107`

#### `GpC_twist`

```igor
Function GpC_twist()
```
- Usage: call `GpC_twist()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Source: `Triangle Lattice_Graphene_like.ipf:130`

#### `initialcontrolforstrainan`

```igor
Function initialcontrolforstrainan()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `initialcontrolforstrainan()` from Igor procedure code or the command line.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Lawler-Fujita Drift Correction.ipf:3803`

#### `jprm`

```igor
Function jprm(namew,cond,jump)
```
- Notes: Main Function Usage: call as `jprm(namew, cond, jump)`. Code behavior: creates output waves; reshapes existing waves. Main internal calls: `checkfirstNonnan()`, `reorgjump()`.
- Parameters:
  - `namew`: Igor wave reference used as input or output.
  - `cond`: =pi
  - `jump`: =2*pi
- Source: `Lawler-Fujita Drift Correction.ipf:752`

#### `jump2pi`

```igor
Function jump2pi(namew)
```
- Notes: Main Function Usage: call as `jump2pi(namew)`.
- Parameters:
  - `namew`: Igor wave reference used as input or output.
- Source: `Lawler-Fujita Drift Correction.ipf:942`

#### `jump2piX`

```igor
Function jump2piX(namew)
```
- Usage: call as `jump2piX(namew)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `namew`: Igor wave reference used as input or output.
- Source: `Lawler-Fujita Drift Correction.ipf:1015`

#### `jump2piY`

```igor
Function jump2piY(namew)
```
- Usage: call as `jump2piY(namew)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `namew`: Igor wave reference used as input or output.
- Source: `Lawler-Fujita Drift Correction.ipf:966`

#### `latticefit`

```igor
Function latticefit(w,x,y) : FitFunc
```
- Usage: call as `latticefit(w, x, y)`.
- Notes: #III.5 FitFunction for Cos(q*r+phi) Purpose: fits or extracts spectral/peak parameters.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lattice Segregation.ipf:431`

#### `LF`

```igor
Function LF(name,qx1,qy1,qx2,qy2,avedia,condA,condB,factor)
```
- Usage: Update-log guidance - 2023-07-13: Lawler-Fujita drift correction using the same Launch/GetA/GetB workflow as the FFT filter.
- Notes: Main Function Usage: call as `LF(name, qx1, qy1, qx2, qy2, avedia, condA, condB)`. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; cleans up waves/windows or brings an existing window forward. Main internal calls: `unpadding()`, `atan2()`, `cnfyj()`. Later FFT Engineering updates use Q_A and Q_B from the interpolated FFT image and support marquee-based fitting limits.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qx2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
  - `condA`: the threshold for determine a jump for vector A, in portion of pi
  - `condB`: the threshold for determine a jump for vector B, in portion of pi
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Lawler-Fujita Drift Correction.ipf:144`

#### `LFt`

```igor
Function LFt(scrwave,XX,YY)
```
- Usage: call as `LFt(scrwave, XX, YY)`.
- Notes: Function for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `scrwave`: Igor wave reference used as input or output.
  - `XX`: Used in: Make/O/N=(dimsize(XX,0),3) sampleTripletLFall
  - `YY`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:2165`

#### `linkstsmapjr`

```igor
Function linkstsmapjr(name,wn)
```
- Usage: call as `linkstsmapjr(name, wn)`.
- Notes: link 2D Map Purpose: combines many waves/slices into a map, linecut, or averaged output. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `wn`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:601`

#### `lockinp`

```igor
Function lockinp(name,qx1,qy1,qx2,qy2,avedia)
```
- Usage: call as `lockinp(name, qx1, qy1, qx2, qy2, avedia)`.
- Notes: Main Function Purpose: performs Fourier, filtering, phase, or lock-in processing. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations. Main internal calls: `unpadding()`, `atan2()`, `dilf()`, `modifyphase()`, `modifyphasefr()`, `modifyphasetopo()`.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qx2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
- Source: `Lawler-Fujita Drift Correction.ipf:1521`

#### `lockinp2`

```igor
Function lockinp2(name,qx1,qy1,qx2,qy2,avedia)
```
- Usage: call as `lockinp2(name, qx1, qy1, qx2, qy2, avedia)`.
- Notes: End of continuely tuning line thickness on the graph Purpose: performs Fourier, filtering, phase, or lock-in processing. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing. Main internal calls: `unpadding()`, `atan2()`.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qx2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
- Source: `Lawler-Fujita Drift Correction.ipf:1936`

#### `lockinpnew`

```igor
Function lockinpnew(name,qx1,qy1,qx2,qy2,avedia)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `lockinpnew(name, qx1, qy1, qx2, qy2, avedia)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: performs Fourier-transform or inverse-transform operations; duplicates or stages waves for downstream processing. Main internal calls: `unpadding()`, `atan2()`.
- Parameters:
  - `name`: the data to be manipulated
  - `qx1`: The FFT value of the vector, conversion (1/a), the scaled value directly readed from igor FFT image.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qx2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `avedia`: The FWHM in real space (guassian window)
- Source: `Lawler-Fujita Drift Correction.ipf:2638`

#### `LS`

```igor
Function LS()
```
- Usage: call `LS()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Main internal calls: `levelimage2()`, `FTconsls()`, `SimlatticebyFit()`, `MDoLSegre()`.
- Source: `Lattice Segregation.ipf:39`

#### `LS22`

```igor
Function LS22(src,topo)
```
- Usage: call as `LS22(src, topo)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Main internal calls: `levelimage2()`, `FTconsls()`, `SimlatticebyFit()`, `MDoLSegre()`.
- Parameters:
  - `src`: This is the 2D wave to be segregated
  - `topo`: This is the 2D wave to extract simulated sublattices
- Source: `Lattice Segregation.ipf:68`

#### `makelattice`

```igor
Function makelattice(a,angle,num,areaL,q_por)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makelattice(a, angle, num, areaL, q_por)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `FFTrQPIsimu()`.
- Parameters:
  - `a`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `angle`: Angle parameter used for ARPES momentum conversion or rotation.
  - `num`: Number of spectra, waves, points, bins, or iterations to process. In this context it is used for Fourier/QPI or phase-map processing.
  - `areaL`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `q_por`: Momentum or Q-vector coordinate/index. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Models.ipf:12235`

#### `makeshearXimage`

```igor
Function makeshearXimage(pp,name,factor)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makeshearXimage(pp, name, factor)`.
- Notes: Utility function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; uses Igor matrix operations for vectorized calculation. Main internal calls: `func_NaN0()`.
- Parameters:
  - `pp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `name`: Name or reference of the source wave/matrix being processed.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Shear Correction for C4.ipf:622`

#### `mdg_strain1`

```igor
Function mdg_strain1(index,divornot)
```
- Usage: call as `mdg_strain1(index, divornot)`.
- Notes: Kernel#10 ** Color style Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`, `CtabList()`.
- Parameters:
  - `index`: Step size or energy/voltage increment used by the calculation.
  - `divornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:4825`

#### `mdg_strain2`

```igor
Function mdg_strain2(index,divornot,num)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `mdg_strain2(index, divornot, num)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `CtabList()`.
- Parameters:
  - `index`: Step size or energy/voltage increment used by the calculation.
  - `divornot`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: subwindow number
- Source: `Lawler-Fujita Drift Correction.ipf:4244`

#### `Mdiag_sym`

```igor
Function Mdiag_sym(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Mdiag_sym(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `di()`, `color3s_for3dFFT()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Symmetrization.ipf:312`

#### `Mdiag_sym_3dplot`

```igor
Function Mdiag_sym_3dplot(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Mdiag_sym_3dplot(name)`.
- Notes: Function for Nanonis/STM data loading or map conversion; symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Smart_3D_Viewer_New.ipf:10532`

#### `MDoLSegre`

```igor
Function MDoLSegre(Src,Topo)
```
- Notes: #IV.1 Body Function Usage: call as `MDoLSegre(Src, Topo)`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `DoLSegre()`, `DoLSegreXY()`, `FFTrls()`, `color3s_for3dm()`, `color3s_for3dmf()`, `color3s_followtopo()`.
- Parameters:
  - `Src`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Topo`: Topography wave or topography-derived reference data. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Lattice Segregation.ipf:466`

#### `MDoLSegrereapply`

```igor
Function MDoLSegrereapply()
```
- Usage: call `MDoLSegrereapply()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `DoLSegre()`, `DoLSegreXY()`, `FFTrls()`, `color3s_for3dm2()`, `color3s_for3dmf()`, `color3s_followtopo()`.
- Source: `Lattice Segregation.ipf:764`

#### `meanexceptzero`

```igor
Function meanexceptzero(name)
```
- Usage: call as `meanexceptzero(name)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:3243`

#### `minorbugremove`

```igor
Function minorbugremove(namew)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `minorbugremove(namew)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; ARPES-style loading, plotting, or momentum conversion.
- Parameters:
  - `namew`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Lawler-Fujita Drift Correction.ipf:627`

#### `modifyphase`

```igor
Function modifyphase()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `modifyphase()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `margin()`, `tpw()`. Update log: 08/18-02/2023: correct the scale bar location problem in modifyphase() modifyphasetopo() modifyphasefr() and format/topo.
- Source: `Lawler-Fujita Drift Correction.ipf:1732`

#### `modifyphasefr`

```igor
Function modifyphasefr()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `modifyphasefr()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `margin()`, `tpw()`. Update log: 08/18-02/2023: correct the scale bar location problem in modifyphase() modifyphasetopo() modifyphasefr() and format/topo.
- Source: `Lawler-Fujita Drift Correction.ipf:1799`

#### `modifyphasefrnew`

```igor
Function modifyphasefrnew()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `modifyphasefrnew()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`.
- Source: `Lawler-Fujita Drift Correction.ipf:2875`

#### `modifyphasenew`

```igor
Function modifyphasenew()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `modifyphasenew()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`.
- Source: `Lawler-Fujita Drift Correction.ipf:2808`

#### `modifyphasetopo`

```igor
Function modifyphasetopo()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `modifyphasetopo()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `margin()`, `tpw()`. Update log: 08/18-02/2023: correct the scale bar location problem in modifyphase() modifyphasetopo() modifyphasefr() and format/topo.
- Source: `Lawler-Fujita Drift Correction.ipf:1765`

#### `modifyphasetoponew`

```igor
Function modifyphasetoponew()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `modifyphasetoponew()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `tpw()`.
- Source: `Lawler-Fujita Drift Correction.ipf:2841`

#### `Moffdiag_sym`

```igor
Function Moffdiag_sym(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Moffdiag_sym(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `di()`, `color3s_for3dFFT()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Symmetrization.ipf:392`

#### `Mx_sym`

```igor
Function Mx_sym(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `Mx_sym(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `di()`, `color3s_for3dFFT()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Symmetrization.ipf:341`

#### `Mx_sym_3dplot`

```igor
Function Mx_sym_3dplot(name)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `Mx_sym_3dplot(name)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Smart_3D_Viewer_New.ipf:10545`

#### `My_sym`

```igor
Function My_sym(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `My_sym(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `di()`, `color3s_for3dFFT()`, `tpw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Symmetrization.ipf:473`

#### `Newrotate_forstrain`

```igor
Function Newrotate_forstrain(name,theta1)
```
- Usage: call as `Newrotate_forstrain(name, theta1)`.
- Notes: Kernel#04 ** Function for Rotate the matrix Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `theta1`: counter clock-wise degree
- Source: `Lawler-Fujita Drift Correction.ipf:4599`

#### `Newrotate_forstrain_c`

```igor
Function Newrotate_forstrain_c(name,theta1)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `Newrotate_forstrain_c(name, theta1)`.
- Notes: Function for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `theta1`: counter clock-wise degree
- Source: `Lawler-Fujita Drift Correction.ipf:5080`

#### `Newrotate_forstrainback`

```igor
Function Newrotate_forstrainback(name,theta1)
```
- Usage: call as `Newrotate_forstrainback(name, theta1)`.
- Notes: Kernel#09 ** Function for Rotate the matrix back Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `theta1`: counter clock-wise degree
- Source: `Lawler-Fujita Drift Correction.ipf:4772`

#### `projectstrainmatrix`

```igor
Function projectstrainmatrix(ux,uy,theta)
```
- Usage: call as `projectstrainmatrix(ux, uy, theta)`.
- Notes: Kernel#02 ** Function for Project matrix to arbitary axis Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ux`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `uy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: degree
- Source: `Lawler-Fujita Drift Correction.ipf:4536`

#### `projectstrainmatrix45`

```igor
Function projectstrainmatrix45(ux,uy,theta)
```
- Usage: call as `projectstrainmatrix45(ux, uy, theta)`.
- Notes: Kernel#03 ** Function for Project matrix to arbitary axis +45 Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ux`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `uy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: degree
- Source: `Lawler-Fujita Drift Correction.ipf:4567`

#### `projectstrainmatrix_c`

```igor
Function projectstrainmatrix_c(ux,uy,theta)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `projectstrainmatrix_c(ux, uy, theta)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ux`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `uy`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: degree
- Source: `Lawler-Fujita Drift Correction.ipf:5130`

#### `removezerop`

```igor
Function removezerop(name)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: call as `removezerop(name)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Lawler-Fujita Drift Correction.ipf:3261`

#### `reorgjump`

```igor
Function reorgjump(namew,cond,jump,jumppoin,addvalu)
```
- Usage: call as `reorgjump(namew, cond, jump, jumppoin, addvalu)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `namew`: Igor wave reference used as input or output.
  - `cond`: =pi
  - `jump`: =2*pi
  - `jumppoin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `addvalu`: Step size or energy/voltage increment used by the calculation.
- Source: `Lawler-Fujita Drift Correction.ipf:841`

#### `rotatedshear`

```igor
Function rotatedshear(name,pp,theta)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `rotatedshear(name, pp, theta)`.
- Notes: Function for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; uses Igor matrix operations for vectorized calculation; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_NaN0()`, `dilf()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `pp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Shear Correction for C4.ipf:501`

#### `Shear_q1x`

```igor
Function Shear_q1x(w,x) : FitFunc
```
- Usage: call as `Shear_q1x(w, x)`.
- Notes: Fit function q1x Purpose: applies geometric correction or extracts deformation/strain information.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Shear Correction for C4.ipf:374`

#### `Shear_q1y`

```igor
Function Shear_q1y(w,x) : FitFunc
```
- Usage: call as `Shear_q1y(w, x)`.
- Notes: Fit function q1y Purpose: applies geometric correction or extracts deformation/strain information.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Shear Correction for C4.ipf:394`

#### `shear_q2x`

```igor
Function shear_q2x(w,x) : FitFunc
```
- Usage: call as `shear_q2x(w, x)`.
- Notes: Fit function q2x Purpose: applies geometric correction or extracts deformation/strain information.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Shear Correction for C4.ipf:414`

#### `shear_q2y`

```igor
Function shear_q2y(w,x) : FitFunc
```
- Usage: call as `shear_q2y(w, x)`.
- Notes: Fit function q2y Purpose: applies geometric correction or extracts deformation/strain information.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Shear Correction for C4.ipf:435`

#### `shearall`

```igor
Function shearall(mat3dn,refwave,factor)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `shearall(mat3dn, refwave, factor)`.
- Notes: Function for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; assigns Igor axis scaling/units to the output. Main internal calls: `func_zeroNaN()`. Update log: 2023-09-12: Applies shear correction to every layer of a 3D matrix through the "Go3D" workflow.
- Parameters:
  - `mat3dn`: name of the mat3d to be sheared = "g2_001_G"
  - `refwave`: name of the shear reference wave = "g2_001_T" string barename = replaceString("_G",mat3dn,"_T")
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Shear Correction for C4.ipf:986`

#### `shearxy`

```igor
Function/Wave shearxy(x0,y0,sh,Dis)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `shearxy(x0, y0, sh, Dis)`.
- Notes: Wave-returning helper for symmetry or reflection processing; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `x0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sh`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Dis`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Shear Correction for C4.ipf:1187`

#### `SimlatticebyFit`

```igor
Function SimlatticebyFit(name)
```
- Usage: call as `SimlatticebyFit(name)`.
- Notes: #III.1 Simulate Lattice Body Purpose: fits or extracts spectral/peak parameters. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `mean()`, `cktable()`, `sign()`, `color3s_for3dm()`, `color3s_for3dmLS()`, `ckfig_child()`.
- Parameters:
  - `name`: The name of experimental data [From this wave to extract]
- Source: `Lattice Segregation.ipf:158`

#### `Strain_angle`

```igor
Function Strain_angle(ux,uy,order,mothername,selqmode)
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: call as `Strain_angle(ux, uy, order, mothername, selqmode)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `umatrixsubbackground_c()`, `atan2()`, `projectstrainmatrix_c()`, `Newrotate_forstrain_c()`, `differentiatealongx()`, `differentiatealongy()`.
- Parameters:
  - `ux`: bare displacement matrix Ux
  - `uy`: bare displacement matrix Uy
  - `order`: polynomial order to remove background
  - `mothername`: name of the topography
  - `selqmode`: prompt sel,"Which Q?",popup,"Global;Just Fitted"
- Source: `Lawler-Fujita Drift Correction.ipf:4932`

#### `Strainanalysis`

```igor
Function Strainanalysis(ux,uy,order,mothername,selqmode,frame2ornot,theta)
```
- Usage: call as `Strainanalysis(ux, uy, order, mothername, selqmode, frame2ornot, theta)`.
- Notes: ## MAIN FUNCTION Purpose: applies geometric correction or extracts deformation/strain information. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `umatrixsubbackground()`, `projectstrainmatrix()`, `projectstrainmatrix45()`, `Newrotate_forstrain()`, `differentiatealongx()`, `differentiatealongy()`.
- Parameters:
  - `ux`: bare displacement matrix Ux
  - `uy`: bare displacement matrix Uy
  - `order`: polynomial order to remove background
  - `mothername`: name of the topography
  - `selqmode`: prompt sel,"Which Q?",popup,"Global;Just Fitted"
  - `frame2ornot`: whether or not calculate the second frame, =1 yes; =0 No
  - `theta`: angle of the lattice in topo degree
- Source: `Lawler-Fujita Drift Correction.ipf:4286`

#### `SumCompOrder_square`

```igor
Function SumCompOrder_square(A1,A2,theta)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `SumCompOrder_square(A1, A2, theta)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `cvtcmplx()`, `max()`, `ckfig_child()`.
- Parameters:
  - `A1`: Used in: Display/HOST=#/W=(0,0.05,0.4,0.55);appendimage Square1;ModifyGraph mirror=2;ModifyImage Square1 ctab= {-max(A1,A2),max(A1,A2),VioletOrangeYe / setActiveSubwindow ##;Display/HOST=#/W=(0.3,0.05,0.7,0.55);appendimage Square2;ModifyGraph mirror=2;ModifyImage Square2 ctab= {-max(A1,A2),m
  - `A2`: Used in: Display/HOST=#/W=(0,0.05,0.4,0.55);appendimage Square1;ModifyGraph mirror=2;ModifyImage Square1 ctab= {-max(A1,A2),max(A1,A2),VioletOrangeYe / setActiveSubwindow ##;Display/HOST=#/W=(0.3,0.05,0.7,0.55);appendimage Square2;ModifyGraph mirror=2;ModifyImage Square2 ctab= {-max(A1,A2),m
  - `theta`: Angle parameter used for ARPES momentum conversion or rotation.
- Source: `Models.ipf:11521`

#### `sym1FDplus2asLor`

```igor
Function sym1FDplus2asLor(parwave,x) : fitfunc  //only one lorenztian is asymmetric
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `sym1FDplus2asLor(parwave, x)`.
- Notes: Function for symmetry or reflection processing; Igor wave/matrix/cube data operation.
- Parameters:
  - `parwave`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Pierre's Template.ipf:21721`

#### `symkrtl`

```igor
function symkrtl(n,temp)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `symkrtl(n, temp)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `n`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `temp`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `Miscellaneous_Codes.ipf:2941`

#### `symkrtl_cmplx`

```igor
function symkrtl_cmplx(n,temp)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `symkrtl_cmplx(n, temp)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `n`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `temp`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `Miscellaneous_Codes.ipf:3022`

#### `symkrtr`

```igor
function symkrtr(n,temp)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `symkrtr(n, temp)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `n`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `temp`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `Miscellaneous_Codes.ipf:2917`

#### `symkrtr_cmplx`

```igor
function symkrtr_cmplx(n,temp)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `symkrtr_cmplx(n, temp)`.
- Notes: Function for symmetry or reflection processing; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `n`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `temp`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `Miscellaneous_Codes.ipf:2997`

#### `symmetrizeQPIdall`

```igor
Function symmetrizeQPIdall(name)
```
- Purpose: applies symmetry, mirror, or reflection operations.
- Usage: call as `symmetrizeQPIdall(name)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `D4_sym_3dplot()`, `Mdiag_sym_3dplot()`, `Mx_sym_3dplot()`, `C4_sym_3dplot()`, `margin()`, `color3s_subfor3dFFT()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Symmetrization.ipf:587`

#### `twoDinterpolatexyflf`

```igor
Function twoDinterpolatexyflf(name,xpoint,ypoint)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `twoDinterpolatexyflf(name, xpoint, ypoint)`.
- Notes: Function for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `makematrix2()`, `makematrix3()`, `dilf()`, `color3slf()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Lawler-Fujita Drift Correction.ipf:1078`

#### `umatrixsubbackground`

```igor
Function umatrixsubbackground(ux,uy,order)
```
- Usage: call as `umatrixsubbackground(ux, uy, order)`.
- Notes: Kernel#01 ** Function for remove polynomial background Purpose: smooths, normalizes, or removes background/trend components. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ux`: Used in: CurveFit/Q/W=0 poly2D order, ux/D = destfitw
  - `uy`: Used in: CurveFit/Q/W=0 poly2D order, uy/D = destfitw
  - `order`: =2
- Source: `Lawler-Fujita Drift Correction.ipf:4497`

#### `umatrixsubbackground_c`

```igor
Function umatrixsubbackground_c(ux,uy,order)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `umatrixsubbackground_c(ux, uy, order)`.
- Notes: Function for geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ux`: Used in: CurveFit/Q/W=0 poly2D order, ux/D = destfitw
  - `uy`: Used in: CurveFit/Q/W=0 poly2D order, uy/D = destfitw
  - `order`: =2
- Source: `Lawler-Fujita Drift Correction.ipf:5158`

#### `unpadding`

```igor
Function unpadding(name,winput)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `unpadding(name, winput)`.
- Notes: Function for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: name of the original image which contain the original dimsize before padding
  - `winput`: the padded wave
- Source: `Lawler-Fujita Drift Correction.ipf:402`



## Model Simulation and Theory

### Panel callbacks and control handlers

#### `ButtonProc_appendQPIESimu_getGMcut2`

```igor
Function ButtonProc_appendQPIESimu_getGMcut2(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Append Band`. Code behavior: duplicates or stages waves for downstream processing.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Append Band`
- Source: `Models.ipf:13467`

#### `ButtonProc_Batchscalingoure`

```igor
Function ButtonProc_Batchscalingoure(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Batchscalingoure()`.
- Notes: Scaling 2D Panel control(s): `2D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2D`
- Source: `Models.ipf:395`

#### `ButtonProc_calculateband`

```igor
Function ButtonProc_calculateband(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `calculateband()`.
- Notes: Ref: arXiv:1802.01376v1 This code is compiled to show a 2D (kx,ky,0) eigne value. If you want to get band along high symmetry line please check the code and change the k-space range setting to what you want e.g. G-M you need ky=kx G-X you need ky=0 Panel control(s): `Band abinitio`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Band abinitio`
- Source: `Models.ipf:154`

#### `ButtonProc_CalculateP0Ec`

```igor
Function ButtonProc_CalculateP0Ec(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CalculateP0Ec()`.
- Notes: Ref: Ast, C., Jäck, B., Senkpiel, J. et al. Sensing the quantum limit in scanning tunnelling spectroscopy. Nat Commun 7, 13009 (2016).
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `P(E) Theory`
- Source: `ModelingTunneling.ipf:25`

#### `ButtonProc_CdGMDirac`

```igor
Function ButtonProc_CdGMDirac(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `CdGM_Dirac_singlevortex()`.
- Notes: Here is the procedure to calcultate energe level of CdGM states related to single quantized vortex (voeticity n=1), in a superconducting Dirac Material Refenrence Eq (20) in PHYSICAL REVIEW B 79, 224506 (2009) Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `CdGM`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CdGM`
- Source: `Models.ipf:598`

#### `ButtonProc_Cons3dplotlcf2`

```igor
Function ButtonProc_Cons3dplotlcf2(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Launch arbitary Linecut extraction (Z is horizental) }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Linecut`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartppf()`, `findendppf()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqqf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut`
- Source: `Models.ipf:1067`

#### `ButtonProc_Cons3dplotlcf2SC`

```igor
Function ButtonProc_Cons3dplotlcf2SC(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Launch arbitary Linecut extraction (Z is horizental) }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Linecut`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartppf()`, `findendppf()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqqf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut`
- Source: `Models.ipf:1757`

#### `ButtonProc_Cons3dplotlcf3`

```igor
Function ButtonProc_Cons3dplotlcf3(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Linecut`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartppf()`, `findendppf()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqqf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut`
- Source: `Models.ipf:11295`

#### `ButtonProc_Cons3dplotlcfmodel`

```igor
Function ButtonProc_Cons3dplotlcfmodel(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Linecut`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartppf()`, `findendppf()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqqf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut`
- Source: `SmartDisplay.ipf:5278`

#### `ButtonProc_Cons3dplotZ`

```igor
Function ButtonProc_Cons3dplotZ(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Launch arbitary Linecut extraction (Z is horizental) }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Linecut`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `findstartppf()`, `findendppf()`, `grabwin()`, `lsize()`, `rgb()`, `findstartqqf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut`, `Linecut`, `Linecut`, `Linecut`
- Source: `Models.ipf:2448`

#### `ButtonProc_convallc`

```igor
Function ButtonProc_convallc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `convallc()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `T-conv batch`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `T-conv batch`
- Source: `General_Simu.ipf:1726`

#### `ButtonProc_convolvetempc`

```igor
Function ButtonProc_convolvetempc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `convolvetempc()`.
- Notes: Convolute any curve by temperature broadening Panel control(s): `Temp convolve`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Temp convolve`
- Source: `General_Simu.ipf:1658`

#### `ButtonProc_fanoline`

```igor
Function ButtonProc_fanoline(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `fanoline()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Fano`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Fano`
- Source: `Models.ipf:738`

#### `ButtonProc_FeSC_normal`

```igor
Function ButtonProc_FeSC_normal(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: TB FeSC Fermi Surface Purpose: smooths, normalizes, or removes background/trend components. Panel control(s): `FeSC FS`. Main internal calls: `FeSC_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FeSC FS`
- Source: `Models.ipf:11056`

#### `ButtonProc_fourbandkpFeSC`

```igor
Function ButtonProc_fourbandkpFeSC(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Gamma kp model used in Nat. Phys. 15, 41 (2019) Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `FeSC(Γ k.p)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FeSC(Γ k.p)`
- Source: `Models.ipf:2273`

#### `ButtonProc_gapdisGua2_forPDMsimu`

```igor
Function ButtonProc_gapdisGua2_forPDMsimu(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Extract Delta( / m Gaus)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Extract Delta( / m Gaus)`
- Source: `General_Simu.ipf:1040`

#### `ButtonProc_gapdisGua2_forPDMsimuc2`

```igor
Function ButtonProc_gapdisGua2_forPDMsimuc2(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Extract Delta(g_{ / m max})`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Extract Delta(g_{ / m max})`
- Source: `General_Simu.ipf:1236`

#### `ButtonProc_gaph_forPDMsimu`

```igor
Function ButtonProc_gaph_forPDMsimu(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Panel control(s): `Extract g(Delta)`. Main internal calls: `extracthight_PDMsimu()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Extract g(Delta)`
- Source: `General_Simu.ipf:1199`

#### `ButtonProc_getparameterPDMsimu`

```igor
Function ButtonProc_getparameterPDMsimu(ctrlName) : ButtonControl
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Print Parameters`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Print Parameters`
- Source: `General_Simu.ipf:1588`

#### `ButtonProc_HaldaneA`

```igor
Function ButtonProc_HaldaneA(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Build Haldane Spectral Function Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `A(k,ω)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `A(k,ω)`
- Source: `Models.ipf:3185`

#### `ButtonProc_HaldaneBC`

```igor
Function ButtonProc_HaldaneBC(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Calculate Berry Curvature Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `Ω(k)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ω(k)`
- Source: `Models.ipf:3281`

#### `ButtonProc_HaldaneCons`

```igor
Function ButtonProc_HaldaneCons(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Interactive Tuning Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `Haldane Model`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Haldane Model`
- Source: `Models.ipf:3497`

#### `ButtonProc_LDOSQPIESimu`

```igor
Function ButtonProc_LDOSQPIESimu(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Panel control(s): `LDOS`. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LDOS`
- Source: `Models.ipf:13475`

#### `ButtonProc_LFuModel`

```igor
Function ButtonProc_LFuModel(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `makeu1u2()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Profile`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Profile`
- Source: `Miscellaneous_Codes.ipf:6336`

#### `ButtonProc_make3DfakeSISTMc`

```igor
Function ButtonProc_make3DfakeSISTMc(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Panel control(s): `Make 3D`. Main internal calls: `make3DfakeSISTM()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make 3D`
- Source: `General_Simu.ipf:899`

#### `ButtonProc_make3DfakeSISTMcconv`

```igor
Function ButtonProc_make3DfakeSISTMcconv(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Show Conv`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show Conv`
- Source: `General_Simu.ipf:1034`

#### `ButtonProc_make3DfakeSISTMcraw`

```igor
Function ButtonProc_make3DfakeSISTMcraw(ctrlName) : ButtonControl
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Show Raw`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Show Raw`
- Source: `General_Simu.ipf:1028`

#### `ButtonProc_MZMscaling_poison`

```igor
Function ButtonProc_MZMscaling_poison(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Batchscaling()`.
- Notes: Scaling+Poison output Gs (2e2/h) vs Gamma (meV) curve under different poiosning rate Batchscaling(0.01,10,0.4,0,0.1,5,1000) Panel control(s): `Scal.+Poison`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Scal.+Poison`
- Source: `Models.ipf:478`

#### `ButtonProc_nonsinodal`

```igor
Function ButtonProc_nonsinodal(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `nonsinodal()`.
- Notes: The nonsinodal CPR from andreev bound state, the model is from PHYSICAL REVIEW B 100, 064523 (2019) Panel control(s): `CPR`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CPR`
- Source: `Models.ipf:915`

#### `ButtonProc_oneUCFeSegapsimu`

```igor
Function ButtonProc_oneUCFeSegapsimu(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `oneUCFeSegapsimu()`.
- Notes: Plot the 3D surface Gizmo for the 1UC-FeSe gap distribution. Gap function is from ref: https://journals.aps.org/prl/abstract/10.1103/PhysRevLett.117.117001 Panel control(s): `∆(1UC-FeSe)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `∆(1UC-FeSe)`
- Source: `Models.ipf:13756`

#### `ButtonProc_pdmsimu_example1`

```igor
Function ButtonProc_pdmsimu_example1(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: cdynetest=exp(-x^2/(FWHM/(2*sqrt(ln(2)))^2)) Examples of parameters Case#1 only have in-phase gap size modulation (no peak height modulation ) Panel control(s): `1`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `1`
- Source: `General_Simu.ipf:1326`

#### `ButtonProc_pdmsimu_example2`

```igor
Function ButtonProc_pdmsimu_example2(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#2 No gap modulation but severe out-of-phase height modulation Panel control(s): `2`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2`
- Source: `General_Simu.ipf:1340`

#### `ButtonProc_pdmsimu_example3`

```igor
Function ButtonProc_pdmsimu_example3(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#3 Case of 1UC-FeSe case 1, gap in-phase/ height out-of-phase Panel control(s): `3`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3`
- Source: `General_Simu.ipf:1356`

#### `ButtonProc_pdmsimu_example4`

```igor
Function ButtonProc_pdmsimu_example4(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#4 only have in-phase gap size modulation (no peak height modulation ) Panel control(s): `4`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `4`
- Source: `General_Simu.ipf:1371`

#### `ButtonProc_pdmsimu_example5`

```igor
Function ButtonProc_pdmsimu_example5(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#5 in-phase gap size modulation/ out-of-phase peak height modulation Panel control(s): `5`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `5`
- Source: `General_Simu.ipf:1387`

#### `ButtonProc_pdmsimu_example6`

```igor
Function ButtonProc_pdmsimu_example6(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#6 single gap Panel control(s): `6`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `6`
- Source: `General_Simu.ipf:1402`

#### `ButtonProc_pdmsimu_example7`

```igor
Function ButtonProc_pdmsimu_example7(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#7 maybe our case? with correct peak height Panel control(s): `7`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `7`
- Source: `General_Simu.ipf:1417`

#### `ButtonProc_pdmsimu_example8`

```igor
Function ButtonProc_pdmsimu_example8(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Case#8 special Panel control(s): `8`. Code behavior: reads or updates panel controls. Main internal calls: `pdmsimu_example()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `8`
- Source: `General_Simu.ipf:1432`

#### `ButtonProc_PE_DCB`

```igor
Function ButtonProc_PE_DCB(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Main internal calls: `SimulateDCB_IV()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `ModelingTunneling.ipf:662`

#### `ButtonProc_PE_dcbbatchexample`

```igor
Function ButtonProc_PE_dcbbatchexample(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for ARPES-style loading, plotting, or momentum conversion. Panel control(s): `e.g.`. Main internal calls: `CalculateDCB()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `e.g.`
- Source: `ModelingTunneling.ipf:888`

#### `ButtonProc_PE_save2`

```igor
Function ButtonProc_PE_save2(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `ModelingTunneling.ipf`. Panel control(s): `Examaple / #2`. Main internal calls: `CalculateP0E_auto()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Examaple / #2`
- Source: `ModelingTunneling.ipf:703`

#### `ButtonProc_PE_save3`

```igor
Function ButtonProc_PE_save3(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `ModelingTunneling.ipf`. Panel control(s): `Examaple / #3`. Main internal calls: `CalculateP0E_auto()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Examaple / #3`
- Source: `ModelingTunneling.ipf:741`

#### `ButtonProc_PE_save4`

```igor
Function ButtonProc_PE_save4(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `ModelingTunneling.ipf`. Panel control(s): `Examaple / #4`. Main internal calls: `CalculateP0E_auto()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Examaple / #4`
- Source: `ModelingTunneling.ipf:778`

#### `ButtonProc_PE_saveantenamode`

```igor
Function ButtonProc_PE_saveantenamode(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `ModelingTunneling.ipf`. Panel control(s): `Examaple / #1`. Main internal calls: `CalculateP0E_auto()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Examaple / #1`
- Source: `ModelingTunneling.ipf:621`

#### `ButtonProc_PRB_98_214503_n`

```igor
Function ButtonProc_PRB_98_214503_n(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: This is the simulation of normal bands in PRB 98,214503 (2018) This is the eignevalue of equation (1) Panel control(s): `FeSC(M k.p)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FeSC(M k.p)`
- Source: `Models.ipf:990`

#### `ButtonProc_predictLL`

```igor
Function ButtonProc_predictLL(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `calculateLLNewc()`.
- Notes: Simulate Landau Level Landau level prediction. valid when 2D dirac cone istropic. Panel control(s): `LL`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LL`
- Source: `Models.ipf:4216`

#### `ButtonProc_QHZ`

```igor
Function ButtonProc_QHZ(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Cons_QHZcut2c()`.
- Notes: Interactive Tune band structure Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `Qi-Hughes-Zhang`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Qi-Hughes-Zhang`
- Source: `Models.ipf:5632`

#### `ButtonProc_QHZ_bandemhq`

```igor
Function ButtonProc_QHZ_bandemhq(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for topological-model simulation; graph display, formatting, or window management. Panel control(s): `HQ`. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations. Main internal calls: `Solveedgestate_QHZhq_cmplx()`, `Solveedgestate_QHZhq()`, `displaymultiFQHZ()`, `userticks()`, `rgb()`, `ckfig()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `HQ`
- Source: `Models.ipf:8822`

#### `ButtonProc_QHZ_LDOSE`

```igor
Function ButtonProc_QHZ_LDOSE(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation; graph display, formatting, or window management. Panel control(s): `LDOS(E)`. Main internal calls: `QHZLDOSindex()`, `QHZLDOS()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LDOS(E)`
- Source: `Models.ipf:9428`

#### `ButtonProc_QHZ_LDOSEawrhq`

```igor
Function ButtonProc_QHZ_LDOSEawrhq(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Panel control(s): `HQ`. Main internal calls: `QHZslabcut_cmplx()`, `QHZslabcut()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `HQ`
- Source: `Models.ipf:9667`

#### `ButtonProc_QHZ_LDOSEawrrn`

```igor
Function ButtonProc_QHZ_LDOSEawrrn(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Rename`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rename`
- Source: `Models.ipf:9705`

#### `ButtonProc_QHZ_LDOSindex`

```igor
Function ButtonProc_QHZ_LDOSindex(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation; graph display, formatting, or window management. Panel control(s): `LDOS(index)`. Code behavior: reads or updates panel controls. Main internal calls: `QHZLDOSindex()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LDOS(index)`
- Source: `Models.ipf:9399`

#### `ButtonProc_QHZ_LDOSindexHQ`

```igor
Function ButtonProc_QHZ_LDOSindexHQ(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation; graph display, formatting, or window management. Main internal calls: `QHZLDOSindex()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Models.ipf:9414`

#### `ButtonProc_QHZband`

```igor
Function ButtonProc_QHZband(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for topological-model simulation. Panel control(s): `3D`. Main internal calls: `QHZSpectralFunction()`, `QWZSpectralFunction2()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3D`
- Source: `Models.ipf:5750`

#### `ButtonProc_QHZChernd0`

```igor
Function ButtonProc_QHZChernd0(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Calculate Chern number when d = 0 Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `Chern(Δ=0)`. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `QHZ_d0pBerryCurvature()`, `QHZ_d0mBerryCurvature()`, `ckfig_child()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Chern(Δ=0)`
- Source: `Models.ipf:6840`

#### `ButtonProc_QHZChernd0l1`

```igor
Function ButtonProc_QHZChernd0l1(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; topological-model simulation. Panel control(s): `Z(m,Δ=0,μ=μ0)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Z(m,Δ=0,μ=μ0)`
- Source: `Models.ipf:7231`

#### `ButtonProc_QHZChernd0l2`

```igor
Function ButtonProc_QHZChernd0l2(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; topological-model simulation. Panel control(s): `Z(μ,Δ=0,m=m0)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Z(μ,Δ=0,m=m0)`
- Source: `Models.ipf:7235`

#### `ButtonProc_QHZChernm0`

```igor
Function ButtonProc_QHZChernm0(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Calculate Chern number when m = 0 Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `Chern(m=0)`. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `QHZ_hpBerryCurvature()`, `QHZ_hmBerryCurvature()`, `ckfig_child()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Chern(m=0)`
- Source: `Models.ipf:5936`

#### `ButtonProc_QHZChernm02`

```igor
Function ButtonProc_QHZChernm02(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for topological-model simulation. Panel control(s): `Z(Δ,m=0)`. Main internal calls: `QHZeq8Chernline()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Z(Δ,m=0)`
- Source: `Models.ipf:5954`

#### `ButtonProc_QHZedge`

```igor
Function ButtonProc_QHZedge(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; topological-model simulation. Panel control(s): `Slab`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Slab`
- Source: `Models.ipf:8674`

#### `ButtonProc_QHZLDOSCall`

```igor
Function ButtonProc_QHZLDOSCall(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Calculate the LDOS of Edge mode, continue slab calculation Calculate Egienvalue index resolved LDOS Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `*(LDOS)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `*(LDOS)`
- Source: `Models.ipf:8946`

#### `ButtonProc_QHZslabcutc`

```igor
Function ButtonProc_QHZslabcutc(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Calculate the Spectral function with layer weight Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `*(A[k,ω,L])`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `*(A[k,ω,L])`
- Source: `Models.ipf:9447`

#### `ButtonProc_QPIESimutemp1`

```igor
Function ButtonProc_QPIESimutemp1(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Temp#h`. Main internal calls: `QPIESimu2Dband_interact()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Temp#h`
- Source: `Models.ipf:12763`

#### `ButtonProc_QPIESimutemp2`

```igor
Function ButtonProc_QPIESimutemp2(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Temp#e-h`. Main internal calls: `QPIESimu2Dband_interact()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Temp#e-h`
- Source: `Models.ipf:12875`

#### `ButtonProc_QPIESimutemp3`

```igor
Function ButtonProc_QPIESimutemp3(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Temp#e`. Main internal calls: `QPIESimu2Dband_interact()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Temp#e`
- Source: `Models.ipf:12819`

#### `ButtonProc_QPISIMUNew`

```igor
Function ButtonProc_QPISIMUNew(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: New simultaneous simulation of Spectral function of QPI 2 band tight-binding model Gamma hole & M electron Panel control(s): `FeSC-A(ω,k)+LDOS+QPI / Two bands TB`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `FeSC-A(ω,k)+LDOS+QPI / Two bands TB`
- Source: `Models.ipf:12343`

#### `ButtonProc_QWZ3d`

```igor
Function ButtonProc_QWZ3d(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for topological-model simulation; Igor wave/matrix/cube data operation. Panel control(s): `3D`. Main internal calls: `QWZSpectralFunction()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `3D`
- Source: `Models.ipf:5008`

#### `ButtonProc_QWZChern`

```igor
Function ButtonProc_QWZChern(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for topological-model simulation. Panel control(s): `Chern`. Main internal calls: `QWZChernline()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Chern`
- Source: `Models.ipf:5018`

#### `ButtonProc_QWZCons`

```igor
Function ButtonProc_QWZCons(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #02 Interactive Link Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `Qi-Wu-Zhang`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Qi-Wu-Zhang`
- Source: `Models.ipf:5003`

#### `ButtonProc_RCSJ`

```igor
Function ButtonProc_RCSJ(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `RCSJc()`.
- Notes: Simulate RCSJ Model for Supercurrent Panel control(s): `RCSJ Model`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `RCSJ Model`
- Source: `ModelingTunneling.ipf:1371`

#### `ButtonProc_S_Svortex`

```igor
Function ButtonProc_S_Svortex(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `automultiSV()`.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `S/Vortex Ln`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `S/Vortex Ln`
- Source: `Models.ipf:254`

#### `ButtonProc_S_Svortexindiv`

```igor
Function ButtonProc_S_Svortexindiv(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `indivlinecut()`.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Ind. S/Vortex`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ind. S/Vortex`
- Source: `Models.ipf:348`

#### `ButtonProc_simuCDWc`

```igor
Function ButtonProc_simuCDWc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `simuCDWc()`.
- Notes: Simulation for NbSe2 STM data, Science 372,1447 (2021) Panel control(s): `{ / m NbSe}_{2}`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `{ / m NbSe}_{2}`
- Source: `Models.ipf:13616`

#### `ButtonProc_SimuPDW1Dc`

```igor
Function ButtonProc_SimuPDW1Dc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `SimuPDW1Dc()`.
- Notes: Simulation of 1D PDW (only Qx) linecut and corresponding 2D PDW map (Qx and Qy) Panel control(s): `PDW Linecut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `PDW Linecut`
- Source: `Models.ipf:11785`

#### `ButtonProc_SimuPDW2Dc`

```igor
Function ButtonProc_SimuPDW2Dc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `SimuPDW2Dc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `Make3D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Make3D`
- Source: `Models.ipf:12014`

#### `ButtonProc_simupdwwithw`

```igor
Function ButtonProc_simupdwwithw(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `simupdwwithw()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `OLD`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `OLD`
- Source: `Models.ipf:11752`

#### `ButtonProc_Solveedgestate_QHZc`

```igor
Function ButtonProc_Solveedgestate_QHZc(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction; topological-model simulation. Panel control(s): `*(Slab)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `*(Slab)`
- Source: `Models.ipf:8562`

#### `ButtonProc_sum2dlinecutc`

```igor
Function ButtonProc_sum2dlinecutc(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `sum2dlinecutc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `It2/1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `It2/1`
- Source: `Models.ipf:11909`

#### `ButtonProc_SumCompOrder`

```igor
Function ButtonProc_SumCompOrder(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `SumCompOrder()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Multi.Order`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Multi.Order`
- Source: `Models.ipf:11485`

#### `ButtonProc_Svortex`

```igor
Function ButtonProc_Svortex(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `DyneModbyvortex()`.
- Notes: Panel button callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `BCSVortex line`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BCSVortex line`
- Source: `Models.ipf:682`

#### `ButtonProc_TBGsimu`

```igor
Function ButtonProc_TBGsimu(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `TBGsimu()`.
- Notes: Simulation of TBG and TTG lattice Panel control(s): `TBG`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `TBG`
- Source: `General_Simu.ipf:1801`

#### `ButtonProc_tbmodel`

```igor
Function ButtonProc_tbmodel(ctrlName) : ButtonControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: triggered as a wrapper that opens/runs `tbmodel()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `TB_Cprt`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `TB_Cprt`
- Source: `Models.ipf:770`

#### `ButtonProc_tbmodelIBSC`

```igor
Function ButtonProc_tbmodelIBSC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `fourbandmodel()`.
- Notes: a tight binding model used in arXiv:1808.08390, and builded in Phys. Rev. X 2, 021009 (2012), to simulate the band structure of Iron-based superconductor. In this model only four bands were considered, in which on double degenerate hole band around gamma point Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `TB_FeSC`
- Source: `Models.ipf:847`

#### `ButtonProc_TTGsimu`

```igor
Function ButtonProc_TTGsimu(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `TTGsimu()`.
- Notes: Panel button callback for linecut, slice, or region extraction; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `TTG`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `TTG`
- Source: `General_Simu.ipf:1930`

#### `ButtonProc_vortexantivortexsimuc`

```igor
Function ButtonProc_vortexantivortexsimuc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `vortexantivortexsimuc()`.
- Notes: Ref: PHYSICAL REVIEW B 93, 064522 (2016) https://journals.aps.org/prb/pdf/10.1103/PhysRevB.93.064522 eq. 5b Panel control(s): `vortex pair`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `vortex pair`
- Source: `Models.ipf:10612`

#### `PopMenuProc_simuSISTM1`

```igor
Function PopMenuProc_simuSISTM1(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: String varName variable/G dphih2_simuSISTM = varNum*pi pi //phi of the gap size 1 controlsimuSISTM1() end Panel control(s): `Convolute`, `Convolve`. Main internal calls: `controlsimuSISTM1()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `Convolute`, `Convolve`
- Source: `General_Simu.ipf:620`

#### `PopMenuProc_simuSISTMSeFe`

```igor
Function PopMenuProc_simuSISTMSeFe(ctrlName,popNum,popStr) : PopupMenuControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel popup-menu callback for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `r(Delta_{ / m max})`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `popNum`: Selected popup-menu item index.
  - `popStr`: Selected popup-menu item text.
- Panel controls: `r(Delta_{ / m max})`, `r(Delta_{ / m max})`
- Source: `General_Simu.ipf:524`

#### `SetVarProc_addX3dplotf2`

```igor
Function SetVarProc_addX3dplotf2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##02 Control Function:Change addX, Call to change LinecutH and change the indicative line Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `LV`. Main internal calls: `anglelinecutVf()`, `angleline_3dpf2()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `LV`, `LV`, `LV`, `LV`, `LV`
- Source: `Models.ipf:1246`

#### `SetVarProc_addY3dplotf2`

```igor
Function SetVarProc_addY3dplotf2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##01 Control Function:Change addY, Call to change LinecutH and change the indicative line Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `LH`. Main internal calls: `anglelinecutHf()`, `angleline_3dpf2()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `LH`, `LH`, `LH`, `LH`, `LH`
- Source: `Models.ipf:1221`

#### `SetVarProc_aph`

```igor
Function SetVarProc_aph(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis. Panel control(s): `a(Å)`, `θ(˚)`, `dx(Å)`, `dy(Å)`. Main internal calls: `Fitimagehoneycomb()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a(Å)`, `θ(˚)`, `dx(Å)`, `dy(Å)`
- Source: `General_Simu.ipf:2298`

#### `SetVarProc_aptri`

```igor
Function SetVarProc_aptri(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis. Panel control(s): `a(Å)`, `θ(˚)`, `dx(Å)`, `dy(Å)`. Main internal calls: `Fitimagesquare()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a(Å)`, `θ(˚)`, `dx(Å)`, `dy(Å)`
- Source: `General_Simu.ipf:2564`

#### `SetVarProc_Cons3dplot2`

```igor
Function SetVarProc_Cons3dplot2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; topological-model simulation. Panel control(s): `Z`. Main internal calls: `tpw()`, `Cons3dplotc()`, `Cons3dplotcQWZ()`, `grabwinnonew()`, `Cons3dplotcf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `Models.ipf:9813`

#### `SetVarProc_Cons3dplotFeSC_normalc`

```igor
Function SetVarProc_Cons3dplotFeSC_normalc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Panel control(s): `E'`. Main internal calls: `updateFeSC_normalFS()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `E'`
- Source: `Models.ipf:11132`

#### `SetVarProc_Cons3dplotpdw`

```igor
Function SetVarProc_Cons3dplotpdw(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Z`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_zeroNaN()`, `max()`, `grabwinchild()`, `color3s_for3d()`, `checkmultiopen()`, `color3s_for3dm()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `Models.ipf:12061`

#### `SetVarProc_Cons3dplotprbc`

```igor
Function SetVarProc_Cons3dplotprbc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Control the simulation parameters }}}}}}}}}} Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Z`. Main internal calls: `updatePRB_98_214503_normalFS()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Z`
- Source: `Models.ipf:1388`

#### `SetVarProc_ConstQPIsimuand2bands`

```igor
Function SetVarProc_ConstQPIsimuand2bands(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Panel control(s): `Im∑`, `N1/S2`, `NxN`, `BandShape(h)`, `Δ(h)`, `μ(h)`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `QPIESimu2Dband_interact()`, `max()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Im∑`, `N1/S2`, `NxN`, `BandShape(h)`, `Δ(h)`, `μ(h)`, `+M(E,A)_h`, `+M(E,σ)_h`
- Source: `Models.ipf:12931`

#### `SetVarProc_ConstQPIsimuand2bandsFS`

```igor
Function SetVarProc_ConstQPIsimuand2bandsFS(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Panel control(s): `E`. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `QPIESimu2Dband_interactFS()`, `max()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `E`
- Source: `Models.ipf:12955`

#### `SetVarProc_DiracLL`

```igor
Function SetVarProc_DiracLL(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for ARPES-style loading, plotting, or momentum conversion. Panel control(s): `Velocity (meV*A)`, `B (T)`, `ED (meV)`, `Num`. Main internal calls: `calculateLLNewcons()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Velocity (meV*A)`, `B (T)`, `ED (meV)`, `Num`
- Source: `Models.ipf:4323`

#### `SetVarProc_FeSC_normal`

```igor
Function SetVarProc_FeSC_normal(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `t1`, `t2`, `t`, `t'`, `u'`, `Im(∑)''`. Main internal calls: `updateFeSC_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t1`, `t2`, `t`, `t'`, `u'`, `Im(∑)''`, `Re(∑)'`
- Source: `Models.ipf:11182`

#### `SetVarProc_FTstripesimu`

```igor
Function SetVarProc_FTstripesimu(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Panel control(s): `P1`, `Q1`, `P2`, `Q2`, `L(X)`, `L(Y)`. Main internal calls: `FFTstripesimulation_cons()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `P1`, `Q1`, `P2`, `Q2`, `L(X)`, `L(Y)`, `Background`, `σ(noise)`
- Source: `Models.ipf:13577`

#### `SetVarProc_haldane`

```igor
Function SetVarProc_haldane(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: #02 Interactive Link Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Panel control(s): `t1`, `t2`, `M`, `phi`, `Im(Σ)`, `Update`. Main internal calls: `Cons_Haldanecut()`, `Cons_HaldaneFS()`, `cons_HaldaneBC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `t1`, `t2`, `M`, `phi`, `Im(Σ)`, `Update`, `HQ`
- Source: `Models.ipf:3552`

#### `SetVarProc_lamda_fesc2d`

```igor
Function SetVarProc_lamda_fesc2d(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Igor wave/matrix/cube data operation. Panel control(s): `λ1`, `λ2`, `λ3`, `λR`. Main internal calls: `updatefourbandFeSC2D()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `λ1`, `λ2`, `λ3`, `λR`
- Source: `Models.ipf:2823`

#### `SetVarProc_pe`

```igor
Function SetVarProc_pe(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Panel control(s): `Antenna on?`, `omega_{0}^{ / m{Antenna}} (ueV)`, `alpha`, `C_J (fF)`, `I_c (nA)`, `R(omega = 0) (Ω)`. Main internal calls: `SimulateDCB_IV()`, `CalculateP0E_auto()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Antenna on?`, `omega_{0}^{ / m{Antenna}} (ueV)`, `alpha`, `C_J (fF)`, `I_c (nA)`, `R(omega = 0) (Ω)`, `T (K)`, `Data range: ±E_{ / m{max}} (ueV)`
- Source: `ModelingTunneling.ipf:253`

#### `SetVarProc_peic`

```igor
Function SetVarProc_peic(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; differentiates wave data. Main internal calls: `SimulateDCB_IV()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Source: `ModelingTunneling.ipf:180`

#### `SetVarProc_PRB_98_214503_normala`

```igor
Function SetVarProc_PRB_98_214503_normala(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `a`. Main internal calls: `updatePRB_98_214503_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a`
- Source: `Models.ipf:1418`

#### `SetVarProc_PRB_98_214503_normalm`

```igor
Function SetVarProc_PRB_98_214503_normalm(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `m`. Main internal calls: `updatePRB_98_214503_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `m`
- Source: `Models.ipf:1410`

#### `SetVarProc_PRB_98_214503_normalselfi`

```igor
Function SetVarProc_PRB_98_214503_normalselfi(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `∑''`. Main internal calls: `updatePRB_98_214503_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `∑''`
- Source: `Models.ipf:1435`

#### `SetVarProc_PRB_98_214503_normalselfr`

```igor
Function SetVarProc_PRB_98_214503_normalselfr(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `∑'`. Main internal calls: `updatePRB_98_214503_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `∑'`
- Source: `Models.ipf:1443`

#### `SetVarProc_PRB_98_214503_normalu`

```igor
Function SetVarProc_PRB_98_214503_normalu(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `µ`. Main internal calls: `updatePRB_98_214503_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `µ`
- Source: `Models.ipf:1402`

#### `SetVarProc_PRB_98_214503_normalvsoc`

```igor
Function SetVarProc_PRB_98_214503_normalvsoc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for smoothing, normalization, or background removal. Panel control(s): `Vsoc`. Main internal calls: `updatePRB_98_214503_normal()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Vsoc`
- Source: `Models.ipf:1426`

#### `SetVarProc_PRB_98_214503_SCa`

```igor
Function SetVarProc_PRB_98_214503_SCa(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `a`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a`
- Source: `Models.ipf:1943`

#### `SetVarProc_PRB_98_214503_SCd0`

```igor
Function SetVarProc_PRB_98_214503_SCd0(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `∆`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `∆`
- Source: `Models.ipf:1985`

#### `SetVarProc_PRB_98_214503_SCd2`

```igor
Function SetVarProc_PRB_98_214503_SCd2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `∆`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `∆`
- Source: `Models.ipf:1976`

#### `SetVarProc_PRB_98_214503_SCk0`

```igor
Function SetVarProc_PRB_98_214503_SCk0(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `k'`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `k'`
- Source: `Models.ipf:1993`

#### `SetVarProc_PRB_98_214503_SCm`

```igor
Function SetVarProc_PRB_98_214503_SCm(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `m`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `m`
- Source: `Models.ipf:1935`

#### `SetVarProc_PRB_98_214503_SCselfi`

```igor
Function SetVarProc_PRB_98_214503_SCselfi(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `∑''`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `∑''`
- Source: `Models.ipf:1960`

#### `SetVarProc_PRB_98_214503_SCselfr`

```igor
Function SetVarProc_PRB_98_214503_SCselfr(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `∑'`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `∑'`
- Source: `Models.ipf:1968`

#### `SetVarProc_PRB_98_214503_SCu`

```igor
Function SetVarProc_PRB_98_214503_SCu(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `µ`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `µ`
- Source: `Models.ipf:1927`

#### `SetVarProc_PRB_98_214503_SCvsoc`

```igor
Function SetVarProc_PRB_98_214503_SCvsoc(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `Vsoc`. Main internal calls: `updatePRB_98_214503_SC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Vsoc`
- Source: `Models.ipf:1951`

#### `SetVarProc_QHZ_band`

```igor
Function SetVarProc_QHZ_band(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; topological-model simulation. Panel control(s): `A`, `B`, `M`, `μ`, `Re(Δ)`, `Im(Δ)`. Main internal calls: `Cons_QHZcut()`, `Cons_QHZcut2()`, `grabwinnonew()`, `Solveedgestate_QHZ_cmplx()`, `Solveedgestate_QHZ()`, `effQHZ_LDOSEawr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `A`, `B`, `M`, `μ`, `Re(Δ)`, `Im(Δ)`, `Im(Δ)`, `Im(Σ)`
- Source: `Models.ipf:5885`

#### `SetVarProc_QHZ_bandem`

```igor
Function SetVarProc_QHZ_bandem(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Panel control(s): `A`, `B`, `μ`. Main internal calls: `Solveedgestate_QHZ_cmplx()`, `Solveedgestate_QHZ()`, `Cons_QHZcut()`, `Cons_QHZcut2()`, `effQHZ_LDOSEawr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `A`, `B`, `μ`
- Source: `Models.ipf:8909`

#### `SetVarProc_QHZ_LDOSEawr`

```igor
Function SetVarProc_QHZ_LDOSEawr(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Panel control(s): `A(ω,k,i)`, `A(Update)`. Main internal calls: `effQHZ_LDOSEawr()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `A(ω,k,i)`, `A(Update)`
- Source: `Models.ipf:9603`

#### `SetVarProc_QPIrangesimu`

```igor
Function SetVarProc_QPIrangesimu(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; lattice, moire, or twist-angle simulation/analysis. Panel control(s): `Latt. a (Å)`, `Latt.Q (°)`, `Grid (NxN)`, `Lenght (nm x nm)`, `λ(a0)`. Code behavior: creates output waves.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Latt. a (Å)`, `Latt.Q (°)`, `Grid (NxN)`, `Lenght (nm x nm)`, `λ(a0)`
- Source: `Models.ipf:12206`

#### `SetVarProc_QWZ`

```igor
Function SetVarProc_QWZ(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Panel control(s): `A`, `B`, `M`, `μ`, `Im(Σ)`, `Update`. Main internal calls: `Cons_QWZcut()`, `Cons_QWZFS()`, `cons_QWZBC()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `A`, `B`, `M`, `μ`, `Im(Σ)`, `Update`, `HQ`
- Source: `Models.ipf:5029`

#### `SetVarProc_RCSJ`

```igor
Function SetVarProc_RCSJ(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `ModelingTunneling.ipf`. Panel control(s): `T (K)`, `beta_{c}`, `smooth`. Main internal calls: `SimulateRCSJ_IV_Hysteresis_Realistic()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `T (K)`, `beta_{c}`, `smooth`
- Source: `ModelingTunneling.ipf:1446`

#### `SetVarProc_RCSJ2`

```igor
Function SetVarProc_RCSJ2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Igor wave/matrix/cube data operation. Panel control(s): `I/ I_{c}`, `T_{Voltage only} (K)`. Main internal calls: `SimulateRCSJ_voltage_vs_Time()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `I/ I_{c}`, `T_{Voltage only} (K)`
- Source: `ModelingTunneling.ipf:1466`

#### `SetVarProc_rotate3dplotf2`

```igor
Function SetVarProc_rotate3dplotf2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: ##03 Control Function:Change Rotation, Call to change LinecutH, LinecutV and change the indicative line, and Setvariable range setaddX/setaddY Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Rotate`. Code behavior: duplicates or stages waves for downstream processing; reads or updates panel controls. Main internal calls: `anglelinecutHf()`, `anglelinecutVf()`, `angleline_3dpf2()`, `findrangeforangle_LHf()`, `grabwin()`, `findrangeforangle_LVf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotate`, `Rotate`, `Rotate`, `Rotate`, `Rotate`
- Source: `Models.ipf:1271`

#### `SetVarProc_rotate3dplotf22`

```igor
Function SetVarProc_rotate3dplotf22(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Panel control(s): `Rotate`, `LH`, `LV`. Code behavior: assigns Igor axis scaling/units to the output. Main internal calls: `angleline_3dpf2()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotate`, `LH`, `LV`
- Source: `Models.ipf:1580`

#### `SetVarProc_rotate3dplotf22SC`

```igor
Function SetVarProc_rotate3dplotf22SC(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Panel control(s): `Rotate`, `LH`, `LV`. Code behavior: assigns Igor axis scaling/units to the output. Main internal calls: `angleline_3dpf2()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotate`, `LH`, `LV`
- Source: `Models.ipf:2150`

#### `SetVarProc_SimPDW1dlinecut`

```igor
Function SetVarProc_SimPDW1dlinecut(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `Δ0 (meV)`, `|Δp| (meV)`, `FWHM (meV)`, `T (K)`, `FWHM(|E|)?`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations. Main internal calls: `SimuPDW1D()`, `sum2dlinecut()`, `FFTLpdw()`, `color3s_for3dmf()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Δ0 (meV)`, `|Δp| (meV)`, `FWHM (meV)`, `T (K)`, `FWHM(|E|)?`
- Source: `Models.ipf:11852`

#### `SetVarProc_simuSISTM1`

```igor
Function SetVarProc_simuSISTM1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `left|Gamma_{ / m Q}^{Delta_{1}} / ight|`, `left|Gamma_{ / m Q}^{Delta_{2}} / ight|`, `Gamma_{0}^{Delta_{1}}`, `Gamma_{0}^{Delta_{2}}`, `left| Delta^{1}_{ / m Q} / ight|`, `left| Delta^{2}_{ / m Q} / ight|`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; reads or updates panel controls. Main internal calls: `controlsimuSISTM1()`, `tconvchoiceparameter()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `left|Gamma_{ / m Q}^{Delta_{1}} / ight|`, `left|Gamma_{ / m Q}^{Delta_{2}} / ight|`, `Gamma_{0}^{Delta_{1}}`, `Gamma_{0}^{Delta_{2}}`, `left| Delta^{1}_{ / m Q} / ight|`, `left| Delta^{2}_{ / m Q} / ight|`, `Delta^{1}_{0}`, `Delta^{2}_{0}`
- Source: `General_Simu.ipf:628`

#### `SetVarProc_simuSISTM1convmode`

```igor
Function SetVarProc_simuSISTM1convmode(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `General_Simu.ipf`. Panel control(s): `mode`. Main internal calls: `tconvchoiceparameter()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `mode`
- Source: `General_Simu.ipf:513`

#### `SetVarProc_simuSISTM1slice`

```igor
Function SetVarProc_simuSISTM1slice(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Panel control(s): `slice`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `slice`
- Source: `General_Simu.ipf:545`

#### `SetVarProc_simuSISTM2`

```igor
Function SetVarProc_simuSISTM2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Panel control(s): `deltaphi^{Delta_{1}}`, `deltaphi^{Delta_{2}}`, `deltaphi^{ / m H_{1}}`, `deltaphi^{ / m H_{2}}`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; reads or updates panel controls. Main internal calls: `controlsimuSISTM1()`, `tconvchoiceparameter()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `deltaphi^{Delta_{1}}`, `deltaphi^{Delta_{2}}`, `deltaphi^{ / m H_{1}}`, `deltaphi^{ / m H_{2}}`
- Source: `General_Simu.ipf:664`

#### `SetVarProc_simuSISTMlinecutx`

```igor
Function SetVarProc_simuSISTMlinecutx(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Panel control(s): `x-slice`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `x-slice`
- Source: `General_Simu.ipf:474`

#### `SetVarProc_SumCompOrder_tria`

```igor
Function SetVarProc_SumCompOrder_tria(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `Models.ipf`. Panel control(s): `|A1|`, `|A2|`, `|A3|`, `θ`, `|A4|`, `δθ`. Main internal calls: `cons_SumCompOrder_tria()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `|A1|`, `|A2|`, `|A3|`, `θ`, `|A4|`, `δθ`, `δλ`, `λ`
- Source: `Models.ipf:11695`

#### `SetVarProc_TBG`

```igor
Function SetVarProc_TBG(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `X(Å)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `X(Å)`
- Source: `General_Simu.ipf:1852`

#### `SetVarProc_TBG1`

```igor
Function SetVarProc_TBG1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `a1(Å)`, `θ1(˚)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a1(Å)`, `θ1(˚)`
- Source: `General_Simu.ipf:1884`

#### `SetVarProc_TBG2`

```igor
Function SetVarProc_TBG2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `a2(Å)`, `θ2(˚)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a2(Å)`, `θ2(˚)`
- Source: `General_Simu.ipf:1908`

#### `SetVarProc_Tconv`

```igor
Function SetVarProc_Tconv(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback in `General_Simu.ipf`. Panel control(s): `T(K)`, `mode`. Main internal calls: `convolvetemp()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `T(K)`, `mode`
- Source: `General_Simu.ipf:1684`

#### `SetVarProc_tp_cpr`

```igor
Function SetVarProc_tp_cpr(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `τp`, `Δ(meV)`, `T(mK)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `τp`, `Δ(meV)`, `T(mK)`
- Source: `Models.ipf:958`

#### `SetVarProc_TTG`

```igor
Function SetVarProc_TTG(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `X(Å)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `X(Å)`
- Source: `General_Simu.ipf:2000`

#### `SetVarProc_TTG1`

```igor
Function SetVarProc_TTG1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `a1(Å)`, `θ1(˚)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a1(Å)`, `θ1(˚)`
- Source: `General_Simu.ipf:2040`

#### `SetVarProc_TTG2`

```igor
Function SetVarProc_TTG2(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `a2(Å)`, `θ2(˚)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a2(Å)`, `θ2(˚)`
- Source: `General_Simu.ipf:2066`

#### `SetVarProc_TTG3`

```igor
Function SetVarProc_TTG3(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Panel control(s): `a3(Å)`, `θ3(˚)`. Code behavior: duplicates or stages waves for downstream processing. Main internal calls: `ExampleTriangularLattice()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `a3(Å)`, `θ3(˚)`
- Source: `General_Simu.ipf:2090`

#### `SetVarProc_vortex1`

```igor
Function SetVarProc_vortex1(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Sepration`, `A`, `B`, `|m|:vorticity`, `[-*]:vorticity`, `∆φ(π)`. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `vortexantivortexsimu_tune()`, `makevectorfield_vortexsimu_tune()`, `shrinkvf()`, `cyclecolorwave()`, `tpw()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Sepration`, `A`, `B`, `|m|:vorticity`, `[-*]:vorticity`, `∆φ(π)`
- Source: `Models.ipf:10923`


### Interactive procedures and macros

#### `calculateband`

```igor
proc calculateband()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Models.ipf`. Main internal calls: `claculateband()`.
- Source: `Models.ipf:158`

#### `CalculateP0Ec`

```igor
Proc CalculateP0Ec()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Code behavior: creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `CalculateP0E_auto()`, `log()`, `mirror()`, `rgb()`, `lstyle()`, `axRGB()`. Update log: 2025-06-08: Opens the P(E)-theory simulation panel for environmental tunneling calculations and dynamic Coulomb blockade examples.
- Source: `ModelingTunneling.ipf:29`

#### `CdGM_Dirac_singlevortex`

```igor
Proc CdGM_Dirac_singlevortex(EF,hv,delta,level)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `EF`, `hv`, `delta`, `level`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: EF = Fermi Energy (meV); hv = Fermi Velocity (meV A); delta = SC gap (meV); level = How many levels do you want to display. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `macdonald()`, `zero()`, `nticks()`, `grid()`.
- Parameters:
  - `EF`: Fermi Energy (meV)
  - `hv`: Fermi Velocity (meV A)
  - `delta`: SC gap (meV)
  - `level`: How many levels do you want to display
- Source: `Models.ipf:602`

#### `choiceparameterc`

```igor
Proc choiceparameterc()//(ampw1,ampw2,ampini1,ampini2,ampg1,ampg2,ampgini1,ampgini2)
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis. Main internal calls: `choiceparametercf()`. Update log: 05/12/2024: Added a new simulation program for 2D PDM with 2 gaps. chooseparameterc() V02:debug V03:updated and added examples.
- Source: `General_Simu.ipf:22`

#### `Cons_QHZcut2c`

```igor
Proc Cons_QHZcut2c(A,B,m,mu,ReD,ImD,selfimg,numxy,mode)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `A`, `B`, `m`, `mu`, `ReD`, `ImD`, `selfimg`, `numxy`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; topological-model simulation. Dialog prompts: mu = μ; ReD = Re(Δ); ImD = Im(Δ); selfimg = Im(Σ); numxy = Number of point in k space; mode = Pairing sym. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZ_bandtune()`.
- Parameters:
  - `A`: = A_QHZ
  - `B`: = B_QHZ
  - `m`: = m_QHZ
  - `mu`: μ
  - `ReD`: Re(Δ)
  - `ImD`: Im(Δ)
  - `selfimg`: Im(Σ)
  - `numxy`: Number of point in k space
  - `mode`: Pairing sym
- Source: `Models.ipf:5636`

#### `ConsHaldane`

```igor
Proc ConsHaldane()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: #01 Body Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `Cons_Haldanecut()`, `Cons_HaldaneFS()`, `cons_HaldaneBC()`, `max()`, `ckfig_child()`, `ShrinkDigit()`.
- Source: `Models.ipf:3503`

#### `ConsQWZ`

```igor
Proc ConsQWZ()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: #01 Body Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `Cons_QWZcut()`, `Cons_QWZFS()`, `cons_QWZBC()`, `max()`, `ckfig_child()`, `ShrinkDigit()`.
- Source: `Models.ipf:4948`

#### `convallc`

```igor
Proc convallc(bathname,num,mode,temp)
```
- Usage: run from Igor with parameters `bathname`, `num`, `mode`, `temp`.
- Notes: Interactive Igor procedure in `General_Simu.ipf`. Dialog prompts: bathname = Bath name; num = Number of the batch; mode = Select mode; temp = Temperature (K). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `convall()`.
- Parameters:
  - `bathname`: Bath name
  - `num`: Number of the batch
  - `mode`: Select mode
  - `temp`: Temperature (K)
- Source: `General_Simu.ipf:1730`

#### `convolvetempc`

```igor
Proc convolvetempc(name,tt2)
```
- Usage: run from Igor with parameters `name`, `tt2`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = The 1D wave want to be convolved; tt2 = Temperature (K). Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `convolvetemp()`, `rgb()`, `margin()`.
- Parameters:
  - `name`: The 1D wave want to be convolved
  - `tt2`: Temperature (K)
- Source: `General_Simu.ipf:1662`

#### `DyneModbyvortex`

```igor
proc DyneModbyvortex(grid,K,b1,delta,xrange,Erange)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `grid`, `K`, `b1`, `delta`, `xrange`, `Erange`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: Grid = Number of data point; K = Coherent length (nm); b1 = Dyne broadenig (meV); delta = Gap Amplitude (meV); xrange = Distance from Core center (nm); Erange = Energy Range (meV). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `DOSvor()`.
- Parameters:
  - `grid`: Grid/map size, grid wave, or grid-data selector used by STM/STS workflows. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `K`: Coherent length (nm)
  - `b1`: Dyne broadenig (meV)
  - `delta`: Gap Amplitude (meV)
  - `xrange`: Distance from Core center (nm)
  - `Erange`: Energy Range (meV)
- Source: `Models.ipf:686`

#### `fanoline`

```igor
proc fanoline(G_res, E_res,endnum)
```
- Usage: run from Igor with parameters `G_res`, `E_res`, `endnum`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Parameters:
  - `G_res`: FWHM of resonace peak
  - `E_res`: resonace energy
  - `endnum`: End point, index, or coordinate for the selected range.
- Source: `Models.ipf:742`

#### `FFTstripesimulationc`

```igor
Proc FFTstripesimulationc(p1,q1,p2,q2,scalex,scaley,b,sigma,h,hd)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: run from Igor with parameters `p1`, `q1`, `p2`, `q2`, `scalex`, `scaley`, `b`, `sigma`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: p1 = P for the 1st point; q1 = Q for the 1st point; p2 = P for the 2nd point; q2 = Q for the 2nd point; scalex = X length for the real space image; scaley = Y length for the real space image. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `FFTstripesimulation()`.
- Parameters:
  - `p1`: P for the 1st point
  - `q1`: Q for the 1st point
  - `p2`: P for the 2nd point
  - `q2`: Q for the 2nd point
  - `scalex`: X length for the real space image
  - `scaley`: Y length for the real space image
  - `b`: Background intensity of image
  - `sigma`: Guassian noise sigma
  - `h`: Intensity of 1st/2nd points
  - `hd`: difference between st/2nd points
- Source: `Models.ipf:13495`

#### `fourbandkpFeSC`

```igor
Proc fourbandkpFeSC(sel)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `sel`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: sel = select mode. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `fourbandFeSC2D()`, `fourbandFeSC3D()`.
- Parameters:
  - `sel`: select mode
- Source: `Models.ipf:2278`

#### `fourbandmodel`

```igor
Proc fourbandmodel(a,t2,t2p,t1,t3,u,grid,rx1,rx2,ry1)
```
- Usage: run from Igor with parameters `a`, `t2`, `t2p`, `t1`, `t3`, `u`, `grid`, `rx1`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Dialog prompts: t1 = t1(N) meV; a = Lattice constance (A); t2 = t2(NN_1) meV; t2p = t2p(NN_2) meV; t3 = t3(NNN) meV; u = Chemical potential meV. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations.
- Parameters:
  - `a`: Lattice constance (A)
  - `t2`: t2(NN_1) meV
  - `t2p`: t2p(NN_2) meV
  - `t1`: t1(N) meV
  - `t3`: t3(NNN) meV
  - `u`: Chemical potential meV
  - `grid`: Number of points
  - `rx1`: left of kx (0,2pi)
  - `rx2`: right of kx (0,2pi)
  - `ry1`: left of ky (0,2pi)
- Source: `Models.ipf:851`

#### `gapdisGua2_forPDMsimuc`

```igor
proc gapdisGua2_forPDMsimuc(tip,namemat3d,froml,tol,fromr,tor)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `tip`, `namemat3d`, `froml`, `tol`, `fromr`, `tor`.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Dialog prompts: namemat3d = Name of convoluted 3D data. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `gapdisGua2_forPDMsimu()`.
- Parameters:
  - `tip`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `namemat3d`: Name of convoluted 3D data
  - `froml`: -(ampgini2_simuSISTM+2*ampg2_simuSISTM)
  - `tol`: -(ampgini1_simuSISTM-2*ampg1_simuSISTM)
  - `fromr`: = (ampgini1_simuSISTM-2*ampg1_simuSISTM)
  - `tor`: = (ampgini2_simuSISTM+2*ampg2_simuSISTM)
- Source: `General_Simu.ipf:1059`

#### `gapdisGua2_forPDMsimuc2`

```igor
proc gapdisGua2_forPDMsimuc2()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `gapdisGua2_forPDMsimu2()`.
- Source: `General_Simu.ipf:1253`

#### `HaldaneBerryCurvaturec`

```igor
Proc HaldaneBerryCurvaturec(t1,t2,M,phi,numxy,sel)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `t1`, `t2`, `M`, `phi`, `numxy`, `sel`.
- Notes: Interactive Igor procedure for topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: numxy = N x N in (kx,ky) space; sel = Select algorithm. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `HaldaneBerryCurvature()`, `HaldaneBCFukui()`.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `M`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: N x N in (kx,ky) space
  - `sel`: Select algorithm
- Source: `Models.ipf:3285`

#### `HaldaneSpectralFunctionc`

```igor
Proc HaldaneSpectralFunctionc(t1,t2,M,phi,selfimg,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `t1`, `t2`, `M`, `phi`, `selfimg`, `numxy`.
- Notes: Interactive Igor procedure for topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: selfimg = Im(Σ); numxy = N x N in (kx,ky) space. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `HaldaneSpectralFunction()`.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `M`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Im(Σ)
  - `numxy`: N x N in (kx,ky) space
- Source: `Models.ipf:3189`

#### `make3DfakeSISTMc`

```igor
proc make3DfakeSISTMc()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Main internal calls: `make3DfakeSISTM()`.
- Source: `General_Simu.ipf:922`

#### `makeFeSC_normalFSc`

```igor
Proc makeFeSC_normalFSc(t1,t2,t,tp,u,selfimg,selfreal)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `t1`, `t2`, `t`, `tp`, `u`, `selfimg`, `selfreal`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Main internal calls: `makeFeSC_normalFS()`.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: = 1
  - `t`: = 1
  - `tp`: = 1
  - `u`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: = 0.1
  - `selfreal`: =
- Source: `Models.ipf:11449`

#### `nonsinodal`

```igor
Proc nonsinodal()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `tanh()`, `renormcuts_k()`, `nticks()`, `grid()`, `margin()`, `rgb()`.
- Source: `Models.ipf:919`

#### `nonsinodalcprf`

```igor
proc nonsinodalcprf()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `tanh()`, `renormcuts_k()`.
- Source: `Models.ipf:966`

#### `oneUCFeSegapsimu`

```igor
Proc oneUCFeSegapsimu()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Main internal calls: `makegap1UCFeSe()`, `di()`, `makegap1UCFeSed()`, `makegap1UCFeSebabs()`. Update log: 04/28/2024: Added Model simulation oneUCFeSegapsimu() To plot the 3D surface Gizmo of the 1UC-FeSe gap function.
- Source: `Models.ipf:13760`

#### `PRB_98_214503`

```igor
Proc PRB_98_214503(sel)
```
- Usage: run from Igor with parameters `sel`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Dialog prompts: sel = Model of Phys Rev B 98, 214503 (2018). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `PRB_98_214503_normal()`, `PRB_98_214503_SC()`.
- Parameters:
  - `sel`: Model of Phys Rev B 98, 214503 (2018)
- Source: `Models.ipf:995`

#### `QHZd0Chernlinemmc`

```igor
Proc QHZd0Chernlinemmc(mm)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `mm`.
- Notes: Interactive Igor procedure for topological-model simulation. Dialog prompts: mm = please fix a m value, we calculate Z along μ. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZd0Chernlinemm()`.
- Parameters:
  - `mm`: please fix a m value, we calculate Z along μ
- Source: `Models.ipf:7300`

#### `QHZd0Chernlineuuc`

```igor
Proc QHZd0Chernlineuuc(uu)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `uu`.
- Notes: Interactive Igor procedure for topological-model simulation. Dialog prompts: uu = please fix a μ value, we calculate Z along m. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZd0Chernlineuu()`.
- Parameters:
  - `uu`: please fix a μ value, we calculate Z along m
- Source: `Models.ipf:7240`

#### `QHZLDOSC`

```igor
Proc QHZLDOSC(A,B,m,mu,ReD,ImD,nslab,num,Vmin,Vmax)
```
- Usage: run from Igor with parameters `A`, `B`, `m`, `mu`, `ReD`, `ImD`, `nslab`, `num`.
- Notes: Calculate LDOS(E) Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Dialog prompts: nslab = Number of Slab; num = Number of point in k space; Vmin = E(min); Vmax = E(max). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZLDOS()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Number of Slab
  - `num`: Number of point in k space
  - `Vmin`: E(min)
  - `Vmax`: E(max)
- Source: `Models.ipf:9247`

#### `QHZLDOSCall`

```igor
Proc QHZLDOSCall(m,mu,ReD,ImD,nslab,num,Vmax,sel,mode)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `m`, `mu`, `ReD`, `ImD`, `nslab`, `num`, `Vmax`, `sel`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Dialog prompts: nslab = Number of Slab; num = Number of point in k space; Vmax = E(max); sel = Select Mode; mode = Pairing sym. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZLDOSCindex()`, `QHZLDOSC()`.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Number of Slab
  - `num`: Number of point in k space
  - `Vmax`: E(max)
  - `sel`: Select Mode
  - `mode`: Pairing sym
- Source: `Models.ipf:8950`

#### `QHZLDOSCindex`

```igor
Proc QHZLDOSCindex(A,B,m,mu,ReD,ImD,nslab,num)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `A`, `B`, `m`, `mu`, `ReD`, `ImD`, `nslab`, `num`.
- Notes: Interactive Igor procedure for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Dialog prompts: nslab = Number of Slab; num = Number of point in k space. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZLDOSindex()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Number of Slab
  - `num`: Number of point in k space
- Source: `Models.ipf:8980`

#### `QHZslabcutc`

```igor
Proc QHZslabcutc(m,mu,ReD,ImD,nslab,numxy,ratio,xi,Vmax,mode)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `m`, `mu`, `ReD`, `ImD`, `nslab`, `numxy`, `ratio`, `xi`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; topological-model simulation. Dialog prompts: nslab = Number of Slab; numxy = Number of point in k space; ratio = Weight?; xi = ξ(layer) [Slab decay length]; mode = Pairing sym. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QHZslabcut_cmplx()`, `QHZslabcut()`, `di()`.
- Parameters:
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Number of Slab
  - `numxy`: Number of point in k space
  - `ratio`: Weight?
  - `xi`: ξ(layer) [Slab decay length]
  - `Vmax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mode`: Pairing sym
- Source: `Models.ipf:9451`

#### `QPIESimu2Dbandc`

```igor
Proc QPIESimu2Dbandc(shape_hole,shape_ele,mu_hole,mu_ele,gap_hole,gap_ele,selfE,sel)
```
- Usage: run from Igor with parameters `shape_hole`, `shape_ele`, `mu_hole`, `mu_ele`, `gap_hole`, `gap_ele`, `selfE`, `sel`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: shape_hole = Band shape (hole band); shape_ele = Band shape (electron band); gap_hole = Δ(hole band); gap_ele = Δ(electron band); mu_hole = μ(hole band); mu_ele = μ(electron band). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `QPIESimu2Dband()`.
- Parameters:
  - `shape_hole`: Band shape (hole band)
  - `shape_ele`: Band shape (electron band)
  - `mu_hole`: μ(hole band)
  - `mu_ele`: μ(electron band)
  - `gap_hole`: Δ(hole band)
  - `gap_ele`: Δ(electron band)
  - `selfE`: Im∑
  - `sel`: SC or Normal
- Source: `Models.ipf:12348`

#### `RCSJc`

```igor
Proc RCSJc(TT,beta_c,sm,current)
```
- Usage: run from Igor with parameters `TT`, `beta_c`, `sm`, `current`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; graph display, formatting, or window management. Dialog prompts: TT = Temperature (K); beta_c = beta_{c}:Stewart–McCumber parameter; sm = smooth; current = Normalized drive current （Ic）. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `SimulateRCSJ_IV_Hysteresis_Realistic()`, `SimulateRCSJ_voltage_vs_Time()`, `margin()`, `zColor()`, `lstyle()`, `rgb()`.
- Parameters:
  - `TT`: Temperature (K)
  - `beta_c`: beta_{c}:Stewart–McCumber parameter
  - `sm`: smooth
  - `current`: Normalized drive current （Ic）
- Source: `ModelingTunneling.ipf:1375`

#### `simuCDWc`

```igor
Proc simuCDWc(theta,wLength,c1,c2,c3)
```
- Usage: run from Igor with parameters `theta`, `wLength`, `c1`, `c2`, `c3`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Dialog prompts: theta = Latticfe angle˚; wLength = Lattice constant; c1 = Phase shift of Q1 (c1*2π/3); c2 = Phase shift of Q2 (c2*2π/3); c3 = Phase shift of Q3 (c3*2π/3). Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `simuCDW()`, `margin()`.
- Parameters:
  - `theta`: Latticfe angle˚
  - `wLength`: Lattice constant
  - `c1`: Phase shift of Q1 (c1*2π/3)
  - `c2`: Phase shift of Q2 (c2*2π/3)
  - `c3`: Phase shift of Q3 (c3*2π/3)
- Source: `Models.ipf:13621`

#### `SimuPDW1Dc`

```igor
Proc SimuPDW1Dc(delta0,deltap,FWHM,T,Edb)
```
- Usage: run from Igor with parameters `delta0`, `deltap`, `FWHM`, `T`, `Edb`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: delta0 = Δ0 (meV); deltap = |Δp| (meV); FWHM = FWHM (meV); T = T (K); Edb = FWHM(|E|)?. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `SimuPDW1Dbody()`.
- Parameters:
  - `delta0`: Δ0 (meV)
  - `deltap`: |Δp| (meV)
  - `FWHM`: FWHM (meV)
  - `T`: T (K)
  - `Edb`: FWHM(|E|)?
- Source: `Models.ipf:11789`

#### `SimuPDW2Dc`

```igor
Proc SimuPDW2Dc(delta0,deltap,FWHM,T,Edb)
```
- Usage: run from Igor with parameters `delta0`, `deltap`, `FWHM`, `T`, `Edb`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: delta0 = Δ0 (meV); deltap = |Δp| (meV); FWHM = FWHM (meV); T = T (K); Edb = FWHM(|E|)?. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `SimuPDW2D()`.
- Parameters:
  - `delta0`: Δ0 (meV)
  - `deltap`: |Δp| (meV)
  - `FWHM`: FWHM (meV)
  - `T`: T (K)
  - `Edb`: FWHM(|E|)?
- Source: `Models.ipf:12018`

#### `simupdwwithw`

```igor
proc simupdwwithw(indicate)
```
- Usage: run from Igor with parameters `indicate`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction. Dialog prompts: indicate = Indicate. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; loads data from files or paths; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`, `FFTL2()`.
- Parameters:
  - `indicate`: Indicate
- Source: `Models.ipf:11756`

#### `SIMUtopodefectc`

```igor
Proc SIMUtopodefectc(a1,a2,add,lattice,zero,amp,x0,y0,windhalf,windsingle)
```
- Usage: run from Igor with parameters `a1`, `a2`, `add`, `lattice`, `zero`, `amp`, `x0`, `y0`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal. Dialog prompts: a1 = Angle of dislocation (°); a2 = Angle of Order Q (°); add = Phase offset; lattice = Lattice constant; zero = ∆0 background; amp = Amplitude of PDW. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `SIMUtopodefecthalf()`, `SIMUtopodefectsingle()`, `makeQindicate()`, `ckfig_child()`.
- Parameters:
  - `a1`: Angle of dislocation (°)
  - `a2`: Angle of Order Q (°)
  - `add`: Phase offset
  - `lattice`: Lattice constant
  - `zero`: ∆0 background
  - `amp`: Amplitude of PDW
  - `x0`: Frame X offset
  - `y0`: Frame Y offset
  - `windhalf`: Half vortex winding number
  - `windsingle`: Single vortex winding number
- Source: `Models.ipf:10418`

#### `Solveedgestate_QHZc`

```igor
Proc Solveedgestate_QHZc(A,B,m,mu,ReD,ImD,nslab,num,mode)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `A`, `B`, `m`, `mu`, `ReD`, `ImD`, `nslab`, `num`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; topological-model simulation; graph display, formatting, or window management. Dialog prompts: nslab = Number of Slab; num = Number of point in k space; mode = Pairing sym. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `Solveedgestate_QHZ_cmplx()`, `Solveedgestate_QHZ()`, `displaymultiFQHZ()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Number of Slab
  - `num`: Number of point in k space
  - `mode`: Pairing sym
- Source: `Models.ipf:8566`

#### `sum2dlinecutc`

```igor
Proc sum2dlinecutc(name,zn,sufout,show)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `name`, `zn`, `sufout`, `show`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = The source 2D wave; zn = The dimension to keep, 0 for x; 1 for y; sufout = The suffix add to the name of output; show = Do you want display?. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `sum2dlinecut()`.
- Parameters:
  - `name`: The source 2D wave
  - `zn`: The dimension to keep, 0 for x; 1 for y
  - `sufout`: The suffix add to the name of output
  - `show`: Do you want display?
- Source: `Models.ipf:11913`

#### `TBGsimu`

```igor
Proc TBGsimu(xx,a1,a2,theta1,theta2)
```
- Usage: run from Igor with parameters `xx`, `a1`, `a2`, `theta1`, `theta2`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; lattice, moire, or twist-angle simulation/analysis; graph display, formatting, or window management. Dialog prompts: xx = Length of the image (Å); a1 = Lattice constant of layer 1 (Å); a2 = Lattice constant of layer 2 (Å); theta1 = Twist angle of layer 1 (˚); theta2 = Twist angle of layer 2 (˚). Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls; asks the user for parameters through Igor prompts. Main internal calls: `ExampleTriangularLattice()`, `rgb()`.
- Parameters:
  - `xx`: Length of the image (Å)
  - `a1`: Lattice constant of layer 1 (Å)
  - `a2`: Lattice constant of layer 2 (Å)
  - `theta1`: Twist angle of layer 1 (˚)
  - `theta2`: Twist angle of layer 2 (˚)
- Source: `General_Simu.ipf:1805`

#### `tbmodel`

```igor
proc tbmodel(t1,t2,t3,u,rx1,rx2,ry1,ry2,grid)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: run from Igor with parameters `t1`, `t2`, `t3`, `u`, `rx1`, `rx2`, `ry1`, `ry2`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: t1 = t1(N) meV; t2 = t2(NN) meV; t3 = t3(NNN) meV; u = Chemical potential meV; grid = Number of points; rx1 = left of kx (0,2pi). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `linkstsmap_P()`.
- Parameters:
  - `t1`: t1(N) meV
  - `t2`: t2(NN) meV
  - `t3`: t3(NNN) meV
  - `u`: Chemical potential meV
  - `rx1`: left of kx (0,2pi)
  - `rx2`: right of kx (0,2pi)
  - `ry1`: left of ky (0,2pi)
  - `ry2`: right of ky (0,2pi)
  - `grid`: Number of points
- Source: `Models.ipf:775`

#### `testgs`

```igor
proc testgs()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Main internal calls: `Integrate1D()`.
- Source: `Models.ipf:462`

#### `testgspo`

```igor
proc testgspo()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Main internal calls: `Integrate1D()`.
- Source: `Models.ipf:552`

#### `vortexantivortexsimuc`

```igor
Proc vortexantivortexsimuc(a_sep,AA,BB,vorticity,pshif,reverseV)
```
- Usage: run from Igor with parameters `a_sep`, `AA`, `BB`, `vorticity`, `pshif`, `reverseV`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: a_sep = Vortex-antivortex speration; AA = A; BB = B; vorticity = |m|:vorticity; pshif = Phase shift (π); reverseV = [-*]: sign of vorticity. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `vortexantivortexsimu()`.
- Parameters:
  - `a_sep`: Vortex-antivortex speration
  - `AA`: A
  - `BB`: B
  - `vorticity`: |m|:vorticity
  - `pshif`: Phase shift (π)
  - `reverseV`: [-*]: sign of vorticity
- Source: `Models.ipf:10616`


### Computational and data helpers

#### `CalculateP0E`

```igor
Function CalculateP0E(T)
```
- Notes: Individual P(E) theory calculation Usage: call as `CalculateP0E(T)`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; differentiates wave data; cleans up waves/windows or brings an existing window forward. Main internal calls: `numtype()`, `elseif()`, `areaXY()`, `CutWave_EcenteredRange()`.
- Parameters:
  - `T`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `ModelingTunneling.ipf:1054`

#### `CalculateP0E_auto`

```igor
Function CalculateP0E_auto()
```
- Usage: call `CalculateP0E_auto()` from Igor procedure code or the command line.
- Notes: Utility function in `ModelingTunneling.ipf`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; differentiates wave data; cleans up waves/windows or brings an existing window forward. Main internal calls: `numtype()`, `elseif()`, `areaXY()`, `CutWave_EcenteredRange()`, `SimulateDCB_IV()`.
- Source: `ModelingTunneling.ipf:296`

#### `ChernN_haldane`

```igor
Function ChernN_haldane(BCname)
```
- Usage: call as `ChernN_haldane(BCname)`.
- Notes: #07 Calculate Chern number Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Update log: 10/18_03/2023: Correct the counting range of ChernN_haldane(BCname). now it is [ , ) for both dimension. 10/18_02/2023: Improve Haldane Model performance, added Chern number calculation ChernN_haldane(BCname).
- Parameters:
  - `BCname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Models.ipf:3906`

#### `ChernN_qhz`

```igor
Function ChernN_qhz(BCname)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `ChernN_qhz(BCname)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; topological-model simulation; Igor wave/matrix/cube data operation.
- Parameters:
  - `BCname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Models.ipf:7767`

#### `ChernN_qwz`

```igor
Function ChernN_qwz(BCname)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `ChernN_qwz(BCname)`.
- Notes: Function for topological-model simulation; Igor wave/matrix/cube data operation. Update log: 10/28/2023: Added Qi-Wu-Zhang model for 2D Chern insulator. ChernN_QWZ(BCname), [ , ) for square lattice.
- Parameters:
  - `BCname`: String name used to locate an Igor wave, graph, folder, or output object.
- Source: `Models.ipf:4909`

#### `choiceparameter`

```igor
Function choiceparameter(ampw1,ampw2,ampini1,ampini2,ampg1,ampg2,ampgini1,ampgini2,dphid1,dphid2,dphih1,dphih2,shrinkratio)
```
- Usage: call as `choiceparameter(ampw1, ampw2, ampini1, ampini2, ampg1, ampg2, ampgini1, ampgini2)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `ampw1`: =0.025 // amplitude of the width of gap 1
  - `ampw2`: =0.025 // amplitude of the width of gap 2
  - `ampini1`: =0.07 // average of the width of gap 1
  - `ampini2`: =0.07 // average of the width of gap 2
  - `ampg1`: =0.22 // amplitude of the size of gap 1
  - `ampg2`: =0.31 // amplitude of the size of gap 2
  - `ampgini1`: =1.4 // average of the size of gap 1
  - `ampgini2`: =1.9 // average of the size of gap 2
  - `dphid1`: =pi //phi of the gap size 1
  - `dphid2`: =pi // phi of the gap size 2
  - ... 3 additional parameters
- Source: `General_Simu.ipf:874`

#### `choiceparametercf`

```igor
Function choiceparametercf(ampw1,ampw2,ampini1,ampini2,ampg1,ampg2,ampgini1,ampgini2)
```
- Usage: call as `choiceparametercf(ampw1, ampw2, ampini1, ampini2, ampg1, ampg2, ampgini1, ampgini2)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `choiceparameter()`, `margin()`, `rgb()`, `tconvchoiceparameter()`, `tpw()`, `mode()`.
- Parameters:
  - `ampw1`: = 0.025 // amplitude of the width of gap 1
  - `ampw2`: = 0.03 // amplitude of the width of gap 2
  - `ampini1`: = 0.07 // average of the width of gap 1
  - `ampini2`: = 0.07 // average of the width of gap 2
  - `ampg1`: = 0.22 // amplitude of the size of gap 1
  - `ampg2`: = 0.31 // amplitude of the size of gap 2
  - `ampgini1`: = 1.4 // average of the size of gap 1
  - `ampgini2`: = 1.9 // average of the size of gap 2
- Source: `General_Simu.ipf:35`

#### `claculateband`

```igor
Function claculateband()
```
- Usage: call `claculateband()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays.
- Source: `Models.ipf:163`

#### `color3s_PDMsimu`

```igor
Function color3s_PDMsimu(tt)
```
- Usage: call as `color3s_PDMsimu(tt)`.
- Notes: Function for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `tt`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `General_Simu.ipf:806`

#### `Complextorealfpdw`

```igor
Function Complextorealfpdw(name1w,select)
```
- Usage: call as `Complextorealfpdw(name1w, select)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name1w`: Igor wave reference used as input or output.
  - `select`: Mode flag controlling which branch of the workflow is used.
- Source: `Models.ipf:11985`

#### `Cons3dplotcQWZ`

```igor
Function Cons3dplotcQWZ()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call `Cons3dplotcQWZ()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `func_zeroNaN()`, `max()`, `grabwinchild()`, `color3s_for3d()`.
- Source: `Models.ipf:9854`

#### `cons_HaldaneBC`

```igor
Function cons_HaldaneBC(t1,t2,M,phi,numxy)
```
- Usage: call as `cons_HaldaneBC(t1, t2, M, phi, numxy)`.
- Notes: #05 Functional: Calculate Berry curvature Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `HaldaneModel()`, `ChernN_haldane()`.
- Parameters:
  - `t1`: = 1
  - `t2`: = 0.2
  - `M`: = 0.2
  - `phi`: = 0.2*pi
  - `numxy`: = 100
- Source: `Models.ipf:3719`

#### `Cons_Haldanecut`

```igor
Function Cons_Haldanecut(t1,t2,M,phi,selfimg,numxy)
```
- Usage: call as `Cons_Haldanecut(t1, t2, M, phi, selfimg, numxy)`.
- Notes: #04 Functional: make band dispersion cut Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `HaldaneModel()`.
- Parameters:
  - `t1`: = 1
  - `t2`: = 0
  - `M`: = 0.2
  - `phi`: = 0.2*pi
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:3659`

#### `Cons_HaldaneFS`

```igor
Function Cons_HaldaneFS(t1,t2,M,phi,selfimg,numxy)
```
- Usage: call as `Cons_HaldaneFS(t1, t2, M, phi, selfimg, numxy)`.
- Notes: #03 Functional: make Fermi surface Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `HaldaneModel()`.
- Parameters:
  - `t1`: = 1
  - `t2`: = 0
  - `M`: = 0.2
  - `phi`: = 0.2*pi
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:3584`

#### `Cons_QHZcut`

```igor
Function Cons_QHZcut(A,B,m,mu,ReD,ImD,selfimg,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `Cons_QHZcut(A, B, m, mu, ReD, ImD, selfimg, numxy)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZ()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:5771`

#### `Cons_QHZcut2`

```igor
Function Cons_QHZcut2(A,B,m,mu,ReD,ImD,selfimg,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `Cons_QHZcut2(A, B, m, mu, ReD, ImD, selfimg, numxy)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZ()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:5827`

#### `cons_QWZBC`

```igor
Function cons_QWZBC(A,B,M,mu,numxy)
```
- Usage: call as `cons_QWZBC(A, B, M, mu, numxy)`.
- Notes: #05 Functional: Calculate Berry curvature Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QWZ()`, `ChernN_QWZ()`.
- Parameters:
  - `A`: = 1
  - `B`: = 0.2
  - `M`: = 0.2
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:5199`

#### `Cons_QWZcut`

```igor
Function Cons_QWZcut(A,B,M,mu,selfimg,numxy)
```
- Usage: call as `Cons_QWZcut(A, B, M, mu, selfimg, numxy)`.
- Notes: #04 Functional: make band dispersion cut Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QWZ()`.
- Parameters:
  - `A`: = 1
  - `B`: = 0
  - `M`: = 0.2
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:5137`

#### `Cons_QWZFS`

```igor
Function Cons_QWZFS(A,B,M,mu,selfimg,numxy)
```
- Usage: call as `Cons_QWZFS(A, B, M, mu, selfimg, numxy)`.
- Notes: #03 Functional: make Fermi surface Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QWZ()`.
- Parameters:
  - `A`: = 1
  - `B`: = 0
  - `M`: = 0.2
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:5062`

#### `controlsimuSISTM1`

```igor
Function controlsimuSISTM1()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `controlsimuSISTM1()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `tconvchoiceparameter()`, `choiceparameter()`, `color3s_PDMsimu()`.
- Source: `General_Simu.ipf:699`

#### `convall`

```igor
Function convall(bathname,num,mode,temp)
```
- Usage: call as `convall(bathname, num, mode, temp)`.
- Notes: Function for graph display, formatting, or window management. Code behavior: opens or updates graph/image displays. Main internal calls: `convolvetempall()`, `displaymultiF()`. Update log: 07/01/2024: Added T-conv to a batch of spectrum.
- Parameters:
  - `bathname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `mode`: Step size or energy/voltage increment used by the calculation.
  - `temp`: Temperature parameter, usually in kelvin when used in spectral modeling.
- Source: `General_Simu.ipf:1742`

#### `convolvetemp`

```igor
Function convolvetemp(name,tt2)
```
- Usage: call as `convolvetemp(name, tt2)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; differentiates wave data; cleans up waves/windows or brings an existing window forward. Update log: 05/10/2024: Added T_convolve for any 1D curve convolvetemp(name,tt2).
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `General_Simu.ipf:1692`

#### `convolvetempall`

```igor
Function convolvetempall(name,tt2)
```
- Usage: call as `convolvetempall(name, tt2)`.
- Notes: Function for smoothing, normalization, or background removal; lattice, moire, or twist-angle simulation/analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; extracts image/wave statistics; differentiates wave data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `tt2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `General_Simu.ipf:1766`

#### `displaymultiFQHZ`

```igor
Function  displaymultiFQHZ(dataname,from,to)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `displaymultiFQHZ(dataname, from, to)`.
- Notes: Function for topological-model simulation; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics.
- Parameters:
  - `dataname`: String name used to locate an Igor wave, graph, folder, or output object.
  - `from`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `to`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8648`

#### `effQHZ_LDOSEawr`

```igor
Function effQHZ_LDOSEawr()
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call `effQHZ_LDOSEawr()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Code behavior: creates output waves; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZslabcut_cmplx()`, `QHZslabcut()`, `di()`, `userticks()`.
- Source: `Models.ipf:9611`

#### `extracthight_PDMsimu`

```igor
Function extracthight_PDMsimu(namemat3d)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `extracthight_PDMsimu(namemat3d)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namemat3d`: Matrix/2D wave input or matrix output name.
- Source: `General_Simu.ipf:1205`

#### `FeSC_normal`

```igor
Function FeSC_normal()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `FeSC_normal()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `max()`, `grabwinchild()`.
- Source: `Models.ipf:11060`

#### `FFTLpdw`

```igor
Function FFTLpdw(name,sel)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTLpdw(name, sel)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; formats graph axes, labels, colors, or annotations. Main internal calls: `func_NaN0()`, `cvtcmplx()`, `Complextorealfpdw()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `sel`: 1 for x, 2 for y
- Source: `Models.ipf:11963`

#### `FFTstripesimulation`

```igor
Function FFTstripesimulation(p1,q1,p2,q2,scalex,scaley,b,sigma,h,hd)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTstripesimulation(p1, q1, p2, q2, scalex, scaley, b, sigma)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `gnoise()`, `cvtcmplx()`.
- Parameters:
  - `p1`: = 1
  - `q1`: = 1
  - `p2`: = 198
  - `q2`: = 198
  - `scalex`: = 10
  - `scaley`: = 10
  - `b`: = 0
  - `sigma`: = 0.1
  - `h`: = 100
  - `hd`: = 0
- Source: `Models.ipf:13520`

#### `FFTstripesimulation_cons`

```igor
Function FFTstripesimulation_cons(p1,q1,p2,q2,scalex,scaley,b,sigma,h,hd)
```
- Purpose: performs Fourier, filtering, phase, or lock-in processing.
- Usage: call as `FFTstripesimulation_cons(p1, q1, p2, q2, scalex, scaley, b, sigma)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `gnoise()`, `cvtcmplx()`.
- Parameters:
  - `p1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `q1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `p2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `q2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `scalex`: Scaling, normalization, interpolation, or correction factor.
  - `scaley`: Scaling, normalization, interpolation, or correction factor.
  - `b`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sigma`: Width or sigma parameter for smoothing, fitting, or Fourier filtering.
  - `h`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `hd`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:13597`

#### `fourbandFeSC2D`

```igor
Function fourbandFeSC2D()
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call `fourbandFeSC2D()` from Igor procedure code or the command line.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; reads or updates panel controls; cleans up waves/windows or brings an existing window forward. Main internal calls: `mpc()`, `s0()`, `di()`.
- Source: `Models.ipf:2588`

#### `fourbandFeSC3D`

```igor
Function fourbandFeSC3D()
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call `fourbandFeSC3D()` from Igor procedure code or the command line.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `mpc()`, `s0()`, `tpw()`, `max()`, `grabwinchild()`.
- Source: `Models.ipf:2290`

#### `Function_MBSGS`

```igor
Function Function_MBSGS(inX)
```
- Usage: call as `Function_MBSGS(inX)`.
- Notes: Function in `Models.ipf`. Main internal calls: `cosh()`.
- Parameters:
  - `inX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:453`

#### `Function_MBSGSpo`

```igor
Function Function_MBSGSpo(inX)
```
- Usage: call as `Function_MBSGSpo(inX)`.
- Notes: Function in `Models.ipf`. Main internal calls: `cosh()`.
- Parameters:
  - `inX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:543`

#### `gapdisGua2_forPDMsimu`

```igor
Function gapdisGua2_forPDMsimu(namemat3d,froml,tol,fromr,tor)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `gapdisGua2_forPDMsimu(namemat3d, froml, tol, fromr, tor)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Parameters:
  - `namemat3d`: the 3D matrix g(r,v)
  - `froml`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tol`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fromr`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `tor`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `General_Simu.ipf:1071`

#### `gapdisGua2_forPDMsimu2`

```igor
Function gapdisGua2_forPDMsimu2(namemat3d)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `gapdisGua2_forPDMsimu2(namemat3d)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `namemat3d`: Matrix/2D wave input or matrix output name.
- Source: `General_Simu.ipf:1261`

#### `HaldaneBCFukui`

```igor
Function HaldaneBCFukui(t1,t2,M,phi,numxy)
```
- Usage: call as `HaldaneBCFukui(t1, t2, M, phi, numxy)`.
- Notes: #08 A different method for calculating Berry Curvature, but it is slower ref: Journal of the Physical Society of Japan, 2005, 74(6): 1674-1677. ref: https://mp.weixin.qq.com/s/zhyowoXSP91H3xYdByNDEQ Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `HaldaneModel()`, `di()`, `ChernN_haldane()`, `StopMSTimer()`.
- Parameters:
  - `t1`: = 1
  - `t2`: = 0.2
  - `M`: = 0.2
  - `phi`: = 0.2*pi
  - `numxy`: = 100
- Source: `Models.ipf:3941`

#### `HaldaneBerryCurvature`

```igor
Function HaldaneBerryCurvature(t1,t2,M,phi,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `HaldaneBerryCurvature(t1, t2, M, phi, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `HaldaneModel()`, `di()`, `ChernN_haldane()`, `StopMSTimer()`.
- Parameters:
  - `t1`: = 1
  - `t2`: = 0.2
  - `M`: = 0.2
  - `phi`: = 0.2*pi
  - `numxy`: = 100
- Source: `Models.ipf:3301`

#### `HaldaneModel`

```igor
Function/Wave HaldaneModel(t1,t2,M,phi,kx,ky)
```
- Usage: call as `HaldaneModel(t1, t2, M, phi, kx, ky)`.
- Notes: Build Haldane Hamiltonian Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `wn()`, `wC()`, `Wn()`, `automatrixC()`.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `M`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `phi`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
- Source: `Models.ipf:3133`

#### `HaldaneSpectralFunction`

```igor
Function HaldaneSpectralFunction(t1,t2,M,phi,selfimg,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `HaldaneSpectralFunction(t1, t2, M, phi, selfimg, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `HaldaneModel()`, `tpw()`, `max()`, `grabwinchild()`.
- Parameters:
  - `t1`: = 1
  - `t2`: = 0
  - `M`: = 0.2
  - `phi`: = 0.2*pi
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:3201`

#### `Interactiveshow`

```igor
Function Interactiveshow()
```
- Usage: call `Interactiveshow()` from Igor procedure code or the command line.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `Solveedgestate_QHZ_cmplx()`, `Solveedgestate_QHZ()`, `displaymultiFQHZ()`, `userticks()`, `hook()`.
- Source: `Models.ipf:8679`

#### `macdonald`

```igor
Function macdonald(a,b)
```
- Notes: Here is a non- accurate numerical bulit of Macdonald Function Usage: call as `macdonald(a, b)`. Main internal calls: `integrate1d()`.
- Parameters:
  - `a`: the order of function
  - `b`: the independence variable
- Source: `Models.ipf:565`

#### `macintegrand`

```igor
Function macintegrand(t)
```
- Usage: call as `macintegrand(t)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis.
- Parameters:
  - `t`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:573`

#### `madelldos`

```igor
Function madelldos(hv,B,ED,tem,num,ER,index)
```
- Usage: call as `madelldos(hv, B, ED, tem, num, ER, index)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `calculateLLall()`, `mean()`.
- Parameters:
  - `hv`: =4.4/0.025//fermi velocity (meV*A)
  - `B`: =4// magnetic field (T)
  - `ED`: =-4.4//Dirac point (meV)
  - `tem`: =15
  - `num`: =100
  - `ER`: =10 //meV
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Models.ipf:4510`

#### `make3DfakeSISTM`

```igor
Function make3DfakeSISTM(ampw1,ampw2,ampini1,ampini2,ampg1,ampg2,ampgini1,ampgini2,dphid1,dphid2,dphih1,dphih2,shrinkratio)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `make3DfakeSISTM(ampw1, ampw2, ampini1, ampini2, ampg1, ampg2, ampgini1, ampgini2)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; differentiates wave data; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ampw1`: =0.025 // amplitude of the width of gap 1
  - `ampw2`: =0.025 // amplitude of the width of gap 2
  - `ampini1`: =0.07 // average of the width of gap 1
  - `ampini2`: =0.07 // average of the width of gap 2
  - `ampg1`: =0.22 // amplitude of the size of gap 1
  - `ampg2`: =0.31 // amplitude of the size of gap 2
  - `ampgini1`: =1.4 // average of the size of gap 1
  - `ampgini2`: =1.9 // average of the size of gap 2
  - `dphid1`: =pi //phi of the gap size 1
  - `dphid2`: =pi // phi of the gap size 2
  - ... 3 additional parameters
- Source: `General_Simu.ipf:926`

#### `makeFeSC_normalFS`

```igor
Function makeFeSC_normalFS(t1,t2,t,tp,u,selfimg,selfreal)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makeFeSC_normalFS(t1, t2, t, tp, u, selfimg, selfreal)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `di()`.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: = 1
  - `t`: = 1
  - `tp`: = 1
  - `u`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: = 0.1
  - `selfreal`: = 0
- Source: `Models.ipf:11466`

#### `makeQindicate`

```igor
Function makeQindicate(a2_td)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makeQindicate(a2_td)`.
- Notes: Utility function for Nanonis/STM data loading or map conversion; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves.
- Parameters:
  - `a2_td`: = lattice//lattice constance
- Source: `Models.ipf:10500`

#### `makevectorfield_vortexsimu`

```igor
Function makevectorfield_vortexsimu(name,amp)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makevectorfield_vortexsimu(name, amp)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `mode()`, `arrowMarker()`, `margin()`.
- Parameters:
  - `name`: Name of the phase map
  - `amp`: Name of the amplitude map
- Source: `Models.ipf:10673`

#### `makevectorfield_vortexsimu_tune`

```igor
Function makevectorfield_vortexsimu_tune(name,amp)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call as `makevectorfield_vortexsimu_tune(name, amp)`.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; extracts image/wave statistics.
- Parameters:
  - `name`: Name of the phase map
  - `amp`: Name of the amplitude map
- Source: `Models.ipf:10838`

#### `MergeColumneach`

```igor
Function MergeColumneach(name,each)
```
- Notes: Merge every several columns for a matrix Usage: call as `MergeColumneach(name, each)`. Code behavior: creates output waves. Update log: 10/31/2023: (1) MergeColumneach(name,each): Merge number of "each" column of the matrix, the output the dimension size(name,1) reduced to be 1/each.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `each`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:9142`

#### `MergeRoweach`

```igor
Function MergeRoweach(name,each)
```
- Usage: call as `MergeRoweach(name, each)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations. Main internal calls: `di()`. Update log: 10/31/2023: (6) MergeRoweach(name,each): Merge number of "each" row of the matrix, the output the dimension size(name,0) reduced to be 1/each.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `each`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:9375`

#### `myCursorMovedHookQHZ`

```igor
Function myCursorMovedHookQHZ(s)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `myCursorMovedHookQHZ(s)`.
- Notes: Function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Main internal calls: `Solveedgestate_QHZ_cmplx()`, `hcsr()`, `vcsr()`, `Solveedgestate_QHZ()`, `Cons_QHZcut()`, `Cons_QHZcut2()`.
- Parameters:
  - `s`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8782`

#### `pdmsimu_example`

```igor
Function pdmsimu_example(ampw1,ampw2,ampini1,ampini2,ampg1,ampg2,ampgini1,ampgini2,dphid1,dphid2,dphih1,dphih2,shrinkratio,slice,gaponwhich,linecutslice,tt2,convornot,convmode)
```
- Notes: Function for example cases Usage: call as `pdmsimu_example(ampw1, ampw2, ampini1, ampini2, ampg1, ampg2, ampgini1, ampgini2)`. Main internal calls: `PDMSIMU_examplecore()`.
- Parameters:
  - `ampw1`: _simuSISTM = 0
  - `ampw2`: _simuSISTM = 0
  - `ampini1`: _simuSISTM = 0.03
  - `ampini2`: _simuSISTM = 0.05
  - `ampg1`: _simuSISTM = 0.22
  - `ampg2`: _simuSISTM = 0.31
  - `ampgini1`: _simuSISTM = 1.4
  - `ampgini2`: _simuSISTM = 1.9
  - `dphid1`: _simuSISTM = 0
  - `dphid2`: _simuSISTM = 0
  - ... 9 additional parameters
- Source: `General_Simu.ipf:1448`

#### `PDMSIMU_examplecore`

```igor
Function PDMSIMU_examplecore()
```
- Usage: call `PDMSIMU_examplecore()` from Igor procedure code or the command line.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics; reads or updates panel controls. Main internal calls: `controlsimuSISTM1()`, `tconvchoiceparameter()`, `make3DfakeSISTM()`, `gapdisGua2_forPDMsimu2()`, `extracthight_PDMsimu()`.
- Source: `General_Simu.ipf:1490`

#### `PDWonSC`

```igor
Function PDWonSC(w,x) : FitFunc
```
- Usage: call as `PDWonSC(w, x)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; Igor wave/matrix/cube data operation.
- Parameters:
  - `w`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `FFT.ipf:2192`

#### `PRB_98_214503_normal`

```igor
Function PRB_98_214503_normal()
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call `PRB_98_214503_normal()` from Igor procedure code or the command line.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `max()`, `grabwinchild()`.
- Source: `Models.ipf:1007`

#### `PRB_98_214503_SC`

```igor
Function PRB_98_214503_SC()
```
- Notes: This is the simulation of normal bands in PRB 98,214503 (2018) This is the eignevalue of equation (2) Usage: call `PRB_98_214503_SC()` from Igor procedure code or the command line. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `tpw()`, `max()`, `grabwinchild()`.
- Source: `Models.ipf:1677`

#### `QHZ`

```igor
Function/Wave QHZ(A,B,m,mu,ReD,ImD,kx,ky)
```
- Usage: call as `QHZ(A, B, m, mu, ReD, ImD, kx, ky)`.
- Notes: Correct QHZ BdG Hamiltonian Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Main internal calls: `wC()`, `Wn()`, `automatrixC()`, `conj()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
- Source: `Models.ipf:5451`

#### `QHZ_bandtune`

```igor
Function QHZ_bandtune(A,B,m,mu,ReD,ImD,selfimg,numxy,mode)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_bandtune(A, B, m, mu, ReD, ImD, selfimg, numxy)`.
- Notes: Function for linecut, slice, or region extraction; topological-model simulation; graph display, formatting, or window management. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `Cons_QHZcut()`, `Cons_QHZcut2()`, `zero()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
  - `mode`: Step size or energy/voltage increment used by the calculation.
- Source: `Models.ipf:5654`

#### `QHZ_d0mBerryCurvature`

```igor
Function QHZ_d0mBerryCurvature(numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_d0mBerryCurvature(numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZd0()`, `ChernN_QHZ()`.
- Parameters:
  - `numxy`: = 100
- Source: `Models.ipf:7044`

#### `QHZ_d0mBerryCurvaturemu`

```igor
Function QHZ_d0mBerryCurvaturemu(numxy,m,u)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_d0mBerryCurvaturemu(numxy, m, u)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZd0mu()`, `ChernN_QHZ()`.
- Parameters:
  - `numxy`: = 100
  - `m`: = 100
  - `u`: = 100
- Source: `Models.ipf:7581`

#### `QHZ_d0pBerryCurvature`

```igor
Function QHZ_d0pBerryCurvature(numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_d0pBerryCurvature(numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZd0()`, `ChernN_QHZ()`.
- Parameters:
  - `numxy`: = 100
- Source: `Models.ipf:6859`

#### `QHZ_d0pBerryCurvaturemu`

```igor
Function QHZ_d0pBerryCurvaturemu(numxy,m,u)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_d0pBerryCurvaturemu(numxy, m, u)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZd0mu()`, `ChernN_QHZ()`.
- Parameters:
  - `numxy`: = 100
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `u`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:7395`

#### `QHZ_hmBerryCurvature`

```igor
Function QHZ_hmBerryCurvature(numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_hmBerryCurvature(numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `qHZeq5hphm()`, `ChernN_QHZ()`.
- Parameters:
  - `numxy`: = 100
- Source: `Models.ipf:6175`

#### `QHZ_hmBerryCurvatured`

```igor
Function QHZ_hmBerryCurvatured(d,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_hmBerryCurvatured(d, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `qHZeq5hphmdelta()`, `ChernN_QHZ()`.
- Parameters:
  - `d`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: = 100
- Source: `Models.ipf:6615`

#### `QHZ_hpBerryCurvature`

```igor
Function QHZ_hpBerryCurvature(numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_hpBerryCurvature(numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `qHZeq5hphm()`, `ChernN_QHZ()`.
- Parameters:
  - `numxy`: = 100
- Source: `Models.ipf:5989`

#### `QHZ_hpBerryCurvatured`

```igor
Function QHZ_hpBerryCurvatured(d,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZ_hpBerryCurvatured(d, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `qHZeq5hphmdelta()`, `ChernN_QHZ()`.
- Parameters:
  - `d`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: = 100
- Source: `Models.ipf:6428`

#### `QHZBCFukui`

```igor
Function QHZBCFukui(A,B,m,mu,ReD,ImD,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZBCFukui(A, B, m, mu, ReD, ImD, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZ()`, `di()`, `ChernN_qhz()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: = 100
- Source: `Models.ipf:8115`

#### `QHZBerryCurvature`

```igor
Function QHZBerryCurvature(A,B,M,mu,ReD,ImD,numxy)
```
- Usage: call as `QHZBerryCurvature(A, B, M, mu, ReD, ImD, numxy)`.
- Notes: Failed trails to calculate Berry phase directly on BdG Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZ()`, `di()`, `ChernN_QHZ()`.
- Parameters:
  - `A`: = 1
  - `B`: = 0.2
  - `M`: = 0.2
  - `mu`: = 0.2
  - `ReD`: = 0.2
  - `ImD`: = 0.2
  - `numxy`: = 100
- Source: `Models.ipf:7801`

#### `QHZd0`

```igor
Function QHZd0(kx, ky)
```
- Usage: call as `QHZd0(kx, ky)`.
- Notes: Calculate Chern number when d = 0 Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves.
- Parameters:
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
- Source: `Models.ipf:6804`

#### `QHZd0Chernlinemm`

```igor
Function QHZd0Chernlinemm(mm)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZd0Chernlinemm(mm)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `QHZ_d0pBerryCurvaturemu()`, `ChernN_QHZ()`, `QHZ_d0mBerryCurvaturemu()`, `rgb()`, `marker()`, `mrkThick()`.
- Parameters:
  - `mm`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:7305`

#### `QHZd0Chernlineuu`

```igor
Function QHZd0Chernlineuu(uu)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZd0Chernlineuu(uu)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `QHZ_d0pBerryCurvaturemu()`, `ChernN_QHZ()`, `QHZ_d0mBerryCurvaturemu()`, `rgb()`, `marker()`, `mrkThick()`.
- Parameters:
  - `uu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:7245`

#### `QHZd0mu`

```igor
Function QHZd0mu(kx,ky,mm,u)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZd0mu(kx, ky, mm, u)`.
- Notes: Function for topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves.
- Parameters:
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
  - `mm`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `u`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:7361`

#### `QHZedgeH1`

```igor
Function/wave QHZedgeH1(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH1(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 01 S [real] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpn()`, `diagM()`, `superdiagM()`, `subdiagM()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8369`

#### `QHZedgeH2`

```igor
Function/wave QHZedgeH2(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH2(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 02 S+iS [complex] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpc()`, `diagM_cmplx()`, `superdiagM_cmplx()`, `subdiagM_cmplx()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8393`

#### `QHZedgeH3`

```igor
Function/wave QHZedgeH3(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH3(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 03 P+iP [real] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpn()`, `diagM()`, `superdiagM()`, `subdiagM()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8417`

#### `QHZedgeH4`

```igor
Function/wave QHZedgeH4(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH4(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 04 S± [real] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpn()`, `diagM()`, `superdiagM()`, `subdiagM()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8440`

#### `QHZedgeH5`

```igor
Function/wave QHZedgeH5(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH5(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 05 dx2-y2 [real] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpn()`, `diagM()`, `superdiagM()`, `subdiagM()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8464`

#### `QHZedgeH6`

```igor
Function/wave QHZedgeH6(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH6(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 06 dxy [Complex] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpc()`, `diagM_cmplx()`, `superdiagM_cmplx()`, `subdiagM_cmplx()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8488`

#### `QHZedgeH7`

```igor
Function/wave QHZedgeH7(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH7(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 07 Px [real] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpn()`, `diagM()`, `superdiagM()`, `subdiagM()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8513`

#### `QHZedgeH8`

```igor
Function/wave QHZedgeH8(A,B,m,mu,ReD,ImD,kx,nslab)
```
- Usage: call as `QHZedgeH8(A, B, m, mu, ReD, ImD, kx, nslab)`.
- Notes: ## 08 Py [Complex] Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing. Main internal calls: `mpc()`, `diagM_cmplx()`, `superdiagM_cmplx()`, `subdiagM_cmplx()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:8537`

#### `qHZeq5hphm`

```igor
Function qHZeq5hphm(kx, ky)
```
- Usage: call as `qHZeq5hphm(kx, ky)`.
- Notes: # PRB 82, 184516 eq. (5) calculate the chern number of m = 0 case Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves.
- Parameters:
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
- Source: `Models.ipf:5960`

#### `qHZeq5hphmdelta`

```igor
Function qHZeq5hphmdelta(kx,ky,d)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `qHZeq5hphmdelta(kx, ky, d)`.
- Notes: Function for topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves.
- Parameters:
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
  - `d`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:6401`

#### `QHZeq8Chernline`

```igor
Function QHZeq8Chernline()
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call `QHZeq8Chernline()` from Igor procedure code or the command line.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `QHZ_hpBerryCurvatured()`, `ChernN_QHZ()`, `QHZ_hmBerryCurvatured()`, `rgb()`, `ckfig()`.
- Source: `Models.ipf:6361`

#### `QHZLDOS`

```igor
Function QHZLDOS(A,B,m,mu,ReD,ImD,nslab,num,Vmin,Vmax)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZLDOS(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `SolveslabLDOS_QHZ_cmplx()`, `SolveslabLDOS_QHZ()`, `MergeRoweach()`, `di()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `Vmin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Vmax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:9266`

#### `QHZLDOSindex`

```igor
Function QHZLDOSindex(A,B,m,mu,ReD,ImD,nslab,num)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZLDOSindex(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `Solveslabindex_QHZ_cmplx()`, `Solveslabindex_QHZ()`, `MergeColumneach()`, `di()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:8995`

#### `QHZslabcut`

```igor
Function QHZslabcut(A,B,m,mu,ReD,ImD,selfimg,nslab,numxy,ratio,xi,v1,v2)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZslabcut(A, B, m, mu, ReD, ImD, selfimg, nslab)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZedgeH1()`, `QHZedgeH3()`, `QHZedgeH4()`, `QHZedgeH5()`, `QHZedgeH7()`, `sortColumnindex()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
  - `ratio`: Scaling, normalization, interpolation, or correction factor.
  - ... 3 additional parameters
- Source: `Models.ipf:10273`

#### `QHZslabcut_cmplx`

```igor
Function QHZslabcut_cmplx(A,B,m,mu,ReD,ImD,selfimg,nslab,numxy,ratio,xi,v1,v2)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QHZslabcut_cmplx(A, B, m, mu, ReD, ImD, selfimg, nslab)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `QHZedgeH2()`, `QHZedgeH6()`, `QHZedgeH8()`, `sortColumnindex()`, `MergeColumneach()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: Number of spectra, waves, points, bins, or iterations to process.
  - `ratio`: Scaling, normalization, interpolation, or correction factor.
  - ... 3 additional parameters
- Source: `Models.ipf:9479`

#### `QHZSpectralFunction`

```igor
Function QHZSpectralFunction(A,B,m,mu,ReD,ImD,selfimg,numxy)
```
- Usage: call as `QHZSpectralFunction(A, B, m, mu, ReD, ImD, selfimg, numxy)`.
- Notes: 3D Spetral Function QHZSpectralFunction(1,1,0.5,0,1,0,0.1,20) Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `QHZ()`, `tpw()`, `max()`, `grabwinchild()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:5540`

#### `QHZT`

```igor
Function QHZT()
```
- Usage: call `QHZT()` from Igor procedure code or the command line.
- Notes: Text the form of Hamiltonian We find the current form missing the PH0(k)P^-1 = -conj(H0(-k)) Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Main internal calls: `wT()`, `Wn()`, `automatrixT()`.
- Source: `Models.ipf:5424`

#### `QPIESimu2Dband`

```igor
Function QPIESimu2Dband(shape_hole,shape_ele,mu_hole,mu_ele,gap_hole,gap_ele,selfE,sel)
```
- Usage: call as `QPIESimu2Dband(shape_hole, shape_ele, mu_hole, mu_ele, gap_hole, gap_ele, selfE, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Correlate()`, `zero()`, `nticks()`, `max()`.
- Parameters:
  - `shape_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `shape_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `gap_hole`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `gap_ele`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `selfE`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:12367`

#### `QPIESimu2Dband_interact`

```igor
Function QPIESimu2Dband_interact(shape_hole,shape_ele,mu_hole,mu_ele,gap_hole,gap_ele,selfE,sel)
```
- Usage: call as `QPIESimu2Dband_interact(shape_hole, shape_ele, mu_hole, mu_ele, gap_hole, gap_ele, selfE, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; uses Igor matrix operations for vectorized calculation. Main internal calls: `Correlate()`.
- Parameters:
  - `shape_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `shape_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `gap_hole`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `gap_ele`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `selfE`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:12979`

#### `QPIESimu2Dband_interactFS`

```igor
Function QPIESimu2Dband_interactFS(shape_hole,shape_ele,mu_hole,mu_ele,gap_hole,gap_ele,selfE,sel)
```
- Usage: call as `QPIESimu2Dband_interactFS(shape_hole, shape_ele, mu_hole, mu_ele, gap_hole, gap_ele, selfE, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; uses Igor matrix operations for vectorized calculation. Main internal calls: `Correlate()`.
- Parameters:
  - `shape_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `shape_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `gap_hole`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `gap_ele`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `selfE`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:13144`

#### `QPIESimu_getGMcut`

```igor
Function QPIESimu_getGMcut(shape_hole,shape_ele,mu_hole,mu_ele,gap_hole,gap_ele,selfE,sel)
```
- Usage: call as `QPIESimu_getGMcut(shape_hole, shape_ele, mu_hole, mu_ele, gap_hole, gap_ele, selfE, sel)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; uses Igor matrix operations for vectorized calculation; cleans up waves/windows or brings an existing window forward. Main internal calls: `Correlate()`, `di()`, `sumoned()`.
- Parameters:
  - `shape_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `shape_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_hole`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu_ele`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `gap_hole`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `gap_ele`: Gap/amplitude parameter used by superconducting or spectral fitting routines.
  - `selfE`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `sel`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:13312`

#### `QWZ`

```igor
Function/Wave QWZ(A,B,m,kx,ky,mu)
```
- Usage: call as `QWZ(A, B, m, kx, ky, mu)`.
- Notes: Qi-Wu-Zhang Model Ref: Duncan Haldane Phys. Rev. B 74, 085308 (2006) Ref: https://arxiv.org/abs/1509.02295 Ref: https://www.weizmann.ac.il/condmat/oreg/sites/condmat.oreg/files/uploads/amittulchinskythesisfinal.pdf Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `kx`: Momentum or Q-vector coordinate/index.
  - `ky`: Momentum or Q-vector coordinate/index.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:4606`

#### `QWZBerryCurvature`

```igor
Function QWZBerryCurvature(A,B,m,mu,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QWZBerryCurvature(A, B, m, mu, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QWZ()`, `di()`, `ChernN_QWZ()`, `StopMSTimer()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numxy`: = 100
- Source: `Models.ipf:4722`

#### `QWZChernline`

```igor
Function QWZChernline(A,B,mu)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QWZChernline(A, B, mu)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `cons_QWZBC()`, `ChernN_QWZ()`, `ckfig()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:5376`

#### `QWZSpectralFunction`

```igor
Function QWZSpectralFunction(A,B,m,mu,selfimg,numxy)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `QWZSpectralFunction(A, B, m, mu, selfimg, numxy)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; extracts image/wave statistics. Main internal calls: `QWZ()`, `tpw()`, `max()`, `grabwinchild()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:4644`

#### `QWZSpectralFunction2`

```igor
Function QWZSpectralFunction2(A,B,m,mu,selfimg,numxy)
```
- Usage: call as `QWZSpectralFunction2(A, B, m, mu, selfimg, numxy)`.
- Notes: Simutaneous Qi-Wu-Zhang Fermi surface and G-X bands (Normal states) Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `QWZ()`, `di()`, `zero()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: Igor wave or matrix input consumed by the calculation.
  - `numxy`: = 100
- Source: `Models.ipf:9724`

#### `RefY`

```igor
Function RefY(name)
```
- Usage: call as `RefY(name)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Update log: 10/31/2023: (7) RefY(name): Reflect Y axis of the data matrix.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Models.ipf:9235`

#### `shift2pi`

```igor
Function shift2pi(name)
```
- Usage: call as `shift2pi(name)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Models.ipf:10585`

#### `ShrinkDigit`

```igor
Function ShrinkDigit(value, numSigDigits)
```
- Usage: call as `ShrinkDigit(value, numSigDigits)`.
- Notes: #06 Shrink digit Purpose: displays waves, images, contours, or graph overlays. Main internal calls: `str2num()`. Update log: 10/18_02/2023: added ShrinkDigit(value, numSigDigits) to shrink a variable to some digit numbers.
- Parameters:
  - `value`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `numSigDigits`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:3898`

#### `simuCDW`

```igor
Function simuCDW(A1,theta,wLength,c1,c2,c3)
```
- Usage: call as `simuCDW(A1, theta, wLength, c1, c2, c3)`.
- Notes: Function for matrix resampling, scaling, or reshaping; lattice, moire, or twist-angle simulation/analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `A1`: ,p1,p2,p3
  - `theta`: ,p1,p2,p3
  - `wLength`: ,p1,p2,p3
  - `c1`: ,p1,p2,p3
  - `c2`: ,p1,p2,p3
  - `c3`: ,p1,p2,p3
- Source: `Models.ipf:13711`

#### `SimulateDCB_IV`

```igor
Function SimulateDCB_IV(Pconv, RT, T, Vrange, NV)
```
- Notes: NIN tunneling simulate Dynamical Coulomb blockade Calculate the eq.(1) of PRL 110, 157003 (2013) Usage: call as `SimulateDCB_IV(Pconv, RT, T, Vrange, NV)`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; differentiates wave data.
- Parameters:
  - `Pconv`: P(E) function
  - `RT`: Junction resistance (Ohm)
  - `T`: Temperature (K)
  - `Vrange`: Bias range for calculation [-Vrang, Vrange] (V)
  - `NV`: Number of point
- Source: `ModelingTunneling.ipf:986`

#### `SimulateRCSJ_IV_Hysteresis_Realistic`

```igor
Function SimulateRCSJ_IV_Hysteresis_Realistic(T,beta_c,sm)
```
- Notes: Main Function #01 Simulate I(V) curve Usage: call as `SimulateRCSJ_IV_Hysteresis_Realistic(T, beta_c, sm)`. Code behavior: creates output waves; smooths wave data. Main internal calls: `gnoise()`, `mean()`.
- Parameters:
  - `T`: 温度 (K)
  - `beta_c`: = 8
  - `sm`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `ModelingTunneling.ipf:1491`

#### `SimulateRCSJ_voltage_vs_Time`

```igor
Function SimulateRCSJ_voltage_vs_Time(TT,current,beta_c)
```
- Notes: Main Function #02 Simulate V(t) curve Usage: call as `SimulateRCSJ_voltage_vs_Time(TT, current, beta_c)`. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `gnoise()`.
- Parameters:
  - `TT`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `current`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `beta_c`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `ModelingTunneling.ipf:1647`

#### `SimuPDW1D`

```igor
Function SimuPDW1D(delta0,deltap,FWHM,T,Edb)
```
- Usage: call as `SimuPDW1D(delta0, deltap, FWHM, T, Edb)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `delta0`: Step size or energy/voltage increment used by the calculation.
  - `deltap`: Step size or energy/voltage increment used by the calculation.
  - `FWHM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `T`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Edb`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:11884`

#### `SimuPDW1Dbody`

```igor
Function SimuPDW1Dbody(delta0,deltap,FWHM,T,Edb)
```
- Usage: call as `SimuPDW1Dbody(delta0, deltap, FWHM, T, Edb)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `SimuPDW1D()`, `sum2dlinecut()`, `FFTLpdw()`, `rgb()`, `color3s_for3dmf()`, `ckfig_child()`.
- Parameters:
  - `delta0`: Step size or energy/voltage increment used by the calculation.
  - `deltap`: Step size or energy/voltage increment used by the calculation.
  - `FWHM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `T`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Edb`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:11803`

#### `SimuPDW2D`

```igor
Function SimuPDW2D(delta0,deltap,FWHM,T,Edb)
```
- Usage: call as `SimuPDW2D(delta0, deltap, FWHM, T, Edb)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; reads or updates panel controls; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `delta0`: Step size or energy/voltage increment used by the calculation.
  - `deltap`: Step size or energy/voltage increment used by the calculation.
  - `FWHM`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `T`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Edb`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:12031`

#### `SIMUtopodefecthalf`

```igor
Function SIMUtopodefecthalf(a1,a2,add,lattice,zero,amp,x0,y0,wind)
```
- Usage: call as `SIMUtopodefecthalf(a1, a2, add, lattice, zero, amp, x0, y0)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Main internal calls: `atan2()`.
- Parameters:
  - `a1`: = 190 // angle of dislocation
  - `a2`: = 50 // angle of lattice vector q
  - `add`: = 1.3 // added phase
  - `lattice`: = 10//lattice constance
  - `zero`: = 1.1 //background
  - `amp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `wind`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Models.ipf:10544`

#### `SIMUtopodefectsingle`

```igor
Function SIMUtopodefectsingle(a1,a2,add,lattice,zero,amp,x0,y0,wind)
```
- Usage: call as `SIMUtopodefectsingle(a1, a2, add, lattice, zero, amp, x0, y0)`.
- Notes: Function for Nanonis/STM data loading or map conversion; FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output. Main internal calls: `atan2()`, `shift2pi()`.
- Parameters:
  - `a1`: = 190 // angle of dislocation
  - `a2`: = 50 // angle of lattice vector q
  - `add`: = 1.3 // added phase
  - `lattice`: = 10//lattice constance
  - `zero`: = 1.1 //background
  - `amp`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `y0`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `wind`: Igor wave reference used as input or output. In this context it is used for Fourier/QPI or phase-map processing.
- Source: `Models.ipf:10565`

#### `Solveedgestate_QHZ`

```igor
Function Solveedgestate_QHZ(A,B,m,mu,ReD,ImD,nslab,num)
```
- Usage: call as `Solveedgestate_QHZ(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Slab functions for real matrix Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `QHZedgeH1()`, `QHZedgeH3()`, `QHZedgeH4()`, `QHZedgeH5()`, `QHZedgeH7()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:9896`

#### `Solveedgestate_QHZ_cmplx`

```igor
Function Solveedgestate_QHZ_cmplx(A,B,m,mu,ReD,ImD,nslab,num)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `Solveedgestate_QHZ_cmplx(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `QHZedgeH2()`, `QHZedgeH6()`, `QHZedgeH8()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:8589`

#### `Solveedgestate_QHZhq`

```igor
Function Solveedgestate_QHZhq(A,B,m,mu,ReD,ImD,nslab,num)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `Solveedgestate_QHZhq(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `QHZedgeH1()`, `QHZedgeH3()`, `QHZedgeH4()`, `QHZedgeH5()`, `QHZedgeH7()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:9958`

#### `Solveedgestate_QHZhq_cmplx`

```igor
Function Solveedgestate_QHZhq_cmplx(A,B,m,mu,ReD,ImD,nslab,num)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `Solveedgestate_QHZhq_cmplx(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; topological-model simulation; ARPES-style loading, plotting, or momentum conversion. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `QHZedgeH2()`, `QHZedgeH6()`, `QHZedgeH8()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:8854`

#### `Solveslabindex_QHZ`

```igor
Function Solveslabindex_QHZ(A,B,m,mu,ReD,ImD,nslab,num)
```
- Usage: call as `Solveslabindex_QHZ(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function to calculate Eigenvalue index resolved LDOS Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `Indexofmatrix()`, `QHZedgeH1()`, `QHZedgeH3()`, `QHZedgeH4()`, `QHZedgeH5()`, `QHZedgeH7()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:10045`

#### `Solveslabindex_QHZ_cmplx`

```igor
Function Solveslabindex_QHZ_cmplx(A,B,m,mu,ReD,ImD,nslab,num)
```
- Usage: call as `Solveslabindex_QHZ_cmplx(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function to calculate Eigenvalue index resolved LDOS Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `Indexofmatrix_cmplx()`, `QHZedgeH2()`, `QHZedgeH6()`, `QHZedgeH8()`, `RefY()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Models.ipf:9017`

#### `SolveslabLDOS_QHZ`

```igor
Function SolveslabLDOS_QHZ(A,B,m,mu,ReD,ImD,nslab,num,Vmin,Vmax)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `SolveslabLDOS_QHZ(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `LDOSofmatrix_cmplx()`, `QHZedgeH1()`, `QHZedgeH3()`, `QHZedgeH4()`, `QHZedgeH5()`, `QHZedgeH7()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `Vmin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Vmax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:10218`

#### `SolveslabLDOS_QHZ_cmplx`

```igor
Function SolveslabLDOS_QHZ_cmplx(A,B,m,mu,ReD,ImD,nslab,num,Vmin,Vmax)
```
- Purpose: runs a model/simulation workflow and produces calculated spectra, bands, or maps.
- Usage: call as `SolveslabLDOS_QHZ_cmplx(A, B, m, mu, ReD, ImD, nslab, num)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; topological-model simulation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `LDOSofmatrix_cmplx()`, `QHZedgeH2()`, `QHZedgeH6()`, `QHZedgeH8()`.
- Parameters:
  - `A`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `B`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `m`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `mu`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ReD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ImD`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `nslab`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
  - `Vmin`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `Vmax`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:9284`

#### `sortColumnindex`

```igor
Function sortColumnindex(ref,src)
```
- Notes: Sort Column index of a matrix by ref wave Usage: call as `sortColumnindex(ref, src)`. Code behavior: creates output waves; duplicates or stages waves for downstream processing; cleans up waves/windows or brings an existing window forward. Update log: 10/31/2023: (2) sortColumnindex(ref,src): Sort the column order of src by the ref. This is useful to sort the eigenvector following the ordering of eigenvalue.
- Parameters:
  - `ref`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `src`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:9169`

#### `sum2dlinecut`

```igor
Function sum2dlinecut(name,zn,sufout,show)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: call as `sum2dlinecut(name, zn, sufout, show)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Update log: 11/26/2023: Added More functional PDW linecut simulation, can perform both 1Q and 2Q, as well as calculate the inelastic scattering as linear in E; Added a function to integrate one dimension get a spectrum for 2D data, sum2dlinecut($name,zn,sufout,show).
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `zn`: Layer/index value selecting a slice of a 3D wave or energy stack. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `sufout`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `show`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Models.ipf:11924`

#### `tconvchoiceparameter`

```igor
Function tconvchoiceparameter(tt2)
```
- Usage: call as `tconvchoiceparameter(tt2)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics; differentiates wave data.
- Parameters:
  - `tt2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `General_Simu.ipf:848`

#### `updateFeSC_normal`

```igor
Function updateFeSC_normal(t1,t2,t,tp,u,selfimg,selfreal)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `updateFeSC_normal(t1, t2, t, tp, u, selfimg, selfreal)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: assigns Igor axis scaling/units to the output.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: = 1
  - `t`: = 1
  - `tp`: = 1
  - `u`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: = 0.1
  - `selfreal`: = 0
- Source: `Models.ipf:11196`

#### `updateFeSC_normalFS`

```igor
Function updateFeSC_normalFS(t1,t2,t,tp,u,selfimg,selfreal)
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call as `updateFeSC_normalFS(t1, t2, t, tp, u, selfimg, selfreal)`.
- Notes: Function for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation.
- Parameters:
  - `t1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `t2`: = 1
  - `t`: = 1
  - `tp`: = 1
  - `u`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `selfimg`: = 0.1
  - `selfreal`: = 0
- Source: `Models.ipf:11154`

#### `updatefourbandFeSC2D`

```igor
Function updatefourbandFeSC2D()
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: call `updatefourbandFeSC2D()` from Igor procedure code or the command line.
- Notes: Function for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `mpc()`, `s0()`.
- Source: `Models.ipf:2831`

#### `updatePRB_98_214503_normal`

```igor
Function updatePRB_98_214503_normal()
```
- Usage: call `updatePRB_98_214503_normal()` from Igor procedure code or the command line.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Functional main function }}}}}}}}}} Purpose: updates parameters, controls, scales, or display state. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Models.ipf:1457`

#### `updatePRB_98_214503_SC`

```igor
Function updatePRB_98_214503_SC()
```
- Usage: call `updatePRB_98_214503_SC()` from Igor procedure code or the command line.
- Notes: &#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123;&#123; Functional main function }}}}}}}}}} Purpose: updates parameters, controls, scales, or display state. Code behavior: assigns Igor axis scaling/units to the output.
- Source: `Models.ipf:2008`

#### `UserFunctionG`

```igor
Function UserFunctionG(inX)
```
- Usage: call as `UserFunctionG(inX)`.
- Notes: Function for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation.
- Parameters:
  - `inX`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:666`

#### `UserFunctionsstunneling`

```igor
Function UserFunctionsstunneling(dE)//
```
- Usage: call as `UserFunctionsstunneling(dE)`.
- Notes: Function in `Models.ipf`. Main internal calls: `tanh()`.
- Parameters:
  - `dE`: Step size or energy/voltage increment used by the calculation.
- Source: `Models.ipf:260`

#### `vortexantivortexsimu`

```igor
Function vortexantivortexsimu(a_sep,AA,BB,vorticity,pshif,reverseV)
```
- Usage: call as `vortexantivortexsimu(a_sep, AA, BB, vorticity, pshif, reverseV)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `atan2()`, `makevectorfield_vortexsimu()`, `shrinkvf()`, `cyclecolorwave()`, `tpw()`.
- Parameters:
  - `a_sep`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `AA`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `BB`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `vorticity`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `pshif`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `reverseV`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:10632`

#### `vortexantivortexsimu_tune`

```igor
Function vortexantivortexsimu_tune(a_sep,AA,BB,vorticity,pshif,reverseV)
```
- Usage: call as `vortexantivortexsimu_tune(a_sep, AA, BB, vorticity, pshif, reverseV)`.
- Notes: Function for matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `atan2()`.
- Parameters:
  - `a_sep`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `AA`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `BB`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `vorticity`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `pshif`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `reverseV`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Models.ipf:10819`



## General Utilities and Internal Helpers

### Panel callbacks and control handlers

#### `ButtonProc_2ndDall`

```igor
Function ButtonProc_2ndDall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `secondDall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `2ndDif All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2ndDif All`
- Source: `Miscellaneous_Codes.ipf:6077`

#### `ButtonProc_addper`

```igor
Function ButtonProc_addper(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `addper()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `AverSlice (R9)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `AverSlice (R9)`
- Source: `Miscellaneous_Codes.ipf:8165`

#### `ButtonProc_addyall`

```igor
Function ButtonProc_addyall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `addyall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Y+`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Y+`
- Source: `Miscellaneous_Codes.ipf:6201`

#### `ButtonProc_appendVline`

```igor
Function ButtonProc_appendVline(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `appendVline()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Draw lines`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Draw lines`
- Source: `Miscellaneous_Codes.ipf:10179`

#### `ButtonProc_Areacurve`

```igor
Function ButtonProc_Areacurve(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Areacurve()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Area All`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Area All`
- Source: `Miscellaneous_Codes.ipf:6268`

#### `ButtonProc_AuFitkly`

```igor
Function ButtonProc_AuFitkly(ctrlName) : ButtonControl//"Convert All Data"
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Au Fit`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Au Fit`
- Source: `Miscellaneous_Codes.ipf:2107`

#### `ButtonProc_autoclbzero`

```igor
Function ButtonProc_autoclbzero(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `autoclbzero()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Individual Shift`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Individual Shift`
- Source: `Miscellaneous_Codes.ipf:8125`

#### `ButtonProc_autormjump2DPro`

```igor
Function ButtonProc_autormjump2DPro(ctrlName) : ButtonControl
```
- Notes: ckfig() and cklayout() to check if it is should be displayed or not automatically. Also the di() in Kong's Igor Panel was also updated, the internal codes can determine display or not to avoid redudant displays. These features can also be applied to other code Usage: triggered as a wrapper that opens/runs `autormjump2DPro()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:11133`

#### `ButtonProc_bkremover`

```igor
Function ButtonProc_bkremover(ctrlName) : ButtonControl
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: triggered as a wrapper that opens/runs `bkremoverp()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `BKG remover`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `BKG remover`
- Source: `Miscellaneous_Codes.ipf:1332`

#### `ButtonProc_calculate_v0`

```igor
Function ButtonProc_calculate_v0(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `calculate_v0()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:675`

#### `ButtonProc_comparea`

```igor
Function ButtonProc_comparea(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `compare()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Comp_MultiSet`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Comp_MultiSet`
- Source: `Miscellaneous_Codes.ipf:6984`

#### `ButtonProc_Controtate`

```igor
Function ButtonProc_Controtate(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Controtate()`.
- Notes: An interactive GUI for contineously rotate matrix Purpose: applies geometric correction or extracts deformation/strain information. Panel control(s): `adj`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `adj`
- Source: `Miscellaneous_Codes.ipf:160`

#### `ButtonProc_convertG`

```igor
Function ButtonProc_convertG(ctrlName) : ButtonControl//"Load BNL Data in Folder"
```
- Usage: triggered as a wrapper that opens/runs `tta()`.
- Notes: End of remove linear background from a waterfall Panel control(s): `CtG`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `CtG`
- Source: `Miscellaneous_Codes.ipf:11029`

#### `ButtonProc_correct2Dmapc`

```igor
Function ButtonProc_correct2Dmapc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `correct2Dmapc()`.
- Notes: and calculate the average, then put the 1D wave contains the information of zero bias into correct2Dmapc() Before launch this procedure, please make sure the waterfall of the gatemap is on graph and active if the zero bias 1D wave can be extracted from the sam Panel control(s): `Fix Gate Leak`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Fix Gate Leak`
- Source: `Miscellaneous_Codes.ipf:10694`

#### `ButtonProc_cutElarge`

```igor
Function ButtonProc_cutElarge(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `cutElarge()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `E+`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `E+`
- Source: `Miscellaneous_Codes.ipf:2050`

#### `ButtonProc_cutEsmall`

```igor
Function ButtonProc_cutEsmall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `cutEsmall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `E-`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `E-`
- Source: `Miscellaneous_Codes.ipf:2062`

#### `ButtonProc_cutklarge`

```igor
Function ButtonProc_cutklarge(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `cutklarge()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `k+`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `k+`
- Source: `Miscellaneous_Codes.ipf:2038`

#### `ButtonProc_cutksmall`

```igor
Function ButtonProc_cutksmall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `cutksmall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `k-`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `k-`
- Source: `Miscellaneous_Codes.ipf:2026`

#### `ButtonProc_CVT2EK_pi_renorm`

```igor
Function ButtonProc_CVT2EK_pi_renorm(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `CVT2EK_pi_renorm()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `θ to k`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `θ to k`
- Source: `Miscellaneous_Codes.ipf:665`

#### `ButtonProc_dataploter2`

```igor
Function ButtonProc_dataploter2(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `dataploter2()`.
- Notes: Data ploter2 change color Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `P2`. Code behavior: cleans up waves/windows or brings an existing window forward. Main internal calls: `wintype()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `P2`
- Source: `Miscellaneous_Codes.ipf:9636`

#### `ButtonProc_ddd`

```igor
Function ButtonProc_ddd(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Old Version Panel control(s): `Sgl.`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Sgl.`
- Source: `Miscellaneous_Codes.ipf:10268`

#### `ButtonProc_dddautoremovejump1DC`

```igor
Function ButtonProc_dddautoremovejump1DC(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `autoremovejump1Dc()`.
- Notes: New Version Purpose: removes waves/windows/data points or cleans intermediate state. Panel control(s): `Jump1D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Jump1D`
- Source: `Miscellaneous_Codes.ipf:10252`

#### `ButtonProc_Demoautormjump2DPro`

```igor
Function ButtonProc_Demoautormjump2DPro(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `Demoautormjump2DPro()`.
- Notes: Demo of the jump remover Panel control(s): `Demo: Trace Slanted Jumping Line`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Demo: Trace Slanted Jumping Line`
- Source: `Miscellaneous_Codes.ipf:11852`

#### `ButtonProc_diffeall`

```igor
Function ButtonProc_diffeall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `diffeall()`.
- Notes: From I(V) curve to dI/dV(V) curve by numerical differentiate Purpose: displays waves, images, contours, or graph overlays. Panel control(s): `Differentiate I/V`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Differentiate I/V`
- Source: `Miscellaneous_Codes.ipf:12795`

#### `ButtonProc_exitkm`

```igor
Function ButtonProc_exitkm(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `exitkm()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Clean`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Clean`
- Source: `Miscellaneous_Codes.ipf:10234`

#### `ButtonProc_extending1D`

```igor
Function ButtonProc_extending1D(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `extending1D()`.
- Notes: Panel button callback for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Panel control(s): `1D`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `1D`
- Source: `Miscellaneous_Codes.ipf:2079`

#### `ButtonProc_exwidth`

```igor
Function ButtonProc_exwidth(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `exwidth()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Extract_Width`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Extract_Width`
- Source: `Miscellaneous_Codes.ipf:8087`

#### `ButtonProc_findmaxBatch`

```igor
Function ButtonProc_findmaxBatch(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `findmaxBatch()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Max in Batch`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Max in Batch`
- Source: `Miscellaneous_Codes.ipf:9390`

#### `ButtonProc_fix_map`

```igor
Function ButtonProc_fix_map(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `fix_map()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Pt`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Pt`
- Source: `Miscellaneous_Codes.ipf:6962`

#### `ButtonProc_fixline`

```igor
Function ButtonProc_fixline(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `fixline()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Line`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Line`
- Source: `Miscellaneous_Codes.ipf:6929`

#### `ButtonProc_IExy`

```igor
Function ButtonProc_IExy(ctrlName) : ButtonControl
```
- Notes: This Part procedured for QPI. (3D Map) Usage: triggered as a wrapper that opens/runs `mapforSTM()`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:7258`

#### `ButtonProc_intereractionMBS`

```igor
Function ButtonProc_intereractionMBS(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `eval2()`.
- Notes: Reference PRL 103, 107001 (2009) Panel control(s): `MBS Split`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MBS Split`
- Source: `Miscellaneous_Codes.ipf:7207`

#### `ButtonProc_Isolate2C`

```igor
Function ButtonProc_Isolate2C(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `AnaTeSe()`.
- Notes: Follow part is procedure to Analyse two compositions of a topograph Panel control(s): `Ratio`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Ratio`
- Source: `Miscellaneous_Codes.ipf:5818`

#### `ButtonProc_legend_distance_vs_V`

```igor
Function ButtonProc_legend_distance_vs_V(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for graph display, formatting, or window management. Panel control(s): `d/V`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `d/V`
- Source: `Miscellaneous_Codes.ipf:5504`

#### `ButtonProc_length`

```igor
Function ButtonProc_length(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linel()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Length`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Length`
- Source: `Miscellaneous_Codes.ipf:3202`

#### `ButtonProc_LiFeAsfindmiushift`

```igor
Function ButtonProc_LiFeAsfindmiushift(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `LiFeAsfindmiushift()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `µ-shift LFA`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `µ-shift LFA`
- Source: `Miscellaneous_Codes.ipf:9475`

#### `ButtonProc_line2p`

```igor
Function ButtonProc_line2p(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `linenorm2p()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Old LnNorm2P`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Old LnNorm2P`
- Source: `Miscellaneous_Codes.ipf:5709`

#### `ButtonProc_LoadDA30`

```igor
Function ButtonProc_LoadDA30(ctrlName) : ButtonControl//"Load SSRFDA30 in Folder"
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: triggered as a wrapper that opens/runs `loadDA30_kly()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `LDA30`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LDA30`
- Source: `Miscellaneous_Codes.ipf:650`

#### `ButtonProc_MBE1MLTime`

```igor
Function ButtonProc_MBE1MLTime(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `MBE1MLTime()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `MBE_1MLTime`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `MBE_1MLTime`
- Source: `Miscellaneous_Codes.ipf:685`

#### `ButtonProc_myall`

```igor
Function ButtonProc_myall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `myall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Y*`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Y*`
- Source: `Miscellaneous_Codes.ipf:6170`

#### `ButtonProc_Newrotateproc`

```igor
Function ButtonProc_Newrotateproc(ctrlName) : ButtonControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: triggered as a wrapper that opens/runs `Newrotateproc()`.
- Notes: Panel button callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rot`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rot`
- Source: `Miscellaneous_Codes.ipf:26`

#### `ButtonProc_Normdivide`

```igor
Function ButtonProc_Normdivide(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `normlizeformap()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `Norm for map`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Norm for map`
- Source: `Miscellaneous_Codes.ipf:2006`

#### `ButtonProc_reorg2ndd`

```igor
Function ButtonProc_reorg2ndd(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `reorg2ndd()`.
- Notes: Input the name of a 2D matrix, convert the x scale from wavelength (nm) to Energy (eV) This procedure works on interpolate data, the 2D matrix is the matrix after removing uneven Y space. Panel control(s): `LtE`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `LtE`
- Source: `Miscellaneous_Codes.ipf:10527`

#### `ButtonProc_rotall`

```igor
Function ButtonProc_rotall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `rotall()`.
- Notes: END the QPI part Purpose: applies geometric correction or extracts deformation/strain information.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:7955`

#### `ButtonProc_savefitdata`

```igor
Function ButtonProc_savefitdata(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `savefitdata()`.
- Notes: before use this procedure you need set datafolder to fit folder Purpose: saves, exports, or converts data between files and Igor waves. Panel control(s): `Reader_Mpk2.0`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Reader_Mpk2.0`
- Source: `Miscellaneous_Codes.ipf:6532`

#### `ButtonProc_savepic`

```igor
Function ButtonProc_savepic(ctrlName) : ButtonControl
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback in `Miscellaneous_Codes.ipf`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:10228`

#### `ButtonProc_selectlinenorm`

```igor
Function ButtonProc_selectlinenorm(ctrlName) : ButtonControl
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: triggered as a wrapper that opens/runs `selectlinenorm()`.
- Notes: Panel button callback for linecut, slice, or region extraction; smoothing, normalization, or background removal. Panel control(s): `all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `all`
- Source: `Miscellaneous_Codes.ipf:6234`

#### `ButtonProc_sigma`

```igor
Function ButtonProc_sigma(ctrlName) : ButtonControl/////this proc use to evaluate the data qualities.
```
- Usage: triggered as a wrapper that opens/runs `findsigmaofdata()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Data_sigma`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Data_sigma`
- Source: `Miscellaneous_Codes.ipf:6475`

#### `ButtonProc_sizecurveauto`

```igor
Function ButtonProc_sizecurveauto(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `Auto`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Auto`
- Source: `Miscellaneous_Codes.ipf:5461`

#### `ButtonProc_sizecurvenone`

```igor
Function ButtonProc_sizecurvenone(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `None`. Code behavior: formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `None`
- Source: `Miscellaneous_Codes.ipf:5373`

#### `ButtonProc_sizecurveplan`

```igor
Function ButtonProc_sizecurveplan(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `Plan`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Plan`
- Source: `Miscellaneous_Codes.ipf:5441`

#### `ButtonProc_sizecurverect`

```igor
Function ButtonProc_sizecurverect(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Panel control(s): `Rect`. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Rect`
- Source: `Miscellaneous_Codes.ipf:5481`

#### `ButtonProc_sizeintenmap`

```igor
Function ButtonProc_sizeintenmap(ctrlName) : ButtonControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel button callback for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:5282`

#### `ButtonProc_slopedata`

```igor
Function ButtonProc_slopedata(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `slopedata()`.
- Notes: Data Normalization, slope distraction of the topography Panel control(s): `Slope`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Slope`
- Source: `Miscellaneous_Codes.ipf:9540`

#### `ButtonProc_slopedataall`

```igor
Function ButtonProc_slopedataall(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `slopeall()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `all`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `all`
- Source: `Miscellaneous_Codes.ipf:9545`

#### `ButtonProc_stepmove`

```igor
Function ButtonProc_stepmove(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `stepmove()`.
- Notes: This procudure is for calculating how many steps required for jumping from Bias pad to Sample Panel control(s): `Jump Cal`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Jump Cal`
- Source: `Miscellaneous_Codes.ipf:12627`

#### `ButtonProc_subsc`

```igor
Function ButtonProc_subsc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `subsc()`.
- Notes: This procedure is used with a dataset which each curve substrated by the first curve Panel control(s): `Subtset`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Subtset`
- Source: `Miscellaneous_Codes.ipf:9247`

#### `ButtonProc_summul`

```igor
Function ButtonProc_summul(ctrlName) : ButtonControl
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: triggered as a wrapper that opens/runs `summul()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `SumTrace`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `SumTrace`
- Source: `Miscellaneous_Codes.ipf:6303`

#### `ButtonProc_tdtodc`

```igor
Function ButtonProc_tdtodc(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `tdtodc()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `2to1`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `2to1`
- Source: `Miscellaneous_Codes.ipf:1824`

#### `ButtonProc_tempload`

```igor
Function ButtonProc_tempload(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `tempload()`.
- Notes: Panel button callback for linecut, slice, or region extraction.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Source: `Miscellaneous_Codes.ipf:9423`

#### `ButtonProc_twosetdifference`

```igor
Function ButtonProc_twosetdifference(ctrlName) : ButtonControl
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: triggered as a wrapper that opens/runs `twosetdifference()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Two set Diffe`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Two set Diffe`
- Source: `Miscellaneous_Codes.ipf:8962`

#### `ButtonProc_unevenlinep`

```igor
Function ButtonProc_unevenlinep(ctrlName) : ButtonControl
```
- Usage: triggered as a wrapper that opens/runs `unevenlinepc()`.
- Notes: Panel button callback for linecut, slice, or region extraction. Panel control(s): `Linecut from Random`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
- Panel controls: `Linecut from Random`
- Source: `Miscellaneous_Codes.ipf:13008`

#### `setvarplot2`

```igor
Function setvarplot2(ctrlName,varNum,varStr,varName): SetVariableControl
```
- Purpose: updates parameters, controls, scales, or display state.
- Usage: triggered as a wrapper that opens/runs `plotraw2()`.
- Notes: Utility function for linecut, slice, or region extraction; spectroscopy, superconducting-gap, or vortex-model analysis. Panel control(s): `Data`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Data`
- Source: `Miscellaneous_Codes.ipf:9671`

#### `SetVarProc_changecoherence`

```igor
Function SetVarProc_changecoherence(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `x`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `x`
- Source: `Miscellaneous_Codes.ipf:6401`

#### `SetVarProc_changeE`

```igor
Function SetVarProc_changeE(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `E (meV)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `E (meV)`
- Source: `Miscellaneous_Codes.ipf:7518`

#### `SetVarProc_changenpa`

```igor
Function SetVarProc_changenpa(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `V-linecut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `V-linecut`
- Source: `Miscellaneous_Codes.ipf:7538`

#### `SetVarProc_changenqa`

```igor
Function SetVarProc_changenqa(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `H-linecut`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `H-linecut`
- Source: `Miscellaneous_Codes.ipf:7528`

#### `SetVarProc_changeP`

```igor
Function SetVarProc_changeP(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `P`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `P`
- Source: `Miscellaneous_Codes.ipf:7495`

#### `SetVarProc_changeQ`

```igor
Function SetVarProc_changeQ(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Q`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Q`
- Source: `Miscellaneous_Codes.ipf:7508`

#### `SetVarProc_changeu`

```igor
Function SetVarProc_changeu(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction. Panel control(s): `Ef`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Ef`
- Source: `Miscellaneous_Codes.ipf:6384`

#### `SetVarProc_Controtate`

```igor
Function SetVarProc_Controtate(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `θ`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `θ`
- Source: `Miscellaneous_Codes.ipf:178`

#### `SetVarProc_Ener`

```igor
Function SetVarProc_Ener(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Usage: triggered as a wrapper that opens/runs `makeCEM()`.
- Notes: NVAR hv, V0, c, startx,endx Kzcons=0.51199*sqrt(hv-3.81485*(x)^2+V0) kzx=startx+(endx-startx)*x/999 End Panel control(s): `Binding Energy (meV)`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Binding Energy (meV)`
- Source: `Miscellaneous_Codes.ipf:1619`

#### `SetVarProc_rotatedegree`

```igor
Function SetVarProc_rotatedegree(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Rotate`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Rotate`
- Source: `Miscellaneous_Codes.ipf:7556`

#### `SetVarProc_rotatequality`

```igor
Function SetVarProc_rotatequality(ctrlName,varNum,varStr,varName) : SetVariableControl
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: called automatically by Igor when the linked panel control changes.
- Notes: Panel set-variable callback for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis. Panel control(s): `Quality`.
- Parameters:
  - `ctrlName`: Igor control name passed automatically by the panel callback.
  - `varNum`: Numeric value from an Igor SetVariable control.
  - `varStr`: String form of the SetVariable control value.
  - `varName`: Name of the Igor variable edited by the SetVariable control.
- Panel controls: `Quality`
- Source: `Miscellaneous_Codes.ipf:7565`


### Interactive procedures and macros

#### `addper`

```igor
proc addper(mat,per,fromz,size)
```
- Usage: run from Igor with parameters `mat`, `per`, `fromz`, `size`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Dialog prompts: mat = Source name; per = add how much curves; fromz = Source begin number; size = Numbers of source curve. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `summul2()`.
- Parameters:
  - `mat`: Source name
  - `per`: add how much curves
  - `fromz`: Source begin number
  - `size`: Numbers of source curve
- Source: `Miscellaneous_Codes.ipf:8169`

#### `addyall`

```igor
proc addyall(mat,totalnum,t)
```
- Usage: run from Igor with parameters `mat`, `totalnum`, `t`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; totalnum = numbers of the Batch; t = add how much. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `totalnum`: numbers of the Batch
  - `t`: add how much
- Source: `Miscellaneous_Codes.ipf:6206`

#### `adjustline`

```igor
Proc adjustline()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; extracts image/wave statistics.
- Source: `Miscellaneous_Codes.ipf:7702`

#### `adjustlineonimageH`

```igor
Proc adjustlineonimageH()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Miscellaneous_Codes.ipf:7691`

#### `adjustlineonimageV`

```igor
Proc adjustlineonimageV()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Miscellaneous_Codes.ipf:7696`

#### `AnaTeSe`

```igor
proc AnaTeSe(mat, largemat,i)
```
- Usage: run from Igor with parameters `mat`, `largemat`, `i`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: mat = Name of the Matrix to statistics; largemat = Name of the Matrix to find Reference; i = Do you want make isolate Map?. Code behavior: creates output waves; asks the user for parameters through Igor prompts. Main internal calls: `checkupdown()`, `checkupdown2()`.
- Parameters:
  - `mat`: Name of the Matrix to statistics
  - `largemat`: Name of the Matrix to find Reference
  - `i`: Do you want make isolate Map?
- Source: `Miscellaneous_Codes.ipf:5824`

#### `appendVline`

```igor
proc appendVline(yvalue,yincre,xrange,numbers,flag)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `yvalue`, `yincre`, `xrange`, `numbers`, `flag`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Dialog prompts: yvalue = start y value; yincre = increment of Y; xrange = x size of the line; numbers = How manys lines append; flag = append [1] or not [0]. Code behavior: opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `appendVlineF()`.
- Parameters:
  - `yvalue`: start y value
  - `yincre`: increment of Y
  - `xrange`: x size of the line
  - `numbers`: How manys lines append
  - `flag`: append [1] or not [0]
- Source: `Miscellaneous_Codes.ipf:10184`

#### `areacurve`

```igor
proc areacurve(matt,startx,endx,num)
```
- Usage: run from Igor with parameters `matt`, `startx`, `endx`, `num`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: matt = Name of the Batch; startx = start x value; endx = end x value; num = Number of the Batch. Code behavior: creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `area()`.
- Parameters:
  - `matt`: Name of the Batch
  - `startx`: start x value
  - `endx`: end x value
  - `num`: Number of the Batch
- Source: `Miscellaneous_Codes.ipf:6273`

#### `Auall2`

```igor
proc Auall2 (Tem,j)
```
- Usage: run from Igor with parameters `Tem`, `j`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `wintype()`, `Ceil()`, `func_adjust_to_fp()`, `destwave()`.
- Parameters:
  - `Tem`: Temperature parameter, usually in kelvin when used in spectral modeling.
  - `j`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:2431`

#### `AuFitkly`

```igor
Proc AuFitkly (Tem,j,choice)
```
- Purpose: fits or extracts spectral/peak parameters.
- Usage: run from Igor with parameters `Tem`, `j`, `choice`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: Tem = Temperature(K); j = Number of data(J=0 means output All ); choice = Do you want to fix FP. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `wintype()`, `Ceil()`, `func_adjust_to_fp()`, `destwave()`.
- Parameters:
  - `Tem`: Temperature(K)
  - `j`: Number of data(J=0 means output All )
  - `choice`: Do you want to fix FP
- Source: `Miscellaneous_Codes.ipf:2112`

#### `autoclbzero`

```igor
proc autoclbzero(mat,num,left,right)
```
- Usage: run from Igor with parameters `mat`, `num`, `left`, `right`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of Batch; num = Number of Batch; left = Fit From (Point); right = Fit To (Point). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays. Main internal calls: `pcsr()`, `displaymulti()`.
- Parameters:
  - `mat`: name of Batch
  - `num`: Number of Batch
  - `left`: Fit From (Point)
  - `right`: Fit To (Point)
- Source: `Miscellaneous_Codes.ipf:8130`

#### `autoclbzero2`

```igor
proc autoclbzero2(mat,num,left,right)
```
- Usage: run from Igor with parameters `mat`, `num`, `left`, `right`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of Batch; num = Number of Batch; left = Fit From (Point); right = Fit To (Point). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `pcsr()`.
- Parameters:
  - `mat`: name of Batch
  - `num`: Number of Batch
  - `left`: Fit From (Point)
  - `right`: Fit To (Point)
- Source: `Miscellaneous_Codes.ipf:9440`

#### `autoclbzerogateuse`

```igor
proc autoclbzerogateuse(mat,num,left,right)
```
- Usage: run from Igor with parameters `mat`, `num`, `left`, `right`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: mat = name of Batch; num = Number of Batch; left = Fit From (Point); right = Fit To (Point). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `pcsr()`.
- Parameters:
  - `mat`: name of Batch
  - `num`: Number of Batch
  - `left`: Fit From (Point)
  - `right`: Fit To (Point)
- Source: `Miscellaneous_Codes.ipf:12037`

#### `autoremovejump1Dc`

```igor
Proc autoremovejump1Dc(name,mode,valueif2)
```
- Purpose: removes waves/windows/data points or cleans intermediate state.
- Usage: run from Igor with parameters `name`, `mode`, `valueif2`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = the name of wave; mode = Auto or Given; valueif2 = The Given jump value, only active in Mode 2. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `autoremovejump1D()`.
- Parameters:
  - `name`: the name of wave
  - `mode`: Auto or Given
  - `valueif2`: The Given jump value, only active in Mode 2
- Source: `Miscellaneous_Codes.ipf:10256`

#### `autormjump2DPro`

```igor
Proc autormjump2DPro(name,jumpheight,killim,add1)
```
- Usage: run from Igor with parameters `name`, `jumpheight`, `killim`, `add1`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = The DATA INPUT (must be a 2D matrix); jumpheight = dZ threshhold for testing jumpping position; killim = kill intermedia wave (1), or not (0); add1 = unit of value add to data. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `autoromvejump2D()`.
- Parameters:
  - `name`: The DATA INPUT (must be a 2D matrix)
  - `jumpheight`: dZ threshhold for testing jumpping position
  - `killim`: kill intermedia wave (1), or not (0)
  - `add1`: unit of value add to data
- Source: `Miscellaneous_Codes.ipf:11137`

#### `compare`

```igor
proc compare(name,num,batchnum,factor)
```
- Usage: run from Igor with parameters `name`, `num`, `batchnum`, `factor`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: name = Batch name; num = Total number of One Set; batchnum = How many sets Data; Factor = Interpolate times. Code behavior: formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `interpsts2()`, `displaymulti2()`, `color_edc()`.
- Parameters:
  - `name`: Batch name
  - `num`: Total number of One Set
  - `batchnum`: How many sets Data
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:6989`

#### `Controtate`

```igor
Proc Controtate()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `tpw()`, `twoDinterpolatexycr()`, `dilf()`.
- Source: `Miscellaneous_Codes.ipf:164`

#### `csn`

```igor
Proc csn()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; smoothing, normalization, or background removal. Code behavior: smooths wave data. Main internal calls: `reextractL()`, `numslicehnam()`, `numslicevnam()`, `smoothfast()`, `linkstsmap_PqpiH()`, `linkstsmap_PqpiV()`.
- Source: `Miscellaneous_Codes.ipf:7648`

#### `cutElarge`

```igor
proc cutElarge(name,index)
```
- Usage: run from Igor with parameters `name`, `index`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `qcsr()`, `DeletePdata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:2054`

#### `cutEsmall`

```igor
proc cutEsmall(name,index)
```
- Usage: run from Igor with parameters `name`, `index`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `qcsr()`, `DeletePdata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:2066`

#### `cutklarge`

```igor
proc cutklarge(name,index)
```
- Usage: run from Igor with parameters `name`, `index`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `pcsr()`, `DeletePdata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:2042`

#### `cutksmall`

```igor
proc cutksmall(name,index)
```
- Usage: run from Igor with parameters `name`, `index`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction. Main internal calls: `pcsr()`, `DeletePdata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `index`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:2030`

#### `CVT2EK_pi_renorm`

```igor
Proc CVT2EK_pi_renorm (oldmat,answ,fe,phi,factor)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `oldmat`, `answ`, `fe`, `phi`, `factor`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; ARPES-style loading, plotting, or momentum conversion. Dialog prompts: oldmat = Enter the matrix to transform; answ = Do you want also to convert the energy?; fe = Enter the Fermi energy; phi = Enter the manipulator angle (for V polarization only); factor = Enter the factors(pi/a) to renormlize Kx. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `func_CVT2EK_pi()`.
- Parameters:
  - `oldmat`: Enter the matrix to transform
  - `answ`: Do you want also to convert the energy?
  - `fe`: Enter the Fermi energy
  - `phi`: Enter the manipulator angle (for V polarization only)
  - `factor`: Enter the factors(pi/a) to renormlize Kx
- Source: `Miscellaneous_Codes.ipf:893`

#### `ddd_ff`

```igor
Proc ddd_ff(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Main internal calls: `tpw()`, `ddd_f()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:10272`

#### `DeletePdata`

```igor
proc DeletePdata(name,flag,select,stp,edp,number2)
```
- Usage: run from Igor with parameters `name`, `flag`, `select`, `stp`, `edp`, `number2`.
- Notes: make specific dislay of the data. their are two Mode, First one is cutRange Mode, only Begining From startpoint range or Finished at endpoint range can be used. the second mode is hald slice mode, it can retain slice envery one slices. stp 1 means endpoints se Purpose: removes waves/windows/data points or cleans intermediate state.
- Parameters:
  - `name`: Batch name
  - `flag`: Please Select Mode
  - `select`: which Data to handel(0 means handling all)
  - `stp`: From points (Only for cutrange Mode)
  - `edp`: To points (Only for cutrange Mode)
  - `number2`: number of cuts (Only for handling all)
- Source: `Miscellaneous_Codes.ipf:1857`

#### `Demoautormjump2DPro`

```igor
Proc Demoautormjump2DPro()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Main internal calls: `Generatepjlmartrix()`, `autoromvejump2D()`, `cklayout()`, `di()`.
- Source: `Miscellaneous_Codes.ipf:11857`

#### `diffeall`

```igor
Proc diffeall(mat,totalnum,notes)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `mat`, `totalnum`, `notes`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: mat = Name of the Batch waves; totalnum = Numbers of the Batch. Code behavior: differentiates wave data; asks the user for parameters through Igor prompts. Update log: 2025-05-19: Differentiates a batch of I(V) curves to calculate dI/dV curves.
- Parameters:
  - `mat`: Name of the Batch waves
  - `totalnum`: Numbers of the Batch
  - `notes`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:12800`

#### `dsd`

```igor
proc dsd(name,n2)
```
- Usage: run from Igor with parameters `name`, `n2`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; formats graph axes, labels, colors, or annotations; cleans up waves/windows or brings an existing window forward. Main internal calls: `linkstsmap_Pev()`, `displaymulti()`, `di()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `n2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10551`

#### `duplicatpart`

```igor
proc duplicatpart(mat,mat2,suf,x1,x2,totalnum)
```
- Usage: run from Igor with parameters `mat`, `mat2`, `suf`, `x1`, `x2`, `totalnum`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; mat2 = duplicate to What?; totalnum = numbers of the Batch; suf = If you have a suffix,Please input. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `mat2`: duplicate to What?
  - `suf`: If you have a suffix,Please input
  - `x1`: Start point, index, or coordinate for the selected range.
  - `x2`: End point, index, or coordinate for the selected range.
  - `totalnum`: numbers of the Batch
- Source: `Miscellaneous_Codes.ipf:6022`

#### `eval2`

```igor
proc eval2(I,delta,kesai,Pf)// unit nm
```
- Usage: run from Igor with parameters `I`, `delta`, `kesai`, `Pf`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; matrix resampling, scaling, or reshaping; spectroscopy, superconducting-gap, or vortex-model analysis. Dialog prompts: I = Inter-Vortex length (nm); delta = SC gap (meV); kesai = Coherent Length (nm); Pf = Fermi Wavevector (A^-1). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts.
- Parameters:
  - `I`: Inter-Vortex length (nm)
  - `delta`: SC gap (meV)
  - `kesai`: Coherent Length (nm)
  - `Pf`: Fermi Wavevector (A^-1)
- Source: `Miscellaneous_Codes.ipf:7211`

#### `exitkm`

```igor
Proc exitkm()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion. Main internal calls: `KP_NanonisCleanup()`.
- Source: `Miscellaneous_Codes.ipf:10238`

#### `extending1D`

```igor
proc extending1D(name,point)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `name`, `point`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: name = The curve you want ro extending; point = How many point you want to add (each side). Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: The curve you want ro extending
  - `point`: How many point you want to add (each side)
- Source: `Miscellaneous_Codes.ipf:2083`

#### `extracnumfromstr`

```igor
Proc extracnumfromstr(head,volt)
```
- Usage: run from Igor with parameters `head`, `volt`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: head = Head String to eliminate; volt = Set point volatage (meV). Code behavior: creates output waves; duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts. Main internal calls: `str2num()`.
- Parameters:
  - `head`: Head String to eliminate
  - `volt`: Set point volatage (meV)
- Source: `Miscellaneous_Codes.ipf:8066`

#### `exwidth`

```igor
Proc exwidth(num,mat,st,ed)
```
- Usage: run from Igor with parameters `num`, `mat`, `st`, `ed`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: num = number of spectra; mat = name of Batch; st = From (point); ed = To (point). Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; asks the user for parameters through Igor prompts. Main internal calls: `pcsr()`, `rgb()`.
- Parameters:
  - `num`: number of spectra
  - `mat`: name of Batch
  - `st`: From (point)
  - `ed`: To (point)
- Source: `Miscellaneous_Codes.ipf:8092`

#### `findmaxBatch`

```igor
proc findmaxBatch(name,r1,r2)
```
- Usage: run from Igor with parameters `name`, `r1`, `r2`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Batch name; r1 = Start x; r2 = End x. Code behavior: creates output waves; extracts image/wave statistics; asks the user for parameters through Igor prompts.
- Parameters:
  - `name`: Batch name
  - `r1`: Start x
  - `r2`: End x
- Source: `Miscellaneous_Codes.ipf:9394`

#### `findsigmaofdata`

```igor
proc findsigmaofdata(mat,num,x1,x2,x3,x4)
```
- Usage: run from Igor with parameters `mat`, `num`, `x1`, `x2`, `x3`, `x4`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal. Dialog prompts: mat = Batch name; num = Batch number; X1 = Range 1 startx; X2 = Range 1 endx; X3 = Range 2 startx; X4 = Range 2 starty. Code behavior: creates output waves; smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `duplicateall()`.
- Parameters:
  - `mat`: Batch name
  - `num`: Batch number
  - `x1`: Start point, index, or coordinate for the selected range.
  - `x2`: End point, index, or coordinate for the selected range.
  - `x3`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `x4`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:6480`

#### `fix_map`

```igor
Proc fix_map()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Main internal calls: `pcsr()`, `qcsr()`.
- Source: `Miscellaneous_Codes.ipf:6967`

#### `fixline`

```igor
proc fixline()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`.
- Source: `Miscellaneous_Codes.ipf:6933`

#### `Generatepjlmartrix`

```igor
Proc Generatepjlmartrix(qx1,qy1,qx2,qy2,sigma,factor)
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run from Igor with parameters `qx1`, `qy1`, `qx2`, `qy2`, `sigma`, `factor`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `di()`.
- Parameters:
  - `qx1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy1`: Start point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qx2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `qy2`: End point, index, or coordinate for the selected range. In this context it is used for Fourier/QPI or phase-map processing.
  - `sigma`: Width or sigma parameter for smoothing, fitting, or Fourier filtering.
  - `factor`: Scaling, normalization, interpolation, or correction factor.
- Source: `Miscellaneous_Codes.ipf:11884`

#### `getslicerData`

```igor
Proc getslicerData(name,a,b,c,xs,ys,Es,Ed)
```
- Usage: run from Igor with parameters `name`, `a`, `b`, `c`, `xs`, `ys`, `Es`, `Ed`. Update-log guidance - 08/17/2023: The old version was created on early 2021 and was modified until late of 2022, the old version is functional by sequentially running (1) getslicerData(name,a,b,c,xs,ys,Es,Ed) (for slicing a 3D matrix <name>, to be multiple 2D matrix), (2) mapforSTMf(num,name,suff,start,delta) (combining them together to a 3D matrix again, (3) makeextra(), (launch the smarter plot).
- Notes: duplicate/o Root:Grid001:Grid001_LI_Demod_1_Y Root:mat3draw Purpose: extracts values, metadata, cursor information, or derived waves. Dialog prompts: name = Name of the 3D matrix; a = X is which index in Matrix? Row (0); Coloum (1); Layer (2); b = Y is which index in Matrix? Row (0); Coloum (1); Layer (2); c = E is which index in Matrix? Row (0); Coloum (1); Layer (2); xs = x-size; ys = y-size. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `getslicerDataf()`.
- Parameters:
  - `name`: Name of the 3D matrix
  - `a`: X is which index in Matrix? Row (0); Coloum (1); Layer (2)
  - `b`: Y is which index in Matrix? Row (0); Coloum (1); Layer (2)
  - `c`: E is which index in Matrix? Row (0); Coloum (1); Layer (2)
  - `xs`: x-size
  - `ys`: y-size
  - `Es`: Start Energy
  - `Ed`: Energy delta
- Source: `Miscellaneous_Codes.ipf:9707`

#### `getzbc`

```igor
Proc getzbc(l1,l2,r1,r2)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run from Igor with parameters `l1`, `l2`, `r1`, `r2`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Dialog prompts: l1 = V (left,1); l2 = V (left,2); r1 = V (right,2). Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `getzb()`.
- Parameters:
  - `l1`: V (left,1)
  - `l2`: V (left,2)
  - `r1`: V (right,2)
  - `r2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:10826`

#### `imtb`

```igor
proc imtb()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation.
- Source: `Procedure.ipf:3`

#### `intercurve`

```igor
proc intercurve(mat,totalnum,points)
```
- Usage: run from Igor with parameters `mat`, `totalnum`, `points`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: mat = name of the Batch waves; totalnum = numbers of the Batch; Points = How many points do you want. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `totalnum`: numbers of the Batch
  - `points`: =dimsize(sts1,0)
- Source: `Miscellaneous_Codes.ipf:5787`

#### `kly_mapping`

```igor
Proc kly_mapping(initial_angel,final_angel,map_delta,initial_index)
```
- Usage: run from Igor with parameters `initial_angel`, `final_angel`, `map_delta`, `initial_index`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Dialog prompts: initial_angel = Polar of mapping begining; map_delta = Polar delta; initial_index = index of data begining; final_angel = Polar of mapping ending. Code behavior: reshapes existing waves; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `MakeImageTable()`, `MakeCutTable()`, `Generate_chunklist()`, `Make_allchunkcuts()`, `allcuts()`, `mat3d_pir()`.
- Parameters:
  - `initial_angel`: Polar of mapping begining
  - `final_angel`: Polar of mapping ending
  - `map_delta`: Polar delta
  - `initial_index`: index of data begining
- Source: `Miscellaneous_Codes.ipf:691`

#### `LiFeAsfindmiushift`

```igor
Proc LiFeAsfindmiushift(length, combine, number,fits,fite)
```
- Usage: run from Igor with parameters `length`, `combine`, `number`, `fits`, `fite`.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; smoothing, normalization, or background removal; graph display, formatting, or window management. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `autodisplay()`, `secondDall()`, `myall()`, `Constantoffset_n()`, `autoclbzero2()`, `offtheset()`.
- Parameters:
  - `length`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `combine`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `number`: Number of spectra, waves, points, bins, or iterations to process. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `fits`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `fite`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:9479`

#### `linel`

```igor
proc linel()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `hcsr()`, `vcsr()`.
- Source: `Miscellaneous_Codes.ipf:3207`

#### `linenorm2p`

```igor
proc linenorm2p(mat)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `vcsr()`, `hcsr()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:5714`

#### `LoadDataalltypes2`

```igor
Proc LoadDataalltypes2()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Main internal calls: `LoadDataFromIgorFiles()`, `LoadDataFromtxt()`, `LoadDataFromdat()`, `LoadDataFromA1()`, `LoadDataFromasc()`.
- Source: `Miscellaneous_Codes.ipf:12078`

#### `LoadDataFromA1_KLY`

```igor
Proc LoadDataFromA1_KLY()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `Center_detector()`.
- Source: `Miscellaneous_Codes.ipf:1140`

#### `LoadDataFromasc`

```igor
Proc LoadDataFromasc()
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; loads data from files or paths; creates, switches, or removes Igor data folders. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`, `Center_detector()`.
- Source: `Miscellaneous_Codes.ipf:12099`

#### `LoadDataFromNewmk`

```igor
Proc LoadDataFromNewmk(x,y)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `x`, `y`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: x = Column Number of x wave; y = Column Number of y wave. Code behavior: duplicates or stages waves for downstream processing; loads data from files or paths; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`.
- Parameters:
  - `x`: Column Number of x wave
  - `y`: Column Number of y wave
- Source: `Miscellaneous_Codes.ipf:310`

#### `LoadDataFromNewmk_ave`

```igor
Proc LoadDataFromNewmk_ave(x,y1,y2)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `x`, `y1`, `y2`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: x = Column Number of x wave; y1 = Column Number of y1 wave to ave; y2 = Column Number of y2 wave to ave. Code behavior: duplicates or stages waves for downstream processing; loads data from files or paths; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`.
- Parameters:
  - `x`: Column Number of x wave
  - `y1`: Column Number of y1 wave to ave
  - `y2`: Column Number of y2 wave to ave
- Source: `Miscellaneous_Codes.ipf:471`

#### `LoadDataFromNewmkfab`

```igor
Proc LoadDataFromNewmkfab(x,y1,y2)
```
- Purpose: loads data or launches an automatic loading workflow.
- Usage: run from Igor with parameters `x`, `y1`, `y2`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Dialog prompts: x = Column Number of x wave; y1 = Column Number of y wave [Forward]; y2 = Column Number of y wave [Backward]. Code behavior: duplicates or stages waves for downstream processing; loads data from files or paths; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward; asks the user for parameters through Igor prompts. Main internal calls: `IndexedFile()`, `wintype()`, `winType()`, `sortlist()`.
- Parameters:
  - `x`: Column Number of x wave
  - `y1`: Column Number of y wave [Forward]
  - `y2`: Column Number of y wave [Backward]
- Source: `Miscellaneous_Codes.ipf:558`

#### `makeCEM`

```igor
proc makeCEM()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; cleans up waves/windows or brings an existing window forward. Main internal calls: `ceil()`.
- Source: `Miscellaneous_Codes.ipf:1763`

#### `makeextra`

```igor
Proc makeextra()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback. Update-log guidance - 08/17/2023: The old version was created on early 2021 and was modified until late of 2022, the old version is functional by sequentially running (1) getslicerData(name,a,b,c,xs,ys,Es,Ed) (for slicing a 3D matrix <name>, to be multiple 2D matrix), (2) mapforSTMf(num,name,suff,start,delta) (combining them together to a 3D matrix again, (3) makeextra(), (launch the smarter plot).
- Notes: Interactive Igor procedure for symmetry or reflection processing; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `makespec()`, `displaymulti()`, `constantoffset_n()`, `color_edc()`, `rgb()`, `lstyle()`. This is a stupid algorithm owing to retro back to early year, I am trying to make use of some well written procedures in the General ARPES template, in order to make them compatible, I need to slice every layer to be 2D image first.
- Source: `Miscellaneous_Codes.ipf:7348`

#### `makeu1u2`

```igor
proc makeu1u2()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; symmetry or reflection processing; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations; reads or updates panel controls. Main internal calls: `rgb()`, `remakeu1u2()`, `grid()`, `nticks()`.
- Source: `Miscellaneous_Codes.ipf:6342`

#### `mapforSTM`

```igor
Proc mapforSTM(num,name,suff,start,delta)
```
- Usage: run from Igor with parameters `num`, `name`, `suff`, `start`, `delta`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Dialog prompts: num = Number of spatial Map; name = Name of Map; suff = For smooth data use _VSHS; start = Enrgy start (meV); Delta = Energy delta (meV). Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; smooths wave data; asks the user for parameters through Igor prompts.
- Parameters:
  - `num`: Number of spatial Map
  - `name`: Name of Map
  - `suff`: For smooth data use _VSHS
  - `start`: Enrgy start (meV)
  - `delta`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:7262`

#### `mapforSTMf`

```igor
Proc mapforSTMf(num,name,suff,start,delta)
```
- Usage: run from Igor with parameters `num`, `name`, `suff`, `start`, `delta`. Update-log guidance - 08/17/2023: The old version was created on early 2021 and was modified until late of 2022, the old version is functional by sequentially running (1) getslicerData(name,a,b,c,xs,ys,Es,Ed) (for slicing a 3D matrix <name>, to be multiple 2D matrix), (2) mapforSTMf(num,name,suff,start,delta) (combining them together to a 3D matrix again, (3) makeextra(), (launch the smarter plot).
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; geometric correction, rotation, shear, drift, or strain analysis; smoothing, normalization, or background removal. Dialog prompts: num = Number of spatial Map; name = Name of Map; suff = For smooth data use _VSHS; start = Enrgy start (meV); Delta = Energy delta (meV). Code behavior: smooths wave data; asks the user for parameters through Igor prompts. Main internal calls: `mapforSTMff()`.
- Parameters:
  - `num`: Number of spatial Map
  - `name`: Name of Map
  - `suff`: For smooth data use _VSHS
  - `start`: Enrgy start (meV)
  - `delta`: = deltaE
- Source: `Miscellaneous_Codes.ipf:9843`

#### `MBE1MLTime`

```igor
Proc MBE1MLTime(Flux,SM,cM,NM,NT,ST,cT)//valid only self-confined growth mode and  with enough subordinate elemet
```
- Usage: run from Igor with parameters `Flux`, `SM`, `cM`, `NM`, `NT`, `ST`, `cT`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Dialog prompts: Flux = MainElement Flux(A/s); SM = MainElement inplaneArea1UC(A2); cM = MainElement Vertical-Length 1UC(A); NM = Main Element atom Number1UC; NT = Targetstruc atom Number1UC; ST = Targetstruc inplaneArea1UC(A2). Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `Flux`: MainElement Flux(A/s)
  - `SM`: MainElement inplaneArea1UC(A2)
  - `cM`: MainElement Vertical-Length 1UC(A)
  - `NM`: Main Element atom Number1UC
  - `NT`: Targetstruc atom Number1UC
  - `ST`: Targetstruc inplaneArea1UC(A2)
  - `cT`: Targetstruc Vertical-Length(A)
- Source: `Miscellaneous_Codes.ipf:1064`

#### `movecur`

```igor
proc movecur()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Source: `Miscellaneous_Codes.ipf:7807`

#### `myall`

```igor
proc myall(mat,totalnum,t)
```
- Usage: run from Igor with parameters `mat`, `totalnum`, `t`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; totalnum = numbers of the Batch; t = add how much. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `totalnum`: numbers of the Batch
  - `t`: add how much
- Source: `Miscellaneous_Codes.ipf:6174`

#### `Newrotateproccont`

```igor
Proc Newrotateproccont()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis. Main internal calls: `Newrotatecont()`.
- Source: `Miscellaneous_Codes.ipf:186`

#### `normrange`

```igor
proc normrange(matname,totalnum,down,up)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `matname`, `totalnum`, `down`, `up`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: matname = name of the Batch waves; totalnum = numbers of the Batch; down = Norm from; up = Norm to. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `normwave()`.
- Parameters:
  - `matname`: name of the Batch waves
  - `totalnum`: numbers of the Batch
  - `down`: Norm from
  - `up`: Norm to
- Source: `Miscellaneous_Codes.ipf:5730`

#### `normrange2`

```igor
proc normrange2(matname,totalnum,down,up,down2,up2)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `matname`, `totalnum`, `down`, `up`, `down2`, `up2`.
- Notes: Interactive Igor procedure for smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: matname = name of the Batch waves; totalnum = numbers of the Batch; down = Norm from; up = Norm to. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `normwave_2()`.
- Parameters:
  - `matname`: name of the Batch waves
  - `totalnum`: numbers of the Batch
  - `down`: Norm from
  - `up`: Norm to
  - `down2`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `up2`: End point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:6431`

#### `plotraw2`

```igor
Proc plotraw2()
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `ImageNameList()`.
- Source: `Miscellaneous_Codes.ipf:9680`

#### `reextractL`

```igor
Proc reextractL()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Miscellaneous_Codes.ipf:7796`

#### `reextractlinecutH`

```igor
Proc reextractlinecutH()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `numslicehnam()`, `linkstsmap_PqpiH()`.
- Source: `Miscellaneous_Codes.ipf:7706`

#### `reextractlinecutV`

```igor
Proc reextractlinecutV()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `numslicevnam()`, `linkstsmap_PqpiV()`.
- Source: `Miscellaneous_Codes.ipf:7746`

#### `reextractM`

```igor
Proc reextractM()
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Miscellaneous_Codes.ipf:7787`

#### `remakeu1u2`

```igor
Proc remakeu1u2()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `bessJ()`.
- Source: `Miscellaneous_Codes.ipf:6409`

#### `renormcuts_k`

```igor
Proc renormcuts_k(matname,which,factor)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `matname`, `which`, `factor`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal. Dialog prompts: matname = Enter the name of the cut; which = You want to renorm the kx or the ky?; factor = Enter the convertion factor (to divide). Code behavior: assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`.
- Parameters:
  - `matname`: Enter the name of the cut
  - `which`: You want to renorm the kx or the ky?
  - `factor`: Enter the convertion factor (to divide)
- Source: `Miscellaneous_Codes.ipf:861`

#### `reorg2ndd`

```igor
proc reorg2ndd(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; ARPES-style loading, plotting, or momentum conversion; Igor wave/matrix/cube data operation. Dialog prompts: name = name of the matrix to be manipulated. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `slicemdc()`, `dsd()`.
- Parameters:
  - `name`: name of the matrix to be manipulated
- Source: `Miscellaneous_Codes.ipf:10531`

#### `resetvarrange`

```igor
Proc resetvarrange()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for geometric correction, rotation, shear, drift, or strain analysis; Igor wave/matrix/cube data operation. Code behavior: reads or updates panel controls.
- Source: `Miscellaneous_Codes.ipf:7577`

#### `rotatemap`

```igor
Proc rotatemap()
```
- Purpose: applies geometric correction or extracts deformation/strain information.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; geometric correction, rotation, shear, drift, or strain analysis. Code behavior: creates output waves; assigns Igor axis scaling/units to the output. Main internal calls: `rotall()`, `setscaleall()`, `mapforSTMff()`, `makespec()`, `reextractM()`.
- Source: `Miscellaneous_Codes.ipf:7594`

#### `savefitdata`

```igor
proc savefitdata()
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; reshapes existing waves; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `str2num()`.
- Source: `Miscellaneous_Codes.ipf:6536`

#### `savefitdata2`

```igor
proc savefitdata2()//single
```
- Purpose: saves, exports, or converts data between files and Igor waves.
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; creates, switches, or removes Igor data folders; cleans up waves/windows or brings an existing window forward. Main internal calls: `str2num()`.
- Source: `Miscellaneous_Codes.ipf:6646`

#### `selectlinenorm`

```igor
proc selectlinenorm(mat,start,endd,num)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: run from Igor with parameters `mat`, `start`, `endd`, `num`.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Dialog prompts: mat = name of the Batch waves; num = numbers of the Batch; start = start point; endd = end point. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: name of the Batch waves
  - `start`: start point
  - `endd`: end point
  - `num`: numbers of the Batch
- Source: `Miscellaneous_Codes.ipf:6239`

#### `slopeall`

```igor
Proc slopeall(mat,number)
```
- Usage: run from Igor with parameters `mat`, `number`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Main internal calls: `slopedata()`.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `number`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Miscellaneous_Codes.ipf:9550`

#### `slopedata`

```igor
proc slopedata(mat)
```
- Usage: run from Igor with parameters `mat`.
- Notes: Interactive Igor procedure for Nanonis/STM data loading or map conversion; graph display, formatting, or window management. Dialog prompts: mat = name of the topograph. Code behavior: runs fitting routines on spectra, curves, or map-derived traces; creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts. Main internal calls: `tpw()`, `ssx()`, `ssy()`.
- Parameters:
  - `mat`: name of the topograph
- Source: `Miscellaneous_Codes.ipf:9565`

#### `stepmove`

```igor
Proc stepmove(ddx,ddy)
```
- Usage: run from Igor with parameters `ddx`, `ddy`.
- Notes: Interactive Igor procedure for lattice, moire, or twist-angle simulation/analysis. Dialog prompts: ddx = X vector to Jump (nm), the length should with a '+/-' sign; ddy = Y vector to Jump (nm), the length should with a '+/-' sign. Code behavior: asks the user for parameters through Igor prompts.
- Parameters:
  - `ddx`: X vector to Jump (nm), the length should with a '+/-' sign
  - `ddy`: Y vector to Jump (nm), the length should with a '+/-' sign
- Source: `Miscellaneous_Codes.ipf:12631`

#### `subsc`

```igor
proc subsc(name,num)
```
- Usage: run from Igor with parameters `name`, `num`.
- Notes: Interactive Igor procedure for symmetry or reflection processing; graph display, formatting, or window management. Code behavior: formats graph axes, labels, colors, or annotations. Main internal calls: `duplicateall()`, `displaymulti()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `num`: Number of spectra, waves, points, bins, or iterations to process.
- Source: `Miscellaneous_Codes.ipf:9251`

#### `summul`

```igor
proc summul(matt,from,to)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `matt`, `from`, `to`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: matt = Name of the Batch; from = Start index for sum; to = End index for sum. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays; asks the user for parameters through Igor prompts.
- Parameters:
  - `matt`: Name of the Batch
  - `from`: Start index for sum
  - `to`: End index for sum
- Source: `Miscellaneous_Codes.ipf:6308`

#### `summul2`

```igor
proc summul2(matt,from,to,name)
```
- Purpose: combines many waves/slices into a map, linecut, or averaged output.
- Usage: run from Igor with parameters `matt`, `from`, `to`, `name`.
- Notes: Interactive Igor procedure for graph display, formatting, or window management. Dialog prompts: matt = Name of the Batch; from = Start index for sum; to = End index for sum. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `matt`: Name of the Batch
  - `from`: Start index for sum
  - `to`: End index for sum
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:8198`

#### `tdtodc`

```igor
Proc tdtodc(name)
```
- Usage: run from Igor with parameters `name`.
- Notes: Interactive Igor procedure for Igor wave/matrix/cube data operation. Dialog prompts: name = Name of the 2D wave, Now convert it to 1D. Code behavior: asks the user for parameters through Igor prompts. Main internal calls: `tdtod()`.
- Parameters:
  - `name`: Name of the 2D wave, Now convert it to 1D
- Source: `Miscellaneous_Codes.ipf:1829`

#### `tempload`

```igor
proc tempload()
```
- Usage: run interactively from Igor or from a panel callback.
- Notes: Interactive Igor procedure for matrix resampling, scaling, or reshaping; graph display, formatting, or window management. Code behavior: creates, switches, or removes Igor data folders. Main internal calls: `Makegraphtable()`, `LoadDataFromNewmk()`, `Initialize_Global_Variables()`, `rescalexmultiwhat()`, `displaymulti()`.
- Source: `Miscellaneous_Codes.ipf:9427`

#### `twosetdifference`

```igor
proc twosetdifference(mat,num,start)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: run from Igor with parameters `mat`, `num`, `start`.
- Notes: Interactive Igor procedure in `Miscellaneous_Codes.ipf`. Dialog prompts: mat = Batch name; num = Number of One set; start = Number of 1st Spectrum. Code behavior: duplicates or stages waves for downstream processing; asks the user for parameters through Igor prompts.
- Parameters:
  - `mat`: Batch name
  - `num`: Number of One set
  - `start`: Number of 1st Spectrum
- Source: `Miscellaneous_Codes.ipf:8967`


### Computational and data helpers

#### `appendVlineF`

```igor
Function appendVlineF(yvalue,yincre,xrange,numbers,flag)
```
- Purpose: displays waves, images, contours, or graph overlays.
- Usage: call as `appendVlineF(yvalue, yincre, xrange, numbers, flag)`.
- Notes: Function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `lstyle()`, `rgb()`, `lsize()`.
- Parameters:
  - `yvalue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `yincre`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xrange`: Axis limit or numeric range boundary used for cropping, fitting, or display.
  - `numbers`: Number of spectra, waves, points, bins, or iterations to process.
  - `flag`: Mode flag controlling which branch of the workflow is used.
- Source: `Miscellaneous_Codes.ipf:10201`

#### `Areaselect_d`

```igor
function Areaselect_d(upcurve,downcurve)
```
- Usage: call as `Areaselect_d(upcurve, downcurve)`.
- Notes: .. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Parameters:
  - `upcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `downcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:2796`

#### `Areaselect_p`

```igor
function Areaselect_p(upcurve,downcurve)
```
- Usage: call as `Areaselect_p(upcurve, downcurve)`.
- Notes: .. Code behavior: creates output waves; duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Parameters:
  - `upcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `downcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:2683`

#### `autoromvejump2D`

```igor
Function autoromvejump2D(name,jumpheight,killim,add1)
```
- Usage: call as `autoromvejump2D(name, jumpheight, killim, add1)`.
- Notes: Function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: performs Fourier-transform or inverse-transform operations; creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; extracts image/wave statistics. Main internal calls: `sign()`, `findjumpslice()`, `appendattractedmin_max()`, `sftvfollowjump()`, `reorganizedata()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it is used for Fourier/QPI or phase-map processing.
  - `jumpheight`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `killim`: kill (1) or not kill (0) intermedia waves, that is the horizental waves from fit
  - `add1`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:11149`

#### `calculates`

```igor
Function calculates(Pvalue,xshouldbe,name)
```
- Usage: call as `calculates(Pvalue, xshouldbe, name)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Main internal calls: `scalewave()`. Update log: 2025-05-19: Calculates the divider-correction factor needed to align a measured feature from `Pvalue` to the target value `xshouldbe`.
- Parameters:
  - `Pvalue`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xshouldbe`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:12869`

#### `checkfirstNonnan`

```igor
Function checkfirstNonnan(name)
```
- Usage: call as `checkfirstNonnan(name)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:11301`

#### `checkupdown`

```igor
Function checkupdown(N,large)
```
- Usage: call as `checkupdown(N, large)`.
- Notes: Function for matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; formats graph axes, labels, colors, or annotations. Main internal calls: `Medianfinder()`.
- Parameters:
  - `N`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `large`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5910`

#### `checkupdown2`

```igor
function checkupdown2(N,large)
```
- Usage: call as `checkupdown2(N, large)`.
- Notes: Function for Igor wave/matrix/cube data operation. Code behavior: creates output waves. Main internal calls: `Medianfinder()`.
- Parameters:
  - `N`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `large`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:5839`

#### `cuthalfall`

```igor
Function cuthalfall(name,flag,select,number2)//testing finished
```
- Usage: call as `cuthalfall(name, flag, select, number2)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `flag`: half k (raw)=2 half E (colum) =3
  - `select`: choose which data to deal with, if==0 means all
  - `number2`: number of cuts
- Source: `Miscellaneous_Codes.ipf:1964`

#### `ddd_f`

```igor
Function ddd_f(name)
```
- Usage: call as `ddd_f(name)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Main internal calls: `vcsr()`, `pcsr()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
- Source: `Miscellaneous_Codes.ipf:10276`

#### `DPdata`

```igor
Function DPdata(name,select,flag,stp,edp,numbercut)//testing finished
```
- Usage: call as `DPdata(name, select, flag, stp, edp, numbercut)`.
- Notes: Function for linecut, slice, or region extraction; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `select`: choose which data to deal with, if==0 means all
  - `flag`: delete k demision(raw)=0 delete E demision(colum) =1
  - `stp`: Start point, index, or coordinate for the selected range.
  - `edp`: End point, index, or coordinate for the selected range.
  - `numbercut`: number of cuts
- Source: `Miscellaneous_Codes.ipf:1886`

#### `fixmapline`

```igor
Function fixmapline(name)
```
- Usage: call as `fixmapline(name)`.
- Notes: Function for Igor wave/matrix/cube data operation. Main internal calls: `qcsr()`, `pcsr()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:6937`

#### `func_mat2d_1orNaN_2d`

```igor
Function func_mat2d_1orNaN_2d(matname)
```
- Usage: call as `func_mat2d_1orNaN_2d(matname)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `matname`: Name or reference of the source wave/matrix being processed.
- Source: `Miscellaneous_Codes.ipf:1488`

#### `getpolaraveRcurve`

```igor
Function getpolaraveRcurve(name,OP,OQ)
```
- Purpose: extracts values, metadata, cursor information, or derived waves.
- Usage: call as `getpolaraveRcurve(name, OP, OQ)`.
- Notes: Utility function for matrix resampling, scaling, or reshaping; graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output; opens or updates graph/image displays; extracts image/wave statistics; cleans up waves/windows or brings an existing window forward. Main internal calls: `Lineprofilefromcircle2()`, `mean()`, `ckfig()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `OP`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `OQ`: Momentum or Q-vector coordinate/index.
- Source: `Miscellaneous_Codes.ipf:3367`

#### `ID_d`

```igor
function ID_d(mat,upcurve,downcurve)
```
- Usage: call as `ID_d(mat, upcurve, downcurve)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `upcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `downcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:2757`

#### `ID_p`

```igor
Function ID_p(mat,upcurve,downcurve)
```
- Notes: Selective area sum Usage: call as `ID_p(mat, upcurve, downcurve)`. Code behavior: duplicates or stages waves for downstream processing; opens or updates graph/image displays.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `upcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `downcurve`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:2644`

#### `KP_EnsurePhysicalConstants`

```igor
Function KP_EnsurePhysicalConstants()
```
- Usage: call `KP_EnsurePhysicalConstants()` from Igor procedure code or the command line.
- Notes: Function in `KP_GlobalState.ipf`.
- Source: `KP_GlobalState.ipf:27`

#### `KP_EnsureRootGlobalString`

```igor
Function KP_EnsureRootGlobalString(name, defaultValue)
```
- Usage: call as `KP_EnsureRootGlobalString(name, defaultValue)`.
- Notes: Function in `KP_GlobalState.ipf`. Main internal calls: `SVAR_Exists()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `defaultValue`: Step size or energy/voltage increment used by the calculation.
- Source: `KP_GlobalState.ipf:16`

#### `KP_EnsureRootGlobalVariable`

```igor
Function KP_EnsureRootGlobalVariable(name, defaultValue)
```
- Usage: call as `KP_EnsureRootGlobalVariable(name, defaultValue)`.
- Notes: Function in `KP_GlobalState.ipf`. Main internal calls: `NVAR_Exists()`.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `defaultValue`: Step size or energy/voltage increment used by the calculation.
- Source: `KP_GlobalState.ipf:5`

#### `KP_EnsureStartupGlobals`

```igor
Function KP_EnsureStartupGlobals()
```
- Usage: call `KP_EnsureStartupGlobals()` from Igor procedure code or the command line.
- Notes: Function in `KP_GlobalState.ipf`. Main internal calls: `KP_EnsurePhysicalConstants()`, `KP_EnsureTemplateRootGlobals()`.
- Source: `KP_GlobalState.ipf:74`

#### `LiangFu`

```igor
Function LiangFu(w,x) : FitFunc
```
- Usage: call as `LiangFu(w, x)`.
- Notes: Function for Igor wave/matrix/cube data operation. Main internal calls: `bessJ()`.
- Parameters:
  - `w`: Igor wave reference used as input or output.
  - `x`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Procedure.ipf:261`

#### `linenorm2pf`

```igor
Function linenorm2pf(mat,p1,p2)
```
- Purpose: smooths, normalizes, or removes background/trend components.
- Usage: call as `linenorm2pf(mat, p1, p2)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `mat`: Name or reference of the source wave/matrix being processed.
  - `p1`: Start point, index, or coordinate for the selected range.
  - `p2`: End point, index, or coordinate for the selected range.
- Source: `Miscellaneous_Codes.ipf:11009`

#### `Lineprofilefromcircle2`

```igor
Function Lineprofilefromcircle2(name,OP,OQ,RP)
```
- Usage: call as `Lineprofilefromcircle2(name, OP, OQ, RP)`.
- Notes: Function for matrix resampling, scaling, or reshaping; smoothing, normalization, or background removal; Igor wave/matrix/cube data operation. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Parameters:
  - `name`: Name or reference of the source wave/matrix being processed.
  - `OP`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `OQ`: Momentum or Q-vector coordinate/index.
  - `RP`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:3395`

#### `makespec`

```igor
Function makespec()
```
- Purpose: creates new waves, maps, figures, or simulation data.
- Usage: call `makespec()` from Igor procedure code or the command line.
- Notes: Utility function for FFT, QPI, phase, or lock-in analysis; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Code behavior: creates output waves; assigns Igor axis scaling/units to the output.
- Source: `Miscellaneous_Codes.ipf:7914`

#### `mapforSTMff`

```igor
Function mapforSTMff(num,name,suff,start,delta)
```
- Usage: call as `mapforSTMff(num, name, suff, start, delta)`.
- Notes: Function for linecut, slice, or region extraction; matrix resampling, scaling, or reshaping; Igor wave/matrix/cube data operation. Code behavior: creates output waves; duplicates or stages waves for downstream processing; assigns Igor axis scaling/units to the output.
- Parameters:
  - `num`: number of cuts.
  - `name`: Name or reference of the source wave/matrix being processed.
  - `suff`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `start`: Start point, index, or coordinate for the selected range.
  - `delta`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:9868`

#### `scsc`

```igor
Function scsc(pw,inx):FitFunc
```
- Usage: call as `scsc(pw, inx)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; Igor wave/matrix/cube data operation. Main internal calls: `integrate1d()`.
- Parameters:
  - `pw`: Igor wave reference used as input or output.
  - `inx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:12285`

#### `scsc2`

```igor
Function scsc2(pw,yw,xw):FitFunc
```
- Usage: call as `scsc2(pw, yw, xw)`.
- Notes: For integrated and then differentiated Fit. Code behavior: differentiates wave data. Main internal calls: `integrate1d()`.
- Parameters:
  - `pw`: Igor wave reference used as input or output.
  - `yw`: Igor wave reference used as input or output.
  - `xw`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:12208`

#### `scsc2S`

```igor
Function scsc2S(pw,yw,xw):FitFunc
```
- Usage: call as `scsc2S(pw, yw, xw)`.
- Notes: For integrated and then differentiated Fit. Code behavior: differentiates wave data. Main internal calls: `integrate1d()`.
- Parameters:
  - `pw`: Igor wave reference used as input or output.
  - `yw`: Igor wave reference used as input or output.
  - `xw`: Igor wave reference used as input or output.
- Source: `Miscellaneous_Codes.ipf:12398`

#### `scscS`

```igor
Function scscS(pw,inx):FitFunc
```
- Usage: call as `scscS(pw, inx)`.
- Notes: Function for Nanonis/STM data loading or map conversion; linecut, slice, or region extraction; matrix resampling, scaling, or reshaping. Main internal calls: `integrate1d()`.
- Parameters:
  - `pw`: Igor wave reference used as input or output. In this context it usually refers to STM/STS or Nanonis-derived data.
  - `inx`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:12482`

#### `ssx`

```igor
Function ssx(namedata,ref,ypoint)
```
- Usage: call as `ssx(namedata, ref, ypoint)`.
- Notes: Function for Igor wave/matrix/cube data operation.
- Parameters:
  - `namedata`: Name or reference of the source wave/matrix being processed.
  - `ref`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `ypoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:9600`

#### `ssy`

```igor
Function ssy(namedata,ref,xpoint)
```
- Usage: call as `ssy(namedata, ref, xpoint)`.
- Notes: Function for graph display, formatting, or window management; Igor wave/matrix/cube data operation.
- Parameters:
  - `namedata`: Name or reference of the source wave/matrix being processed.
  - `ref`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
  - `xpoint`: Numeric parameter controlling the calculation branch, index, range, scale, or model value.
- Source: `Miscellaneous_Codes.ipf:9615`

#### `UserIntergrand`

```igor
Function UserIntergrand(dE)//
```
- Notes: only for graph Usage: call as `UserIntergrand(dE)`.
- Parameters:
  - `dE`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:12250`

#### `UserIntergrand2`

```igor
Function UserIntergrand2(dE)//
```
- Usage: call as `UserIntergrand2(dE)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management.
- Parameters:
  - `dE`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:12226`

#### `UserIntergrand2S`

```igor
Function UserIntergrand2S(dE)//
```
- Usage: call as `UserIntergrand2S(dE)`.
- Notes: Function for spectroscopy, superconducting-gap, or vortex-model analysis; graph display, formatting, or window management.
- Parameters:
  - `dE`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:12417`

#### `UserIntergrandS`

```igor
Function UserIntergrandS(dE)//
```
- Notes: only for graph Usage: call as `UserIntergrandS(dE)`.
- Parameters:
  - `dE`: Step size or energy/voltage increment used by the calculation.
- Source: `Miscellaneous_Codes.ipf:12445`


